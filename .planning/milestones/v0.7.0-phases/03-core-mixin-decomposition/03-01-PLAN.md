---
phase: 3
plan: 1
title: "Extract 4 focused mixins from core.py"
wave: 1
depends_on: []
files_modified:
  - src/cacheness/_verification_mixin.py
  - src/cacheness/_stats_mixin.py
  - src/cacheness/_custom_metadata_mixin.py
  - src/cacheness/_storage_mode_mixin.py
  - src/cacheness/core.py
autonomous: true
requirements: [DECO-03, DECO-04]
---

# Plan: Extract 4 Focused Mixins from core.py

## Objective

Decompose `core.py` (3,307 lines) into 4 focused mixin classes — VerificationMixin, StatsMixin, CustomMetadataMixin, StorageModeMixin — as flat sibling files in `src/cacheness/`. Core.py retains `__init__`, put/get orchestration, query/file/batch/convenience ops, and module-level helpers. All existing import paths and tests pass unchanged.

## Tasks

<task id="1">
<title>Create _verification_mixin.py</title>
<read_first>
- src/cacheness/core.py (lines 1208-1280 for _extract_signable_fields, 1262-1275 for _calculate_file_hash, 1293-1412 for _verify_entry, 1620-1648 for _sign_entry_if_enabled, 3555-3580 for verify_integrity)
- src/cacheness/interfaces.py (SignableFields, IntegrityReport, HandlerResult)
- src/cacheness/security.py (EntrySigner)
</read_first>
<action>
Create `src/cacheness/_verification_mixin.py` with class `VerificationMixin` containing these methods extracted from core.py:

1. `_extract_signable_fields(self, *, cache_key, entry_data, metadata)` — builds SignableFields dict for signing/verification
2. `_calculate_file_hash(self, file_path)` — xxhash file hashing
3. `_verify_entry(self, cache_key, entry, metadata, file_path, *, storage_mode=False)` — integrity hash + signature verification
4. `_sign_entry_if_enabled(self, cache_key, entry_data, metadata_dict)` — sign entry during put
5. `verify_integrity(self)` — public integrity check API

Use relative imports matching core.py style:
- `from .interfaces import SignableFields, IntegrityReport`
- Other imports as needed by the extracted methods (logging, Path, Optional, Dict, Any, etc.)

Each method body is copied exactly from core.py — no logic changes.
</action>
<acceptance_criteria>
- File `src/cacheness/_verification_mixin.py` exists
- File contains `class VerificationMixin:`
- Class contains methods: `_extract_signable_fields`, `_calculate_file_hash`, `_verify_entry`, `_sign_entry_if_enabled`, `verify_integrity`
- No `__init__` method in VerificationMixin
- Uses relative imports (`from .interfaces import`)
- File starts with underscore (private module)
</acceptance_criteria>
</task>

<task id="2">
<title>Create _stats_mixin.py</title>
<read_first>
- src/cacheness/core.py (lines 1283-1300 for _record_hit/_record_miss, 3726-3743 for get_stats)
- src/cacheness/config.py (CacheConfig)
</read_first>
<action>
Create `src/cacheness/_stats_mixin.py` with class `StatsMixin` containing these methods extracted from core.py:

1. `_record_hit(self)` — increment hit counter if stats enabled
2. `_record_miss(self)` — increment miss counter if stats enabled
3. `get_stats(self)` — return comprehensive stats dict

Use relative imports as needed. Methods access self.config, self.metadata_backend, self.cache_dir, self.actual_backend via cooperative inheritance.
</action>
<acceptance_criteria>
- File `src/cacheness/_stats_mixin.py` exists
- File contains `class StatsMixin:`
- Class contains methods: `_record_hit`, `_record_miss`, `get_stats`
- No `__init__` method in StatsMixin
- Uses relative imports
</acceptance_criteria>
</task>

<task id="3">
<title>Create _custom_metadata_mixin.py</title>
<read_first>
- src/cacheness/core.py (lines 295-340 for _supports_custom_metadata/_normalize_custom_metadata, 336-351 for _init_custom_metadata_support note — stays in core, 441-600 for _store/_get_custom_metadata, 592-740 for query methods, 1015-1040 for get_custom_metadata_for_entry)
- src/cacheness/custom_metadata.py (get_custom_metadata_model, get_namespace_custom_model, etc.)
</read_first>
<action>
Create `src/cacheness/_custom_metadata_mixin.py` with class `CustomMetadataMixin` containing these methods extracted from core.py:

1. `_supports_custom_metadata(self)` — check backend support
2. `_normalize_custom_metadata(self, custom_metadata)` — normalize input to iterable
3. `_store_custom_metadata(self, cache_key, custom_metadata)` — write custom metadata via SQLAlchemy
4. `_get_custom_metadata(self, cache_key)` — read custom metadata for cache key
5. `_get_registered_schemas(self)` — list registered schema types
6. `query_custom(self, schema_name, filters=None)` — public query with filters
7. `query_custom_session(self, schema_name)` — context manager for advanced queries
8. `query_custom_metadata(self, ...)` — query with model class
9. `get_custom_metadata_for_entry(self, cache_key)` — get metadata for specific entry

Use relative imports for custom_metadata module, metadata module, logging, typing, etc. Keep lazy imports (inside method bodies) that exist in core.py as-is.
</action>
<acceptance_criteria>
- File `src/cacheness/_custom_metadata_mixin.py` exists
- File contains `class CustomMetadataMixin:`
- Class contains methods: `_supports_custom_metadata`, `_normalize_custom_metadata`, `_store_custom_metadata`, `_get_custom_metadata`, `_get_registered_schemas`, `query_custom`, `query_custom_session`, `query_custom_metadata`, `get_custom_metadata_for_entry`
- No `__init__` method in CustomMetadataMixin
- Uses relative imports
</acceptance_criteria>
</task>

<task id="4">
<title>Create _storage_mode_mixin.py</title>
<read_first>
- src/cacheness/core.py (lines 1680-1870 for _storage_mode_put/_storage_mode_get/_storage_mode_get_with_metadata)
</read_first>
<action>
Create `src/cacheness/_storage_mode_mixin.py` with class `StorageModeMixin` containing these methods extracted from core.py:

1. `_storage_mode_put(self, data, cache_key, description)` — put without eviction/stats
2. `_storage_mode_get(self, cache_key)` — get without TTL/stats/auto-delete
3. `_storage_mode_get_with_metadata(self, cache_key)` — get with metadata, no TTL

Use relative imports. Methods access self._blob_store, self.metadata_backend, self._get_cache_file_path, self._try_inline_blob, self._try_direct_inline, self._build_metadata_dict, self._sign_entry_if_enabled (from VerificationMixin), self._read_inline_blob, self._cleanup_stale_blob, self._resolve_actual_path, self._verify_entry (from VerificationMixin) via cooperative inheritance.
</action>
<acceptance_criteria>
- File `src/cacheness/_storage_mode_mixin.py` exists
- File contains `class StorageModeMixin:`
- Class contains methods: `_storage_mode_put`, `_storage_mode_get`, `_storage_mode_get_with_metadata`
- No `__init__` method in StorageModeMixin
- Uses relative imports
</acceptance_criteria>
</task>

<task id="5">
<title>Update core.py — import mixins and remove extracted methods</title>
<read_first>
- src/cacheness/core.py (full file — understand all imports and class declaration)
- src/cacheness/_verification_mixin.py (just created)
- src/cacheness/_stats_mixin.py (just created)
- src/cacheness/_custom_metadata_mixin.py (just created)
- src/cacheness/_storage_mode_mixin.py (just created)
</read_first>
<action>
1. Add imports at top of core.py (after existing imports):
   ```python
   from ._verification_mixin import VerificationMixin
   from ._stats_mixin import StatsMixin
   from ._custom_metadata_mixin import CustomMetadataMixin
   from ._storage_mode_mixin import StorageModeMixin
   ```

2. Change class declaration from:
   ```python
   class UnifiedCache:
   ```
   to:
   ```python
   class UnifiedCache(VerificationMixin, StatsMixin, CustomMetadataMixin, StorageModeMixin):
   ```

3. Remove the following methods from core.py (they now live in mixin files):
   - `_extract_signable_fields`
   - `_calculate_file_hash`
   - `_verify_entry`
   - `_sign_entry_if_enabled`
   - `verify_integrity`
   - `_record_hit`
   - `_record_miss`
   - `get_stats`
   - `_supports_custom_metadata`
   - `_normalize_custom_metadata`
   - `_store_custom_metadata`
   - `_get_custom_metadata`
   - `_get_registered_schemas`
   - `query_custom`
   - `query_custom_session`
   - `query_custom_metadata`
   - `get_custom_metadata_for_entry`
   - `_storage_mode_put`
   - `_storage_mode_get`
   - `_storage_mode_get_with_metadata`

4. Keep ALL other methods, __init__, module-level helpers (_PutCleanup, _normalize_function_args), and all imports they need.

5. Remove any imports from core.py that are ONLY used by extracted methods and are now imported in the mixin files instead. Keep imports that are still used by remaining methods.
</action>
<acceptance_criteria>
- core.py contains `from ._verification_mixin import VerificationMixin`
- core.py contains `from ._stats_mixin import StatsMixin`
- core.py contains `from ._custom_metadata_mixin import CustomMetadataMixin`
- core.py contains `from ._storage_mode_mixin import StorageModeMixin`
- core.py contains `class UnifiedCache(VerificationMixin, StatsMixin, CustomMetadataMixin, StorageModeMixin):`
- core.py does NOT contain `def _verify_entry(`
- core.py does NOT contain `def _record_hit(`
- core.py does NOT contain `def _store_custom_metadata(`
- core.py does NOT contain `def _storage_mode_put(`
- core.py DOES contain `def put(`
- core.py DOES contain `def get(`
- core.py DOES contain `def __init__(`
- `from cacheness.core import UnifiedCache` works
- All 1,427+ existing tests pass without modification
</acceptance_criteria>
</task>

## Verification

### must_haves
1. `from cacheness.core import UnifiedCache` works unchanged
2. UnifiedCache inherits from VerificationMixin, StatsMixin, CustomMetadataMixin, StorageModeMixin
3. No mixin defines `__init__`
4. All existing import paths work (DECO-04)
5. All 1,427+ existing tests pass without modification

### Verification Commands
```bash
# Quick smoke test
uv run python -c "from cacheness.core import UnifiedCache; print('Import OK')"

# Full test suite
uv run pytest tests/ -x -q --ignore=tests/test_tensorflow_handler.py
```
