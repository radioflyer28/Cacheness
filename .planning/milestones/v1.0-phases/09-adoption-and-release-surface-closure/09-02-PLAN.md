---
phase: 09-adoption-and-release-surface-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tools/run_phase8_packaging.py
  - tests/packaging/test_wheel_matrix.py
  - tests/test_stored_compatibility.py
  - tests/test_public_api_contract.py
  - tests/test_handlers.py
  - pyproject.toml
  - AGENTS.md
  - .codex/skills/spike-findings-cacheness/SKILL.md
  - .codex/skills/spike-findings-cacheness/references/handler-integration.md
autonomous: true
requirements:
  - CACH-06
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A source-free minimal wheel imports quietly when optional dataframe libraries are absent, while requesting a dataframe capability still produces focused capability feedback (D-09)."
    - "The literal wheel export contract contains FormatHandler and excludes the retired protocol/error names (D-15)."
    - "A durable filesystem-plus-SQLite entry reopens with byte-for-byte identical handler_type, payload_format, payload_format_version, and version dimensions, without migration or rebuild (D-16)."
    - "Package metadata describes a BlobStore-first storage and caching library without claiming publication, frozen v1.0 API, or remote qualification (D-05, D-07, D-08)."
    - "Current agent and spike guidance promotes the format-handler noun and preserves private path-based staging, one lifecycle authority, and request-bound optional integrations."
  artifacts:
    - path: tools/run_phase8_packaging.py
      provides: "Source-free wheel probes for FormatHandler export, retired-name absence, and silent base import"
    - path: tests/packaging/test_wheel_matrix.py
      provides: "Regression contract for minimal and optional wheel behavior"
    - path: pyproject.toml
      provides: "BlobStore-first package description with unchanged capability extras"
    - path: tests/test_stored_compatibility.py
      provides: "Durable reopen proof that persisted handler and payload identities did not change"
    - path: .codex/skills/spike-findings-cacheness/SKILL.md
      provides: "Current auto-loaded format-handler and lifecycle-authority guidance"
  key_links:
    - from: tests/packaging/test_wheel_matrix.py
      to: tools/run_phase8_packaging.py
      via: "isolated built-wheel execution outside the checkout"
      pattern: "run_phase8_packaging|run_base_probe"
    - from: tools/run_phase8_packaging.py
      to: src/cacheness/storage/__init__.py
      via: "literal BASE_PUBLIC_EXPORTS inventory"
      pattern: "FormatHandler"
    - from: tests/test_stored_compatibility.py
      to: src/cacheness/storage/manifest.py
      via: "filesystem-plus-SQLite close/reopen compares stable persisted identity and version fields"
      pattern: "handler_type|payload_format|payload_format_version"
    - from: .codex/skills/spike-findings-cacheness/SKILL.md
      to: docs/adr/0001-topology-specific-storage-guarantees.md
      via: "path-based handlers remain private participants below BlobStore authority"
      pattern: "FormatHandler|authority"
---

<objective>
Make the installable package and active agent guidance publish the same alias-free, BlobStore-first surface as the live code.

Purpose: Close the package/import portion of CACH-06 and remove unsolicited optional-dataframe noise without redesigning extras or storage.
Output: Quiet minimal-wheel import, exact public export probes, durable identity/reopen proof, current package identity, and synchronized project/skill guidance.
</objective>

<scope_guard>
No dependency or lockfile change is allowed. Keep every existing optional group and its behavior; Phase 10 owns any dependency removal caused by SqlCache deletion. Do not add Narwhals, broaden top-level exports, run remote services, change handler I/O, or strengthen qualification claims.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-adoption-and-release-surface-closure/09-CONTEXT.md
@.planning/phases/09-adoption-and-release-surface-closure/09-RESEARCH.md
@.planning/phases/09-adoption-and-release-surface-closure/09-PATTERNS.md
@.planning/phases/09-adoption-and-release-surface-closure/09-01-SUMMARY.md
@tools/run_phase8_packaging.py
@tests/packaging/test_wheel_matrix.py
@tests/test_stored_compatibility.py
@tests/test_public_api_contract.py
@pyproject.toml
@.codex/skills/spike-findings-cacheness/SKILL.md
</context>

<interfaces>
- `tools/run_phase8_packaging.py::BASE_PUBLIC_EXPORTS` remains a literal reviewed inventory rather than a runtime-derived list.
- Existing source-free `build_wheel(...)` and `run_base_probe(...)` isolation remain the packaging authority; extend them rather than creating another runner.
- Existing optional groups remain `recommended`, `dataframes`, `tensorflow`, `s3`, `postgresql`, and `cloud` until separately scoped work changes them.
- The generic protocol remains public from `cacheness.storage`, not newly added to the top-level `cacheness` barrel.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the alias-free wheel surface and identity-preserving durable reopen</name>
  <files>tests/packaging/test_wheel_matrix.py, tools/run_phase8_packaging.py, tests/test_stored_compatibility.py, tests/test_public_api_contract.py, pyproject.toml</files>
  <behavior>
    - "A wheel installed outside the source checkout imports `cacheness` and `cacheness.storage` with no package-generated stdout/stderr when dataframe extras are absent."
    - "The wheel exposes FormatHandler through the storage barrel and exposes neither retired handler name."
    - "Generic-object, NumPy, and existing UnifiedCache base probes still pass; the independent dataframe extra still performs its Parquet round trip."
    - "A filesystem-plus-SQLite entry closes/reopens with the same value, handler_type, payload_format, payload_format_version, and store/manifest version dimensions, without maintenance."
    - "The package description is BlobStore-first and does not state that an immutable release or remote topology is qualified."
  </behavior>
  <action>Write the wheel-matrix, public-contract, and durable-reopen assertions first. Update the literal storage export inventory and isolated base probe to require `FormatHandler`, reject both retired public names, and fail on package-generated import output; Plan 09-01 already removed import-time availability logging atomically with the source cutover, so do not reopen `handlers.py`. Add the D-16 regression using the established local-topology close/reopen pattern: capture persisted identity/version fields before close, reopen, prove the value and identities are unchanged, and assert no migration/rebuild/manifest rewrite was required. Update only the project description in `pyproject.toml` to the BlobStore-first product identity. Preserve dependencies, extras, version, Python floor, lockfile, built-in formats, and the independent dataframe Parquet probe. Do not derive the export inventory from `__all__` or add another packaging script.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/packaging/test_wheel_matrix.py tests/test_stored_compatibility.py tests/test_public_api_contract.py tests/test_guarded_handler_io.py -x</automated>
  </verify>
  <done>A freshly built source-free wheel exposes only the generic names, imports silently, preserves optional probes, and a durable store proves that the Python rename changed no persisted identity or version.</done>
</task>

<task type="auto">
  <name>Task 2: Promote format-handler identity in current project and spike guidance</name>
  <files>AGENTS.md, .codex/skills/spike-findings-cacheness/SKILL.md, .codex/skills/spike-findings-cacheness/references/handler-integration.md, tests/test_handlers.py</files>
  <action>Update current live guidance and remaining handler-test descriptions to use `FormatHandler`/`FormatHandlerError`, with `store.handlers.register_handler(...)` unchanged. Record the primary noun as format handler and promote it without a parallel abstraction. Describe integrations as optional/request-bound capabilities. Preserve private staging/snapshot, immutable publication, suffix/locator constraints, and BlobStore authority. Do not edit preserved spike experiments or historical planning evidence merely to erase old wording.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_handlers.py -x &amp;&amp; uv run python -c "from pathlib import Path; paths=[Path('AGENTS.md'),Path('.codex/skills/spike-findings-cacheness/SKILL.md'),Path('.codex/skills/spike-findings-cacheness/references/handler-integration.md'),Path('tests/test_handlers.py')]; text='\n'.join(p.read_text() for p in paths); assert 'FormatHandler' in text; assert 'store.handlers.register_handler' in text or 'register_handler' in text; assert 'CacheHandler' not in text"</automated>
  </verify>
  <done>Current agent-facing guidance uses one generic format protocol, keeps optional integrations capability-scoped, and continues to enforce private handler I/O beneath the single BlobStore authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source checkout -> built wheel | Public exports and import behavior must be proved from packaged bytes, not inherited source paths. |
| base install -> optional capability request | Missing optional dataframe libraries must remain silent until the user requests that capability. |
| agent guidance -> future storage changes | Current guidance must preserve private staging and one lifecycle authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-05 | Spoofing | source-free public export probe | high | mitigate | Install the freshly built wheel outside the checkout, use a literal export inventory, and reject inherited source paths and retired aliases. |
| T-09-06 | Information Disclosure | minimal import output | low | mitigate | Assert empty package-generated output and defer missing-capability feedback to the explicit request boundary. |
| T-09-07 | Tampering | optional dependency inventory | medium | mitigate | Keep extras and lockfile unchanged and rerun the existing independent optional-group matrix. |
| T-09-08 | Elevation of Privilege | handler guidance | high | mitigate | Preserve guarded private paths, safe suffixes, and BlobStore authority; update terminology without adding an alternate I/O or lifecycle seam. |
</threat_model>

<verification>
- The source-free wheel matrix passes for the base package and existing optional groups.
- Durable reopen and public API contracts prove D-16 identities and alias absence.
- The base import produces no package-generated optional-dataframe warning.
- `uv.lock` is unchanged and Narwhals is not installed.
- Current AGENTS/skill guidance contains the promoted format-handler decision and no old public protocol term.
</verification>

<acceptance_criteria>
- D-05/D-07 package identity matches the local-ready BlobStore-first development posture.
- D-09 optional integrations are quiet and task-bound rather than import-time announcements.
- D-15 reaches built-wheel exports and current agent guidance without an alias.
- No Phase-10-owned dependency or SqlCache implementation surface is removed.
</acceptance_criteria>

<source_coverage>
- GOAL: Package presentation and active guidance match the completed architecture.
- REQ: CACH-06 coherent public import and optional-export behavior.
- RESEARCH: Source-free wheel runner reuse, literal export inventory, import-time warning removal, package description refresh, and active skill guidance.
- CONTEXT: D-05, D-07, D-08, D-09, D-15, D-17.
</source_coverage>

<success_criteria>
- A user can install the local wheel and import the base package quietly.
- The package advertises the storage foundation and cache-policy layer without a publication or remote-support overclaim.
- Agent guidance cannot steer later work back toward a cache-only handler abstraction or second lifecycle authority.
</success_criteria>

<output>
Create `.planning/phases/09-adoption-and-release-surface-closure/09-02-SUMMARY.md` when done.
</output>
