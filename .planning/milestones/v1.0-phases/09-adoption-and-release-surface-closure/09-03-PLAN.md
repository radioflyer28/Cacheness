---
phase: 09-adoption-and-release-surface-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - tests/test_phase9_examples.py
  - examples/memory_blob_store.py
  - examples/durable_catalog_store.py
  - examples/unified_cache.py
  - examples/custom_mcap_format.py
autonomous: true
requirements:
  - CACH-06
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Exactly four named canonical journeys are implemented as the published files: memory BlobStore, durable filesystem-plus-SQLite/catalog, UnifiedCache/decorator, and custom MCAP-style FormatHandler (D-10)."
    - "The exact files run unchanged twice in fresh child processes with sockets disabled, deterministic success markers, assertions, and no residual storage (D-12, D-13)."
    - "The custom example uses FormatHandler, stable data/payload/version identity, `.mcap`, contained path I/O, and store.handlers.register_handler(..., priority=0) (D-14, D-17)."
  artifacts:
    - path: tests/test_phase9_examples.py
      provides: "Literal exact-file, repeatability, network-blocking, marker, and residue harness"
    - path: examples/memory_blob_store.py
      provides: "Disposable memory/memory direct storage journey"
    - path: examples/durable_catalog_store.py
      provides: "Filesystem-plus-SQLite catalog and reopen journey"
    - path: examples/unified_cache.py
      provides: "UnifiedCache policy and explicit decorator journey"
    - path: examples/custom_mcap_format.py
      provides: "Store-local MCAP-style FormatHandler journey"
  key_links:
    - from: tests/test_phase9_examples.py
      to: examples
      via: "literal four-path subprocess allowlist executed unchanged twice"
      pattern: "memory_blob_store.py|durable_catalog_store.py|unified_cache.py|custom_mcap_format.py"
    - from: examples/custom_mcap_format.py
      to: src/cacheness/handlers.py
      via: "store-local registry selection"
      pattern: "store.handlers.register_handler"
    - from: examples/durable_catalog_store.py
      to: src/cacheness/storage/blob_store.py
      via: "explicit topology, catalog put/query/update, close, and reopen"
      pattern: "filesystem|sqlite|query_catalog|update_catalog"
---

<objective>
Publish the four canonical executable journeys and their exact-file harness as the supported example source of truth.

Purpose: Let adopters run real current APIs while CI later executes the same files, with no copied snippets or hidden helper implementation.
Output: Four disposable network-free examples and one exact-file harness.
</objective>

<scope_guard>
Examples call current public APIs directly and must not introduce an example helper layer, new dependency, global cache/store, network call, remote topology, or lifecycle mechanism. Dedicated or mixed SqlCache examples remain Phase 10-owned and unmodified in this plan.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-adoption-and-release-surface-closure/09-CONTEXT.md
@.planning/phases/09-adoption-and-release-surface-closure/09-RESEARCH.md
@.planning/phases/09-adoption-and-release-surface-closure/09-PATTERNS.md
@.planning/phases/09-adoption-and-release-surface-closure/09-01-SUMMARY.md
@tests/test_phase6_examples.py
@examples/custom_metadata_demo.py
@src/cacheness/storage/blob_store.py
@src/cacheness/core.py
@src/cacheness/decorators.py
</context>

<interfaces>
- Use `StoreTopology(payload=BackendRef(...), authority=BackendRef(...))` with registered names `memory`, `filesystem`, and `sqlite`; filesystem options use `base_dir` and SQLite options use `root`.
- Call `BlobStore.initialize()` deliberately, use `put_entry`/`get`, catalog query/update APIs where applicable, and close resources deterministically.
- Construct `UnifiedCache(config, store=...)`, initialize it, inspect typed lookup outcomes, and use `@cached(cache=cache)`.
- A custom FormatHandler owns `data_type`, `payload_format`, `payload_format_version`, `.mcap` suffix, path-based `put`/`get`, and returns its contained actual path/size metadata.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish and execute the exact four canonical example journeys</name>
  <files>tests/test_phase9_examples.py, examples/memory_blob_store.py, examples/durable_catalog_store.py, examples/unified_cache.py, examples/custom_mcap_format.py</files>
  <behavior>
    - "The harness owns a literal four-file allowlist and invokes each published path directly, never a copied implementation or Markdown snippet."
    - "Each file runs successfully twice from fresh working directories with socket connections blocked and deterministic stdout markers."
    - "Each file uses a TemporaryDirectory/private root, asserts its observable result, closes its store/cache, and leaves its child working directory free of storage residue."
    - "The durable example proves catalog metadata and value survive close/reopen; the cache example proves typed hit behavior and explicit decorator reuse; the MCAP example proves store-local handler selection and round trip."
  </behavior>
  <action>Write `tests/test_phase9_examples.py` first with the literal four-file tuple, expected deterministic markers, child-only socket guard, sanitized cache environment, two-run loop, and residue assertions. Then implement the four complete published files against current public signatures. The memory example uses explicit memory/memory topology. The durable example uses filesystem payload plus SQLite authority, explicit initialization, catalog schema/values/query/update, close/reopen, and durability assertions. The cache example composes UnifiedCache over a caller-selected local memory store, proves typed lookup plus cached `None` or call count, uses `@cached(cache=cache)`, and clears/closes within its private scope. The MCAP-style example subclasses FormatHandler, declares stable MCAP data/payload/version identities and `.mcap`, performs only path-based I/O on its private staging path, registers through `store.handlers.register_handler(..., priority=0)`, and round-trips bytes. Every script prints one short stable success marker and cleans up without network, credentials, remote services, or repository-local cache paths.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase9_examples.py -x</automated>
  </verify>
  <done>All four exact published files run unchanged twice, self-verify their distinct current workflow, remain network-free, and leave no residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| example process -> host network/environment | Published scripts must not use sockets, credentials, shared cache directories, or remote services. |
| custom handler -> private guarded stage | The MCAP-style handler may write only its contained regular artifact with a safe suffix. |
| published examples -> adopter expectations | Examples must not imply stronger topology, durability, or publication guarantees than their qualified local scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-09 | Information Disclosure | example environment and network | high | mitigate | Run in fresh child processes with sockets blocked, credentials unused, cache env sanitized, and private temporary roots. |
| T-09-10 | Tampering | MCAP handler output | high | mitigate | Use existing GuardedHandlerIO boundary, safe `.mcap` suffix, contained regular-file result, and round-trip assertions. |
| T-09-11 | Repudiation | duplicated documentation examples | medium | mitigate | Execute the exact published files from a literal allowlist twice and keep no test-only equivalent implementation. |
| T-09-12 | Spoofing | topology qualification | medium | mitigate | Limit promoted examples to memory and local filesystem-plus-SQLite; remove S3 promotion and link remote support only from the qualification reference. |
</threat_model>

<verification>
- `tests/test_phase9_examples.py` passes all four paths twice with blocked sockets and residue checks.
- Targeted Ruff passes for the four new scripts and harness.
- No Phase-10-owned SqlCache source/test/example is changed.
</verification>

<acceptance_criteria>
- D-10 through D-14 are executable rather than prose-only.
- Each canonical workflow uses current public constructors, explicit initialization, typed results, and deterministic cleanup.
- Unsupported remote use is absent from the canonical journeys; Plans 09-04 through 09-06 remove obsolete scripts in bounded batches.
</acceptance_criteria>

<source_coverage>
- GOAL: Current architecture is usable through exact published examples.
- REQ: CACH-06 coherent constructor, decorator, result, and format-extension surface.
- RESEARCH: Literal four-file harness, repeatability/network/residue controls, exact local topology names/options, and Phase-10 cleanup boundary.
- CONTEXT: D-08, D-10, D-12, D-13, D-14, D-17.
</source_coverage>

<success_criteria>
- A developer can run any of the four files from a checkout and observe a verified current workflow.
- CI-ready exact-file tests prevent examples from drifting away from implementation.
- The exact-file harness gives later deletion and CI plans one executable source of truth.
</success_criteria>

<output>
Create `.planning/phases/09-adoption-and-release-surface-closure/09-03-SUMMARY.md` when done.
</output>
