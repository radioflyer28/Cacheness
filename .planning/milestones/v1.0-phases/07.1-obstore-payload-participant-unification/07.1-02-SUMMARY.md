---
phase: 07.1-obstore-payload-participant-unification
plan: 02
subsystem: testing
tags: [obstore, s3, moto, sdk-parity, immutable-publication]
requires:
  - phase: 07.1-01
    provides: "Approved obstore 0.11.1 provenance and release gate"
provides:
  - "Pinned obstore 0.11.1 core dependency with deterministic mocked-S3 test support"
  - "Executable LocalStore, MemoryStore, and S3Store parity contract for the consumed synchronous object surface"
  - "D-16 owner-pinning opt-out and loopback-only moto endpoint policy tests"
affects: [07.1-03, 07.1-04, 07.1-09, payload-participant, s3]
actuals:
  tokens: 44936
  tasks: 2
  commits: 4
tech-stack:
  added: ["obstore==0.11.1", "moto[s3,server] test dependency"]
  patterns:
    - "SDK parity contracts exercise only the narrow consumed object mechanics before production cutover."
    - "Exact head, never list, recovers ambiguous immutable create outcomes."
key-files:
  created: [tests/contracts/test_obstore_sdk_parity.py]
  modified: [pyproject.toml, uv.lock]
key-decisions:
  - "Pin the user-approved obstore 0.11.1 release as a core dependency before any payload adapter implementation."
  - "D-16 rejects ExpectedBucketOwner rather than emulating it through headers, boto3, or a parallel participant."
  - "The parity gate qualifies deterministic local, memory, and mocked S3 mechanics only; it does not claim lifecycle authority, live AWS, or performance qualification."
patterns-established:
  - "Use mode='create' and use_multipart=False for immutable generation publication."
  - "Treat only documented typed missing-object errors as absence; all other SDK errors remain distinguishable."
requirements-completed: [BACK-01, BACK-06, QUAL-04, QUAL-07]
coverage:
  - id: D1
    description: "Pinned SDK parity for LocalStore, MemoryStore, and mocked S3 conditional immutable object mechanics."
    requirement: BACK-01
    verification:
      - kind: integration
        ref: "tests/contracts/test_obstore_sdk_parity.py#test_consumed_sync_object_primitives_have_exact_immutable_parity"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-16 configuration and endpoint policies prevent owner-pinning/header workarounds in the consumed surface."
    requirement: BACK-06
    verification:
      - kind: integration
        ref: "tests/contracts/test_obstore_sdk_parity.py#test_sdk_error_types_keep_collision_absence_and_configuration_distinct"
        status: pass
      - kind: unit
        ref: "tests/contracts/test_obstore_sdk_parity.py#test_moto_s3_fixture_never_reintroduces_owner_pinning"
        status: pass
    human_judgment: false
  - id: D3
    description: "The contract bounds direct create and list use while preserving typed error distinctions."
    requirement: QUAL-04
    verification:
      - kind: integration
        ref: "uv run pytest -q -o log_cli=false tests/contracts/test_obstore_sdk_parity.py -x"
        status: pass
      - kind: other
        ref: "uv run ruff check tests/contracts/test_obstore_sdk_parity.py"
        status: pass
    human_judgment: false
status: complete
---

# Phase 07.1 Plan 02: SDK Parity Summary

**Obstore 0.11.1 is pinned and guarded by deterministic LocalStore, MemoryStore, and mocked-S3 immutable object-mechanics contracts.**

## Performance

- **Duration:** 34 minutes
- **Started:** 2026-09-13T15:08:31Z
- **Completed:** 2026-09-13T15:42:49Z
- **Tasks:** 2/2
- **Files modified:** 3

## Accomplishments

- Added the exact approved `obstore==0.11.1` core dependency and deterministic moto S3-server test support without removing legacy runtime code.
- Proved direct immutable create, exact recovery via `head`, sync streaming, bounded listing, exact deletion, and typed absence for local, memory, and mocked S3 stores.
- Froze error distinctions, non-multipart invocation, no-list recovery, D-16 owner-pinning rejection, supported transport options, and loopback-only test endpoint behavior.

## Task Commits

1. **Task 1: Prove one exact immutable object round trip across all three stores** - `341c2f8` (RED) and `2c61e17` (GREEN)
2. **Task 2: Freeze error, bound, and policy parity** - `64c1deb` (RED) and `1f8bd6e` (GREEN)

## Files Created/Modified

- `pyproject.toml` - Adds the approved obstore core dependency and moto server support for deterministic tests.
- `uv.lock` - Resolves the pinned package graph.
- `tests/contracts/test_obstore_sdk_parity.py` - Defines the complete direct-SDK consumed-surface parity contract.

## Decisions Made

- The SDK contract stays below Cacheness's lifecycle boundary: it proves payload primitives but does not establish visibility, recovery state, live AWS behavior, or performance guarantees.
- Create-response recovery observes the one immutable locator with `head()` and explicitly proves no listing occurs.
- S3 endpoint override is test-fixture-only, and D-16 rejects `expected_bucket_owner` rather than substituting a generic header or boto3 fallback.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The sandboxed `uv` cache was not readable for the final verification command; rerunning through the existing approved `uv run` environment succeeded. This did not alter project dependencies or source.

## Known Stubs

None.

## Next Phase Readiness

- Plans 03 and 04 can implement the shared payload participant against a pinned and executable SDK boundary.
- Plan 09 can perform the atomic built-in cutover without re-discovering basic LocalStore, MemoryStore, or mocked-S3 SDK behavior.

## Self-Check: PASSED

- Found `pyproject.toml`, `uv.lock`, and `tests/contracts/test_obstore_sdk_parity.py`.
- Found task commits `341c2f8`, `2c61e17`, `64c1deb`, and `1f8bd6e`.
- `uv run pytest -q -o log_cli=false tests/contracts/test_obstore_sdk_parity.py -x` passed: 13 tests.
- `uv run ruff check tests/contracts/test_obstore_sdk_parity.py` passed.
