---
phase: 07.1-obstore-payload-participant-unification
plan: 11
type: execute
wave: 10
depends_on:
  - 07.1-10
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_full_suite_environment.py
  - tests/qualification/conftest.py
  - tests/qualification/test_live_evidence.py
  - README.md
  - docs/API_REFERENCE.md
  - docs/PLUGIN_DEVELOPMENT.md
  - docs/SECURITY.md
  - tools/verify_phase071_contracts.py
  - tests/test_phase071_contract_verifier.py
  - tests/test_public_api_contract.py
autonomous: true
requirements:
  - BACK-01
  - BACK-04
  - BACK-06
  - SECU-03
  - SECU-07
  - QUAL-01
  - QUAL-04
  - QUAL-07
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A clean base wheel imports current public symbols and completes a memory-backed round trip with obstore installed as core (D-04)."
    - "S3/cloud extras no longer install boto3 for production; boto3 exists only in test tooling that provisions deterministic/live fixtures (D-06)."
    - "Public docs describe the 128 MiB direct-create bound, explicit bucket/region, no owner pinning, production endpoint rejection, signed transport evidence, and canonical SHA-256 behavior (D-02, D-03, D-12, D-16)."
    - "A fixed verifier binds every Phase 07.1 decision, requirement, threat, and prohibited architecture to exact executable selectors (D-13, D-14)."
    - "Real AWS/platform/full advertised optional-group packaging matrices, RSS/performance budgets, and SHA-256-versus-XXH3 benchmarking remain Phase 8 gates (D-15)."
  artifacts:
    - path: "tools/verify_phase071_contracts.py"
      provides: "Fixed Phase 07.1 contract and architecture verifier"
    - path: "tests/test_phase071_contract_verifier.py"
      provides: "Fail-closed selector/threat/source-inventory self-tests"
    - path: "docs/SECURITY.md"
      provides: "D-16 cross-account residual-risk and mitigation guidance"
  key_links:
    - from: "pyproject.toml"
      to: "tests/test_full_suite_environment.py"
      via: "targeted clean wheel/base plus selected S3/cloud cutover checks"
      pattern: "obstore|s3|cloud"
    - from: "tools/verify_phase071_contracts.py"
      to: "tests/contracts/test_obstore_generation_io.py"
      via: "literal AST-validated exact selectors"
      pattern: "PHASE071_MATRIX"
---

<objective>
Close the package, documentation, and deterministic verification boundary for the obstore cutover.

Purpose: Prove the released package has one payload transport implementation and document its bounded S3/security semantics without claiming Phase 8 evidence.
Output: Final extras/lock, clean-wheel tests, updated public docs, fixed verifier, and complete deterministic phase gate.
</objective>

<scope_guard>
This plan closes only the listed package, documentation, and fixed deterministic evidence surfaces. If verification reveals an unexpected cross-cutting repair outside those surfaces, stop and create a new split plan; do not absorb lifecycle, migration, or topology repair into this finalization plan.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-10-SUMMARY.md
@.planning/phases/07.1-obstore-payload-participant-unification/COVERAGE.md
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-DISCUSS-CHECKPOINT.json
@pyproject.toml
@tests/test_full_suite_environment.py
</context>

<interfaces>
The runtime now imports obstore from core and exposes current payload providers/comparison values through deliberate storage barrels. The live qualification runner remains fail closed and Phase 8-owned. Fixed verifiers use literal exact test selectors validated by AST before pytest execution.
</interfaces>

<artifacts_this_phase>
- Runtime dependency groups contain no boto3 payload fallback; development qualification tooling may retain boto3 through moto.
- Public docs state supported behavior and D-16 limitation.
- A fixed deterministic verifier covers the complete Phase 07.1 contract while emitting no live/performance claim.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove targeted clean-wheel and selected-extra cutover checks</name>
  <files>pyproject.toml, uv.lock, tests/test_full_suite_environment.py, tests/qualification/conftest.py, tests/qualification/test_live_evidence.py</files>
  <read_first>
    - pyproject.toml: core, optional S3/cloud, and development groups after Plan 02
    - tests/test_full_suite_environment.py: clean wheel and isolated dependency probes
    - tests/qualification/conftest.py: test-only boto3 provisioning and AWS configuration model
  </read_first>
  <behavior>
    - "A clean base wheel imports cacheness/storage public symbols and completes a MemoryStore-backed BlobStore round trip."
    - "Targeted S3 and cloud cutover checks prove obstore is core and production boto3 is absent without claiming the full independent optional-group matrix."
    - "Test tooling may import boto3 for bucket fixture administration, but installed runtime source does not."
    - "Live qualification configuration requires explicit bucket/region and contains no expected-owner setting."
  </behavior>
  <action>Remove boto3 from runtime `s3` and `cloud` extras after confirming no production import remains; keep the `s3` extra backed by core obstore and retain boto3 only through the development/moto qualification toolchain per D-06. Regenerate the lock. Extend targeted isolated wheel tests for the base install and selected `s3`/`cloud` imports plus a memory round trip, solely to prove this cutover's dependency boundary. Do not claim the independent advertised optional-group matrix; Phase 8 owns that full matrix under D-15. Update qualification fixtures/evidence contracts to remove owner pinning, require explicit bucket/region, retain external credentials and exact run-owned cleanup, and preserve the Phase 8 live-service gate.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_full_suite_environment.py tests/qualification/test_live_evidence.py -x</automated>
  </verify>
  <done>Clean installations exercise the current runtime without a boto3 payload dependency and retain truthful live-qualification configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Document the unified transport, handler seam, and bounded S3 security contract</name>
  <files>README.md, docs/API_REFERENCE.md, docs/PLUGIN_DEVELOPMENT.md, docs/SECURITY.md</files>
  <read_first>
    - .planning/phases/07.1-obstore-payload-participant-unification/07.1-CONTEXT.md: D-02, D-03, D-09, D-12, D-15, D-16
    - README.md: current backend overview
    - docs/API_REFERENCE.md: legacy payload backend examples
    - docs/PLUGIN_DEVELOPMENT.md: handler/provider extension examples
    - docs/SECURITY.md: storage trust and S3 guidance
  </read_first>
  <action>Replace public examples and reference text that expose `S3BlobBackend`, `BlobBackend`, boto3 payload mechanics, or multipart configuration with current StoreTopology/provider and BlobStore comparison APIs. Document the unchanged `CacheHandler.put/get(Path)` plus store-local registration seam per D-09. State that direct S3 conditional create is capped at a configurable 128 MiB initial default, canonical SHA-256 plus size is always verified before deserialization, ETag/version is signed opaque transport evidence, and the comparison method is noncanonical/read-only. Document D-16 clearly: require explicit bucket/region, tightly scoped IAM/bucket policy, stable bucket-name ownership, and no production endpoint overrides; owner pinning and compatible-service qualification are unsupported. Route real AWS/platform/RSS/performance/hash benchmark acceptance to Phase 8 per D-15.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_public_api_contract.py tests/test_phase071_contract_verifier.py -k 'documentation or public or expected_bucket_owner' -x</automated>
  </verify>
  <done>Public documentation matches the current provider/handler/API surface and states every S3 security and integrity limitation without overclaiming qualification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind the full deterministic phase contract to exact evidence</name>
  <files>tools/verify_phase071_contracts.py, tests/test_phase071_contract_verifier.py, tests/test_public_api_contract.py</files>
  <read_first>
    - tools/verify_phase7_contracts.py: fixed literal matrix, AST selector validation, and non-claim reporting
    - tests/test_phase7_contract_verifier.py: removed/renamed/duplicate selector self-tests
    - .planning/phases/07.1-obstore-payload-participant-unification/07.1-VALIDATION.md: exact plan/task map
    - .planning/phases/07.1-obstore-payload-participant-unification/COVERAGE.md: consumed API surface
  </read_first>
  <behavior>
    - "The verifier owns a literal Plan 01-11 inventory, every threat ID, every locked D-01 through D-16 decision, and exact pytest selectors for each claim."
    - "Removed, renamed, duplicated, malformed, or unowned selectors/threats fail before pytest can produce a false green."
    - "Source/AST gates deny production boto3, custom payload dictionaries, multipart lifecycle, runtime selectors/fallbacks, duplicate authority mechanisms, and handler signature drift."
    - "Quick mode stays under 30 seconds; all mode reruns the complete deterministic Local/Memory/mocked-S3, lifecycle, migration, public, and targeted package set and reports Phase 8 items as NOT RUN / NOT QUALIFIED."
  </behavior>
  <action>Create the Phase 07.1 fixed verifier using exact repository-relative `path::test_name` selectors and AST validation. Map all eleven plans, D-01 through D-16, the threat registry, source prohibitions, coverage rows, and documentation assertions to fixed evidence. Add self-tests that delete/rename/duplicate a selector and alter the threat/plan inventory. Quick mode must run the participant/evidence core under 30 seconds; all mode must rerun every deterministic LocalStore, MemoryStore, mocked-S3, authority, migration, cache-boundary, public, and targeted package contract from Plans 02-10 plus scoped Ruff on changed production/tests. Report real AWS, live PostgreSQL, native platforms, the full independent advertised optional-group matrix, RSS/performance budgets, and SHA-256-versus-XXH3 benchmarks as Phase 8 non-claims per D-15.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase071_contract_verifier.py -x && uv run python tools/verify_phase071_contracts.py --quick && uv run python tools/verify_phase071_contracts.py --all</automated>
  </verify>
  <done>The complete deterministic phase evidence is fixed, fail closed, and explicitly bounded away from Phase 8 qualification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package extras/wheel -> installed runtime | Test-only boto3 must not become a hidden production transport dependency. |
| docs/public API -> operator configuration | Security limitations and integrity semantics must not be misstated. |
| implementation -> verification ledger | A passing unrelated test cannot substitute for a removed contract node. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-11-01 | Tampering | package dependency boundary | high | mitigate | Isolated wheel/extra tests and production import/source denial prove boto3 remains test-only. |
| T-07.1-11-02 | Spoofing | cross-account/operator guidance | medium | mitigate | Document D-16 residual risk and mandatory bucket/region/IAM/policy/stable-name controls. |
| T-07.1-11-03 | Tampering | misleading integrity API | high | mitigate | Public docs/tests distinguish transport corroboration from canonical SHA-256 verification. |
| T-07.1-11-04 | Repudiation | verification drift | high | mitigate | Literal AST-validated selectors and exact plan/threat/source inventories fail closed. |
| T-07.1-11-05 | Denial of Service | unbounded qualification | medium | mitigate | Keep quick fixed evidence under 30 seconds and retain measured workloads in Phase 8. |
</threat_model>

<verification>
- Clean wheel/base and selected S3/cloud cutover checks pass; no full optional-group matrix is claimed.
- Public docs and APIs contain no legacy backend examples or hidden owner-pinning claim.
- Fixed quick/all verification, full deterministic suite, and scoped Ruff pass.
- Phase 8 non-claims are recorded exactly and are not satisfied by mocks/skips.
</verification>

<success_criteria>
- The package installs one core transport implementation and no production boto3 payload path.
- Operators understand the D-16 security boundary and canonical-versus-transport evidence distinction.
- Every phase decision and security invariant has exact deterministic evidence.
- Phase 8 retains all non-substitutable live, platform, performance, and hash-benchmark work.
</success_criteria>

<source_coverage>
- GOAL: Deliver a reliable, documented, package-correct obstore payload participant cutover.
- REQ: BACK-01/04/06, SECU-03/07, QUAL-01/04/07.
- RESEARCH: packaging cutover, runtime inventory, public negative checks, validation architecture.
- CONTEXT: D-02, D-03, D-04, D-06, D-07, D-09, D-12, D-13, D-14, D-15, D-16.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-11-SUMMARY.md` when done.
</output>
