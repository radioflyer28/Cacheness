---
phase: 07.1-obstore-payload-participant-unification
plan: 10
type: execute
wave: 9
depends_on:
  - 07.1-09
files_modified:
  - src/cacheness/storage/backends/blob_backends.py
  - src/cacheness/storage/backends/s3_backend.py
  - src/cacheness/storage/backends/__init__.py
  - src/cacheness/storage/__init__.py
  - tests/contracts/test_payload_generation_io.py
  - tests/contracts/test_s3_generation_io.py
  - tests/test_s3_blob_backend.py
  - tests/test_migration_public_contract.py
  - tests/integration/test_s3_generation.py
  - tests/integration/test_remote_topology.py
autonomous: true
requirements:
  - BACK-01
  - BACK-03
  - BACK-04
  - BACK-06
  - MIGR-01
  - MIGR-04
  - QUAL-04
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Custom byte dictionaries, filesystem payload CRUD, boto3 runtime calls, multipart methods/config, and old wide backend contracts are absent (D-06, D-14)."
    - "Public barrels expose only current provider/composition contracts; removed pre-production classes receive no aliases or shims (D-07, D-08)."
    - "Deterministic and frozen live test suites target the new provider while preserving truthful mocked-versus-live boundaries."
    - "Migration/public contract tests deny concrete-backend introspection and legacy payload selectors."
  artifacts:
    - path: "src/cacheness/storage/backends/__init__.py"
      provides: "Current authority/backend exports without legacy payload mechanics"
    - path: "tests/test_migration_public_contract.py"
      provides: "Static denial of legacy backend coupling"
  key_links:
    - from: "src/cacheness/storage/__init__.py"
      to: "src/cacheness/storage/obstore_generation_io.py"
      via: "deliberate current payload provider exports"
      pattern: "Obstore"
    - from: "tests/integration/test_s3_generation.py"
      to: "src/cacheness/storage/composition.py"
      via: "current S3 participant construction"
      pattern: "StoreTopology|BackendRef"
---

<objective>
Delete superseded custom payload mechanics and update every retained contract consumer.

Purpose: Make the atomic cutover irreversible by ordinary runtime selection while preserving explicit migration and qualification suites.
Output: Retired legacy modules/exports, updated tests, and executable absence contracts.
</objective>

<scope_guard>
This plan may retire legacy mechanics and rewrite only the listed deterministic/live consumers. If execution exposes an unexpected repair spanning another subsystem or architectural boundary, stop and create a new split plan; do not expand this plan into cross-cutting lifecycle repair.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-09-SUMMARY.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-COVERAGE.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-24-SUMMARY.md
@src/cacheness/storage/backends/blob_backends.py
@src/cacheness/storage/backends/s3_backend.py
</context>

<interfaces>
Plans 03/04/09 provide current obstore local, memory, and S3 providers at the stable composition seam. Phase 7 maintenance no longer imports wide backends. Pre-production compatibility reset permits deleting old public classes and stored-layout readers while retaining versioned maintenance tooling.
</interfaces>

<artifacts_this_phase>
- Legacy `BlobBackend`, bespoke filesystem/memory byte stores, `InMemoryHandlerIO`, boto3 S3 implementation, multipart types, and capability flags are removed.
- Test suites refer to current participant/composition APIs and preserve live-service non-claims.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Retire legacy modules and public exports without compatibility paths</name>
  <files>src/cacheness/storage/backends/blob_backends.py, src/cacheness/storage/backends/s3_backend.py, src/cacheness/storage/backends/__init__.py, src/cacheness/storage/__init__.py, tests/test_migration_public_contract.py</files>
  <read_first>
    - src/cacheness/storage/backends/blob_backends.py: wide CRUD and memory dictionary symbols
    - src/cacheness/storage/backends/s3_backend.py: boto3, multipart, evidence, and generation-I/O symbols
    - src/cacheness/storage/backends/__init__.py: legacy exports and lazy optional flags
    - tests/test_migration_public_contract.py: prohibited symbol/import inventory
  </read_first>
  <behavior>
    - "Importing current cacheness storage APIs does not import boto3/botocore or expose legacy payload classes/capability flags."
    - "Repository production AST has no custom payload dictionary, multipart publication/abort/listing, wide byte CRUD base, or legacy selector."
    - "Current provider and PostgreSQL authority exports remain deliberate and importable under their dependency contracts."
  </behavior>
  <action>After repository-wide consumer search, delete the obsolete implementation bodies/files or reduce package structure to current non-compatibility exports only. Remove `BlobBackend`, custom filesystem/memory CRUD, `InMemoryHandlerIO`, `S3BlobBackend`, `_S3GenerationIO`, `BOTO3_AVAILABLE`, multipart configuration/receipt/inventory code, and related lazy export branches per D-06/D-07/D-14. Export the current obstore providers/composition types deliberately. Extend static public/migration contracts to fail on any runtime selector, fallback, wide CRUD, concrete-backend introspection, or duplicate lifecycle authority. Do not add aliases, deprecated wrappers, or legacy stored-layout reads.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_migration_public_contract.py tests/test_public_api_contract.py -x</automated>
  </verify>
  <done>The installed runtime contains no reachable custom payload mechanics or boto3 fallback and exposes only the current participant surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute deterministic suites and collect only deferred live suites</name>
  <files>tests/contracts/test_payload_generation_io.py, tests/contracts/test_s3_generation_io.py, tests/test_s3_blob_backend.py, tests/integration/test_s3_generation.py, tests/integration/test_remote_topology.py</files>
  <read_first>
    - tests/contracts/test_obstore_generation_io.py: canonical new participant fixture and assertions
    - tests/contracts/test_s3_generation_io.py: behavior to retain without old class construction
    - tests/integration/test_s3_generation.py: live AWS test semantics retained for Phase 8
    - tests/integration/test_remote_topology.py: PostgreSQL/S3 shared topology construction
  </read_first>
  <behavior>
    - "Legacy local/memory/S3 contract files either import the current providers or delegate to the shared obstore contract; they cannot instantiate removed classes."
    - "Live AWS tests construct the current S3 provider with explicit bucket/region and no owner-pinning field."
    - "Remote topology tests retain PostgreSQL authority, external credentials, exact namespace ownership, and Phase 8 markers."
    - "No mocked contract is presented as real AWS qualification."
  </behavior>
  <action>Update or consolidate retained payload/S3 tests onto `ObstoreGenerationIO` and current provider constructors. Preserve unique behavior assertions while eliminating duplicate contracts tied to old classes. Execute every rewritten deterministic LocalStore, MemoryStore, and moto-backed S3 contract normally, including the shared obstore contract, retained generation contracts, mocked S3 backend suite, and migration public contract. Update frozen live AWS/remote PostgreSQL topology construction to explicit bucket/region and native credentials, remove the superseded owner option, and keep production endpoint overrides unavailable; only those genuinely deferred live suites use collection-only here because Phase 8 owns their configured execution. Keep test-only boto3 limited to resource provisioning/cleanup until Plan 11 packaging finalizes the boundary.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/contracts/test_obstore_generation_io.py tests/contracts/test_payload_generation_io.py tests/contracts/test_s3_generation_io.py tests/test_s3_blob_backend.py tests/test_migration_public_contract.py -x &amp;&amp; uv run pytest -q -o log_cli=false tests/integration/test_s3_generation.py tests/integration/test_remote_topology.py --collect-only -x</automated>
  </verify>
  <done>All rewritten deterministic suites execute against the current participant, while only the explicitly deferred live AWS/PostgreSQL suites collect without false qualification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public imports/config -> runtime implementation | Old names must not silently select obsolete security/lifecycle behavior. |
| deterministic/live tests -> qualification claims | Mocked evidence must remain distinguishable from real AWS acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-10-01 | Elevation | legacy runtime escape hatch | high | mitigate | Delete old implementations/exports and enforce AST/import absence contracts. |
| T-07.1-10-02 | Tampering | duplicate lifecycle mechanics | high | mitigate | Deny wide CRUD, multipart states, selectors, and concrete backend branches. |
| T-07.1-10-03 | Spoofing | mocked evidence as live AWS | high | mitigate | Preserve markers/fail-closed live runner and never emit qualification from deterministic tests. |
| T-07.1-10-04 | Repudiation | removed API silently aliased | medium | mitigate | Assert explicit import absence under the pre-production cutover contract. |
</threat_model>

<verification>
- Current deterministic storage contracts execute and pass without removed imports.
- Production source has no boto3/botocore, multipart, custom byte dictionary, or wide legacy backend symbols.
- Only live AWS/PostgreSQL suites remain collected but unexecuted/unqualified until Phase 8.
</verification>

<success_criteria>
- Custom filesystem/memory/S3 mechanics are physically absent.
- No compatibility layer can route production operations back to them.
- Maintained tests describe only the current architecture and truthful qualification boundary.
</success_criteria>

<source_coverage>
- GOAL: Complete the one-implementation cutover and remove divergence.
- REQ: BACK-01/03/04/06, MIGR-01/04, QUAL-04.
- RESEARCH: mechanics-to-remove and public/packaging negative checks.
- CONTEXT: D-02, D-06, D-07, D-08, D-10, D-14, D-15, D-16.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-10-SUMMARY.md` when done.
</output>
