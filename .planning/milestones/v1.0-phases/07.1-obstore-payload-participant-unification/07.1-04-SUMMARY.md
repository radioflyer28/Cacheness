---
phase: 07.1-obstore-payload-participant-unification
plan: 04
subsystem: storage
tags: [obstore, S3Store, moto, immutable-generations, BlobStore]
requires:
  - phase: 07.1-03
    provides: Shared LocalStore and MemoryStore generation-I/O participant with private handler staging and exact response-loss settlement.
provides:
  - Mocked-S3 S3Store construction through the shared obstore generation-I/O participant.
  - Direct bounded immutable publication with opaque head and report-only inventory evidence.
  - Executable D-16 rejection of owner pinning and production endpoint overrides.
affects: [07.1-05, payload-participant-cutover, phase-08-live-aws-qualification]
actuals:
  tokens: 6438
  tasks: 2
  commits: 4
tech-stack:
  added: []
  patterns:
    - S3Store construction is explicit and test-only endpoint overrides are isolated behind a private moto flag.
    - Exact object observations preserve ETag and version as opaque transport evidence rather than canonical payload identity.
key-files:
  created: []
  modified:
    - src/cacheness/storage/obstore_generation_io.py
    - tests/contracts/test_obstore_generation_io.py
key-decisions:
  - "D-16 rejects expected_bucket_owner before S3Store construction; no signer, fork, boto3 production fallback, or parallel participant restores it."
  - "S3 publication remains one direct conditional create with multipart disabled and a 128 MiB default transfer cap."
  - "Exact head and bounded inventory observations are report-only transport evidence; AuthorityLifecycleEngine remains the only visibility authority."
patterns-established:
  - "Mocked S3 contract: a test-only loopback moto endpoint composes the real BlobStore engine with deterministic remote-shaped authority evidence without claiming real AWS or PostgreSQL qualification."
requirements-completed: [STOR-03, STOR-04, STOR-05, STOR-07, STOR-08, BACK-01, BACK-04, BACK-06, QUAL-04, QUAL-07]
coverage:
  - id: D1
    description: "Mocked S3 completes real BlobStore lifecycle transitions for native NumPy and separately registered MCAP-style path handlers without exposing managed locators."
    requirement: BACK-01
    verification:
      - kind: integration
        ref: "tests/contracts/test_obstore_generation_io.py#test_mocked_s3_provider_completes_one_authoritative_lifecycle_for_native_and_mcap"
        status: pass
    human_judgment: false
  - id: D2
    description: "Mocked S3 collision and accepted-response loss settle only from the exact immutable object while inventory never establishes lifecycle visibility."
    requirement: STOR-04
    verification:
      - kind: integration
        ref: "tests/contracts/test_obstore_generation_io.py#test_mocked_s3_collision_and_lost_create_response_settle_only_by_exact_object"
        status: pass
    human_judgment: false
  - id: D3
    description: "S3 configuration rejects owner pinning and unsafe endpoints; direct uploads, opaque observations, bounded inventory, and exact deletion preserve D-16 and resource boundaries."
    requirement: QUAL-07
    verification:
      - kind: integration
        ref: "uv run pytest -q -o log_cli=false tests/contracts/test_obstore_generation_io.py -k 's3 or expected_bucket_owner or endpoint' -x"
        status: pass
    human_judgment: false
duration: 11m 19s
completed: 2026-09-13
status: complete
---

# Phase 07.1 Plan 04: Mocked S3 Generation I/O Summary

**The shared obstore participant now drives bounded mocked-S3 immutable payload mechanics through real BlobStore lifecycle transitions, while keeping canonical integrity and visibility solely in Cacheness's existing authority engine.**

## Performance

- **Duration:** 11m 19s
- **Started:** 2026-09-13T16:24:40Z
- **Completed:** 2026-09-13T16:35:59Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added explicit S3Store construction with bucket, region, contained prefix, `conditional_put="etag"`, bounded retries, and an internal loopback-only moto exception.
- Exercised native NPZ and registered MCAP handlers through mocked S3, exact immutable collision, response-loss settlement, authoritative promotion, reads, and deletion.
- Added D-16 fail-closed configuration checks plus direct-upload, opaque ETag/version, bounded listing, exact-delete, and unrelated-object contracts.

## Task Commits

1. **Task 1: Complete one BlobStore lifecycle through moto-backed S3Store**
   - `0f87779` — `test(07.1-04): add failing mocked S3 lifecycle contracts`
   - `17e9b75` — `feat(07.1-04): add mocked S3 generation participant`
2. **Task 2: Enforce S3 configuration, bounds, evidence, and exact maintenance behavior**
   - `02b4854` — `test(07.1-04): add failing S3 policy contracts`
   - `74db4ff` — `feat(07.1-04): enforce bounded S3 evidence policy`

## Files Created/Modified

- `src/cacheness/storage/obstore_generation_io.py` — constructs bounded S3Store participants, rejects D-16 opt-outs, and returns exact opaque transport observations and report-only inventory pages.
- `tests/contracts/test_obstore_generation_io.py` — exercises real moto HTTP object calls, authority-engine lifecycles, configuration rejection, direct-put bounds, opaque evidence, inventory, and exact cleanup.

## Decisions Made

- `expected_bucket_owner` is explicitly rejected before S3Store construction. AWS IAM, bucket policy, explicit bucket/region, and stable bucket ownership remain the documented defense-in-depth boundary.
- ETags and optional versions are returned unchanged as opaque transport observations. SHA-256 plus byte size stays canonical identity and inventory never grants visibility.
- Production endpoint overrides are rejected. Only the underscore-prefixed test flag may reach a loopback HTTP moto server.

## TDD Gate Compliance

- Task 1 completed RED (`0f87779`) before GREEN (`17e9b75`).
- Task 2 completed RED (`02b4854`) before GREEN (`74db4ff`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Test fixture bug] Created the private handler root before guarded I/O construction**
- **Found during:** Task 1
- **Issue:** `GuardedHandlerIO` correctly requires its root to exist; the new moto fixture had not created it.
- **Fix:** Created the fixture's private handler root before constructing the participant.
- **Files modified:** `tests/contracts/test_obstore_generation_io.py`
- **Verification:** Mocked-S3 lifecycle contract passes.
- **Committed in:** `17e9b75`

**2. [Rule 2 - Missing critical validation] Contained the configured S3 namespace before SDK construction**
- **Found during:** Task 2
- **Issue:** A non-empty prefix alone could permit traversal-like or malformed namespace text at the remote trust boundary.
- **Fix:** Enforced canonical non-traversing prefix segments and normalized malformed loopback endpoints to `CacheConfigurationError` before any S3Store construction.
- **Files modified:** `src/cacheness/storage/obstore_generation_io.py`, `tests/contracts/test_obstore_generation_io.py`
- **Verification:** Focused configuration and mocked-S3 contract tests pass.
- **Committed in:** `74db4ff`

---

**Total deviations:** 2 auto-fixed (1 Rule 1, 1 Rule 2).
**Impact on plan:** Both fixes are directly required for the guarded-handler and S3 configuration safety boundaries; no lifecycle authority or runtime fallback was added.

## Issues Encountered

The sandbox could not initially open the existing `uv` cache or Git index. Re-running the planned checks and commits with approved workspace access succeeded; no project code or dependency change was required.

## User Setup Required

None - moto and boto3 remain deterministic test-only tooling. This plan does not request AWS credentials or claim live AWS qualification.

## Next Phase Readiness

- The shared participant can now supply mocked S3 payload mechanics without changing `BlobStore` lifecycle sequencing or handler path contracts.
- Real AWS credentials, production endpoint behavior, PostgreSQL service behavior, platform coverage, and measured performance remain Phase 8 qualification work.

## Self-Check: PASSED

- The mocked-S3 participant, its contract suite, and this summary exist on disk.
- All four RED/GREEN task commits are present in git history.
