---
phase: 07.1-obstore-payload-participant-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 07.1-01
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/contracts/test_obstore_sdk_parity.py
autonomous: true
requirements:
  - BACK-01
  - BACK-06
  - QUAL-04
  - QUAL-07
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact approved obstore release is pinned as a core dependency, and executable LocalStore/MemoryStore/S3Store SDK parity is established before the atomic built-in cutover in Plan 09 (D-04)."
    - "An executable contract proves every consumed SDK primitive before production-source edits (D-05)."
    - "Direct conditional create uses one immutable locator and no multipart/copy lifecycle (D-01, D-02)."
    - "Owner pinning is an explicit D-16 opt-out, while explicit bucket/region and production endpoint policy remain mandatory."
  artifacts:
    - path: "tests/contracts/test_obstore_sdk_parity.py"
      provides: "Pinned LocalStore, MemoryStore, and moto HTTP S3 SDK parity gate"
    - path: "pyproject.toml"
      provides: "Approved core obstore and deterministic moto-server test dependencies"
  key_links:
    - from: "tests/contracts/test_obstore_sdk_parity.py"
      to: "obstore 0.11.1"
      via: "direct construction and consumed method calls"
      pattern: "LocalStore|MemoryStore|S3Store|mode=.create."
---

<objective>
Install the approved dependency and freeze an executable gate over the complete consumed obstore surface.

Purpose: Ensure later production work and Plan 09's built-in cutover rely only on API behavior proven for the pinned release.
Output: Core dependency/lock changes and a deterministic all-store SDK parity contract.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-01-SUMMARY.md
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-RESEARCH.md
@.planning/phases/07.1-obstore-payload-participant-unification/COVERAGE.md
@pyproject.toml
</context>

<artifacts_this_phase>
- `tests/contracts/test_obstore_sdk_parity.py` is the first production-edit gate required by D-05.
- `pyproject.toml` and `uv.lock` pin the approved core transport dependency without removing legacy runtime code yet.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one exact immutable object round trip across all three stores</name>
  <files>pyproject.toml, uv.lock, tests/contracts/test_obstore_sdk_parity.py</files>
  <read_first>
    - .planning/phases/07.1-obstore-payload-participant-unification/07.1-RESEARCH.md: pinned constructor/method signatures and parity table
    - .planning/phases/07.1-obstore-payload-participant-unification/COVERAGE.md: INTEGRATE and OPT-OUT decisions
    - pyproject.toml: core, S3/cloud, and development dependency groups
  </read_first>
  <behavior>
    - "LocalStore, MemoryStore, and moto-backed S3Store each conditionally create one exact key, head it, stream it, list one bounded prefix page, delete it, and prove absence."
    - "A second `mode='create'` call for the same locator produces the documented typed collision without replacing bytes."
    - "S3 construction requires explicit bucket and region in the Cacheness test fixture and uses only the local moto endpoint exception."
  </behavior>
  <action>After the Plan 01 approval, add exactly `obstore==0.11.1` to core dependencies and add the moto server extra to the development group, then regenerate `uv.lock`. Write the contract against the bound store methods and a real HTTP moto server, using test-only boto3 only to provision the bucket. Exercise `put` with `mode="create"` and `use_multipart=False`, exact `head`, synchronous streamed `get`, bounded `list`, exact `delete`, typed collision, and typed absence for all three stores per D-01, D-02, D-04, and D-05. Assert ObjectMeta/PutResult expose size, ETag, and optional version without treating ETag as content identity. Keep boto3 in runtime extras until the atomic cutover plan.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/contracts/test_obstore_sdk_parity.py -x</automated>
  </verify>
  <done>The pinned SDK passes the complete directly consumed object-mechanics tracer for local, memory, and mocked S3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze error, bound, and policy parity</name>
  <files>tests/contracts/test_obstore_sdk_parity.py</files>
  <read_first>
    - .planning/phases/07.1-obstore-payload-participant-unification/07.1-RESEARCH.md: exception taxonomy, upload behavior, and D-16 resolution
    - docs/adr/0001-topology-specific-storage-guarantees.md: safety/progress/performance separation
  </read_first>
  <behavior>
    - "Known collision, absence, permission/configuration, and invalid-path variants remain distinguishable; arbitrary failures are not absence."
    - "The S3 call recorder proves direct create disables multipart and exact recovery does not list."
    - "The pinned SDK rejects `expected_bucket_owner`; generic headers are not used to emulate it (D-16)."
    - "Native async, ranges, copy/rename, tags, object administration, and production endpoints stay outside the consumed matrix."
  </behavior>
  <action>Extend the parity contract with call recording and exact exception assertions covering every `INTEGRATE` row in `COVERAGE.md`. Add explicit tests for the D-16 owner-option rejection and for test-only endpoint scoping. Assert bounded page sizes and synchronous iteration without creating availability deadlines. Keep the parity test at the SDK boundary: it certifies primitives, not authority visibility, recovery state, live AWS, or performance.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/contracts/test_obstore_sdk_parity.py -x</automated>
  </verify>
  <done>The executable parity gate matches the checked-in API coverage matrix and blocks production edits if any consumed primitive changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Cacheness -> obstore SDK | External exceptions, buffering, and request semantics must match the pinned contract. |
| test client -> moto HTTP server | The emulator proves deterministic protocol behavior only, not live AWS qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-02-SC | Tampering | dependency lock | high | mitigate | Pin the human-approved release and regenerate the lock before executing its code. |
| T-07.1-02-01 | Spoofing | S3 account/bucket configuration | medium | mitigate | Require bucket/region in fixtures, reject production endpoint semantics later, and encode D-16 owner pinning as an explicit opt-out. |
| T-07.1-02-02 | Denial of Service | nonmultipart put/list | medium | mitigate | Prove nonmultipart invocation and bounded listing; production adapter enforces byte caps in Plans 03/04. |
| T-07.1-02-03 | Tampering | exception normalization | high | mitigate | Freeze typed collision/absence/configuration variants and forbid generic failure-to-absence conversion. |
</threat_model>

<verification>
- The direct SDK contract passes in under 30 seconds.
- Every `INTEGRATE` row in `COVERAGE.md` has an executable assertion or an explicit later-plan link.
- No production source is modified in this plan.
</verification>

<success_criteria>
- The dependency is approved, pinned, locked, and proven across the consumed surface.
- Later plans can fail closed against SDK drift without inspecting broad obstore APIs.
</success_criteria>

<source_coverage>
- GOAL: Establish one qualified transport core before cutover.
- REQ: BACK-01, BACK-06, QUAL-04, QUAL-07.
- RESEARCH: Fail-closed parity matrix, pinned API reference, package cutover, exception taxonomy.
- CONTEXT: D-01, D-02, D-04, D-05, D-15, D-16.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-02-SUMMARY.md` when done.
</output>
