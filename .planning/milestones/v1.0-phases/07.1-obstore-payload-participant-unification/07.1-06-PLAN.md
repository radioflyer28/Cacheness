---
phase: 07.1-obstore-payload-participant-unification
plan: 06
type: execute
wave: 6
depends_on:
  - 07.1-05
files_modified:
  - src/cacheness/storage/manifest.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/storage/backends/postgresql_lifecycle_authority.py
  - tests/test_manifest_versions.py
  - tests/test_sqlite_lifecycle_authority.py
  - tests/contracts/test_postgresql_lifecycle_authority.py
  - tests/contracts/test_lifecycle_authority.py
autonomous: true
requirements:
  - STOR-01
  - STOR-02
  - BACK-02
  - BACK-04
  - MIGR-02
  - MIGR-04
  - MIGR-07
  - SECU-04
  - SECU-05
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "SQLite and PostgreSQL persist the same evidence bytes in the existing verification transaction and copy them during promotion (D-11)."
    - "New authority schemas have explicit versions/capabilities, and ordinary open rejects older development layouts without mutation (D-08)."
    - "The manifest's persisted version dimensions remain truthful after the SQLite authority schema bump (D-08)."
    - "SQLite and PostgreSQL catalog/user-metadata updates each use one CAS authority transaction, preserving exact generation, locator, canonical payload SHA-256/size, and evidence; stale updates change nothing (D-11)."
  artifacts:
    - path: "src/cacheness/storage/sqlite_lifecycle_authority.py"
      provides: "Versioned durable transport-evidence columns and transactional transitions"
    - path: "src/cacheness/storage/backends/postgresql_lifecycle_authority.py"
      provides: "Equivalent PostgreSQL schema/capability and evidence persistence"
  key_links:
    - from: "src/cacheness/storage/lifecycle_authority.py"
      to: "src/cacheness/storage/sqlite_lifecycle_authority.py"
      via: "VerificationProof and EntrySnapshot serialization"
      pattern: "transport_evidence"
    - from: "src/cacheness/storage/manifest.py"
      to: "src/cacheness/storage/sqlite_lifecycle_authority.py"
      via: "truthful current schema version dimension"
      pattern: "CURRENT_SQLITE_USER_VERSION|SQLITE_USER_VERSION"
---

<objective>
Persist signed transport evidence transactionally in every durable lifecycle authority.

Purpose: Keep memory, SQLite, and PostgreSQL semantically aligned while preserving explicit offline schema cutover behavior.
Output: Versioned SQLite/PostgreSQL authority schemas, exact transition wiring, and reopen/replay contracts.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-05-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@src/cacheness/storage/manifest.py
@src/cacheness/storage/sqlite_lifecycle_authority.py
@src/cacheness/storage/backends/postgresql_lifecycle_authority.py
</context>

<interfaces>
Plan 05 makes `VerificationProof.transport_evidence` and `EntrySnapshot.transport_evidence` optional bounded authenticated bytes bound to immutable payload identity, and adds one backend-neutral conditional metadata-replacement method to `LifecycleAuthority`. SQLite schema 8 and PostgreSQL schema/capability 4 are pre-cutover development layouts; Plan 06 deliberately advances SQLite and its manifest dimension to version 9 under the pre-production cutover, and ordinary opens validate exact current identity without altering schema.
</interfaces>

<artifacts_this_phase>
- SQLite schema/user version and PostgreSQL schema/capability advance coherently.
- Unsupported prior development layouts receive existing typed migration/rebuild-required outcomes without an implicit ALTER path.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reopen one promoted SQLite entry with exact transport evidence</name>
  <files>src/cacheness/storage/manifest.py, src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_manifest_versions.py, tests/test_sqlite_lifecycle_authority.py, tests/contracts/test_lifecycle_authority.py</files>
  <read_first>
    - src/cacheness/storage/sqlite_lifecycle_authority.py: schema constants, DDL, `record_verification`, `promote_mutation`, page/snapshot readers
    - src/cacheness/storage/manifest.py: `StoreVersionDimensions` and current SQLite dimension
    - tests/test_sqlite_lifecycle_authority.py: initialize/reopen/version mismatch contracts
  </read_first>
  <behavior>
    - "Schema 9 stores optional bounded evidence in mutations, entries, migration-store entries, and any temporary snapshot that reconstructs EntrySnapshot."
    - "Verification writes digest, size, manifest, and evidence in one conditional transaction; promotion copies them in its one transaction."
    - "Close/reopen returns byte-identical authenticated evidence and unchanged payload identity after catalog/user-metadata-only updates, catalog pages, clear, unchanged-payload migration, and reconciliation reads; tombstones return none."
    - "SQLite stale-expectation metadata/catalog updates return the typed CAS conflict and leave manifest, evidence, and revision unchanged."
    - "Schema 8 ordinary open fails with explicit migration/rebuild-required evidence and performs no implicit ALTER."
  </behavior>
  <action>Add nullable bounded transport-evidence columns wherever SQLite persists a verification proof or EntrySnapshot. Advance `SQLITE_USER_VERSION`, `SCHEMA_VERSION`, and the manifest's current SQLite version dimension coherently to explicit version 9 per D-08/D-11 and the approved pre-production cutover. Store evidence in the existing conditional verification update and copy it in promotion. Implement the Plan 05 authority metadata-replacement method as one SQL CAS transaction keyed by the exact expected entry identity/revision: verify immutable generation, locator, canonical payload SHA-256, and byte size; update only newly signed mutable manifest/catalog bytes and revision; preserve evidence byte-for-byte. A stale expectation returns the existing typed conflict with zero row change. Add SQL statement/participant spies proving catalog/user-metadata updates perform no prepare, publish, snapshot/head, record-verification, promote, cleanup, or second transaction. Preserve prepared replay and unchanged-payload migration evidence; newly verified generations store fresh evidence; tombstones store none. Update decoders without an implicit schema-8 reader/upgrade.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_manifest_versions.py tests/test_sqlite_lifecycle_authority.py tests/contracts/test_lifecycle_authority.py -k 'transport or version or reopen or replay or metadata or catalog or stale or cas' -x</automated>
  </verify>
  <done>SQLite schema 9 truthfully persists evidence across all authoritative read models and refuses schema 8 without mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror the evidence transition in PostgreSQL schema and capability</name>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py, tests/contracts/test_lifecycle_authority.py</files>
  <read_first>
    - src/cacheness/storage/backends/postgresql_lifecycle_authority.py: schema/capability constants, DDL, proof update, promotion, pages, migration snapshots
    - tests/contracts/test_postgresql_lifecycle_authority.py: DB-API fake and schema-version assertions
    - docs/adr/0001-topology-specific-storage-guarantees.md: PostgreSQL transaction boundary
  </read_first>
  <behavior>
    - "PostgreSQL schema/capability 5 persists optional BYTEA evidence across the same proof/entry/snapshot/update/tombstone surfaces as SQLite."
    - "PostgreSQL catalog/user-metadata updates are one conditional authority transaction; stale CAS leaves entry, evidence, and revision unchanged."
    - "Verification and promotion remain short authority-only transactions; no object call occurs inside them."
    - "A schema/capability 4 authority fails explicit validation and is routed to stopped-worker maintenance."
    - "Retryable PostgreSQL progress errors remain typed and do not alter evidence or claim cross-resource ACID."
  </behavior>
  <action>Advance the PostgreSQL authority schema and capability identifiers to 5 and add nullable bounded BYTEA evidence columns to mutation, entry, and migration snapshot records. Extend bound SQL parameters and row decoders; keep one `record_verification` update and one promotion transaction. Implement the same backend-neutral metadata-replacement method as a single conditional PostgreSQL authority transaction: validate unchanged generation, locator, canonical payload SHA-256/size; update only signed mutable manifest/catalog bytes and revision; preserve evidence byte-for-byte. Prove stale CAS has zero effect and add DB-API/participant spies denying prepare, publish, snapshot/head, record-verification, promotion, cleanup, and extra transaction work. Preserve unchanged replay/migration evidence, use fresh evidence only for newly verified generations, and none for tombstones. Keep real PostgreSQL service qualification in Phase 8 and add no distributed lock, payload call, or coordinator.</action>
  <verify>
    <automated>uv run --extra postgresql pytest -q -o log_cli=false tests/contracts/test_postgresql_lifecycle_authority.py tests/contracts/test_lifecycle_authority.py -k 'transport or schema or capability or replay or metadata or catalog or stale or cas' -x</automated>
  </verify>
  <done>PostgreSQL schema/capability 5 preserves the same evidence contract and authority-only transaction boundary as memory and SQLite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| encoded evidence -> SQL rows -> typed snapshots | Persisted bytes must not be dropped, mutated, or accepted under a stale schema identity. |
| authority transaction -> external object store | Database ACID stops at authority state; payload effects remain outside. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-06-01 | Tampering | stale schema/version | high | mitigate | Advance and validate explicit SQLite/PostgreSQL identities and reject prior development layouts without mutation. |
| T-07.1-06-02 | Tampering | evidence persistence or stale metadata overwrite | high | mitigate | Cover every persistence path and assert byte-identical evidence plus zero-effect stale CAS across SQLite/PostgreSQL. |
| T-07.1-06-03 | Elevation | cross-resource transaction claim | high | mitigate | Keep obstore calls outside authority transactions and test transition boundaries independently. |
| T-07.1-06-04 | Denial of Service | oversized evidence row | medium | mitigate | Validate the bounded evidence encoding before SQL writes and on decode. |
</threat_model>

<verification>
- Focused SQLite and PostgreSQL-fake suites pass; no live-service result is claimed.
- Ordinary open of old schema fixtures is non-mutating and typed.
- Every EntrySnapshot-producing query preserves evidence or explicit absence.
- SQLite/PostgreSQL metadata/catalog CAS tests preserve immutable payload identity/evidence and stale attempts leave zero change.
- Spies prove metadata-only transactions perform no participant or prepare/verify/promote/cleanup operation.
</verification>

<success_criteria>
- All three authority implementations persist evidence through the same semantic transition.
- Version identifiers accurately describe the new durable layout.
- Explicit migration/rebuild machinery remains the only route for unsupported layouts.
- Metadata-only changes use the same narrow authority interface across memory, SQLite, and PostgreSQL.
</success_criteria>

<source_coverage>
- GOAL: Keep transport unification aligned with the canonical authority across durable topologies.
- REQ: STOR-01/02, BACK-02/04, MIGR-02/04/07, SECU-04/05.
- RESEARCH: authority schema implications and explicit version coupling.
- CONTEXT: D-08, D-10, D-11.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-06-SUMMARY.md` when done.
</output>
