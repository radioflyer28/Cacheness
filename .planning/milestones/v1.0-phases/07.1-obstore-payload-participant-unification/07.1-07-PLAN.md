---
phase: 07.1-obstore-payload-participant-unification
plan: 07
type: execute
wave: 7
depends_on:
  - 07.1-06
files_modified:
  - src/cacheness/storage/composition.py
  - src/cacheness/storage/read_contract.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/__init__.py
  - tests/test_blob_store_read_contract.py
  - tests/test_payload_transport_evidence.py
autonomous: true
requirements:
  - STOR-02
  - STOR-08
  - BACK-03
  - BACK-07
  - SECU-03
  - SECU-04
  - SECU-05
  - SECU-08
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A developer can compare the authoritative generation's signed transport evidence with one exact participant head without downloading payload bytes (D-12)."
    - "The result identifies the checked generation and distinguishes match, mismatch, unavailable evidence, absent object, and backend failure."
    - "A match means transport corroboration only; it cannot authorize repair, promotion, deletion, or handler deserialization (D-03, D-10, D-12)."
    - "A narrow backend-neutral optional transport-observation protocol lives at the existing participant seam; BlobStore never imports or type-checks the concrete obstore adapter (BACK-03)."
    - "Injected providers without transport observation remain selected and report UNAVAILABLE rather than acquiring a second lifecycle authority."
  artifacts:
    - path: "src/cacheness/storage/read_contract.py"
      provides: "Immutable typed transport comparison result/status"
    - path: "src/cacheness/storage/blob_store.py"
      provides: "Read-only authoritative-generation transport comparison API"
    - path: "src/cacheness/storage/composition.py"
      provides: "Backend-neutral optional exact transport-observation protocol"
  key_links:
    - from: "src/cacheness/storage/blob_store.py"
      to: "src/cacheness/storage/lifecycle_authority.py"
      via: "committed EntrySnapshot selection and evidence authentication"
      pattern: "read_entry|transport_evidence"
    - from: "src/cacheness/storage/blob_store.py"
      to: "src/cacheness/storage/composition.py"
      via: "optional structural transport-observation capability"
      pattern: "PayloadTransportObservationProvider|observe_transport"
---

<objective>
Expose a narrow read-only transport comparison for the exact committed generation.

Purpose: Let developers cheaply corroborate S3 object identity without confusing ETag evidence with canonical verification.
Output: Typed comparison result, BlobStore method, public export, and mutation-free contracts.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-06-SUMMARY.md
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/read_contract.py
@src/cacheness/storage/lifecycle_authority.py
@src/cacheness/storage/transport_evidence.py
</context>

<interfaces>
`BlobStore.get_entry_info()` resolves only committed authority state. Plan 05 evidence authenticates immutable store/key identity, generation, locator, canonical SHA-256, and byte size. The existing `PayloadGenerationIOProvider` protocol in `composition.py` is the backend-neutral participant seam; an optional sibling capability exposes an exact untrusted transport observation and never mutates authority.
</interfaces>

<artifacts_this_phase>
- A typed immutable public result reports the checked generation and transport status.
- One BlobStore read-only method connects committed authority evidence to the optional participant observation capability without concrete-adapter coupling.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compare one committed S3 generation without downloading it</name>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/read_contract.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    - src/cacheness/storage/blob_store.py: admitted read methods, authenticated manifest helpers, and injected participant ownership
    - src/cacheness/storage/composition.py: `PayloadGenerationIOProvider` structural seam and runtime capability validation
    - src/cacheness/storage/read_contract.py: frozen result/value conventions
    - src/cacheness/storage/transport_evidence.py: evidence authentication and identity binding
  </read_first>
  <behavior>
    - "For one committed S3 entry, exact head values equal persisted signed evidence and the result reports MATCH plus key/generation/locator."
    - "BlobStore queries a backend-neutral optional observation protocol and has no import or concrete type check for `ObstoreGenerationIO`."
    - "The comparison performs no get/list, handler call, authority mutation, projection, repair, or cleanup."
    - "The result explicitly states that canonical payload integrity was not verified."
  </behavior>
  <action>Define a narrow backend-neutral optional `PayloadTransportObservationProvider` protocol beside `PayloadGenerationIOProvider` in `composition.py`, exposing only exact observation for a validated locator. Add a frozen typed status/result in `read_contract.py` and a narrowly named BlobStore method implementing D-12. Resolve the committed EntrySnapshot, authenticate manifest and optional evidence, verify immutable payload-identity binding, query the optional protocol once, and compare opaque ETag, byte size, and optional version. BlobStore must not import or type-check `ObstoreGenerationIO`; structural capability is the only connection. Include the authoritative generation/locator in a result that cannot be mistaken for canonical integrity. Use existing admission/closed-store rules and preserve injected provider identity without another authority.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k transport -x</automated>
  </verify>
  <done>A developer can perform one mutation-free exact-head comparison and see which committed generation was checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make mismatch, absence, unsupported evidence, and backend failures explicit</name>
  <files>src/cacheness/storage/read_contract.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/__init__.py, tests/test_blob_store_read_contract.py, tests/test_payload_transport_evidence.py</files>
  <read_first>
    - src/cacheness/storage/read_contract.py: public failure/result vocabulary
    - src/cacheness/error_handling.py: integrity, missing, configuration, and backend errors
    - src/cacheness/storage/__init__.py: deliberate public export list
  </read_first>
  <behavior>
    - "Changed ETag/size/version returns MISMATCH without mutating the entry."
    - "A participant lacking the optional observation protocol or an evidence-free record returns UNAVAILABLE rather than a false match."
    - "Typed not-found reports ABSENT for the checked generation; permission/network/configuration errors remain typed backend failures with causes."
    - "Malformed, unsigned, or transplanted evidence fails closed as integrity error before head."
    - "After catalog or user-metadata-only CAS, the explicit comparison authenticates the preserved evidence against the unchanged generation/locator/payload identity."
  </behavior>
  <action>Complete the status matrix and public export per D-12. Normalize only exact typed not-found to an absent comparison; propagate other participant errors through the Cacheness backend hierarchy with causes. Return UNAVAILABLE for legitimately absent evidence or any custom/injected provider that lacks the optional observation protocol. Reject malformed/untrusted/binding-mismatched evidence before an object call. Add spies proving no repair/deletion/promotion/projection/list/get occurs and that ordinary `get` still downloads and verifies SHA-256 even after a prior comparison match. Replace the pre-cutover read-contract expectation that user-metadata update creates a new generation: catalog/user-metadata CAS preserves exact generation/locator/payload identity/evidence and performs no observation call; only a later explicit comparison may issue one head.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py tests/test_payload_transport_evidence.py -k 'transport or canonical' -x</automated>
  </verify>
  <done>The public comparison surface is complete, typed, mutation-free, and cannot replace canonical verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| signed authority evidence -> exact object head | The comparison must bind both observations to the same committed generation. |
| comparison result -> developer decision | A transport match must not be represented as cryptographic payload verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-07-01 | Tampering | evidence transplant | high | mitigate | Authenticate and bind evidence before head, returning the exact generation identity. |
| T-07.1-07-02 | Spoofing | ETag as content digest | high | mitigate | Use an explicit transport-only status and prove ordinary reads still SHA-256 verify. |
| T-07.1-07-03 | Repudiation | error-to-absence conversion | high | mitigate | Normalize only typed not-found; preserve other backend failures and causes. |
| T-07.1-07-04 | Elevation | read API mutates lifecycle | high | mitigate | Spy on authority/participant surfaces and permit only committed read plus exact head. |
</threat_model>

<verification>
- All status cases pass focused tests in under 30 seconds.
- The API calls no list/get/delete/promotion/reconciliation path.
- A transport match never bypasses canonical SHA-256 on `BlobStore.get`.
</verification>

<success_criteria>
- The public method identifies and compares exactly one committed generation.
- Evidence corruption and backend failures remain distinguishable from absence/mismatch.
- Transport corroboration is useful but never authoritative.
</success_criteria>

<source_coverage>
- GOAL: Make unified transport observable without changing BlobStore authority.
- REQ: STOR-02/08, BACK-03/07, SECU-03/04/05/08.
- RESEARCH: developer-facing check and signed transport evidence design.
- CONTEXT: D-03, D-10, D-11, D-12.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-07-SUMMARY.md` when done.
</output>
