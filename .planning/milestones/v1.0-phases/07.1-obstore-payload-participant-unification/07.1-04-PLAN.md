---
phase: 07.1-obstore-payload-participant-unification
plan: 04
type: execute
wave: 4
depends_on:
  - 07.1-03
files_modified:
  - src/cacheness/storage/obstore_generation_io.py
  - tests/contracts/test_obstore_generation_io.py
autonomous: true
requirements:
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-07
  - STOR-08
  - BACK-01
  - BACK-04
  - BACK-06
  - QUAL-04
  - QUAL-07
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Mocked S3 uses the same adapter and immutable-locator contract as local and memory (D-01, D-04)."
    - "S3 direct create always disables multipart and rejects payloads above the configured 128 MiB initial bound (D-02)."
    - "Production S3 requires explicit bucket and region, rejects endpoint overrides and `expected_bucket_owner`, and relies on native AWS authorization (D-16)."
    - "Collision, lost response, exact deletion, bounded listing, and error classification never turn S3 presence into authority visibility (D-10)."
  artifacts:
    - path: "src/cacheness/storage/obstore_generation_io.py"
      provides: "S3Store construction and bounded object mechanics in the shared adapter"
    - path: "tests/contracts/test_obstore_generation_io.py"
      provides: "Moto HTTP S3 deterministic qualification"
  key_links:
    - from: "src/cacheness/storage/obstore_generation_io.py"
      to: "obstore.store.S3Store"
      via: "explicit bucket/region/prefix construction and conditional object calls"
      pattern: "S3Store|conditional_put|use_multipart"
---

<objective>
Qualify Amazon S3 object mechanics deterministically through the shared obstore adapter.

Purpose: Complete the remote participant behavior without making live-AWS, cross-resource ACID, or owner-pinning claims.
Output: S3 construction policy and moto-backed immutable publication/recovery/deletion contracts.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-03-SUMMARY.md
@.planning/phases/07.1-obstore-payload-participant-unification/COVERAGE.md
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@src/cacheness/storage/obstore_generation_io.py
</context>

<interfaces>
The adapter already implements conditional create, exact snapshot, head, delete-or-prove-absence, bounded inventory, and close for LocalStore/MemoryStore. S3Store must satisfy that same interface; S3 configuration remains topology data, not lifecycle state.
</interfaces>

<artifacts_this_phase>
- S3Store becomes the third store supported by the shared adapter.
- The moto-server suite is deterministic contract evidence, explicitly distinct from Phase 8 live AWS qualification.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete one BlobStore lifecycle through moto-backed S3Store</name>
  <files>src/cacheness/storage/obstore_generation_io.py, tests/contracts/test_obstore_generation_io.py</files>
  <read_first>
    - tests/contracts/test_obstore_sdk_parity.py: approved S3Store constructor and HTTP fixture
    - src/cacheness/storage/obstore_generation_io.py: shared adapter and typed exception boundary
    - tests/contracts/test_s3_generation_io.py: current immutable generation semantics to preserve
  </read_first>
  <behavior>
    - "One injected S3 provider completes prepare, direct publish, exact snapshot verification, authority promotion, read, and exact delete through BlobStore for a built-in native handler and the Plan 03 MCAP-style custom handler."
    - "A same-locator contender yields one complete object and typed collision; no partial object or authority disagreement is visible."
    - "An accepted-then-raised create recovers from exact head/get SHA-256 plus size and does not call list."
  </behavior>
  <action>Reuse the Plan 02 moto HTTP fixture and extend `ObstoreGenerationIO` construction for S3Store with explicit bucket, prefix, region, conditional-put configuration, bounded retries, and the test-only endpoint exception. Run real BlobStore lifecycles for both a built-in native handler and the separately registered MCAP-style handler through an injected participant and an established deterministic authority, preserving the exact topology label as mocked S3 rather than live BACK-05. Assert native suffix preservation and no obstore/locator exposure. Use direct `mode="create"` with multipart disabled, exact locator observation for collision/ambiguity, and canonical digest plus size to accept only matching bytes per D-01, D-02, D-03, D-09, D-10, and D-13.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/contracts/test_obstore_generation_io.py -k s3 -x</automated>
  </verify>
  <done>The mocked-S3 participant completes one authoritative lifecycle and recovers exact response loss without list-based inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce S3 configuration, bounds, evidence, and exact maintenance behavior</name>
  <files>src/cacheness/storage/obstore_generation_io.py, tests/contracts/test_obstore_generation_io.py</files>
  <read_first>
    - .planning/phases/07.1-obstore-payload-participant-unification/07.1-CONTEXT.md: D-02, D-05, D-13, D-15, D-16
    - .planning/phases/07.1-obstore-payload-participant-unification/COVERAGE.md: S3Store capability decisions
    - .codex/skills/spike-findings-cacheness/references/backend-mechanics.md: direct-put resource and exact-delete limits
  </read_first>
  <behavior>
    - "Missing bucket or region, production endpoint override, and any owner-pinning option fail before S3Store construction."
    - "Upload over the configured cap fails before the SDK put call; multipart/copy methods are never invoked."
    - "Head exposes opaque ETag, size, and optional version; quoted or multipart-looking ETags are never treated as digests."
    - "Delete debt retires only after exact head proves absence; bounded listing is report-only and returns an opaque continuation."
  </behavior>
  <action>Add strict constructor and operation contracts for D-02, D-05, D-13, and D-16. Reject production custom endpoints and the removed owner option with `CacheConfigurationError`; allow only an explicit internal test flag to reach moto. Preserve AWS credential-chain/credential-provider use without logging secrets. Assert upload/download/list limits, opaque ETag/version observation, exact deletion with post-delete head, unrelated-object survival, and narrow error translation. Keep owner confusion as the documented residual risk mitigated by explicit bucket/region, IAM, bucket policy, and stable bucket ownership; add no signer, fork, or fallback.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/contracts/test_obstore_generation_io.py -k 's3 or expected_bucket_owner or endpoint' -x</automated>
  </verify>
  <done>Mocked S3 satisfies the bounded participant contract and the D-16 unsupported owner-pinning policy is executable and fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| configuration/credentials -> S3Store | Bucket, region, endpoint, and AWS identity determine the remote account boundary. |
| S3 response -> lifecycle engine | Object presence and metadata are evidence only; authority promotion remains visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-04-01 | Spoofing | cross-account bucket confusion | medium | mitigate | Require explicit bucket/region, native IAM/bucket policy, stable ownership guidance, and reject production endpoints; document D-16 residual risk. |
| T-07.1-04-02 | Denial of Service | direct S3 upload/list/read | medium | mitigate | Enforce pre-call upload cap, download byte count, and bounded inventory pages. |
| T-07.1-04-03 | Tampering | ambiguous create outcome | high | mitigate | Observe only the exact immutable locator and accept only canonical digest/size equality. |
| T-07.1-04-04 | Repudiation | S3 errors mapped to absence | high | mitigate | Map only documented not-found variants to absence and preserve all other causes. |
| T-07.1-04-05 | Tampering | list/delete authority confusion | high | mitigate | Use authority-attributed exact locators; list remains report-only and delete settles via exact head. |
</threat_model>

<verification>
- Mocked S3 tests pass in under 30 seconds and use real HTTP requests from obstore.
- No test result is labeled live AWS or compatible-S3 qualification.
- No owner-binding workaround or multipart/copy lifecycle exists.
</verification>

<success_criteria>
- S3Store supplies the same narrow participant primitives as local and memory.
- Security and resource policy rejects unsupported/unsafe configuration before object calls.
- Exact collision, ambiguity, deletion, and inventory semantics preserve ADR 0001 authority boundaries.
</success_criteria>

<source_coverage>
- GOAL: Add S3 to the one obstore payload participant.
- REQ: STOR-03/04/05/07/08, BACK-01/04/06, QUAL-04/07.
- RESEARCH: parity table, D-16 resolution, direct-put/resource and exception findings.
- CONTEXT: D-01, D-02, D-03, D-04, D-05, D-10, D-13, D-15, D-16.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-04-SUMMARY.md` when done.
</output>
