---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "02"
status: superseded
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/storage/blob_store.py
  - tests/test_manifest_repository_cas.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-03, STOR-05, STOR-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Create-if-absent, replace-if-exact-record, and remove-if-exact-record are atomic for JSON, memory, and SQLite reference repositories (D-02, D-18)."
    - "A stale generation or same-generation stale canonical record loses with a typed conflict and cannot overwrite or remove the winner."
    - "Same-generation user-metadata patches use exact-record/revision-token CAS so concurrent patches cannot lose updates."
  artifacts:
    - path: src/cacheness/storage/manifest_repository.py
      provides: "ManifestExpectation and truthful conditional operations for all Phase 2 local repositories"
    - path: tests/test_manifest_repository_cas.py
      provides: "Independent-instance CAS, rollback, and conditional-retirement contract"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/manifest_repository.py
      via: "authenticated raw record digest accompanies expected generation"
      pattern: "ManifestExpectation"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Make exact-record conditional publication truthful across the admitted local repositories and every same-generation metadata mutation.

Purpose: Ensure local ordering never hides a broken cross-instance authority boundary or a lost metadata update.
Output: Atomic create/replace/remove contracts for JSON, memory, and SQLite plus independent-instance proof.
</objective>

## Artifacts this phase produces

- New internal value `ManifestExpectation(generation, record_digest)` and repository methods `publish_if_expected` and `remove_if_expected` in `manifest_repository.py`.
- New test file `tests/test_manifest_repository_cas.py`.
- No new public symbols or additional files.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-01-SUMMARY.md
</context>

<interfaces>
`ManifestRepository` transports opaque canonical bytes. The lifecycle engine authenticates current records; adapters compare the supplied exact expectation inside the same lock/transaction as publication. `BlobStore.update_metadata()` currently re-signs `user_metadata` and must publish conditionally without changing generation.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact-record CAS for every admitted local repository</name>
  <depends_on>Plan 03-01 defines the expectation and JSON tracer behavior.</depends_on>
  <files>src/cacheness/storage/manifest_repository.py, tests/test_manifest_repository_cas.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>src/cacheness/metadata.py</file>
    <file>tests/test_blob_manifest_backends.py</file>
    <file>tests/test_sqlite_concurrency.py</file>
  </read_first>
  <behavior>
    - "Absence expectation creates exactly once; a second independent instance receives CacheBlobLifecycleConflictError and winner bytes remain unchanged."
    - "Expected generation plus exact canonical-record digest rejects a same-generation stale update."
    - "SQLite compatibility projection and canonical BLOB update or roll back in one transaction."
    - "JSON refreshes under a short OS-backed compare/publication boundary; memory compares under its backend lock."
    - "Conditional remove cannot retire a tombstone or record replaced after the caller's authenticated read."
  </behavior>
  <action>Build the full Wave 0 CAS matrix from the Phase 2 raw-repository fixtures. Strengthen `ManifestExpectation` to require both the authenticated generation and SHA-256 digest of the exact canonical bytes, with a distinct absence form. Implement compare-and-publish/remove in one critical section for in-memory, one refresh/compare/durable-replace OS-backed boundary for JSON, and one SQLite transaction covering the compatibility projection plus canonical BLOB. Repositories must not decode unauthenticated manifests to discover generation. Stale comparisons raise the existing typed lifecycle conflict and preserve winner bytes and projections. If a topology cannot implement the primitive truthfully, fail with the typed unsupported lifecycle capability instead of falling back to check-then-write per D-18.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_manifest_repository_cas.py -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_blob_manifest_backends.py -x</automated>
  </verify>
  <acceptance_criteria>All three local adapters pass create, exact replace, same-generation stale record, independent-instance, rollback, and conditional-remove cases; every loser leaves exact winner bytes intact.</acceptance_criteria>
  <done>The repository boundary, not process-local locks, determines each local authority winner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route metadata patching and lifecycle retirement through exact CAS</name>
  <depends_on>Task 1 supplies exact-record conditional publication and removal.</depends_on>
  <files>src/cacheness/storage/blob_store.py, tests/test_manifest_repository_cas.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>src/cacheness/storage/manifest.py</file>
  </read_first>
  <behavior>
    - "Two callers patching the same generation from the same record yield one winner and one typed conflict, not last-write-wins loss."
    - "A patch authenticates committed authority, changes only user_metadata, re-signs, and publishes against the exact observed record."
    - "Conditional retirement never removes a newer committed or tombstoned record."
  </behavior>
  <action>Replace unconditional `put_raw` in `BlobStore.update_metadata` with exact expectation CAS built from the authenticated canonical record. Preserve the public boolean success contract for a winning patch and propagate typed conflict for a stale patch. Route lifecycle final record retirement through `remove_if_expected`, re-reading/authenticating where necessary rather than trusting a cached generation alone. Add deterministic event/barrier tests using two independent stores; do not rely on sleeps or one shared local lock.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_manifest_repository_cas.py -k "metadata or retire or independent" -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "update_metadata" -x &amp;&amp; uv run ruff check src/cacheness/storage/manifest_repository.py src/cacheness/storage/blob_store.py tests/test_manifest_repository_cas.py tests/test_blob_store_read_contract.py</automated>
  </verify>
  <acceptance_criteria>Forced same-generation patch races cannot lose a winner; stale retirement cannot remove a changed record; compatible single-caller metadata behavior remains green.</acceptance_criteria>
  <done>Every authority mutation uses exact-record-equivalent CAS rather than generation-only or unconditional publication.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V2 business logic | Exact observed-record preconditions gate every authority mutation. | Independent-instance stale expectation tests. |
| V15 architecture | Backend CAS, not local locking, is the cross-instance correctness boundary. | JSON/SQLite dual-instance tests. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated expectation -> repository transaction | The adapter may compare opaque trusted expectation tokens but may not interpret untrusted manifest fields. |
| independent store instance -> shared metadata | Cross-instance contenders must observe one atomic winner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05 | Tampering | manifest CAS | high | mitigate | Exact generation plus canonical-record digest compare and atomic publish/remove preserve the winner. |
| T-03-06 | Tampering / Repudiation | same-generation metadata update | high | mitigate | Exact-record CAS exposes stale patches as typed conflicts with deterministic tests. |
| T-03-07 | Denial of Service | short repository critical section | low | accept | Locks cover only refresh/compare/publication, not serialization or payload I/O. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-05 | `uv run pytest -q -o log_cli=false tests/test_manifest_repository_cas.py -k "independent or stale or rollback or retire" -x` |
| T-03-06 | `uv run pytest -q -o log_cli=false tests/test_manifest_repository_cas.py -k "metadata" -x` |
</threat_model>

<verification>
Run repository CAS, Phase 2 repository regression, metadata patch regression, and targeted Ruff. Winner raw bytes and compatibility projections are postconditions, not exception-only assertions.
</verification>

<success_criteria>
- All admitted local repositories expose truthful exact CAS.
- Same-generation metadata patches cannot overwrite unseen changes.
- Stale retirement cannot remove a winner.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-02-SUMMARY.md` when done.
</output>
