---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "24"
type: execute
wave: 23
depends_on: ["03-23"]
files_modified:
  - src/cacheness/core.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/read_contract.py
  - tests/test_unified_cache_lifecycle_authority.py
  - tests/test_unified_cache_adversarial_lifecycle.py
  - tests/test_cached_query_meta.py
  - tests/test_cache_integrity.py
  - docs/STORAGE_INITIALIZATION.md
autonomous: false
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-07]
estimate:
  tokens: 36000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Canonical cache put/get/TTL consume the supported BlobStore engine interface, not private admission, raw authority rows, separate snapshots or deferred cleanup (D-24, D-33, D-36)."
    - "Canonical cache policy uses authenticated metadata for its observed generation; corrupt or stale projection fields cannot select a blob for deletion (CR-03; D-03, D-15, D-16, D-34)."
    - "A failed optional export does not revoke or obscure a committed generation; explicitly requested external custom-link failure reports a typed post-commit partial outcome."
    - "Separate cache and non-cache instances reuse the same engine without TTL/clear affecting the non-cache namespace (D-33)."
    - "Recognized legacy read-only formats and configured signing remain protected; no new dual-written payload lifecycle appears."
  artifacts:
    - path: src/cacheness/core.py
      provides: "Canonical cache policy over supported BlobStore entries/receipts"
    - path: tests/test_unified_cache_lifecycle_authority.py
      provides: "Canonical cache tracer and separate-instance isolation"
    - path: tests/test_unified_cache_adversarial_lifecycle.py
      provides: "Projection outage/corruption and exact-generation policy regressions"
    - path: docs/STORAGE_INITIALIZATION.md
      provides: "Approved post-commit/projection/custom-link compatibility outcomes"
  key_links:
    - from: src/cacheness/core.py
      to: src/cacheness/storage/blob_store.py
      via: "Canonical put_entry/open_entry and exact conditional delete"
      pattern: "put_entry|open_entry|expected="
    - from: tests/test_cached_query_meta.py
      to: src/cacheness/core.py
      via: "Strict projection parsing without authority deletion"
      pattern: "METADATA_CORRUPT|entry_signature|cache_key_params"
  prohibitions:
    - "[UNVERIFIED] No canonical deletion, rollback, or replacement from optional projection/link failure or transient read failure."
    - "[UNVERIFIED] No canonical read repair/retry loop across two catalogs and no new generic cache-policy plugin."
    - "[UNVERIFIED] No removal of strict signing, path containment, legacy compatibility, or exact-generation cleanup protections."
---

<objective>
Close the structural CR-03/audit gap by making the cache use the BlobStore engine's supported interface. This is the early local composition proof, not all Phase 6 policy/backend work.
Output: simpler canonical cache workflows, explicitly derived catalog behavior, exact-generation policy and preserved compatibility.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/phase3-architecture-audit-2026-09-06.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-GAP-REPLAN.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
</context>

<planner_assumptions>
SQLite/filesystem, initialized one host; memory only one process. STOR-03/07 integrity requires one generation; STOR-04/05 recovery requires promotion to remain authoritative despite external failure. Backend/conflict/timeout results remain distinct, performance separate. CACH-01/02/03/06 and BACK-07 receive partial early evidence only; their owner phases retain completion.
</planner_assumptions>

<tasks>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve observable derived-state failure semantics</name>
  <reversibility rating="one-way">Public failure/acknowledgment semantics must not change silently.</reversibility>
  <files>docs/STORAGE_INITIALIZATION.md</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/custom_metadata.py</file>
    <file>tests/test_unified_cache_adversarial_lifecycle.py</file>
    <file>tests/test_cached_query_meta.py</file>
  </read_first>
  <action>Characterize projection publish failure, strict-signing read failure, requested custom_metadata failure, concurrent cache close, and typed cleanup outcomes. Present the exact changes: canonical reads use authenticated authority metadata; optional export failure keeps the successful key with structured diagnostic; explicitly requested external ORM-link failure raises a typed post-commit partial outcome containing committed identity and cause. After the engine operation completes, a concurrent close may prevent remaining derived work and must use those same declared outcomes rather than hold storage admission across a second catalog. BlobStore's own in-flight operation/close contract remains intact. No valid canonical deletion or automatic corrupt-projection repair. Direct projection/query operations still fail closed on malformed evidence. Confirm which legacy behaviors are preserved and which assertions change.</action>
  <decision>Approve this precise post-commit compatibility contract or stop for a smaller revised adapter contract. Do not silently choose result semantics or weaken signing.</decision>
  <options><option id="approve">Approve the enumerated optional-projection and requested-custom-link outcomes.</option><option id="revise">Revise the compatibility adapter contract before cutover.</option></options>
  <resume-signal>Explicit user approval or requested revision after the before/after behavior table.</resume-signal>
  <verify>Record actual approval and before/after behavior table before cutover.</verify>
  <done>User-visible acknowledgment and custom-metadata semantics are explicit.</done>
</task>
<task type="tracer" tdd="true">
  <name>Task 2: Run cache put/read/expiry entirely through the supported engine seam</name>
  <depends_on>Task 1 approved and Plan 23 interface verified.</depends_on>
  <reversibility rating="costly">Canonical path cutover; recognized legacy adapter remains.</reversibility>
  <files>src/cacheness/core.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/read_contract.py, tests/test_unified_cache_lifecycle_authority.py, tests/test_cache_integrity.py</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/read_contract.py</file>
    <file>tests/test_unified_cache_lifecycle_authority.py</file>
    <file>tests/test_cache_integrity.py</file>
  </read_first>
  <action>Canonical put uses put_entry; get uses open_entry with authenticated metadata for TTL and configured signature authorization before read(). Keep current cache signature fields in the signed manifest, with any retained pre-sign transform pure and unable to change identity/locator/digest. Remove the cache's duplicate guarded snapshot/hash plus fresh BlobStore.get lookup. Keep key derivation, TTL and counters as cache policy, but route expiry through delete(expected=observed expectation). Snapshot metadata, not a reread projection, defines created_at/type/key diagnostics/signature.

Change shared canonical observation helpers so eviction/predicate/clear callers cannot obtain deletion authority from projection fields. Preserve exact conditional deletion and BlobStore's owned-resource close. Do not keep storage admission open across derived catalog work. Refuse new cache calls after close begins; a close that prevents remaining post-commit export/link work uses the explicitly approved diagnostics/partial-outcome contract. Snapshot contexts retain their existing BlobStore lifetime guarantee. Test these cases without adding a new lock, registry, queue or cross-catalog close barrier; if that requires a stronger public close promise, stop at the compatibility checkpoint rather than invent another coordinator.

Preserve recognized legacy read-only paths and errors as isolated adapters; canonical exceptions do not fall through broad catches that retire valid entries. Use read_contract classification for backend/timeout/conflict/integrity policy; transient/unsupported/projection failures preserve canonical data. A strict integrity failure may become the existing recorded miss, never relax verification.

Tracer: initialize cache and separate direct store, write same logical diagnostic key to both, read/reopen, expire cache via controlled clock, verify only cache entry is deleted. Spy on BlobStore interface to prove delegation and one snapshot/handler read; make cache-owned raw snapshot and private put/admission/settle calls fail if canonical execution reaches them. Prove stored None is still an entry internally, without claiming the full decorator None fix.</action>
  <acceptance_criteria>Canonical put/get/TTL work through supported methods; no facade raw authority/read-repair or duplicate payload snapshot is required. Strict signing precedes deserialization. TTL deletion with stale expectation cannot delete a newer generation. Separate instance data survives cache expiry/clear.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_unified_cache_lifecycle_authority.py tests/test_cache_integrity.py tests/test_blob_store_translation_seam.py tests/test_blob_store_legacy_contract.py -o log_cli=false</automated></verify>
  <done>One local cache slice demonstrates actual BlobStore-engine reuse, not just shared helpers.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Make projections optional and retire redundant facade lifecycle sequencing</name>
  <depends_on>Task 2 tracer passes.</depends_on>
  <reversibility rating="costly">Remove canonical-only coordination; retain explicit legacy/query adapters.</reversibility>
  <files>src/cacheness/core.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/lifecycle.py, tests/test_unified_cache_adversarial_lifecycle.py, tests/test_cached_query_meta.py, docs/STORAGE_INITIALIZATION.md</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>tests/test_unified_cache_adversarial_lifecycle.py</file>
    <file>tests/test_cached_query_meta.py</file>
    <file>tests/test_projection_mutation_contract.py</file>
    <file>tests/test_projection_sql_atomicity.py</file>
  </read_first>
  <action>Move optional projection publication after complete engine write using only the committed receipt. Implement the approved diagnostics/partial-outcome semantics without a second durable queue. Keep generation-conditional derived row/link updates; stale receipts never borrow a newer entry and explicit custom-link failure never revokes the blob. Current authority metadata is sufficient for rebuildable exports; document explicit rebuild/report path rather than repairing corrupt rows during get. No cleanup waiting for projection success.

Delete canonical-only usages and then dead definitions of _put_with_result facade entry, defer_cleanup/settle_put_cleanup, _repair_projection_after_failed_put and canonical _authority_snapshot_entry compare/repair loops where no other supported caller needs them. Retain an actually used pure signing/legacy formatter rather than rename dead coordination. Reference-search each deletion and preserve unrelated adapters.

Test valid canonical authority with corrupt, absent, stale and unavailable projection under strict signing. Canonical get returns authenticated data without rewriting corrupt bytes; explicit query/point/list projection reads raise METADATA_CORRUPT when observing malformed rows. Revise Plan 21's intermediate get assertion, never its preservation checks. Inject optional projection transport failure and requested ORM-link failure after promotion, a newer writer before export, stale TTL/size/predicate candidates, and transient backend read errors. Verify canonical/payload/link evidence remains correct, caller outcome is explicit, and debt reconciliation proceeds independently.</action>
  <acceptance_criteria>Projection absence/corruption/failure cannot delete or invalidate canonical data or suppress cleanup. No normal canonical get needs repair of a second catalog. Optional export failure and requested custom-link partial failure match the approved contract. Required old projection CAS protections remain for derived links, not lifecycle authority.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_unified_cache_adversarial_lifecycle.py tests/test_cached_query_meta.py tests/test_projection_mutation_contract.py tests/test_projection_sql_atomicity.py tests/test_query_meta.py -o log_cli=false</automated></verify>
  <done>CR-03 has both parser and architectural protection, and obsolete canonical facade coordination is actually removed.</done>
</task>
</tasks>
<threat_model>
Derived SQL rows, cache signatures and custom links cross the storage/policy seam. Threats: corrupt fields delete valid data, stale policy deletes a winner, broad catches turn transient failure into data loss. Mitigations: authenticated same-generation results, exact expected delete, pure pre-deserialization signing checks, typed non-destructive errors, conditional derived links. Fault tests cover each. No trusted-payload boundary weakening.
</threat_model>
<verification>Run named public/legacy tests, scoped Ruff and Phase 3 delta from disposable isolated fixtures. Record removed responsibilities and retained adapters, not line-count targets. Stop if compatibility needs two authoritative writes, another gate or a new protocol; do not expand this plan into all Phase 6.</verification>
<success_criteria>Local canonical cache composition is one-engine and non-destructive under projection failure. User-approved compatibility outcomes are tested. BACK-07 and CACH requirements remain pending for their full later-phase scope.</success_criteria>
<output>Create 03-24-SUMMARY.md with approval, supported-call proof, removed private sequencing, projection/custom-link outcomes, fault tests and exact commands/commit.</output>
