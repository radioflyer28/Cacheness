---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "08"
type: execute
wave: 8
depends_on: ["03-07"]
files_modified:
  - src/cacheness/storage/coordination.py
  - src/cacheness/storage/path_security.py
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store_close_contract.py
  - tests/test_filesystem_containment.py
  - tests/test_manifest_repository_cas.py
  - tests/test_public_api_contract.py
  - tests/test_phase3_scheduler_retirement.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Retained filesystem helpers provide only containment, immutable payload publication/snapshot, fsync, hashing/size, and exact durable deletion; scheduler control and lock authority are absent (D-01, D-06, D-31)."
    - "Coordination contains only bounded process-local key ordering and instance admission/close ownership, never cross-process correctness claims (D-17 through D-20)."
    - "Manifest repository code is projection/import compatibility only; BlobStore composes exactly one LifecycleAuthority with no feature flag or fallback (D-24, D-26, D-30)."
  artifacts:
    - path: src/cacheness/storage/coordination.py
      provides: "Bounded process-local key ordering and instance admission only"
    - path: src/cacheness/storage/path_security.py
      provides: "Retained Phase 1/2 payload containment and durable I/O primitives"
    - path: src/cacheness/storage/manifest_repository.py
      provides: "Revisioned JSON projection/import compatibility boundary only"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Sole lifecycle authority composition"
      pattern: "LifecycleAuthority"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No store/family/file lock authority, staged control promotion, manifest CAS, or scheduler helper may remain."
    - "[FLAGGED/UNVERIFIED] Retiring scheduler helpers must not weaken payload containment or durable exact deletion."
---

<objective>
Trim retained shared modules after scheduler deletion and seal the transactional authority as the only lifecycle engine (D-24, D-26, D-31).

Purpose: Separate high-risk core deletion from careful preservation of Phase 1/2 security and compatibility helpers.
Output: Scheduler-free coordination/path security, projection-only manifest compatibility, and one-authority runtime composition.
</objective>

## Artifacts this phase produces

- Minimal bounded process-local coordination.
- Preserved native-payload containment, immutable I/O, fsync, digest, and exact delete primitives.
- Projection-only manifest repository and one-authority BlobStore composition.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-07-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trim lock/control helpers while preserving payload safety</name>
  <reversibility rating="costly">D-31 removes custom POSIX/Win32 lock authority; retained security helpers remain compatibility-critical.</reversibility>
  <files>src/cacheness/storage/coordination.py, src/cacheness/storage/path_security.py, tests/test_blob_store_close_contract.py, tests/test_filesystem_containment.py, tests/test_phase3_scheduler_retirement.py</files>
  <read_first>
    <file>src/cacheness/storage/coordination.py</file>
    <file>src/cacheness/storage/path_security.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>tests/test_filesystem_containment.py</file>
    <file>tests/test_blob_store_close_contract.py</file>
  </read_first>
  <behavior>
    - "Store/family/file lock authority, inode/handle registries, xattr reservation, and staged control promotion are absent on POSIX and Windows."
    - "Traversal/symlink/topology rejection, immutable exclusive payload creation, guarded snapshots, fsync, digest/size, and durable exact delete retain Phase 1/2 behavior."
    - "Coordination entries retire/bound growth, distinct keys overlap, and close admission releases only owned resources."
  </behavior>
  <action>Inventory production call sites, then trim `coordination.py` to `KeyCoordinatorRegistry` and `InstanceAdmission` semantics only. Remove custom POSIX/Win32 file/store/family lock authority and any inode/handle/xattr reservation used as authority. In `path_security.py`, preserve only the containment, no-follow/wrong-object, immutable payload publication/snapshot, fsync, digest/size, and exact durable deletion primitives proven by Phases 1/2; delete scheduler-control helpers. Update tests around retained public behavior and include Windows symbol/branch denial in the retirement gate.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_blob_store_close_contract.py tests/test_filesystem_containment.py tests/test_phase3_scheduler_retirement.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>All scheduler and platform-lock helpers are absent; payload containment/durability regressions remain green; bounded local coordination and ownership-aware close still work.</acceptance_criteria>
  <done>Retained shared helpers are scheduler-free without weakening native payload safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce manifest compatibility and seal one-authority composition</name>
  <reversibility rating="costly">D-24/D-26 make LifecycleAuthority the sole truth while preserving JSON as a public compatibility projection.</reversibility>
  <files>src/cacheness/storage/manifest_repository.py, src/cacheness/storage/blob_store.py, tests/test_manifest_repository_cas.py, tests/test_public_api_contract.py, tests/test_phase3_scheduler_retirement.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_manifest_repository_cas.py</file>
    <file>tests/test_public_api_contract.py</file>
    <file>tests/test_phase3_scheduler_retirement.py</file>
  </read_first>
  <behavior>
    - "Manifest repository has no lifecycle CAS, inventory, pending-control, lock, or recovery responsibility."
    - "BlobStore constructs/injects exactly one LifecycleAuthority and has no feature flag, import fallback, or legacy engine."
    - "Projection/import compatibility preserves characterized public shapes but cannot answer canonical lifecycle reads or rebuild missing authority."
  </behavior>
  <action>Remove remaining scheduler CAS/inventory responsibilities from `manifest_repository.py`, retaining only Plan 06 revisioned projection/import compatibility. Simplify BlobStore runtime composition to the one selected authority. Extend tests across constructor state, monkeypatchable factories, public operations, projection corruption, and fresh/reopened artifacts; any alternate authority read, JSON recovery, or scheduler fallback must fail the negative gate.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_manifest_repository_cas.py tests/test_public_api_contract.py tests/test_phase3_scheduler_retirement.py tests/test_blob_store_read_contract.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Manifest code is projection-only; every BlobStore operation reaches one authority; missing/corrupt authority never falls back to JSON or scheduler state.</acceptance_criteria>
  <done>Runtime composition and retained compatibility code cannot resurrect dual authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| BlobStore → retained filesystem helpers | Containment and durable exact deletion must survive helper retirement. |
| JSON compatibility → canonical authority | Derived data must never arbitrate lifecycle truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08-01 | Elevation of Privilege | retained path helpers | high | mitigate | Preserve no-follow containment and exact durable payload operations with regression tests. |
| T-03-08-02 | Tampering | JSON/manifest compatibility | high | mitigate | Authority-only reads and negative fallback tests; projection remains revision-bound. |
| T-03-08-03 | Denial of Service | local coordination | medium | mitigate | Bounded/retired key entries and no store-wide payload lock. |
</threat_model>

<verification>
Run the exact retained-helper, projection, public-API, and retirement tests plus the checked-in Phase 3 Ruff delta gate.
</verification>

<success_criteria>
- Scheduler helper semantics are absent from retained modules.
- Phase 1/2 payload safety remains green.
- BlobStore has exactly one LifecycleAuthority and JSON remains projection-only.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-08-SUMMARY.md` when done.
</output>
