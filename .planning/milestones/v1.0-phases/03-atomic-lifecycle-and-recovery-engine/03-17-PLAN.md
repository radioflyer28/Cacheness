---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "17"
type: execute
wave: 17
depends_on: ["03-16"]
files_modified:
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - benchmarks/lifecycle_authority_benchmark.py
  - benchmarks/lifecycle_authority_baseline.json
  - tests/test_sqlite_authority_admission.py
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Two independent authority instances and two spawned processes can start mutations against a genuinely absent root; the loser joins only a bounded, reclassified valid winner and both operations reach deterministic authority outcomes."
    - "A wrong-object or symlink substitution at root, reserved directory, or database leaf during bootstrap is rejected unchanged and never joined as a winner."
    - "SqliteBackend has explicit idempotent close ownership and interpreter shutdown emits no destructor exception."
    - "Same-process SQLite authority writers across one or many facade instances enter only the short authority transaction through a fair per-authority admission queue; queue service wait, scheduler dispatch, SQLite busy-handler wait, writer hold, and post-commit cleanup are separately observable while one existing absolute authority deadline bounds admission plus database acquisition."
    - "Measured high-contention scheduling fits the unchanged 0.187-second authority deadline without a new timeout setting or renewed budget; a genuinely held database writer still ends in CacheBlobLifecycleTimeoutError, while payload I/O and cleanup for distinct keys remain concurrent."
    - "Every CR-01 through CR-09 schedule is forced through real facade/backend paths, all focused/full/compatibility/Ruff gates pass, and current Darwin Windows evidence remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false."
  artifacts:
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Bounded cross-instance/process fresh-root winner reclassification"
    - path: src/cacheness/metadata.py
      provides: "Explicit idempotent SqliteBackend close without unsafe finalizer work"
    - path: tests/test_sqlite_bootstrap_concurrency.py
      provides: "Unseeded two-instance/process bootstrap and substitution schedules"
    - path: tests/test_phase3_gap_acceptance.py
      provides: "Cross-plan adversarial assertions and release-gate inventory"
    - path: benchmarks/lifecycle_authority_benchmark.py
      provides: "Production-path contention stage measurements and additive baseline verification"
    - path: benchmarks/lifecycle_authority_baseline.json
      provides: "Checked-in contention-stage distribution and provenance without a new deadline or relaxed legacy envelope"
    - path: tests/test_sqlite_authority_admission.py
      provides: "Deterministic facade/authority contention, timeout, fairness, and cleanup-overlap schedules"
  key_links:
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_sqlite_bootstrap_concurrency.py
      via: "Named bootstrap hooks force both actors past missing classification before root/leaf winner selection"
      pattern: "bootstrap.*(hook|classif|winner)|FileExistsError"
    - from: src/cacheness/metadata.py
      to: tests/test_phase3_gap_acceptance.py
      via: "Context-managed explicit close is idempotent and a subprocess exits without finalizer diagnostics"
      pattern: "close|__exit__|Exception ignored"
    - from: tests/test_phase3_gap_acceptance.py
      to: tests/test_unified_cache_adversarial_lifecycle.py
      via: "Final acceptance enumerates and executes the real-path CR-01 through CR-09 schedule matrix"
      pattern: "CR-0[1-9]|gap.*acceptance"
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_sqlite_authority_admission.py
      via: "Named queue/grant/begin/acquired/commit/release observations force and classify every contention stage through real UnifiedCache operations"
      pattern: "admission|scheduler|begin.*wait|writer.*hold|cleanup"
    - from: benchmarks/lifecycle_authority_benchmark.py
      to: benchmarks/lifecycle_authority_baseline.json
      via: "The benchmark records a fixed-cardinality public contention distribution, verifies its capture provenance, and preserves the existing 0.187-second deadline plus every pre-existing release envelope"
      pattern: "contention|admission|authority_busy_timeout_seconds|verify_regression"
  prohibitions:
    - "[UNVERIFIED] Fresh-root joining cannot accept an unclassified object, symlink, arbitrary established store, or unbounded wait."
    - "[UNVERIFIED] SQLite contention cannot be hidden by increasing or resetting the existing busy timeout, adding a second admission budget, sleeping and retrying, swallowing typed failures, serializing payload or cleanup work, or loosening a checked-in legacy performance envelope."
    - "[UNVERIFIED] Darwin UNAVAILABLE evidence cannot be promoted to native Windows qualification."
---

<objective>
Close fresh-root authority bootstrap races, remove unsafe SQLite finalization, replace nondeterministic same-process SQLite writer contention with measured bounded short-transaction admission, and run one adversarial release acceptance matrix over all blockers.

Purpose: Even correct lifecycle and projection transactions cannot converge if independent first mutators fail at root creation, same-process writers spend the SQLite lock deadline waiting for Python scheduling, or shutdown diagnostics obscure process-level evidence. The final plan proves every repaired interleaving through production facade/backend paths and reruns Phase 3's exact release gates.
Output: Safe bounded bootstrap winner joining, explicit SqliteBackend close behavior, FIFO SQLite writer admission under the existing authority deadline with stage diagnostics, complete CR/WR regression coverage, and fresh full-suite/compatibility/Ruff evidence without a Windows qualification claim.

Decision fidelity for the retained implementation and final regression gate: D-01: immutable generations; D-02: exact expected-generation CAS; D-03: committed-only coherent reads; D-04: promotion as authority point. D-05: bounded intent; D-06: validated provenance; D-07: pre/post-promotion residue classification; D-08: typed cleanup failure. D-09: signed tombstone delete; D-10: bounded clear snapshot; D-11: idempotent ownership-aware cleanup; D-12: ownership-aware close. D-13: dry-run reconciliation; D-14: resumable apply; D-15: evidence-proven repair; D-16: no payload-deserialization guessing. D-17: bounded retiring local coordination; D-18: backend CAS remains correctness boundary; D-19: deterministic same-key conflict; D-20: distinct-key independence. D-21: trusted local-owner boundary; D-22: limited Windows topology. D-23: discarded file scheduler stays retired; D-24: one deep lifecycle authority; D-25: payloads remain outside authority transactions; D-26: stdlib SQLite remains the local authority; D-27: no filesystem scheduler in other adapters. D-28: only short SQLite commits serialize; D-29: evidence-derived bounds and indexed reconciliation; D-30: replacement behavior tests stay on the authority/BlobStore surface; D-31: no platform lock-file authority; D-32: native Windows qualification remains deferred and unclaimed.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-16-SUMMARY.md
@src/cacheness/storage/sqlite_lifecycle_authority.py
@src/cacheness/metadata.py
@benchmarks/lifecycle_authority_benchmark.py
@benchmarks/lifecycle_authority_baseline.json
</context>

## Partial Execution Baseline

Tasks 1 and 2 are already implemented and committed. They are intentionally absent from the executable `<tasks>` list so resume cannot repeat them: Task 1 is represented by `ac92708`, `91b0ea8`, `abc54f7`, and `3de7214`; Task 2 is represented by `4fb7a9d` and `c8203d8`. Preserve those commits and their source unchanged. Task 3's final gate re-runs their focused tests as regressions, but only Task 3 may produce a new implementation commit and the renewed full-suite acceptance claim.

<tasks>

<task type="auto" tdd="true">
  <name>Task 3: Measure and bound fair SQLite authority writer admission</name>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, benchmarks/lifecycle_authority_benchmark.py, benchmarks/lifecycle_authority_baseline.json, tests/test_sqlite_authority_admission.py</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/core.py</file>
    <file>tests/test_sqlite_concurrency.py</file>
    <file>tests/test_sqlite_concurrency_temp.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
    <file>benchmarks/lifecycle_authority_benchmark.py</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
  </read_first>
  <behavior>
    - "A barrier-driven public UnifiedCache schedule with at least eight distinct-key writers reproduces the former deadline exhaustion, records connection preflight, FIFO queue service, eligible-thread dispatch, SQLite busy-handler wait, transaction hold, and payload/cleanup time as distinct non-negative stages, and completes within the existing authority deadline while every entry remains readable."
    - "Independent warmed SqliteLifecycleAuthority and UnifiedCache instances resolving to the same canonical authority path share FIFO admission for ordinary SQLite lifecycle write transactions only. Each ticket completes or exhausts the one absolute deadline created at `_transaction` entry; cancellation or pre-grant close removes its exact ticket and wakes the next waiter without leaking registry state."
    - "A writer paused after becoming FIFO-eligible but before BEGIN IMMEDIATE demonstrates that scheduler dispatch is charged to the same original absolute deadline. Connection setup, queueing, dispatch, and SQLite lock acquisition never reset, extend, or replace that deadline."
    - "An independent sqlite3 connection or spawned process that actually holds BEGIN IMMEDIATE beyond 0.187 seconds still causes CacheBlobLifecycleTimeoutError with stage, elapsed, and configured-bound context; no retry loop, sleep-based success condition, or exception swallowing converts it to a miss/success."
    - "A put paused during native payload publication or post-promotion cleanup does not own writer admission: another distinct-key facade put reaches and commits its short authority transition before the pause is released. Same-key racing puts still resolve through exact authority CAS/lineage semantics."
    - "Two spawned processes remain arbitrated only by SQLite and exact authority CAS; the process-local FIFO gate neither coordinates them nor changes their winner/conflict/typed-timeout outcomes."
    - "The benchmark baseline additively records fixed contention cardinality, separate capture command/source/environment provenance, and non-overlapping stage distributions; its benchmark-only contention envelope is derived from measured p99 plus a named multiplier and must fit inside the unchanged 0.187-second authority deadline. Every pre-existing metric, derived configuration value, provenance record, and release envelope remains byte-for-byte unchanged."
    - "The three previously nondeterministic public stress nodes complete, the distinct-key overlap envelope remains satisfied, and a deliberately stalled admission owner produces a bounded typed timeout rather than an unbounded queue."
  </behavior>
  <action>Start RED with `tests/test_sqlite_authority_admission.py` and deterministic Events/Barriers, a controllable monotonic-clock/wait seam, and bounded joins; wall-clock sleeps and repeated probabilistic attempts are not acceptance evidence. Add a private process-local registry in `sqlite_lifecycle_authority.py` keyed by the canonical resolved authority database path. The registry contains independent per-path FIFO ticket gates: its one module lock protects only map/refcount changes and never serializes transactions from different authority paths. A per-path gate coordinates only same-process ordinary lifecycle write transactions from immediately before `BEGIN IMMEDIATE` through COMMIT/ROLLBACK and transaction-finally accounting. Connection classification/configuration/schema validation happens before this gate; bootstrap and versioned schema initialization retain their existing bounded SQLite/O_EXCL protocol and are not reclassified as ordinary admission. The gate is not lifecycle authority and never spans handler staging, payload publication, verification, facade projection, custom-metadata work, post-promotion debt cleanup/reclamation, reads, or reconciliation orchestration outside an individual authority transaction.

Use one FIFO ticket/Condition gate shared by independent warmed authority instances. Define lock ordering exactly: acquire the registry lock only to obtain/refcount or retire a gate; release it before acquiring/waiting on the gate Condition; release the Condition before reacquiring the registry lock for refcount retirement. Never hold either coordination lock while calling SQLite, payload code, or a test/metrics callback. Mark the head eligible atomically under the Condition, then invoke any observer outside it; non-head waiters cannot overtake. On success, callback failure, timeout, cancellation, or pre-grant authority close, remove the exact ticket and notify the next waiter in `finally`; after an owner's SQLite transaction finishes, clear ownership and notify before retiring the reference. Delete only an ownerless, queue-empty, zero-refcount gate so arbitrary cache paths cannot grow the registry without bound. Direct `close()` marks the instance closed and wakes its queued tickets but never steals an active transaction; normal BlobStore close continues to drain instance admission before authority close. Test two different authority paths committing concurrently to prove the registry lock is not a process-global transaction gate, and test two independent instances on one path for FIFO order and retirement. This is only local ordering around SQLite's own single-writer commit; SQLite plus expected-lineage CAS remains the sole cross-process/correctness authority per D-18/D-24/D-26.

Create exactly one `absolute_deadline` at `_transaction` entry using the existing `_deadline(deadline)` behavior and unchanged `LifecycleLimits.authority_busy_timeout_seconds == 0.187`. The same deadline covers connection preflight, FIFO wait, eligible-thread dispatch, and SQLite lock acquisition; pass only `_remaining(absolute_deadline)` to SQLite. Do not add `authority_writer_admission_timeout_seconds`, reset the clock after grant, sum per-stage allowances, or derive a new runtime limit. A pre-grant expiry removes the exact ticket and raises `CacheBlobLifecycleTimeoutError` with `stage=writer_admission`; expiry after eligibility but before the SQLite attempt uses `stage=scheduler_dispatch`; genuine busy/locked expiry after `BEGIN IMMEDIATE` is attempted uses `stage=sqlite_busy`. Preserve the original sqlite3 cause for the latter and include sanitized elapsed/one-bound context for all stages. No retry loop, contention sleep, public API change, or fallback-to-miss/success is permitted. A spawned-process/external-connection schedule must prove the local gate does not replace SQLite arbitration and that a real held writer still times out under the original bound.

Provide an optional internal observer, inert when unset, with timestamped events `sqlite.connection_preflight.started`/`.finished`, `writer_admission.enqueued`, `.eligible`, `.granted`, `sqlite.begin.attempt`, `.acquired`/`.timed_out`, `transaction.finished`, and `writer_admission.released`. Use one monotonic clock and non-overlapping definitions: connection preflight is transaction entry→enqueue; queue service is enqueue→eligible; eligible-thread dispatch is eligible→BEGIN attempt; SQLite busy-handler wait is BEGIN attempt→acquired/typed timeout; writer hold is acquired→COMMIT/ROLLBACK finish. Existing lifecycle hooks measure payload publication and cleanup separately. Test observers may block only outside coordination locks and must not affect production ordering when absent.

Extend the checked-in benchmark additively, using real public UnifiedCache/BlobStore operations at one fixed recorded worker/operation cardinality. Add a one-time `--extend-contention-baseline` command that first validates the existing document, captures only the new contention section, records its exact command, source revision, Python/SQLite/platform/filesystem facts, cardinality, repetitions, warmups, and all non-overlapping stage distributions, atomically upgrades the schema, and refuses to overwrite an existing contention section. Do not run whole-baseline recalibration. Preserve all legacy raw evidence/provenance, derived configuration (including `authority_busy_timeout_seconds: 0.187`), and release envelopes byte-for-byte. Add a named positive benchmark-only multiplier with rationale for the contention p99 envelope; validation must prove its derivation, prove that envelope does not exceed 0.187, and reject any legacy drift. Verify mode remeasures contention, requires p99 within that benchmark-only envelope, and retains the existing distinct-key overlap threshold. Verification must run the exact three reported stress nodes, existing held-writer timeout and distinct-key overlap checks, completed Tasks 1/2 regressions, and the full frozen release gate. Do not edit either legacy concurrency test to hide failures, regenerate the Ruff baseline, recapture Windows evidence, or claim live PostgreSQL qualification.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_authority_admission.py tests/test_sqlite_lifecycle_authority.py tests/test_sqlite_concurrency_temp.py::TestSQLiteConcurrency::test_deadlock_prevention tests/test_sqlite_concurrency.py::TestSQLiteConcurrency::test_concurrent_cache_stats_access tests/test_sqlite_concurrency.py::TestSQLiteConcurrency::test_concurrent_put_operations -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_close_contract.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py tests/test_filesystem_containment.py tests/test_unified_cache_adversarial_lifecycle.py tests/test_projection_sql_atomicity.py tests/test_cached_custom_metadata.py tests/test_sqlite_bootstrap_concurrency.py tests/test_sqlite_authority_admission.py tests/test_phase3_gap_acceptance.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check benchmarks/lifecycle_authority_benchmark.py tests/test_sqlite_authority_admission.py tests/test_sqlite_bootstrap_concurrency.py tests/test_phase3_gap_acceptance.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>Measured FIFO admission eliminates scheduler-induced SQLite deadline failures in all three reported public stress schedules under the one unchanged 0.187-second absolute authority deadline, without adding a timeout budget, serializing payload/cleanup or different authority paths, weakening same-key/cross-process CAS, relaxing an old benchmark envelope, or hiding typed timeouts.</acceptance_criteria>
  <done>The systemic SQLite authority contention blocker is closed with deterministic non-sleep stage evidence, one existing deadline, bounded fair local ordering, unchanged SQLite arbitration, and a green complete Phase 3 release gate.</done>
</task>

</tasks>

## Gap Coverage Audit

| Finding | Owner | Deterministic acceptance evidence | Status |
|---|---|---|---|
| CR-01 unadmitted facade put | 03-15 Task 1 | Real facade put paused after admission versus close and clear snapshot | COVERED |
| CR-02 destructive pre-promotion links | 03-15 Task 2 | Linked M1 plus signing/verification/promotion failures | COVERED |
| CR-03 peer pending token adoption | 03-15 Task 2 | Two M1-based pending candidates before either promotion | COVERED |
| CR-04 non-atomic SQL CAS | 03-16 Task 1 | Independent SQLite instance/process absent/existing races and PostgreSQL transaction schedules | COVERED |
| CR-05 fresh-root bootstrap race | 03-17 Task 1 | Unseeded two-instance and spawn-process schedules plus substitutions | COVERED |
| CR-06 cached custom metadata disabled | 03-16 Task 2 | Public memory-cached SQLite/PostgreSQL put/get/query/session/replacement workflows | COVERED |
| CR-07 empty canonical legacy inference | 03-15 Task 3 | Two-facade first-put versus clear/invalidate | COVERED |
| CR-08 hostile put locator | 03-15 Task 3 | Top-level/nested absolute/traversal/symlink zero-mutation matrix | COVERED |
| CR-09 PostgreSQL key-param nesting | 03-16 Task 3 | Signed public facade get/list/stats without retirement | COVERED |
| WR-01 destructor exception | 03-17 Task 2 | Explicit close/idempotency and clean subprocess teardown | COVERED |
| WR-02 missing forced schedules | 03-15 through 03-17 | Acceptance inventory plus execution of all real-path nodes | COVERED |
| Execution blocker: systemic SQLite authority contention | 03-17 Task 3 | Barrier-driven public facade contention with per-stage timing, held-writer timeout, cleanup overlap, and all three reported stress nodes | COVERED |

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generation with every incomplete outcome recoverable | 03-15 through 03-17 | COVERED | Admission, post-promotion projection ownership, atomic SQL, safe bootstrap, explicit close, and bounded SQLite writer admission close the discovered gaps. |
| REQ | STOR-03 | No partial/mixed payload and metadata state | 03-15 Tasks 1-3; 03-16 Tasks 1/3 | COVERED | Exact admission, pure candidate projection, SQL CAS, hostile preflight, and signing parity. |
| REQ | STOR-04 | Failed write preserves valid generation and recoverable residue | 03-15 Task 2; 03-16 Task 2 | COVERED | Linked M1 survives pre-promotion failures; stale links/objects never attach. |
| REQ | STOR-05 | Idempotent overwrite/delete/clear/close cleanup | 03-15 Tasks 1/3; 03-17 Tasks 1-3 | COVERED | Exactly-once admission, canonical empty cleanup, bootstrap convergence, explicit close, and fair bounded transaction admission. |
| REQ | STOR-06 | Safe dry-run/resumable reconciliation | 03-15 Task 2; 03-17 Task 2 | COVERED | No destructive pre-promotion evidence loss; existing bounded authority reconciliation is regression-gated. |
| REQ | STOR-07 | Deterministic same-key races without global distinct-key serialization | 03-15 Tasks 1/2; 03-16 Task 1; 03-17 Tasks 1/3 | COVERED | Forced pending, SQL, bootstrap, clear, close, measured authority admission, and distinct-key payload/cleanup schedules. |
| RESEARCH | — | LifecycleAuthority remains the sole complete transactional authority | 03-15 through 03-17 | COVERED | Projection/cache/link state stays derived; no scheduler or secondary authority returns. |
| RESEARCH | — | Immutable native payload work stays outside short transactions | 03-15 Tasks 1/2; 03-16 Task 1 | COVERED | Admission exclusion is snapshot-only; SQL transactions contain rows/links only. |
| RESEARCH | — | Bounded deterministic fault/process coverage and checked release gates | 03-15 through 03-17 | COVERED | Named hooks/barriers, spawn workers, separately measured contention stages, complete suite, compatibility, Ruff. |
| CONTEXT | D-01–D-04 | Immutable generations, exact CAS, committed-only reads, promotion authority | 03-15 Task 2; 03-16 Tasks 1/3 | COVERED | Projection/link ownership follows exact successful promotion. |
| CONTEXT | D-05–D-08 | Durable intent/provenance and pre/post-promotion failure classification | 03-15 Task 2; 03-17 Task 1 | COVERED | Candidate residue remains authority-indexed; bootstrap joins only proven state. |
| CONTEXT | D-09–D-12 | Tombstone, clear, idempotent cleanup, ownership-aware close | 03-15 Tasks 1/3; 03-17 Task 2 | COVERED | Canonical empty clear and admitted close have forced schedules. |
| CONTEXT | D-13–D-16 | Bounded safe resumable reconciliation without guessing | 03-15 Tasks 2/3; 03-17 Task 2 | COVERED | Repairs retain authority evidence and final suite reruns reconciliation contracts. |
| CONTEXT | D-17–D-20 | Per-key ordering, backend CAS, exact races, distinct-key concurrency | 03-15 Tasks 1/2; 03-16 Task 1; 03-17 Tasks 1/3 | COVERED | Exact CAS remains authoritative; per-path admission covers short same-process writer transactions only and payload/cleanup remains concurrent. |
| CONTEXT | D-21–D-22 | Trusted local owner and limited Windows topology | 03-15 Task 3; 03-17 Tasks 1/2 | COVERED | Fail-closed paths remain and no native Windows proof is claimed. |
| CONTEXT | D-23–D-31 | Transactional-authority replacement, JSON projection, SQLite, concurrency, measured bounds, retirement | 03-15 through 03-17 | COVERED | Changes deepen the existing SQLite authority with an evidence-derived local admission optimization; no alternate authority/protocol/ORM schema is introduced. |
| CONTEXT | D-32 | Windows qualification deferred without eligible environment | 03-17 Task 2 | COVERED | Darwin remains UNAVAILABLE/NOT_QUALIFIED, native_evidence false; Phase 999.1 unchanged. |

<threat_model>
OWASP ASVS L1 applies. Every high-severity threat below is blocking and must be mitigated before the release gate can pass.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Missing filesystem root → authority bootstrap | A loser may join only exact safe winner state under one deadline. |
| Interpreter shutdown → SQL resource cleanup | Finalization cannot import/log/raise or obscure process results. |
| Focused test evidence → release claim | Every blocker needs deterministic production-path evidence; optional platforms/services remain explicitly unqualified. |
| Projection repair → reconciliation evidence | Derived cleanup cannot erase the only canonical intent/debt or last valid generation. |
| Python thread scheduler → SQLite authority writer | Same-process contenders need fair bounded admission without turning a local optimization into lifecycle authority. |
| SQLite busy handler → lifecycle timeout classification | Real database-lock consumption must remain distinct from queue/scheduler delay and retain a typed bound. |
| Authority commit → payload/cleanup work | The short transaction gate must be released before native cleanup or unrelated-key work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-17-01 | Spoofing | Fresh-root winner classification | high | mitigate | Reclassify after FileExistsError and accept only exact contained regular authority state with confirmed identity/schema. |
| T-03-17-02 | Tampering | Symlink/wrong-object bootstrap substitution | high | mitigate | lstat each root/reserved/leaf transition and reject unchanged before SQLite open/adoption. |
| T-03-17-03 | Denial of Service | Bootstrap winner wait | high | mitigate | Reuse one absolute LifecycleLimits deadline and terminate with a typed timeout/migration outcome. |
| T-03-17-04 | Repudiation | Shutdown diagnostics | medium | mitigate | Prefer explicit idempotent close and remove destructor work; subprocess stderr is an automated gate. |
| T-03-17-05 | Repudiation | Windows/PostgreSQL qualification | high | mitigate | Separate deterministic local contract evidence from unavailable native/live qualification and preserve D-32 fields. |
| T-03-17-06 | Tampering | Regression evidence substitution | high | mitigate | Collect named CR nodes, forbid skip/xfail/private-helper-only substitution, and run the exact full/compatibility/Ruff gates. |
| T-03-17-07 | Denial of Service | Same-process authority writer admission | high | mitigate | Use FIFO tickets under the one existing absolute authority deadline, exact cancellation, finally-release, and zero-refcount registry retirement. |
| T-03-17-08 | Repudiation | Contention attribution | high | mitigate | Record queue, eligible/dispatch, sqlite busy-handler, writer-hold, and cleanup intervals independently with monotonic stage events. |
| T-03-17-09 | Denial of Service | SQLite external writer lock | high | mitigate | Preserve the one 0.187-second absolute authority bound and raise a typed sqlite-busy-stage timeout when BEGIN IMMEDIATE cannot acquire within its remaining time. |
| T-03-17-10 | Tampering | Whole-cache serialization disguised as admission | high | mitigate | Gate only one per-path authority transaction and prove another distinct-key payload/cleanup interval overlaps outside it while the checked-in overlap envelope remains green. |
</threat_model>

<verification>
Re-run the completed unseeded authority bootstrap and explicit-close schedules, then execute the new deterministic writer-admission instrumentation and the three reported public SQLite stress nodes on Python 3.11. Verify the additive benchmark schema and unchanged legacy thresholds before the full CR-01 through CR-09 focused matrix. Finish with the exact frozen isolated repository suite, compatibility corpus, qualification verifier, Phase 3 Ruff-delta verifier, and direct Ruff over the new benchmark/test paths that are not grandfathered by the delta baseline. Do not edit or promote Windows qualification evidence and do not treat an unavailable live PostgreSQL service as PASS.
</verification>

<success_criteria>
- Fresh-root independent instances/processes converge on one valid SQLite authority or typed same-key CAS, never FileExistsError.
- Wrong-object/symlink bootstrap states remain unchanged and unadopted.
- SqliteBackend explicit close is idempotent and interpreter shutdown is silent.
- Same-process writers receive fair bounded per-authority transaction admission; all preflight, queue, scheduler, and SQLite-lock time consumes the same unchanged absolute authority deadline, with no driver-level thundering herd.
- Real SQLite writer holds still raise a stage-specific CacheBlobLifecycleTimeoutError after the configured bound, and no ticket/registry entry leaks after timeout or close.
- Writer admission never spans payload publication or cleanup, same-key exact CAS remains unchanged, all three reported stress nodes pass, and the old distinct-key overlap envelope is not relaxed.
- Every CR-01 through CR-09 and WR-01/WR-02 has forced production-path coverage.
- Full suite, checked-in benchmark verification, compatibility corpus, Ruff delta, and direct Ruff gates pass.
- D-32 remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false and no live PostgreSQL qualification claim.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-17-SUMMARY.md` after Task 3 completes, preserving the commit evidence for completed Tasks 1 and 2. Record bootstrap winner/rejection schedules, destructor disposition, connection-preflight/queue-service/scheduler-dispatch/sqlite-busy/writer-hold/payload-cleanup measurements, the benchmark-only contention-envelope formula, proof that no runtime deadline was added or reset, the unchanged 0.187-second authority bound/legacy envelopes, all three stress-node results, CR/WR node map, exact full/compatibility/Ruff results, Python 3.11 evidence, and unchanged D-32/live-PostgreSQL qualification status.
</output>
