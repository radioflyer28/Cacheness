---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "09"
type: execute
wave: 9
depends_on: ["03-08"]
files_modified:
  - tests/_lifecycle_test_support.py
  - tests/test_blob_store_atomic_lifecycle.py
  - tests/test_blob_store_reconciliation.py
  - tests/test_blob_store_concurrency.py
  - tests/test_blob_store_close_contract.py
  - tests/test_sqlite_lifecycle_authority.py
  - tests/test_phase3_scheduler_retirement.py
  - tests/test_phase3_windows_contract.py
  - verify_platform.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every named crash boundary reopens to exactly one committed old-or-new state with indexed recoverable debt and no payload deserialization (D-01, D-02, D-06, D-24, D-25)."
    - "Same-key races serialize by CAS, distinct-key payload work overlaps, clear targets exact lineages, and close/fork/busy outcomes are bounded and typed (D-11, D-17, D-19, D-27, D-29)."
    - "Focused behavior passes under a real Python 3.11 interpreter and the repository Python 3.13 environment; missing interpreter evidence is a reported blocker, never a passing skip."
    - "The platform runner emits deterministic non-Windows UNAVAILABLE evidence and never turns absent native Windows proof into a passing skip or a Windows support claim; native protected-root, Python 3.11, same-session, and different-token evidence remains deferred to Phase 999.1 (D-22, D-31, D-32)."
  artifacts:
    - path: verify_platform.py
      provides: "Machine-readable Phase 3 host, Python, SQLite, topology, ACL, and focused-test evidence command"
    - path: tests/test_phase3_windows_contract.py
      provides: "Native Windows token/logon-SID/DACL, same-session positive, and different-session/service-token negative tests"
    - path: tests/test_phase3_scheduler_retirement.py
      provides: "Platform-neutral proof that the abandoned scheduler remains unreachable"
  key_links:
    - from: verify_platform.py
      to: tests/test_phase3_windows_contract.py
      via: "Native Windows Phase 3 target and explicit unavailable/fail/pass result"
      pattern: "phase3|Windows|evidence"
    - from: tests/test_blob_store_reconciliation.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Crash/reopen and no-deserialization authority evidence"
      pattern: "reconcile"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Platform or Python availability must not be represented as a passing skip for final acceptance."
    - "[FLAGGED/UNVERIFIED] Crash/race proof must not depend on timing sleeps or scheduler internals."
    - "[FLAGGED/UNVERIFIED] Non-Windows UNAVAILABLE output must not be cited as native Windows qualification or a Windows support claim (D-32)."
---

<objective>
Close the behavioral risk with deterministic crash/race evidence and honest supported-Python/platform execution.

Purpose: Prove the transactional authority converges for the race classes that defeated the abandoned scheduler before performance thresholds are calibrated.
Output: Complete crash/interleaving matrices, Python 3.11/3.13 evidence, and an executable platform runner whose non-Windows result stays explicitly UNAVAILABLE per D-32.
</objective>

## Artifacts this phase produces

- Deterministic subprocess crash and reopen matrix at every lifecycle boundary.
- Same-key, distinct-key, clear-overlap, fork, close, and busy-deadline race suites.
- Repository compatibility runner that distinguishes pass, fail, and unavailable evidence.
- Native Windows pre-provisioned-root/logon-SID-bound DACL verification with no runtime root/mode/ACL mutation, unchanged absent/unsafe/drifted rejection, same-session positive, and different-session/service-token negative targets.

## Executor Context Boundaries

- Complete and commit Task 1, retain only its test outcomes and changed-file summary, then release crash/race file bodies before Task 2.
- Complete and commit Task 2, then let the executor create the normal 03-09 summary. Plan 03-11 consumes that completed summary for qualification attestation.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-08-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VERIFICATION.md

D-32 requires this plan to preserve an explicit non-Windows UNAVAILABLE result and avoid unverified Windows scheduler, DACL, or support claims. The dependent 03-11 plan captures and attests that result after the normal 03-09 summary exists.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete deterministic crash, reopen, and interleaving coverage</name>
  <reversibility rating="reversible">Tests and fault hooks can evolve without changing committed storage semantics.</reversibility>
  <files>tests/_lifecycle_test_support.py, tests/test_blob_store_atomic_lifecycle.py, tests/test_blob_store_reconciliation.py, tests/test_blob_store_concurrency.py, tests/test_sqlite_lifecycle_authority.py</files>
  <read_first>
    <file>tests/_lifecycle_test_support.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
    <file>tests/test_blob_store_concurrency.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "Abrupt exit before/after payload publish, intent commit, manifest commit, cleanup-debt commit, delete, clear snapshot/page, reconciliation action/checkpoint, and projection publication reopens old-or-new only."
    - "Faults inside a transaction roll back; faults outside it preserve exact indexed intent/debt/checkpoint evidence."
    - "Barrier-controlled write/write, write/delete, get/delete, clear/put, clear/delete, reconcile/mutation, and close/in-flight races end in enumerated allowed states."
    - "Reconciliation authenticates before locator use, reads bounded metadata only, and never invokes a handler deserializer."
  </behavior>
  <action>Extend the deterministic gates/events/subprocess harness without probabilistic sleeps. Drive every D-01/D-02/D-06/D-11/D-17/D-19/D-24/D-25/D-27/D-29 boundary through public BlobStore calls and authority observer/fault interfaces. Assert exact manifest, lineage, generation, payload set, debt, clear target/progress, reconciliation checkpoint, and projection revision after interruption. Exercise same-key winners, ABA, distinct-key overlap, close admission, fork ownership, and one absolute busy deadline. Add hostile bounded-row, wrong-object/symlink, invalid-identity, and ambiguous-evidence cases; monkeypatch handler reads to fail during reconciliation.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py tests/test_sqlite_lifecycle_authority.py -x</automated>
  </verify>
  <acceptance_criteria>Every named boundary has deterministic whole-state assertions; races converge to finite outcomes; distinct keys overlap; security failures preserve evidence and never deserialize payloads.</acceptance_criteria>
  <done>Crash and concurrency safety are demonstrated by explicit state/interleaving coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add honest Python-version and native-platform evidence commands</name>
  <reversibility rating="reversible">The compatibility runner records evidence without changing authority semantics.</reversibility>
  <files>verify_platform.py, tests/test_phase3_windows_contract.py, tests/test_blob_store_concurrency.py, tests/test_blob_store_close_contract.py, tests/test_phase3_scheduler_retirement.py</files>
  <read_first>
    <file>verify_platform.py</file>
    <file>pyproject.toml</file>
    <file>.python-version</file>
    <file>tests/test_phase3_windows_contract.py</file>
    <file>tests/test_blob_store_close_contract.py</file>
  </read_first>
  <behavior>
    - "The repository runner emits machine-readable host, Python, SQLite, filesystem/topology, test-target, and pass/fail/unavailable evidence."
    - "The non-Windows required-system result is one canonically serialized JSON object with stable key order and no volatile timestamp, random identifier, or elapsed-time field, so repeated execution on the same host/runtime produces identical stdout bytes for attestation."
    - "Python 3.11 is discovered through repository/uv tooling and actually runs the focused suite; absence exits nonzero with an explicit missing-interpreter result."
    - "On Windows, the native target proves the pre-existing local NTFS root was provisioned before Cacheness starts, the current token's logon SID is the sole ordinary mutation principal, Cacheness performs no directory/mode/ACL write, absent/unsafe/drifted roots are rejected unchanged with an actionable offline command, persistent account-SID and inherited/unrelated write ACEs are rejected, narrowly non-mutating SYSTEM/Administrators entries are verified, same-session multiprocess SQLite contention succeeds, and a different-session/service token is denied without custom Win32 authority."
    - "On non-Windows, the native Windows target reports unavailable and exits distinctly; it never converts unavailability into passing evidence."
  </behavior>
  <action>Extend `verify_platform.py` with a Phase 3 evidence mode and explicit required-system/required-Python checks. Use `uv run --python 3.11 --frozen` for real 3.11 execution and `.venv/bin/python` for the repository 3.13 environment; remove machine-specific interpreter paths. Serialize the non-Windows required-system result as one canonical UTF-8 JSON object with stable ordering and a single trailing newline; omit volatile timestamps, random IDs, durations, and machine-specific paths so the same host/runtime produces byte-identical UNAVAILABLE stdout for the dependent 03-11 attestation. On native Windows, require the local NTFS root to have been provisioned before Cacheness starts. Derive the current token's `S-1-5-5-X-Y` logon SID from supported token-group evidence (`whoami /groups /fo csv /nh`) and corroborate it through PowerShell/.NET identity data. Invoke only read-only `icacls.exe /verify` and PowerShell `Get-Acl`; prove inheritance is disabled, the logon SID is the sole ordinary mutation grant, the persistent account SID has no mutation grant, unrelated/inherited write ACEs are absent, and any SYSTEM/Administrators ACE is explicit, narrowly documented, and non-mutating. Instrument directory, chmod/mode, PowerShell, and `icacls.exe` calls to prove Cacheness never provisions the Windows root or changes its ACL. Exercise absent, unsafe, and then drifted roots and prove each mutation fails typed and byte-for-byte unchanged while reporting the exact actionable offline PowerShell/`icacls.exe` provisioning command from the documentation; read-only absent/empty inspection remains zero-mutation empty. Run same-logon-session multiprocess SQLite contention as the positive case. The native evidence harness must also launch or use a genuinely different logon-session or service token and prove that it cannot open the authority database or create/change/delete under the root; recording only a same-session positive is failure. If the host/runner cannot access that second token, emit `UNAVAILABLE` for the native security proof and preserve the unqualified future-release obligation from D-32 rather than claiming evidence. Record only bounded SID classes/session identifiers and local filesystem class, without account names or secrets. Unsupported host/interpreter combinations return a distinct nonzero unavailable status and the offline provisioning/diagnostic command; test failure remains a separate nonzero status. Finish with `.venv/bin/python tools/verify_phase3_ruff_delta.py`.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase3_windows_contract.py tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_phase3_scheduler_retirement.py -k "not native_windows" -x &amp;&amp; uv run --python 3.11 --frozen pytest -q tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_phase3_scheduler_retirement.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Real 3.11 and repository-runtime focused runs succeed; the runner distinguishes unavailable from pass; native Windows can prove offline pre-provisioning, zero Cacheness root/mode/ACL mutation, unchanged absent/unsafe/drift rejection with actionable guidance, logon-SID-bound mutation, and denial from a genuinely different session/service token; without an eligible host it remains explicitly UNAVAILABLE and cannot support a Windows qualification claim (D-32).</acceptance_criteria>
  <done>Supported runtime and platform claims have executable, non-silent evidence paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| crash harness → store | Abrupt termination must not bypass durable-state assertions. |
| concurrent callers → authority | Contention must preserve lineage and consume bounded deadlines. |
| host evidence → support claim | Missing Python execution fails acceptance; non-Windows UNAVAILABLE output cannot be represented as native Windows success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-09-01 | Tampering | crash/reopen state | high | mitigate | Deterministic boundary matrix proves rollback or exact recoverable evidence. |
| T-03-09-02 | Denial of Service | busy/race/close | high | mitigate | One absolute deadline, bounded admission, and event-driven interleaving tests. |
| T-03-09-03 | Elevation of Privilege | Windows directory/database | high | mitigate | Preserve the implemented offline-provisioning and denial contract, mark this milestone unqualified, and require backlog Phase 999.1 native PASS before any Windows-qualified release (D-32). |
| T-03-09-04 | Repudiation | platform runner output | high | mitigate | Emit canonical machine-readable PASS, FAIL, or UNAVAILABLE records; test exit semantics and reserve byte-level capture for dependent Plan 03-11. |
</threat_model>

<verification>
Run deterministic crash/reopen and race suites under the repository Python 3.13 environment and uv-resolved Python 3.11. Run platform-runner contract tests and scheduler-negative reachability. On a non-Windows host, require an explicit UNAVAILABLE result and never interpret it as native qualification; Plan 03-11 captures and attests that evidence after this plan's summary exists (D-32).
</verification>

<success_criteria>
- Crash, retry, duplicate, clear, reconcile, close, fork, and race matrices converge deterministically.
- Python 3.11 and repository Python 3.13 focused suites actually execute and pass.
- Non-Windows execution reports UNAVAILABLE with distinct nonzero semantics and makes no Windows support claim; Phase 999.1 retains native Python 3.11 protected-root, same-session, different-token, and scheduler-retirement proof (D-32).
- Scheduler paths remain unreachable in both tested Python environments through the platform-neutral retirement gate.
</success_criteria>

<output>
Create the normal `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md` after both tasks complete. Its frontmatter must include a `platform-evidence` mapping with the exact Phase 3 runner command, `non_windows_status: UNAVAILABLE`, `unavailable_exit_code: 2`, `windows_qualified: false`, and `backlog_phase: 999.1`; these describe the runner contract and D-32 boundary, not native qualification. Plan 03-11 consumes this completed summary and performs the qualification capture.
</output>
