---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "21"
type: execute
wave: 20
depends_on: ["03-20"]
files_modified:
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/metadata.py
  - tests/test_blob_store_reconciliation.py
  - tests/test_cached_query_meta.py
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06]
estimate:
  tokens: 24000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "Public memory reconciliation pages past unpublished aborts without KeyError or resurrection (CR-01; D-07, D-08, D-13, D-14, D-27, D-29)."
    - "Memory cleanup resumes use stable monotonic IDs so earlier retirement cannot skip or replay captured debt (CR-02; D-11, D-14, D-29)."
    - "SQLite point/list/query reads strictly decode every observed non-null cache_key_params and preserve malformed evidence (CR-03; D-03, D-15, D-16, D-34)."
    - "Corrupt projection parameters under strict signing cannot retire a valid authority generation, payload, or custom link."
  artifacts:
    - path: src/cacheness/storage/memory_lifecycle_authority.py
      provides: "Terminal mutation paging and monotonic cleanup-debt identity"
    - path: src/cacheness/metadata.py
      provides: "Shared raw SQLite parameter decoder"
    - path: tests/test_blob_store_reconciliation.py
      provides: "Public abort and paged-resume proofs"
    - path: tests/test_cached_query_meta.py
      provides: "Corruption and strict-signing evidence-preservation regressions"
  key_links:
    - from: src/cacheness/storage/memory_lifecycle_authority.py
      to: src/cacheness/storage/reconciliation.py
      via: "Stable keyset identity through signed resume tokens"
      pattern: "page_reconciliation_work|retire_cleanup_debt"
    - from: src/cacheness/metadata.py
      to: tests/test_cached_query_meta.py
      via: "One raw type and byte decoder"
      pattern: "_decode_cache_key_params|METADATA_CORRUPT"
  prohibitions:
    - "[UNVERIFIED] No new scheduler, queue, durable control file, or cross-process memory guarantee."
    - "[UNVERIFIED] No silent omission, repair, or canonical deletion from malformed projection evidence."
---

<objective>
Close CR-01/02/03 through bounded representation/parser corrections. This replaces the unexecuted private-database-install draft; bootstrap and cache restructuring are not part of this plan.
Output: stable memory recovery identities, strict SQLite decoding, public regressions.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/phase3-architecture-audit-2026-09-06.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-GAP-REPLAN.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
</context>

<planner_assumptions>
Use the finite spec-less model in 03-GAP-REPLAN.md. STOR-03 is integrity; STOR-04/05 integrity plus recovery; STOR-06 recovery. Memory is one-process-only. SQLite remains the durable authority for catalog/intent/debt; filesystem effects stay outside its transactions. No progress or performance guarantee changes.
</planner_assumptions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Public reconciliation survives an unpublished memory abort</name>
  <reversibility rating="reversible">Private in-process state only.</reversibility>
  <files>src/cacheness/storage/memory_lifecycle_authority.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/memory_lifecycle_authority.py</file>
    <file>src/cacheness/storage/reconciliation.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
  </read_first>
  <action>Reproduce prepare plus abort_mutation(candidate_persisted=False), then public BlobStore.reconcile() and apply, including a snapshot captured before abort. Correct dangling _mutation_order lookup through stable terminal/retired-row handling. Preserve monotonic high-water, existing report/token contracts and non-mutating dry-run; use existing one-process synchronization, not a new scheduler.</action>
  <acceptance_criteria>Failing-before/passing-after public proof: no KeyError, finding, candidate cleanup or resurrection; repeated dry-run/apply and abort between pages converge.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_blob_store_reconciliation.py -o log_cli=false</automated></verify>
  <done>CR-01 is reproduced and closed through the public reconciliation path.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Retire memory debt without invalidating captured pages</name>
  <depends_on>Task 1 tracer passes.</depends_on>
  <reversibility rating="reversible">Private memory IDs, no persisted schema.</reversibility>
  <files>src/cacheness/storage/memory_lifecycle_authority.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/memory_lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
  </read_first>
  <action>Reproduce CR-02 with three real cleanup debts, operation_page_size=1 and max_reconcile_actions=1. Replace array-position identity with monotonic IDs/keyset traversal; keep pending debt and reconciliation pages consistent. Earlier physical retirement cannot change captured cursor/high-water. New debts after capture belong to a later run. Retire idempotently without unbounded terminal-row retention.</action>
  <acceptance_criteria>Each captured locator is reclaimed once in uninterrupted resumed apply; interrupted apply may safely retry its exact action. Earlier retirement, new insertion, empty final page and a second complete apply never skip, IndexError or delete a live locator.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_blob_store_reconciliation.py tests/test_lifecycle_authority_contract.py -o log_cli=false</automated></verify>
  <done>CR-02 converges with stable bounded paging, not mutable offsets.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Share strict SQLite projection decoding across observations</name>
  <depends_on>Task 2 passes.</depends_on>
  <reversibility rating="reversible">Enforces existing fail-closed semantics.</reversibility>
  <files>src/cacheness/metadata.py, tests/test_cached_query_meta.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/core.py</file>
    <file>tests/test_cached_query_meta.py</file>
    <file>tests/test_query_meta_security.py</file>
  </read_first>
  <action>Reuse _decode_cache_key_params for point/list/query. Fetch raw typeof and CAST AS BLOB before ORM text coercion can lose invalid UTF-8/type evidence; SQL NULL stays absent, every non-null value must pass the same bounded mapping decoder. Attribute errors to key and actual operation; remove swallow-and-omit catches. Cover malformed JSON, duplicate keys, non-object JSON, invalid UTF-8, BLOB/numeric storage, depth/node/item/string/byte bounds, valid mappings and NULL. These are existing structured-parser limits, not payload size policy.

In disposable fixtures seed a valid strict-signed cache entry with custom links, corrupt only the parameter column using raw SQL, and exercise point/list/query and cache get. Assert canonical bytes/expectation, payload digest, corrupt SQL bytes and links unchanged, with no pre-error hit/miss mutation. At this intermediate step cache get may raise METADATA_CORRUPT; Plan 24 changes canonical get to read authority directly, retaining evidence preservation.</action>
  <acceptance_criteria>No malformed observed non-null field is omitted or produces a raw decode error. Typed corruption precedes signature-policy deletion. Valid and NULL behavior remains compatible.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_cached_query_meta.py tests/test_query_meta_security.py tests/test_cache_integrity.py -o log_cli=false</automated></verify>
  <done>CR-03 parser gap has deterministic non-destructive public proof; structural cache correction remains explicit in Plan 24.</done>
</task>
</tasks>

<threat_model>
Raw SQL metadata and resume cursors cross trust boundaries. Strict bounded decoding prevents corrupt signing inputs from deleting canonical data; stable IDs plus exact generation/provenance prevent wrong cleanup. Tasks 1–3 exercise these threats. High-severity integrity gaps block. No deserialization is used to infer recovery ownership.
</threat_model>
<verification>
Run the named tests, scoped Ruff and tools/verify_phase3_ruff_delta.py in a disposable isolated checkout per 03-GAP-REPLAN.md, never against original dirty fixture sidecars. No new public symbols, schema or external service. Stop at any ADR stop condition.
</verification>
<success_criteria>CR-01/02/03 bounded regressions pass without new coordination; phase verification remains gaps_found pending later plans.</success_criteria>
<output>Create 03-21-SUMMARY.md with exact commits/commands, reproduction, stable-ID semantics, decoder matrix and preserved evidence.</output>

