---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "18"
type: execute
wave: 18
depends_on: ["03-17"]
files_modified:
  - src/cacheness/core.py
  - src/cacheness/metadata.py
  - tests/test_cached_query_meta.py
  - tests/test_sqlite_concurrency.py
  - tests/test_sqlite_concurrency_temp.py
  - tests/test_blob_store_reconciliation.py
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Completed admission guarantees remain intact: failed clear tickets retire, one absolute 0.187-second authority deadline covers every stage, and a forked child receives fresh process-local coordination."
    - "UnifiedCache.query_meta uses CachedMetadataBackend's backend-neutral capability without SessionLocal inspection; six simultaneous pre-populated readers, including while an external metadata writer owns BEGIN IMMEDIATE, return concrete committed results with no raw lock, timeout, None, or resource leak."
    - "Only the three named extreme write/metadata schedules classify exact CacheBlobLifecycleTimeoutError reason/stage combinations; every attempt is accounted for with bounded progress, integrity, and zero leaked tickets, while ordinary put/mixed/file/stats/integrity/query/get tests remain strict all-success paths."
    - "The reconciliation time-budget test uses normal authority setup limits and proves only reconciliation's injected-clock deadline, so cold SQLite preflight variance cannot invalidate an unrelated assertion."
    - "SQLite pool expansion performs only connection-local read-safe configuration; database-scoped WAL/schema/optimization work occurs at an explicit constructor-owned initialization boundary before the backend is published, including safe idempotent initialization by concurrent fresh processes and a non-WAL in-memory branch."
    - "Built-in metadata queries are observational and filter on exact committed authority key-plus-locator pairs, so mismatches remain reconciliation evidence and a concurrent same-key projection replacement is never substituted; only typed database-operation failures retain the established logged-None result while programmer and integrity failures propagate."
    - "Exact same-key CAS, hostile containment, committed-only visibility, distinct-key payload concurrency, benchmark envelopes, compatibility, and qualification status remain unchanged."
  artifacts:
    - path: src/cacheness/storage/coordination.py
      provides: "Exact pre-entry clear-ticket retirement and waiter notification"
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Single-deadline SQLite busy budgeting and fork-reset process-local admission state"
    - path: src/cacheness/metadata.py
      provides: "Backend-neutral built-in metadata-query capability with cached-wrapper delegation"
    - path: src/cacheness/core.py
      provides: "Public query_meta routing without concrete SQL session sniffing"
    - path: tests/test_phase3_postreview_concurrency.py
      provides: "Deterministic public clear, combined-deadline, and fork schedules"
    - path: tests/test_cached_query_meta.py
      provides: "Cached matching/unsupported policy plus deterministic QueuePool reader, external-writer/WAL, exact-token coherence, and resource-retirement regressions"
    - path: tests/test_sqlite_concurrency.py
      provides: "Primary historical stress policy with exact typed-timeout classification and meaningful progress/integrity accounting"
    - path: tests/test_sqlite_concurrency_temp.py
      provides: "Duplicate historical stress policy kept behaviorally identical to the primary module"
    - path: tests/test_blob_store_reconciliation.py
      provides: "Reconciliation deadline proof isolated from cold authority setup timing"
  key_links:
    - from: src/cacheness/storage/coordination.py
      to: tests/test_phase3_postreview_concurrency.py
      via: "A public BlobStore clear schedule forces timeout before entry, then proves exact ticket retirement and later FIFO progress"
      pattern: "clear_operation|_discard_clear_ticket|notify_all"
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_phase3_postreview_concurrency.py
      via: "Admission observers, external BEGIN IMMEDIATE ownership, and fork synchronization prove one deadline and child-state reset"
      pattern: "busy_timeout|register_at_fork|writer_admission|sqlite_busy"
    - from: src/cacheness/core.py
      to: src/cacheness/metadata.py
      via: "query_meta invokes the explicit metadata-query capability; CachedMetadataBackend delegates to its wrapped backend"
      pattern: "supports_.*metadata_query|query_.*key_params"
    - from: src/cacheness/metadata.py
      to: tests/test_cached_query_meta.py
      via: "SQLite owns query construction/results while cached and unsupported adapters expose truthful capability behavior"
      pattern: "CachedMetadataBackend|SqliteBackend|query_meta"
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_sqlite_concurrency.py
      via: "Stress tests recognize only the bounded lifecycle reason and exact admission/busy stages emitted by production"
      pattern: "blob_lifecycle_timeout|writer_admission|scheduler_dispatch|sqlite_busy"
    - from: tests/test_sqlite_concurrency.py
      to: tests/test_sqlite_concurrency_temp.py
      via: "Duplicate stress modules apply the same exception classifier, attempt accounting, bounded completion, integrity, and ticket-retirement assertions"
      pattern: "expected.*contention|successful|timed_out|registry"
    - from: tests/test_blob_store_reconciliation.py
      to: src/cacheness/storage/reconciliation.py
      via: "Normal authority setup precedes an independently injected reconciliation clock that exhausts the reconciliation work budget"
      pattern: "ExpiredClock|monkeypatch|resume_token"
    - from: src/cacheness/metadata.py
      to: tests/test_cached_query_meta.py
      via: "A constructor-owned database bootstrap is separated from per-connection read-safe PRAGMAs, and backend-owned sessions retire every pooled connection"
      pattern: "journal_mode|connect|QueuePool|checkedout"
    - from: src/cacheness/core.py
      to: src/cacheness/metadata.py
      via: "UnifiedCache derives exact committed key-plus-locator observations without repair and passes them through the delegated query capability"
      pattern: "live_.*projection.*token|actual_path|query_entries_by_key_params"
  prohibitions:
    - "[UNVERIFIED] A timed-out clear ticket cannot remain queued or retain admission accounting."
    - "[UNVERIFIED] No deadline stage can reset the 0.187-second absolute authority budget or discard the underlying SQLite busy error."
    - "[UNVERIFIED] Forked children cannot reuse any inherited admission registry, registry lock, per-path gate, ticket, owner, or refcount state."
    - "[UNVERIFIED] query_meta cannot inspect SessionLocal or silently lose supported capability through CachedMetadataBackend."
    - "[UNVERIFIED] Release tests cannot accept raw database-lock strings, unrelated CacheBlob failures, unbounded workers, zero-progress outcomes, corrupt committed values, or leaked admission state."
    - "[UNVERIFIED] The query fix cannot change LifecycleAuthority, LifecycleLimits defaults, benchmark measurements, multipliers, or release envelopes."
    - "[UNVERIFIED] A read-only query cannot run journal-mode, schema, optimize, projection-repair, or other write-side work while acquiring or using a pooled metadata connection."
---

<objective>
Preserve the completed post-Plan-17 repairs and RED test-policy checkpoint, then remove unintended metadata-writer work from public read-only queries before final qualification.

Purpose: RED checkpoint `fcc54ac` correctly made reader-only schedules strict and exposed a production bug: six public `query_meta` readers over pre-populated data can expand SQLAlchemy's file-backed `QueuePool`, whose current connect listener reruns database-scoped `PRAGMA journal_mode=WAL` and `PRAGMA optimize`; those connection-time write-capable operations can raise raw `sqlite3.OperationalError: database is locked`. The SELECT sessions themselves are backend-owned and bounded, while `_live_authority_projection_keys()` can also perform projection repair and therefore is not yet a pure query boundary. Test policy must remain strict; the production query path must become genuinely read-only.
Output: Completed Tasks 1-3 and Task 4 RED evidence retained without replay, one backend-owned/read-only metadata-query fix with deterministic reader and reader/writer schedules, and fresh full release qualification without changing authority limits, benchmarks, Windows evidence, or live-PostgreSQL claims.

Decision fidelity: D-01: immutable generations remain unchanged; D-02: exact authority CAS remains decisive; D-03: public query results remain committed-only; D-04: promotion remains the authority point. D-05: bounded intent and D-06: validated provenance remain intact; D-07: failure residue and D-08: typed cleanup outcomes remain recoverable. D-09: tombstone deletion remains unchanged; D-10: clear snapshot admission is bounded and resumable; D-11: cleanup is idempotent; D-12: close remains ownership-aware. D-13: dry-run, D-14: resumable apply, D-15: evidence-proven repair, and D-16: no payload guessing remain regression-gated. D-17: coordination retires when unused; D-18: backend CAS, not local locks, is correctness authority; D-19: same-key races remain deterministic; D-20: distinct-key payload work remains concurrent. D-21: local-owner trust and D-22: limited Windows topology are unchanged. D-23: the file scheduler stays retired; D-24: BlobStore retains one deep authority; D-25: payload work stays outside transactions; D-26: stdlib SQLite remains local authority; D-27: no alternate scheduler is introduced. D-28: only short authority commits serialize; D-29: existing measured bounds and indexed reconciliation remain exact; D-30: tests use the authority/BlobStore facade; D-31: no platform lock-file authority is added; D-32: native Windows remains UNAVAILABLE/NOT_QUALIFIED and unclaimed.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-17-SUMMARY.md
@src/cacheness/storage/coordination.py
@src/cacheness/storage/sqlite_lifecycle_authority.py
@src/cacheness/core.py
@src/cacheness/metadata.py
</context>

## Partial Execution Baseline — Do Not Replay

Tasks 1 through 3 are complete, and Task 4's RED test-policy checkpoint is committed. None may be replayed:

| Completed task | Test commits | Production/follow-up commits | Preserved result |
|---|---|---|---|
| Task 1 — clear FIFO retirement | `2bf0d84` | `2df8f4b` | Timed-out pre-entry clear tickets retire exactly and later clears progress. |
| Task 2 — one deadline and fork reset | `1792d48` | `5041ac8`; `1eebea8` (production margin plus focused-test correction) | Remaining busy budget is applied before BEGIN and forked children receive fresh coordination state. |
| Task 3 — delegated query_meta | `0ebf2d6` | `9f248d9` | Cached SQLite metadata queries use an explicit backend capability. |
| Task 4 — release-policy RED checkpoint | `fcc54ac` | — | Strict reader-only checks and corrected contention/reconciliation policy expose the pooled-connection query bug; production remains intentionally red. |

The executor must begin at Task 5. It must not edit, amend, squash, replay, or recommit Tasks 1-3 or recreate Task 4's RED checkpoint. Commit `fcc54ac` is the immutable failing specification; its strict policy assertions remain regression gates, while new deterministic query diagnosis belongs in `tests/test_cached_query_meta.py`.

<tasks>

<completed_task number="1" commits="2bf0d84,2df8f4b" replay="forbidden">
  <name>Task 1: Retire a timed-out public clear's exact FIFO ticket</name>
  <files>src/cacheness/storage/coordination.py, tests/test_phase3_postreview_concurrency.py</files>
  <read_first>
    <file>src/cacheness/storage/coordination.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_unified_cache_adversarial_lifecycle.py</file>
    <file>tests/test_blob_store_concurrency.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md</file>
  </read_first>
  <behavior>
    - "A first public BlobStore.clear pauses after committing its snapshot while retaining active-clear ownership; a second public clear queues and deterministically exhausts its pre-entry close_wait deadline."
    - "The second clear raises CacheBlobCloseTimeoutError, removes only its own ticket under the admission Condition, notifies all waiters, and never changes active-clear, snapshot-owner, in-flight, or admitted-thread accounting owned by the first clear."
    - "After releasing the first clear, a third public clear enters and completes within a bounded join; the queue is empty, active/snapshot owners are absent, in_flight is zero, and retrying does not grow retained coordination state."
    - "The same cleanup path is idempotent when close cancellation already discarded the queued ticket, and it cannot underflow admitted references."
  </behavior>
  <action>Write the public regression first using BlobStore.clear(), the existing `clear.snapshot_committed` lifecycle hook, Events/Barriers, and InstanceAdmission's injectable monotonic/wait seam; do not use sleeps as a success condition. Force clear A to remain active at the snapshot boundary, make queued clear B's fake clock consume exactly its configured `close_wait_seconds`, assert B's typed timeout, release A, then require clear C to complete. Add a narrow test-only admission snapshot/property only if needed to prove queue/owner/in-flight retirement without changing public APIs.

In `InstanceAdmission.clear_operation()`, make the finally path handle both states. An entered ticket keeps the existing snapshot release plus active-clear/admitted-reference teardown. A ticket that failed before entry must reacquire `_condition`, call `_discard_clear_ticket(ticket)` for that exact object regardless of whether timeout, cancellation, or close caused exit, and notify all waiters after the discard attempt. Do not pop by position, promote another clear from the failure path, or alter the active clear's accounting. Preserve D-10's separation between short snapshot exclusion and full active-clear ownership, D-12 close precedence, D-17 bounded retirement, and D-20 unrelated-operation progress.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_phase3_postreview_concurrency.py -k "timed_out_clear or clear_ticket" -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>A pre-entry timeout or cancellation cannot leave a FIFO tombstone: the exact waiter retires, the active clear remains valid, a later clear succeeds, and all admission bookkeeping returns to zero.</acceptance_criteria>
  <done>Fresh review CR-01 is closed through the public clear path without weakening bounded clear/close semantics.</done>
</completed_task>

<completed_task number="2" commits="1792d48,5041ac8,1eebea8" replay="forbidden">
  <name>Task 2: Enforce one SQLite deadline and reset all admission state after fork</name>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_phase3_postreview_concurrency.py</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/test_sqlite_authority_admission.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
  </read_first>
  <behavior>
    - "With an external sqlite3 connection holding BEGIN IMMEDIATE, an admission observer consumes about 0.12 seconds of the 0.187-second absolute deadline before the authority attempts SQLite; BEGIN IMMEDIATE then receives only the remaining budget and total elapsed is at most one deadline plus 0.025 seconds of scheduler tolerance."
    - "Immediately before BEGIN IMMEDIATE, the connection busy timeout is replaced with a conservative non-extending millisecond value derived from `_remaining_for_stage(absolute_deadline)`; sub-millisecond remainder becomes immediate/no-wait rather than rounding upward past the deadline."
    - "A genuine busy failure remains CacheBlobLifecycleTimeoutError with `stage=sqlite_busy`, elapsed/budget context, and the originating sqlite3.OperationalError as `__cause__`; admission or scheduler exhaustion retains its own typed stage."
    - "Forking while a parent admission gate is owned leaves the parent's gate/order intact. In the child, the inherited authority fails its PID guard, while a fresh authority for the same root sees a new empty registry and new unlocked registry lock, commits successfully, and retires its child gate."
    - "A second fork schedule holds the module registry lock itself at fork and proves the child reset occurs without touching the inherited lock; PID-qualified keys alone do not satisfy the test."
    - "No local gate spans payload/cleanup, exact same-key CAS is unchanged, the authority timeout stays 0.187 seconds, and every checked-in benchmark envelope remains byte-for-byte unchanged."
  </behavior>
  <action>Extend the new post-review test module before production changes. For the combined-deadline schedule, warm the schema, let an independent sqlite3 connection own BEGIN IMMEDIATE, use an observer Event timeout to consume a controlled 0.12-second dispatch interval (not a retry loop), measure monotonic transaction elapsed, and require completion by `authority_busy_timeout_seconds + 0.025` seconds. The 0.025-second allowance covers only test-runner scheduling after the typed failure, not SQLite configuration or another retry budget. Also retain deterministic fake-clock unit coverage for zero/sub-millisecond remaining conversion. Do not change LifecycleLimits, the baseline JSON, or any benchmark multiplier/envelope.

In `_transaction()`, move the `sqlite.begin.attempt` observer before the final timeout calculation, then immediately before `connection.execute("BEGIN IMMEDIATE")` call `_remaining_for_stage()` against the original `absolute_deadline` and set that same connection's `PRAGMA busy_timeout` from the remaining seconds using a floor/conservative millisecond conversion that cannot extend the deadline. Implement this as a focused helper that accepts only the generated non-negative integer milliseconds; use an integer-only formatted PRAGMA because sqlite3 does not parameter-bind PRAGMA assignment values. Recheck the same absolute deadline after applying the PRAGMA and immediately before BEGIN, so observer dispatch and PRAGMA overhead cannot authorize an already-expired acquisition. Keep one absolute deadline from method entry through connection preflight, admission, dispatch, and SQLite acquisition. Preserve the original sqlite3 busy exception as the translated timeout cause and retain stage-specific context. Do not add sleeps, retries, a second deadline, another BEGIN attempt, or a fallback success/miss.

Make writer admission genuinely process-scoped. Define one child-reset function that rebinds both `_WRITER_ADMISSION_REGISTRY` to a new empty dictionary and `_WRITER_ADMISSION_REGISTRY_LOCK` to a newly constructed Lock, and register it once with `os.register_at_fork(after_in_child=...)` where supported. Resetting only entries, adding PID to the key, or acquiring the inherited lock in the child is insufficient. New authority instances naturally allocate fresh instance locks/ticket maps; inherited instances continue to fail `_owner_pid` validation. Force fork schedules with pipes/Events and bounded joins: first pause the parent specifically in the `writer_admission.granted` observer, after it owns the Python gate but before `BEGIN IMMEDIATE`, so the child is testing inherited process-local state rather than legitimate cross-process SQLite contention; separately fork while a helper thread owns the module registry lock. Capture the pre-fork lock reference so the child can assert the rebound module global is a distinct unlocked Lock without acquiring the inherited object. In both schedules the child must exercise real authority mutation with a newly constructed object, report registry sizes before/after, and exit cleanly; the parent must release and retire its own state afterward. Guard fork-only tests by platform capability without changing D-22/D-31/D-32 or claiming native Windows evidence.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_phase3_postreview_concurrency.py -k "combined_deadline or busy_budget or fork" -x -o log_cli=false &amp;&amp; uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_authority_admission.py tests/test_sqlite_lifecycle_authority.py -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>All pre-BEGIN work and SQLite busy waiting fit one unchanged absolute deadline plus at most 0.025 seconds of test-runner scheduling tolerance, real busy errors retain their cause/stage, and a fresh child can never observe or block on inherited admission state even when fork occurs at either locked boundary.</acceptance_criteria>
  <done>Fresh review CR-02 and CR-03 are closed without changing authority policy, benchmark evidence, cross-process CAS, or distinct-key concurrency.</done>
</completed_task>

<completed_task number="3" commits="0ebf2d6,9f248d9" replay="forbidden">
  <name>Task 3: Route query_meta through an explicit delegated backend capability</name>
  <files>src/cacheness/core.py, src/cacheness/metadata.py, tests/test_cached_query_meta.py</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/query_validation.py</file>
    <file>tests/test_query_meta.py</file>
    <file>tests/test_query_meta_security.py</file>
    <file>tests/test_cached_custom_metadata.py</file>
    <file>tests/test_cache_integrity.py</file>
  </read_first>
  <behavior>
    - "With metadata_backend=sqlite, enable_memory_cache=True, and store_cache_key_params=True, public UnifiedCache.query_meta returns the same matching dictionaries as the unwrapped SQLite backend for string, numeric-threshold, boolean, and nested-field filters."
    - "The query capability receives the authority-derived committed live-key set, so stale projection rows and tombstones remain absent even when their key parameters match."
    - "CachedMetadataBackend truthfully delegates both capability detection and query execution to its wrapped backend without exposing SessionLocal, engine, or raw session ownership."
    - "JSON, plain memory, sqlite_memory, PostgreSQL, or another backend without the capability follows the established public policy: query_meta returns None and emits `query_meta() requires SQLite backend`; disabled key-parameter storage retains its existing warning, and invalid query fields/numeric values still raise CacheQueryValidationError before backend access."
    - "Core query_meta contains no SessionLocal/engine/SQLAlchemy/CacheEntry inspection, and its return dictionaries plus public signature remain compatible."
  </behavior>
  <action>Start with public tests for cached file-backed SQLite matching/nested/numeric queries, committed-only filtering, and the exact unsupported result/warning for JSON, plain memory, and sqlite_memory. Verify PostgreSQL/custom default to unsupported with a non-live capability stub rather than claiming live PostgreSQL behavior. Add an explicit capability pair to `MetadataBackend`, such as `supports_entry_metadata_query()` and `query_entries_by_key_params(filters, *, live_keys)`, with a truthful default unsupported implementation. Implement the SQLAlchemy/SQLite JSON1 query and row-to-dictionary conversion inside SqliteBackend, where SessionLocal is owned, but advertise support only for the file-backed SQLite configuration that the existing public method supports; the `SqliteBackend(":memory:")` instance used by sqlite_memory must remain unsupported rather than inheriting a class-wide true flag. At that direct backend boundary, repeat field/numeric validation before deriving SQLite JSON paths so no raw field name is interpolated merely because a caller bypassed UnifiedCache. Delegate both capability methods unchanged through CachedMetadataBackend. Do not alias SessionLocal or leak a session factory through the wrapper. Other backends remain explicitly unsupported unless they already have equivalent proven semantics and the plan adds an explicit compatibility decision plus tests.

Refactor UnifiedCache.query_meta in the existing compatibility order: validate all filters before backend access; test the explicit capability and, when absent, return `None` with the exact existing `query_meta() requires SQLite backend` warning; then enforce `store_cache_key_params` with its exact existing warning; only then compute `_live_authority_projection_keys()` and invoke the backend method. Remove the `actual_backend == "sqlite"` and `hasattr(SessionLocal)` concrete checks plus SQLAlchemy/CacheEntry query construction from core. Preserve the empty-live-key result (`[]`), dictionary field names/ISO timestamp rendering, numeric >= semantics, serialized key-parameter comparison, and CacheQueryValidationError propagation. Operational backend failures may retain the existing logged-None compatibility policy, but capability absence must not be inferred from an exception. Add a source-level/AST assertion that the query_meta body does not reference SessionLocal or engine. This closes WR-01 while preserving committed-only visibility D-03 and the BlobStore-authority/UnifiedCache-policy boundary D-24/D-26.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_cached_query_meta.py tests/test_query_meta.py tests/test_query_meta_security.py tests/test_cached_custom_metadata.py -x -o log_cli=false &amp;&amp; uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_phase3_postreview_concurrency.py tests/test_sqlite_authority_admission.py tests/test_unified_cache_adversarial_lifecycle.py tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_blob_store_reconciliation.py tests/test_cache_integrity.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_phase3_postreview_concurrency.py tests/test_cached_query_meta.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>Cached SQLite query_meta returns correct committed matches through an explicit delegated capability; unsupported backends retain the documented None/warning behavior; core has no concrete session sniffing; and every focused/full/benchmark/compatibility/qualification/Ruff gate passes.</acceptance_criteria>
  <done>Fresh review WR-01 is closed and Phase 3 has focused implementation evidence for STOR-03 through STOR-07; final release qualification belongs to Task 4.</done>
</completed_task>

<completed_red_checkpoint number="4" commit="fcc54ac" replay="forbidden">
  <name>Task 4: Correct bounded-contention and reconciliation release-test policy</name>
  <files>tests/test_sqlite_concurrency.py, tests/test_sqlite_concurrency_temp.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>tests/test_sqlite_concurrency.py</file>
    <file>tests/test_sqlite_concurrency_temp.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
    <file>tests/test_sqlite_authority_admission.py</file>
    <file>tests/test_phase3_postreview_concurrency.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/reconciliation.py</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
  </read_first>
  <behavior>
    - "In both historical SQLite concurrency modules, only `test_high_concurrency_stress`, `test_concurrent_metadata_access`, and `test_deadlock_prevention` classify expected extreme contention by exception type and machine-readable context: CacheBlobLifecycleTimeoutError, reason `blob_lifecycle_timeout`, operation `lifecycle_authority`, and stage in exactly `writer_admission`, `scheduler_dispatch`, or `sqlite_busy`."
    - "A raw sqlite3/SQLAlchemy `database is locked` error, another CacheBlob error/reason/stage, a serialization/projection/integrity failure, or an arbitrary exception remains a hard test failure and is never accepted by message substring."
    - "Each named high-contention worker continues after an allowed per-operation timeout, returns structured success/timeout/failure accounting, and finishes under a bounded future/join deadline; classified attempts equal attempted operations, at least 75 percent succeed within each named test, and every worker records at least one successful operation."
    - "Every successful put records its public key parameters and expected value, is represented by one live listed entry, and reads back through public get with the exact expected value; timed-out puts remain absent from the live set, and the SQLite writer-admission registry is empty after the workload/cache close."
    - "The primary and `_temp` duplicate modules use the same helper, accepted stage set, accounting fields, progress thresholds, corruption checks, and bounded completion policy in the three named extreme schedules. Query-only and get-only raw-lock allowances are removed with no replacement and require every operation to succeed."
    - "`test_concurrent_put_operations`, `test_concurrent_different_data_operations`, `test_thread_safety_with_file_operations`, `test_concurrent_cache_stats_access`, and `test_thread_safety_data_integrity` remain strict all-success tests with their existing exact count/integrity assertions in both modules."
    - "The reconciliation row/action/byte/time-bounds test constructs and warms the time-limited store with the normal 0.187-second authority policy, then proves reconciliation's time limit solely through the existing monkeypatched reconciliation monotonic clock; it no longer applies a 0.001-second cold-authority deadline."
    - "Task 4 records its actual stress counts separately from the checked-in benchmark; the existing benchmark provenance, samples, multipliers, envelopes, and 0.187-second runtime policy remain byte-for-byte unchanged and are reverified rather than remeasured into a new baseline."
  </behavior>
  <action>Change tests only. In both `test_sqlite_concurrency.py` and `test_sqlite_concurrency_temp.py`, add the same narrow helper that returns true only for a `CacheBlobLifecycleTimeoutError` whose context has `reason == CacheReason.BLOB_LIFECYCLE_TIMEOUT.value`, `operation == "lifecycle_authority"`, and `stage` in the frozen set `{writer_admission, scheduler_dispatch, sqlite_busy}`. Import the concrete error/reason types. Invoke this helper only inside `test_high_concurrency_stress`, `test_concurrent_metadata_access`, and `test_deadlock_prevention`, the named write-bearing extreme-contention schedules. Raw sqlite3/SQLAlchemy errors and every nonmatching CacheBlob exception remain hard failures with their original object/type/context available to assertions; no message substring is an acceptance criterion.

Refactor only those three extreme-contention worker loops so an allowed timeout classifies the individual public call that raised it and does not abort the worker or return `True`, zero, or an empty list as if the call succeeded. Return structured per-worker attempted/success/allowed-timeout/hard-failure counts plus successful and timed-out put records containing the original public key-parameter mapping and expected value. A put counts as successful only after the public call returns; query/list calls count as successful only when they return the expected concrete result shape rather than `None`. Run each complete extreme schedule behind an outer child-process boundary: the child creates/uses/closes its cache, executes the threaded workload, checks its own admission-registry retirement, and sends one structured result through a pipe/queue; the parent uses a monotonic bounded join, treats a still-live child as a hard deadlock failure, and terminates/joins it only for cleanup after recording that failure. Use a POSIX process context available on the current qualified host and mark an unavailable context as a platform skip, not Windows evidence. Inside the child use a test-level monotonic deadline and bounded `future.result()`/thread joins; do not rely on a `ThreadPoolExecutor` context whose implicit shutdown can wait forever after the deadline. Assert separately in each named test: the child and every worker terminate; successes + allowed timeouts + hard failures equals the exact attempted-call count; hard failures are empty; at least 75 percent of calls succeed; every worker makes at least one successful call; and the existing elapsed/deadlock bound remains enforced. Existing sleeps may remain only as workload shaping and must not be increased or used to order a race or establish success.

Use unique public key parameters for every write attempt in these evidence paths (including adding the iteration/item discriminator to the metadata stress path) so a successful overwrite cannot mask a timed-out attempt during accounting. After every extreme workload has fully stopped, use each recorded public key-parameter mapping with `cache.get(**params)` and require the exact expected value, and corroborate that the live `list_entries()` count/keys match only successful puts. Any post-workload read exception or timeout is a hard failure reported distinctly from a wrong-value corruption; it is not eligible for the contention classifier. A timed-out put's parameters must produce a public miss and must not appear in the live entry projection merely because candidate residue exists. Close the cache in `finally`, then assert `SqliteLifecycleAuthority.admission_registry_size_for_test() == 0` so failures cannot strand gate/ticket/refcount state.

For the two read-only tests that currently suppress `"database is locked"` (`test_concurrent_query_operations` and `test_concurrent_get_operations`), delete the substring allowance without replacing it: retain exception objects, require all workers/calls to finish boundedly, require all query results to be concrete matching lists and all gets to equal their pre-populated values, and fail on any exception including a lifecycle timeout. Leave `test_concurrent_put_operations`, `test_concurrent_different_data_operations`, `test_thread_safety_with_file_operations`, `test_concurrent_cache_stats_access`, and `test_thread_safety_data_integrity` as strict all-success tests with their existing exact count/value assertions; the timeout helper must not be called from them. Apply the same scopes and assertions to the `_temp` duplicate. Do not add retries, sleeps, production monkeypatches, blanket `except CacheError`, or weaker zero/some-success assertions.

In `test_blob_store_reconciliation.py::test_reconciliation_enforces_row_action_byte_and_time_bounds`, remove the unrelated `authority_busy_timeout_seconds=0.001` override from the store used to establish the time-limit fixture. Complete `put` and `prepare_mutation` under normal measured authority limits before monkeypatching only `cacheness.storage.reconciliation.time`. Retain the existing finite deterministic clock sequence (`0.0` to establish the reconciliation deadline, then `1.0` to expire it), assert the expected clock-call count, and retain the exact report assertions that zero operation records were consumed and a resume token was returned. This proves the reconciliation loop stopped on its own injected time budget rather than a cold SQLite timeout. Do not change `LifecycleLimits`, SQLite authority code, reconciliation production code, benchmark code/data, multipliers, or envelopes. Cold setup timing is an unrelated precondition and is not authority to relax runtime or reconciliation behavior.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_concurrency.py tests/test_sqlite_concurrency_temp.py tests/test_blob_store_reconciliation.py tests/test_sqlite_authority_admission.py tests/test_phase3_postreview_concurrency.py tests/test_cached_query_meta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_sqlite_concurrency.py tests/test_sqlite_concurrency_temp.py tests/test_blob_store_reconciliation.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>Only the three named extreme schedules in each duplicate suite accept exact bounded lifecycle timeout reasons/stages while proving bounded completion, at least 75 percent per-test success, per-worker progress, exact attempt accounting, committed-value integrity, and zero ticket leakage. Ordinary put/mixed/file/stats/integrity and query/get-only tests remain strict all-success checks. Reconciliation timing is isolated from cold authority setup; all release and direct-Ruff gates pass with production defaults and benchmark evidence unchanged.</acceptance_criteria>
  <done>RED checkpoint committed at `fcc54ac`: strict test-policy corrections are preserved, and reader-only `query_meta` remains intentionally failing until Task 5 removes unintended metadata write contention.</done>
</completed_red_checkpoint>

<task type="tracer" tdd="true">
  <name>Task 5: Make delegated SQLite metadata queries read-only under pooled concurrency</name>
  <files>src/cacheness/metadata.py, src/cacheness/core.py, tests/test_cached_query_meta.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/core.py</file>
    <file>tests/test_cached_query_meta.py</file>
    <file>tests/test_sqlite_concurrency.py</file>
    <file>tests/test_sqlite_concurrency_temp.py</file>
    <file>tests/test_query_meta.py</file>
    <file>tests/test_query_meta_security.py</file>
    <file>tests/test_cached_custom_metadata.py</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
  </read_first>
  <behavior>
    - "After pre-populating a cached file-backed SQLite facade, dispose/empty the metadata engine pool, start six public `query_meta` callers on one barrier, and require all 48 calls to return the exact matching concrete lists within bounded joins; no call may log/return None, raise a raw lock, or receive a lifecycle timeout."
    - "With a separate sqlite3 connection holding `BEGIN IMMEDIATE` on the metadata database, newly opened pooled connections still execute public `query_meta` and return the last committed matching rows before that external writer is released; WAL readers do not attempt a writer-side PRAGMA."
    - "The SQLAlchemy connect hook applies only connection-local settings that cannot change database schema/header/journal state or run maintenance. `journal_mode=WAL` is established once at a constructor-owned initialization boundary before SessionLocal/backend publication, and `optimize`, schema creation, stats initialization, and any other write-capable setup never run during pool checkout."
    - "Two fresh spawned processes released together can initialize and close independent SqliteBackend instances against one previously absent file; both complete boundedly, the final database is WAL with the current schema/stats, and no process relies on a Python/global lock. SqliteBackend(':memory:') keeps its supported non-WAL behavior."
    - "Every backend query fully materializes results inside its backend-owned session context; success and injected exception paths return the metadata QueuePool checked-out count to zero and leave no transaction open. An injected constructor bootstrap failure likewise closes its connection and disposes the provisional engine. CachedMetadataBackend delegates without exposing or retaining a SessionLocal/session."
    - "Aggregate query visibility is observational: it compares authenticated committed authority entries to compatibility rows without calling `_sync_authority_projection`, conditional projection mutation, schema creation, or custom-link cleanup. A mismatch is omitted and left as reconciliation evidence."
    - "The query capability receives exact `(cache_key, actual_path)` authority observations and the SQLite WHERE clause binds both columns. If a projection changes between authority observation and SQL execution, the old token matches no row; the query never admits the replacement merely because the key is unchanged."
    - "The exact-pair predicate is one tuple-membership expression (or an OR of paired AND clauses), never independent key-IN/path-IN sets whose cross-product could admit a mixed pair."
    - "Unsupported-backend warnings, invalid-filter exceptions, empty-live results, cached-wrapper behavior, and dictionary/timestamp shapes remain unchanged. A backend translates only SQLAlchemy DBAPI operational failures to a caused CacheMetadataError; query_meta logs/returns None only for that domain failure, while assertion/programming errors and authority-manifest integrity failures propagate. The fix adds no retry, sleep, process-global lock, or public API."
  </behavior>
  <action>Keep `fcc54ac` intact and first add focused deterministic regressions to `tests/test_cached_query_meta.py`. Use a file-backed SQLite `UnifiedCache` with `enable_memory_cache=True`, pre-populate stable committed entries, unwrap only to obtain the owned engine for test orchestration, dispose idle pooled connections so the reader wave must create new DB-API connections, and release six threads on a Barrier. Require all public calls and bounded joins to complete with exact list contents, no `None`, and no captured exception; after completion and after an injected backend-query exception, assert the engine pool reports zero checked-out connections. Do not use sleeps as synchronization or call the backend query helper instead of the public facade.

Add a second real-path schedule that opens an independent `sqlite3` connection to the metadata database, commits setup, acquires `BEGIN IMMEDIATE`, and keeps that writer transaction open while a barrier releases new-connection public readers. Require every reader to complete and return the last committed snapshot before the external writer rolls back/releases. This distinguishes WAL-safe SELECT behavior from the current connect-time `PRAGMA journal_mode=WAL` writer attempt. Add a bounded spawned-process schedule over a previously absent metadata file: in each child wrap the named constructor bootstrap helper so a process-shared Barrier is reached after its constructor connection is checked out but immediately before the persistent WAL statement, then invoke the real helper. Require both independently initialized backends to complete read checks/close, and verify afterward that one valid current schema/stats database exists in WAL mode. Use a POSIX process context and skip an unavailable context rather than claiming Windows; this is database-arbitrated initialization evidence, not permission for a module/global lock, retry, or sleep. Also retain direct `SqliteBackend(":memory:")` CRUD coverage and require that the non-file backend is not rejected merely because its journal mode is `memory`. In a separate test monkeypatch the named schema-bootstrap boundary to raise after its constructor connection has checked out, capture the provisional engine returned by the existing factory, and assert the failed constructor leaves zero checked-out connections and a disposed/empty pool while preserving the injected exception as the cause; this must not require publishing `SessionLocal` or a test-only production callback. Extract named database-bootstrap and per-connection configuration helpers so a source-level assertion can prove the connect listener calls only the read-safe helper and that `journal_mode`, `page_size`, `optimize`, and DDL are absent from that helper; do not add a production observer solely for tests. Preserve the existing raw-lock strictness in both historical modules; run their duplicate `test_concurrent_query_operations` nodes as acceptance tests rather than modifying or weakening them.

In `SqliteBackend.__init__`, split database bootstrap from per-connection configuration. Install the read-safe listener before the engine's first checkout, but keep the provisional engine/session factory constructor-local until initialization completes. For a file database, use one explicitly closed constructor-owned SQLAlchemy connection with `isolation_level="AUTOCOMMIT"` (no long-lived ORM transaction) to execute `PRAGMA journal_mode=WAL`, consume and require its returned mode to be `wal`, then perform idempotent `Base.metadata.create_all`, stats-row initialization, and the one retained `PRAGMA optimize` at named constructor/maintenance boundaries under SQLite's existing 30-second connection busy policy. Replace the current SELECT-then-INSERT stats bootstrap race with one SQLite `INSERT OR IGNORE` (all required default columns supplied) in a short committed transaction, so two fresh constructors cannot both observe absence and lose one initializer to a primary-key error. These steps may be repeated safely by independent constructors/processes and use SQLite file/schema arbitration only; add no Python/global initialization lock and no retry. For `:memory:`, skip the persistent-WAL requirement (its valid journal mode is `memory`) while still creating schema/stats on that backend's own pooled connection. Publish `self.engine` and `self.SessionLocal` only after the applicable bootstrap is complete. Remove `PRAGMA journal_mode=WAL`, `PRAGMA page_size`, and `PRAGMA optimize` from the engine `connect` listener: `journal_mode` changes persistent database state, `page_size` is a database-header setting (and the current post-WAL assignment is ineffective), and `optimize` may perform maintenance writes. Keep only proven connection-local settings in the listener, including the existing 30-second metadata busy policy, foreign keys, synchronous/cache/temp/mmap settings, analysis-limit, and WAL-autocheckpoint configuration; do not change their values. Do not change an existing database's page size or make the previously ineffective 32768 assignment effective. On every bootstrap failure, close the active connection/session if any, dispose the provisional engine, leave `engine`/`SessionLocal` unavailable, and re-raise with the original exception as cause. This metadata-engine policy is separate from, and must not import or alter, LifecycleAuthority's 0.187-second deadline.

Keep the backend-neutral capability introduced by Task 3, but strengthen its internal argument from key-only membership to immutable authority projection tokens. In `UnifiedCache`, derive a read-only mapping/tuple of exact committed `(cache_key, actual_path)` observations from one projection listing followed by authority entries whose manifests are authenticated: include only exact projection/authority matches, omit pending/tombstone/missing/mismatched rows, and never call `_authority_snapshot_entry()` or `_sync_authority_projection()` from this aggregate-query helper. That ordering may conservatively omit a generation replaced between observations; it may not synthesize or repair one. Reuse only the keys from those validated pairs for custom metadata queries, whose object/link rows are projection-owned and transactionally cleaned/relinked only after a generation is current, and pass the full exact pairs into `query_entries_by_key_params`. In `MetadataBackend`, `CachedMetadataBackend`, and `SqliteBackend`, keep session ownership behind the capability and bind each `CacheEntry.cache_key` with its corresponding `CacheEntry.actual_path` using SQLAlchemy tuple membership or an OR of paired AND expressions; explicitly forbid independent key/path membership sets and interpolation. Return immediately for an empty pair set. Fully materialize dictionaries before leaving `with self.SessionLocal() as session`, and rely on context-manager rollback/close on exceptions. Add a public stale-token/replacement interleaving test by wrapping the delegated backend method with a test Barrier after core has supplied its exact token, performing a real same-key public `put` through an already-constructed second facade before releasing the original backend call, and requiring the stale token to return no mixed row. Require the captured pair to be M1 and the final stored projection/authority pair to be the distinct M2 locator so the test cannot pass without exercising replacement. Separately commit M1, prepare but do not promote an M2 mutation through the real lifecycle authority, force the compatibility projection to M2's candidate locator with a custom link, replace both `_authority_snapshot_entry` and `_sync_authority_projection` with fail-if-called test sentinels, and prove public built-in and custom aggregate queries omit the row while the projection row, M1 authority row/manifest bytes, M2 pending-operation evidence, and custom-link evidence remain byte-for-byte unchanged. Existing point get/list/reconciliation paths retain their current explicit repair responsibilities; only aggregate visibility becomes observational.

Do not change the public `query_meta` signature or its established validation, unsupported warning/None, or empty-list policy. Replace its catch-all with a narrow `CacheMetadataError` operational path after `CacheQueryValidationError`; in both SQLite operations used by this public path (`list_entries` during exact-pair observation and `query_entries_by_key_params` during delegated filtering), catch only SQLAlchemy `DBAPIError` raised by checkout/query/session execution, translate it to `CacheMetadataError` with operation/backend context and `raise ... from error`, and let assertion, programming, validation, authority-conflict, lifecycle-integrity, and all non-DBAPI exceptions propagate. Deterministically inject a caused SQLAlchemy DBAPI operational error after session checkout at each of those two stages to prove public logging/None plus zero checked-out connections, and separately inject `AssertionError` to prove it is not swallowed or relabeled. Do not change JSON/in-memory error semantics while making this SQLite boundary typed. The production correction is eliminating write-capable checkout and key-only admission, not masking `OperationalError`: add no retry loop, delay, broad exception handler, timeout-classification exception, facade/global lock, or SessionLocal sniffing. Preserve D-03 committed-only visibility, D-18 backend/CAS authority, D-20 distinct-key concurrency, D-24 facade/authority separation, D-26 SQLite ownership, and D-28 short writer transactions. Modify no LifecycleAuthority/config limit, benchmark file/code/envelope, qualification artifact, or PostgreSQL claim.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_cached_query_meta.py tests/test_sqlite_concurrency.py::TestSQLiteConcurrency::test_concurrent_query_operations tests/test_sqlite_concurrency_temp.py::TestSQLiteConcurrency::test_concurrent_query_operations tests/test_query_meta.py tests/test_query_meta_security.py tests/test_cached_custom_metadata.py -x -o log_cli=false &amp;&amp; uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_concurrency.py tests/test_sqlite_concurrency_temp.py tests/test_blob_store_reconciliation.py tests/test_sqlite_authority_admission.py tests/test_phase3_postreview_concurrency.py tests/test_unified_cache_lifecycle_authority.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_cached_query_meta.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>All six reader threads and all 48 public query calls succeed against a fresh QueuePool both normally and while an external metadata writer owns BEGIN IMMEDIATE; two fresh processes safely initialize one absent file and the in-memory variant remains valid; connection checkout performs no write-capable setup; exact paired locator tokens prevent mixed-generation rows; mismatches remain invisible without repair; query and failed-bootstrap resources retire to zero; only caused DBAPI operational failures retain logged None while programming/integrity failures propagate; direct Ruff, both strict duplicate regressions, and every full release gate pass.</acceptance_criteria>
  <done>Task 4 turns GREEN through a production query-path correction, with public compatibility, committed-only visibility, authority timing, runtime defaults, benchmark evidence, Windows status, and live-PostgreSQL claims unchanged.</done>
</task>

</tasks>

## Gap Coverage Audit

| Fresh review finding | Owner | Deterministic acceptance evidence | Status |
|---|---|---|---|
| CR-01 timed-out clear FIFO tombstone | 03-18 Task 1 | Public A-active/B-timeout/C-success clear schedule plus zero queue/reference state | COMPLETED (`2bf0d84`, `2df8f4b`) |
| CR-02 additive SQLite timeout | 03-18 Task 2 | Combined observer dispatch + external BEGIN IMMEDIATE schedule bounded by one deadline | COMPLETED (`1792d48`, `5041ac8`, `1eebea8`) |
| CR-03 inherited fork admission state | 03-18 Task 2 | Fork with owned gate and separately held registry lock; inherited object rejects and fresh child succeeds | COMPLETED (`1792d48`, `5041ac8`, `1eebea8`) |
| WR-01 cached query_meta capability loss | 03-18 Task 3 | Public cached SQLite match/live filtering and unsupported-backend policy tests | COMPLETED (`0ebf2d6`, `9f248d9`) |
| Release-test policy: typed bounded contention rejected while raw lock text accepted | 03-18 Tasks 4/5 | Strict scopes/accounting committed in `fcc54ac`; Task 5 runs the entire corrected suite after the production reader fix | COVERED |
| Release-test policy: reconciliation clock coupled to 1 ms cold authority setup | 03-18 Tasks 4/5 | Normal-limit setup plus independently injected expired reconciliation clock, rerun in Task 5 final gate | COVERED |
| Reader-only pool expansion performs write-capable PRAGMAs | 03-18 Task 5 | Six-barrier public query wave over fresh QueuePool connections, including external BEGIN IMMEDIATE writer ownership; forced concurrent fresh-process bootstrap and `:memory:` coverage protect initialization semantics | COVERED |
| Aggregate query can repair projection and key-only SQL can admit a replacement | 03-18 Task 5 | Public mismatch/no-repair evidence test plus M1-observation/M2-replacement paired-predicate SQL schedule | COVERED |
| Backend query resource lifetime/error policy under success/failure | 03-18 Task 5 | Materialized results, DBAPI failures at observation/query stages, failed constructor bootstrap, and programmer-failure propagation prove zero checkout plus narrow logged-None behavior | COVERED |

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generation with every incomplete outcome recoverable | 03-18 Tasks 1-5 | COVERED | Completed repairs and RED policy remain intact; Task 5 makes read-only visibility exact-token coherent and qualifies the full result. |
| REQ | STOR-03 | Never expose partial payload/metadata state | 03-18 Tasks 2-5 | COVERED | Authority timing remains transactional, Task 4 reads successful stress writes exactly, and Task 5 binds query rows to exact committed locators. |
| REQ | STOR-04 | Failed write preserves valid generation and recoverable residue | 03-18 Task 2 plus 03-17 regressions | COVERED | Admission reset changes no authority CAS/evidence and full prior failure suites rerun. |
| REQ | STOR-05 | Idempotent overwrite/delete/clear/close cleanup | 03-18 Tasks 1/4 | COVERED | Pre-entry clear failure retires exact queue state; Task 4 gates zero admission leakage after stress. |
| REQ | STOR-06 | Safe bounded reconciliation | 03-18 Tasks 2/4 | COVERED | Explicit outer deadlines remain hard caps and Task 4 isolates the deterministic reconciliation clock from cold setup. |
| REQ | STOR-07 | Deterministic races without global distinct-key serialization | 03-18 Tasks 1/2/4/5 | COVERED | FIFO/fork behavior is complete; strict stress policy remains, and pooled reader plus reader/writer schedules require concurrency without a new facade/global lock. |
| RESEARCH | — | LifecycleAuthority remains sole canonical transaction authority | 03-18 Tasks 1-5 | COVERED | Metadata bootstrap and read-only filtering remain projection mechanics; no alternate lifecycle authority is introduced. |
| RESEARCH | — | Immutable payload work remains outside short transactions | 03-18 Task 2 | COVERED | No gate/deadline fix spans payload publication or cleanup. |
| RESEARCH | — | Bounded deterministic fault/process schedules and measured release gates | 03-18 Tasks 1-5 | COVERED | Task 4 aligns stress policy; Task 5 uses barriers/external BEGIN IMMEDIATE and preserves the unchanged benchmark verifier. |
| CONTEXT | D-01–D-08 | Immutable exact-CAS publication and recoverable residue | 03-18 Task 2 plus full gate | COVERED | Deadline/fork changes are local admission mechanics only. |
| CONTEXT | D-09–D-12 | Tombstone, clear, cleanup, close convergence | 03-18 Task 1 | COVERED | Exact queued-clear retirement restores convergence without disturbing active ownership. |
| CONTEXT | D-13–D-16 | Bounded evidence-driven reconciliation | 03-18 Tasks 2/4/5 | COVERED | Reconciliation timing is isolated; query mismatches are omitted as evidence rather than destructively repaired. |
| CONTEXT | D-17–D-20 | Retiring local coordination, backend CAS, deterministic races, distinct-key progress | 03-18 Tasks 1/2/4/5 | COVERED | Failed tickets retire, stress accounting is strict, and query readers remain concurrent without repair writes or key-only substitution. |
| CONTEXT | D-21–D-22 | Trusted local owner and limited Windows topology | 03-18 Task 2 | COVERED | Fork safety strengthens POSIX process isolation without broadening Windows claims. |
| CONTEXT | D-23–D-31 | SQLite authority replacement, short transactions, measured bounds, no file scheduler | 03-18 Tasks 1-5 | COVERED | Task 5 separates metadata initialization from checkout without touching authority transactions or adding scheduler/lock authority. |
| CONTEXT | D-32 | Native Windows qualification deferred | 03-18 Task 5 gate | COVERED | Darwin stays UNAVAILABLE/NOT_QUALIFIED with native_evidence false; evidence is verify-only. |

<threat_model>
OWASP ASVS L1 applies. Every high-severity item is blocking and must be mitigated before Plan 18 can complete.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Timed-out clear contender → instance FIFO | Failed pre-entry work must not become immortal queue state. |
| Python admission/scheduler → SQLite busy handler | Every stage must consume one caller-owned absolute deadline. |
| Multithreaded parent → forked child | Copied mutexes/tickets are untrusted unusable process state. |
| UnifiedCache policy → metadata backend | Capability must be explicit without leaking SQL session ownership. |
| Derived projection → public metadata query | Only keys proven committed by authority may be returned. |
| SQLAlchemy pool checkout → SQLite metadata file | A nominal reader must not execute persistent PRAGMAs, DDL, maintenance, or projection repair while acquiring a connection. |
| Authority observation → delegated SQL predicate | Key identity alone is insufficient across projection replacement; the immutable locator is part of the trusted comparison token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-18-01 | Denial of Service | InstanceAdmission clear FIFO | high | mitigate | Exact-ticket discard and notify-all on every pre-entry exit, with public A/B/C progress test and zero-state assertion. |
| T-03-18-02 | Denial of Service | SQLite BEGIN IMMEDIATE | high | mitigate | Reapply conservative remaining busy_timeout immediately before BEGIN and enforce one absolute deadline plus small scheduler tolerance. |
| T-03-18-03 | Repudiation | SQLite timeout translation | high | mitigate | Preserve OperationalError cause and stage/elapsed/budget context for genuine busy expiry. |
| T-03-18-04 | Denial of Service | Forked writer-admission registry | critical | mitigate | after_in_child rebinds both registry and lock; owned-gate and held-lock fork tests prove fresh child progress. |
| T-03-18-05 | Tampering | Fork PID shortcut | high | mitigate | Reject inherited authority and explicitly test that PID-qualified keys alone cannot bypass an inherited locked registry. |
| T-03-18-06 | Information Disclosure | query_meta backend boundary | high | mitigate | Backend owns its query/session; facade passes validated filters and committed live keys only. |
| T-03-18-07 | Spoofing | Cached capability advertisement | high | mitigate | Wrapper delegates explicit support and execution methods; unsupported backends return the established None/warning policy. |
| T-03-18-08 | Denial of Service | Distinct-key work | high | mitigate | No new whole-cache/payload lock; only existing short SQLite writer interval is ordered and overlap regressions remain mandatory. |
| T-03-18-09 | Repudiation | Historical concurrency tests | high | mitigate | Replace raw-message allowances with exact error type/reason/stage classification and retain every other exception as a hard failure. |
| T-03-18-10 | Tampering | Stress success accounting | high | mitigate | Account for every attempted call, require ≥75% success and per-worker progress, read back committed values, and assert zero registry leakage. |
| T-03-18-11 | Denial of Service | Test worker lifecycle | high | mitigate | Bound every future/join and preserve elapsed deadlock limits even when expected typed timeouts occur. |
| T-03-18-12 | Repudiation | Reconciliation time-limit evidence | high | mitigate | Use normal authority limits for setup and inject expiration only at the reconciliation clock boundary being tested. |
| T-03-18-13 | Tampering | Runtime/benchmark policy | high | mitigate | Task 5 changes metadata query implementation only; full benchmark verification rejects authority/runtime/default/envelope drift. |
| T-03-18-14 | Denial of Service | SQLite metadata pool checkout/bootstrap | high | mitigate | Move persistent/write-capable PRAGMAs and maintenance to constructor-owned bootstrap; six-reader/external-writer schedules require bounded read progress, while fresh spawned constructors prove idempotent database arbitration without a global lock and `:memory:` retains its valid non-WAL mode. |
| T-03-18-15 | Tampering | Aggregate live-key derivation | high | mitigate | Query visibility becomes read-only and exact-token based; mismatches are omitted without projection mutation. |
| T-03-18-16 | Spoofing | Key-only projection replacement | high | mitigate | Backend SQL binds each key with its corresponding immutable actual_path in one paired predicate so neither a later row nor a key/path cross-product can impersonate the observed generation. |
| T-03-18-17 | Denial of Service | SQLAlchemy session/pool lifetime | high | mitigate | Materialize within backend-owned contexts and assert zero checked-out connections after success, caused DBAPI failure, and constructor-bootstrap failure; programmer/integrity failures are never swallowed as misses. |
</threat_model>

<verification>
Begin at Task 5; Tasks 1-3 and Task 4 RED commit `fcc54ac` must not replay. First run the deterministic cached-wrapper QueuePool, external-writer/WAL, exact-token, mismatch/no-repair, and connection-retirement tests plus both strict duplicate reader-only nodes on Python 3.11. Then run all of Task 4's concurrency/reconciliation policy tests and completed admission/deadline/fork regressions. Finish with the exact frozen isolated full repository suite, unchanged checked-in benchmark verifier, compatibility corpus, verify-only qualification attestation, and Phase 3 Ruff delta. Only `src/cacheness/metadata.py`, `src/cacheness/core.py`, and `tests/test_cached_query_meta.py` may receive new Task 5 changes; the three `fcc54ac` test files remain immutable RED evidence. Do not edit LifecycleAuthority, runtime limits, benchmark data/code, Windows evidence, or PostgreSQL behavior, and do not claim live PostgreSQL qualification.
</verification>

<success_criteria>
- A timed-out queued clear cannot strand later clears or retain queue/admission state.
- SQLite preflight, queue, dispatch, and busy acquisition finish or time out within one unchanged absolute deadline plus a named small scheduler tolerance.
- Busy timeout translation retains the sqlite3.OperationalError cause and exact stage context.
- A fresh post-fork authority cannot observe any inherited registry, lock, gate, ticket, owner, or refcount.
- Cached SQLite query_meta returns committed matching entries through explicit backend delegation; unsupported policy remains compatible.
- Only high-concurrency stress, concurrent metadata stress, and deadlock-prevention accept exact CacheBlobLifecycleTimeoutError reason/stage combinations; each accounts for every call, finishes behind a bounded process join, achieves at least 75 percent success with per-worker progress, preserves committed values, and leaks no writer tickets.
- Ordinary concurrent put/mixed/file/stats/integrity and query/get-only tests require complete success and do not invoke the timeout classifier.
- Reconciliation deadline evidence is driven only by the injected reconciliation clock after normal-limit authority setup.
- Public query_meta pool expansion is read-only: six readers succeed concurrently, including during an external metadata BEGIN IMMEDIATE, and no pooled connection or transaction leaks.
- Aggregate query filtering neither repairs projections nor admits a same-key replacement; exact authority key-plus-locator pairs define visibility.
- Full suite, benchmark, compatibility, qualification verification, and Phase 3 Ruff delta gates pass.
- Native Windows remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false; live PostgreSQL remains unclaimed.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-18-SUMMARY.md` after Task 5 completes. Preserve and enumerate the seven Task 1-3 commits plus RED checkpoint `fcc54ac`; record the exact accepted timeout type/reason/stage set, actual stress accounting, committed-value/registry retirement, isolated reconciliation clock, the diagnosed QueuePool connect-time writer cause, constructor-versus-connect PRAGMA split, six-reader/external-writer results, exact-token/no-repair visibility proof, zero checked-out connections, exact full release-gate results, and unchanged Windows/live-PostgreSQL status. Keep Task 4 stress observations separate from the checked-in benchmark provenance; do not merge transient diagnostics into, recapture, or relabel the baseline.
</output>
