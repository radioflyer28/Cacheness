---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "07"
status: superseded
type: execute
wave: 7
depends_on: [03-06]
files_modified:
  - src/cacheness/storage/reconciliation.py
  - src/cacheness/storage/operation_repository.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/error_handling.py
  - tests/test_blob_store_reconciliation.py
autonomous: true
requirements: [STOR-04, STOR-06]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "`BlobStore.reconcile()` is dry-run by default, byte-for-byte non-mutating, deterministic, paged, and returns a stable machine report plus human summary (D-13)."
    - "Reconciliation merges bounded ManifestPage and operation-record pages with independent stable cursors, so manifest-only inconsistencies are visible without claiming an unprovable payload inventory (D-13, D-15, D-16)."
    - "Every finding records authoritative generation, operation provenance, residue type, proposed action, reason, and safe/blocked/requires-confirmation status."
    - "Apply re-authenticates/revalidates immediately before every action, durably checkpoints completion, and resumes without repeating destructive work (D-14)."
    - "Malformed, future, ambiguous, escaping, symlinked, or provenance-free evidence is untouched unless a separately proven safe contained quarantine exists; payloads are never deserialized (D-15, D-16)."
    - "Reconciler receives BlobStore's exact `CacheConfig.lifecycle_limits` object; caller overrides of `max_reconcile_actions` and `orphan_grace_seconds` bound actions and frozen-clock eligibility without a duplicate policy."
  artifacts:
    - path: src/cacheness/storage/reconciliation.py
      provides: "ReconciliationFinding, ReconciliationReport, and Reconciler dry-run/apply engine"
    - path: src/cacheness/storage/operation_repository.py
      provides: "Exact durable reconciliation action checkpoints and dual-cursor resume state"
    - path: tests/test_blob_store_reconciliation.py
      provides: "Dry-run immutability, bounded manifest/operation paging, manifest-only findings, apply/revalidation, checkpoint/resume contract"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/reconciliation.py
      via: "public reconcile facade is dry-run by default"
      pattern: "reconcile"
    - from: src/cacheness/storage/reconciliation.py
      to: src/cacheness/storage/lifecycle.py
      via: "safe actions reuse lifecycle recovery/reclamation primitives"
      pattern: "LifecycleEngine"
    - from: src/cacheness/storage/reconciliation.py
      to: src/cacheness/storage/manifest_repository.py
      via: "ManifestPage cursor supplies bounded manifest-authority inventory alongside operation pages"
      pattern: "(ManifestPage|list_page)"
    - from: src/cacheness/storage/reconciliation.py
      to: src/cacheness/storage/coordination.py
      via: "Plan 03-05 StoreAdmissionBarrier bounds snapshot/revalidation setup before apply"
      pattern: "StoreAdmissionBarrier"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Give operators deterministic dry-run and resumable apply reconciliation over authenticated lifecycle evidence.

Purpose: Repair or report crash residue without guessing ownership, future schemas, or payload contents.
Output: Stable reports, bounded analysis, revalidated actions, checkpoints, and safe quarantine/report policy.
</objective>

## Artifacts this phase produces

- New public `ReconciliationStatus`, `ReconciliationAction`, `ReconciliationFinding`, `ReconciliationReport`, and `BlobStore.reconcile(...)` exported from `cacheness.storage`.
- New internal `Reconciler` in `reconciliation.py` and exact durable reconciliation apply cursor/checkpoint support in `operation_repository.py`.
- New public errors `CacheBlobOperationIntegrityError`, `CacheBlobOperationUnsupportedVersionError`, and `CacheBlobReconciliationConflictError` with stable reasons and compatible bases.
- Completed `tests/test_blob_store_reconciliation.py`; no other new files.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-03-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-05-SUMMARY.md
</context>

<interfaces>
The config-owned `LifecycleLimits` supplies page/action/grace bounds to storage consumers. Plan 03-05 provides `ManifestRepository.list_page -> ManifestPage`, exact durable clear-target checkpoints, and `StoreAdmissionBarrier`; `OperationRecordRepository.list_page` returns exact evidence bytes and stable cursors. The reconciler consumes manifest and operation pages through independent bounded cursors, must call lifecycle recovery primitives for mutations, and must not call handlers.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produce stable zero-mutation dry-run reports</name>
  <depends_on>Plan 03-06 completes predecessor-evidence compatibility after Plans 03-03 and 03-05 supply bounded evidence pages and mutation/clear states.</depends_on>
  <files>src/cacheness/storage/reconciliation.py, src/cacheness/storage/operation_repository.py, src/cacheness/storage/blob_store.py, src/cacheness/error_handling.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/operation_record.py</file>
    <file>src/cacheness/storage/operation_repository.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_clear_recovery.py</file>
    <file>.planning/phases/02-canonical-storage-and-integrity-contract/02-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - "Default reconcile performs no write/delete/quarantine/key creation/access update and returns byte-identical deterministic machine output for the same snapshot."
    - "Findings distinguish uncommitted candidate, committed cleanup debt, tombstone debt, completed/retirable evidence, conflict, malformed/future/ambiguous evidence, and unsupported inventory."
    - "Bounded manifest and operation pages are merged deterministically with independent resumable cursors; call counts remain page plus a documented constant per item."
    - "An authenticated manifest-only missing-reference or tombstone inconsistency is reported from manifest authority; unprovable payload namespace inventory is blocked/unsupported and untouched."
    - "Each finding includes exact locked fields and redacts payload/key material and unsafe raw paths."
    - "Analysis never invokes a handler or infers ownership from payload bytes or loose locator names."
  </behavior>
  <action>Create frozen report value types with deterministic ordering and JSON-compatible `to_dict`, plus a bounded human summary. Accept the exact `LifecycleLimits` instance retained by BlobStore; use its operation/manifest page sizes, `max_reconcile_actions`, and `orphan_grace_seconds` directly without rebuilding configuration. Implement `Reconciler.analyze` as a deterministic merge over Plan 03-05's stable `ManifestPage` cursor and stable operation-record pages, carrying independent cursors in the report/resume token and classifying residue exclusively from authenticated authority plus validated provenance. Include authenticated manifest-only missing-reference/tombstone inconsistencies even when no operation record exists. Represent payload inventory that cannot prove ownership as blocked/unsupported and untouched; never scan candidate-like compatibility filenames. `BlobStore.reconcile()` defaults to dry run and exposes cursor/page/action policy without mutation. Add full pre/post audit snapshots of manifest bytes, operation bytes, payload hashes/bytes, mtimes, key bytes, directory membership, backend counters, bounded page/call counts, and mutation spies. With caller-owned small limits, assert action cutoff/resume and frozen-clock pre/post-grace findings while identity spies prove the reconciler received the same object. Report malformed, future, ambiguous, provenance-free, unsupported inventory, and unsafe locator evidence untouched; do not deserialize payloads, create a quarantine directory during analysis, or use normal read APIs that update state. Define the precise reason-coded error classes needed by these branches in `error_handling.py`, preserving compatible broad bases and causes; Task 3 owns public barrel exports.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "dry_run or report or deterministic or manifest_page or manifest_only or bounded_call or unsupported_inventory or configured_limits or grace or action_bound" -x</automated>
  </verify>
  <acceptance_criteria>Two dry runs yield identical output and state; caller small action/grace overrides change cutoff/resume and frozen-clock eligibility; Reconciler retains BlobStore's exact limits identity; required findings are asserted and mutation/handler spies remain empty.</acceptance_criteria>
  <done>Operators can inspect lifecycle inconsistency safely and deterministically without changing the store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revalidate, checkpoint, and resume every safe apply action</name>
  <depends_on>Task 1 provides stable findings and proposed actions.</depends_on>
  <files>src/cacheness/storage/reconciliation.py, src/cacheness/storage/operation_repository.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/path_security.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "Apply re-authenticates manifest and operation evidence immediately before each action and blocks if either changed since analysis."
    - "Each successful action has a durable exact checkpoint; BaseException after every checkpoint reopens and resumes without repeating a successful destructive call."
    - "Automatic repair is limited to proven rollback/roll-forward/retirement; safe quarantine requires contained backend-native move and otherwise becomes report-only."
    - "The last valid generation, a different winner, malformed/future evidence, and provenance-free residue are never removed."
  </behavior>
  <action>Implement explicit `apply=True` using the same bounded dual-cursor report and Plan 03-05's `StoreAdmissionBarrier` only around snapshot/revalidation setup. Before each action, reload and authenticate exact manifest/evidence bytes; authority drift changes disposition to blocked/conflict. Persist an exact reconciliation action checkpoint before advancing either cursor, delegate proven candidate cleanup, old-generation reclamation, tombstone completion, and evidence retirement to `LifecycleEngine`, then conditionally mark that action complete. Add an optional contained backend-native quarantine action only when `ManagedFileOps` can atomically move an authenticated owned locator without leaving the root; otherwise retain report-only `requires_confirmation`/blocked. Interrupt before/after each destructive action and checkpoint with `BaseException`, reopen, and assert cursor/checkpoint bytes plus call spies prove successful actions are not repeated. Never quarantine or delete the only valid copy.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "apply or revalidate or checkpoint or resume or quarantine or last_valid" -x &amp;&amp; uv run ruff check src/cacheness/storage/reconciliation.py src/cacheness/storage/operation_repository.py src/cacheness/storage/blob_store.py src/cacheness/storage/__init__.py src/cacheness/error_handling.py tests/test_blob_store_reconciliation.py</automated>
  </verify>
  <acceptance_criteria>Every safe action resumes after interruption, authority drift blocks mutation, repeated apply converges with no repeated destructive calls, and all unsafe/ambiguous/only-copy cases remain exact-byte untouched.</acceptance_criteria>
  <done>Reconciliation apply is bounded, evidence-gated, resumable, and incapable of guessing away the winner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Export the stable reconciliation and typed failure surface</name>
  <depends_on>Tasks 1 and 2 complete the report values and all typed apply outcomes.</depends_on>
  <files>src/cacheness/storage/reconciliation.py, src/cacheness/storage/__init__.py, src/cacheness/error_handling.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/__init__.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/test_blob_store_translation_seam.py</file>
  </read_first>
  <behavior>
    - "Stable report/status/action values and BlobStore.reconcile are importable from cacheness.storage."
    - "Operation integrity/version and reconciliation conflict errors retain existing CacheStorageError/CacheBlobIntegrityError compatibility bases and stable lower-snake-case reasons."
    - "Implementation-only codecs, repositories, coordination entries, and signing material are not exported."
  </behavior>
  <action>Finalize the public surface after behavior is green: export only `ReconciliationStatus`, `ReconciliationAction`, `ReconciliationFinding`, `ReconciliationReport`, and the precise typed failures required for caller branching. Keep `Reconciler`, operation codecs/repositories, mutable checkpoints, coordination internals, and key material private. Add import/type/reason/inheritance tests and verify that constructing or serializing reports has no store mutation or `UnifiedCache` dependency.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "public or import or reason or inheritance or pure" -x &amp;&amp; uv run ruff check src/cacheness/storage/reconciliation.py src/cacheness/storage/operation_repository.py src/cacheness/storage/blob_store.py src/cacheness/storage/__init__.py src/cacheness/error_handling.py tests/test_blob_store_reconciliation.py</automated>
  </verify>
  <acceptance_criteria>The documented public report and failure symbols import successfully with exact types/reasons; implementation internals remain absent from `__all__`; report construction/serialization is pure.</acceptance_criteria>
  <done>Operators and callers have a stable minimal reconciliation surface without exposing lifecycle internals.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V2 business logic | Immediate revalidation and exact disposition gate every action. | Authority-drift and resume tests. |
| V5 file handling | Contained quarantine/delete only for authenticated owned locators. | Escape/symlink/quarantine tests. |
| V16 logging/error handling | Stable redacted reports preserve reason/provenance without payloads or keys. | Report schema/redaction tests. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted inventory -> dry-run report | Untrusted control evidence must remain non-authorizing until authenticated and bounded. |
| report finding -> apply action | A stale report cannot authorize mutation; current state is revalidated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-17 | Tampering / Repudiation | dry-run analyzer | high | mitigate | Dedicated non-mutating path plus exact pre/post bytes, mtimes, keys, counters, and call spies. |
| T-03-18 | Tampering / Denial of Service | reconciliation apply | high | mitigate | Immediate exact revalidation, bounded actions, durable checkpoints, and lifecycle primitive reuse. |
| T-03-19 | Elevation of Privilege | payload ownership inference | high | mitigate | No handler/deserialization/filename inference; authenticated provenance and contained locators only. |
| T-03-20 | Information Disclosure | operator report | medium | mitigate | Stable bounded IDs/reasons and relative/redacted locators; never payload or signing-key bytes. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-17 | `uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "dry_run or non_mutating" -x` |
| T-03-18 | `uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "apply or revalidate or resume" -x` |
| T-03-19 | `uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "handler_forbidden or provenance_free or escape or symlink" -x` |
</threat_model>

<verification>
Run the complete reconciliation module and targeted Ruff. All high threats require exact no-mutation/winner-preservation evidence and no unconditional skips.
</verification>

<success_criteria>
- Dry-run is deterministic and zero-mutation.
- Apply revalidates and checkpoints every action.
- Resume never repeats destructive work.
- Ambiguous/future/provenance-free/only-copy evidence remains untouched.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-07-SUMMARY.md` when done.
</output>
