---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "07"
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - src/cacheness/storage/operation_repository.py
  - src/cacheness/storage/operation_record.py
  - src/cacheness/storage/clear_recovery.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/__init__.py
  - tests/test_clear_recovery.py
  - tests/test_public_api_contract.py
  - tests/test_phase3_scheduler_retirement.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The receipt/inventory/head/tail/anchor/cursor/pending-control scheduler modules are physically deleted only after Plan 01 proves they were unreleased (D-23, D-30)."
    - "Known development scheduler controls produce a typed rebuild-required result without parsing, replay, deletion, directory creation, or authority bootstrap."
    - "No package barrel exports a scheduler symbol; lifecycle recovery remains available only through LifecycleAuthority."
  artifacts:
    - path: tests/test_phase3_scheduler_retirement.py
      provides: "Module, import, export, sentinel, and runtime negative-reachability proof"
  key_links:
    - from: tests/test_phase3_scheduler_retirement.py
      to: docs/lifecycle-authority.md
      via: "Release-evidence precondition and exact development-store rebuild outcome"
      pattern: "rebuild|required|scheduler"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No scheduler parser, replay adapter, compatibility wrapper, or conditional fallback may survive."
---

<objective>
Delete the abandoned file-native scheduler core after the fail-closed release checkpoint (D-23, D-30).

Purpose: Remove the non-converging protocol without mixing that deletion with retained security-helper refactoring.
Output: Deleted scheduler modules, rewritten public-behavior tests, and permanent negative-reachability gates.
</objective>

## Artifacts this phase produces

- Physical removal of operation records/repositories and clear journals.
- Typed, read-only scheduler-sentinel classification that never revives scheduler semantics.
- Import/export/runtime gates preventing scheduler resurrection.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-01-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-06-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete operation records, repositories, and clear journals</name>
  <precondition>03-01-SUMMARY.md proves no scheduler format crossed a supported release boundary and records the disposable development-store rebuild contract.</precondition>
  <reversibility rating="costly">D-23/D-30 remove a large internal engine; git preserves history, but runtime reintroduction would reopen the rejected architecture.</reversibility>
  <files>src/cacheness/storage/operation_repository.py, src/cacheness/storage/operation_record.py, src/cacheness/storage/clear_recovery.py, tests/test_clear_recovery.py, tests/test_phase3_scheduler_retirement.py</files>
  <read_first>
    <file>docs/lifecycle-authority.md</file>
    <file>src/cacheness/storage/operation_repository.py</file>
    <file>src/cacheness/storage/operation_record.py</file>
    <file>src/cacheness/storage/clear_recovery.py</file>
    <file>tests/test_clear_recovery.py</file>
  </read_first>
  <behavior>
    - "Removed modules cannot be imported and no production source imports their symbols."
    - "Known scheduler controls are classified before mutation and return the exact rebuild-required outcome without content parsing, replay, cleanup, or authority creation."
    - "Old/new, clear, resume, and recovery behavior remains covered through BlobStore and LifecycleAuthority public seams."
  </behavior>
  <action>Assert the Plan 01 release precondition, then delete the three scheduler modules per D-23/D-30. Replace tests of private receipts, inventories, cursors, and clear journals with public BlobStore/LifecycleAuthority behavior. Keep only a bounded exact-name/type sentinel classifier from Plan 02: it may identify known controls and fail typed, but it must not open, decode, replay, rename, quarantine, or delete them. Preserve archived `.file-native.md` planning evidence as history only.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_clear_recovery.py tests/test_phase3_scheduler_retirement.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py -x</automated>
  </verify>
  <acceptance_criteria>The modules are absent and unimportable; development controls remain byte-for-byte unchanged and authority-free after open; public lifecycle recovery tests remain green.</acceptance_criteria>
  <done>The receipt, inventory, operation-record, and clear-journal scheduler core is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove scheduler exports and seal negative reachability</name>
  <reversibility rating="reversible">Barrel cleanup and denial tests can evolve without changing committed authority semantics.</reversibility>
  <files>src/cacheness/storage/__init__.py, src/cacheness/__init__.py, tests/test_public_api_contract.py, tests/test_phase3_scheduler_retirement.py</files>
  <read_first>
    <file>src/cacheness/storage/__init__.py</file>
    <file>src/cacheness/__init__.py</file>
    <file>tests/test_public_api_contract.py</file>
    <file>tests/test_phase3_scheduler_retirement.py</file>
  </read_first>
  <behavior>
    - "No public/private barrel exports or lazily imports a scheduler symbol."
    - "AST/import scans find no production import, factory, feature flag, fallback, or runtime construction path to removed scheduler types."
    - "Fresh, empty-inspected, mutated, and reopened stores create no scheduler control artifact."
  </behavior>
  <action>Remove scheduler exports and compatibility imports from both barrels. Extend negative-reachability tests across production AST imports, package exports, importlib resolution, monkeypatchable factories, fresh and reopened runtime trees, and string/symbol references. Allow scheduler vocabulary only in the sentinel classifier, explicit typed rebuild reason, and retirement denylist; those paths must have tests proving no parser or recovery engine is reachable.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_public_api_contract.py tests/test_phase3_scheduler_retirement.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Imports, exports, factories, runtime trees, and reopen behavior expose only LifecycleAuthority; the denylist test fails on any scheduler resurrection.</acceptance_criteria>
  <done>The deleted scheduler cannot be reached through a supported or internal package path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| development controls → open | Abandoned controls are untrusted evidence and must never invoke a parser or recovery engine. |
| public import → runtime composition | Compatibility paths must not expose a second authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-07-01 | Tampering | development controls | high | mitigate | Exact read-only sentinel classification returns rebuild-required before mutation; no parse/replay/delete. |
| T-03-07-02 | Tampering | runtime composition | high | mitigate | AST/import/export/factory/runtime negative-reachability gates prohibit dual authority. |
| T-03-07-03 | Information Disclosure | diagnostics | medium | mitigate | Report bounded reason/control class only, without payload, key, or raw hostile path. |
</threat_model>

<verification>
Run the retirement/public-API suites and the checked-in Phase 3 Ruff delta gate; assert the three removed module paths are absent and unimportable.
</verification>

<success_criteria>
- Scheduler core modules are physically absent.
- Existing scheduler evidence fails typed and unchanged without creating the authority.
- No scheduler symbol or construction path is reachable.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-07-SUMMARY.md` when done.
</output>
