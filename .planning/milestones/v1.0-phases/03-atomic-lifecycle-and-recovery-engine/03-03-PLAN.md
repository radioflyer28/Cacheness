---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/cacheness/storage/lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/config.py
  - src/cacheness/error_handling.py
  - tests/test_lifecycle_authority_contract.py
  - tests/test_sqlite_lifecycle_authority.py
  - docs/lifecycle-authority.md
  - tests/test_phase3_windows_contract.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-07]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SQLite opens only the confirmed contained local authority, verifies database/store identity, and fails unchanged on a wrong object, symlink, wrong application ID, or future schema (D-21, D-22, D-26, D-31)."
    - "Every state-changing authority method uses a bounded explicit short BEGIN IMMEDIATE transaction with rollback on any failure and deterministic typed busy timeout (D-18, D-28)."
    - "Entry, intent, cleanup debt, clear targets, reconciliation checkpoints, projection revision, and ABA lineage change atomically within their complete transitions (D-24, D-25, D-29)."
    - "Windows support is executable only when a local NTFS root was provisioned before Cacheness starts and the current token's logon SID (S-1-5-5-X-Y) is the sole ordinary mutation principal on its protected DACL; Cacheness never creates or alters that root/DACL, and absent, unsafe, drifted, or unprovable scope fails unchanged with offline guidance before authority database creation/open/mutation (D-22, D-31)."
  artifacts:
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Complete durable stdlib-sqlite3 authority with normalized schema and process ownership"
    - path: tests/test_sqlite_lifecycle_authority.py
      provides: "Pragma, identity, migration, rollback, contention, integrity, close, fork, and topology proof"
    - path: src/cacheness/config.py
      provides: "Caller-owned absolute busy deadline and bounded authority record/page/action limits"
    - path: tests/test_phase3_windows_contract.py
      provides: "Platform-conditional pre-mutation topology rejection plus native Windows ACL/deployment assertions"
  key_links:
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Every complete interface transition implemented with parameterized SQL"
      pattern: "class SqliteLifecycleAuthority"
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: src/cacheness/error_handling.py
      via: "Typed capability, integrity, backend, lifecycle timeout, and conflict translation with preserved causes"
      pattern: "CacheBlob"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] SQLite rollback journals and sidecars must remain SQLite-owned and must never be scanned, copied, moved, or deleted by lifecycle reconciliation."
    - "[FLAGGED/UNVERIFIED] The adapter must not silently accept a remote/network filesystem or a weaker durability mode than declared."
    - "[FLAGGED/UNVERIFIED] SQL values and identifiers must not be constructed from persisted metadata or logical keys."
---

<objective>
Complete the durable local reference authority with explicit identity, durability, transaction, rollback, contention, and ownership contracts.

Purpose: Make SQLite—not custom files—the proven cross-process correctness boundary while keeping every writer transaction short.
Output: Full SqliteLifecycleAuthority, bounded config/error translation, and adversarial adapter tests.
</objective>

## Artifacts this phase produces

- Normalized authority schema for metadata, key lineage/entries, mutations, cleanup debt, clear runs/targets, reconciliation runs/actions, and projection state.
- Verified `journal_mode=DELETE`, `synchronous=EXTRA`, `foreign_keys=ON`, `trusted_schema=OFF`, application ID, and user version.
- Absolute busy-deadline, transaction observer, integrity-check, local-topology, PID/fork, and close contracts.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-02-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden SQLite identity, connection policy, busy deadline, and ownership</name>
  <reversibility rating="costly">The confirmed database identity and schema migration sequence become persistent state; changes require an ordered authority migration.</reversibility>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/config.py, src/cacheness/error_handling.py, tests/test_sqlite_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/path_security.py</file>
    <file>src/cacheness/storage/integrity.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/_lifecycle_test_support.py</file>
  </read_first>
  <behavior>
    - "A new and reopened database reports journal_mode delete, synchronous extra, foreign_keys on, trusted_schema off, application_id 0x43414348, and user_version 1."
    - "A symlink/non-regular reserved directory or database, wrong application ID/store identity, corrupt database, unsupported future user_version, or declared non-local topology fails typed without mutation or JSON reconstruction."
    - "An independently held writer produces a typed lifecycle timeout by one monotonic absolute deadline, rolls back, preserves the sqlite cause, and never sleeps beyond remaining budget."
    - "Inherited post-fork authority use fails; a child-created authority succeeds; close is idempotent and releases only owned resources."
  </behavior>
  <action>Use only stdlib `sqlite3` with method-scoped process/thread-owned connections, `isolation_level=None`, and parameter-bound constant SQL. Validate the reserved `.cacheness` directory and `lifecycle-authority-v1.sqlite3` as contained non-symlink objects before open. Non-Windows supported local filesystems may create the directory/database with restrictive permissions where supported. On Windows, require the directory to have been provisioned before process start and validate its token-bound protected DACL before creating/opening only the authority database; never create the root, call chmod, disable inheritance, or edit the DACL. Configure and verify rollback `DELETE`, `synchronous=EXTRA`, foreign keys, and trusted schema on every connection. Validate the confirmed application ID, user version, and generated store identity; perform ordered initialization/migrations in one bounded `BEGIN EXCLUSIVE`, and reject wrong/future/corrupt state without rebuilding from JSON. Carry a monotonic absolute deadline through connect, `busy_timeout`, retry, commit classification, and rollback; translate BUSY/LOCKED to the existing typed lifecycle timeout with cause. Capture PID and owned-resource state, reject inherited use after fork, and expose local-only capability per D-21/D-26/D-31.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_sqlite_lifecycle_authority.py -k "pragma or identity or locator or wrong_object or symlink or busy or fork or close or topology" -x</automated>
  </verify>
  <acceptance_criteria>All required pragmas and identities are read back and asserted; wrong objects and topology fail unchanged; busy work terminates by the absolute deadline with rollback/cause; fork and close ownership are deterministic on Python 3.11+.</acceptance_criteria>
  <done>The SQLite authority database can be created only inside an eligible root, opened, contended, closed, and rejected on unsupported topology without hidden downgrade; Windows root/ACL provisioning remains entirely offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make every complete authority transition atomic and rollback-safe</name>
  <reversibility rating="costly">D-24/D-29 make normalized transition semantics the contract later authority adapters must match.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_lifecycle_authority_contract.py, tests/test_sqlite_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>tests/test_lifecycle_authority_contract.py</file>
  </read_first>
  <behavior>
    - "Statement-by-statement injected faults during promotion leave entry, mutation, debt, projection dirty state, and authority revision all-before or all-after, never partial."
    - "An uncertain commit is classified by reopening and reading the unique operation plus entry state; promotion is not blindly retried."
    - "Malformed/oversized/unknown row values fail typed before locator use, and indexed corroborating columns must exactly agree with the authenticated canonical manifest."
    - "Common idempotent repeats, exact conflicts, clear snapshots, high-water pages, and checkpoints pass unchanged against SQLite and memory."
  </behavior>
  <action>Finish every LifecycleAuthority method using a shared transaction helper that acquires `BEGIN IMMEDIATE`, performs only bounded indexed reads/comparisons/row changes, commits, rolls back on BaseException, and closes in `finally`. Store canonical manifest bytes once; treat indexed state/generation/digest/locator fields as mandatory corroboration rather than a second record. Preserve a monotonic per-key lineage token after absence. Make unique operation IDs and complete transition inputs idempotent; a mismatched replay conflicts. For promotion, atomically revalidate exact lineage/generation/prior-manifest digest, replace the entry, mark the mutation promoted, create old/candidate cleanup debt as applicable, dirty projection state, and advance revision. Inject faults at every statement and commit boundary; after an ambiguous commit error, reopen/classify exact operation state before returning. Run `PRAGMA integrity_check` and `foreign_key_check` in explicit diagnostics, never automatically repair.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>All common transition cases pass on both adapters; each injected SQLite fault yields whole-state rollback or a correctly classified committed outcome; integrity/foreign-key checks remain clean.</acceptance_criteria>
  <done>The local persistent authority is a complete atomic implementation of the deep semantic contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the Windows one-user/session capability and deployment contract</name>
  <files>src/cacheness/config.py, src/cacheness/storage/sqlite_lifecycle_authority.py, docs/lifecycle-authority.md, tests/test_phase3_windows_contract.py</files>
  <read_first>
    <file>docs/lifecycle-authority.md</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>verify_platform.py</file>
  </read_first>
  <behavior>
    - "Configuration requesting cross-user, Windows service-plus-interactive, cross-session, remote, or userspace/network filesystem sharing fails typed before SQLite open, JSON creation, or handler staging; an absent Windows root fails the first mutation unchanged with an actionable offline provisioning command."
    - "The documented supported deployment uses a local NTFS root with DACL inheritance disabled, grants mutation to the current token's S-1-5-5-X-Y logon SID rather than its persistent account SID, and has no unrelated or inherited write ACE."
    - "Native Windows tests derive the logon SID from the current access token, run read-only `icacls.exe /verify` and PowerShell `Get-Acl`, prove Cacheness never creates the root or changes mode/ACL, require any SYSTEM/Administrators ACE to be explicit, non-mutating, and narrowly documented, reject absent/unsafe/drifted roots unchanged, and exercise same-session plus negative different-session/service-token access against SQLite DELETE+EXTRA."
    - "No Win32 named mutex, lock file, byte-range lock wrapper, or platform-specific authority is introduced; SQLite remains the only cross-process commit authority."
  </behavior>
  <action>Represent D-22 as an explicit requested topology/capability contract, not an inferred promise. Reject unsupported multi-user, service-plus-interactive, cross-session, or non-local claims in configuration validation before the lazy authority factory can mutate the store. Document exactly one supported Windows provisioning path: deployment tooling creates the local NTFS root while the store is unavailable, disables inheritance without copying inherited ACEs, and installs the protected DACL before Cacheness starts. Include an exact actionable PowerShell/`icacls.exe` command sequence parameterized by the root and current token logon SID; surface that command in the typed absent/unsafe/drift failure without executing it. Resolve the current access token's logon SID by parsing the `S-1-5-5-X-Y` token-group SID from `whoami /groups /fo csv /nh`, and corroborate the resulting SID and ACL with read-only PowerShell/.NET `WindowsIdentity` and `Get-Acl` plus `icacls.exe /verify`; never substitute the persistent account SID. The only ordinary create/write/append/delete/modify grant is the current logon SID; ordinary account-SID mutation grants and unrelated/inherited write ACEs are rejection conditions. Permit explicit SYSTEM (`S-1-5-18`) and built-in Administrators (`S-1-5-32-544`) entries only for documented inspection/recovery rights that exclude mutation, DACL change, ownership change, and delete rights. Validate the existing root, token/logon SID, and exact DACL before authority database creation/open and before every mutation; token change, absent root, missing proof, unsafe ownership/descriptor, or DACL drift fails typed and byte-for-byte unchanged. Cacheness must never create the Windows root, invoke chmod, disable inheritance, or add/remove/reorder ACEs. Add platform-neutral rejection-before-mutation tests and native-Windows tests that instrument filesystem and OS-command calls to prove no root creation/mode/ACL write, plus absent-root, unsafe-root, drift, persistent-account versus logon-SID distinction, same-session multiprocess contention, and a different-logon-session or service-token process that cannot open or mutate. OS ACL enforcement supplies the session boundary; do not add a Cacheness Win32 named mutex, lock file, handle registry, or other lock authority. A non-Windows host reports the native target unavailable; Plan 09 supplies the blocking evidence gate.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase3_windows_contract.py -k "rejects_unsupported_before_mutation or contract_shape" -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Unsupported Windows sharing and every absent/unsafe/drifted root or missing token/DACL proof fail typed and byte-for-byte unchanged with the offline command before database/payload effects; Cacheness never creates or alters the Windows root/DACL; the protected DACL binds mutation to the current logon SID rather than account SID; native evidence includes a denied different-session/service-token attempt; the implementation contains no custom Win32 lock authority.</acceptance_criteria>
  <done>D-22 is an executable bounded capability, with native proof explicitly required at final acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| filesystem object → SQLite connection | Reserved directory/database topology and identity must be proven before open. |
| logical key/row fields → SQL engine | All values cross through bounded validation and parameter binding. |
| SQLite error/commit uncertainty → public result | Backend details must map to typed fail-closed outcomes without guessing commit state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-07 | Spoofing / Tampering | authority database/store identity | high | mitigate | Containment/type checks plus application ID, schema version, and store identity verification before mutation. |
| T-03-08 | Tampering | SQL construction and row decoding | high | mitigate | Constant schema SQL, parameter binding, checks/bounds, manifest authentication, and exact column corroboration. |
| T-03-09 | Denial of Service | SQLite writer contention | medium | mitigate | One monotonic absolute deadline, bounded retry, rollback, and typed timeout with cause. |
| T-03-10 | Tampering | SQLite sidecar family | high | mitigate | Let SQLite exclusively own rollback journals/temp sidecars; lifecycle code never enumerates or manipulates them. |
| T-03-11 | Elevation of Privilege | post-fork inherited connection | high | mitigate | PID ownership guard and fresh child construction tests. |
| T-03-11W | Elevation of Privilege | Windows cross-user/session topology | high | mitigate | Require an offline pre-provisioned protected root whose sole ordinary mutation grant is the current token logon SID; Cacheness only validates and never changes it; reject absent/unsafe/drifted/account/inherited/unrelated states unchanged, and natively prove a different-session/service token is denied. |
</threat_model>

<verification>
Run both authority suites and inspect SQLite pragmas, query plans for indexed pages, fault rollback, busy timing bounds, integrity checks, and post-fork rejection.
</verification>

<success_criteria>
- SQLite durability and identity configuration are explicit and verified.
- Complete transitions are atomic across every participating authority row.
- Busy, corrupt, wrong-store, future-schema, topology, fork, and close states fail deterministically.
- No authority transaction contains external payload or callback work.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-03-SUMMARY.md` when done.
</output>
