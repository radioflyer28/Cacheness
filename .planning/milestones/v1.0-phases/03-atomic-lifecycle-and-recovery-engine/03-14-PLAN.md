---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "14"
type: execute
wave: 14
depends_on: ["03-13"]
files_modified:
  - src/cacheness/core.py
  - src/cacheness/metadata.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/backends/postgresql_backend.py
  - tests/test_unified_cache_lifecycle_authority.py
  - tests/test_projection_mutation_contract.py
  - tools/verify_phase3_ruff_delta.py
  - tests/fixtures/phase3_ruff_baseline.json
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Each put carries its own pre-operation projection token through the private lifecycle hook and carries its exact promoted generation/locator back to UnifiedCache for custom-link binding, while public BlobStore.put continues returning only the key."
    - "A stale facade may conditionally replace or retire only the exact compatibility projection generation/locator it observed; a concurrent M2 projection and its custom-metadata links survive unchanged per D-01, D-02, D-11, D-18, and D-19."
    - "A failed first put or failed projection repair is idempotent: repeating the repair reaches the same authority-derived projection or confirmed absence and cannot attach, remove, or relabel another generation's custom metadata per D-04, D-07, and D-11."
    - "Every public cache, built-in metadata-query, and custom-metadata surface treats authenticated non-committed/tombstoned authority state as absent while the canonical intent/debt remains available to dry-run and resumable reconciliation per D-03, D-09, and D-13 through D-16."
    - "A facade read returns an authority snapshot and compatibility projection for one exact generation-specific locator, retries at most once on a proven change, and otherwise raises CacheBlobLifecycleConflictError rather than applying policy to mixed observations per D-03 and D-19."
    - "Projection commits may use only a short backend-local mutation critical section; no process-global or whole-cache lock spans authority access, payload I/O, custom-metadata work, or unrelated keys per D-17, D-20, and D-28."
    - "Cached projection mismatches evict stale local cache state, and get_stats counts/sizes only committed live authority entries."
    - "Darwin remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false, and Phase 999.1 still requires native Windows Python 3.11 PASS at exit 0 per D-22, D-31, and D-32."
  artifacts:
    - path: src/cacheness/metadata.py
      provides: "Generation/locator-aware conditional projection mutation contract and atomic local-backend implementations"
    - path: src/cacheness/storage/lifecycle.py
      provides: "Private put-result and pre-promotion context carrying the operation's expected and promoted projection identities"
    - path: src/cacheness/storage/blob_store.py
      provides: "Internal result-preserving put seam while the public BlobStore.put return contract remains unchanged"
    - path: src/cacheness/storage/backends/postgresql_backend.py
      provides: "PostgreSQL parity for the same conditional projection outcome contract without a live-service requirement"
    - path: src/cacheness/core.py
      provides: "Committed-only facade visibility, exact projection ownership, and bounded coherent snapshot/projection reads"
    - path: tests/test_unified_cache_lifecycle_authority.py
      provides: "Deterministic two-facade stale-removal, failed-put, tombstone, query, and mixed-generation regressions"
    - path: tests/test_projection_mutation_contract.py
      provides: "Local adapter, cached-wrapper, JSON cross-instance, and mocked PostgreSQL conditional mutation contracts"
    - path: tools/verify_phase3_ruff_delta.py
      provides: "Exact Ruff-delta scope covering every Plan 03-14 modified Python path"
    - path: tests/fixtures/phase3_ruff_baseline.json
      provides: "Pre-Plan-03-14 fingerprints for newly scoped existing files without grandfathering Plan 03-14 findings"
  key_links:
    - from: src/cacheness/core.py
      to: src/cacheness/metadata.py
      via: "UnifiedCache passes the exact observed projection token into conditional replace/remove and handles applied, already-converged, and mismatch outcomes"
      pattern: "projection.*(expect|token|conditional|compare)"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/core.py
      via: "Private lifecycle put context supplies the operation's exact prior projection token before promotion and the promoted token after success"
      pattern: "expected.*(generation|locator)|promoted.*(generation|locator)"
    - from: src/cacheness/core.py
      to: src/cacheness/storage/backends/postgresql_backend.py
      via: "UnifiedCache._store_custom_metadata delegates its promoted projection token to the SQL backend's same-transaction conditional custom-metadata/link insertion seam"
      pattern: "store_custom_metadata_if_current|CacheBlobLifecycleConflictError"
    - from: src/cacheness/metadata.py
      to: src/cacheness/custom_metadata.py
      via: "SQLite changes the cache row and removes links owned by the replaced token in one transaction; mismatches and same-token refreshes preserve links"
      pattern: "CacheMetadataLink|actual_path|authority_generation"
    - from: src/cacheness/core.py
      to: src/cacheness/storage/lifecycle.py
      via: "Only authenticated committed manifests become live facade rows; tombstones stay canonical reconciliation evidence"
      pattern: "committed|tombstoned|CacheBlobLifecycleConflictError"
    - from: src/cacheness/core.py
      to: tests/test_unified_cache_lifecycle_authority.py
      via: "Controlled two-facade hooks force the four previously untested interleavings without timing sleeps"
      pattern: "Barrier|Event|test_hook|monkeypatch"
  prohibitions:
    - "[UNVERIFIED] The compatibility projection must never become lifecycle authority or infer recovery state independently of BlobStore/LifecycleAuthority."
    - "[UNVERIFIED] No process-global or whole-cache lock may span a facade lifecycle operation; distinct-key payload work must remain concurrent."
    - "[UNVERIFIED] Current Darwin evidence must not be reported as native Windows qualification."
---

<objective>
Close the final authority-projection races by making compatibility metadata mutation generation/locator-conditional, hiding non-live authority states from every public facade surface, and returning only coherent authority/projection pairs.

Purpose: Phase 3's canonical LifecycleAuthority already preserves payload generations, but stale key-only compatibility cleanup can delete a newer facade row and its custom links, recoverable tombstones can remain publicly visible, and repair can pair two generations. These projection defects violate STOR-03 through STOR-07 even though the direct BlobStore lifecycle is sound.
Output: One exact conditional projection repository contract, committed-only facade/query behavior, deterministic two-facade regressions, and a fresh complete-suite acceptance run.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-FINAL-REVIEW.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-04-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-08-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-13-SUMMARY.md
@src/cacheness/core.py
@src/cacheness/metadata.py
@src/cacheness/storage/backends/postgresql_backend.py
@src/cacheness/storage/lifecycle.py
@src/cacheness/storage/lifecycle_authority.py
@src/cacheness/custom_metadata.py
@tests/test_unified_cache_lifecycle_authority.py
@tests/test_custom_metadata.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve a concurrent M2 projection and its custom links through stale teardown</name>
  <files>src/cacheness/metadata.py, src/cacheness/core.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/blob_store.py, tests/test_unified_cache_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/custom_metadata.py</file>
    <file>tests/test_unified_cache_lifecycle_authority.py</file>
    <file>tests/test_custom_metadata.py</file>
  </read_first>
  <behavior>
    - "Two SQLite-backed UnifiedCache facades share one authority and projection store. After facade A observes the exact M1 projection and authority absence, facade B completes M2 put plus custom-metadata linking before A's teardown; A reports a compare mismatch/no-op and M2 remains gettable, listed, queryable, and linked."
    - "The same interleaving after a failed first put observes an absent authority and a candidate projection, then lets B complete M2 plus custom metadata; A's repair cannot remove or overwrite M2 or its links."
    - "If A began against M1 and B publishes M2 before A's pre-promotion projection hook, A compares only against its captured M1 token, cannot replace M2, then loses authority promotion cleanly without touching M2."
    - "If B replaces A's promoted generation before A stores custom metadata, A compares against its own promoted generation/locator and raises a typed conflict without attaching A's metadata to B."
    - "Repeating a conditional projection write or removal with the same expectation is convergent: the already-requested state succeeds without extra link deletion, while a different generation-specific locator returns mismatch and changes zero rows and zero links."
    - "A token-changing SQLite projection mutation owns cache-row replacement and old CacheMetadataLink removal in one transaction; a same-token refresh preserves links; a mismatch rolls back all row/link changes."
    - "On one UnifiedCache instance, distinct-key B finishes while A is paused at payload publication; the compatibility `_lock` attribute remains present but is not held across handler/payload/authority work."
  </behavior>
  <action>Write four deterministic SQLite/custom-metadata regressions first: stale absence followed by M2, failed-first-put repair followed by M2, B replacing M1 before A's projection hook, and B replacing A's promoted generation before A's custom-link insert. Use two independently constructed facades and controlled Events/Barriers or narrow hooks; do not use sleeps or probabilistic repetition. Add a same-facade distinct-key barrier regression that pauses A inside handler/payload publication and proves B completes before A is released. Change the private lifecycle contract, without changing public `BlobStore.put`, so `AuthorityLifecycleEngine.put()` produces an internal result containing the exact pre-operation `EntrySnapshot`/absence expectation and exact promoted `EntrySnapshot`; let BlobStore expose that result only through a private/internal put seam while its public method still returns the key. Pass a pre-promotion context containing the operation-captured expected generation/locator to `_prepare_authority_projection()` rather than letting that hook re-observe whatever row is current, and pass the returned promoted generation/locator directly to `_store_custom_metadata()` rather than looking it up after another actor may replace it. At the metadata repository boundary, introduce one explicit conditional projection mutation contract whose expected value is the exact observed generation-specific locator plus the generation when that adapter preserves it; the immutable locator is the equivalent identity for existing SQLite columns, so do not invent a generalized identity model or alter the ORM schema. The contract must distinguish applied/already-converged from comparison-mismatch and define affected-row semantics: expected absence matches only no row; conditional delete treats an already absent row as converged; conditional replacement may update only the exact operation token; and a current replacement token is never modified by a stale actor. Implement SQLite inside one transaction and preserve CacheMetadataLink ownership as described in the behavior block. Route `_prepare_authority_projection()`, `_sync_authority_projection()`, `_repair_projection_after_failed_put()`, `_retire_exact_authority_snapshot()`, and authority-backed invalidation/cleanup branches through that primitive. Bind custom-link insertion to the successful promoted token and verify that token in the same SQLite transaction that inserts its links; on mismatch raise `CacheBlobLifecycleConflictError` and attach nothing. Remove or narrow `_clear_coordinated`/`_clear_read_coordinated` on ordinary key-scoped methods: retain `UnifiedCache._lock` and the compatibility fallback for test doubles, but acquire it only for brief facade-local field/close/admission transitions, never across BlobStore authority calls, handler serialization, payload I/O, projection/custom-metadata transactions, or the body of `get`, `put`, `invalidate`, list/query, cleanup, or clear. Store-wide lifecycle admission remains owned by BlobStore clear, and per-key correctness remains authority CAS. Keep BlobStore/LifecycleAuthority canonical per D-23 through D-26 and D-28.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_unified_cache_lifecycle_authority.py -k "stale_absence or failed_first_put or replacement_before_projection or replacement_before_custom or projection_link or conditional_projection or distinct_key" -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The forced M1/absence/M2 and operation-token schedules pass on SQLite, both M2 cache rows and every M2 custom-metadata link survive, repeated repair converges, public BlobStore.put still returns only its key, and same-facade distinct keys overlap while `_lock` remains available for compatibility.</acceptance_criteria>
  <done>STOR-03 and the custom-link portion of STOR-04/STOR-06 have an end-to-end generation-conditional projection path instead of a key-only teardown race.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hide tombstones and return only coherent authority/projection pairs</name>
  <files>src/cacheness/core.py, tests/test_unified_cache_lifecycle_authority.py, tools/verify_phase3_ruff_delta.py, tests/fixtures/phase3_ruff_baseline.json</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/test_unified_cache_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "After delete publishes an authenticated tombstone and forced cleanup/debt settlement fails, get, list_entries, query_meta, get_custom_metadata_for_entry, query_custom, query_custom_session, TTL cleanup, size cleanup, and corrupt-read cleanup all treat the key as absent; the tombstone and cleanup debt remain readable through canonical authority/reconciliation APIs."
    - "For the same forced tombstone state, get_stats reports zero contribution from that key to total_entries and total_size_mb while retaining compatible hit/miss counters."
    - "Repeated facade synchronization of the same tombstone stays absent and does not consume, rewrite, or hide the canonical reconciliation finding."
    - "If a replacement commits after a tombstone observation but before conditional projection retirement, the replacement projection and links survive and the next bounded observation returns M2."
    - "If M2 is promoted while `_authority_snapshot_entry()` repairs M1, the method returns M2 snapshot plus M2 projection after one retry; if another controlled change prevents stability on the retry, it raises CacheBlobLifecycleConflictError with operation/key/attempt context and returns no mixed pair."
    - "A distinct-key overlap regression still demonstrates that the coherence retry introduces no store-wide serialization."
  </behavior>
  <action>Add deterministic regressions for invalidate, expiration, and corrupt-read cleanup by failing the post-tombstone cleanup/debt boundary after promotion, then assert every listed public cache/query/custom-metadata surface omits the key while a dry-run reconciliation still reports the authenticated tombstone/debt. Include `get_stats()` in that matrix and require the tombstoned row to contribute neither `total_entries` nor `total_size_mb`; preserve compatible hit/miss counters. Centralize facade liveness classification so only a manifest whose authenticated state is exactly `committed` may produce a live projection or policy input; a tombstone/non-committed manifest produces internal absence and uses Task 1's exact conditional retirement without changing authority evidence. Make built-in metadata queries, custom metadata queries, and aggregate statistics synchronize/filter by this committed-only authority view, and ensure custom queries require a live cache-row link rather than returning orphan metadata objects. Rework `_authority_snapshot_entry()` as an initial observation plus at most one retry: authenticate one snapshot, compare its exact generation/locator with the projection token, conditionally repair, then re-read both and return only when the pair matches. A proven change consumes the sole retry; a second mismatch/change raises `CacheBlobLifecycleConflictError` rather than applying TTL, invalidation, integrity, statistics, or size policy to a mixed pair. Extend `tools/verify_phase3_ruff_delta.py` and its fixture so every Plan 03-14 Python path is covered: add pre-existing `core.py` and `postgresql_backend.py` as baseline-governed paths using only diagnostics from source-start commit `8c3556b`, add `test_unified_cache_lifecycle_authority.py` and `test_projection_mutation_contract.py` as clean Phase 3 paths, and retain already-covered metadata/blob_store/lifecycle files. Never regenerate the baseline from Plan 03-14 working files or grandfather a new finding. Preserve the existing public miss behavior for stable tombstone/absence, the existing `_lock` compatibility attribute without holding it across these operations, D-03 committed-only visibility, D-09 tombstone recovery, D-13 through D-16 reconciliation evidence, and D-17/D-20 distinct-key concurrency.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_unified_cache_lifecycle_authority.py tests/test_custom_metadata.py -k "tombstone or coherent or mixed_generation or custom_metadata or distinct_key or get_stats" -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>All facade surfaces and aggregate entry/size statistics expose only committed authority state, canonical tombstone/debt evidence remains reconcilable, and every read returns one matching generation/locator pair or a typed bounded-retry conflict.</acceptance_criteria>
  <done>STOR-04, STOR-05, STOR-06, and STOR-07 no longer fail through tombstone resurrection or mixed-generation policy input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seal backend parity and rerun complete Phase 3 acceptance</name>
  <files>src/cacheness/core.py, src/cacheness/metadata.py, src/cacheness/storage/backends/postgresql_backend.py, tests/test_projection_mutation_contract.py</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/storage/backends/postgresql_backend.py</file>
    <file>tests/test_metadata.py</file>
    <file>tests/test_postgresql_backend.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md</file>
  </read_first>
  <behavior>
    - "InMemoryBackend, JsonBackend, SqliteBackend, and CachedMetadataBackend implement the same conditional projection outcomes. Applied/already-converged may populate the proven resulting row; mismatch always evicts or refreshes the stale cached entry even though the underlying row mutation did not occur."
    - "With M1 cached in CachedMetadataBackend, peer M2 publication followed by an M1-expected mismatch evicts or refreshes M1; the next read returns M2."
    - "Two JsonBackend instances deterministically race one document comparison: a short cross-platform sibling projection-commit lock serializes refresh/compare/atomic-replace only, so the stale writer returns mismatch and the M2 document survives; the lock never represents lifecycle authority or spans caller work."
    - "PostgresBackend expresses the same compare predicate and affected-row semantics in one database transaction: token-changing replacement removes old CacheMetadataLink rows, same-token refresh preserves them, mismatch changes neither row nor links, and delayed custom-link insertion rechecks the promoted token inside its insert transaction."
    - "The deterministic PostgreSQL schedule exercises real UnifiedCache._store_custom_metadata → PostgresBackend wiring for promotion A → replacement B → delayed A insertion; B's row/links survive and A commits zero metadata objects/links with CacheBlobLifecycleConflictError."
    - "The full frozen isolated suite, focused Python 3.11 authority tests, compatibility corpus, and an expanded Phase 3 Ruff delta covering every Plan 03-14 Python path all pass."
  </behavior>
  <action>Create `tests/test_projection_mutation_contract.py` for the local adapters, memory-cache wrapper, JSON two-instance race, and mocked PostgreSQL transaction schedules. Cover absent/create, exact replace, exact delete, same-token idempotency, mismatch no-op, and one unambiguous cache rule: applied/already-converged may populate only the proven result, while mismatch always evicts or refreshes the cached entry even though no underlying row changed. Include M1 cached → peer M2 → stale M1-expectation mismatch → next read M2. For JsonBackend, add a short document-scoped cross-instance commit guard using the existing cross-platform descriptor-lock implementation (`_locked_key_handle`, POSIX flock/Windows LockFileEx) over a stable sibling projection-commit lock object. Hold it only for live-document refresh, token comparison, durable atomic replacement, and in-memory refresh; it is short-lived rebuildable projection-only coordination under D-31, never platform-lock lifecycle authority, a manifest, a receipt, or recovery evidence, and it must not span authority access, payload I/O, callbacks, or custom metadata. Force two independent JsonBackend instances so M2 wins and the stale M1 conditional mutation returns mismatch without reverting the document. Implement the equivalent single-transaction predicate and row-count result in PostgresBackend. A token-changing replacement deletes old CacheMetadataLink rows before updating, same-token refresh preserves them, and mismatch changes neither. Define the exact internal SQL custom-metadata seam as `store_custom_metadata_if_current(cache_key, expected_projection_token, metadata_objects)`: `UnifiedCache._store_custom_metadata()` passes the promoted generation-specific locator/token from Task 1 to this backend method. PostgreSQL must lock or conditionally verify the current projection row's exact token inside the same `SessionLocal` transaction that adds every custom metadata object and `CacheMetadataLink`, then commit them together. On mismatch it rolls back, commits no A metadata object or link, and raises `CacheBlobLifecycleConflictError`. The deterministic promotion A → replacement B → delayed A insertion test must call the real `UnifiedCache.put(..., custom_metadata=...)`/`_store_custom_metadata()` core-to-Postgres wiring with controlled transaction hooks, not invoke only a backend helper; require B's row and B links unchanged and zero A objects/links. Leave live PostgreSQL qualification to its scheduled backend phase. A custom backend lacking conditional projection mutation raises `CacheBlobBackendError` with reason `CacheReason.BLOB_BACKEND_CAPABILITY_UNSUPPORTED` and `capability: conditional_projection_mutation`. Run focused tests, the exact full suite, compatibility corpus, and the expanded Ruff delta established in Task 2. Do not edit qualification artifacts: D-32 remains Darwin exit 2, UNAVAILABLE/NOT_QUALIFIED, `native_evidence: false`, with native Windows PASS/exit 0 owned by Phase 999.1.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_unified_cache_lifecycle_authority.py tests/test_projection_mutation_contract.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Every facade metadata adapter has explicit conditional semantics, CachedMetadataBackend cannot retain M1 after an M2 mismatch, the JSON two-instance stale writer loses safely without platform-lock authority, PostgreSQL replacement and delayed custom-link insertion use exact predicates in their transactions, every changed Python path is Ruff-delta covered from pre-plan evidence, and all Python 3.11/full-suite/compatibility/Ruff gates pass without changing D-32.</acceptance_criteria>
  <done>All five Phase 3 requirements have fresh end-to-end evidence for canonical lifecycle state plus race-safe compatibility projection behavior.</done>
</task>

</tasks>

## Spec-less Edge Assumptions Resolved by This Plan

| Probe | Resolution |
|---|---|
| STOR-03 unclassified/manual-review fallback | Converted to Task 1's exact old-or-new projection truth and deterministic M1/absence/M2 tests. |
| STOR-04 unclassified/manual-review fallback | Converted to Tasks 1–2 failure-boundary truths: failed projection work preserves the authoritative generation and canonical residue/debt. |
| STOR-05 idempotency | Task 1 tests the same conditional mutation/repair twice; Task 2 repeats tombstone synchronization and requires the same absent facade result. |
| STOR-05 concurrency | Tasks 1–2 force parallel replacement before projection, custom-link binding, stale teardown, and tombstone retirement; the newer generation and links survive. |
| STOR-06 unclassified/manual-review fallback | Task 2 proves facade absence does not remove the canonical tombstone/debt and dry-run reconciliation still reports it. |
| STOR-07 unclassified/manual-review fallback | Tasks 1–2 prove one matching pair or typed conflict after a fixed two-observation bound and same-facade distinct-key completion while another key pauses at payload I/O. |

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generations and recoverable incomplete outcomes | 03-14 | COVERED | Conditional projection ownership and committed-only visibility extend the already-complete authority lifecycle to the facade. |
| REQ | STOR-03 | Never expose partial or mixed payload/metadata state | 03-14 Tasks 1–2 | COVERED | Operation-captured projection tokens, exact projection CAS, and coherent-pair return. |
| REQ | STOR-04 | Failed writes preserve valid state and recoverable residue | 03-14 Tasks 1–2 | COVERED | Failed-put repair is conditional and tombstone/debt stays canonical. |
| REQ | STOR-05 | Idempotent overwrite/delete/clear/close cleanup | 03-14 Tasks 1–2 | COVERED | Conditional mutation, token-bound custom links, non-live retirement, and aggregate stats have explicit repeated-run tests; existing clear/close coverage remains. |
| REQ | STOR-06 | Bounded dry-run/resumable reconciliation | 03-14 Tasks 1–2 | COVERED | Custom links cannot be lost to stale actors, and hidden tombstone/debt remains reportable. |
| REQ | STOR-07 | Deterministic same-key races without global distinct-key serialization | 03-14 Tasks 1–3 | COVERED | Replacement-before-projection/link interleavings, fixed retry bound, short backend-local commits, and same-facade distinct-key regression. |
| RESEARCH | — | LifecycleAuthority is sole canonical state; JSON is projection only | 03-14 Tasks 1–3 | COVERED | Conditional facade storage never arbitrates lifecycle state per D-23 through D-27. |
| RESEARCH | — | Payload work stays outside short authority/backend commits | 03-14 Tasks 1 and 3 | COVERED | No new lock or transaction spans serialization, payload I/O, verification, reclamation, or unrelated keys per D-28. |
| RESEARCH | — | Deterministic fault/race tests and complete frozen acceptance | 03-14 Tasks 1–3 | COVERED | Controlled hooks run focused Python 3.11 tests and the exact full suite. |
| CONTEXT | D-01–D-04 | Immutable exact generations, CAS publication, committed-only reads, publication authority | 03-14 Tasks 1–2 | COVERED | Projection token follows the generation-specific locator; only committed pairs are live. |
| CONTEXT | D-05–D-08 | Durable evidence and pre/post-authority failure classification | Existing 03-03–03-06 plus 03-14 Tasks 1–2 | COVERED | Plan 14 preserves authority intent/debt and prevents facade repair from destroying derived state. |
| CONTEXT | D-09–D-12 | Tombstone/idempotent cleanup/clear/close | Existing 03-04–03-05 plus 03-14 Task 2 | COVERED | Tombstone stays recoverable but absent from facade; existing clear/close semantics are regression-gated. |
| CONTEXT | D-13–D-16 | Safe bounded resumable reconciliation | Existing 03-05 plus 03-14 Task 2 | COVERED | Reconciliation evidence remains authoritative and payload-free while facade projections retire. |
| CONTEXT | D-17–D-20 | Per-key ordering, backend CAS, exact races, distinct-key concurrency | 03-14 Tasks 1–3 | COVERED | Short conditional metadata mutations and bounded coherent reads do not add global operation locking. |
| CONTEXT | D-21–D-22 | Trusted local owner and limited Windows topology | Existing 03-03/03-09 plus 03-14 Task 3 | COVERED | No security-boundary change; current host remains unqualified. |
| CONTEXT | D-23–D-31 | Transactional-authority replacement architecture | Existing 03-02–03-08 plus 03-14 Tasks 1–3 | COVERED | No scheduler, alternate authority, payload wrapper, or platform-lock authority is introduced; the sibling descriptor lock coordinates only one rebuildable JSON projection document commit. |
| CONTEXT | D-32 | Honest unavailable Windows evidence | 03-14 Task 3 | COVERED | Required status remains UNAVAILABLE/NOT_QUALIFIED with Phase 999.1 unchanged. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| LifecycleAuthority snapshot → compatibility metadata repository | Authenticated canonical state becomes rebuildable public metadata without transferring lifecycle authority. |
| Facade instance A → shared projection mutated by facade instance B | A stale actor must prove exact row ownership before changing B's replacement or linked custom metadata. |
| Non-live authority evidence → public cache/query surface | Tombstone and incomplete states remain reconciliation-only and cannot be rendered as stored user objects. |
| Bounded observation loop → cache policy | TTL, integrity, invalidation, and statistics may consume only a generation/locator-coherent pair. |
| Platform evidence → release qualification | Darwin unavailable evidence cannot cross into a native Windows support claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-14-01 | Tampering | Stale projection teardown/repair | high | mitigate | Compare the current projection's exact generation-specific locator inside the metadata repository transaction; mismatches affect zero rows and zero links. |
| T-03-14-02 | Spoofing | Tombstone compatibility rendering | high | mitigate | Authenticate authority state and allow only exact `committed` manifests to produce live facade/query results; conditionally retire non-live rows. |
| T-03-14-03 | Tampering | Snapshot/projection read pair | high | mitigate | Require exact generation/locator equality after repair and one retry, then raise CacheBlobLifecycleConflictError on continued instability. |
| T-03-14-04 | Tampering | SQLite custom-metadata link ownership/rollback | high | mitigate | Change projection row and token-owned links in one transaction; preserve same-token links and roll back row/link mutations together on failure or mismatch. |
| T-03-14-05 | Denial of Service | Coherence repair retry | medium | mitigate | Use exactly two total observations (initial plus one retry), no sleep/backoff loop, and a typed terminal conflict with context. |
| T-03-14-06 | Repudiation | Windows qualification evidence | high | mitigate | Reuse the fixed D-32 contract unchanged and require the complete suite without editing or promoting unavailable evidence. |
| T-03-14-07 | Tampering | Per-operation projection/custom-link token plumbing | high | mitigate | Carry the authority expectation and promoted snapshot in a private lifecycle result; never re-observe a later generation when preparing projection or binding custom metadata. |
| T-03-14-08 | Denial of Service | Facade-wide `_lock` admission | high | mitigate | Remove whole-operation decorator ownership and prove a second key completes while the first is blocked at payload publication; retain `_lock` only for brief compatibility state transitions. |
| T-03-14-09 | Tampering | Cached/JSON/PostgreSQL projection commits | high | mitigate | Evict cached state on mismatch, serialize only JSON refresh/compare/replace with a projection-only descriptor guard, and recheck PostgreSQL promoted token inside both row/link replacement and delayed custom-link insert transactions. |
</threat_model>

<verification>
Run the deterministic two-facade SQLite/custom-metadata race matrix first, then all local metadata-adapter contracts and PostgreSQL unit semantics on Python 3.11. Finish with the exact frozen isolated full repository suite, compatibility corpus, and expanded Phase 3 Ruff delta covering every modified Python path. No timing-only race assertion, skip, xfail, deselection, new-debt baseline regeneration, or native-Windows claim can satisfy the gates.
</verification>

<success_criteria>
- A stale absence or failed-first-put repair cannot change M2's projection or custom-metadata links.
- Pre-promotion projection and post-promotion custom-link work use the originating operation's captured tokens without changing public BlobStore.put results.
- Conditional projection replacement/removal is idempotent and has explicit applied/already-converged/mismatch semantics across facade metadata adapters.
- Authenticated tombstones/non-committed manifests remain visible only to canonical reconciliation, never to cache/list/query/custom-metadata surfaces.
- Aggregate total_entries/total_size exclude non-live authority state; cached mismatches and cross-instance JSON/PostgreSQL commits cannot resurrect stale rows or links.
- `_authority_snapshot_entry()` returns one exact generation/locator pair after at most one retry or raises CacheBlobLifecycleConflictError.
- Same-facade distinct-key payload work remains concurrent; `_lock` stays compatible but no process-global/whole-cache operation lock spans work.
- The focused Python 3.11 matrix and exact complete-suite/compatibility/Ruff gates pass while D-32 remains unqualified.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-14-SUMMARY.md` after all three tasks complete. Record the conditional projection token/outcome contract, SQLite custom-link transaction semantics, exact retry bound, deterministic race tests, full-suite result, and unchanged D-32 UNAVAILABLE/NOT_QUALIFIED disposition.
</output>
