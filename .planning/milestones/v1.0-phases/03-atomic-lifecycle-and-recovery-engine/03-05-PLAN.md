---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/cacheness/storage/lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/reconciliation.py
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store_atomic_lifecycle.py
  - tests/test_blob_store_reconciliation.py
autonomous: true
requirements: [STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Clear transactionally snapshots exact key+lineage+generation+manifest-digest targets; later creates/overwrites survive and per-target progress resumes (D-10, D-11, D-20, D-29)."
    - "Reconciliation is stable/non-mutating by default, pages indexed authority work by captured high-water/keyset cursors, and independently bounds rows/actions/bytes/time (D-13, D-14, D-29)."
    - "The canonical versioned reconciliation machine view includes authoritative and expected generation, operation provenance, residue type/role, proposed action, reason, disposition, and checkpoint state; the characterized zero-argument dictionary remains an explicit legacy v1 adapter (D-13)."
    - "Apply revalidates authenticated provenance and current non-ownership immediately before each destructive action, never deserializes payload or guesses ownership, and checkpoints idempotently (D-06, D-14 through D-16)."
  artifacts:
    - path: src/cacheness/storage/lifecycle.py
      provides: "Transactional clear snapshot and resumable target execution"
    - path: src/cacheness/storage/reconciliation.py
      provides: "Stable redacted versioned D-13 machine reports plus the exact legacy v1 compatibility adapter"
    - path: tests/test_blob_store_reconciliation.py
      provides: "Dry-run, bounded paging, resume, quarantine/report, revalidation, and interruption proof"
  key_links:
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Exact clear snapshot, keyset targets, and transactional progress"
      pattern: "begin_clear|page_clear|checkpoint_clear"
    - from: src/cacheness/storage/reconciliation.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Captured high-water indexed work and apply checkpoints"
      pattern: "begin_reconciliation|page_reconciliation|checkpoint_reconciliation"
    - from: src/cacheness/storage/reconciliation.py
      to: src/cacheness/storage/blob_store.py
      via: "Exact contained payload action after current-entry revalidation"
      pattern: "reconcile"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Clear and reconciliation must not enumerate payload filenames or maintain a second inventory."
    - "[FLAGGED/UNVERIFIED] Reconciliation must not deserialize application payloads or infer provenance from handler bytes."
    - "[FLAGGED/UNVERIFIED] Dry-run must not create checkpoints, mutate attempts, quarantine, delete, or refresh projections."
---

<objective>
Deliver exact resumable clear and bounded operator reconciliation directly from transactional authority state.

Purpose: Replace filename discovery and scheduler cursors with database-owned target/work rows whose safety can be revalidated.
Output: Clear target state machine, stable reports, resumable apply checkpoints, and adversarial tests.
</objective>

## Artifacts this phase produces

- Transactional `INSERT ... SELECT` clear membership and keyset target processing.
- A canonical versioned `ReconciliationFinding`/`ReconciliationReport` machine view over authority-indexed work, plus the exact characterized legacy v1 adapter.
- Stable high-water resume tokens and exact destructive-action checkpointing.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-04-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot and resume exact clear targets transactionally</name>
  <reversibility rating="costly">D-10/D-29 make clear membership and progress rows part of the authority contract consumed by later adapters.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/storage/memory_lifecycle_authority.py, src/cacheness/storage/lifecycle.py, tests/test_blob_store_atomic_lifecycle.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>tests/_lifecycle_test_support.py</file>
  </read_first>
  <behavior>
    - "One authority transaction creates a clear run and target rows for every then-committed exact lineage/generation/digest."
    - "A post-snapshot create is absent from targets; a post-snapshot overwrite conflicts/skips the stale target and remains readable."
    - "Interrupting at target delete or progress checkpoint resumes without repeating harmful work or losing the only valid generation."
    - "Target paging is indexed/keyset and obeys row/action/byte/time bounds without offset or filename order."
  </behavior>
  <action>Implement `begin_clear` as one short SQLite `INSERT ... SELECT` transaction and equivalent atomic memory snapshot, persisting clear run revision plus exact key, lineage, generation, and manifest digest for each target. Commit before any payload deletion. Keyset-page immutable target IDs, invoke the exact delete transition per target, mark changed targets conflict/skipped, and checkpoint counters/state atomically. Treat already absent exact targets as completed only after lineage proof. Resume the same clear ID after interruption and finish only when no bounded target remains. Do not keep a store-wide barrier after snapshot commit; do not enumerate filenames or materialize all targets in Python.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_lifecycle_authority_contract.py tests/test_blob_store_atomic_lifecycle.py -k "clear or target or snapshot or resume" -x</automated>
  </verify>
  <acceptance_criteria>Clear membership is exact and transactional; later keys/generations survive; repeated/interrupted execution converges; query-plan assertions show indexed keyset pages and bounds.</acceptance_criteria>
  <done>Clear satisfies STOR-05/STOR-07 without a file inventory or long-lived admission lease.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile indexed intent and cleanup debt with stable resumable reports</name>
  <reversibility rating="costly">D-13/D-14 freeze machine-readable report and resume semantics used by operators and Phase 7 tooling.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/storage/reconciliation.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/reconciliation.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
  </read_first>
  <behavior>
    - "Two equal dry-runs return equal redacted dictionaries/summaries and make no state/filesystem changes."
    - "Each canonical v2 finding carries a stable finding ID, authority/run revision, redacted operation provenance, key fingerprint, expected and authoritative generation, residue type and role, proposed action, reason code, disposition, and applied/checkpoint state."
    - "Calling the existing zero-argument to_dict methods returns the exact characterized legacy v1 dictionary, while an explicit versioned machine-view API returns a schema_version=2 envelope without dropping any D-13 field."
    - "A run captures mutation/debt high-water IDs and pages each monotonically once within independent row/action/byte/time bounds; new work waits for a later run."
    - "Interrupted apply resumes by run ID/cursors, treats already-retired work as complete, and neither skips nor repeats destructive effects."
    - "Malformed/unsupported/ambiguous/provenance-free evidence is blocked or confirmation-required and remains untouched; payload handler/deserializer is never called."
  </behavior>
  <action>Retain the characterized `ReconciliationStatus`, `ReconciliationAction`, zero-argument `ReconciliationFinding.to_dict`/`ReconciliationReport.to_dict`, and `human_summary` as an explicit legacy v1 compatibility adapter while changing their source to authority pages. Add a canonical versioned machine-view API and `schema_version: 2` envelope whose findings always contain: stable finding ID; authority and run revision; redacted operation ID/provenance; key fingerprint; expected generation; authoritative generation; exact residue type and role; proposed action; stable reason code; disposition (`safe`, `blocked`, or `confirmation_required`); and applied/checkpoint state. Make the v1 adapter a pure projection from the same v2 finding object so it cannot become a second semantic model. Reject unknown requested report versions explicitly. Start a run by capturing immutable mutation/debt high-water IDs; use indexed `id > cursor AND id <= high_water` pages and independent inspected-row, proposed/applied-action, byte, and monotonic-time budgets. Encode/authenticate bounded resume state without exposing raw keys/locators. Dry-run performs no writes. Apply reloads exact work and canonical entry, authenticates/validates the manifest before locator trust, proves containment and current non-ownership, then executes safe delete/quarantine outside a transaction and checkpoints outcome atomically. Per D-15, malformed, oversized, unknown-version, or ambiguous evidence is never repaired destructively. Choose report-only disposition when backend-native quarantine cannot be proven atomic. Preserve the last valid generation and never invoke a handler per D-13, D-14, and D-16.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_blob_store_reconciliation.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Stable dry-run is byte/equality repeatable and non-mutating; exact-key assertions prove every canonical v2 finding contains every D-13 evidence field and rejects unknown versions; legacy v1 dictionaries/summaries remain byte/equality compatible; apply resumes within all limits; destructive work is exact and revalidated; ambiguous evidence is preserved; no payload deserializer or filename inventory is reached.</acceptance_criteria>
  <done>Operators have a safe bounded reconciliation workflow satisfying STOR-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authority work row → operator report | Persisted evidence is untrusted until bounded decode/authentication and must be redacted. |
| report finding → destructive apply | Scan-time findings cannot authorize action without fresh exact revalidation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16 | Tampering | stale reconciliation finding | critical | mitigate | Reload exact row/current entry and reauthenticate immediately before destructive action. |
| T-03-17 | Denial of Service | reconciliation/clear enumeration | high | mitigate | Indexed high-water/keyset pages plus independent row/action/byte/time bounds. |
| T-03-18 | Elevation of Privilege | quarantine/delete locator | high | mitigate | Containment, exact operation provenance, generation/digest binding, and live-locator non-ownership. |
| T-03-19 | Information Disclosure | report/resume token | medium | mitigate | Redacted fingerprints, authenticated bounded tokens, safe reason codes, no raw keys/locators/payload metadata. |
</threat_model>

<verification>
Run clear and reconciliation suites, assert dry-run filesystem/database equality, inspect query plans for indexes, and inject interruption before/after every action checkpoint.
</verification>

<success_criteria>
- Clear targets exact snapshot generations and resumes safely.
- Reconciliation is stable/non-mutating by default and bounded in apply mode.
- Every destructive action is revalidated from authenticated authority evidence.
- No file inventory, offset pagination, or payload deserialization participates.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-05-SUMMARY.md` when done.
</output>
