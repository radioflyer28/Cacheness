---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "06"
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/storage/lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/metadata.py
  - src/cacheness/config.py
  - tests/test_manifest_repository_cas.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All direct BlobStore operations read/write committed state only through LifecycleAuthority; JSON is a revision-checked rebuildable projection and never CAS/recovery authority (D-24, D-26)."
    - "Projection export uses a consistent private SQLite backup, streams committed rows, atomically publishes JSON, and marks clean only if authority revision still matches (D-26, D-29)."
    - "Unsupported durable/multiprocess topology fails typed; memory makes only same-process claims and future PostgreSQL/S3 adapters consume semantic capabilities without Phase 3 implementations (D-18, D-27, D-31)."
  artifacts:
    - path: src/cacheness/storage/manifest_repository.py
      provides: "Revision-checked JSON projection/import compatibility boundary, not lifecycle authority"
    - path: src/cacheness/storage/blob_store.py
      provides: "One authority composition root with preserved public metadata/read shapes"
    - path: tests/test_manifest_repository_cas.py
      provides: "Projection lag/rebuild/revision race and no-dual-authority proof"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Direct get/list/metadata/mutation/clear/reconcile authority access"
      pattern: "lifecycle_authority"
    - from: src/cacheness/storage/manifest_repository.py
      to: src/cacheness/storage/sqlite_lifecycle_authority.py
      via: "Snapshot backup and expected-revision clean acknowledgement"
      pattern: "backup|projection"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Missing/corrupt SQLite authority must not be reconstructed automatically from JSON."
    - "[FLAGGED/UNVERIFIED] Projection rendering must not hold a live authority transaction or load every manifest into memory."
    - "[FLAGGED/UNVERIFIED] Phase 3 must not implement PostgreSQL or S3 authority adapters or simulate them with files."
---

<objective>
Make the lifecycle authority the sole BlobStore composition root and demote JSON to an exact revisioned compatibility projection.

Purpose: Prevent dual authority while preserving current public metadata representations and defining honest downstream capability semantics.
Output: Revision-checked projection exporter, authority-only BlobStore wiring, topology validation, and projection race tests.
</objective>

## Artifacts this phase produces

- Consistent SQLite-backup → streamed JSON projection with authority revision CAS.
- Authority-backed adapters preserving current `get_metadata`, `list`, and backend-facing JSON shapes.
- Backend-neutral capability contract for later PostgreSQL/S3 implementations without concrete adapters.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-05-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild JSON as a revision-checked streaming projection</name>
  <reversibility rating="costly">D-26 changes JSON from writable authority to compatibility projection; public shapes remain compatible while lifecycle ownership moves.</reversibility>
  <files>src/cacheness/storage/manifest_repository.py, src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/metadata.py, tests/test_manifest_repository_cas.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/storage/path_security.py</file>
    <file>tests/test_manifest_repository_cas.py</file>
  </read_first>
  <behavior>
    - "Promotion dirties projection state in the same transaction; export marks clean only for the exact captured authority revision."
    - "A slow revision-R exporter cannot mark clean or overwrite revision R+1 as current; failure leaves dirty/error state and never rolls back committed authority."
    - "Export takes a mode-restricted private SQLite backup, closes live source access, keyset-streams canonical committed rows, and atomically replaces compatible JSON."
    - "Missing/corrupt projection rebuilds; an established store with missing authority and any corrupt/wrong authority fail closed, while a truly never-written store retains Plan 02's non-materialized empty view."
  </behavior>
  <action>Replace manifest-repository lifecycle CAS/inventory duties with a projection/export adapter. On entry transitions, keep dirty flag and authority revision changes inside the authority transaction. For SQLite export, create a restrictive private destination, use `sqlite3.Connection.backup` for a consistent snapshot, release the live source, read revision R from the snapshot, keyset-stream authenticated committed rows into the characterized JSON metadata shape, fsync and atomically replace, then mark clean only with expected revision R. If authority advanced, keep projection dirty and rebuild later. Projection errors are bounded typed lag/debt, not mutation rollback. Preserve Plan 02's lazy empty view for a truly never-written root, but treat JSON/payload/metadata as established evidence when authority is absent. Keep JSON imports/read-only compatibility only where Phase 1/2 require; never use JSON to restore missing authority or arbitrate cross-process publication.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_manifest_repository_cas.py -k "projection or revision or backup or json or rebuild" -x</automated>
  </verify>
  <acceptance_criteria>Revision race, export failure, corruption, streaming bounds, and rebuild cases pass; authority remains sole truth; public JSON shape matches Wave 0 characterization.</acceptance_criteria>
  <done>JSON compatibility survives without becoming a second lifecycle authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish authority-only BlobStore composition and capability rejection</name>
  <reversibility rating="costly">D-24/D-27 establish the semantic adapter seam every later backend composition must honor.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/memory_lifecycle_authority.py, src/cacheness/storage/blob_store.py, src/cacheness/config.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/storage/read_contract.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
  </read_first>
  <behavior>
    - "Put/get/get_metadata/update_metadata/delete/exists/list/clear/reconcile all obtain canonical state from the one selected authority."
    - "JSON and SQLite compatibility backends preserve public metadata shape while SQLite authority owns lifecycle state; explicit memory selection uses memory authority with honest same-process capabilities."
    - "A request for durable multiprocess guarantees with memory/unsupported topology fails before handler staging or authority creation."
    - "Caller-injected authority remains selected and open after BlobStore close; internally created authority closes exactly once."
  </behavior>
  <action>Make BlobStore select/inject exactly one authority: SQLite at the reserved locator for persistent local stores and InMemory only for explicit ephemeral same-process use. Route every direct operation through it; backend metadata objects become compatibility projections/adapters, never alternate authoritative reads. Preserve constructor/public return/error/metadata shapes established by earlier phases. Validate the semantic capability set—durability, local-process sharing, exact CAS, indexed paging, projection—and fail with `BLOB_BACKEND_CAPABILITY_UNSUPPORTED` before staging when claims cannot be met. Define the Phase 4/5 adapter contract only in backend-neutral terms; do not add PostgreSQL/S3 implementations. Track ownership so BlobStore closes only internally created authority/projection/backend resources.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_blob_store_read_contract.py tests/test_lifecycle_authority_contract.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>All public direct operations use one authority; metadata/report shapes remain compatible; topology and ownership tests pass; no concrete remote adapter or file fallback is introduced.</acceptance_criteria>
  <done>BlobStore has one honest authority composition root ready for later backend adapters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SQLite authority snapshot → JSON projection | Derived output must remain revision-bound, bounded, redacted, and non-authoritative. |
| user topology/config → authority selection | Unsupported durability/sharing claims must fail before persistent effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-20 | Tampering | stale JSON exporter | high | mitigate | Expected-revision clean CAS, atomic replace, dirty-on-race, and two-exporter deterministic test. |
| T-03-21 | Spoofing | JSON presented as authority | high | mitigate | All direct operations read LifecycleAuthority; missing/corrupt authority fails without JSON reconstruction. |
| T-03-22 | Information Disclosure | private backup/projection | medium | mitigate | Restrictive temporary object, no payload bytes, bounded safe fields, cleanup, and no secret/raw error logging. |
| T-03-23 | Tampering | unsupported topology downgrade | high | mitigate | Explicit capabilities and pre-staging typed rejection. |
</threat_model>

<verification>
Run projection race/corruption tests and the complete direct-read/common-authority contracts; inspect event logs to prove no projection callback runs inside a live authority transaction.
</verification>

<success_criteria>
- Lifecycle authority is the only read/write truth for BlobStore.
- JSON is rebuildable, streamed, revision-checked, and compatibility-shaped.
- Unsupported topology and ownership errors fail before staging.
- Later adapters have a semantic seam but no premature implementation.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-06-SUMMARY.md` when done.
</output>
