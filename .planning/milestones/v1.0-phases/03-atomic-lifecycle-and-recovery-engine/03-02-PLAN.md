---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/cacheness/storage/lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/blob_store.py
  - tests/test_lifecycle_authority_contract.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-07]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A direct BlobStore write/read/reopen uses one LifecycleAuthority as the only committed-manifest authority and returns the native handler value (D-01, D-03, D-04, D-24, D-25)."
    - "Durable intent commits before the first managed payload side effect, while serialization, publish/fsync, verification, and cleanup occur with no authority transaction open (D-05, D-08, D-25, D-28)."
    - "Memory and SQLite implement the same deep transition contract; the interface contains no SQLite SQL/path concepts and no file-scheduler primitives (D-23, D-24, D-27)."
    - "A compatible never-written root is inspected lazily with zero filesystem mutation; established or suspicious roots missing authority fail typed and unchanged instead of being mistaken for empty (D-21, D-24, D-26)."
  artifacts:
    - path: src/cacheness/storage/lifecycle_authority.py
      provides: "Deep complete-transition interface and frozen bounded domain records"
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Thin persistent tracer implementation at the confirmed local locator"
    - path: src/cacheness/storage/memory_lifecycle_authority.py
      provides: "Same-process reference implementation"
    - path: src/cacheness/storage/lifecycle.py
      provides: "Payload-I/O orchestration around authority transitions"
    - path: tests/test_blob_store_read_contract.py
      provides: "Byte-for-byte zero-mutation bootstrap and established-store classification proof"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "All committed manifest lookup and publication"
      pattern: "LifecycleAuthority"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/guarded_handler_io.py
      via: "Native immutable candidate publication outside authority transactions"
      pattern: "stage|publish_generation|open_snapshot"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "prepare then promote complete transitions"
      pattern: "prepare_mutation|promote_mutation"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] The authority interface must not expose receipt, inventory, head, tail, anchor, pending-file, lock-file, or raw-table CRUD."
    - "[FLAGGED/UNVERIFIED] No authority transaction may span handler or payload I/O."
    - "[FLAGGED/UNVERIFIED] Native payload bytes must remain handler-owned and unwrapped."
---

<objective>
Prove the replacement architecture end to end with one persistent BlobStore write/read/reopen path, then make the same deep contract executable in memory.

Purpose: Validate the hardest architectural constraint before broad lifecycle expansion: one transaction authority can coordinate immutable external payloads without becoming another scheduler.
Output: LifecycleAuthority domain contract, SQLite and memory adapters, and a production tracer through BlobStore.
</objective>

## Artifacts this phase produces

- `LifecycleAuthority` complete transition families and immutable bounded records.
- `SqliteLifecycleAuthority` and `InMemoryLifecycleAuthority` implementations.
- A real BlobStore put/get/reopen tracer using native payload bytes and canonical manifests.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-01-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VERIFICATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one SQLite-authority BlobStore write, read, and reopen path</name>
  <precondition>03-01-SUMMARY.md records `confirm-recommended` and the scheduler release-history preflight passed.</precondition>
  <reversibility rating="costly">The Plan 01-confirmed application ID, user_version, authority locator, and normalized schema become persistent store identity and require an ordered migration to change.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/blob_store.py, tests/test_lifecycle_authority_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest.py</file>
    <file>src/cacheness/storage/integrity.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/_lifecycle_test_support.py</file>
  </read_first>
  <behavior>
    - "A first put against absence commits intent, publishes and verifies one immutable native candidate, atomically promotes one authenticated committed manifest, and returns the key."
    - "A reopened BlobStore reads the authority entry, authenticates the canonical manifest, snapshots and verifies the exact payload, and returns the original value."
    - "At every payload observer pause, the authority reports zero open write transactions."
    - "An operation prepared against an earlier absence cannot promote after an intervening create/delete lineage change."
    - "Before first mutation, the confirmed authority path does not exist; the first put creates it only after the same read-only preflight has classified the root as compatible and empty."
    - "On Windows, the local NTFS root already exists before Cacheness starts; authority database creation/open/mutation first proves the current token logon SID and a protected root DACL whose only ordinary mutation grant is that S-1-5-5-X-Y logon SID, while an absent root/proof fails typed and unchanged with the offline provisioning command."
  </behavior>
  <action>Start with the failing tracer and observer assertions. Define frozen bounded records for entry expectation/snapshot, mutation specification/preparation, verification proof, promotion result, cleanup debt, clear/reconciliation page tokens, projection revision, and adapter capabilities. Define `LifecycleAuthority` in semantic complete-transition terms: read entry, prepare, record verification, promote/abort, exact delete/tombstone retirement, clear snapshot/page/checkpoint, reconciliation start/page/checkpoint, projection compare-and-mark, and close. Implement only the complete methods needed by the tracer in SQLite while retaining the full interface shape for later expansion. Use the confirmed reserved locator and normalized schema. Add a two-stage preflight: first `lstat`/bounded directory classification with no mkdir, SQLite connect, JSON open-for-write, journal/control creation, or handler staging; only when a contained regular authority file already exists may a second stage open it explicitly read-only to validate identity/version/integrity. If no authority exists, any payload, metadata, legacy, scheduler sentinel, future marker, wrong object, or mixed Cacheness evidence fails immediately; only absent/no-artifact state receives the empty view. On non-Windows supported local filesystems, create the root/authority only when the first mutation proceeds after an empty-compatible preflight. On Windows, read-only inspection of an absent/empty root still returns the zero-mutation empty view, but the first and every later mutation require the local NTFS root to have been provisioned before Cacheness starts. Before authority database creation/open and every mutation, resolve the current token's `S-1-5-5-X-Y` logon SID through supported OS token evidence and validate the existing protected DACL: the logon SID is the sole ordinary mutation principal, inherited and unrelated write ACEs and account-SID mutation grants are absent, and any SYSTEM/Administrators ACE is explicitly non-mutating and documented for inspection/recovery. Cacheness never creates that Windows root, changes its mode, disables inheritance, or edits ACEs. An absent root, missing logon SID, unavailable ACL proof, owner/DACL mismatch, unsafe descriptor, or scope drift fails typed and byte-for-byte unchanged before SQLite or payload effects and includes the exact offline PowerShell/`icacls.exe` provisioning command documented by Plan 01. This is an OS ACL deployment boundary, not a Cacheness lock. Rewrite the BlobStore/lifecycle tracer so D-01 through D-08 and D-24/D-25 make the promotion transaction the sole visibility switch; canonical manifest authentication remains before locator trust, and native handler output gains no extra framing. Instrument transaction boundaries so tests prove D-28 structurally and behaviorally.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_lifecycle_authority_contract.py -k "tracer or reopen or transaction_boundary or aba" -x &amp;&amp; .venv/bin/pytest -q tests/test_blob_store_read_contract.py -k "tracer or authenticates_validates" -x</automated>
  </verify>
  <acceptance_criteria>The real SQLite database path is absent through read-only preflight and is created only by first mutation inside an eligible root; on Windows that root was provisioned before Cacheness starts and absent/unsafe/drifted roots fail unchanged with offline guidance. The store survives close/reopen, returns the native value through Phase 2 authentication ordering, changes visibility in one promotion commit, preserves ABA lineage, and observes no authority transaction during payload work.</acceptance_criteria>
  <done>The transactional-authority architecture is proven end to end and is the only skeleton later tasks expand.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove lazy zero-mutation inspection and fail-closed store classification</name>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/storage/blob_store.py, tests/test_lifecycle_authority_contract.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>docs/lifecycle-authority.md</file>
    <file>src/cacheness/storage/path_security.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
  </read_first>
  <behavior>
    - "On an absent or empty compatible root, get/get_metadata return normal misses, exists is false, list is empty, and two dry-run reconciliations return the same empty report."
    - "Those operations and close leave exact before/after tree snapshots equal and create no root directory, authority database, JSON projection, SQLite journal, temporary file, or scheduler control."
    - "The same empty behavior survives a read-only close/reopen; authority database creation remains deferred until the first mutation, and on Windows an absent root still causes that mutation to fail typed and unchanged."
    - "Any existing payload/metadata/legacy/scheduler/future/corrupt/wrong-object evidence without the confirmed authority returns a typed rebuild-or-migration-required result and leaves every byte and timestamp unchanged."
  </behavior>
  <action>Route every inspection-only BlobStore entry point through the two-stage classifier and a non-materialized empty-authority view. Treat only a genuinely absent root or an existing directory with no Cacheness artifacts as empty-compatible. Stage 1 uses only contained `lstat` and bounded names/types: authority missing plus payload/manifest/metadata/legacy/scheduler/future/wrong-object/mixed evidence fails before open. Stage 2 is permitted only for an existing contained regular authority file and opens SQLite read-only to validate application/store/schema identity and report corrupt/future state without sidecars or mutation. Established evidence without authority is never empty. Keep JSON a non-authoritative projection and never rebuild SQLite from it. Use exact recursive path/type/mode/mtime/size/digest snapshots in tests, including nonexistent-parent state, repeated calls, close, and reopen.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_blob_store_read_contract.py tests/test_lifecycle_authority_contract.py -k "empty_inspection or lazy_bootstrap or established_missing_authority or read_only_reopen" -x</automated>
  </verify>
  <acceptance_criteria>Every read-only public result is normal and stable for a truly empty store; no persistent object is created; Windows mutation never provisions an absent root; all nonempty authority-missing and Windows absent/unsafe/drifted classifications fail typed and byte-for-byte unchanged; JSON never supplies authority.</acceptance_criteria>
  <done>Inspection is lazy and non-mutating without weakening fail-closed handling of established stores.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the same complete transition contract in memory</name>
  <reversibility rating="costly">D-24 and D-27 make the deep interface the semantic contract future PostgreSQL and S3-capable adapters must implement.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/memory_lifecycle_authority.py, tests/test_lifecycle_authority_contract.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/test_lifecycle_authority_contract.py</file>
  </read_first>
  <behavior>
    - "The common create/overwrite/conflict/rollback/idempotent-repeat cases pass unchanged against memory and SQLite."
    - "Memory returns immutable copies, advances the same lineage/revision semantics, and rejects durable or multiprocess capability requests."
    - "Separate memory authority instances are isolated; multiple stores coordinate only when injected with the same authority instance."
  </behavior>
  <action>Implement `InMemoryLifecycleAuthority` with one private lock per short complete transition and copy-on-read immutable values. Keep the same operation identifiers, lineage tokens, exact manifest expectations, projection revisions, and typed conflicts as SQLite. Per D-17 through D-20, local locking is an ordering aid only; shared-instance CAS selects same-process winners and unrelated payload work remains outside the lock. Expose only backend-neutral capability facts needed by Phases 4/5, without creating PostgreSQL/S3 adapters or file-based fallbacks.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_lifecycle_authority_contract.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>The parametrized contract passes unchanged for both adapters; memory makes no durability/cross-process claim; the interface contains complete domain transitions and no scheduler/SQL/path primitives.</acceptance_criteria>
  <done>Both Phase 3 authorities share one deep semantic contract ready for durable expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authority rows → canonical manifest | Persisted row data must be bounded and the manifest authenticated before locator use. |
| authority intent → payload backend | Only exact operation-bound contained locators may receive persistent payload bytes. |
| verified payload → promotion | Digest/size and expected lineage cross into the sole visibility transaction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04 | Tampering | authority record decoder | high | mitigate | Bound every field/version, authenticate canonical bytes first, and corroborate indexed fields exactly. |
| T-03-05 | Elevation of Privilege | candidate locator | high | mitigate | Retain containment/no-follow/exclusive create and bind locator to operation plus generation. |
| T-03-06 | Denial of Service | transaction scope | high | mitigate | Transaction observers assert no serialization, fsync, verification, deletion, callback, logging, or retry sleep occurs while a write transaction is open. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager install exists; both adapters use the Python standard library. |
</threat_model>

<verification>
Run the full common authority contract plus Phase 2 read-order selection. Inspect the interface for semantic methods only and assert transaction observer balance at every payload boundary.
</verification>

<success_criteria>
- One durable BlobStore write/read/reopen works through the authority.
- Both adapters implement one complete semantic interface.
- ABA-safe lineage and exact canonical expectations select authority transitions.
- Payload bytes and I/O remain entirely outside authority transactions.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-02-SUMMARY.md` when done.
</output>
