---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "12"
type: execute
wave: 10
depends_on: ["03-09"]
files_modified:
  - .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The completed 03-09 summary remains byte-for-byte unchanged and retains `.venv/bin/python verify_platform.py --phase3` as the repository-runtime evidence command."
    - "A separate additive contract designates exactly `uv run --python 3.11 --frozen python verify_platform.py --phase3 --require-system Windows --require-python 3.11` as the Python 3.11 native-qualification attestation command (D-32)."
    - "The additive contract binds to the completed 03-09 summary digest and recorded Task 1/Task 2 commits without rewriting or repeating their implementation."
    - "The completed 03-12 summary exposes both command roles in one structured `platform-evidence-contract` mapping so Plan 03-11 can bind to the qualification mapping rather than misreading the repository-runtime mapping."
    - "UNAVAILABLE/exit 2 remains NOT_QUALIFIED with native_evidence=false; Phase 999.1 still requires native PASS/exit 0 before any Windows-qualified release (D-22, D-31, D-32)."
  artifacts:
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md
      provides: "Additive, digest-bound distinction between repository-runtime evidence and Python 3.11 native-qualification attestation"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md
      provides: "Completed structured command-role contract consumed by Plans 03-11 and 03-10"
  key_links:
    - from: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md
      via: "Exact source-summary SHA-256 and preserved repository-runtime command"
      pattern: "source_summary_sha256|repository_runtime_command"
    - from: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md
      via: "Structured `platform-evidence-contract` mirror with separate designated command fields"
      pattern: "repository_runtime_command|qualification_attestation_command"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] The completed 03-09 summary and its implementation commits must not be edited, rewritten, squashed, or repeated."
    - "[FLAGGED/UNVERIFIED] The repository-runtime command must not be relabeled as the Python 3.11 native-qualification command."
    - "[FLAGGED/UNVERIFIED] UNAVAILABLE must not be promoted to PASS or cited as native Windows support (D-32)."
---

<objective>
Add a truthful post-03-09 command-role contract without mutating completed evidence.

Purpose: Resolve the immutable summary's command ambiguity before qualification capture, preserving Plan 03-09 history while giving Plan 03-11 one correct structured mapping to attest.
Output: A digest-bound platform-evidence addendum and a normal completed 03-12 summary that distinguish repository-runtime testing from Python 3.11 native qualification.
</objective>

## Artifacts this phase produces

- An additive contract that preserves the completed Plan 03-09 summary and implementation evidence.
- A structured completed summary mapping that downstream plans can parse without relying on prose.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/ROADMAP.md

The completed 03-09 summary is immutable. Its `platform-evidence.command` is the repository-runtime evidence command, not the separate Python 3.11 native-qualification attestation command. This plan adds a new contract; it does not correct history by mutation.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Publish the additive two-command evidence contract</name>
  <reversibility rating="reversible">The additive contract can be superseded by a later version without changing completed Plan 03-09 or lifecycle code.</reversibility>
  <files>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md</files>
  <read_first>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <action>Create a Markdown artifact with bounded YAML frontmatter and `contract_version: 1`. Add a `repository-runtime-evidence` mapping whose command is exactly `.venv/bin/python verify_platform.py --phase3` and whose role states that it records the repository interpreter/current-host runner behavior already completed by Plan 03-09. Add a distinct `qualification-attestation` mapping whose command is exactly `uv run --python 3.11 --frozen python verify_platform.py --phase3 --require-system Windows --require-python 3.11`, expected status is UNAVAILABLE, expected exit code is 2, milestone status is NOT_QUALIFIED, native evidence is false, backlog phase is 999.1, future required status is PASS, and future required exit code is 0 per D-32. State that the fixed qualification argv is non-overridable and that an eligible Phase 999.1 Windows run additionally supplies the protected NTFS root and distinct session/service-token evidence required by D-22 and D-31. Do not edit the completed 03-09 summary or any implementation/test file.</action>
  <verify>
    <automated>uv run --python 3.11 --frozen python -c "from pathlib import Path; p=Path('.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md').read_text(); runtime='.venv/bin/python verify_platform.py --phase3'; qualification='uv run --python 3.11 --frozen python verify_platform.py --phase3 --require-system Windows --require-python 3.11'; assert p.count(runtime) == 1; assert p.count(qualification) == 1; assert runtime != qualification; assert all(x in p for x in ('UNAVAILABLE', 'NOT_QUALIFIED', 'native_evidence: false', 'backlog_phase: 999.1', 'future_required_status: PASS', 'future_required_exit_code: 0'))"</automated>
  </verify>
  <acceptance_criteria>The artifact assigns the two exact commands to separate structured roles and preserves honest UNAVAILABLE/NOT_QUALIFIED semantics with Phase 999.1 future native PASS (D-32).</acceptance_criteria>
  <done>Downstream tooling has an unambiguous designated qualification-attestation mapping without any mutation of completed Plan 03-09.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the addendum to immutable Plan 03-09 evidence</name>
  <reversibility rating="reversible">The binding is additive and can be reissued if the evidence format is versioned.</reversibility>
  <files>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md</files>
  <read_first>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md</file>
  </read_first>
  <action>Compute SHA-256 over the exact completed `03-09-SUMMARY.md` bytes and add a `source-binding` frontmatter mapping containing the source summary path, digest, completion commit `947f76d`, and the eleven existing Task 1/Task 2 implementation commit IDs already recorded by that summary: `9abeb10`, `f9e64e7`, `5735e1a`, `5165dc1`, `d74ec66`, `5488478`, `f05e6c1`, `7888814`, `a846c10`, `7d59ff4`, and `d0dcc73`. Verify each commit object exists and that the completed summary is identical to its committed `947f76d` version. This binding records provenance only; do not replay, amend, squash, or create replacements for those commits. Preserve the two command mappings from Task 1 exactly.</action>
  <verify>
    <automated>git diff --exit-code 947f76d -- .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md &amp;&amp; git cat-file -e 9abeb10^{commit} &amp;&amp; git cat-file -e f9e64e7^{commit} &amp;&amp; git cat-file -e 5735e1a^{commit} &amp;&amp; git cat-file -e 5165dc1^{commit} &amp;&amp; git cat-file -e d74ec66^{commit} &amp;&amp; git cat-file -e 5488478^{commit} &amp;&amp; git cat-file -e f05e6c1^{commit} &amp;&amp; git cat-file -e 7888814^{commit} &amp;&amp; git cat-file -e a846c10^{commit} &amp;&amp; git cat-file -e 7d59ff4^{commit} &amp;&amp; git cat-file -e d0dcc73^{commit} &amp;&amp; uv run --python 3.11 --frozen python -c "from hashlib import sha256; from pathlib import Path; a=Path('.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md').read_text(); s=Path('.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md'); assert f'source_summary_sha256: {sha256(s.read_bytes()).hexdigest()}' in a"</automated>
  </verify>
  <acceptance_criteria>The additive contract is cryptographically bound to the unchanged completed summary and names every existing implementation commit without rewriting or re-executing Plan 03-09 work.</acceptance_criteria>
  <done>The command-role correction is additive, immutable-source-bound, and ready for Plan 03-11 structural attestation.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generations and recoverable incomplete outcomes | 03-02..03-10 | COVERED | Plan 03-12 changes evidence labeling only. |
| REQ | STOR-03..STOR-07 | Atomic lifecycle, recovery, cleanup, reconciliation, and concurrency | 03-02..03-10 | COVERED | Completed implementation and Plan 03-09 evidence remain unchanged. |
| RESEARCH | — | Python/platform evidence and scheduler retirement | 03-09, 03-12, 03-11, 03-10 | COVERED | Plan 03-12 makes the two command roles explicit before attestation. |
| CONTEXT | D-01..D-31 | Locked lifecycle and replacement-architecture decisions | 03-01..03-10 | COVERED | No architecture or implementation is reopened. |
| CONTEXT | D-32 | Honest UNAVAILABLE/NOT_QUALIFIED record and Phase 999.1 native-PASS obligation | 03-09, 03-12, 03-11, 03-10 | COVERED | Separate mappings preserve history and designate the fixed qualification argv. |

Exclusions remain unchanged: PostgreSQL/S3 adapters are Phases 4/5; UnifiedCache policy is Phase 6; stored-format migration is Phase 7; archived file-native artifacts are historical evidence only.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| completed summary → additive contract | Historical evidence must be referenced and digest-bound, never silently reinterpreted or mutated. |
| additive contract → downstream helper | The helper must select the designated qualification mapping rather than infer a command from prose or a different role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-12-01 | Tampering | completed Plan 03-09 evidence | high | mitigate | Require exact committed-summary identity, SHA-256 binding, and existing task-commit objects. |
| T-03-12-02 | Spoofing | command role | high | mitigate | Publish separately named structured mappings with exact commands; Plan 03-11 binds only to `qualification_attestation_command`. |
| T-03-12-03 | Repudiation | native Windows claim | high | mitigate | Preserve UNAVAILABLE/exit 2, NOT_QUALIFIED, native_evidence=false, and Phase 999.1 PASS/exit 0 requirements (D-32). |
</threat_model>

<verification>
Verify the additive artifact contains both exact commands under distinct roles, matches the exact completed 03-09 summary digest and commit, references every recorded implementation commit, and leaves the completed summary byte-for-byte unchanged.
</verification>

<success_criteria>
- Completed Plan 03-09 evidence and implementation history are unchanged.
- Repository-runtime and Python 3.11 native-qualification commands are structurally distinct.
- The fixed qualification command remains exact and non-overridable.
- UNAVAILABLE is NOT_QUALIFIED, not Windows support; Phase 999.1 retains native PASS/exit 0.
- Plan 03-11 can structurally bind its helper to a completed `platform-evidence-contract` mapping.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md` only after both tasks complete. Add a `platform-evidence-contract` frontmatter mapping with `contract_artifact`, `contract_artifact_sha256`, `source_summary`, `source_summary_sha256`, `repository_runtime_command`, `qualification_attestation_command`, `expected_status: UNAVAILABLE`, `expected_exit_code: 2`, `milestone_status: NOT_QUALIFIED`, `native_evidence: false`, `backlog_phase: 999.1`, `future_required_status: PASS`, and `future_required_exit_code: 0`. This completed mapping is the authoritative structured command-role source consumed by Plans 03-11 and 03-10.
</output>
