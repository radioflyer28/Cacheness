---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "22"
type: execute
wave: 21
depends_on: ["03-21"]
files_modified:
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/core.py
  - tests/test_sqlite_bootstrap_concurrency.py
  - tests/test_sqlite_lifecycle_authority.py
  - tests/test_unified_cache_lifecycle_authority.py
  - docs/STORAGE_INITIALIZATION.md
autonomous: false
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-07]
estimate:
  tokens: 30000
  tasks: 3
  confidence: medium
must_haves:
  truths:
    - "An initialized store reopens in independent spawned workers without hidden schema creation or migration (CR-04; D-26, D-28, D-35)."
    - "Incomplete, foreign, corrupt or future authority evidence fails typed and unchanged; zero application ID is not initializer ownership (D-15, D-16, D-21)."
    - "BUSY/LOCKED are caused retryable timeouts; IOERR/FULL/READONLY/CANTOPEN are caused backend errors rather than migration advice (WR-01; D-18, D-19)."
    - "Absent inspection stays non-mutating and single-process convenience uses the same explicit initializer."
  artifacts:
    - path: src/cacheness/storage/blob_store.py
      provides: "Supported idempotent initialize"
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Creation separated from validation-only runtime and code-based error typing"
    - path: docs/STORAGE_INITIALIZATION.md
      provides: "Approved startup/maintenance contract and compatibility disposition"
    - path: tests/test_sqlite_bootstrap_concurrency.py
      provides: "Initialized-worker and incomplete-evidence proof"
  key_links:
    - from: src/cacheness/core.py
      to: src/cacheness/storage/blob_store.py
      via: "Cache initialization delegates storage setup"
      pattern: "initialize"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/sqlite_lifecycle_authority.py
      via: "Single explicit creation path"
      pattern: "initialize|_validate_schema"
  prohibitions:
    - "[UNVERIFIED] No private-candidate installation, database replacement, readiness sidecar, FIFO, lease or new filesystem coordination protocol."
    - "[UNVERIFIED] No automatic adoption, deletion, or migration of incomplete/incompatible evidence during ordinary access."
    - "[UNVERIFIED] Native Windows remains UNAVAILABLE/NOT_QUALIFIED; protected sidecars remain untouched."
---

<objective>
Resolve CR-04 by an approved initialization-before-workers contract, not a stronger bootstrap protocol. Close WR-01 with precise SQLite error translation.
Output: explicit initializer, validation-only normal opens, public worker tracer, documented compatibility and maintenance.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/phase3-architecture-audit-2026-09-06.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-GAP-REPLAN.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
</context>

<planner_assumptions>
Integrity protects partial/foreign evidence. Recovery after incomplete initialization is typed non-destructive maintenance guidance. Shared-store progress begins after initialization completes; concurrent first creation/online migration has no success promise. Performance is separate. D-35 is a proposal requiring approval, not authority to delete old tests.
</planner_assumptions>

<tasks>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve startup and schema-maintenance compatibility changes</name>
  <reversibility rating="one-way">Published behavior withdrawal needs explicit approval.</reversibility>
  <files>docs/STORAGE_INITIALIZATION.md</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/core.py</file>
    <file>tests/test_sqlite_bootstrap_concurrency.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
  </read_first>
  <action>Inventory actual constructor, first-put, absent-read and implicit-schema-upgrade behaviors/tests. Present initialization once before workers, preserved absent inspection and single-process convenience, unchanged rejection of incomplete evidence, maintenance-only upgrades. Enumerate internal schemas currently accepted by _migrate_schema_layout and give explicit maintenance or confirmed rebuild guidance for each; Phase 7 retains the general migration tool.</action>
  <decision>Approve the enumerated startup/maintenance compatibility change, or retain the current contract and stop for replanning; do not choose on the user's behalf.</decision>
  <options><option id="approve">Approve initialization-before-workers with the enumerated compatibility changes.</option><option id="revise">Retain the current startup contract and replan before implementation.</option></options>
  <resume-signal>Explicit user approval or a requested revision after reviewing the inventory.</resume-signal>
  <verify>Record actual human approval before runtime edits.</verify>
  <done>Exact compatibility scope and approval are recorded; refusal blocks rather than triggering an alternative filesystem protocol.</done>
</task>
<task type="tracer" tdd="true">
  <name>Task 2: Initialize once, then exercise independent local workers</name>
  <depends_on>Task 1 approved.</depends_on>
  <reversibility rating="costly">Startup/runtime separation; retain schema identity and public wrappers.</reversibility>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/core.py, tests/test_sqlite_bootstrap_concurrency.py, tests/test_unified_cache_lifecycle_authority.py, docs/STORAGE_INITIALIZATION.md</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/core.py</file>
    <file>tests/test_sqlite_bootstrap_concurrency.py</file>
    <file>tests/test_unified_cache_lifecycle_authority.py</file>
  </read_first>
  <action>Add BlobStore.initialize() using the existing schema transaction and confirmed identity. Complete storage-owned key/control prerequisites before return, honoring injected ownership and Windows offline provisioning. Cache setup delegates to BlobStore and finishes configured metadata initialization before workers; expose a documented cache initializer if needed to preserve constructor laziness. Memory initialize is idempotent in-process.

Normal existing-authority connections validate without hidden migration. Route single-process first-write convenience through the SAME initializer. Preserve zero-mutation absent inspection; remove bootstrap-only readiness/scheduling inference no longer needed by the approved contract. Keep containment and exclusive non-clobber creation, never rename/replace an initialized DB. A crashed initializer's leaf is not adopted, truncated or automatically deleted.

Tracer: parent initializes, closes, spawns fresh processes with no inherited instance; workers construct, put/read/overwrite/delete, then parent reopens/reconciles. Repeat the cache initialization route and an already valid store. Instrument schema creation/migration to prove workers never perform them. Convert only explicitly superseded availability premises and preserve integrity assertions.

Deterministically kill initialization before/during schema commit. Incomplete open/mutation fails typed unchanged; completed initialization reopens. Concurrent-first-create negative proof requires no clobber or partial publication, not universal success. Document manual inventory and explicit maintenance/rebuild guidance without destructive automation.</action>
  <acceptance_criteria>Initialized public worker workflows pass without app-ID-zero bootstrap failure. Absent inspection and single-process convenience remain characterized. Foreign/incomplete/future evidence stays unchanged. No transaction spans payload work and no coordinator is added.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_bootstrap_concurrency.py tests/test_unified_cache_lifecycle_authority.py tests/test_blob_store_read_contract.py tests/test_blob_store_legacy_contract.py -o log_cli=false</automated></verify>
  <done>CR-04 is closed for the approved topology, with startup limits explicit.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Preserve operational SQLite failure meaning</name>
  <depends_on>Task 2 tracer passes.</depends_on>
  <reversibility rating="reversible">Error typing, not data format or timeout policy.</reversibility>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_sqlite_lifecycle_authority.py, docs/STORAGE_INITIALIZATION.md</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
  </read_first>
  <action>Mask sqlite_errorcode to the primary code. BUSY/LOCKED retain caused retryable lifecycle timeouts; IOERR/FULL/READONLY/CANTOPEN become caused CacheBlobBackendError; CORRUPT/NOTADB and explicit identity/schema mismatch remain fail-closed corruption/migration. Unknown/missing codes default to backend failure, not assumed migration or text matching. Test primary/extended/unknown codes, connection/setup/transaction failure, rollback and preserved committed state. Do not change runtime defaults or add timeout-stage allowlists.</action>
  <acceptance_criteria>Matrix preserves __cause__ and operation context, retryability only where promised; DatabaseError inheritance alone never implies migration.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_lifecycle_authority.py tests/test_sqlite_authority_admission.py -o log_cli=false</automated></verify>
  <done>WR-01 closes with exact code-based outcomes and no new scheduling policy.</done>
</task>
</tasks>
<threat_model>
Existing control paths/schema bytes are trust boundaries. Adoption of foreign or partial evidence and hidden runtime migration are threats; validation-only opens, initializer ownership limited to its newly created leaf, existing containment/DACL controls and caused errors mitigate them. High-severity integrity gaps block. The compatibility checkpoint prevents silent contract withdrawal.
</threat_model>
<verification>Use isolated disposable fixtures, named tests, scoped Ruff and the Phase 3 delta verifier. No new schema/version or external service means no schema-push task. Any required new migration mechanism triggers the ADR stop rule. Windows stays unqualified.</verification>
<success_criteria>Approved startup works end to end; unsupported startup preserves integrity; incomplete evidence is untouched; WR-01 classification is precise. D-01 through D-04, D-21, D-22, D-26, D-28, D-31, D-32 remain protected.</success_criteria>
<output>Create 03-22-SUMMARY.md with actual approval, compatibility inventory, initializer/maintenance contract, process tests and exact commands/commit.</output>
