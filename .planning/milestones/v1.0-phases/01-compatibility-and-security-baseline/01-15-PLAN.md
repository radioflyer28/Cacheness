---
phase: 01-compatibility-and-security-baseline
plan: "15"
type: execute
wave: 10
depends_on:
  - 01-14
files_modified:
  - src/cacheness/storage/blob_store.py
  - src/cacheness/core.py
  - tests/test_filesystem_containment.py
  - tests/test_cache_integrity.py
  - tests/test_clear_recovery.py
  - tests/test_phase1_quality_gates.py
  - .planning/phases/01-compatibility-and-security-baseline/01-15-EXECUTION-EVIDENCE.md
autonomous: true
gap_closure: true
requirements:
  - SECU-01
review_findings:
  - CR-02
  - CR-03
  - CR-04
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per CR-02, BlobStore first writes and overwrites serialize to unique contained candidates; metadata failure removes only the candidate and preserves the exact prior metadata, bytes, handler format, and readable value."
    - "Per CR-03, UnifiedCache owns its candidate through one entire pre-commit region; snapshot, digest, signing, and metadata failures leave no candidate and preserve an overwrite exactly."
    - "Per CR-04, UnifiedCache.clear_all() uses Plan 01-14's prerequisite fixed-journal coordinator and topology rules, stages every payload before metadata commit, rolls every pre-commit failure back, and finishes committed cleanup after reopen."
    - "Candidate cleanup failure is explicit and chained; after successful metadata publication, prior-payload cleanup failure cannot undo or mislabel the new authoritative entry."
    - "Executors leave validation draft/pending and record implementation evidence without authoring review artifacts; only the orchestrator may run renewed review and restore final approval after execution."
    - "STOR-03..STOR-06 and CACH-03 remain INCOMPLETE downstream contracts; this plan closes reviewed cases without claiming the complete lifecycle or invalidation surface."
  artifacts:
    - path: src/cacheness/storage/blob_store.py
      provides: "Private candidate publication and shared clear-coordinator use"
    - path: src/cacheness/core.py
      provides: "Unified candidate ownership and shared clear_all recovery"
    - path: tests/test_cache_integrity.py
      provides: "UnifiedCache pre-commit candidate failure matrix"
    - path: tests/test_clear_recovery.py
      provides: "UnifiedCache every-position and close/reopen recovery matrix"
    - path: .planning/phases/01-compatibility-and-security-baseline/01-15-EXECUTION-EVIDENCE.md
      provides: "Executor-authored base/head, command, count, CR-mapping, and downstream-incomplete evidence for orchestrator review"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: its metadata backend
      via: "candidate locator becomes authoritative only when put_entry succeeds"
      pattern: "candidate|put_entry"
    - from: src/cacheness/core.py
      to: src/cacheness/storage/clear_recovery.py
      via: "clear_all uses the same fixed-journal state machine and topology gate as BlobStore"
      pattern: "ClearRecoveryCoordinator"
    - from: tests/test_phase1_quality_gates.py
      to: .planning/phases/01-compatibility-and-security-baseline/01-15-EXECUTION-EVIDENCE.md
      via: "the executable Ruff/validation gate leaves approval pending while the evidence file records measured execution results"
      pattern: "ruff|status: draft|Approval.*pending"
---

<objective>
Close candidate-publication and UnifiedCache-clear blockers, then hand current execution evidence to the orchestrator for renewed review.

Purpose: Ensure failed publication never changes or leaks application payload state, reuse the bounded clear primitive rather than duplicate it, and preserve separation between implementation execution and independent approval.
Output: Candidate-safe BlobStore/UnifiedCache writes, transactional UnifiedCache clear, full regression evidence, and an executor evidence handoff.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-REVIEW.md
@.planning/phases/01-compatibility-and-security-baseline/01-14-SUMMARY.md
@src/cacheness/storage/clear_recovery.py
@src/cacheness/storage/blob_store.py
@src/cacheness/core.py
@tests/test_filesystem_containment.py
@tests/test_cache_integrity.py
@tests/test_clear_recovery.py
@tests/test_phase1_quality_gates.py

<interfaces>
- A candidate is uncommitted until the matching metadata `put_entry()` returns successfully; no pre-commit cleanup may address the prior committed locator.
- Candidate deletion succeeds only when `ManagedFileOps.delete()` returns true or a contained `exists()` check proves absence; a false/raised unresolved cleanup becomes `CacheStorageError` chained from the triggering exception.
- After metadata commit, the candidate locator is the authoritative entry. Prior-payload deletion is post-commit cleanup and cannot roll metadata back through a stale snapshot.
- UnifiedCache must instantiate the same internal clear coordinator, fixed journal name, topology identity rules, admission locks, bounds, and recovery ordering from Plan 01-14; it may supply owner/callback adapters but not a second state machine.
- UnifiedCache clear support is limited to active production-selected JsonBackend, persistent SqliteBackend, and InMemoryBackend instances it can construct through its normal configuration path. Active PostgreSQL is rejected before staging. This plan does not claim custom injected metadata-backend support or BACK-03; BlobStore may reject concrete custom backends through its own public constructor.
- Validation remains draft/pending throughout execution. The executor records measured commands against base `494d661` in `01-15-EXECUTION-EVIDENCE.md`; the orchestrator alone runs renewed review and decides whether to finalize validation.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one BlobStore replacement through a private candidate</name>
  <files>src/cacheness/storage/blob_store.py, tests/test_filesystem_containment.py</files>
  <behavior>
    - First-write metadata failure leaves no metadata, candidate, or live payload.
    - Overwrite metadata failure preserves exact old metadata and bytes and returns the old value.
    - Cross-format overwrites use handlers with different suffix/type/format metadata so stale metadata cannot accidentally decode replacement bytes.
    - Candidate cleanup false/raise becomes a chained `CacheStorageError`; it never deletes the previous locator or silently leaves residue.
    - After metadata commit, only the new candidate locator is authoritative; prior-payload cleanup failure is reported without restoring stale metadata.
  </behavior>
  <action>Write RED first-write and overwrite failure tests parameterized across at least two handlers with different suffix, data type, storage format, and serializer/compression metadata. In BlobStore.put, derive a unique backend-safe candidate from the logical key's deterministic physical base, serialize only to it, validate the produced contained locator, and build metadata pointing to that candidate. Keep previous metadata/locator/bytes untouched until `backend.put_entry()` returns. Wrap the complete pre-commit region in one ownership guard that deletes only the candidate on every exception. Require deletion success or verified absence; if cleanup cannot be proved, raise `CacheStorageError` chained from the original failure and retain both error contexts. After commit, delete the prior locator only when it differs; if that cleanup fails, keep the new metadata authoritative and raise/report a post-commit cleanup error without attempting stale rollback. Preserve public keys, return values, custom metadata merge, handler selection, and D-09..D-12 containment.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_filesystem_containment.py -k "blob_store and (candidate or metadata or overwrite)" -x</automated>
  </verify>
  <acceptance_criteria>
    - Every uncommitted BlobStore candidate is removed or an explicit chained cleanup error proves why it remains.
    - Cross-format overwrite failure preserves exact prior evidence and readability.
    - No pre-commit branch deletes or mutates the committed locator.
    - Post-commit cleanup cannot revert the authoritative metadata.
  </acceptance_criteria>
  <done>BlobStore has one production candidate-to-metadata commit path that preserves the last committed entry on every reviewed failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify UnifiedCache candidate ownership and clear recovery</name>
  <files>src/cacheness/core.py, tests/test_cache_integrity.py, tests/test_clear_recovery.py</files>
  <behavior>
    - Snapshot-open, digest exception/missing digest, signer missing/exception/empty strict output, and metadata publication failure remove the candidate and preserve exact overwrite evidence.
    - Candidate delete false/raise is explicit and chained; successful metadata commit ends candidate ownership before prior-payload cleanup.
    - UnifiedCache clear faults at every payload stage/delete position and during partial metadata clear restore exact metadata/payload state with no residue.
    - JSON, SQLite, and memory rollback restore the complete backend state, including entries and hit/miss/stat counters, rather than entries alone.
    - Committed final-delete interruption leaves the one fixed journal; matching JSON/SQLite reopen rolls forward and a second reopen is idempotent.
    - Production-selected JSON, SQLite, and memory behavior matches Plan 01-14; active PostgreSQL fails before staging, while custom injection and BACK-03 remain explicitly unsupported and incomplete.
  </behavior>
  <action>Add the complete UnifiedCache pre-commit RED matrix, including an overwrite assertion for every injected boundary. Replace branch-specific candidate deletion with one candidate locator and `metadata_committed` flag spanning guarded publication, snapshot open, digest calculation/validation, signing, entry construction, and `metadata_backend.put_entry()`. In `finally`, clean only an uncommitted candidate using the same proved-delete rule as BlobStore; after commit, clean the prior locator as a distinct post-commit step. Route `clear_all()` through Plan 01-14's coordinator with a UnifiedCache owner/callback adapter and invoke matching recovery after metadata initialization but before cleanup-on-init or normal operations. Do not duplicate journal parsing, limits, topology selection, admission locking, staging, rollback, or finalization in `core.py`. Capture and restore the complete backend state through Plan 01-14 adapters: entries plus hit/miss/stat counters, durably for JSON and transactionally for SQLite/memory. Reject before journal creation if exact snapshot/restore is unavailable. Limit support to the active production-selected JSON, persistent SQLite, and memory backends UnifiedCache can construct; reject active PostgreSQL before staging and do not add or claim custom injected-backend support. BlobStore continues to reject concrete custom backends through its own public constructor. Test every payload position, metadata clear that mutates then raises, prepared interruption, committed tombstone deletion interruption, reopen, second reopen, exact return count, exact counter restoration, the supported production-selected matrix, and PostgreSQL/custom rejection boundaries. Preserve stats reset behavior only at the acknowledged metadata-clear boundary and keep unsafe locator preflight before journal publication. Explicitly keep BACK-03 incomplete.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_cache_integrity.py tests/test_clear_recovery.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Every reviewed UnifiedCache pre-commit failure has zero unowned candidate residue or an explicit chained cleanup failure.
    - An overwrite failure returns the exact prior committed value and evidence.
    - Both clear callers share one coordinator and fixed-journal protocol.
    - UnifiedCache clear is exact before commit and convergent after matching persistent reopen.
    - No cache-policy or non-global invalidation behavior is claimed or changed.
  </acceptance_criteria>
  <done>UnifiedCache closes its candidate leak and global-clear partial-loss regressions through the same narrow storage primitive proven by BlobStore.</done>
</task>

<task type="auto">
  <name>Task 3: Record execution evidence and leave independent approval pending</name>
  <files>tests/test_phase1_quality_gates.py, .planning/phases/01-compatibility-and-security-baseline/01-15-EXECUTION-EVIDENCE.md</files>
  <action>Keep `01-VALIDATION.md` draft/pending while running the focused CR-01..CR-06 matrix, legacy/native ownership regression, full pytest, and the executable Ruff gate. Make `tests/test_phase1_quality_gates.py` invoke Ruff with JSON output, accept Ruff process return code 0 or 1 so findings are parsed rather than mistaken for command failure, assert at most 123 findings across `src tests`, and assert zero findings whose paths belong to the complete Phase 1-created Python-file inventory. A raw concise Ruff invocation may be recorded as diagnostic evidence, but is not an expected-zero completion command. Create `01-15-EXECUTION-EVIDENCE.md` with the exact base commit `494d661`, actual execution head commit, every command, exit code, measured pass/skip/finding count, CR-01..CR-06-to-test/evidence mapping, and explicit declarations that STOR-03..STOR-06, CACH-03, BACK-03, and BACK-06 remain INCOMPLETE. State in that file that executors did not author or overwrite `01-REVIEW.md` or `01-REVIEW-FIX.md`, did not restore validation approval, and that the orchestrator alone must run renewed code/security review and finalize validation after execution. If any required command fails, record the failure and leave validation draft/pending.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase1_quality_gates.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Focused CR-01..CR-06, compatibility, full-suite, and executable Ruff gates pass with recorded actual evidence.
    - `01-15-EXECUTION-EVIDENCE.md` records exact base/head commits, commands, exit codes/counts, CR mappings, and downstream-incomplete declarations.
    - Validation remains draft/pending and existing review artifacts are unchanged by the executor.
    - Quality-gate ownership includes both new Python files and every earlier Phase 1-created Python file with zero Ruff findings.
    - Review/validation/summaries explicitly keep all downstream requirements incomplete.
  </acceptance_criteria>
  <done>Current execution evidence is complete and ready for orchestrator-run review; no executor-authored approval or review artifact exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate payload to metadata authority | Serialized application bytes are uncommitted until metadata publication succeeds. |
| UnifiedCache policy to clear coordinator | Global clear must reuse storage recovery without becoming a second lifecycle implementation. |
| Implementation evidence to independent approval | Executor evidence cannot substitute for orchestrator-run review and validation finalization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-46 | Tampering / Integrity | BlobStore candidate publication | critical | mitigate | Unique candidate, exact old-entry preservation, metadata commit point, and cross-format failure tests. |
| T-01-47 | Information Disclosure / Repudiation | UnifiedCache candidate ownership | high | mitigate | One pre-commit owner/finally region and explicit chained cleanup failure. |
| T-01-48 | Tampering / Information Disclosure | UnifiedCache global clear and phase evidence | critical | mitigate | Shared coordinator, every-position reopen tests, full regression, executor evidence handoff, and orchestrator-only renewed review before approval. |
</threat_model>

## Gap Coverage Audit

| Finding | Plan task | Closure evidence |
|---------|-----------|------------------|
| CR-02 | Task 1 | Cross-format first-write/overwrite metadata-failure preservation and zero residue. |
| CR-03 | Task 2 | Snapshot/digest/sign/metadata failure matrix under one ownership guard. |
| CR-04 | Task 2 | Shared UnifiedCache clear rollback/roll-forward matrix. |
| CR-01, CR-05, CR-06 | Task 3 | Executor evidence maps the Plan 01-13/14 regression commands; the orchestrator independently determines final closure. |

## Source Audit

| SOURCE | ID | Feature/Requirement | Coverage | Status |
|--------|----|---------------------|----------|--------|
| GOAL | — | Frozen compatibility and safe boundary behavior | Tasks 1-3 plus full regression and orchestrator review handoff | COVERED |
| REQ | SECU-01 | Contained candidates and clear paths | Tasks 1-2 | COVERED |
| REQ | SECU-06 | Signed-64 boundary remains intact | Task 3 Plan 01-13 regression | COVERED |
| REQ | MIGR-01, CACH-07, SECU-02, SECU-07 | Remaining Phase 1 contract | Task 3 compatibility/full-suite review | COVERED |
| RESEARCH | — | No canonical manifest; truthful local durability/topology; validate before mutation | Plans 01-14/15 and Task 3 scope audit | COVERED |
| CONTEXT | D-01..D-08 | Public/stored compatibility envelope | Task 3 regression | COVERED |
| CONTEXT | D-09..D-12 | Contained opaque filesystem operations | Tasks 1-2 | COVERED |
| CONTEXT | D-13..D-17 | Read-only legacy arrays and native format ownership | Task 3 regression | COVERED |
| CONTEXT | D-18..D-19 | Query semantics/domain validation | Task 3 Plan 01-13 regression | COVERED |
| CONTEXT | D-20..D-21 | Independent strict SqlCache | Task 3 regression | COVERED |

## Downstream Constraints — Explicitly INCOMPLETE

| Requirement | Status after this plan | Required downstream work |
|-------------|------------------------|--------------------------|
| STOR-03, STOR-04 | INCOMPLETE | The reviewed candidate cases do not provide the Phase 3 generation/commit model for every write boundary or detectable/recoverable residue. |
| STOR-05, STOR-06 | INCOMPLETE | The clear-only coordinator is not the Phase 3 delete/overwrite/close engine or dry-run/resumable general reconciliation system. |
| CACH-03 | INCOMPLETE | Global clear alone is covered; TTL, eviction, predicate, decorator, and single-key invalidations do not yet delegate complete-entry removal through BlobStore. Phase 6 owns this. |
| BACK-06 | INCOMPLETE | Local rejection is not the Phase 4 advertised capability/topology contract. |
| BACK-03 | INCOMPLETE | This plan covers only active production-selected local UnifiedCache backends and does not establish custom metadata-backend injection/composition. |

<verification>
1. `uv run pytest -q -o log_cli=false tests/test_clear_recovery.py tests/test_filesystem_containment.py tests/test_cache_integrity.py tests/test_query_meta.py tests/test_query_meta_security.py -x`.
2. `uv run pytest -q -o log_cli=false tests/test_legacy_array_security.py tests/test_stored_compatibility.py tests/test_security_documentation.py tests/test_sql_cache.py tests/test_sql_cache_failure_contract.py -x`.
3. `uv run pytest -q -o log_cli=false`.
4. `uv run pytest -q -o log_cli=false tests/test_phase1_quality_gates.py -x`; this test invokes Ruff with JSON output, accepts Ruff rc 0/1, parses and enforces at most 123 findings across `src tests`, and requires zero findings in every Phase 1-created Python file.
5. `uv run ruff check src tests --output-format concise`; record its return code and finding count as raw diagnostic evidence only, not as an expected-zero gate.
6. `git diff --check`.
7. Confirm `01-VALIDATION.md` remains draft/pending and `01-REVIEW.md` plus `01-REVIEW-FIX.md` are unchanged. The orchestrator runs renewed review against `494d661` and finalizes validation after execution.
</verification>

<success_criteria>
- All six review blockers have direct tests and an executor evidence map ready for independent orchestrator review.
- Failed first writes/overwrites never expose replacement bytes, alter prior committed evidence, or silently leave candidates.
- BlobStore and UnifiedCache share one bounded topology-bound clear coordinator and converge after supported reopen.
- Full pytest, compatibility, Ruff, phase quality, and repository hygiene gates pass.
- Validation remains draft/pending throughout executor work; only the orchestrator may restore complete/passed after independent review.
- No summary, evidence, review, validation row, or requirements frontmatter marks STOR-03..STOR-06, CACH-03, BACK-03, or BACK-06 complete.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-and-security-baseline/01-15-SUMMARY.md`. Its `requirements-completed` may name only SECU-01. Record RED/GREEN commits and focused/full/executable-Ruff results, link `01-15-EXECUTION-EVIDENCE.md`, state that validation remains draft/pending for orchestrator review, and include an explicit `INCOMPLETE downstream handoff` for STOR-03..STOR-06, CACH-03, BACK-03, and BACK-06. State that Phase 3 must absorb the clear-only primitive and that no manifest/CAS/generation/general lifecycle engine was introduced.
</output>
