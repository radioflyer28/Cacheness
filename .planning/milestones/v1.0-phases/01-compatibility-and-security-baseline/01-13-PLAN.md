---
phase: 01-compatibility-and-security-baseline
plan: "13"
type: execute
wave: 8
depends_on:
  - 01-02
  - 01-03
  - 01-05
  - 01-07
files_modified:
  - .planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
  - src/cacheness/storage/guarded_handler_io.py
  - src/cacheness/query_validation.py
  - tests/test_filesystem_containment.py
  - tests/test_query_meta.py
  - tests/test_query_meta_security.py
  - tests/test_phase1_quality_gates.py
autonomous: true
gap_closure: true
requirements:
  - SECU-01
  - SECU-06
review_findings:
  - CR-01
  - CR-06
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Before implementation begins, Phase 1 validation is truthfully reopened as draft with approval pending, the Wave 8-10 tasks/threats are mapped, and every Python file the gap plans will create is owned by the zero-Ruff gate."
    - "Per D-09, D-11, D-12, SECU-01, and CR-01, the exact regular-file identity validated in the private handler stage is the identity opened for publication; ordinary leaf and ancestor replacements fail in descriptor and fallback modes."
    - "Per D-18, D-19, SECU-06, and CR-06, signed 64-bit endpoint integers remain valid thresholds while every Python integer outside that domain raises CacheQueryValidationError(reason=invalid_query_value) before backend or session access."
    - "Validation remains draft and approval remains pending after this plan and all executor plans; only the orchestrator may finalize it after renewed review."
  artifacts:
    - path: .planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
      provides: "Truthful reopened validation map for gap waves 8-10"
    - path: src/cacheness/storage/guarded_handler_io.py
      provides: "Stage identity descriptor/fallback binding"
    - path: src/cacheness/query_validation.py
      provides: "Pre-session signed-64 query domain validation"
    - path: tests/test_phase1_quality_gates.py
      provides: "Pending-state validation contract and complete Phase 1 Python ownership list"
  key_links:
    - from: src/cacheness/storage/guarded_handler_io.py
      to: tests/test_filesystem_containment.py
      via: "validated st_dev/st_ino/type/link-count identity is checked after the final open in both modes"
      pattern: "st_dev|st_ino|fstat"
    - from: src/cacheness/query_validation.py
      to: src/cacheness/core.py
      via: "validate_query_numeric_filters rejects outside-domain integers before query_meta selects a backend or creates a session"
      pattern: "validate_query_numeric_filters"
    - from: tests/test_phase1_quality_gates.py
      to: .planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
      via: "asserts draft/pending status and exact new task/threat mappings"
      pattern: "Approval.*pending|status: draft"
---

<objective>
Reopen Phase 1 validation and close the remaining filesystem-publication and numeric-query boundary defects.

Purpose: Correct the two Phase 1-owned review blockers before lifecycle work proceeds, while making it impossible for intermediate gap work to retain the previous completed approval.
Output: Pending validation evidence, inode-bound handler publication, signed-64 query validation, and adversarial regressions.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-REVIEW.md
@src/cacheness/storage/guarded_handler_io.py
@src/cacheness/query_validation.py
@src/cacheness/core.py
@tests/test_filesystem_containment.py
@tests/test_query_meta.py
@tests/test_query_meta_security.py
@tests/test_phase1_quality_gates.py

<interfaces>
- `GuardedHandlerIO.put()` may publish only the private staged artifact that its validation inspected; `ManagedFileOps.write_stream()` remains the managed-root publication boundary.
- `CacheQueryValidationError` with `CacheReason.INVALID_QUERY_VALUE` is the existing typed rejection and must remain the only public numeric-domain failure.
- `query_meta()` must call `validate_query_numeric_filters()` before `actual_backend`, `SessionLocal`, or SQL execution is consulted.
- The Phase 1 executable Ruff gate runs JSON output, accepts Ruff return code 0 or 1 so findings can be parsed, enforces at most 123 findings across `src tests`, and requires zero findings in every Phase 1-created Python file. Raw concise Ruff is diagnostic evidence only.
- Executors leave validation draft/pending and do not author or overwrite `01-REVIEW.md` or `01-REVIEW-FIX.md`; only the orchestrator runs renewed review and finalizes validation after Plans 01-13..01-15 execute.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reopen validation, then bind one staged artifact identity end to end</name>
  <files>.planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md, tests/test_phase1_quality_gates.py, src/cacheness/storage/guarded_handler_io.py, tests/test_filesystem_containment.py</files>
  <behavior>
    - Validation changes from complete/passed to draft/pending before production implementation and records Plans 01-13 through 01-15 plus threats T-01-38 through T-01-48 as pending.
    - The quality gate owns `src/cacheness/storage/clear_recovery.py` and `tests/test_clear_recovery.py` before those files exist and accepts only the explicit draft/pending validation state during Waves 8-9.
    - The quality gate parses Ruff JSON when Ruff returns either 0 or 1, enforces the repository cap of 123 findings, and rejects any finding in a Phase 1-created Python file.
    - A validated stage leaf replaced with another ordinary regular inode is rejected before managed publication in descriptor and forced-fallback modes.
    - A validated ancestor replaced with another ordinary directory containing a same-named regular file is rejected in both modes.
    - Existing symlink, reparse, hard-link, suffix, single-link, one-open snapshot, traversal, and outside-root evidence remains green.
  </behavior>
  <action>First edit `01-VALIDATION.md`: set frontmatter `status: draft`, `nyquist_compliant: false`, and `wave_0_complete: false`; change Approval to pending; preserve the prior completed counts as historical evidence rather than current approval; add pending Wave 8-10 rows for CR-01..CR-06 and T-01-38..T-01-48; state explicitly that STOR-03..STOR-06 and CACH-03 remain INCOMPLETE downstream requirements. Update `tests/test_phase1_quality_gates.py` in the same RED commit so its validation assertion requires this pending state, retains every existing AST/Ruff sentinel, adds the two planned Python paths to `PHASE_CREATED_PYTHON_FILES`, and rejects a stale passed approval during Waves 8-10. Implement its Ruff check by requesting JSON output, accepting subprocess return code 0 or 1, parsing the results, enforcing at most 123 findings across `src tests`, and requiring zero results whose normalized paths are in the complete Phase 1-created Python-file inventory. Treat raw concise Ruff output only as evidence. State in validation that executors do not modify the review reports or restore approval; the orchestrator alone renews review and finalizes validation after execution. Then add adversarial RED tests for ordinary leaf-inode replacement and ordinary ancestor-directory replacement, parameterized over descriptor mode when supported and the deterministic forced-fallback seam. Carry a compact validated-stage record containing the open descriptor when it can be retained, otherwise `st_dev`, `st_ino`, regular-file mode, link count, and suffix; immediately after the final descriptor-relative/fallback open, compare `fstat()` with that exact record before any byte reaches `ManagedFileOps`. Raise the existing typed path-race failure on every mismatch, close retained descriptors on all branches, and preserve the single-open snapshot/publication model per D-09, D-11, and D-12.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_filesystem_containment.py tests/test_phase1_quality_gates.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Validation and its executable gate both report draft/pending, with no current passed approval.
    - All four ordinary replacement combinations fail before managed publication and cannot substitute bytes.
    - Descriptor/fallback cleanup leaks no retained stage descriptor.
    - Existing containment and quality-gate tests remain green.
  </acceptance_criteria>
  <done>The validation record is truthfully reopened and one real handler publication path is bound to the exact validated inode in both supported proof tiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the backend-safe signed-64 query domain</name>
  <files>src/cacheness/query_validation.py, tests/test_query_meta.py, tests/test_query_meta_security.py</files>
  <behavior>
    - `-(2**63)` and `2**63 - 1` are accepted and reach SQLite as bound numeric values.
    - `-(2**63)-1`, `2**63`, and much larger positive and negative Python integers raise `CacheQueryValidationError` with reason `invalid_query_value`.
    - Every rejected value fails before backend selection, configuration checks, `SessionLocal`, or execute access at the first, middle, and last filter position.
    - Booleans remain exact-match values, finite floats retain numeric GTE behavior, and mixed endpoint int/float queries do not overflow or become `None`.
  </behavior>
  <action>Write the signed-64 endpoint and rejection matrix first. Extend `validate_query_numeric_filters()` with named signed-64 bounds and handle `bool` before `int`; keep finite-float validation unchanged. Reject outside-domain integers with the existing `_invalid_query_value` helper before any backend/session access. Use real SQLite integration tests for accepted endpoints and mixed int/float comparisons, plus spies proving each rejected mapping position leaves `actual_backend`, `SessionLocal`, and execute untouched. Preserve D-18 raw numeric GTE and string exact semantics within the declared domain and D-19 whole-request prevalidation.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_query_meta.py tests/test_query_meta_security.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Both signed-64 endpoints are accepted and bound.
    - Every outside-domain integer is a typed `invalid_query_value` before state access.
    - Boolean, finite-float, string, and nested-field compatibility behavior is unchanged.
    - No integer overflow is swallowed into a `query_meta()` miss.
  </acceptance_criteria>
  <done>The query boundary has one explicit SQLite-safe integer domain and preserves all locked comparison semantics inside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Handler private stage to managed payload | A path can be replaced after validation but before the final open. |
| Python query values to SQLite binding | Python integers exceed SQLite's bind domain unless rejected before session access. |
| Planning evidence to phase approval | A stale passed validation record could conceal unresolved security blockers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-38 | Tampering / Information Disclosure | GuardedHandlerIO stage publication | high | mitigate | Bind final open to validated descriptor or exact device/inode/type/link identity in descriptor and fallback tests. |
| T-01-39 | Denial of Service / Repudiation | query_meta integer binding | medium | mitigate | Enforce signed-64 bounds before backend/session access and preserve typed rejection. |
| T-01-40 | Repudiation | Phase validation evidence | high | mitigate | Reset approval to pending first and make the quality gate assert the current gap-wave state. |
</threat_model>

## Gap Coverage Audit

| Finding | Plan task | Closure evidence |
|---------|-----------|------------------|
| CR-01 | Task 1 | Ordinary leaf/ancestor replacement rejected in both proof tiers. |
| CR-06 | Task 2 | Signed-64 endpoints accepted; every outside integer rejected before access. |

## Plan-Set Source Audit

| SOURCE | ID | Feature/Requirement | Coverage | Status |
|--------|----|---------------------|----------|--------|
| GOAL | — | Safe boundary behavior before ownership changes | 01-13 Tasks 1-2 | COVERED |
| REQ | SECU-01 | Contained and race-resistant filesystem operations | 01-13 Task 1; 01-14/15 containment non-regression | COVERED |
| REQ | SECU-06 | Safely validated metadata queries | 01-13 Task 2 | COVERED |
| REQ | MIGR-01, CACH-07, SECU-02, SECU-07 | Remaining Phase 1 requirements | Existing Plans 01-01, 01-04, 01-06..01-12 plus Plan 01-15 full regression | COVERED |
| RESEARCH | — | Validate before state access; descriptor/fallback TOCTOU tiers | 01-13 Tasks 1-2 | COVERED |
| CONTEXT | D-01..D-08, D-13..D-17, D-20..D-21 | Compatibility, native-format ownership, and SqlCache contracts | Existing plans plus 01-15 non-regression | COVERED |
| CONTEXT | D-09..D-12 | Typed contained filesystem access | 01-13 Task 1 | COVERED |
| CONTEXT | D-18..D-19 | Numeric GTE/string exact semantics and whole-request validation | 01-13 Task 2 | COVERED |

## Downstream Constraints — Explicitly INCOMPLETE

STOR-03, STOR-04, STOR-05, STOR-06, and CACH-03 are not Phase 1 requirements and are not completed by this plan. Plans 01-14 and 01-15 close only the concrete review regressions and hand a bounded clear primitive to Phase 3. The canonical manifest, generation/CAS model, general lifecycle/reconciliation engine, all invalidation paths, and UnifiedCache-over-BlobStore ownership remain assigned to Phases 2, 3, and 6.

<verification>
1. `uv run pytest -q -o log_cli=false tests/test_filesystem_containment.py tests/test_query_meta.py tests/test_query_meta_security.py tests/test_phase1_quality_gates.py -x`.
2. `uv run pytest -q -o log_cli=false tests/test_phase1_quality_gates.py -x`; the test invokes Ruff with JSON output, accepts rc 0/1, enforces at most 123 findings across `src tests`, and requires zero findings in all Phase 1-created Python files.
3. `uv run ruff check src/cacheness/storage/guarded_handler_io.py src/cacheness/query_validation.py tests/test_filesystem_containment.py tests/test_query_meta.py tests/test_query_meta_security.py tests/test_phase1_quality_gates.py --output-format concise`; retain as raw diagnostic evidence only, not an expected-zero gate.
4. `git diff --check`.
5. Confirm `01-VALIDATION.md` remains draft/pending and names Waves 8-10; do not modify review artifacts or restore final approval in this plan.
</verification>

<success_criteria>
- CR-01 and CR-06 have direct adversarial regressions and pass.
- The exact validated stage identity reaches publication in both supported modes.
- Query integer limits are explicit and typed before state access.
- Gap-wave validation is pending and every current/planned Phase 1 Python file is quality-gate owned.
- No downstream lifecycle requirement is marked complete.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-and-security-baseline/01-13-SUMMARY.md`. Its `requirements-completed` may name only SECU-01 and SECU-06; it must state that STOR-03..STOR-06 and CACH-03 remain INCOMPLETE, that validation is still pending, and that no manifest/CAS/generation/general lifecycle engine was introduced.
</output>
