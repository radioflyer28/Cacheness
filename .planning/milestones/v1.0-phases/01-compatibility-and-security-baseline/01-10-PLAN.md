---
phase: 01-compatibility-and-security-baseline
plan: "10"
type: execute
wave: 4
depends_on: [01-09]
files_modified:
  - tests/fixtures/compat/manifest.json
  - tests/fixtures/compat/sqlite-metadata-json-v039/metadata.sqlite3
  - tests/fixtures/compat/sqlite-metadata-json-v039/payload.npz
  - tests/fixtures/compat/sqlite-metadata-json-v039/provenance.json
  - tests/fixtures/compat/decorator-key-v0313/metadata.json
  - tests/fixtures/compat/decorator-key-v0313/payload.npz
  - tests/fixtures/compat/decorator-key-v0313/provenance.json
autonomous: true
requirements: [MIGR-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02, metadata_json SQLite 0.3.9 and pre-unified decorator-key 0.3.13 variants come from exact commits with immutable provenance."
    - "SQLite schema/data_version is captured via read-only PRAGMA, and decorator provenance records exact function identity, args, kwargs, prefix, and historical candidate key."
    - "After every fixture task, the Plan 08 independent validator enforces the exact matrix/provenance schema, safe paths, per-file source/copied/current SHA-256 agreement, fixed semantics, read-only SQLite schema/data_version invariance, and pickle-disabled NPZ inspection; the accumulated manifest contains exactly six variants without historical reads or arbitrary key scanning per D-03/D-06."
  artifacts:
    - path: tests/fixtures/compat/sqlite-metadata-json-v039/metadata.sqlite3
      provides: "Legacy metadata_json SQLite row/schema"
    - path: tests/fixtures/compat/decorator-key-v0313/metadata.json
      provides: "Pre-unified decorator-key nested JSON entry"
    - path: tests/fixtures/compat/manifest.json
      provides: "Six-record accumulated fixture index"
  key_links:
    - from: tests/fixtures/compat/manifest.json
      to: tests/fixtures/compat/decorator-key-v0313/provenance.json
      via: "exact old key input/candidate digest"
      pattern: "decorator-key-v0313"
---

<objective>
Add the resolved legacy SQLite and decorator-key fixtures.

Purpose: Give exact production metadata/key adapters non-mutating schema and lookup evidence.
Output: Two fixture directories and an accumulated six-record manifest.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-09-SUMMARY.md
@tests/fixtures/compat/manifest.json
@tests/fixtures/compat/validate_corpus.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate the exact metadata_json SQLite fixture</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/sqlite-metadata-json-v039/metadata.sqlite3, tests/fixtures/compat/sqlite-metadata-json-v039/payload.npz, tests/fixtures/compat/sqlite-metadata-json-v039/provenance.json</files>
  <action>At `6e2f9933a9aa66fb6629aec0125f84a0c114c35f`, run only the historical writer with the validator-owned `np.arange(6, dtype=np.int32).reshape(2, 3)` input. Record the complete exact legacy `cache_entries` column set including `metadata_json`, source read-only `PRAGMA data_version`, environment/input, safe file list, and per-file source SHA-256; copy SQLite/NPZ, record copied SHA-256 equality, then remove the worktree. Do not open source or copied evidence through a mutating URI or historical reader per D-02/D-06. Append exactly the fifth normative manifest record with SHA-256 for SQLite, NPZ, and provenance. The Plan 08 validator must recheck the accumulated prefix, copy SQLite again for inspection, open only that inspection copy with `mode=ro`, require the entire normative schema with no current/legacy mixing, inspect NPZ with pickle disabled, and prove source/copy hashes plus schema/data_version do not change.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-metadata-json-v039</automated></verify>
  <done>The exact legacy SQLite fixture passes the independent schema, provenance, digest, safe-path, fixed-semantic, pickle-disabled, and source/copy/read-only invariance gates.</done>
</task>
<task type="auto">
  <name>Task 2: Generate the exact pre-unified decorator-key fixture</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/decorator-key-v0313/metadata.json, tests/fixtures/compat/decorator-key-v0313/payload.npz, tests/fixtures/compat/decorator-key-v0313/provenance.json</files>
  <action>At `76a469f3e090a99d9f9c119c3190a97b11a5e68a`, invoke only the writer for one validator-defined stable fixture function/call that returns the same normative numeric array. Record the exact module, qualname, positional args, kwargs, key prefix, serialized args/kwargs, old XXH3_64 result, logical expected value, environment, safe files, and equal source/copied SHA-256 values in the Plan 08 provenance schema. This recorded tuple is the only allowed production fallback per D-03/D-05. Append exactly the sixth normative manifest record with SHA-256 for every fixture file. Run the validator through this ID so all six records are rechecked; it independently recomputes that one candidate without enumeration, requires the complete current-family JSON discriminator, loads NPZ with pickle disabled, and proves the full matrix, safe paths, per-file/source-copy hashes, schema/data_version, raw frame, semantic, and pre/post invariants.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through decorator-key-v0313</automated></verify>
  <done>The old decorator entry and exact six-record prefix pass the independent one-candidate and every full-stage corpus gate.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SQLite fixture to schema inspector | Normal initialization could mutate historical evidence. |
| Decorator provenance to candidate lookup | Incomplete inputs could encourage arbitrary scans. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-31 | Tampering | SQLite evidence | high | mitigate | Read-only URI, exact PRAGMA discriminator, data-version/hash snapshots. |
| T-01-32 | Information Disclosure | decorator key | medium | mitigate | One exact recomputed candidate; no metadata enumeration. |
</threat_model>
<verification>Run all task commands and confirm only the seven declared files changed.</verification>
<success_criteria>The 0.3.9/0.3.13 D-02 variants are exact, immutable, and production-adapter ready.</success_criteria>
<output>Create `.planning/phases/01-compatibility-and-security-baseline/01-10-SUMMARY.md` when done.</output>
