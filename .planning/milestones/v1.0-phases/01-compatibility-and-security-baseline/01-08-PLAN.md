---
phase: 01-compatibility-and-security-baseline
plan: "08"
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/fixtures/compat/manifest.json
  - tests/fixtures/compat/README.md
  - tests/fixtures/compat/validate_corpus.py
  - tests/fixtures/compat/array-raw-v035-compress/payload.b2nd
  - tests/fixtures/compat/array-raw-v035-compress/provenance.json
  - tests/fixtures/compat/array-raw-v037-compress2/payload.b2nd
  - tests/fixtures/compat/array-raw-v037-compress2/provenance.json
autonomous: true
requirements: [MIGR-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Per D-02, both resolved raw-array variants are immutable bytes generated from their exact commits with fixed numeric int32 `(2, 3)` input."
    - "A reusable production-independent validator owns the exact eight-ID/commit/variant matrix and rejects incomplete provenance, unsafe relative paths, digest drift, discriminator ambiguity, semantic-input drift, executable NPZ inspection, framing/decompressed-byte disagreement, or source/copy mutation."
    - "Each variant records full commit/version/toolchain/input/file provenance plus source and copied SHA-256 before non-evaluating framing inspection."
    - "Historical writers run only in disposable detached worktrees; no historical reader, network install, or production source change is involved per D-03/D-06."
  artifacts:
    - path: tests/fixtures/compat/array-raw-v035-compress/payload.b2nd
      provides: "0.3.5 blosc2.compress raw-array frame"
    - path: tests/fixtures/compat/array-raw-v037-compress2/payload.b2nd
      provides: "0.3.7 blosc2.compress2 raw-array frame"
    - path: tests/fixtures/compat/manifest.json
      provides: "Initial two-record immutable fixture index"
    - path: tests/fixtures/compat/validate_corpus.py
      provides: "Independent staged/full corpus validator for the normative eight-fixture matrix"
  key_links:
    - from: tests/fixtures/compat/manifest.json
      to: tests/fixtures/compat/array-raw-v035-compress/provenance.json
      via: "exact commit and SHA-256 inventory"
      pattern: "array-raw-v035-compress"
    - from: tests/fixtures/compat/validate_corpus.py
      to: tests/fixtures/compat/manifest.json
      via: "exact matrix, provenance, safe-path, semantic, discriminator, digest, non-mutating inspection, and source/copy invariance checks"
      pattern: "EXPECTED_FIXTURES|validate_fixture|validate_manifest"
---

<objective>
Generate and independently pre-read the two resolved legacy raw-array fixtures.

Purpose: Give the bounded production reader exact immutable evidence without mixing fixture generation with reader code.
Output: Two raw payloads, provenance, initial manifest, corpus protocol, and an independent validator reused by every fixture task.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-01-SUMMARY.md

<interfaces>
Use IDs `array-raw-v035-compress` and `array-raw-v037-compress2` and exact commits from RESEARCH.md. Every fixture uses the normative semantic input `np.arange(6, dtype=np.int32).reshape(2, 3)`. Each `provenance.json` has exact top-level keys `schema_version`, `fixture_id`, `source_commit`, `source_version`, `generator_command`, `environment`, `logical_input`, `discriminators`, and `files`; `environment` records Cacheness/Python/NumPy/Blosc2 versions, `logical_input` records dtype/shape/values, and `files` maps each generated evidence file (payload and metadata/database, excluding the self-referential provenance document) to `source_sha256` and `copied_sha256`. `manifest.json` has exact top-level keys `schema_version` and `fixtures`; `fixtures` is an insertion-ordered object keyed by fixture ID whose records contain `source_commit`, `source_version`, `variant`, `logical_input`, `discriminators`, `files`, and `production_reader`. Each manifest `files` map includes every file in that fixture directory, including `provenance.json`, with its current `sha256`; generated evidence digests must also equal both provenance digests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the 0.3.5 compress fixture</name>
  <files>tests/fixtures/compat/validate_corpus.py, tests/fixtures/compat/manifest.json, tests/fixtures/compat/array-raw-v035-compress/payload.b2nd, tests/fixtures/compat/array-raw-v035-compress/provenance.json</files>
  <action>Create `validate_corpus.py` first as a standalone standard-library/NumPy/Blosc2 test utility that does not import Cacheness readers. Its immutable `EXPECTED_FIXTURES` table must enumerate all eight normative IDs in generation order with the exact full commits, versions, file sets, variant kinds, raw-frame API (`compress` versus `compress2`), complete split/current JSON key sets, complete legacy/current SQLite column sets, signature state, and the fixed `int32` `(2, 3)` values `[0, 1, 2, 3, 4, 5]`. Provide `--expected-through ID` for an exact accumulated manifest prefix after each fixture generation. Require the exact provenance/manifest schemas; reject absolute, traversal, drive, UNC, backslash, empty, duplicate, or non-normalized relative paths. For generated evidence, verify current SHA-256 equals both provenance source and copied SHA-256; for `provenance.json`, verify the manifest's independent SHA-256; reject any unlisted or missing fixture file. Snapshot all file digests before and after inspection. Raw inspection must parse the two four-byte little-endian lengths with bounds, exact UTF-8 tuple/dtype discriminators, decompress the remaining frame, and require decompressed bytes equal the fixed numeric input. NPZ inspection must use `numpy.load(..., allow_pickle=False)`, reject object dtype/unexpected members, and compare dtype/shape/value. JSON and SQLite checks must require their entire normative discriminator set with no mixed schema; SQLite opens only a copied temporary database using `mode=ro`, snapshots schema and `PRAGMA data_version`, and proves source/copy bytes plus observed schema/data_version unchanged. Then, in a disposable detached worktree at `041c930fb66c7aa23f53d1f9f524e9fafdd20e68`, invoke only the historical writer on the normative input to produce the older `blosc2.compress` frame. Hash the source before copy, copy the payload, hash the copy, record the equal digests and complete provenance per D-02/D-06, remove the worktree, and never invoke the historical reader. Create manifest.json with exactly this first normative record, including the independent provenance digest, before running the validator.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through array-raw-v035-compress</automated></verify>
  <done>The exact 0.3.5 payload passes the independent matrix, provenance, digest, safe-path, raw-framing, decompressed-byte, semantic-input, and source/copy invariance gates.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the 0.3.7 compress2 fixture</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/array-raw-v037-compress2/payload.b2nd, tests/fixtures/compat/array-raw-v037-compress2/provenance.json</files>
  <action>Repeat the isolated writer-only procedure at `a756d70c858cec13ff1c885e2316c0fe725c4949` for the newer `blosc2.compress2` frame with the identical validator-owned input. Record the exact provenance schema and equal source/copied digests; remove the worktree and do not read through historical code. Append exactly the second normative manifest record with its independent provenance digest, then run the reusable validator through this ID so both records are checked before considering the task complete.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through array-raw-v037-compress2</automated></verify>
  <done>The exact 0.3.7 payload independently passes the same provenance, framing, semantic, digest, safe-path, and source/copy gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Index and safely inspect both raw fixtures</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/README.md</files>
  <behavior>
    - SHA-256 verification precedes inspection.
    - Fixed four-byte lengths, UTF-8 shape/dtype grammar, and Blosc2 decompression validate without array reconstruction or evaluation.
  </behavior>
  <action>Create the corpus README and audit the two-record manifest produced incrementally by Tasks 1-2. The manifest duplicates no unchecked facts: each record must match the validator's ID/commit/version/variant/file/discriminator/semantic table and provenance digests. Document the writer-only generation rule, source/copy digest equality, safe-relative-path rule, immutable test-only index status, staged validator commands, and that 01-09 through 01-11 append the remaining records while 01-04/01-12 own production reads. Run `--expected-through array-raw-v037-compress2` so the validator requires exactly the first two normative records and repeats the full non-mutating inspection.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through array-raw-v037-compress2</automated></verify>
  <done>The exact two-record prefix passes the independent matrix, manifest/provenance, per-file digest, safe-path, framing, semantics, and source/copy invariance gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Historical checkout to fixture | Only fixed writers may produce copied bytes. |
| Raw metadata to inspector | Digest and bounded framing checks precede decompression. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Tampering | raw fixture provenance | high | mitigate | Exact commits, fixed input, full SHA-256 inventory. |
| T-01-29 | Elevation of Privilege | raw shape metadata | high | mitigate | No historical reader/evaluator; fixed bounded inspection only. |
</threat_model>

<verification>Run all task commands and confirm only the seven declared files changed.</verification>
<success_criteria>The two raw D-02 variants are exact, immutable, safely identifiable, and ready for 01-04.</success_criteria>
<output>Create `.planning/phases/01-compatibility-and-security-baseline/01-08-SUMMARY.md` when done.</output>
