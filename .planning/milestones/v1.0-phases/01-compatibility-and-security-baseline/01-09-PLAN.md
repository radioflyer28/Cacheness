---
phase: 01-compatibility-and-security-baseline
plan: "09"
type: execute
wave: 3
depends_on: [01-08]
files_modified:
  - tests/fixtures/compat/manifest.json
  - tests/fixtures/compat/json-split-unsigned-v037/metadata.json
  - tests/fixtures/compat/json-split-unsigned-v037/payload.npz
  - tests/fixtures/compat/json-split-unsigned-v037/provenance.json
  - tests/fixtures/compat/json-split-signed-v038/metadata.json
  - tests/fixtures/compat/json-split-signed-v038/payload.npz
  - tests/fixtures/compat/json-split-signed-v038/provenance.json
autonomous: true
requirements: [MIGR-01]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02, unsigned 0.3.7 and signed 0.3.8 split-map JSON/NPZ variants come from exact commits and fixed semantic input."
    - "The signed fixture uses a fixed test-only 32-byte HMAC key and records both correct-key success and wrong-key expected failure inputs."
    - "The Plan 08 independent validator enforces the exact normative matrix/provenance schema, safe relative paths, source/copy and pre/post inspection SHA-256 invariance, complete split-map discrimination, fixed semantic input, and pickle-disabled NPZ inspection after every fixture task; the manifest contains exactly four accumulated records."
  artifacts:
    - path: tests/fixtures/compat/json-split-unsigned-v037/metadata.json
      provides: "Unsigned split-map metadata"
    - path: tests/fixtures/compat/json-split-signed-v038/metadata.json
      provides: "Signed split-map metadata"
    - path: tests/fixtures/compat/manifest.json
      provides: "Four-record accumulated fixture index"
  key_links:
    - from: tests/fixtures/compat/manifest.json
      to: tests/fixtures/compat/json-split-signed-v038/provenance.json
      via: "signature-key fixture inputs and SHA-256"
      pattern: "json-split-signed-v038"
---

<objective>
Add the exact unsigned and signed split-map JSON compatibility fixtures.

Purpose: Supply production metadata/signature adapters with unambiguous historical bytes and key inputs.
Output: Two split-map fixture directories and an accumulated four-record manifest.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-08-SUMMARY.md
@tests/fixtures/compat/manifest.json
@tests/fixtures/compat/validate_corpus.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate unsigned 0.3.7 split-map JSON/NPZ</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/json-split-unsigned-v037/metadata.json, tests/fixtures/compat/json-split-unsigned-v037/payload.npz, tests/fixtures/compat/json-split-unsigned-v037/provenance.json</files>
  <action>At exact commit `a756d70c858cec13ff1c885e2316c0fe725c4949`, run only the historical writer with the validator-owned `np.arange(6, dtype=np.int32).reshape(2, 3)` input. Require the entire normative split-map top-level key set and absence of `entry_signature`; hash each source file before copy, record equal source/copied SHA-256 plus the exact Plan 08 provenance schema, remove the worktree, and never invoke a historical reader. Append exactly the next normative manifest record, including SHA-256 for metadata.json, payload.npz, and provenance.json. The validator must independently reject missing/extra split-map discriminators, unsafe paths, digest drift, object/unexpected NPZ members, semantic mismatch, or inspection mutation across the exact accumulated prefix.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through json-split-unsigned-v037</automated></verify>
  <done>The exact unsigned split-map fixture passes the independent matrix, complete-schema, provenance, digest, safe-path, pickle-disabled semantic, and source/copy invariance gates.</done>
</task>
<task type="auto">
  <name>Task 2: Generate fixed-key signed 0.3.8 split-map JSON/NPZ</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/json-split-signed-v038/metadata.json, tests/fixtures/compat/json-split-signed-v038/payload.npz, tests/fixtures/compat/json-split-signed-v038/provenance.json</files>
  <action>At exact commit `71e4ba04cbcd7dfbcf74e5651f131213b7d45ab9`, invoke only the writer with the same validator-owned data and fixed test-only 32-byte HMAC key. Require the complete split-map discriminator plus `entry_signature`, record the exact non-secret test key/reference and wrong-key expectation in the normative provenance fields, hash source and copied JSON/NPZ bytes, remove the worktree, and never call the historical verifier. Append exactly the fourth normative manifest record with SHA-256 for every fixture file. Run the validator through this ID so the two raw and two split-map records are all rechecked for the exact matrix, signed/unsigned schema, fixed semantics, safe paths, per-file/source-copy digests, pickle-disabled NPZ, raw framing/decompressed bytes, and pre/post inspection invariance. This remains a test corpus index, not a runtime canonical manifest.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through json-split-signed-v038</automated></verify>
  <done>The exact signed fixture and four-record prefix pass every independent corpus gate.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Historical signing output to test corpus | Test-key provenance must be exact without becoming a production secret. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-30 | Tampering | split-map/signature fixture | high | mitigate | Exact commits, fixed key/input, complete schema discriminator and digest verification. |
</threat_model>
<verification>Run all task commands and confirm only the seven declared files changed.</verification>
<success_criteria>Both split-map D-02 variants are exact, immutable, safely identifiable, and ready for production adapters.</success_criteria>
<output>Create `.planning/phases/01-compatibility-and-security-baseline/01-09-SUMMARY.md` when done.</output>
