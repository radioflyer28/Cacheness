---
phase: 01-compatibility-and-security-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/error_handling.py
  - src/cacheness/__init__.py
  - src/cacheness/config.py
  - tests/test_public_api_contract.py
  - tests/test_config_validation.py
autonomous: true
reversibility_gates: false
requirements:
  - MIGR-01
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-06, an executable matrix freezes supported exports, documented imports and aliases, signatures, constructors, configuration names, registries, decorators, public exceptions, reason codes, and representative results without freezing private internals or reproduced defects."
    - "For every characterized callable that accepts empty or omitted input, the matrix records the result or typed exception; required-argument callables are characterized by signature rather than invented empty-input behavior."
    - "Per D-03 and D-05, safe legacy public names remain callable through adapters and deprecation warnings throughout the milestone, while intentionally unsafe inputs may fail immediately with typed reasons."
    - "Per D-04, historically supported optional top-level names import even when YAML, SQLAlchemy, or pandas is unavailable, and dependency failure occurs only on construction or use with actionable guidance."
    - "Per D-07, JSON and YAML round trips preserve the exact user-authored relative cache path; runtime path resolution does not mutate serialized configuration."
    - "Per D-08, public error categories and CacheReason string values are stable machine-readable contracts while prose remains free to improve."
  prohibitions:
    - statement: "The compatibility baseline must not elevate known defects, private internals, or accidental quirks into supported contracts."
      status: resolved
      verification: judgment
  artifacts:
    - path: src/cacheness/error_handling.py
      provides: "CacheReason plus typed path, query, and legacy-format boundary exceptions"
      contains: "class CacheReason"
    - path: tests/test_public_api_contract.py
      provides: "Executable public-surface, optional-import, alias, signature, decorator, registry, result, and reason-code matrix"
    - path: tests/test_config_validation.py
      provides: "Exact authored-path JSON/YAML round-trip coverage"
  key_links:
    - from: tests/test_public_api_contract.py
      to: src/cacheness/__init__.py
      via: "__all__, star-import, aliases, signatures, and dependency-blocked subprocesses"
      pattern: "__all__|SQLAlchemyDataAdapter|CacheReason"
    - from: src/cacheness/config.py
      to: tests/test_config_validation.py
      via: "authored relative-path JSON/YAML round trips"
      pattern: "cache_dir"
---

<objective>
Freeze the corrected public and configuration compatibility envelope before storage ownership changes.

Purpose: Give every later Phase 1 plan stable typed failures, public aliases, optional-import behavior, and authored configuration semantics without coupling this prompt to the historical fixture corpus.
Output: Stable error/reason contracts, corrected aliases and optional imports, exact authored-path round trips, and an executable public API matrix.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-PATTERNS.md
@src/cacheness/__init__.py
@src/cacheness/error_handling.py
@src/cacheness/config.py
@tests/test_config_validation.py
@tests/test_error_handling.py

<interfaces>
Preserve CacheError(message, context) and cause chaining. The package currently defines SQLAlchemyPullThroughCache and SQLAlchemySqlCacheAdapter but lists the missing SQLAlchemyDataAdapter name in __all__; the corrected baseline defines both aliases. CacheStorageConfig.cache_dir remains a serialized string and runtime boundaries resolve it separately.
</interfaces>
</context>

## Reversibility Gate Override

This autonomous planning cycle selected `--no-reversibility-gates`. Per D-05, the one-way rating remains auditable on Task 1, but no human checkpoint is inserted.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the corrected public surface and typed reason contract</name>
  <reversibility rating="one-way">Per D-05, shortening the milestone-long compatibility-adapter window would break a published promise; gates are suppressed for this autonomous run but the rating remains recorded.</reversibility>
  <files>src/cacheness/error_handling.py, src/cacheness/__init__.py, tests/test_public_api_contract.py</files>
  <read_first>
    - `src/cacheness/__init__.py`: current imports, conditional exports, aliases, `__all__`, and version.
    - `src/cacheness/error_handling.py`: CacheError context/cause conventions and public subclass hierarchy.
    - `tests/test_error_handling.py`: specific-exception and context assertions.
  </read_first>
  <behavior>
    - Star import succeeds and exposes every guaranteed `__all__` name, including both SQLAlchemy adapter aliases per D-01, D-03, and D-06.
    - Blocking YAML, SQLAlchemy, or pandas imports in isolated subprocesses does not remove historically supported top-level names; first use raises actionable dependency guidance per D-04.
    - Public constructors, decorators, registries, result shapes, documented imports, and aliases retain supported signatures and behavior.
    - CacheReason values and the public exception inheritance matrix are exact and machine-readable per D-08.
  </behavior>
  <action>Create `tests/test_public_api_contract.py` first and make it red against the current broken star import and missing reason surface. Extend CacheError without changing its constructor: add `CacheReason(str, Enum)` values covering path traversal/absolute/drive/UNC/rooted/outside-root/race, invalid identifiers, invalid query fields, invalid legacy arrays, unsafe object arrays, unsupported legacy layouts, read-only legacy stores, invalid legacy signatures, SqlCache fetch/gap/upsert failure, and missing optional dependencies. Add `CacheUnsafePathError(CacheStorageError)`, `CacheQueryValidationError(CacheMetadataError)`, and `CacheLegacyFormatError(CacheSerializationError)`; expose the selected stable reason through exception context. Correct `SQLAlchemyDataAdapter = SqlCacheAdapter` while retaining `SQLAlchemySqlCacheAdapter`; keep optional names importable and defer missing-dependency checks to construction/use through the existing actionable dependency-error surfaces. Build the matrix from package exports, documented examples, signatures, aliases, registries, decorators, existing exception classes, selected result shapes, and corrected intended behavior per D-01/D-06. Record D-03/D-05 adapters and deprecations as milestone-long contracts. Do not add dependencies or freeze private names.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_public_api_contract.py tests/test_error_handling.py -x</automated>
  </verify>
  <done>The public surface matrix is green, corrected aliases work, optional names remain importable, and stable typed reason contracts are available to downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve authored configuration values across serialization</name>
  <reversibility rating="costly">Per D-07, changing the stored authored-path representation later affects configuration compatibility.</reversibility>
  <files>src/cacheness/config.py, tests/test_config_validation.py</files>
  <read_first>
    - `src/cacheness/config.py`: CacheStorageConfig.__post_init__ and JSON/YAML load/save functions.
    - `tests/test_config_validation.py`: nested config round-trip and optional YAML patterns.
  </read_first>
  <behavior>
    - `./cache`, `./yaml_cache`, `relative/cache`, and parent-relative authored paths round-trip exactly through JSON and YAML per D-07.
    - Validation still rejects invalid size/configuration values without converting relative paths to absolute strings.
    - Runtime consumers can resolve the authored path independently without mutating the config object.
  </behavior>
  <action>Add the exact-path tests before changing configuration. Remove eager absolutization from `CacheStorageConfig.__post_init__`, retain validation there, and leave runtime `Path.resolve(strict=False)` to the storage boundaries planned in 01-02 and 01-03. Preserve nested dataclass serialization, backward-compatible flat keywords, and lazy YAML dependency behavior.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_config_validation.py -x</automated>
  </verify>
  <done>Authored relative configuration values survive JSON/YAML round trips exactly and remain separately resolvable at runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package consumer to public facade | Imports, aliases, signatures, optional dependency use, and exception contracts are caller-visible. |
| Serialized config to runtime filesystem | Authored relative strings are data; absolute runtime resolution occurs only at the storage boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Elevation of Privilege | optional serializer imports | medium | mitigate | Dependency-blocked subprocess tests prove importability without invoking executable serializers; actual use raises actionable dependency errors. |
| T-01-10 | Repudiation | public exception/reason contract | medium | mitigate | Freeze CacheReason strings and inheritance so caller branching and monitoring remain attributable. |
| T-01-11 | Tampering | relative configuration paths | medium | mitigate | Preserve authored strings and separate runtime resolution, preventing config load/save from silently retargeting storage. |
</threat_model>

<verification>
Run `uv run pytest -q -o log_cli=false tests/test_public_api_contract.py tests/test_config_validation.py tests/test_error_handling.py -x` and Ruff on the five modified files. New files must be Ruff-clean and touched existing files must add no findings.
</verification>

<success_criteria>
- MIGR-01 public-surface characterization covers every public category named by D-01 and D-03 through D-08.
- Optional symbols, authored relative paths, exception categories, and reason codes are stable and machine-testable.
- Historical stored formats remain assigned to 01-08 through 01-12; no canonical manifest, lifecycle ownership, or migration workflow is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-and-security-baseline/01-01-SUMMARY.md` when done.
</output>
