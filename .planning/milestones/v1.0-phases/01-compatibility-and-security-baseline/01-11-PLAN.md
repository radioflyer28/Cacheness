---
phase: 01-compatibility-and-security-baseline
plan: "11"
type: execute
wave: 5
depends_on: [01-10]
files_modified:
  - tests/fixtures/compat/manifest.json
  - tests/fixtures/compat/json-nested-v0314/metadata.json
  - tests/fixtures/compat/json-nested-v0314/payload.npz
  - tests/fixtures/compat/json-nested-v0314/provenance.json
  - tests/fixtures/compat/sqlite-columns-v0314/metadata.sqlite3
  - tests/fixtures/compat/sqlite-columns-v0314/payload.npz
  - tests/fixtures/compat/sqlite-columns-v0314/provenance.json
autonomous: true
requirements: [MIGR-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02, current 0.3.14 nested JSON and denormalized SQLite variants are generated from the final audited source commit."
    - "The completed manifest passes the Plan 08 independent validator with exactly all eight normative IDs, commits/versions, provenance fields, safe relative paths, fixed semantics, complete format discriminators, and per-file source/copied/current SHA-256 agreement."
    - "After every fixture task, production-independent JSON/SQLite/NPZ pre-read inspection proves source/copy and pre/post hash/schema/data-version invariance and establishes the control group for legacy adapters."
  artifacts:
    - path: tests/fixtures/compat/json-nested-v0314/metadata.json
      provides: "Current nested JSON entry"
    - path: tests/fixtures/compat/sqlite-columns-v0314/metadata.sqlite3
      provides: "Current denormalized SQLite entry"
    - path: tests/fixtures/compat/manifest.json
      provides: "Completed eight-variant fixture index"
  key_links:
    - from: tests/fixtures/compat/manifest.json
      to: tests/fixtures/compat/sqlite-columns-v0314/provenance.json
      via: "current schema inventory and SHA-256"
      pattern: "sqlite-columns-v0314"
---

<objective>
Complete the fixture corpus with current JSON and SQLite controls.

Purpose: Finish an exact eight-variant baseline that distinguishes compatibility adapters from current normal readers.
Output: Two current fixture directories and the complete manifest.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-10-SUMMARY.md
@tests/fixtures/compat/manifest.json
@tests/fixtures/compat/validate_corpus.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate current nested JSON control</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/json-nested-v0314/metadata.json, tests/fixtures/compat/json-nested-v0314/payload.npz, tests/fixtures/compat/json-nested-v0314/provenance.json</files>
  <action>At final audited commit `a22f4b4575cb8213d9783ed388d2a70727563db1`, generate the validator-owned `np.arange(6, dtype=np.int32).reshape(2, 3)` entry through the current JSON writer. Require exactly the normative nested top-level discriminator and current unified key; record semantics, environment, safe files, and equal source/copied SHA-256 values in the Plan 08 provenance schema. Append exactly the seventh normative manifest record with SHA-256 for JSON, NPZ, and provenance. The independent validator must recheck the accumulated prefix and require the complete current JSON shape, pickle-disabled NPZ values, exact commit/version, safe paths, per-file/source-copy digests, fixed semantics, and pre/post inspection invariance.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through json-nested-v0314</automated></verify>
  <done>The current nested JSON control passes the independent matrix, complete-schema, provenance, digest, safe-path, semantic, and invariance gates.</done>
</task>
<task type="auto">
  <name>Task 2: Generate current denormalized SQLite control</name>
  <files>tests/fixtures/compat/manifest.json, tests/fixtures/compat/sqlite-columns-v0314/metadata.sqlite3, tests/fixtures/compat/sqlite-columns-v0314/payload.npz, tests/fixtures/compat/sqlite-columns-v0314/provenance.json</files>
  <action>At the same final commit, generate the current SQLite entry with the same validator-owned input. Record the entire normative denormalized column set and absence of `metadata_json`, source read-only data_version, semantics, environment, safe files, and equal source/copied SHA-256 values. Append exactly the eighth normative manifest record, including SHA-256 for SQLite, NPZ, and provenance. Run the Plan 08 validator over the complete set: two raw, two split-map, legacy SQLite, decorator key, current JSON, and current SQLite. The manifest must equal the full normative ID order and exact commit/variant/file matrix, include semantic equality and production-adapter expectations per record, and agree with every provenance source/copied/current digest. This test index is not a runtime canonical manifest. The validator must inspect only a temporary copied database through a read-only URI and re-run all raw framing/decompressed-byte, complete JSON/SQLite discriminator, SQLite schema/data_version, pickle-disabled NPZ, safe-path, one-candidate decorator, fixed-semantic, and source/copy/pre-post invariance checks.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314</automated></verify>
  <done>The current SQLite control and exact eight-variant corpus pass the reusable independent full-matrix validator and are immutable and safely pre-readable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Current fixture controls to legacy comparison | Wrong control provenance could hide adapter regressions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-33 | Repudiation | current control provenance | high | mitigate | Final audited commit, exact schema/key/semantic inventory, SHA-256 and read-only invariance. |
</threat_model>
<verification>Run all task commands and confirm only the seven declared files changed.</verification>
<success_criteria>All eight D-02 variants are exact, immutable, semantically specified, and ready for 01-12.</success_criteria>
<output>Create `.planning/phases/01-compatibility-and-security-baseline/01-11-SUMMARY.md` when done.</output>
