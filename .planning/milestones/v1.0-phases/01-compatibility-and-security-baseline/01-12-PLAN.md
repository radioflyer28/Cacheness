---
phase: 01-compatibility-and-security-baseline
plan: "12"
type: execute
wave: 6
depends_on: [01-03, 01-04, 01-05, 01-11]
files_modified:
  - src/cacheness/metadata.py
  - src/cacheness/security.py
  - src/cacheness/decorators.py
  - src/cacheness/core.py
  - tests/test_stored_compatibility.py
autonomous: true
reversibility_gates: false
requirements: [MIGR-01]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-02/D-03, exact split-map JSON and metadata_json SQLite schemas normalize read-only through production MetadataBackend adapters to the current entry shape, warn, and never guess unknown layouts."
    - "Signed 0.3.8 verifies through one exact production legacy-signature candidate with the fixture key and fails typed with the wrong key without evidence deletion."
    - "Pre-unified 0.3.13 decorator data is found only by one documented old candidate after current-key miss; a legacy hit warns and no arbitrary entry scan occurs per D-05/D-06."
    - "All eight fixtures first pass Plan 08's independent full-corpus validator, are copied, consumed through production adapters/readers, compared semantically, and proven unchanged; raw variants use 01-04's bounded reader."
    - "Legacy metadata adapters never persist bookkeeping: direct mutating operations fail typed, while a successful production UnifiedCache legacy read suppresses only the post-success `legacy_read_only` bookkeeping outcome and records process-local hit/access information where the exact adapter supports it."
    - "Default `cleanup_on_init=True` does not block exact legacy JSON/SQLite construction: only typed `legacy_read_only` startup cleanup on a recognized adapter is suppressed with deprecation, unrelated failures propagate, and subsequent public reads leave evidence invariant."
    - "Any non-`legacy_read_only` reason, untyped backend failure, or `legacy_read_only` raised before successful payload reconstruction remains caller-visible; current JSON/SQLite access-time and hit persistence remains normal."
  artifacts:
    - path: src/cacheness/metadata.py
      provides: "Exact split-map JSON and metadata_json SQLite read-only normalizers"
    - path: src/cacheness/security.py
      provides: "Exact 0.3.8 six-field signature verification candidate"
    - path: src/cacheness/decorators.py
      provides: "Exact 0.3.13 candidate-key fallback and deprecation signal"
    - path: src/cacheness/core.py
      provides: "Exact legacy-signature dispatch and typed non-mutating failure propagation"
    - path: tests/test_stored_compatibility.py
      provides: "Eight-variant production-adapter semantic/invariance suite"
  key_links:
    - from: tests/test_stored_compatibility.py
      to: src/cacheness/metadata.py
      via: "production JsonBackend/SqliteBackend exact layout normalization"
      pattern: "JsonBackend|SqliteBackend"
    - from: src/cacheness/security.py
      to: tests/fixtures/compat/json-split-signed-v038/metadata.json
      via: "constant-time exact historical six-field signature candidate"
      pattern: "cache_key|file_hash|data_type|file_size|created_at|prefix"
    - from: src/cacheness/decorators.py
      to: tests/fixtures/compat/decorator-key-v0313/provenance.json
      via: "one documented args/kwargs candidate after current miss"
      pattern: "serialize_for_cache_key|xxh3_64"
    - from: src/cacheness/core.py
      to: src/cacheness/security.py
      via: "normalized compatibility variant selects exact legacy verification before handler access"
      pattern: "verify_legacy_entry"
    - from: src/cacheness/core.py
      to: src/cacheness/metadata.py
      via: "post-success bookkeeping suppresses only typed legacy_read_only and optionally delegates to non-persistent legacy counters"
      pattern: "legacy_read_only|record_legacy_read"
---

<objective>
Make every resolved historical fixture readable through exact production compatibility adapters.

Purpose: Turn immutable evidence into real stored-data compatibility while keeping adapters read-only, deprecated, and outside later lifecycle/migration work.
Output: Production metadata, signature, and decorator-key adapters plus an eight-variant semantic/invariance suite.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-PATTERNS.md
@.planning/phases/01-compatibility-and-security-baseline/01-04-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-03-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-11-SUMMARY.md
@src/cacheness/metadata.py
@src/cacheness/security.py
@src/cacheness/decorators.py
@src/cacheness/core.py
@tests/fixtures/compat/manifest.json
@tests/fixtures/compat/validate_corpus.py
@tests/test_config_validation.py
@tests/test_cross_system_compatibility.py

<interfaces>
Metadata backends return the current entry dictionary shape. CacheEntrySigner verifies the current contract first, then one exact 0.3.8 candidate over sorted `cache_key`, `file_hash`, `data_type`, `file_size`, `created_at`, and `prefix`. Decorator lookup computes the current key first, then one exact 0.3.13 candidate from function identity, serialized args/kwargs, and optional prefix.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Normalize exact historical JSON and SQLite layouts read-only</name>
  <reversibility rating="costly">Per D-02/D-05, these exact readers become the milestone compatibility window and later migration input.</reversibility>
  <files>src/cacheness/metadata.py, src/cacheness/core.py, tests/test_stored_compatibility.py</files>
  <read_first>
    - `.planning/phases/01-compatibility-and-security-baseline/01-PATTERNS.md`: mapped `tests/test_stored_compatibility.py` analogs, typed-error conventions, temporary filesystem integration fixtures, and immutable compatibility evidence rules.
    - `tests/test_config_validation.py`: `tmp_path`/temporary-directory save-and-reload round-trip pattern; copy fixtures before exercising production adapters and compare authored/evidence state after the read.
    - `tests/test_cross_system_compatibility.py`: real `UnifiedCache`/decorator round-trip integration pattern with shared configuration and explicit close/cleanup; use the real public flow instead of calling only private normalizers.
    - `src/cacheness/metadata.py`: backend initialization, create_all timing, cleanup_expired, exact JSON/SQLite read and mutation methods.
    - `src/cacheness/core.py`: default `cleanup_on_init=True`, `_cleanup_expired`, get ordering, and post-success bookkeeping.
    - `tests/fixtures/compat/validate_corpus.py`: pre-read full-corpus validation and source/copy/schema/data-version invariance contract.
  </read_first>
  <behavior>
    - Complete 0.3.7/0.3.8 split maps normalize counters, timestamps, params, nested metadata, and optional signature.
    - Existing metadata_json SQLite is detected before create_all through read-only PRAGMA and read with exact SQL/JSON only.
    - Current nested JSON/denormalized SQLite remain normal; partial/mixed layouts fail unsupported_legacy_layout.
    - Legacy put/remove/cleanup/hit/miss/access-time operations fail legacy_read_only before writes and do not alter hashes/schema/data_version.
    - Exact legacy backend instances may expose `record_legacy_read(cache_key)` and `legacy_compat_hits`/`legacy_compat_access_times` as process-local state excluded from JSON/SQLite persistence; current backends do not use this compatibility channel.
    - Real legacy JSON and metadata_json SQLite stores construct under the public default `cleanup_on_init=True`: Core suppresses only typed `legacy_read_only` from startup expiry cleanup for an exact recognized legacy backend, emits DeprecationWarning, and propagates every other typed reason or untyped cleanup error.
    - After default construction, real UnifiedCache reads return the normative value; post-success legacy bookkeeping is process-local only, and fixture copies plus source hashes/JSON bytes/SQLite rows/schema/data_version remain invariant.
  </behavior>
  <action>Run `uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314` before manifest-driven failing tests so production adapter work cannot consume a shallow or drifted corpus. Follow the mapped analogs from `01-PATTERNS.md`: use `tests/test_config_validation.py`'s temporary save/load round-trip discipline and `tests/test_cross_system_compatibility.py`'s real public `UnifiedCache` flow, while copying every fixture to a temporary location and snapshotting both source and copy before construction/read. In metadata.py, add private exact-schema detectors/normalizers, not migration machinery. JsonBackend recognizes only the complete resolved split-map key set, normalizes requested entries to the current shape, preserves optional entry_signature, emits DeprecationWarning, and makes the detected legacy instance read-only. SqliteBackend inspects pre-existing `cache_entries` before `Base.metadata.create_all`; exact metadata_json columns select a read-only compatibility query that parses JSON only. Unknown/mixed schemas fail `unsupported_legacy_layout`; every direct persistence path (put/remove/clear/expiry/size/hit/miss/access-time) raises CacheLegacyFormatError reason `legacy_read_only` before changing files, rows, schema, data_version, or counters. For exact legacy instances only, add `record_legacy_read(cache_key)` that updates process-local `legacy_compat_hits` and `legacy_compat_access_times` and is excluded from serialized JSON/SQLite state; make CachedMetadataBackend delegate this optional compatibility method without populating or invalidating persistent/wrapped evidence. In Core startup, retain `cleanup_on_init=True` and call the normal cleanup path, but catch CacheLegacyFormatError only when its reason is exactly `legacy_read_only` and the selected backend carries the exact recognized legacy-layout discriminator; emit DeprecationWarning and continue without mutation. Do not catch `unsupported_legacy_layout`, another CacheLegacyFormatError reason, OSError, SQLAlchemy error, or untyped backend failure. Extract the post-success `_record_successful_read(cache_key, entry)` seam here: after Plan 03's verified snapshot handler reconstruction succeeds, normal backends persist access/hits; exact legacy backends may suppress only typed `legacy_read_only` and delegate to `record_legacy_read`. Add real unsigned legacy JSON and metadata_json SQLite tests using default construction (do not pass `cleanup_on_init=False`), successful public `UnifiedCache.get`, and explicit close/cleanup. Snapshot and compare source/copy SHA-256, JSON bytes, SQLite rows/schema/`PRAGMA data_version`, and process-local compatibility counters before/after; inject other typed reasons and untyped cleanup errors to prove they propagate. Empty/new/current databases preserve existing behavior and continue persisting ordinary cleanup/access/hit bookkeeping. Never infer releases, rewrite bytes/schema, or add canonical manifests per D-02/D-03/D-06.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run pytest -q -o log_cli=false tests/test_stored_compatibility.py -k 'split_map or metadata_json or current_backend or default_cleanup' -x</automated></verify>
  <done>Exact legacy JSON/SQLite stores construct with default cleanup enabled, read through real UnifiedCache without mutation, and suppress only typed recognized read-only cleanup/bookkeeping outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact signature/decorator adapters and prove all eight production reads</name>
  <reversibility rating="one-way">Per D-05, these adapters remain for the full milestone; the autonomous gate override retains the rating without re-asking the locked choice.</reversibility>
  <files>src/cacheness/security.py, src/cacheness/decorators.py, src/cacheness/core.py, tests/test_stored_compatibility.py</files>
  <read_first>
    - `.planning/phases/01-compatibility-and-security-baseline/01-PATTERNS.md`: stored-compatibility round-trip analog, real public-flow integration, typed errors, and temporary-copy invariance conventions.
    - `tests/test_config_validation.py`: temporary save/load round-trip assertions used as the shape for copied compatibility evidence.
    - `tests/test_cross_system_compatibility.py`: real decorator-to-UnifiedCache interoperability flow and explicit resource cleanup.
    - `src/cacheness/storage/guarded_handler_io.py`: Plan 03 context-owned snapshot API and single managed-open guarantee.
    - `src/cacheness/core.py`: Plan 03 snapshot digest/current-signature-before-handler helper and Task 1 startup/bookkeeping seam.
    - `src/cacheness/security.py`: current signer payload and constant-time comparison contract.
    - `src/cacheness/decorators.py`: current key generation and cache lookup order.
  </read_first>
  <behavior>
    - Current signature verification remains first; only exact historical six-field verification is second, constant-time, and deprecated.
    - Wrong key produces typed invalid_legacy_signature without deletion/miss conversion.
    - Current and exact legacy signature verification run against metadata/digest for the same still-live Plan 03 private snapshot before handler invocation; a call-order spy sees no handler event on wrong-key legacy data and no second managed-locator open.
    - Decorated same-call lookup tries current key then exactly one old candidate; legacy hit warns, miss scans nothing.
    - All eight records pass the independent validator and production semantic reads while retaining source/copy hashes plus SQLite schema/data_version.
    - A successful legacy UnifiedCache.get returns data even when post-success access-time/hit persistence raises typed legacy_read_only, then records adapter-supported ephemeral bookkeeping without changing fixture evidence.
    - A different typed reason, ordinary backend exception, or legacy_read_only raised before payload success propagates; only the exact post-success bookkeeping branch is suppressible.
  </behavior>
  <action>Follow the mapped round-trip/public-flow analogs from `01-PATTERNS.md`, `tests/test_config_validation.py`, and `tests/test_cross_system_compatibility.py`: copy fixtures to temporary locations, use real `UnifiedCache` and decorated-function entry points, close resources, and compare source/copy bytes and SQLite observations after every read. In security.py add one exact historical payload helper using sorted six fields with the old empty/datetime/string normalization and `hmac.compare_digest`; current verification stays unchanged for current entries. Have Task 1's exact signed split-map normalizer attach an internal compatibility-layout discriminator, and in core.py dispatch only that discriminator to `verify_legacy_entry` inside Plan 03's already-open snapshot helper, after digest comparison and before `handler.get(snapshot.path, snapshot.metadata)`. Do not reopen `actual_path`, create another snapshot, or verify after deserialization. Legacy success warns; wrong-key raises CacheLegacyFormatError reason `invalid_legacy_signature` ahead of broad miss/cleanup catches so metadata, counters, payload, and snapshot call order remain unchanged. Add ordered spies for snapshot copy, digest, current-first verification, legacy verification, and handler access; right-key legacy data reaches the handler once on the same live snapshot, while wrong-key data omits the handler event and performs no second managed-locator open regardless of `delete_invalid_signatures`. Use Task 1's `_record_successful_read` only after reconstruction and all verification succeeds; injected-backend tests prove reason selectivity, current backend persistence, and propagation of `unsupported_legacy_layout`, OSError, other typed reasons, and pre-success `legacy_read_only`. In decorators.py add `_legacy_v0313_cache_key` reproducing exact serialize_for_cache_key(args/kwargs), function identity, optional prefix, and XXH3_64; wrapper lookup tries it only after current miss and warns only on hit. Complete tests by running the independent full-corpus validator before production consumption, then route raw fixtures through 01-04 ArrayHandler, metadata fixtures through production backends/UnifiedCache, signed data through the production signer, and decorator data through a real decorated function. Compare type/value/dtype/shape/content and metadata semantics; prove right-key success, wrong-key typed failure before handler access, no scans, warnings, default-cleanup/bookkeeping selectivity, and complete invariance per D-02/D-03/D-05/D-06.</action>
  <verify><automated>uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run pytest -q -o log_cli=false tests/test_stored_compatibility.py tests/test_legacy_array_security.py tests/test_public_api_contract.py -x</automated></verify>
  <done>All eight fixtures use production readers/adapters; the exact legacy signature is verified on the same live snapshot before handler access, successful legacy reads tolerate only typed recognized read-only startup/bookkeeping outcomes, and no legacy evidence is mutated.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Legacy schema to current model | Permissive normalization could guess or mutate the only copy. |
| Historical signature/key candidate to current lookup | Broad candidates could accept invalid integrity or scan unrelated entries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Tampering | legacy metadata normalizers/bookkeeping | high | mitigate | Exact full-schema detection, JSON-only parsing, typed mutation rejection, post-success reason-selective suppression, process-local compatible counters, and source/schema/data-version invariance tests. |
| T-01-35 | Spoofing / Tampering | legacy signature | high | mitigate | Current-first, one exact six-field constant-time candidate over the Plan 03 live snapshot metadata/digest before handler access; wrong-key call-order spy proves no deserialization or second managed open. |
| T-01-36 | Information Disclosure | decorator fallback | medium | mitigate | One exact same-call candidate after miss; no enumeration. |
</threat_model>

<verification>Run the independent full-corpus validator followed by the combined pytest command. All eight manifest records must name a production adapter/reader; real UnifiedCache legacy JSON/SQLite stores must construct with default cleanup and read successfully without source/copy/schema/data-version mutation; selectivity tests must show only typed recognized startup/post-success legacy_read_only outcomes are suppressed; legacy signature ordering must be snapshot-copy → digest → current/legacy verification → handler with the failure branch omitting handler and any second managed open; and all five files_modified must equal the union of task files.</verification>
<success_criteria>MIGR-01 and D-02/D-03/D-05/D-06 are production-backed for all eight variants without Phase 2 manifest, Phase 3 lifecycle, or Phase 7 migration scope.</success_criteria>
<output>Create `.planning/phases/01-compatibility-and-security-baseline/01-12-SUMMARY.md` when done.</output>
