---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "02"
type: execute
wave: 1
depends_on: [07-01]
files_modified: []
autonomous: false
requirements: [MIGR-04, MIGR-05, MIGR-06]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The developer explicitly reconfirms the already locked immediately-previous-release support contract before it becomes a published migration promise (MIGR-04; D-02)."
    - "The developer explicitly reconfirms that exact exclusions may omit the only reconstructable form before rebuild exclusion implementation begins (MIGR-06; D-08)."
    - "The developer explicitly reconfirms that finalize permanently ends rollback and that purge can destroy the retained physical prior copy before those actions are implemented (MIGR-05; D-15)."
  artifacts: []
  key_links:
    - from: 07-02-PLAN.md
      to: 07-03-PLAN.md
      via: "D-02 approval gates the published compatibility window"
      pattern: "D-02"
    - from: 07-02-PLAN.md
      to: 07-08-PLAN.md
      via: "D-15 approval gates finalize/purge semantics"
      pattern: "D-15"
    - from: 07-02-PLAN.md
      to: 07-09-PLAN.md
      via: "D-08 approval gates rebuild exclusions"
      pattern: "D-08"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] The checkpoints may not reopen or weaken D-02, D-08, or D-15 by silently selecting an alternative implementation contract."
    - "[FLAGGED/UNVERIFIED] No irreversible contract/action may be implemented before its explicit resume signal is recorded."
---

<objective>
Stop before the three one-way doors and obtain explicit confirmation of the locked release-window, rebuild-exclusion, and retirement contracts.

Purpose: Preserve the reversibility signal for published migration promises and destructive operator choices.
Output: Three recorded decisions that gate Plans 03, 08, and 09; no code or persisted data changes.
</objective>

## Artifacts this phase produces

- No new symbols, fields, or repository files. This plan records three blocking decision outcomes in execution history and its `07-02-SUMMARY.md`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-01-SUMMARY.md
</context>

<planner_assumptions>
The implementation decisions are already locked. These gates satisfy the explicit Phase 7 reversibility instruction: the only proceed option is the locked contract; the alternative stops execution for deliberate replanning rather than inventing a new choice.
</planner_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the published previous-release migration promise</name>
  <reversibility rating="one-way">Once published, narrowing direct support below the immediately previous released layout breaks the migration contract applications may rely on.</reversibility>
  <files>None (decision gate only)</files>
  <read_first>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>Present D-02 exactly: each release supports its current and immediately previous released layout directly, while older released stores advance through successive declared steps. State that Phase 7 creates the first baseline and does not add historical development-layout readers or manufacture a target version.</action>
  <decision>Confirm the D-02 release-window contract before Plan 03 publishes it.</decision>
  <context>Undoing this after release would narrow a public migration promise and strand stores that relied on the direct previous-release step.</context>
  <options>
    <option id="proceed"><name>Proceed with locked D-02</name><pros>Publishes the discussed bounded support promise.</pros><cons>Future releases must retain the immediately previous direct edge.</cons></option>
    <option id="stop"><name>Stop for replanning</name><pros>Avoids publishing a contract the developer no longer accepts.</pros><cons>Phase 7 cannot continue until CONTEXT.md and downstream coverage are deliberately revised.</cons></option>
  </options>
  <resume-signal>Reply `proceed D-02` to continue, or `stop D-02` to replan.</resume-signal>
  <acceptance_criteria>The execution record contains an explicit D-02 proceed response; any other response stops before Plan 03.</acceptance_criteria>
  <verify><automated>test -f .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</automated></verify>
  <done>D-02 confirmation is recorded without changing code or persisted formats.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Confirm exact rebuild exclusion semantics</name>
  <reversibility rating="one-way">A confirmed exclusion can omit the only reconstructable representation of data from the rebuilt destination.</reversibility>
  <files>None (decision gate only)</files>
  <read_first>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-01-SUMMARY.md</file>
  </read_first>
  <action>Present D-08 exactly: rebuild excludes nothing by default; any exclusion requires a newly generated plan naming exact keys or explicit categories, counts, bytes, reasons, and a matching confirmation. Clarify that cross-backend/incompatible work is rebuild, not universal physical migration.</action>
  <decision>Confirm the D-08 exclusion contract before Plan 09 implements rebuild.</decision>
  <context>Confirmation is the operator's deliberate acceptance that named source data will not appear in the rebuilt destination.</context>
  <options>
    <option id="proceed"><name>Proceed with locked D-08</name><pros>Keeps destructive omissions explicit, exact, and reviewable.</pros><cons>Operators must regenerate and reconfirm a plan whenever exclusions change.</cons></option>
    <option id="stop"><name>Stop for replanning</name><pros>Prevents implementation of an exclusion model no longer accepted.</pros><cons>MIGR-06 remains unplanned until the decision is replaced explicitly.</cons></option>
  </options>
  <resume-signal>Reply `proceed D-08` to continue, or `stop D-08` to replan.</resume-signal>
  <acceptance_criteria>The execution record contains an explicit D-08 proceed response; otherwise Plan 09 remains blocked.</acceptance_criteria>
  <verify><automated>test -f .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</automated></verify>
  <done>D-08 confirmation is recorded without selecting exclusions or mutating a source.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Confirm finalize and purge as separate one-way actions</name>
  <reversibility rating="one-way">Finalize permanently ends supported rollback, and later purge may destroy the only retained physical prior copy.</reversibility>
  <files>None (decision gate only)</files>
  <read_first>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-01-SUMMARY.md</file>
  </read_first>
  <action>Present D-15 with D-14 and D-16: activation retains the prior valid store; finalize records operator acceptance and only ends rollback; purge is later, separately confirmed, idempotent physical cleanup whose failure becomes retryable debt and never rewrites activation success.</action>
  <decision>Confirm the D-15 finalize/purge contract before Plan 08 implements it.</decision>
  <context>These actions deliberately cross the rollback and physical-retention boundaries and cannot be inferred from time, candidate presence, or ordinary cleanup.</context>
  <options>
    <option id="proceed"><name>Proceed with locked D-15</name><pros>Preserves a clear acceptance boundary and separately retryable cleanup.</pros><cons>Rollback is unavailable after finalize; purged bytes may be unrecoverable.</cons></option>
    <option id="stop"><name>Stop for replanning</name><pros>Prevents crossing an unaccepted destructive boundary.</pros><cons>MIGR-05 retirement semantics remain incomplete.</cons></option>
  </options>
  <resume-signal>Reply `proceed D-15` to continue, or `stop D-15` to replan.</resume-signal>
  <acceptance_criteria>The execution record contains an explicit D-15 proceed response; otherwise Plan 08 remains blocked.</acceptance_criteria>
  <verify><automated>test -f docs/adr/0001-topology-specific-storage-guarantees.md</automated></verify>
  <done>D-15 confirmation is recorded with finalize and purge kept distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| locked decisions -> irreversible implementation | Human confirmation must be explicit before public or destructive semantics land. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-06 | Repudiation | D-02/D-08/D-15 gates | high | mitigate | Blocking-human checkpoints require exact resume signals and record them in the summary before dependent plans execute. |
| T-07-07 | Tampering | checkpoint options | medium | mitigate | Options either preserve the locked decision or stop; no auto-selected alternate contract exists. |
</threat_model>

<verification>
No code test can replace the human one-way decision. Automated checks only prove the locked source artifacts are present; the executor must stop until each exact resume signal is supplied.
</verification>

<success_criteria>
All three proceed signals are recorded before Plans 03, 08, and 09 execute, or the phase stops cleanly without partial implementation of the affected contracts.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-02-SUMMARY.md` with the exact decisions and resume signals.
</output>
