---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 15
type: execute
wave: 11
depends_on:
  - 07-14
files_modified:
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/migration.py
  - tests/test_rebuild_workflow.py
autonomous: true
requirements:
  - MIGR-05
  - MIGR-06
gap_closure: true
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "For each entry in an explicitly entry/byte/evidence-bounded rebuild run, rebuild deterministically re-derives the same operation ID and invokes Plan 07-14's projection-suppressed canonical maintenance put; after canonical commit-response loss the existing lifecycle authority replays that exact operation and the rebuild checkpoints its identical projection-free BlobReceipt before later source work (MIGR-05, MIGR-06; D-11, D-17 through D-20)."
    - "A projection-equipped destination performs zero derived work during staging, replay, verification, or canonical acceptance; projection rebuild is invoked explicitly only after REBUILD_ACCEPTED and a response-loss retry cannot duplicate it (D-11)."
    - "Resume handles REBUILDING, REBUILD_STAGED, REBUILD_VERIFYING, and REBUILD_VERIFIED by authenticating evidence and revalidating exact destination receipts rather than discovering or adopting destination state (MIGR-05; D-19, D-20)."
    - "Crash or verification failure cleans only destination entries still matching this run's exact receipts; conflicts and operational failures remain durable retryable debt while source data and accepted output remain untouched (MIGR-05, MIGR-06; D-08, D-16, D-21, D-22)."
  artifacts:
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Bounded authenticated exact rebuild receipt batches, retirement progress, and cleanup debt"
    - path: src/cacheness/storage/migration.py
      provides: "Projection-suppressed canonical rebuild staging, receipt-driven every-state resume, verification, acceptance, explicit post-acceptance projection rebuild, and failure cleanup"
    - path: tests/test_rebuild_workflow.py
      provides: "Post-commit/pre-checkpoint response-loss, rebuild-state resume, and receipt-bound cleanup tests"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "each operation ID is deterministically re-derived from bounded plan identity, then its exact canonical receipt is checkpointed before staging continues"
      pattern: "rebuild_receipts|REBUILDING|operation_id"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/blob_store.py
      via: "the evidence-bound operation ID is passed through Plan 07-14's canonical maintenance-put seam so retry consults canonical lifecycle authority and never auto-runs projections"
      pattern: "operation_id|_put_entry_canonical_for_maintenance"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/blob_store.py
      via: "resume and cleanup compare exact BlobReceipt key, generation, locator, and expectation before read or delete"
      pattern: "BlobReceipt|get_entry_info|delete"
---

<objective>
Close the interrupted-rebuild attribution, resume, and cleanup blocker on top of the canonical replay seam from Plan 07-14.

Purpose: Ensure a bounded rebuild recovers from every durable state using the existing lifecycle authority and authenticated exact destination receipts, without adopting or deleting unrelated entries.
Output: Deterministically re-derived canonical operation IDs, bounded projection-free canonical receipt batches, every-state resume, explicit post-acceptance projection rebuild, receipt-bound cleanup/debt, and crash-boundary tests.
</objective>

## Artifacts this phase produces

- Bounded `RebuildReceiptBatch(batch_ordinal, first_entry_ordinal, past_last_entry_ordinal, receipts, batch_digest, byte_count)` in existing authenticated maintenance evidence; no pre-publication maintenance intent, journal, or new lifecycle state is added.
- `MaintenanceRunEvidence.rebuild_receipt_batches` and `MaintenanceRunEvidence.retired_rebuild_operation_ids`; existing `cleanup_debt` stores canonical exact rebuild operation/key/generation/locator debt records, all within explicit per-run entry/byte/evidence limits. Larger inputs are split before mutation into independently recoverable runs.
- Receipt-driven `stage_rebuild`, `verify_rebuild`, `resume`, and failure cleanup through Plan 07-14's projection-suppressed canonical operation replay; existing `rebuild_projection` remains the only derived path and is callable only after explicit canonical acceptance.
- Exact tests named `test_rebuild_response_loss_rederives_operation_id_and_replays_authority_receipt`, `test_projection_equipped_rebuild_replays_canonical_receipt_without_preacceptance_or_duplicate_derived_work`, `test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state`, and `test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/handler-integration.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-09-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-14-SUMMARY.md
@src/cacheness/storage/migration_evidence.py
@src/cacheness/storage/migration.py
@src/cacheness/storage/blob_store.py
@tests/test_rebuild_workflow.py
</context>

<interfaces>
Plan 07-14 adds `BlobStore._put_entry_canonical_for_maintenance(..., operation_id=...)` and exact projection-free canonical lifecycle replay by wrapping the same `_put_with_result_admitted` engine as ordinary writes. The existing lifecycle authority records canonical `MutationSpec` state before immutable publication. `MaintenanceRunEvidence` currently has rebuild states but no receipt collection. `OfflineMigrationService.stage_rebuild()` retains receipts only in a local list, currently calls projection-running `put_entry()`, `verify_rebuild()` passes an empty receipt tuple on failure, and `resume()` has no rebuild-state branches.
</interfaces>

<planner_assumptions>
- MIGR-05 requires authenticated durable attribution through the existing lifecycle authority for canonical destination writes and through bounded receipt evidence for rebuild progress; incidental destination keys never prove progress.
- MIGR-06 keeps rebuild separate from physical migration: reads use authenticated source handlers, writes use destination BlobStore lifecycle, and evidence never becomes destination authority.
- Destination cleanup is an external retryable effect. It preserves exact conflicting/current entries and the complete source rather than claiming cross-resource rollback.
- The response-loss repair uses Plan 07-14's single canonical destination lifecycle authority, which records and replays the exact `MutationSpec`. The rebuild operation ID is a deterministic function of authenticated bounded run/plan/entry identity and is re-derived on retry; Phase 7 adds no maintenance pre-publication intent, journal, state, or adoption path.
- Rebuilds share Plan 07-12's explicit entry, byte, and evidence limits. Larger inputs are split into independently recoverable maintenance runs rather than one unbounded receipt collection.
- D-11 is enforced by using only Plan 07-14's internal projection-suppressed maintenance wrapper during canonical rebuild staging. Verification and `accept_rebuild` do not run projections; after acceptance, the existing `rebuild_projection(plan, controller)` is the sole explicit derived operation and remains unable to authorize or revoke canonical publication.
- The validated obstore spike fixes the handler boundary regardless of payload participant: source and destination handlers keep `CacheHandler.put(data, Path, config)` / `CacheHandler.get(Path, metadata)`, store-local custom registration, private guarded staging, native suffix checks, descriptor identity, containment, and synchronous snapshot lifetime. This plan does not install or expose obstore.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Re-derive one rebuild operation and checkpoint its canonical replay receipt</name>
  <reversibility rating="costly">Persisted rebuild receipt fields require coordinated evidence decoder and resume changes in later releases.</reversibility>
  <files>src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_rebuild_workflow.py</files>
  <read_first>
    - src/cacheness/storage/migration_evidence.py: authenticated MaintenanceRunEvidence schema, bounds, canonical encoding, and legal rebuild transitions
    - src/cacheness/storage/migration.py: stage_rebuild, evidence writes, response-loss handling, and destination put calls
    - src/cacheness/storage/blob_store.py: Plan 07-14 deterministic operation-ID put and canonical receipt replay contract
    - tests/test_rebuild_workflow.py: registered-handler rebuild, integrity-failure, and batch-failure fixtures
    - .codex/skills/spike-findings-cacheness/references/handler-integration.md: path-based custom-handler and private staging/snapshot boundary
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: D-17 through D-20 and D-22
  </read_first>
  <behavior>
    - "Before each destination put, stage_rebuild deterministically derives the operation ID from authenticated bounded run, plan, entry ordinal, source digest, destination identity/revision, and destination key; no separate maintenance intent is persisted."
    - "A process-loss fault at rebuild.destination_committed_before_receipt_checkpoint leaves the existing lifecycle-authority operation but no rebuild receipt; resume re-derives the same operation ID, receives the identical projection-free canonical receipt, checkpoints it, and creates no second generation."
    - "With a configured projection sink, staging plus post-commit replay through the maintenance wrapper returns equal receipts whose projections mapping is empty and records zero projection attempts before REBUILD_ACCEPTED; one explicit rebuild_projection call after acceptance produces exactly one derived attempt."
    - "A catchable response error retries that same deterministic operation through lifecycle authority before cleanup; bounded rebuild evidence contains completed receipt ownership identifiers and digests but no source-value or signing-material sentinel."
  </behavior>
  <action>Write `test_rebuild_response_loss_rederives_operation_id_and_replays_authority_receipt` and `test_projection_equipped_rebuild_replays_canonical_receipt_without_preacceptance_or_duplicate_derived_work` first. Add only bounded canonical `RebuildReceiptBatch(batch_ordinal, first_entry_ordinal, past_last_entry_ordinal, receipts, batch_digest, byte_count)` to existing `MaintenanceRunEvidence.rebuild_receipt_batches`; do not add `RebuildWriteIntent`, `pending_rebuild_intent`, a journal, or any new lifecycle state. Validate explicit per-run entry/byte/evidence limits before mutation and return the same deterministic independently recoverable split-required contract as Plan 07-12 for larger inputs. Derive each operation ID deterministically from authenticated run ID, plan digest, destination identity/revision, exact entry ordinal/key, source manifest digest, and target contract digest. Invoke `destination._put_entry_canonical_for_maintenance(..., operation_id=operation_id)` directly; the existing lifecycle engine records its canonical `MutationSpec` before immutable publication and ordinary `put_entry()` plus `_run_post_commit_projections` remain unused. Expose `rebuild.destination_committed_before_receipt_checkpoint` immediately after the canonical maintenance call returns and before bounded receipt evidence is replaced; after success append that exact projection-free `BlobReceipt` before reading another source entry. Keep handler access on the existing path-based `CacheHandler` API within `GuardedHandlerIO` private snapshots/stages. Use process-loss-style termination in the boundary test. On resume, authenticate the bounded plan/entry identity, re-derive the same operation ID, call the canonical maintenance path, accept only exact authority replay from Plan 07-14, require empty projection outcomes, and checkpoint the recovered receipt. A catchable response error retries that operation through lifecycle authority or leaves REBUILDING with a typed retryable failure; it never authorizes listing, entry adoption, or unrelated receipt cleanup. In the projection-equipped test, prove staging, response-loss replay, verification, and acceptance leave the real projection controller at zero attempts, then call existing `OfflineMigrationService.rebuild_projection()` once after REBUILD_ACCEPTED and prove exactly one derived attempt whose outcome cannot change the accepted receipt. Preserve D-11 and D-17 through D-22 without new coordination, lifecycle authority, pre-publication maintenance intent, obstore, or S3 policy change.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_response_loss_rederives_operation_id_and_replays_authority_receipt tests/test_rebuild_workflow.py::test_projection_equipped_rebuild_replays_canonical_receipt_without_preacceptance_or_duplicate_derived_work -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named fault tests prove process-loss and catchable response-error windows recover by deterministic operation-ID re-derivation plus existing lifecycle-authority replay, checkpoint the identical projection-free receipt, create no duplicate generation, add no maintenance pre-publication intent, perform zero pre-acceptance projection attempts, perform exactly one explicit post-acceptance derived attempt, enforce bounded-run splitting, and persist no payload or signing secret.</acceptance_criteria>
  <done>Each bounded rebuild write is exactly replayable from the existing lifecycle authority without a second intent layer, and derived work begins only through the explicit post-acceptance boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume every rebuild state and clean only exact receipt ownership</name>
  <files>src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_rebuild_workflow.py</files>
  <read_first>
    - src/cacheness/storage/migration_evidence.py: bounded rebuild receipts, cleanup outcomes, and state transition validation from Task 1
    - src/cacheness/storage/migration.py: _discard_rebuild_receipts, _abort_rebuild_after_failure, verify_rebuild, accept_rebuild, and resume
    - src/cacheness/storage/blob_store.py: exact BlobReceipt identity and guarded get/delete surfaces
    - tests/test_rebuild_workflow.py: response-loss tracer plus existing failure-cleanup coverage
    - docs/adr/0001-topology-specific-storage-guarantees.md: external cleanup and single-authority stop conditions
    - .codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md: exact immutable identity and cleanup-debt recovery boundary
  </read_first>
  <behavior>
    - "After each bounded batch, evidence contains exact BlobReceipt ownership and resume covers REBUILDING, REBUILD_STAGED, REBUILD_VERIFYING, and REBUILD_VERIFIED."
    - "Resume revalidates every recorded receipt and re-derives the deterministic lifecycle-authority operation only for the next bounded plan entry lacking a completed receipt."
    - "Failure cleanup deletes only an exact key/generation/locator/expectation match, treats absence as retired, preserves changed ownership, checkpoints each outcome, and retains operational failure as retryable debt."
  </behavior>
  <action>Write `test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state` and `test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt` first. Revalidate every bounded receipt in `rebuild_receipt_batches` against destination authority and continue from the first planned entry lacking a completed receipt by re-deriving its deterministic operation ID. Route REBUILDING, REBUILD_STAGED, REBUILD_VERIFYING, and REBUILD_VERIFIED through their legal next steps. Replace local-list cleanup with evidence-owned receipts: prove exact key, generation, locator, and expectation before delete; treat exact absence as retired and append its operation ID to bounded unique `retired_rebuild_operation_ids`; preserve changed ownership; checkpoint each attributed retirement or conflict; and encode each exact operational failure in existing bounded `cleanup_debt` as `rebuild:<operation_id>:<key>:<generation>:<locator>`. Evidence may reach ABORTED only when every recorded receipt's operation ID is retired or represented by durable debt. Preserve D-08, D-16 through D-21, and D-22: do not scan destination listings, infer/adopt an entry without existing lifecycle-authority replay, delete source or accepted output, or add a coordinator, queue, lease, lock, journal, or lifecycle state.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state tests/test_rebuild_workflow.py::test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt tests/test_rebuild_workflow.py::test_rebuild_discards_only_its_run_owned_destination_entries_after_batch_failure -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The state test covers all four resumable rebuild states, and cleanup tests prove exact retirement/debt handling without duplicate writes, ownership adoption, unrelated deletion, or source damage.</acceptance_criteria>
  <done>Every interrupted rebuild converges through authenticated receipts, a legal resume path, or explicit receipt-bound cleanup debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| deterministic bounded rebuild entry -> canonical destination authority | The same operation ID is re-derived on retry; only its exact existing lifecycle-authority mutation may satisfy response-loss recovery. |
| authenticated rebuild receipts -> destination BlobStore | Replayed or forged receipts must not authorize completion or cleanup. |
| source handler -> destination lifecycle | Trusted application payloads cross handlers only after source authenticity and integrity checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-15-01 | Tampering | Deterministic operation replay, canonical receipts, and projection boundary | high | mitigate | Re-derive the exact operation ID from authenticated bounded run/plan/destination/entry identity, replay only through existing lifecycle authority, bind the projection-free canonical BlobReceipt to ordinal/batch, and permit derived work only through explicit post-acceptance rebuild. |
| T-07-15-02 | Spoofing | Cleanup ownership | high | mitigate | Compare exact key, generation, locator, and expectation before delete; preserve mismatches as debt. |
| T-07-15-03 | Repudiation | Verification failure | medium | mitigate | Record receipt-backed cleanup outcome and remaining debt before terminal evidence state. |
| T-07-15-04 | Information Disclosure | Rebuild evidence | medium | mitigate | Persist ownership identifiers and digests only and retain signing material behind the configured provider. |
</threat_model>

<verification>
Run the four named rebuild tests plus the complete rebuild and migration-evidence suites. Verify safety, exact response-loss receipt recovery, zero pre-acceptance projections, one explicit post-acceptance derived attempt, every-state resume, bounded checkpoints, exact cleanup ownership, and the ADR authority boundary separately.
</verification>

<success_criteria>
- Every destination write uses a deterministically re-derived operation ID recorded by the existing lifecycle authority, and every completed bounded rebuild batch has durable exact receipts; no maintenance pre-publication intent is added.
- A crash after canonical destination commit but before receipt checkpoint replays the same authority operation and returns the identical projection-free canonical receipt without another generation.
- Projection-equipped destinations perform no derived work during staging/replay/verification/acceptance and run it only through one explicit post-acceptance rebuild call.
- Every rebuild state has an authenticated, deterministic resume path.
- Verification and crash cleanup act only on matching run-owned entries and persist partial debt.
- Source data, accepted output, and unrelated destination generations remain intact.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Plan(s) |
|---|---|---|---|
| GOAL | Explicit stopped-worker rebuild without silent mutation or loss | COVERED | 07-14, 07-15 |
| REQ | MIGR-05 authenticated interruption recovery | COVERED | 07-14, 07-15 |
| REQ | MIGR-06 explicit confirmed rebuild integrity | COVERED | 07-15 |
| RESEARCH | Projection-suppressed canonical authority replay plus bounded authenticated rebuild evidence | COVERED | 07-14, 07-15 |
| CONTEXT | D-08, D-11, and D-16 through D-22 rebuild attribution, derived-only projection boundary, resume, cleanup, determinism, and secrecy | COVERED | 07-14, 07-15 |
| CONTEXT | Deferred ideas | EXCLUDED | Not planned, as required |

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-15-SUMMARY.md` when done.
</output>
