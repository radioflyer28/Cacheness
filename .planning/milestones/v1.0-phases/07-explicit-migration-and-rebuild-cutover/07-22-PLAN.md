---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 22
type: execute
wave: 18
depends_on:
  - 07-21
files_modified:
  - tools/verify_phase7_contracts.py
  - tests/test_phase7_contract_verifier.py
  - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
  - .planning/phases/07-explicit-migration-and-rebuild-cutover/deferred-items.md
autonomous: true
requirements:
  - MIGR-03
  - MIGR-04
  - MIGR-05
  - MIGR-06
gap_closure: true
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The fixed Phase 7 verifier maps each of the three current blockers and every new Plan 20-22 threat to exact AST-validated pytest selectors; removing a new behavior cannot leave `--all` green (MIGR-04, MIGR-05; D-18, D-20)."
    - "The exact gap-threat oracle expands from 38 to 50 rows and the complete Phase 7 threat inventory from 84 to 96 unique IDs, with Plans 07-20 through 07-22 included in fixed plan-path ownership checks."
    - "Quick/all evidence records same-version destination transformation, typed S3 abort debt/retry and truthful counts, and rebuild cleanup settlement after participant restoration; requirement PASS is withheld if any exact selector fails or skips outside its declared quick-mode boundary."
    - "The validation ledger preserves the existing MIGR-03/MIGR-06 evidence, corrects the superseded false-green Phase 7 claim with a new dated gap-cycle section, and keeps live services, Windows, Python matrix, performance, perfect invisible-orphan reclamation, and production obstore adoption as explicit non-claims."
    - "The unrelated custom-handler `name=` alias warning is recorded in deferred-items.md for a future API-contract milestone; this gap cycle neither silently omits it nor changes handler registration semantics (WR-02)."
  artifacts:
    - path: tools/verify_phase7_contracts.py
      provides: "Exact current-gap requirement, decision, assumption, prohibition, plan, and 96-threat mappings"
    - path: tests/test_phase7_contract_verifier.py
      provides: "Self-tests proving all three new behavior selectors and 12 new threat rows cannot be dropped"
    - path: .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
      provides: "Truthful dated quick/all evidence for the current three-blocker closure"
    - path: .planning/phases/07-explicit-migration-and-rebuild-cutover/deferred-items.md
      provides: "Explicit future disposition of the unrelated handler registration alias warning"
  key_links:
    - from: tools/verify_phase7_contracts.py
      to: tests/test_migration_cutover.py
      via: "MIGR-04 and D-03/D-10 map the exact same-version/different-format selector"
      pattern: "test_same_version_different_format_uses_exact_directed_transform_and_destination_manifest"
    - from: tools/verify_phase7_contracts.py
      to: tests/test_migration_remote_contract.py
      via: "MIGR-05 and Plan 20 threat rows map the exact typed S3 abort debt/retry selector"
      pattern: "test_s3_abort_typed_operational_failures_checkpoint_exact_debt_and_retry"
    - from: tools/verify_phase7_contracts.py
      to: tests/test_rebuild_workflow.py
      via: "MIGR-05 and Plan 21 threat rows map exact nonterminal-debt settlement and forged/changed-ownership selectors"
      pattern: "test_rebuild_cleanup_debt_stays_resumable_until_exact_settlement|test_rebuild_cleanup_retry"
    - from: tools/verify_phase7_contracts.py
      to: .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
      via: "zero quick/all exit and exact observed selector/threat counts become the current completion evidence"
      pattern: "MIGR-04|MIGR-05|96|PASS"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No ordinary constructor, open, initialize, read, cache-policy, reconciliation, or cleanup path may perform an implicit migration or upgrade."
    - "[FLAGGED/UNVERIFIED] No work-directory file, filesystem path, object listing, candidate presence, projection, lock, queue, lease, journal, sidecar, or process-local gate may become a second lifecycle authority."
    - "[FLAGGED/UNVERIFIED] No unauthenticated evidence, unexplained candidate, mismatched receipt, or incomplete catalog may be adopted."
    - "[FLAGGED/UNVERIFIED] No coordinator-owned universal payload conversion, handler stream cutover, managed-locator exposure, or production obstore adoption may be introduced."
    - "[FLAGGED/UNVERIFIED] No plan or test may claim one ACID transaction across metadata authority and filesystem/S3 effects, exact reclamation of invisible pre-checkpoint orphans, or identical progress guarantees across topologies."
---

<objective>
Bind the three repaired behaviors to the fixed Phase 7 evidence contract and record truthful final validation.

Purpose: Prevent another false-green completion report and preserve explicit disposition of the unrelated handler-alias warning.
Output: Exact verifier mappings through Plan 22, a 96-threat oracle, adversarial self-tests, final quick/all evidence, and a future-work record for WR-02.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/deferred-items.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-18-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-19-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-20-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-21-SUMMARY.md
@.codex/skills/spike-findings-cacheness/SKILL.md
@tools/verify_phase7_contracts.py
@tests/test_phase7_contract_verifier.py
</context>

<adr_0001_contract>
- Supported evidence boundary: deterministic local memory/SQLite/filesystem plus Moto/fake S3 participant coverage; live PostgreSQL/Amazon-S3, Windows, supported-Python, and performance qualification stay in Phase 8.
- Integrity evidence is distinct from recovery evidence: exact target-manifest/ownership failures fail closed, while typed operational failures produce bounded debt and explicit retry outcomes.
- The selected metadata lifecycle authority remains the only visibility/state owner. The verifier and validation ledger are evidence, not runtime authority.
- External blob interruption is represented by authenticated exact debt and settled by deletion or absence proof; no cross-resource ACID claim is made.
- Progress evidence accepts terminal success, bounded nonterminal retry, and typed fail-closed outcomes. Performance remains outside correctness selectors.
</adr_0001_contract>

<flagged_assumptions>
- `[FLAGGED/UNVERIFIED A-MIGR03]` Existing non-mutating inventory and canonical human/machine plans remain the sufficient Phase 7 interpretation and must stay exactly mapped.
- `[FLAGGED/UNVERIFIED A-MIGR04]` Exact destination format identity includes both name and numeric version, and operational abort recovery is part of copy-verify-switch completion.
- `[FLAGGED/UNVERIFIED A-MIGR05]` Existing authenticated evidence plus the singular lifecycle authority can settle every attributed migration/rebuild cleanup obligation without another intent source.
- `[FLAGGED/UNVERIFIED A-MIGR06]` Explicit rebuild remains registered-handler and destination-BlobStore work; no universal conversion or obstore adoption is required by the current gaps.
</flagged_assumptions>

<assumption_delta>
Decision: no-change. The singular lifecycle authority remains the primary noun; negative prose about a second or another authority does not introduce a generalized authority abstraction.
</assumption_delta>

<interfaces>
`tools/verify_phase7_contracts.py` owns literal production/test/plan inventories, `MIGRATION_REQUIREMENT_NODES`, `DECISION_NODES`, `SECURITY_THREAT_NODES`, `FLAGGED_ASSUMPTION_NODES`, descriptor-less `PLAN01_PROHIBITIONS`, and a plan-threat ownership oracle. Before this plan it contains 19 plan paths, 38 gap threats, and 84 total threats. `fixed_pytest_nodes()` validates exact `path::test_name` symbols before execution. `07-VALIDATION.md` is appendable evidence and must not erase historical observations; `07-COVERAGE.md` remains the no-external-API declaration.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the fixed verifier fail if any current gap behavior disappears</name>
  <files>tools/verify_phase7_contracts.py, tests/test_phase7_contract_verifier.py</files>
  <read_first>
    - tools/verify_phase7_contracts.py: reviewed inventories, exact claim maps, gap-plan threat owners, AST selector validation, quick/all execution, and result rendering
    - tests/test_phase7_contract_verifier.py: exact manifest, threat ownership, removed/renamed selector, subprocess forwarding, and false-PASS self-tests
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-20-PLAN.md: exact four new threat rows and required transform/S3/accounting selectors
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-21-PLAN.md: exact four new threat rows and required rebuild-settlement selectors
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-22-PLAN.md: exact four verifier/evidence threat rows
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md: three authoritative current blockers
  </read_first>
  <behavior>
    - "`PHASE7_PLAN_PATHS` contains Plans 01 through 22, `GAP_PLAN_THREAT_IDS` contains the prior 38 plus exactly 12 Plan 20-22 IDs, and the declared inventory contains exactly 96 unique IDs."
    - "MIGR-04 maps the exact same-version/different-format transform and typed S3 abort selectors; MIGR-05 maps S3 debt/retry, accurate abort accounting, rebuild cleanup settlement, terminal-evidence rejection, and forged/changed-ownership refusal selectors."
    - "D-03/D-10, D-16/D-19/D-21, A-MIGR04/A-MIGR05, and the descriptor-less prohibitions include exact new evidence appropriate to their claims."
    - "Deleting or renaming any one new test, removing one Plan 20-22 threat row, or removing one new threat mapping fails validation before pytest and names the exact missing claim."
  </behavior>
  <action>Write `test_fixed_manifest_maps_current_three_gap_repairs_exactly` first and extend the existing exact-manifest/threat-ownership mutation tests. Add Plan 07-20 through 07-22 paths to `PHASE7_PLAN_PATHS`. Extend `GAP_PLAN_THREAT_IDS` and `GAP_PLAN_THREAT_OWNERS` with exactly `T-07-20-01` through `T-07-20-04`, `T-07-21-01` through `T-07-21-04`, and `T-07-22-01` through `T-07-22-04`; require 50 unique gap rows and 96 unique total Phase 7 rows. Map each new threat to the exact Task 1/2 selector that proves its mitigation. Extend MIGR-04, MIGR-05, D-03, D-10, D-16, D-19, D-20, D-21, A-MIGR04, A-MIGR05, and the existing descriptor-less prohibition mappings with the new same-version target, S3 typed-debt/retry/accounting, rebuild-settlement, terminal-evidence-rejection, and forgery/changed-ownership selectors. Preserve MIGR-03/MIGR-06 mappings and exact four flagged-assumption keys. Update self-tests that assert counts, plan ownership, exact selector forwarding, missing/renamed functions, and missing/duplicate/unexpected threat rows. Do not derive the new rows from plan discovery or make passing unrelated tests satisfy a claim. Preserve the quick-mode optional PostgreSQL boundary and the unchanged 07-COVERAGE authority.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase7_contract_verifier.py::test_fixed_manifest_maps_current_three_gap_repairs_exactly tests/test_phase7_contract_verifier.py::test_fixed_manifest_requires_exact_path_and_test_name_selectors tests/test_phase7_contract_verifier.py::test_fixed_manifest_includes_every_gap_plan_threat_exactly_once tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_removed_mapped_test_function_while_file_remains tests/test_phase7_contract_verifier.py::test_main_never_renders_failed_requirement_as_pass -x -o log_cli=false -o addopts=</automated>
  </verify>
  <acceptance_criteria>The verifier owns Plans 01-22, exactly 50 gap threats and 96 total threats, exact selectors for every current blocker, and fail-closed mutations for every new plan/threat/test omission.</acceptance_criteria>
  <done>The fixed evidence contract cannot pass while any of the three current gap repairs is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Run the exact gates and record current evidence plus WR-02 disposition</name>
  <files>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md, .planning/phases/07-explicit-migration-and-rebuild-cutover/deferred-items.md</files>
  <read_first>
    - tools/verify_phase7_contracts.py: quick/all command contract and exact selector/threat totals from Task 1
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md: historical execution evidence and current misleading PASS rows that need a dated superseding section
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md: three blockers, corrected WR-01 accounting, and unrelated WR-02 alias warning
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md: authoritative current three-gap statement
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/deferred-items.md: existing future-work format
    - docs/adr/0001-topology-specific-storage-guarantees.md: topology and claim boundaries
  </read_first>
  <action>Run `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --quick` first, then the same isolated frozen all-extras verifier with `--all`. If either fails, record the exact command, exit status, and failure without marking any blocked requirement complete. On zero exit, append a dated current-gap section to 07-VALIDATION.md that supersedes only the stale current completion disposition: record exact selector counts observed, 50 gap/96 total threat ownership, same-version format transformation evidence, typed S3 snapshot/delete failure debt plus attributed retry, truthful partial `deleted_entries`, nonterminal rebuild debt plus restored-participant convergence, forged/changed-ownership refusal, scoped Ruff, and the full deterministic non-live suite. Preserve MIGR-03/MIGR-06 evidence and record MIGR-04/MIGR-05 PASS only when every mapped selector executes successfully. Explicitly retain the accepted invisible/unattributed pre-checkpoint orphan, no cross-resource ACID, deterministic-not-live S3/PostgreSQL, Phase 8 service/platform/Python/performance ownership, unchanged 07-COVERAGE declaration, and no production obstore claim. Append WR-02 to deferred-items.md as a future public handler-registration API-contract item: decide whether `name=` is removed or made a real persisted alias, with duplicate-`data_type` behavior and lookup/list/unregister tests. State that it is unrelated to the three verified Phase 7 migration gaps and is not fixed or claimed by this cycle. Record WR-01 as closed only by the Plan 20 count tests.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --all</automated>
  </verify>
  <acceptance_criteria>Quick and all evidence is recorded truthfully; all three blocker rows and 96 threat mappings pass exactly or remain visibly incomplete; Phase 8 and ADR limitations stay explicit; WR-01 is backed by exact accounting tests; and WR-02 is explicitly deferred without a false completion claim.</acceptance_criteria>
  <done>The current Phase 7 gap closure has reproducible exact evidence and every review finding has a truthful implemented or deferred disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| exact behavioral tests -> fixed claim maps | A module-level pass cannot substitute for the precise behavior that closes a blocker. |
| plan threat rows -> independent verifier oracle | Plan-owned security claims must be complete, unique, and explicitly mapped. |
| verifier results -> validation ledger | Historical and current evidence must remain distinguishable and failures cannot be rendered as completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-22-01 | Spoofing | Requirement completion | high | mitigate | Bind each current blocker to exact AST-validated selectors and reject whole-file proxies. |
| T-07-22-02 | Tampering | Plan/threat inventory | high | mitigate | Maintain a literal 50-row gap ownership oracle and exact 96-ID union with omission/duplicate/unexpected checks. |
| T-07-22-03 | Repudiation | Validation ledger | high | mitigate | Append dated commands, exit codes, observed counts, and one evidence row per current blocker; preserve failures verbatim. |
| T-07-22-04 | Information Disclosure | Evidence and deferred records | medium | mitigate | Record only identifiers, counts, sanitized diagnostics, and policy boundaries; never include signing material, credentials, or raw sensitive manifests. |
</threat_model>

<verification>
- `uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase7_contract_verifier.py -o log_cli=false -o addopts=` passes.
- `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --quick` exits zero or its exact failure is recorded without completion.
- `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --all` exits zero before current MIGR-04/MIGR-05 closure is recorded.
- The validation ledger and deferred-items record match the actual implemented/deferred state.
</verification>

<success_criteria>
- Every current blocker has exact executable evidence.
- Plans 07-20 through 07-22 and all 12 new threat IDs participate in fixed omission checks.
- The complete threat inventory is exactly 96 unique IDs.
- The all-mode verifier, scoped Ruff, and full deterministic non-live suite pass before completion is claimed.
- WR-02 is explicitly deferred and no Phase 8, obstore, perfect-orphan-reclamation, or cross-resource ACID claim is introduced.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Plan(s) |
|---|---|---|---|
| GOAL | Explicit stopped-worker migration/rebuild without silent mutation or loss of the only valid copy | COVERED | 07-20, 07-21, 07-22 |
| REQ | MIGR-03 non-mutating human/machine plans | COVERED | Existing implementation retained; exact mapping and final gate in 07-22 |
| REQ | MIGR-04 exact offline copy-verify-switch and destination contract | COVERED | 07-20 target identity, transform, S3 abort recovery; 07-22 evidence |
| REQ | MIGR-05 authenticated resumability and deterministic attributed cleanup | COVERED | 07-20 migration abort; 07-21 rebuild settlement; 07-22 evidence |
| REQ | MIGR-06 explicit confirmed handler-backed rebuild | COVERED | Existing rebuild retained; exact settlement safety in 07-21; final mapping in 07-22 |
| RESEARCH | Independent payload dimensions and handler-owned transforms | COVERED | 07-20 |
| RESEARCH | One authority, immutable external effects, exact cleanup debt, and typed topology outcomes | COVERED | 07-20, 07-21 |
| RESEARCH | Deterministic fixed validation and separated safety/recovery/progress/performance evidence | COVERED | 07-22 |
| CONTEXT | D-01 through D-06 supported window, inspection, and independent compatibility | COVERED | Existing plans retained; D-03 exact format regression in 07-20; revalidated 07-22 |
| CONTEXT | D-07 through D-12 whole-store, handler, source-validation, and explicit activation rules | COVERED | 07-20 plus existing plans; revalidated 07-22 |
| CONTEXT | D-13 through D-18 rollback/finalize/cleanup/evidence rules | COVERED | Existing plans retained; D-16 operational debt in 07-20/07-21; revalidated 07-22 |
| CONTEXT | D-19 through D-22 explicit resume, fail-closed evidence, exact abort ownership, and signing boundary | COVERED | 07-20, 07-21, 07-22 |
| CONTEXT | Deferred ideas | EXCLUDED | None declared in 07-CONTEXT.md |
| REVIEW | CR-01, CR-02, CR-03 | COVERED | 07-20, 07-21, 07-22 |
| REVIEW | WR-01 inaccurate partial abort count | COVERED | 07-20 |
| REVIEW | WR-02 handler `name=` alias inconsistency | EXCLUDED | Explicitly added to deferred-items.md by 07-22 as unrelated future API-contract work |
| SPIKE | Path-based handlers, exact immutable locators, authority-only visibility, exact deletion | COVERED | 07-20, 07-21 |
| SPIKE | Production obstore participant, streaming choice, S3 conditional-publication policy | EXCLUDED | Later phase; unnecessary for the three verified gaps |
| OTHER PHASE | Live PostgreSQL/Amazon-S3, Windows, supported Python matrix, packaging, performance | EXCLUDED | Phase 8 |

<source_coverage>
All GOAL, REQ, RESEARCH, and CONTEXT items are covered or explicitly retained from executed Phase 7 plans. The three verifier blockers are fully planned. The unrelated WR-02 warning and later obstore/Phase 8 work are explicitly excluded with destinations rather than silently dropped.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-22-SUMMARY.md` when done.
</output>
