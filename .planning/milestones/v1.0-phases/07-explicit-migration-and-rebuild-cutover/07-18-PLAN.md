---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 18
type: execute
wave: 14
depends_on:
  - 07-17
files_modified:
  - tools/verify_phase7_contracts.py
  - tests/test_phase7_contract_verifier.py
autonomous: true
requirements:
  - MIGR-03
  - MIGR-04
  - MIGR-05
  - MIGR-06
gap_closure: true
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every fixed Phase 7 requirement, decision, security-threat, flagged-assumption, and prohibition node maps to one or more exact `path::test_name` selectors rather than a filename proxy."
    - "CR-01 exact selectors enforce per-run entry/byte/evidence bounds, independently recoverable splitting, attributed-only resume/abort, and the accepted invisible/unadopted pre-checkpoint orphan limit without misclassifying it as failed atomicity or promising perfect reclamation."
    - "The security inventory is the exact union of the original 46 threat IDs and all 38 IDs declared by Plans 07-12 through 07-19; plan-threat parsing rejects any omitted, duplicated, or unexpected row."
    - "The fixed verifier fails before executing tests if a mapped function is removed or renamed while its file remains."
    - "Quick and all modes execute the exact selector union appropriate to their declared scope, so unrelated tests in the same file cannot create a false green."
  artifacts:
    - path: tools/verify_phase7_contracts.py
      provides: "Exact fixed-selector manifest, manifest validation, and selector-based execution"
    - path: tests/test_phase7_contract_verifier.py
      provides: "Self-tests for removed/renamed mapped test detection and exact selector execution"
  key_links:
    - from: tools/verify_phase7_contracts.py
      to: tests/test_phase7_contract_verifier.py
      via: "AST/collection validation of every fixed `path::test_name` selector"
    - from: tools/verify_phase7_contracts.py
      to: Phase 7 behavioral tests
      via: "validated exact selector union passed to pytest"
    - from: .planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-PLAN.md through 07-19-PLAN.md
      to: tools/verify_phase7_contracts.py
      via: "literal 38-row gap-plan threat inventory is cross-checked against parsed threat-model rows and exact SECURITY_THREAT_NODES selectors"
---

<objective>
Close the fixed-verifier false-green blocker by binding every claim node to exact test selectors and validating that each mapped test still exists.

Purpose: Phase completion evidence must fail when the specific test that proves a claim is removed or renamed, even if its file and unrelated tests remain.
Output: Exact selector mappings, fail-closed manifest validation, selector-based execution, and verifier self-tests.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.planning/spikes/MANIFEST.md
@.codex/skills/spike-findings-cacheness/references/handler-integration.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.codex/skills/spike-findings-cacheness/references/backend-mechanics.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-13-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-14-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-15-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-16-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-17-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-18-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-19-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-17-SUMMARY.md
@tools/verify_phase7_contracts.py
@tests/test_phase7_contract_verifier.py
</context>

<planner_assumptions>
- No Phase 7 SPEC exists. MIGR-03 through MIGR-06 are resolved from their ROADMAP/REQUIREMENTS definitions, D-01 through D-22, 07-VALIDATION's existing claim inventory, the authoritative verification blocker, and ADR 0001.
- The existing `07-COVERAGE.md` external API declaration remains authoritative. This plan changes verifier precision only and creates no external-API matrix.
- Exact selectors are repository-relative `tests/...py::test_name` identifiers for concrete test functions. A fixed claim cannot be satisfied merely because its module imports or collects.
- The completed spike does not add an external service integration or Phase 7 dependency. Fixed prohibitions must keep obstore below a Cacheness-owned participant seam if introduced later, preserve the path-based guarded handler contract, and reject any Phase 7 streaming/conditional-publication claim or hidden multipart policy; this gap set itself installs and wires no obstore package.
</planner_assumptions>

<threat_evidence_map>
- `T-07-G12-01`, `T-07-G12-04`, `T-07-G12-05` -> `tests/test_migration_cutover.py::test_migration_run_enforces_entry_byte_and_evidence_limits_with_split_plan`, `tests/test_migration_cutover.py::test_uncheckpointed_candidate_orphan_remains_invisible_unadopted_and_outside_exact_cleanup`, and `tests/test_migration_cutover.py::test_transformed_candidate_checkpointed_metadata_recovers_within_limits_and_uncheckpointed_orphan_remains_invisible`; `T-07-G12-02`, `T-07-G12-03` -> `tests/test_migration_cutover.py::test_resume_and_abort_staging_use_only_authority_attributed_batches`.
- `T-07-13-01`, `T-07-13-03` -> `tests/contracts/test_postgresql_lifecycle_authority.py::test_fresh_blobstore_initialize_rechecks_activated_offline_after_postgresql_identity_load`; `T-07-13-02`, `T-07-13-04` -> `tests/contracts/test_postgresql_lifecycle_authority.py::test_postgresql_preflight_mutation_checks_persisted_worker_fence_after_open`.
- `T-07-14-01` -> `tests/test_blob_store_atomic_lifecycle.py::test_blobstore_maintenance_canonical_put_replays_projection_free_receipt_after_response_loss`, `tests/contracts/test_lifecycle_authority.py::test_local_authorities_expose_exact_operation_replay_without_new_authority_state`, and `tests/contracts/test_postgresql_lifecycle_authority.py::test_postgresql_read_mutation_returns_exact_prepared_and_promoted_replay`.
- `T-07-15-01`, `T-07-15-04` -> `tests/test_rebuild_workflow.py::test_rebuild_response_loss_rederives_operation_id_and_replays_authority_receipt` and `tests/test_rebuild_workflow.py::test_projection_equipped_rebuild_replays_canonical_receipt_without_preacceptance_or_duplicate_derived_work`; `T-07-15-02`, `T-07-15-03` -> `tests/test_rebuild_workflow.py::test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt`.
- `T-07-16-01` -> `tests/test_migration_plan_contract.py::test_compatibility_edge_requires_exact_destination_dimensions`; `T-07-16-02` -> `tests/test_handler_registration.py::test_registered_custom_handler_resolves_one_exact_directed_transformation` and `tests/test_handler_registration.py::test_registered_handler_rejects_declared_edge_without_concrete_transform`; `T-07-16-03`, `T-07-16-05` -> `tests/test_migration_cutover.py::test_migration_executes_one_handler_transform_and_publishes_destination_manifest_identity` and `tests/test_migration_cutover.py::test_transformed_candidate_checkpointed_metadata_recovers_within_limits_and_uncheckpointed_orphan_remains_invisible`; `T-07-16-04` -> `tests/test_migration_cutover.py::test_migration_executes_one_handler_transform_and_publishes_destination_manifest_identity`.
- `T-07-17-01` -> `tests/test_migration_plan_contract.py::test_machine_plan_digest_binds_sensitive_catalog_and_manifest_without_serializing_them`; `T-07-17-02`, `T-07-17-03`, `T-07-17-04`, `T-07-17-05` -> `tests/test_migration_run_evidence.py::test_execution_rereads_authenticates_and_rejects_plan_bound_manifest_or_catalog_drift`.
- `T-07-18-01`, `T-07-18-03`, `T-07-18-04`, `T-07-18-05` -> `tests/test_phase7_contract_verifier.py::test_fixed_manifest_requires_exact_path_and_test_name_selectors`; `T-07-18-02` -> `tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_removed_mapped_test_function_while_file_remains` and `tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_renamed_mapped_test_function_while_file_remains`.
- `T-07-19-01`, `T-07-19-02`, `T-07-19-03` -> `tests/test_phase7_contract_verifier.py::test_main_never_renders_failed_requirement_as_pass`; `T-07-19-04`, `T-07-19-05` -> `tests/test_phase7_contract_verifier.py::test_document_and_coverage_audits_reject_false_qualification_and_secrets`.
- `T-07-13-SC`, `T-07-17-SC`, `T-07-18-SC`, `T-07-19-SC` -> `tests/test_phase7_contract_verifier.py::test_fixed_gap_supply_chain_threats_map_to_frozen_command_contracts`.
</threat_evidence_map>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Convert fixed claim mappings to exact validated selectors</name>
  <files>tools/verify_phase7_contracts.py, tests/test_phase7_contract_verifier.py</files>
  <read_first>
    - tools/verify_phase7_contracts.py: requirement, decision, threat, assumption, prohibition, test-file, and plan-file manifests plus quick/all execution
    - tests/test_phase7_contract_verifier.py: fixed-manifest and verifier subprocess self-tests
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md: fixed requirement/decision/threat/assumption/prohibition claim inventory
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md: exact-selector blocker and six required behavioral additions
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-PLAN.md through 07-19-PLAN.md: authoritative threat-model rows whose exact 38 IDs and mitigation claims must enter the fixed manifest
  </read_first>
  <behavior>
    - Test `test_fixed_manifest_requires_exact_path_and_test_name_selectors`: every value in `MIGRATION_REQUIREMENT_NODES`, `DECISION_NODES`, `SECURITY_THREAT_NODES`, `FLAGGED_ASSUMPTION_NODES`, and the fixed prohibition mapping has `tests/...py::test_name` shape, is non-empty, and is unique within its claim node; `FLAGGED_ASSUMPTION_NODES` contains exact keys `A-MIGR03`, `A-MIGR04`, `A-MIGR05`, and `A-MIGR06` from Plan 07-12.
    - Test proves each selector's repository-relative module is in the fixed Phase 7 test inventory, the gap-owned lifecycle files and operation-replay contract modules are in the fixed production/test/Ruff inventories, every Phase 7 plan path through `07-19-PLAN.md` is included in the plan manifest, and CR-01/CR-02 include exact bounded-authority-evidence, invisible-orphan, deterministic authority-replay, and projection-equipped response-loss selectors.
    - Test `test_fixed_manifest_includes_every_gap_plan_threat_exactly_once`: the parsed threat rows from Plans 07-12 through 07-19 equal the literal 38-ID gap inventory, the complete declared inventory equals the original 46 plus those 38 IDs, and removing, duplicating, or adding one plan threat fails with the exact ID and plan path.
    - Test `test_fixed_gap_supply_chain_threats_map_to_frozen_command_contracts`: the four gap-plan `-SC` IDs map to this exact selector, every plan uses only checked-in dependencies, and every automated execution command that installs/runs Python dependencies contains the frozen-lockfile guard and no package-install action.
    - Test `test_document_and_coverage_audits_reject_false_qualification_and_secrets` also rejects a Phase 7 claim that obstore conditional create is bounded-memory, a silent multipart-copy publication selection, or text that exposes participant handles/managed locators to handlers; the unchanged plan set contains no obstore install/wiring action.
  </behavior>
  <action>Write the exact-shape and gap-threat-inventory tests first. Replace filename values in all fixed requirement, decision, threat, and flagged-assumption mappings with tuples of exact `path::test_name` selectors that actually assert the named claim. Require `FLAGGED_ASSUMPTION_NODES` to have exactly the four spec-less keys `A-MIGR03`, `A-MIGR04`, `A-MIGR05`, and `A-MIGR06` declared in Plan 07-12, mapped respectively to Plan 07-17 confidentiality/revalidation selectors, Plan 07-16 exact-edge/real-transform/bounded-authority-evidence selectors, Plans 07-12 through 07-15 attributed descriptor/canonical authority-replay selectors, and Plans 07-14 through 07-16 confirmed-rebuild/handler selectors. Add or convert the fixed prohibition inventory to the same exact-selector contract for all eight project-value/safety prohibitions represented by 07-VALIDATION, using the accepted Plan 07-12 boundary: ordinary paths never migrate, evidence/listings never authorize, no new coordinator or second authority appears, path-based handlers and guarded staging/snapshots remain intact, immutable generation locators are never reused, authenticated evidence is mandatory, secrets remain absent from shareable renderers and signing/provider secrets remain absent from maintenance artifacts, and Phase 7 makes no obstore streaming or hidden multipart-policy claim. Add exact selector coverage for explicit entry/byte/evidence limits and split-required larger stores; attributed-only resume/abort; and the post-publication/pre-authority-checkpoint orphan remaining invisible, unattributed, unadopted, and outside guaranteed exact cleanup while integrity/authority visibility remain guaranteed as an ADR 0001 progress limit. Map each blocker to its named regression selectors: CR-01 includes `test_migration_run_enforces_entry_byte_and_evidence_limits_with_split_plan`, `test_uncheckpointed_candidate_orphan_remains_invisible_unadopted_and_outside_exact_cleanup`, `test_resume_and_abort_staging_use_only_authority_attributed_batches`, and `test_transformed_candidate_checkpointed_metadata_recovers_within_limits_and_uncheckpointed_orphan_remains_invisible`; CR-02 includes the canonical maintenance replay test, `test_rebuild_response_loss_rederives_operation_id_and_replays_authority_receipt`, the projection-equipped response-loss test, every-state resume test, and receipt-bound cleanup test. Include WR-02's concrete-transform selector. Add the Plan 07-14 lifecycle source/test files to independent reviewed/executed/Ruff inventories as already specified and add `07-12-PLAN.md` through `07-19-PLAN.md` to `PHASE7_PLAN_PATHS`. Extend the literal declared threat inventory and `SECURITY_THREAT_NODES` with exactly the existing 38 plan-declared IDs: `T-07-G12-01` through `T-07-G12-05`; `T-07-13-01` through `T-07-13-04` plus `T-07-13-SC`; `T-07-14-01`; `T-07-15-01` through `T-07-15-04`; `T-07-16-01` through `T-07-16-05`; `T-07-17-01` through `T-07-17-05` plus `T-07-17-SC`; `T-07-18-01` through `T-07-18-05` plus `T-07-18-SC`; and `T-07-19-01` through `T-07-19-05` plus `T-07-19-SC`. Give every ID exact selectors executing its revised mitigation; supply-chain rows map to frozen-lockfile/verifier command-contract self-tests. Parse only the eight fixed plans' threat tables against the independent 38-ID oracle, enforce exact ownership/no duplicates/omissions/unexpected IDs, retain the complete 84-ID union, and build the fixed pytest target set by deterministic de-duplication. Add no new intent/journal/lifecycle-state/coordination/obstore implementation.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase7_contract_verifier.py::test_fixed_manifest_requires_exact_path_and_test_name_selectors tests/test_phase7_contract_verifier.py::test_fixed_manifest_includes_every_gap_plan_threat_exactly_once tests/test_phase7_contract_verifier.py::test_fixed_gap_supply_chain_threats_map_to_frozen_command_contracts</automated>
  </verify>
  <acceptance_criteria>All fixed claim maps use exact unique selectors, CR-01 explicitly covers bounded split runs, attributed-only recovery, and the invisible/unadopted orphan non-reclamation boundary, CR-02 covers deterministic existing-authority replay without a maintenance pre-intent, the exact 84-threat inventory retains all 38 gap rows, and omission/duplicate/unexpected mutations fail deterministically.</acceptance_criteria>
  <done>Each fixed Phase 7 claim is bound to the exact test intended to prove it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed when a mapped test is removed or renamed</name>
  <files>tools/verify_phase7_contracts.py, tests/test_phase7_contract_verifier.py</files>
  <read_first>
    - tools/verify_phase7_contracts.py: manifest validation and subprocess error reporting
    - tests/test_phase7_contract_verifier.py: temporary-repository mutation helpers and fail-closed assertions
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: D-18 deterministic evidence and D-20 fail-closed posture
  </read_first>
  <behavior>
    - Test `test_fixed_manifest_rejects_removed_mapped_test_function_while_file_remains`: deleting only the mapped function from a temporary repository copy makes validation non-zero with the missing selector and claim node named.
    - Test `test_fixed_manifest_rejects_renamed_mapped_test_function_while_file_remains`: renaming only the mapped function produces the same fail-closed result even though the module still imports and contains other passing tests.
    - Test `test_fixed_manifest_rejects_gap_threat_without_exact_selector`: deleting the selector tuple for one of the 38 gap-plan threats while its threat row remains makes validation fail with that exact threat ID; deleting or duplicating the plan row also fails before pytest.
    - Test proves a fully intact manifest validates and the exact selector list is the list passed to pytest.
  </behavior>
  <action>Validate the fixed selector and plan-threat manifests before any pytest subprocess runs. Parse each selector into repository-relative path and test name, reject malformed/absolute/traversal paths, verify the path is in the fixed test inventory, and inspect the module's collected or parsed test symbols so a missing exact function fails with a deterministic claim-specific diagnostic. Prefer pytest collection data if it is already available without executing tests; otherwise use Python AST to recognize module-level test functions and class-qualified selectors consistently. Add temporary-copy self-tests that remove and rename one mapped function while leaving its file and unrelated tests intact, remove one gap threat's exact mapping, remove one gap-plan threat row, and duplicate one gap-plan threat ID. Each failure must name the affected selector or threat ID and source plan. Preserve non-zero exit status for manifest, collection, plan parsing, subprocess, and JSON/schema errors; do not allow later passing suites to overwrite an earlier failure.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_removed_mapped_test_function_while_file_remains tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_renamed_mapped_test_function_while_file_remains tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_gap_threat_without_exact_selector</automated>
  </verify>
  <acceptance_criteria>Removing or renaming only a mapped test, dropping a gap-threat selector, or altering one fixed gap-plan threat row causes a deterministic non-zero verifier result that identifies the exact selector/threat and affected claim while intact mappings execute exactly.</acceptance_criteria>
  <done>The fixed verifier can no longer report claim coverage from a surviving filename alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixed claim manifest -> pytest | Static evidence claims become executable selectors |
| repository test source -> verifier | Current test symbols must match the reviewed fixed manifest |
| subprocess result -> validation ledger | Exit status and diagnostics determine completion evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-18-01 | Tampering | fixed selector and threat mappings | high | mitigate | Validate exact selector shape, inventory membership, symbol existence, and exact one-row plan-threat membership before test execution. |
| T-07-18-02 | Repudiation | claim-to-test evidence | high | mitigate | Emit deterministic diagnostics naming both the missing selector and its requirement/decision/threat/assumption/prohibition node. |
| T-07-18-03 | Spoofing | whole-file proxy coverage | high | mitigate | Execute exact selector unions; unrelated passing tests in a file cannot impersonate the mapped proof. |
| T-07-18-04 | Elevation of Privilege | selector path handling | medium | mitigate | Accept repository-relative fixed inventory paths only and reject absolute/traversal selectors. |
| T-07-18-05 | Denial of Service | duplicate selector expansion | low | accept | The fixed manifest is bounded and deterministically de-duplicated; malformed duplicates fail validation. |
| T-07-18-SC | Tampering | dependency supply chain | low | accept | No package installation occurs; verifier commands use the frozen lockfile. |
</threat_model>

<verification>
- All five named verifier self-tests pass, including complete gap-threat inventory and missing-selector rejection.
- `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase7_contract_verifier.py` passes.
- `uv run --frozen python tools/verify_phase7_contracts.py --quick` validates mappings and executes exact non-live selectors.
</verification>

<success_criteria>
- Every fixed Phase 7 claim is mapped to exact test selectors.
- All 38 threat IDs from Plans 07-12 through 07-19 are individually mapped, and the complete fixed threat inventory contains exactly 84 unique IDs.
- Removed or renamed mapped tests fail while their files remain.
- Quick/all execution uses exact selector unions and retains fail-closed subprocess semantics.
- The existing coverage declaration and stopped-worker architecture remain unchanged.
</success_criteria>

## Artifacts this phase produces

- Exact-selector values in `MIGRATION_REQUIREMENT_NODES`, `DECISION_NODES`, `SECURITY_THREAT_NODES`, `FLAGGED_ASSUMPTION_NODES`, and the fixed prohibition mapping; `FLAGGED_ASSUMPTION_NODES` has exact keys `A-MIGR03` through `A-MIGR06`.
- `PHASE7_PLAN_PATHS` entries for `07-12-PLAN.md` through `07-19-PLAN.md`, a literal 38-row gap threat oracle, and an exact 84-ID combined inventory with omission/duplicate/unexpected-row checks.
- Fail-closed selector-shape and symbol-existence validation.
- Exact self-tests `test_fixed_manifest_requires_exact_path_and_test_name_selectors`, `test_fixed_manifest_includes_every_gap_plan_threat_exactly_once`, `test_fixed_gap_supply_chain_threats_map_to_frozen_command_contracts`, `test_fixed_manifest_rejects_removed_mapped_test_function_while_file_remains`, `test_fixed_manifest_rejects_renamed_mapped_test_function_while_file_remains`, and `test_fixed_manifest_rejects_gap_threat_without_exact_selector`.
- Gap-plan manifest coverage through plan 07-19.

<source_coverage>
- GOAL: Makes fixed completion evidence deterministic and replay-resistant.
- REQ: MIGR-03, MIGR-04, MIGR-05, and MIGR-06.
- RESEARCH: Deterministic validation and fail-closed evidence discipline.
- CONTEXT: D-18 and D-20, while exact selectors cover all D-01 through D-22 claims already cataloged in 07-VALIDATION.
- EXCLUDED: No new external API matrix, frontend work, TypeScript ORM schema push, live qualification, or review-warning expansion.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-18-SUMMARY.md` when done.
</output>
