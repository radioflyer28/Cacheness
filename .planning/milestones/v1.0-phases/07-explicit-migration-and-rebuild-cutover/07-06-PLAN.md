---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "06"
type: execute
wave: 4
depends_on: [07-04, 07-05]
files_modified:
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/migration_authority.py
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/manifest.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - tests/test_migration_cutover.py
  - tests/test_projection_sql_atomicity.py
autonomous: false
requirements: [MIGR-04, MIGR-05]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A supported memory or SQLite same-backend migration stages immutable candidate payload/catalog state in bounded batches, verifies every planned entry and aggregate, and activates the whole candidate only through one authority transition (MIGR-04; D-07, D-12)."
    - "Every mutation revalidates stopped-worker acknowledgement plus source identity/revision; a stale or incomplete candidate never publishes and requires reinspection (D-09)."
    - "Interruption at each payload/evidence/authority boundary resumes idempotently without losing or hiding the prior valid store, and activation retains the prior verified selection until later explicit retirement (MIGR-05; D-14, D-19, D-20)."
    - "SQLite atomicity ends at one SQLite transaction; filesystem copy/verification is an attributable external effect. Projection rebuild occurs separately after activation and cannot affect canonical success (D-11, D-16; ADR 0001)."
    - "The first release baseline uses SQLite authority schema 8 and PostgreSQL authority schema/capability 4, with one authority-owned `candidate -> activated_offline -> active|rolled_back` publication state and retained prior selection; activated-offline rejects every ordinary worker entry point until rollback/finalize, and development schemas 7/3 gain no compatibility promise (D-01, D-03, D-05, D-13)."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "Bounded local stage/verify/activate/resume and projection-after-cutover orchestration"
    - path: src/cacheness/storage/migration_authority.py
      provides: "AuthorityPublicationState plus shared candidate, activation, rollback and finalize receipts used by every backend"
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Authority-transactional verified candidate activation retaining prior selection"
    - path: tests/test_migration_cutover.py
      provides: "Memory/SQLite whole-store and every-interruption contract"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/blob_store.py
      via: "Candidate payload work uses guarded immutable participant operations and authenticated manifests"
      pattern: "open_entry|payload_backend"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/sqlite_lifecycle_authority.py
      via: "Only a VerifiedCandidateReceipt enters one SQLite activation transaction"
      pattern: "activate_verified_candidate"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/projections.py
      via: "Projection rebuild is invoked after canonical activation and reports derived partial outcome"
      pattern: "rebuild_projection|ProjectionResult"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No per-entry publication, partial replacement, candidate-path switch, filesystem rename authority, or cross-resource ACID claim."
    - "[FLAGGED/UNVERIFIED] No writer lease/lock/queue is added; stopped workers are an explicit operator precondition and revision drift is the fail-closed check."
    - "[FLAGGED/UNVERIFIED] No activation/failure path deletes the prior store, adopts unexplained output, or lets projection status gate canonical publication."
---

<objective>
Complete safe offline same-backend cutover and interruption recovery for memory/memory and SQLite/filesystem.

Purpose: Deliver the local deterministic MIGR-04/MIGR-05 contract using one topology authority and attributable external payload effects.
Output: Bounded stage/verify/activate/resume behavior, authority-atomic local publication, prior retention, and derived-only projection handling.
</objective>

## Artifacts this phase produces

- `OfflineMigrationService.stage()`, `verify()`, `activate()`, and `resume()` complete local implementations returning bounded `MigrationStepResult`/continuations.
- `CandidateBatchReceipt`, `CandidateVerificationReport`, and complete `VerifiedCandidateReceipt` fields for exact planned keys/generations, manifest/payload digests, counts, bytes, and candidate identity.
- Shared `AuthorityPublicationState`, `PriorStoreReceipt`, `ActivationReceipt`, `RollbackReceipt`, and `FinalizeReceipt` contracts, including exact `candidate`, `activated_offline`, `active`, and `rolled_back` states.
- SQLite release-baseline schema 8 and the exact PostgreSQL schema/capability 4 contract: `migration_run_id`, `migration_plan_digest`, `migration_candidate_digest`, `migration_source_revision`, `migration_activated_revision`, `migration_state`, `migration_active_selection`, and `migration_rollback_eligible` live in the authority singleton, while `migration_store_entries(run_id, selection, key, generation, locator, manifest, manifest_digest, lineage, entry_revision)` retains `candidate` and `prior` rows. Development schemas SQLite 7/PostgreSQL 3 are not production migration edges.
- `MigrationAuthority.record_verified_candidate()` and `activate_verified_candidate()` implementations for memory and SQLite, plus retained `PriorStoreReceipt`/`ActivationReceipt` authority state.
- Expanded `tests/test_migration_cutover.py` local interruption matrix and projection-separation cases.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-04-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-05-SUMMARY.md
@src/cacheness/storage/lifecycle.py
@src/cacheness/storage/projections.py
@tests/test_lifecycle_authority_contract.py
</context>

<interfaces>
Use the completed authority identity/raw inventory and authenticated evidence contracts. Existing payload publication is immutable and handler reads occur from authenticated private snapshots. The shared contract fixes the first release authority identities at SQLite schema 8 and PostgreSQL schema/capability 4. SQLite `_transaction()` already provides rollback and uncertain-commit classification within the authority resource; external filesystem work must be recorded/revalidated around it rather than folded into it. `activated_offline` rejects ordinary worker open/read/query/mutation entry points with `CacheBlobMigrationOfflineDecisionRequiredError` until rollback or finalize, so rollback is sealed before any worker restart rather than inferred from a later write. Narrow maintenance status/receipt methods remain callable for recovery.
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the one-way authority publication schema baseline</name>
  <reversibility rating="one-way">Publishing authority schema 8/4 and its whole-store state machine creates the first release baseline that later releases must recognize.</reversibility>
  <files>None (decision gate only)</files>
  <read_first>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
  </read_first>
  <action>Present resolved decision RQ-01 with D-01, D-03, D-05, D-07, D-12, and D-13 exactly: whole-store activation requires SQLite schema 8 and PostgreSQL schema/capability 4; one authority owns `candidate`, `activated_offline`, `active`, and `rolled_back` state plus candidate/prior entry rows; `activated_offline` blocks every ordinary worker open/read/query/mutation entry point until rollback or finalize while narrow maintenance status/receipt methods remain available; development schemas 7/3 remain unsupported pre-release inputs rather than promised migration edges. State that payload/evidence effects remain outside the authority transaction and recover through exact receipts.</action>
  <decision>Confirm the exact Phase 7 authority schema/publication baseline before schema constants, tables, or public semantics change.</decision>
  <context>This is a one-way release-format and publication-state decision. It is required by real whole-store activation, not a demonstration version bump.</context>
  <options>
    <option id="proceed"><name>Proceed with resolved RQ-01</name><pros>Creates the smallest authority-owned whole-store cutover state and correctly seals rollback before worker restart.</pros><cons>Schema 8/4 becomes the first release baseline and earlier development schemas remain unsupported.</cons></option>
    <option id="stop"><name>Stop for replanning</name><pros>Avoids publishing an unaccepted persisted contract.</pros><cons>MIGR-04/MIGR-05 cannot proceed because current authority schemas cannot represent whole-store candidate/prior selection.</cons></option>
  </options>
  <resume-signal>Reply `proceed RQ-01` to continue, or `stop RQ-01` to replan.</resume-signal>
  <acceptance_criteria>The execution record contains an explicit `proceed RQ-01`; any other response stops before persisted schema/publication changes.</acceptance_criteria>
  <verify><automated>rg -q "RQ-01 — Persisted authority schema evolution is required" .planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md</automated></verify>
  <done>The one-way authority baseline is explicitly accepted before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define shared publication contracts and activate complete SQLite candidates</name>
  <precondition>Task 1 recorded `proceed RQ-01` before the first release authority schema and publication semantics are implemented.</precondition>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_authority.py, src/cacheness/storage/manifest.py, src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/migration_authority.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>tests/test_migration_cutover.py</file>
  </read_first>
  <behavior>
    - "Stage copies bounded immutable batches without changing source visibility; verify fails unless planned keys/generations/manifests/digests/sizes/totals match exactly."
    - "Recording a verified candidate rejects incomplete verification, stale source, mismatched candidate/plan, or absent stopped-worker acknowledgement before the authority transaction."
    - "One authority transaction activates the recorded candidate, retains a corroborated prior selection, and enters `activated_offline`; ordinary worker open/read/query/mutation remains blocked until rollback or finalize."
  </behavior>
  <action>Expand the tracer to all planned SQLite entries per D-01, D-03, D-05, D-07, D-09, D-12, D-13, and D-14 after the RQ-01 checkpoint. In `migration_authority.py`, define frozen `AuthorityPublicationState`, `PriorStoreReceipt`, `ActivationReceipt`, `RollbackReceipt`, and `FinalizeReceipt` values before any backend implementation consumes them. Stage exact-compatible payloads as immutable external effects in bounded batches, preserving signed manifest/catalog bytes and recording per-batch receipts. Verify manifest authentication, handler eligibility, locator containment, payload SHA-256/size, exact key/generation set, entry totals, and bytes before constructing `VerifiedCandidateReceipt`. Revalidate source identity/revision and the evidence-bound stopped-worker acknowledgement before every mutation. Change both `SQLITE_USER_VERSION` and manifest `CURRENT_SQLITE_USER_VERSION` from 7 to the first release baseline 8. Add `migration_run_id`, `migration_plan_digest`, `migration_candidate_digest`, `migration_source_revision`, nullable `migration_activated_revision`, `migration_state`, `migration_active_selection`, and `migration_rollback_eligible` to `authority_state`, plus `migration_store_entries(run_id, selection, key, generation, locator, manifest, manifest_digest, lineage, entry_revision)` keyed by `(run_id, selection, key)` for exact `candidate`/`prior` rows. Enforce the state/selection/nullability combinations resolved in RQ-01. Do not add a production 7-to-8 edge; ordinary open/initialize rejects version 7 unchanged. Record a verified candidate in one authority transaction, then activate separately in one later authority transaction that retains current entries as `prior`, replaces canonical entries from `candidate`, increments revision, and enters `activated_offline`. The shared contract fixes PostgreSQL's corresponding Plan 07 target at schema 4 and capability `postgresql-lifecycle-authority-v4`. While `activated_offline`, make every ordinary worker open/read/query/mutation authority entry point fail with `CacheBlobMigrationOfflineDecisionRequiredError`; keep only narrow maintenance status/receipt/rollback/finalize methods available. Inject faults around every SQLite authority boundary and assert rollback/uncertain-commit classification from canonical authority state. External payload/evidence operations stay outside these authority transactions.</action>
  <acceptance_criteria>Complete candidates activate all at once; every missing/mismatched/stale/unverified case leaves source authority and payloads unchanged; SQLite fault tests prove transaction rollback without cross-resource claims.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_cutover.py tests/test_lifecycle_authority_contract.py -x -o log_cli=false</automated></verify>
  <done>The shared publication contract is fixed and SQLite has a complete copy-verify-switch path with the prior valid store retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resume every local interruption and rebuild projections separately</name>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_evidence.py, src/cacheness/storage/memory_lifecycle_authority.py, tests/test_migration_cutover.py, tests/test_projection_sql_atomicity.py</files>
  <read_first>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/memory_lifecycle_authority.py</file>
    <file>src/cacheness/storage/projections.py</file>
    <file>tests/test_projection_sql_atomicity.py</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
  </read_first>
  <behavior>
    - "Faults before/after every batch, evidence checkpoint, complete verification, authority commit, and post-commit observation resume idempotently from explicit evidence."
    - "A response-loss/uncertain activation is classified from the authority receipt/state, never candidate presence."
    - "Projection rebuild begins only after canonical activation; dirty/missing/failing projection yields a derived outcome without revoking activation or rewriting payload eligibility."
  </behavior>
  <action>Implement the Plan 06 shared publication states and receipts in `InMemoryLifecycleAuthority` with same-process semantics, then use Plan 05 evidence to make each local external payload and authority boundary replay-safe. At resume, reverify recorded candidate outputs before skipping, classify uncertain candidate-recording or activation by exact authority receipts and the authority-owned publication state, and preserve the prior store through all pre-finalization states. Define interruption order explicitly: external immutable batch effect, evidence checkpoint, complete verification, authority candidate-record transaction, evidence checkpoint, authority activation transaction, then post-commit evidence observation. A lost response is classified by exact authority run/plan/candidate/revision state, never by a path or work file. Add deterministic hooks around every boundary for memory and SQLite. After activation, offer an explicit derived projection rebuild through existing `ProjectionController`; retain the activation receipt when projection work is current, dirty, partial, missing, or fails. Keep integrity, recovery, progress, and performance assertions in separate test sections; use typed bounded continuation/timeouts rather than timing sleeps.</action>
  <acceptance_criteria>Every named interruption converges idempotently or fails typed without losing the source/prior store; projection outcomes remain derived and cannot change canonical activation.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_cutover.py tests/test_migration_run_evidence.py tests/test_projection_sql_atomicity.py -x -o log_cli=false</automated></verify>
  <done>Local migration recovery is deterministic across external-effect and authority commit boundaries, with projections explicitly separate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source manifests/payloads -> candidate | Authenticated canonical entries cross into immutable external candidate effects. |
| verified receipt -> authority transaction | Only corroborated whole-candidate evidence may request canonical activation. |
| activated canonical state -> projection rebuild | Derived work occurs after publication and cannot revoke or redefine it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-21 | Tampering | source revision/candidate | high | mitigate | Revalidate identity/revision before every mutation and exact manifests/digests/counts before receipt issuance. |
| T-07-22 | Tampering | whole-store activation | critical | mitigate | Authority records only a complete verified receipt, activates the exact recorded candidate in one authority transaction, retains prior rows, and blocks all ordinary worker use while `activated_offline`. |
| T-07-23 | Repudiation | interruption/uncertain commit | high | mitigate | Domain-authenticated step receipts plus authority-state classification at every deterministic fault boundary. |
| T-07-24 | Elevation of Privilege | derived projection | high | mitigate | Invoke projection rebuild only post-activation and assert it cannot authorize membership, cleanup, rollback, or activation. |
</threat_model>

<verification>
Focused local cutover, authority, evidence, and projection tests must pass. Integrity tests prove complete-only publication; recovery tests cover every named boundary; progress tests accept typed continuation/conflict/timeout; performance has no correctness threshold.
</verification>

<success_criteria>
Supported local same-backend candidates stage and verify completely, activate through one authority transition, resume across every fault, retain prior recovery state, and keep projections non-authoritative.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-06-SUMMARY.md` when done.
</output>
