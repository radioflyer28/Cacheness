---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 20
type: execute
wave: 16
depends_on:
  - 07-19
files_modified:
  - src/cacheness/storage/migration.py
  - tests/test_migration_cutover.py
  - tests/test_migration_remote_contract.py
autonomous: true
requirements:
  - MIGR-04
  - MIGR-05
gap_closure: true
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A destination handler's declared current payload format and version are always the migration target; the handler's ability to read the source decides readability only and cannot turn a different-format/same-version edge into an identity copy (MIGR-04; D-03, D-10)."
    - "A same-numeric-version migration such as `mcap-v1@1 -> mcap-v2@1` invokes exactly one store-local directed transform and the authenticated candidate manifest declares `mcap-v2@1` (MIGR-04; D-07, D-10, D-12)."
    - "Abort converts typed payload-participant operational failures, including S3 `CacheBlobBackendError`, into existing exact authority-attributed cleanup debt and a nonterminal `STAGING` receipt; a later explicit retry deletes or proves absence and reaches `ABORTED` (MIGR-05; D-16, D-19, D-21)."
    - "`AbortReceipt.deleted_entries` counts only candidates actually deleted or proven absent during that call, while malformed manifests, ownership mismatches, and integrity conflicts continue to fail closed (MIGR-05; D-20, D-21; WR-01)."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "Destination-owned target identity plus typed operational abort normalization and accurate cleanup accounting"
    - path: tests/test_migration_cutover.py
      provides: "Same-version/different-format transform and fail-closed abort regressions"
    - path: tests/test_migration_remote_contract.py
      provides: "Moto-backed S3 abort failure, exact debt checkpoint, and attributed retry settlement contract"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/handlers.py
      via: "`_configured_destination_contract` reads the destination handler's current format/version while source support is used only before exact transformation resolution"
      pattern: "_configured_destination_contract|supports_payload_contract|resolve_payload_transformation"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/backends/s3_backend.py
      via: "abort recognizes the participant's typed operational failure and checkpoints the existing retirement digest before returning"
      pattern: "CacheBlobBackendError|cleanup_debt|AbortReceipt"
    - from: tests/test_migration_remote_contract.py
      to: src/cacheness/storage/migration.py
      via: "a deterministic S3 participant injects snapshot and exact-delete failures, then restores the participant and retries the same authority-attributed candidates"
      pattern: "abort|CacheBlobBackendError|STAGING|ABORTED"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No ordinary constructor, open, initialize, read, cache-policy, reconciliation, or cleanup path may perform an implicit migration or upgrade."
    - "[FLAGGED/UNVERIFIED] No work-directory file, filesystem path, object listing, candidate presence, projection, lock, queue, lease, journal, sidecar, or process-local gate may become a second lifecycle authority."
    - "[FLAGGED/UNVERIFIED] No unauthenticated evidence, unexplained candidate, mismatched receipt, or incomplete catalog may be adopted."
    - "[FLAGGED/UNVERIFIED] No coordinator-owned universal payload conversion, handler stream cutover, managed-locator exposure, or production obstore adoption may be introduced."
    - "[FLAGGED/UNVERIFIED] No plan or test may claim one ACID transaction across metadata authority and filesystem/S3 effects, exact reclamation of invisible pre-checkpoint orphans, or identical progress guarantees across topologies."
---

<objective>
Close the destination-contract and migration-abort blockers using the existing handler and lifecycle-authority seams.

Purpose: Ensure format identity is never inferred from an equal version integer, and ensure every authority-attributed S3 cleanup failure remains explicitly retryable without overstating completed deletion.
Output: Exact destination selection, same-version directed-transform coverage, typed S3 abort debt, accurate abort receipts, and deterministic retry tests.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-PATTERNS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-16-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/handler-integration.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.codex/skills/spike-findings-cacheness/references/backend-mechanics.md
@src/cacheness/storage/migration.py
@src/cacheness/storage/backends/s3_backend.py
@src/cacheness/error_handling.py
@tests/test_migration_cutover.py
@tests/test_migration_remote_contract.py
</context>

<adr_0001_contract>
- Supported topology: stopped-worker memory/memory and SQLite/filesystem implementation evidence, plus deterministic Moto-backed S3 payload-participant evidence only; live PostgreSQL/Amazon-S3 qualification remains Phase 8.
- Integrity: destination identity is exact and authenticated; malformed, mismatched, unsafe, or ownership-conflicting evidence fails closed.
- Recovery: immutable payload snapshot/delete calls occur outside the metadata-authority transaction; typed operational interruption leaves exact retirement debt in authenticated evidence and a later explicit retry settles only that attributed effect.
- Progress: abort returns terminal `ABORTED`, nonterminal `STAGING`, or a typed fail-closed error. It does not promise that every remote attempt succeeds.
- Performance: no latency, throughput, timeout, or memory measurement becomes a correctness condition in this plan.
- Single authority: the selected lifecycle metadata authority remains the only owner of candidate visibility/state/debt; work-directory evidence corroborates the run and S3 object state never authorizes lifecycle state.
</adr_0001_contract>

<flagged_assumptions>
- `[FLAGGED/UNVERIFIED A-MIGR03]` MIGR-03 remains satisfied by the existing non-mutating inventory and canonical human/machine plans; this repair must not change that surface.
- `[FLAGGED/UNVERIFIED A-MIGR04]` MIGR-04 requires the destination handler's declared current contract to define the target even when source and destination version integers are equal.
- `[FLAGGED/UNVERIFIED A-MIGR05]` MIGR-05 treats authenticated authority attribution plus exact cleanup debt as recovery evidence; a typed participant outage is a retryable outcome, not absence or rollback.
- `[FLAGGED/UNVERIFIED A-MIGR06]` MIGR-06 continues to use registered path-based handlers and the destination BlobStore lifecycle; no universal converter or production obstore participant is implied.
</flagged_assumptions>

<assumption_delta>
Decision: no-change. The primary noun remains the singular lifecycle authority; prohibiting another authority narrows implementation choices and does not generalize the architecture.
</assumption_delta>

<interfaces>
`OfflineMigrationService._configured_destination_contract()` returns the destination handler, destination version dimensions, and target payload identity. `HandlerRegistry.resolve_payload_transformation()` already resolves one exact directed edge. `OfflineMigrationService.abort()` retires candidates returned only by existing authority-owned evidence and returns `AbortReceipt(run_id, deleted_entries, state)`. S3 snapshot and exact-delete operational failures are normalized as `CacheBlobBackendError`; `FileNotFoundError` proves absence. `CacheBlobLifecycleConflictError`, integrity/manifest errors, unsafe-path failures, and evidence mismatches are fail-closed outcomes rather than cleanup debt.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove a same-version format change reaches one exact transform</name>
  <files>src/cacheness/storage/migration.py, tests/test_migration_cutover.py</files>
  <read_first>
    - src/cacheness/storage/migration.py: `_configured_destination_contract`, compatibility classification, staging copy/transform branch, and candidate manifest construction
    - tests/test_migration_cutover.py: `_MigrationMcapHandler`, `_transforming_service`, existing version-changing transform test, and candidate-manifest assertions
    - src/cacheness/handlers.py: exact transformation resolution and store-local handler selection
    - src/cacheness/interfaces.py: `PayloadTransformationEdge` and path-based transformation contract
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: D-03, D-07, D-10, and D-12
  </read_first>
  <behavior>
    - "A destination handler declaring current identity `mcap-v2@1` while reading `mcap-v1@1` returns `mcap-v2@1` from `_configured_destination_contract`."
    - "Inspection classifies the exact `mcap-v1@1 -> mcap-v2@1` directed edge as executable rather than identity-compatible."
    - "Stage invokes the handler's declared transform exactly once; exact-copy logic is not selected merely because the numeric versions match."
    - "The verified/activated candidate manifest authenticates with payload format `mcap-v2`, version `1`, transformed bytes, and the handler's runtime metadata."
  </behavior>
  <action>Write `test_same_version_different_format_uses_exact_directed_transform_and_destination_manifest` first using a store-local MCAP-style handler that can read both `mcap-v1@1` and `mcap-v2@1`, declares only the exact directed edge between them, and counts transform calls. Make `_configured_destination_contract()` always form `target_identity` from the destination handler's validated `payload_format` and `payload_format_version`. Use `supports_payload_contract(source_format, source_version)` only to establish source readability in the existing compatibility/transform path; never substitute source format into the target tuple. Preserve exact byte copy only when the full source identity equals the declared target identity. Authenticate the staged and activated manifest in the test and assert destination format/version, digest/size, payload bytes, and exactly one transform. Retain D-03/D-10 handler ownership and the accepted pre-checkpoint invisible-orphan limit; add no converter registry, persisted version, compatibility reader, stream API, or participant exposure.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_migration_cutover.py::test_same_version_different_format_uses_exact_directed_transform_and_destination_manifest tests/test_migration_cutover.py::test_migration_executes_one_handler_transform_and_publishes_destination_manifest_identity tests/test_migration_plan_contract.py::test_compatibility_edge_requires_exact_destination_dimensions -x -o log_cli=false -o addopts=</automated>
  </verify>
  <acceptance_criteria>The same-version/different-format test fails before the implementation change, then proves one exact directed transform and an authenticated destination-format manifest without changing existing version-changing or exact-copy behavior.</acceptance_criteria>
  <done>Destination format identity is determined by the destination handler rather than by source readability or numeric-version equality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checkpoint typed S3 abort debt and report only completed deletions</name>
  <files>src/cacheness/storage/migration.py, tests/test_migration_cutover.py, tests/test_migration_remote_contract.py</files>
  <read_first>
    - src/cacheness/storage/migration.py: `abort`, `_retirement_digest`, attributed candidate loading, evidence replacement, and `AbortReceipt`
    - src/cacheness/storage/backends/s3_backend.py: `open_snapshot`, `delete_or_prove_absent`, and `CacheBlobBackendError` normalization
    - src/cacheness/error_handling.py: backend, integrity, lifecycle-conflict, and evidence-error hierarchy
    - tests/test_migration_cutover.py: attributed STAGING resume/abort and deletion-acknowledgement-loss tests
    - tests/test_migration_remote_contract.py: Moto-backed S3 client wrappers and non-live scope markers
    - .codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md: exact attributed deletion and typed-error boundary
  </read_first>
  <behavior>
    - "A deterministic S3 `open_snapshot` operational failure becomes one exact retirement digest in authenticated `STAGING` evidence and returns a partial `AbortReceipt` instead of escaping."
    - "A deterministic S3 exact-delete or absence-proof `CacheBlobBackendError` has the same debt result; no list call, adoption, or inferred absence occurs."
    - "After restoring the S3 participant, a second abort retries only the authority-attributed candidate, deletes or proves exact absence, clears debt, discards the unactivated authority candidate, and returns `ABORTED`."
    - "The partial receipt's `deleted_entries` equals only candidates deleted or proven absent in that call; integrity, manifest, ownership, unsafe-path, and evidence conflicts remain typed fail-closed exceptions."
  </behavior>
  <action>Write `test_s3_abort_typed_operational_failures_checkpoint_exact_debt_and_retry`, `test_partial_abort_receipt_counts_only_deleted_or_proven_absent_candidates`, and `test_abort_integrity_and_ownership_conflicts_remain_fail_closed` first. Use the real Moto-backed `S3BlobBackend`/`_S3GenerationIO` at the existing payload-participant seam; if an end-to-end `BlobStore` is constructed, pair it only with the established deterministic PostgreSQL authority fake rather than relabeling an unsupported S3+memory/SQLite topology. Inject one snapshot failure and one delete/absence-proof failure as `CacheBlobBackendError`, assert zero list calls, exact authority attribution, one bounded retirement digest, `STAGING`, and the actual completed count. Restore the participant and call abort with the same authenticated plan/run/evidence; assert exact cleanup settlement and terminal `ABORTED`. In production, classify only narrow operational cleanup errors (`OSError` and exact `CacheBlobBackendError`) as retryable debt while `FileNotFoundError` remains proven absence. Do not broadly catch `CacheStorageError`, `ValueError`, integrity errors, lifecycle conflicts, or evidence mismatches. Track per-call deleted-or-proven-absent candidates and use that count in `AbortReceipt`; preserve the candidate authority record until every attributed cleanup item settles. Add a fail-closed test for malformed/authentication or ownership disagreement so expanding the operational catch cannot hide tampering.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_migration_remote_contract.py::test_s3_abort_typed_operational_failures_checkpoint_exact_debt_and_retry tests/test_migration_cutover.py::test_partial_abort_receipt_counts_only_deleted_or_proven_absent_candidates tests/test_migration_cutover.py::test_abort_integrity_and_ownership_conflicts_remain_fail_closed tests/test_migration_cutover.py::test_resume_and_abort_staging_use_only_authority_attributed_batches -x -o log_cli=false -o addopts=</automated>
  </verify>
  <acceptance_criteria>The named tests prove both S3 operational failure sites checkpoint exact bounded debt, retry without listing/adoption, converge after participant restoration, report truthful completed counts, and leave integrity/ownership failures fail closed.</acceptance_criteria>
  <done>All authority-attributed migration-abort effects have accurate typed outcomes and a deterministic retry path across the deterministic S3 participant contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source payload identity -> destination handler contract | A readable source contract cannot redefine the destination's declared persisted identity. |
| authenticated candidate evidence -> S3 participant | Only authority-attributed exact candidates may be observed or deleted; remote errors cannot be mistaken for absence. |
| participant outcome -> public abort receipt | Retryable failures and completed deletions must remain distinguishable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-20-01 | Tampering | Destination payload identity | high | mitigate | Derive target format/version only from the destination handler and authenticate the resulting target manifest. |
| T-07-20-02 | Spoofing | S3 candidate cleanup | high | mitigate | Load candidates only from existing authority-owned evidence and retry exact locators without listings or adoption. |
| T-07-20-03 | Tampering | Abort exception classification | high | mitigate | Normalize only `OSError` and exact `CacheBlobBackendError` as operational debt; preserve integrity, ownership, path, and evidence failures. |
| T-07-20-04 | Repudiation | `AbortReceipt.deleted_entries` | medium | mitigate | Count only candidates deleted or proven absent during the current call and retain explicit debt for every pending candidate. |
</threat_model>

<verification>
- The four exact task selectors pass in the isolated frozen all-extras environment.
- Existing version-changing transform, attributed STAGING recovery, and deterministic S3 participant tests remain green.
- Tests separate integrity failures, recovery/debt settlement, progress outcomes, and performance non-claims.
</verification>

<success_criteria>
- Same numeric version cannot collapse a different target format into an identity copy.
- Typed S3 snapshot/delete failures produce authenticated exact cleanup debt and a retryable `STAGING` outcome.
- Restored participant access lets a later attributed abort settle debt and reach `ABORTED`.
- Partial abort receipts report only completed deletions/absence proofs.
- Integrity and ownership conflicts remain fail closed.
</success_criteria>

<source_coverage>
- GOAL: Preserves explicit stopped-worker migration with a truthful target format and deterministic attributed cleanup.
- REQ: MIGR-04 and MIGR-05.
- RESEARCH: Independent payload dimensions, handler-owned transforms, exact immutable deletion, typed topology outcomes, and one authority.
- CONTEXT: D-03, D-07, D-10, D-12, D-16, D-19, D-20, D-21, and D-22.
- REVIEW: CR-01 and CR-02 are closed; WR-01 is corrected in the same abort seam.
- EXCLUDED: WR-02 handler `name=` alias behavior is unrelated and is explicitly deferred by Plan 07-22; obstore adoption, live services, and performance remain outside this plan.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-20-SUMMARY.md` when done.
</output>
