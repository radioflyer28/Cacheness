---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 24
type: execute
wave: 20
depends_on:
  - 07-23
files_modified:
  - tools/verify_phase7_contracts.py
  - tests/test_phase7_contract_verifier.py
autonomous: true
gap_closure: true
requirements:
  - MIGR-05
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "`SECURITY_THREAT_NODES['T-07-21-03']` preserves the forged-debt selector and also names `test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts`, binding the shipped terminal-state recovery invariant to exact executable MIGR-05 evidence (D-16, D-19, D-20, D-21)."
    - "`test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly` asserts the complete ordered `T-07-21-03` selector tuple, so deleting either required selector fails closed."
    - "The existing current-three-gap exact-map assertion agrees with the expanded two-selector tuple, and the fixed Phase 7 quick verifier passes without changing its reviewed Plan 01-23 inventory or 54-gap-threat/100-total-threat counts."
  artifacts:
    - path: tools/verify_phase7_contracts.py
      provides: "Complete exact selector ownership for the existing Plan 21 terminal-state threat"
    - path: tests/test_phase7_contract_verifier.py
      provides: "Fail-closed complete-tuple assertions for the Plan 23 cross-plan threat binding"
  key_links:
    - from: tools/verify_phase7_contracts.py
      to: tests/test_rebuild_workflow.py
      via: "`SECURITY_THREAT_NODES['T-07-21-03']` lists both the older forged-debt test and the shipped forward-fence/resume regression"
      pattern: "T-07-21-03.*test_rebuild_cleanup_retry_rejects_forged_debt_without_payload_access.*test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts"
    - from: tests/test_phase7_contract_verifier.py
      to: tools/verify_phase7_contracts.py
      via: "the exact Plan 23 mapping test compares the complete ordered tuple instead of checking only membership"
      pattern: "SECURITY_THREAT_NODES.*T-07-21-03"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No production lifecycle source file changes are permitted; shipped cleanup-debt behavior is already verified."
    - "[FLAGGED/UNVERIFIED] No lifecycle state, authority, lock, queue, lease, journal, sidecar, listing/adoption rule, stronger atomicity claim, or production obstore adoption may be introduced."
    - "[FLAGGED/UNVERIFIED] No work on the pre-existing Phase 3 SQLite fresh-root contention test may enter this plan."
    - "[FLAGGED/UNVERIFIED] The optional state-matrix warning from `07-REVIEW.md` is not a verification-report gap and remains outside this closure."
    - "[FLAGGED/UNVERIFIED] Fixed Plan 01-23 paths, gap-threat ownership, threat counts, requirement maps, decision maps, assumption maps, descriptor-less prohibitions, and Phase 8 qualification boundaries remain unchanged except for the one `T-07-21-03` tuple."
---

<objective>
Make the fixed Phase 7 verifier fail closed on the complete existing `T-07-21-03` evidence binding.

Purpose: Close the sole independent-verification blocker for MIGR-05 without revisiting already verified production cleanup-debt behavior or changing ADR 0001 lifecycle guarantees.
Output: One exact selector-table edit, matching verifier self-test assertions, and green focused/quick verification.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-PATTERNS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-23-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-23-SUMMARY.md
@.codex/skills/spike-findings-cacheness/SKILL.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@tools/verify_phase7_contracts.py
@tests/test_phase7_contract_verifier.py
</context>

<adr_0001_contract>
- Supported evidence remains the deterministic fixed Phase 7 verifier; live PostgreSQL/Amazon-S3, Windows, supported-Python, packaging, performance, and production-obstore qualification remain Phase 8 or later.
- Integrity/recovery behavior is already shipped: one metadata lifecycle authority remains primary, exact receipt-bound debt remains external cleanup work, and authority promotion remains the visibility point.
- This plan changes verifier evidence wiring only. It creates no external effect, transaction, lifecycle state, authority method, progress guarantee, or cross-resource atomicity claim.
- Stop if implementation would require production lifecycle changes or any new lock, queue, lease, journal, sidecar, listing/adoption source, or second authority.
</adr_0001_contract>

<flagged_assumptions>
- `[FLAGGED/UNVERIFIED A-MIGR03]` The executed non-mutating inventory and human/machine plan remain the Phase 7 MIGR-03 interpretation; this plan does not change or newly claim them.
- `[FLAGGED/UNVERIFIED A-MIGR04]` The executed offline copy-verify-switch workflow remains the Phase 7 MIGR-04 interpretation; this plan does not change compatibility or cutover behavior.
- `[FLAGGED/UNVERIFIED A-MIGR05]` The exact forward-fence/resume regression is necessary evidence for the existing Plan 21 terminal-state threat in addition to the older forged-debt regression; the verification report identifies this exact missing edge.
- `[FLAGGED/UNVERIFIED A-MIGR06]` The executed explicit rebuild remains the Phase 7 MIGR-06 interpretation; no rebuild behavior changes here.
</flagged_assumptions>

<assumption_delta>
Decision: no-change. The detector matched text rejecting a second or another authority; the design still has one primary metadata lifecycle authority, and this verifier-only closure does not alter that noun or boundary.
</assumption_delta>

<interfaces>
`SECURITY_THREAT_NODES` is the literal threat-to-pytest-selector mapping consumed by `validate_fixed_manifest()`. Its existing `T-07-21-03` value is a one-element tuple containing `tests/test_rebuild_workflow.py::test_rebuild_cleanup_retry_rejects_forged_debt_without_payload_access`. The complete required value appends `tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts` in that order. `test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly` is the Plan 23 exact contract test; `test_fixed_manifest_maps_current_three_gap_repairs_exactly` also asserts the old tuple and must be kept consistent so the fixed quick verifier remains green.
</interfaces>

## Artifacts this phase produces

- No new production module, public API, persisted schema, lifecycle state, authority method, handler contract, or backend behavior.
- An expanded two-selector `SECURITY_THREAT_NODES['T-07-21-03']` tuple in `tools/verify_phase7_contracts.py`.
- Exact complete-tuple regression assertions in `tests/test_phase7_contract_verifier.py`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind the exact forward-fence regression to the existing terminal-state threat</name>
  <files>tools/verify_phase7_contracts.py, tests/test_phase7_contract_verifier.py</files>
  <read_first>
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md: the sole `gaps` entry and its two missing items
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md: WR-01 as the required mapping repair; WR-02 is explicitly outside this plan
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-23-PLAN.md: the promised `T-07-21-03` cross-binding and fixed 23-plan/54-gap-threat/100-total-threat contract
    - tools/verify_phase7_contracts.py: `SECURITY_THREAT_NODES['T-07-21-03']`; do not alter unrelated inventory or selector maps
    - tests/test_phase7_contract_verifier.py: `test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly` and `test_fixed_manifest_maps_current_three_gap_repairs_exactly`
    - docs/adr/0001-topology-specific-storage-guarantees.md: one authority, cleanup-debt recovery, topology-specific claims, and stop conditions
  </read_first>
  <behavior>
    - "Before implementation, the exact Plan 23 mapping self-test is insufficient because it does not assert `T-07-21-03`; after implementation it asserts the tuple `(forged_debt, forward_fence)` exactly."
    - "The fixed mapping retains `test_rebuild_cleanup_retry_rejects_forged_debt_without_payload_access` and adds `test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts` as the second `T-07-21-03` selector."
    - "Removing either selector from `T-07-21-03`, or replacing either with an adjacent test, fails the exact Plan 23 self-test."
    - "The current-three-gap exact-map test expects the same complete tuple, preventing its prior one-selector equality from breaking the fixed quick verifier."
  </behavior>
  <action>In `tools/verify_phase7_contracts.py`, change only the value of `SECURITY_THREAT_NODES['T-07-21-03']`: preserve `tests/test_rebuild_workflow.py::test_rebuild_cleanup_retry_rejects_forged_debt_without_payload_access` as the first tuple member and append `tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts` as the second. This exact evidence link retains D-16: external cleanup remains attributable debt, D-19: explicit resume is the settlement path, D-20: invalid evidence fails closed, and D-21: cleanup authority remains exact and run-owned. In `tests/test_phase7_contract_verifier.py`, define or reuse the exact `forged_debt` and `forward_fence` selector strings in `test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly`, then assert equality with the complete ordered two-member tuple so either omission fails. Update the already affected equality in `test_fixed_manifest_maps_current_three_gap_repairs_exactly` to that same ordered tuple; this is required because it currently asserts the superseded one-member value. Do not change `PHASE7_PLAN_PATHS`, `_PHASE7_REVIEWED_PLAN_PATHS`, requirement/decision/assumption maps, gap-plan threat IDs/owners, counts, fixed pytest-node composition, production files, lifecycle tests, `07-COVERAGE.md`, or the optional WR-02 state-matrix warning.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase7_contract_verifier.py::test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly -x -o log_cli=false -o addopts= &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --quick</automated>
  </verify>
  <acceptance_criteria>`SECURITY_THREAT_NODES['T-07-21-03']` equals the exact ordered `(forged_debt, forward_fence)` tuple; both exact-map tests assert that complete tuple; the exact Plan 23 self-test and fixed Phase 7 quick verifier exit zero; no file outside the two listed verifier artifacts changes.</acceptance_criteria>
  <done>The independent verifier can no longer report the existing terminal-state threat satisfied after either its forged-debt evidence or its forward-fence/resume evidence is removed.</done>
  <reversibility rating="reversible">This is a local literal selector/test-contract correction with no persisted or production behavior; reverting touches only the two verifier artifacts.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixed threat map -> exact pytest selector | A literal selector is accepted as evidence for a reviewed threat only when the exact test function exists and executes. |
| Plan 23 claim -> Plan 21 terminal-state threat | The newer forward-fence/resume regression must corroborate the older terminal-state threat without replacing its forged-debt evidence. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-21-03 | Repudiation | Rebuild terminal-state evidence | high | mitigate | Preserve the forged-debt selector and add the exact forward-fence/resume selector to the existing literal threat mapping. |
| T-07-23-04 | Spoofing | Fixed MIGR-05 completion | high | mitigate | Make the exact Plan 23 self-test compare the complete ordered `T-07-21-03` tuple so either missing selector fails closed. |
</threat_model>

<verification>
- `uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase7_contract_verifier.py::test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly -x -o log_cli=false -o addopts=` passes and directly proves the repaired cross-plan mapping contract.
- `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --quick` passes with the existing Plan 01-23, 54-gap-threat, and 100-total-threat fixed inventories.
- `git diff --name-only` for the execution commit lists only `tools/verify_phase7_contracts.py` and `tests/test_phase7_contract_verifier.py`; no production lifecycle source or Phase 3 SQLite contention test changes are accepted.
</verification>

<success_criteria>
- The older forged-debt selector remains mapped to `T-07-21-03`.
- The exact forward-fence/resume selector is also mapped to `T-07-21-03`.
- The exact Plan 23 self-test compares the complete ordered tuple and therefore catches removal of either selector.
- The pre-existing exact Plan 21 assertion is consistent with the expanded tuple.
- The exact self-test and fixed Phase 7 quick verifier pass with no production or optional state-matrix changes.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID/item | Plan | Status | Notes |
|---|---|---|---|---|
| GOAL | Explicit stopped-worker migration/rebuild without silent mutation or loss of the only valid copy | 07-01 through 07-23 | COVERED | Shipped production behavior is verified; 07-24 closes only its independent fixed-verifier edge. |
| REQ | MIGR-03 non-mutating human/machine inventory and plans | 07-01 through 07-23 | COVERED | Existing executed behavior; unclassified SPEC probe retained as an assumption, not new scope. |
| REQ | MIGR-04 explicit same-backend offline copy-verify-switch | 07-01 through 07-23 | COVERED | Existing executed behavior; unclassified SPEC probe retained as an assumption, not new scope. |
| REQ | MIGR-05 authenticated interruption recovery without loss/adoption | 07-24 | COVERED | Binds the already passing exact forward-fence/resume regression to the applicable terminal-state threat and protects both selectors with equality assertions. |
| REQ | MIGR-06 explicit confirmed handler-backed rebuild | 07-01 through 07-23 | COVERED | Existing executed behavior; unclassified SPEC probe retained as an assumption, not new scope. |
| RESEARCH | One lifecycle authority, exact cleanup debt, and topology-specific guarantees | 07-01 through 07-23 | COVERED | 07-24 changes evidence mapping only and preserves all architectural boundaries. |
| RESEARCH | Live/platform/performance qualification and production obstore | Phase 8 or later | EXCLUDED | Explicitly outside this deterministic verifier closure. |
| CONTEXT | D-01 through D-15 | 07-01 through 07-23 | COVERED | No phase decision is reopened. |
| CONTEXT | D-16, D-19, D-20, D-21 | 07-24 | COVERED | The exact regression that proves these cleanup/resume decisions is bound to the applicable existing threat. |
| CONTEXT | D-17, D-18, D-22 | 07-01 through 07-23 | COVERED | Evidence representation and signing boundaries remain unchanged. |
| CONTEXT | Deferred ideas | — | EXCLUDED | None declared in `07-CONTEXT.md`. |
| REVIEW | WR-01 missing Plan 21 threat binding | 07-24 | COVERED | This is the verification report's sole actionable gap. |
| REVIEW | WR-02 optional state-matrix warning | — | EXCLUDED | The verification report does not require it, and the user explicitly kept it outside this plan. |
| SPEC PROBE | MIGR-03, MIGR-04, MIGR-05, MIGR-06 are unclassified/unresolved | 07-24 assumptions | COVERED | All four are surfaced explicitly; only MIGR-05 receives a new concrete closure truth from `07-VERIFICATION.md`. |
| PROHIBITIONS | ADR 0001 and project-value guardrails | 07-24 | COVERED | Retained as descriptor-less `[FLAGGED/UNVERIFIED]` statements without invented fallback checks. |

<source_coverage>
All GOAL, REQ, RESEARCH, and CONTEXT items are covered by the executed Phase 7 plan set or this narrow MIGR-05 verifier closure. No deferred item, optional review warning, production lifecycle change, or Phase 3 contention work is included.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-24-SUMMARY.md` when done.
</output>
