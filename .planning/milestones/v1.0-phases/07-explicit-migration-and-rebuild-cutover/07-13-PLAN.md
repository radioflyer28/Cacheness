---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 13
type: execute
wave: 9
depends_on:
  - 07-11
files_modified:
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/backends/postgresql_lifecycle_authority.py
  - tests/contracts/test_postgresql_lifecycle_authority.py
autonomous: true
requirements:
  - MIGR-04
  - MIGR-05
gap_closure: true
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh PostgreSQL-backed BlobStore that loads persisted `activated_offline` state fails closed before payload materialization or readiness per D-12 and D-20."
    - "The post-load worker fence uses the canonical persisted authority state and cannot be bypassed through `preflight_mutation`."
    - "Persisted `idle` and `active` states continue through the existing initialization contract without adding local coordination or live-service claims."
  artifacts:
    - path: src/cacheness/storage/blob_store.py
      provides: "Post-identity-load canonical authority fence before materialization"
    - path: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      provides: "Post-open ordinary-worker fence in the PostgreSQL authority preflight"
    - path: tests/contracts/test_postgresql_lifecycle_authority.py
      provides: "Deterministic fresh-construction activated-offline regression coverage"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      via: "initialize/preflight loads identity, then checks persisted publication state"
    - from: src/cacheness/storage/blob_store.py
      to: payload materialization
      via: "authority fence must complete before materialization and `_initialized = True`"
---

<objective>
Close the fresh PostgreSQL-backed BlobStore initialization fence gap after persisted identity is loaded.

Purpose: A newly constructed ordinary worker must not materialize or report readiness when the canonical PostgreSQL authority already records `activated_offline`.
Output: Post-load initialization fence, authority preflight enforcement, and deterministic adapter-backed tests.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-11-SUMMARY.md
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/backends/postgresql_lifecycle_authority.py
@tests/contracts/test_postgresql_lifecycle_authority.py
</context>

<planner_assumptions>
- No Phase 7 SPEC exists. MIGR-04 and MIGR-05 are resolved from D-12, D-19, D-20, the authoritative verification blocker, and ADR 0001's PostgreSQL authoritative-coordination contract.
- The tests remain deterministic and adapter-backed. PostgreSQL live qualification belongs to Phase 8 and is not claimed here.
- This repair reuses the canonical backend authority; it introduces no lockfile, lease, local lock, sidecar, queue, or second lifecycle authority.
- The obstore spike confirms that a payload participant cannot replace this fence: persisted PostgreSQL metadata authority remains the sole visibility state. This plan changes no payload backend, package dependency, handler API, or S3 publication policy.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fence fresh BlobStore initialization after PostgreSQL identity load</name>
  <files>src/cacheness/storage/blob_store.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    - src/cacheness/storage/blob_store.py: `BlobStore.initialize`, `_require_canonical_store`, authority initializer, and materialization order
    - src/cacheness/storage/backends/postgresql_lifecycle_authority.py: `PostgresqlLifecycleAuthority` identity load and `publication_state` behavior when identity is absent versus present
    - tests/contracts/test_postgresql_lifecycle_authority.py: fresh authority provisioning and activated-offline worker-fence fixtures
    - docs/adr/0001-topology-specific-storage-guarantees.md: stopped-worker and PostgreSQL authoritative-state requirements
    - .codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md: metadata-authority visibility and external-participant boundary
  </read_first>
  <behavior>
    - Test `test_fresh_blobstore_initialize_rechecks_activated_offline_after_postgresql_identity_load`: a fresh object initially sees no in-memory identity, loads a persisted identity whose publication state is `activated_offline`, raises the existing typed maintenance fence, performs no payload materialization, and leaves `_initialized` false.
    - The same test proves the persisted authority is queried after identity initialization and before key-provider setup or destination materialization.
  </behavior>
  <action>Write the failing fresh-construction test first. Reorder `BlobStore.initialize()` so the authority initializer/open step loads canonical identity, then invoke the existing ordinary-worker canonical-store fence before payload materialization, key-provider readiness, or `_initialized = True`. Keep the current decorator fence as an early check for already-loaded authorities, but do not treat its synthetic pre-identity `idle` observation as sufficient. Preserve typed error propagation and cleanup so a rejected initialization cannot leave a half-ready store. Use the canonical PostgreSQL authority only; do not introduce any process-local coordination mechanism or availability promise beyond ADR 0001.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/contracts/test_postgresql_lifecycle_authority.py::test_fresh_blobstore_initialize_rechecks_activated_offline_after_postgresql_identity_load</automated>
  </verify>
  <acceptance_criteria>A fresh store with persisted `activated_offline` state fails with the canonical typed fence before materialization/readiness and remains uninitialized.</acceptance_criteria>
  <done>Fresh PostgreSQL-backed initialization cannot pass readiness on a synthetic pre-identity state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make PostgreSQL mutation preflight enforce the persisted worker fence</name>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    - src/cacheness/storage/backends/postgresql_lifecycle_authority.py: `PostgresqlLifecycleAuthority.preflight_mutation`, `open`, `require_ordinary_worker_access`, and publication-state reads
    - tests/contracts/test_postgresql_lifecycle_authority.py: active, idle, and activated-offline authority tests
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: D-12 and D-20 fail-closed lifecycle rules
  </read_first>
  <behavior>
    - Test `test_postgresql_preflight_mutation_checks_persisted_worker_fence_after_open`: preflight opens/loads identity and then rejects persisted `activated_offline` through the canonical ordinary-worker guard.
    - Test proves persisted `idle` and `active` states still satisfy preflight, while malformed/unreadable authority state fails closed through the existing error taxonomy.
  </behavior>
  <action>After `PostgresqlLifecycleAuthority.preflight_mutation()` opens and loads the canonical identity, call the existing ordinary-worker access guard against that persisted state. Avoid recursion between `open`, `publication_state`, preflight, and the guard by keeping state reads side-effect-free once identity is loaded. Ensure BlobStore's post-load check and direct preflight callers converge on the same guard and typed errors. This is an authority-fence repair, not an online writer protocol or a live PostgreSQL qualification.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/contracts/test_postgresql_lifecycle_authority.py::test_postgresql_preflight_mutation_checks_persisted_worker_fence_after_open tests/contracts/test_postgresql_lifecycle_authority.py::test_fresh_blobstore_initialize_rechecks_activated_offline_after_postgresql_identity_load</automated>
  </verify>
  <acceptance_criteria>Every PostgreSQL mutation preflight observes persisted post-open state, rejects `activated_offline`, and accepts the existing ordinary-worker states without recursion.</acceptance_criteria>
  <done>The canonical PostgreSQL authority fence covers both fresh BlobStore initialization and direct mutation preflight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL authority -> process | Persisted identity and lifecycle state replace synthetic pre-open state |
| initialization -> payload materialization | Worker readiness is granted only after the canonical authority fence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13-01 | Elevation of Privilege | fresh `BlobStore.initialize` | high | mitigate | Re-check ordinary-worker access after canonical identity load and before materialization/readiness. |
| T-07-13-02 | Tampering | persisted publication state | high | mitigate | Read state through the existing PostgreSQL authority contract and fail closed on malformed/unreadable values. |
| T-07-13-03 | Spoofing | synthetic pre-identity `idle` state | high | mitigate | Treat the early observation only as a fast check; persisted post-open state is authoritative. |
| T-07-13-04 | Denial of Service | guard recursion | medium | mitigate | Keep post-open state inspection side-effect-free and test active/idle success paths. |
| T-07-13-SC | Tampering | dependency supply chain | low | accept | This plan installs no packages and uses the frozen checked-in dependency graph. |
</threat_model>

<verification>
- Both exact fresh-initialization and preflight selectors pass.
- `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/contracts/test_postgresql_lifecycle_authority.py` passes.
- Tests assert call ordering and absence of payload materialization, not a live PostgreSQL availability property.
</verification>

<success_criteria>
- Persisted `activated_offline` always fences a freshly constructed ordinary worker after identity load.
- Rejection occurs before materialization and readiness.
- PostgreSQL preflight uses the same canonical guard.
- No new coordination mechanism or live-service claim is introduced.
</success_criteria>

## Artifacts this phase produces

- Post-identity-load call to the existing ordinary-worker guard inside `BlobStore.initialize()` before key-provider/payload materialization and `_initialized = True`.
- Post-`open()` call to the same existing guard inside `PostgresqlLifecycleAuthority.preflight_mutation()`.
- Deterministic regression tests for `activated_offline`, `idle`, and `active` initialization paths.

<source_coverage>
- GOAL: Prevents an ordinary worker from silently proceeding through an offline activation state.
- REQ: MIGR-04 and MIGR-05.
- RESEARCH: PostgreSQL `activated_offline` worker fence and authoritative identity load.
- CONTEXT: D-12, D-19, and D-20.
- EXCLUDED: Live PostgreSQL qualification remains Phase 8; WR-01 and WR-02 are unrelated follow-up warnings.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-13-SUMMARY.md` when done.
</output>
