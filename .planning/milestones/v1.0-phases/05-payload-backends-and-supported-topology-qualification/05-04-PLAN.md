---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "04"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/cacheness/storage/backends/postgresql_lifecycle_authority.py
  - tests/contracts/test_postgresql_lifecycle_authority.py
autonomous: true
requirements: [BACK-04, BACK-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PostgreSQL is a real LifecycleAuthority with explicit exact-version initialization and no DDL/migration during ordinary open (D-14, D-17)."
    - "Prepare, verification, promotion, abort, manifest/catalog fields, and cleanup debt change only inside short PostgreSQL transactions with exact CAS (D-15, D-18)."
    - "The adapter exposes semantic lifecycle types only and never stages, publishes, verifies, lists, or deletes S3 objects."
  artifacts:
    - path: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      provides: "Direct psycopg PostgreSQL lifecycle authority and versioned schema"
    - path: tests/contracts/test_postgresql_lifecycle_authority.py
      provides: "Driver-boundary unit contracts for initialization and mutation CAS"
  key_links:
    - from: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "Implements semantic LifecycleAuthority values and transitions"
      pattern: "LifecycleAuthority|PreparedMutation|PromotionResult"
  prohibitions:
    - "No SQLAlchemy metadata-backend reuse, projection authority, S3 calls, advisory locks, queue, global connection, or second lifecycle sequence."
    - "No implicit schema creation/migration on construction/open and no compatibility translation of development-only layouts."
    - "No dynamic SQL identifier interpolation or secret-bearing error context."
---

<objective>
Implement the transactional visibility half of the remote reference topology.

Purpose: Give the existing engine one multi-host authority whose exact CAS and durable intent/debt semantics match the already-proven local authority contract.
Output: Versioned PostgreSQL schema, explicit initialization, and core mutation transitions behind `LifecycleAuthority`.
</objective>

## Artifacts this phase produces

- `PostgresqlLifecycleAuthority` with explicit `initialize()` and read-only compatibility validation.
- Current PostgreSQL authority schema/store identity version constants for future Phase 7 tooling.
- Core read/expectation/prepare/verify/promote/abort contract tests at the driver boundary.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-PATTERNS.md
@src/cacheness/storage/lifecycle_authority.py
@src/cacheness/storage/sqlite_lifecycle_authority.py
@src/cacheness/storage/memory_lifecycle_authority.py
</context>

<source_coverage>GOAL: PostgreSQL authority for the remote pairing. REQ: PostgreSQL portions of BACK-04/BACK-05. RESEARCH: direct psycopg, short transactions, exact CAS, explicit schema. CONTEXT: D-14, D-15, D-16, D-17, D-18.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Explicitly initialize and reopen one versioned PostgreSQL authority</name>
  <reversibility rating="costly">The PostgreSQL schema and persisted authority capability contract become Phase 7 migration inputs per D-14.</reversibility>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/backends/postgresql_backend.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "Construction performs no DDL; `initialize()` creates the exact current schema idempotently under a bounded database-only uniqueness/DDL race."
    - "Reopen validates schema version, store identity, table/constraint shape, and capability declaration without mutation."
    - "Older, newer, malformed, or foreign layouts raise `CacheBlobMigrationRequiredError` and remain unchanged."
    - "Connections are leased per transaction from an injected factory/pool and are never shared across fork by the authority."
  </behavior>
  <action>Implement the new module with guarded optional psycopg imports, an injected connection factory/pool, a validated schema identifier composed only with `psycopg.sql.Identifier`, fixed table/column SQL, parameterized values, and bounded transaction-local statement/lock timeouts. Model tables after the semantic SQLite state: store identity/version, authority revision, entries, mutations/verification, cleanup debt, clear/reconciliation state, and projection revisions. Constructor/open may inspect existing state read-only but must not create or upgrade. `initialize()` is explicit and idempotent; it creates only the exact current schema and rejects incompatible/foreign state with the migration-required family. Use fake driver transcripts to assert SQL composition, no constructor DDL, idempotent initialization, exact version detection, rollback, and secret-redacted error context. Do not reuse the Phase 4 PostgreSQL projection class as authority.</action>
  <acceptance_criteria>A clean schema can be explicitly initialized and reopened; every incompatible/malformed version fails typed without mutation; no connection, cursor, URL, or secret leaks into public state/errors.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra postgresql pytest -q tests/contracts/test_postgresql_lifecycle_authority.py -x -o log_cli=false</automated></verify>
  <done>The remote authority has an explicit, versioned, future-migratable transaction boundary before lifecycle mutations exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact prepare-verify-promote-abort transitions in PostgreSQL</name>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>tests/test_lifecycle_authority_contract.py</file>
  </read_first>
  <behavior>
    - "Read/expectation returns exact lineage, revision, generation, digest, locator, and canonical manifest bytes."
    - "Prepare is operation-idempotent and enforces every expected field with unique constraints/conditional SQL."
    - "Verification proof and promotion require the exact prepared mutation; zero affected rows is a lifecycle conflict."
    - "Promotion commits canonical manifest/catalog values, authority revision, mutation state, and old-generation cleanup debt together."
    - "Abort creates attributable candidate cleanup debt when publication may have occurred and is idempotent."
  </behavior>
  <action>Port semantic behavior, not SQLite SQL. Implement preflight, read_entry, read_expectation, snapshot_state, prepare_mutation, record_verification, promote_mutation, and abort_mutation using one short transaction per transition. Use unique operation/key constraints, `INSERT ... ON CONFLICT` where idempotency requires it, and exact conditional `UPDATE ... RETURNING` predicates for CAS. Store canonical manifest bytes and authoritative catalog values in the same promotion transaction; record cleanup debt in that transaction for superseded generations and in abort for possibly published candidates. On uncertain commit, reopen through a fresh lease and classify the exact operation identity rather than guessing. Return only existing lifecycle dataclasses. Add driver-level concurrent transcript tests for stale expectations, replay, rollback, and uncertain commit classification.</action>
  <acceptance_criteria>Every core transition is exact-CAS/idempotent, canonical descriptor plus catalog plus debt is transactional inside PostgreSQL, and no external payload effect occurs in the adapter.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra postgresql pytest -q tests/contracts/test_postgresql_lifecycle_authority.py tests/test_lifecycle_authority_contract.py -x -o log_cli=false</automated></verify>
  <done>PostgreSQL can own remote visibility and durable intent/debt while the engine retains all cross-resource ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Configuration -> PostgreSQL schema/connection | Identifiers and credentials require safe composition and redaction. |
| Engine transition -> SQL transaction | Expected generation and canonical bytes must commit exactly or conflict. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-14 | Tampering | SQL identifier/value construction | high | mitigate | Fixed SQL plus `sql.Identifier` and bound values; transcript tests reject interpolation. |
| T-05-15 | Spoofing | authority schema/store identity | high | mitigate | Exact version, identity, shape, and capability validation before mutation. |
| T-05-16 | Tampering | CAS/promotion | high | mitigate | Unique constraints and full expected-field predicates with `RETURNING`. |
| T-05-17 | Information disclosure | driver errors | high | mitigate | Allow-listed bounded context and preserved cause without DSN/query parameters. |
| T-05-18 | Elevation of privilege | projection placeholder | high | mitigate | New class implements authority protocol directly; projection class is never adapted or promoted. |

No dependency is added; psycopg is already declared/locked under the PostgreSQL extra.
</threat_model>

<verification>Run the driver contract and inspect the module for boto3/S3 imports, advisory-lock calls, string-formatted identifiers, constructor DDL, and unbounded retry loops; each must be absent. Verify schema/version constants are independently addressable for Phase 7.</verification>

<success_criteria>
- PostgreSQL initialization is explicit, idempotent, versioned, and non-migrating.
- Core lifecycle transitions are exact, atomic within PostgreSQL, and recoverable across uncertain commits.
- The authority has no payload mechanics or second lifecycle sequence.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-04-SUMMARY.md` when done.</output>
