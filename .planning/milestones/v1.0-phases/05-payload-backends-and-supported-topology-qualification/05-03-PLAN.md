---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "03"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/cacheness/storage/backends/s3_backend.py
  - tests/test_s3_blob_backend.py
  - tests/contracts/test_s3_generation_io.py
autonomous: true
requirements: [BACK-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "S3 supplies the existing generation-I/O primitive and never sequences authority transitions (BACK-01; D-09)."
    - "Single and multipart publication are conditional and immutable; ambiguous responses are classified by exact-key digest/size verification (D-09, D-10)."
    - "Reads stream into one contained mode-0600 snapshot under byte/work bounds and close the network body before handler access (D-11)."
    - "Inventory is continuation-token based and bounded; deletes distinguish confirmed absence from typed ambiguous/configuration failures (D-12, D-13)."
  artifacts:
    - path: src/cacheness/storage/backends/s3_backend.py
      provides: "Amazon S3 payload participant and guarded generation-I/O implementation"
      contains: "materialize_handler_io"
    - path: tests/contracts/test_s3_generation_io.py
      provides: "Deterministic S3 single/multipart, ambiguity, bounds, and containment contracts"
  key_links:
    - from: src/cacheness/storage/backends/s3_backend.py
      to: src/cacheness/storage/composition.py
      via: "S3 participant satisfies PayloadGenerationIOProvider with truthful capabilities"
      pattern: "materialize_handler_io|topology_capabilities"
    - from: src/cacheness/storage/backends/s3_backend.py
      to: src/cacheness/storage/lifecycle.py
      via: "The engine invokes five payload primitives and remains the only sequencer"
      pattern: "publish_generation|open_snapshot|delete_or_prove_absent"
  prohibitions:
    - "No ETag-as-integrity, overwrite-capable publication, whole-object buffering, unbounded paginator accumulation, or swallowed ClientError."
    - "No custom endpoint or compatible-service support claim; this participant's qualified target is Amazon S3 per D-08."
    - "No authority mutation call, S3-derived visibility decision, global retry scheduler, or second lifecycle coordinator."
    - "No legacy direct byte-CRUD compatibility surface retained solely for pre-production callers."
---

<objective>
Replace the old direct S3 CRUD backend with a bounded immutable-generation participant for the shared engine.

Purpose: Supply secure S3 mechanics without reproducing BlobStore lifecycle sequencing or overstating service evidence.
Output: S3 generation I/O with conditional publication, contained reads, exact deletion, bounded inventory, and deterministic contract tests.
</objective>

## Artifacts this phase produces

- `S3BlobBackend.materialize_handler_io()` and its private S3 generation-I/O object.
- Typed immutable publication, private snapshot, bounded inventory, and deletion semantics.
- Deterministic fake/Moto contract coverage that is explicitly not BACK-05 evidence.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-PATTERNS.md
@src/cacheness/storage/backends/s3_backend.py
@src/cacheness/storage/guarded_handler_io.py
@src/cacheness/storage/lifecycle.py
</context>

<source_coverage>GOAL: S3 through the shared engine. REQ: S3 portion of BACK-01. RESEARCH: low-level conditional PutObject/multipart, contained verified reads, bounded listing, typed errors. CONTEXT: D-08 through D-13 and D-18.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish and read one conditional S3 generation through the payload seam</name>
  <reversibility rating="one-way">Immutable generation locators and publication semantics are persisted and governed by future Phase 7 migration tooling; D-09 already locks this decision.</reversibility>
  <files>src/cacheness/storage/backends/s3_backend.py, tests/contracts/test_s3_generation_io.py</files>
  <read_first>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>src/cacheness/storage/backends/blob_backends.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>tests/test_s3_blob_backend.py</file>
  </read_first>
  <behavior>
    - "The participant uses standard boto3 credential resolution or an injected client, an exact bucket/prefix, optional expected owner, bounded settings, and a private staging root."
    - "A small staged artifact is uploaded with `IfNoneMatch='*'`; an existing generation cannot be overwritten."
    - "An exact object is streamed to a contained mode-0600 local snapshot with enforced content-length/byte/work limits and a closed StreamingBody."
    - "The lifecycle engine verifies canonical SHA-256 and size before the handler reads the snapshot; ETag is diagnostic only."
  </behavior>
  <action>Write a failing Moto/fake-client tracer that stages a native handler file, conditionally publishes one unique generation, opens one private snapshot, and verifies the engine's manifest digest/size before deserialization. Replace the old generic direct byte CRUD implementation instead of wrapping it for compatibility. Keep `S3BlobBackend` as the payload participant, but narrow construction to Amazon S3 service configuration or an injected boto3 client; remove inline-secret and generic endpoint behaviors that would imply unsupported services. Validate bucket, normalized managed prefix, relative generation locator, suffix/text limits, staging-root containment, numeric bounds, and expected owner before requests. Implement `materialize_handler_io()` returning only the engine-consumed primitive; never pass authority methods or credentials to the engine. Use low-level `put_object(..., IfNoneMatch='*')`, HEAD/preflight, bounded StreamingBody copying, response closure, private file permissions, and domain errors with preserved botocore causes.</action>
  <acceptance_criteria>One small immutable S3 generation travels from native handler stage to verified private snapshot through the existing engine seam without overwrite, buffering, ETag authorization, or authority sequencing.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/contracts/test_s3_generation_io.py -x -o log_cli=false</automated></verify>
  <done>The S3 adapter has a production-quality thin path through the same payload interface as memory/filesystem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded conditional multipart and exact ambiguity classification</name>
  <files>src/cacheness/storage/backends/s3_backend.py, tests/contracts/test_s3_generation_io.py</files>
  <read_first>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md</file>
    <file>src/cacheness/error_handling.py</file>
  </read_first>
  <behavior>
    - "Large files use bounded low-level multipart upload with bounded part size/count and conditional `CompleteMultipartUpload`."
    - "Known pre-completion failure attempts abort the exact upload ID; 409 requires a new bounded attempt and 412 verifies the existing exact key."
    - "A response lost after service acceptance is success only when exact-key contained read matches canonical size/SHA-256; absence is retryable and mismatch is conflict/integrity failure."
    - "No internal retry loop is unbounded and every typed failure preserves stage plus original cause."
  </behavior>
  <action>Implement multipart directly with create/upload-part/complete operations because the installed transfer helper cannot carry the required complete precondition. Enforce explicit chunk, part, byte, and attempt limits and abort a known upload on observed pre-completion failure. Add deterministic client faults for before acceptance, after acceptance/before response, part failure, complete 409, and 412 existing-key cases. Resolve ambiguity using exact-key HEAD/stream plus signed manifest SHA-256/size, never listing or ETag. Permit at most the configured bounded whole-upload retry for the AWS 409 semantics and otherwise return typed conflict or retryable backend failure.</action>
  <acceptance_criteria>Multipart publication cannot replace a generation, ambiguity never becomes an untracked success/absence guess, and all retries/work are bounded and typed.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/contracts/test_s3_generation_io.py -x -o log_cli=false</automated></verify>
  <done>S3 single and multipart paths implement the same immutable publication contract under deterministic ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bound S3 evidence pages and exact deletion/absence proof</name>
  <files>src/cacheness/storage/backends/s3_backend.py, tests/test_s3_blob_backend.py, tests/contracts/test_s3_generation_io.py</files>
  <read_first>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>tests/test_s3_blob_backend.py</file>
    <file>src/cacheness/error_handling.py</file>
  </read_first>
  <behavior>
    - "One inventory call returns at most one continuation-token page under object, represented-byte, response-byte, and work limits."
    - "Malformed cursors/responses and service failures are typed failures, never empty inventory."
    - "Delete is idempotent and every accepted delete is followed by one bounded exact-key HEAD; only 404/NoSuchKey proves absence, while present/permission/timeout/service outcomes remain distinct typed failures."
    - "Only the managed prefix is listable/deletable and bucket/prefix swaps fail closed."
  </behavior>
  <action>Replace the old list accumulation and warning-based path parsing with one bounded evidence-page operation. Validate opaque continuation tokens and each returned key/size before accumulating, stop at configured caps, and return the next token without walking the bucket. Add exact managed-prefix multipart-upload paging that returns bounded `(key, upload_id, initiated_at)` evidence. Abort a known current upload ID immediately after observed pre-completion failure. After process death, permit automatic abort only when the engine correlates the exact aged key to PostgreSQL pending-mutation or cleanup-debt attribution; unknown uploads remain report-only. The live harness may abort uploads only beneath its separately ownership-validated unique run prefix. Implement `delete_or_prove_absent` as one exact-key `DeleteObject` followed after every accepted response by exactly one bounded exact-key HEAD: only 404/NoSuchKey succeeds, a still-present object is cleanup failure, and permission/configuration/timeout/transport/5xx outcomes remain distinct typed failures. Repeating this sequence is idempotent. Never use listing, ETag, or version history as absence proof. Rewrite the old S3 tests around the new participant contract rather than preserving direct CRUD or compatible-endpoint expectations.</action>
  <acceptance_criteria>Inventory, multipart cleanup, and deletion stay within exact prefix/run ownership and explicit work bounds; no remote error is collapsed into false or an empty list.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_s3_blob_backend.py tests/contracts/test_s3_generation_io.py -x -o log_cli=false</automated></verify>
  <done>The S3 participant exposes all bounded evidence/cleanup mechanics needed by engine reconciliation without acquiring lifecycle authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Engine locator -> S3 key | Relative locators must remain under one exact bucket/prefix. |
| S3 network body -> local handler | Untrusted remote bytes require bounded contained snapshot and signed digest verification. |
| Qualification cleanup -> AWS resources | Deletes/aborts must remain exact-run owned. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-09 | Tampering | object publication | high | mitigate | Unique locators plus conditional single/multipart creation and exact ambiguity verification. |
| T-05-10 | Elevation of privilege | remote payload read | high | mitigate | Private contained snapshot plus signed manifest SHA-256/size before handler. |
| T-05-11 | Information disclosure | credentials/logs | high | mitigate | Standard provider/injected client; never persist or log secret values/body content. |
| T-05-12 | Denial of service | download/list/multipart | high | mitigate | Enforce explicit byte, part, page, object, token, and work caps. |
| T-05-13 | Tampering | cleanup target | high | mitigate | Exact bucket/prefix/locator validation and run-attributed multipart abort/delete. |

No dependency is added; boto3/botocore and Moto are already declared and locked.
</threat_model>

<verification>Run focused tests and inspect imports/calls to confirm the S3 module never imports or invokes lifecycle-authority transitions. Confirm tests explicitly label Moto/fake evidence as contract-only and never write a qualified live-service result.</verification>

<success_criteria>
- S3 satisfies the payload primitive contract for single/multipart operations.
- All reads/listing/deletes are contained, bounded, and typed.
- Visibility and recovery decisions remain with AuthorityLifecycleEngine/PostgreSQL authority.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-03-SUMMARY.md` when done.</output>
