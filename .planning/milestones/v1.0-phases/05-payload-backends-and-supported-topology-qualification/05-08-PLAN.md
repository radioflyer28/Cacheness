---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "08"
type: execute
wave: 5
depends_on: [05-06, 05-07]
files_modified:
  - tests/integration/test_postgresql_authority.py
  - tests/integration/test_s3_generation.py
  - tests/integration/test_remote_topology.py
autonomous: true
requirements: [BACK-04, BACK-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Real PostgreSQL tests exercise transactions, exact CAS, cross-connection contention, explicit initialization, version rejection, and partial-failure cleanup."
    - "Real Amazon S3 tests exercise conditional single/multipart publication, checksums/digest verification, streaming, pagination, ambiguity, retries, and exact cleanup (D-05, D-08)."
    - "Two independently constructed remote clients share PostgreSQL/S3/signer state and prove cross-client reads, exact conflicts, recovery, and cleanup debt without cross-resource ACID (D-18)."
    - "Live markers are complete and collection-stable, but their mere collection or ordinary skip does not qualify BACK-05."
  artifacts:
    - path: tests/integration/test_postgresql_authority.py
      provides: "Real PostgreSQL transaction/contention suite"
    - path: tests/integration/test_s3_generation.py
      provides: "Real Amazon S3 generation suite"
    - path: tests/integration/test_remote_topology.py
      provides: "Two-client PostgreSQL/S3 BlobStore qualification suite"
  key_links:
    - from: tests/integration/test_remote_topology.py
      to: src/cacheness/storage/blob_store.py
      via: "Separate clients execute public operations through the exact remote StoreTopology"
      pattern: "BlobStore|StoreTopology"
  prohibitions:
    - "No Moto/fake/local-compatible endpoint in these files and no skip counted as pass."
    - "No shared process-local store/connection object between the two remote clients."
    - "No assertion that every contender succeeds or that PostgreSQL transaction includes S3 effects."
---

<objective>
Define the non-substitutable real-service behavior required to qualify the remote reference topology.

Purpose: Ensure the final gate tests AWS/PostgreSQL semantics and independent-client recovery rather than repeating mock confidence.
Output: Three marked live integration suites consumed by the fixed qualification runner.
</objective>

## Artifacts this phase produces

- Real PostgreSQL authority test matrix.
- Real AWS S3 generation-I/O test matrix.
- Combined two-client remote lifecycle/recovery test matrix.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@tests/qualification/conftest.py
@tools/run_phase5_qualification.py
</context>

<source_coverage>GOAL: real topology-specific service guarantees. REQ: BACK-04/BACK-05. RESEARCH: PostgreSQL, AWS S3, and combined remote test taxonomy. CONTEXT: D-05 through D-08, D-13 through D-22.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exercise PostgreSQL authority and Amazon S3 generation semantics against real services</name>
  <files>tests/integration/test_postgresql_authority.py, tests/integration/test_s3_generation.py</files>
  <read_first>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>tests/qualification/conftest.py</file>
    <file>tests/contracts/test_postgresql_lifecycle_authority.py</file>
    <file>tests/contracts/test_s3_generation_io.py</file>
  </read_first>
  <behavior>
    - "PostgreSQL: explicit initialize/reopen, schema mismatch, prepare/promote/debt atomicity, exact stale CAS, independent connections, SQLSTATE/timeout mapping, and bounded paging."
    - "AWS S3: conditional small/multipart create, 412/409 classification, exact-key ambiguity verification, bounded snapshot/listing/delete, and IAM/credential failures."
    - "Canonical SHA-256/size and signed manifest authorize integrity; ETag and transport checksum remain diagnostic/supplemental."
    - "Every test has the exact live marker and only uses fixtures that assert genuine service identity."
  </behavior>
  <action>Translate the deterministic contracts into real-service tests without broadening semantics. Use independent PostgreSQL leases/process-safe construction and force actual contention with database synchronization primitives in test setup, not a production advisory-lock protocol. Assert allowed result classes and then inspect canonical safety/debt. For AWS, force both size paths, continuation pages, exact prefix containment, conditional conflicts, and cleanup; verify the service identity as AWS and reject custom compatible endpoints. Where an ambiguous network response cannot be safely induced against AWS, exercise the adapter's exact follow-up observation against a real accepted object and retain the deterministic response-loss injection in contract tests. Do not make any test pass by skipping inside the test body.</action>
  <acceptance_criteria>The runner can collect a complete genuine PostgreSQL and AWS matrix covering all required driver/service semantics; no mocked participant can satisfy a marker.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest --collect-only -q tests/integration/test_postgresql_authority.py tests/integration/test_s3_generation.py -o log_cli=false</automated></verify>
  <done>The service-specific halves of BACK-05 are fully specified and runnable when external configuration is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise two independent remote BlobStore clients and recovery boundaries</name>
  <files>tests/integration/test_remote_topology.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/composition.py</file>
    <file>tests/contracts/test_topology_lifecycle.py</file>
    <file>tests/qualification/conftest.py</file>
  </read_first>
  <behavior>
    - "Client A writes and client B, freshly constructed with a separate authority connection and S3 client, reads the committed value using the external shared signer."
    - "Same-key contention yields success, exact conflict, or declared retryable PostgreSQL outcome, while the final committed manifest/payload is one complete generation."
    - "Injected interruption before promotion is attributable intent; post-promotion delete failure is durable cleanup debt that a recreated client reconciles idempotently."
    - "Bounded catalog/inventory paging resumes across clients and listing never revokes a committed entry."
  </behavior>
  <action>Construct two entirely independent BlobStore objects over the run-owned PostgreSQL schema and AWS prefix. Share only configuration identity and the in-memory key bytes supplied separately to each provider; do not share a store, lifecycle engine, connection, or client object. Exercise write/read, overwrite conflict, deletion, clear/reconciliation resume, candidate interruption, committed-with-cleanup-debt, and bounded page continuation. Assert `AuthorityLifecycleEngine` identity on both stores and inspect only public/semantic authority results. Keep safety/recovery assertions independent from progress outcomes and collect any performance distribution without using it as a pass deadline.</action>
  <acceptance_criteria>Independent clients observe one canonical generation and deterministic intent/debt recovery under allowed progress outcomes, with no process-local correctness dependency.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest --collect-only -q tests/integration/test_remote_topology.py -o log_cli=false</automated></verify>
  <done>The combined remote qualification suite captures the exact multi-host contract the final live run must prove.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test harness -> real shared services | Tests must authenticate least-privilege and remain exact-run scoped. |
| Client A -> client B | Cross-client behavior must rely on PostgreSQL/S3/shared signer, not process-local objects. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-34 | Spoofing | AWS service identity | high | mitigate | Reject custom endpoints and record AWS region/service identity in evidence. |
| T-05-35 | Tampering | cross-client generation | high | mitigate | Exact PostgreSQL CAS plus signed manifest digest/size and immutable object keys. |
| T-05-36 | Repudiation | skipped live cases | high | mitigate | Complete fixed marker collection; final runner rejects skips/deselection. |
| T-05-37 | Denial of service | live contention/paging | medium | mitigate | Bounded transactions, outcomes, pages, bytes, and work. |
| T-05-38 | Information disclosure | live assertions | high | mitigate | Never include service credentials, object bodies, DSN, or signing key in assertion output. |
</threat_model>

<verification>Collect each fixed module and ensure every test has exactly one required live marker. Static-inspect fixtures/imports to ensure Moto/fakes/custom endpoints cannot enter these files. Actual pass status is deliberately deferred to Plan 10 and remains non-passing until run.</verification>

<success_criteria>
- Real-service suites cover all service-specific and combined workflows.
- Tests use independent clients and topology-specific outcome sets.
- Collection does not masquerade as qualification.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-08-SUMMARY.md` when done.</output>
