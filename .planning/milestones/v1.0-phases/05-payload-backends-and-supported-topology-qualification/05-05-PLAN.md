---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "05"
type: execute
wave: 3
depends_on: [05-04]
files_modified:
  - src/cacheness/storage/backends/postgresql_lifecycle_authority.py
  - tests/contracts/test_postgresql_lifecycle_authority.py
  - tests/contracts/test_lifecycle_authority.py
autonomous: true
requirements: [BACK-04, BACK-05]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PostgreSQL completes the full LifecycleAuthority protocol, including bounded catalog, clear, reconciliation, debt, tombstone, and projection-revision operations."
    - "Exact conflicts remain distinct from typed retryable serialization/deadlock/lock/statement/connection failures with preserved cause and bounded stage context (D-16)."
    - "No test requires every contender to succeed; same-key operations still cannot expose mixed generations or lose cleanup obligations (D-15, D-18, D-20)."
  artifacts:
    - path: tests/contracts/test_lifecycle_authority.py
      provides: "Tier-aware reusable memory/SQLite/PostgreSQL semantic contract"
    - path: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      provides: "Complete bounded LifecycleAuthority implementation"
  key_links:
    - from: tests/contracts/test_lifecycle_authority.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "All authorities are checked against the same semantic protocol with tiered outcomes"
      pattern: "LifecycleAuthority|allowed_progress_outcomes"
  prohibitions:
    - "No advisory locking protocol, global/fork-shared connection, process queue, indefinite retry, or success-only contention test."
    - "No unbounded list_entries use on remote paths; public remote operations page under configured limits."
    - "No backend or projection error is collapsed into absence, conflict, or an empty page."
---

<objective>
Complete and contract-qualify PostgreSQL as the remote lifecycle authority.

Purpose: Ensure all engine operations—not just put—have bounded, typed, transactionally attributable remote semantics.
Output: Complete authority protocol, typed progress mapping, and reusable tier-aware authority contract.
</objective>

## Artifacts this phase produces

- Complete PostgreSQL debt/delete/clear/reconciliation/catalog/projection-revision primitives.
- `tests/contracts/test_lifecycle_authority.py` parameterized across memory, SQLite, and PostgreSQL tiers.
- Stable typed PostgreSQL progress outcome mapping with preserved driver causes.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@src/cacheness/storage/lifecycle_authority.py
@src/cacheness/storage/sqlite_lifecycle_authority.py
@tests/test_lifecycle_authority_contract.py
</context>

<source_coverage>GOAL: complete remote catalog/lifecycle authority. REQ: PostgreSQL portions of BACK-04/BACK-05. RESEARCH: bounded pages and SQLSTATE classification. CONTEXT: D-14 through D-20.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete bounded catalog, cleanup, clear, and reconciliation workflows</name>
  <reversibility rating="costly">These semantic tables/cursors become persisted authority state consumed by Phase 7 migration tooling.</reversibility>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "Cleanup debt lookup/retirement, delete/tombstone retirement, and operation replay are idempotent and exact-generation guarded."
    - "Catalog, clear, reconciliation, mutation, and debt reads use stable keyset/token pages under row, byte, and work caps."
    - "Projection revision compare/mark is transactional but gives a projection no authority permissions."
    - "A restarted authority resumes clear/reconciliation from durable state without using S3 listing as membership truth."
  </behavior>
  <action>Implement every remaining method in `LifecycleAuthority`: bounded pending mutation/debt reads, debt retirement, delete/tombstone, begin/page/checkpoint clear, begin/snapshot/page/checkpoint reconciliation, catalog_page, compare_and_mark_projection, projection_backup, and close. Use stable keyset cursors and enforce lifecycle row/byte/action bounds in SQL and during decoding; retain `list_entries()` only as a bounded local/debug compatibility of the semantic protocol and never call it from remote public workflows. Validate every stored manifest/cursor/identifier before returning semantic values. Keep operation/debt/clear/reconciliation evidence in PostgreSQL and external payload mechanics out. Prove restart/resume and exact-generation conflict behavior with the fake driver plus semantic state snapshots.</action>
  <acceptance_criteria>The implementation structurally and behaviorally covers the complete authority protocol with bounded remote work, exact idempotency, and durable resumption.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra postgresql pytest -q tests/contracts/test_postgresql_lifecycle_authority.py -x -o log_cli=false</automated></verify>
  <done>Every BlobStore engine workflow has the transactional PostgreSQL primitive it needs without another coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze tier-aware authority and typed PostgreSQL progress contracts</name>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py, tests/contracts/test_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/composition.py</file>
    <file>tests/test_lifecycle_authority_contract.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "Memory accepts success/conflict; SQLite accepts success/conflict/retryable_timeout; PostgreSQL accepts success/conflict plus typed retryable serialization, deadlock, lock/statement, and connection failures."
    - "SQLSTATE 40001, 40P01, 55P03, 57014 and bounded connection operational failures map to `CacheBlobLifecycleTimeoutError` (or one documented retryable subtype) with operation stage and cause."
    - "Unique/CAS zero-row conflicts remain `CacheBlobLifecycleConflictError`, never retryable timeout."
    - "Fatal syntax, schema, permission, and integrity failures remain typed backend/migration/integrity failures, never misses."
  </behavior>
  <action>Create the reusable authority contract by parameterizing the existing semantic cases across memory, SQLite, and PostgreSQL while keeping durability/sharing/progress expectations tier-aware. Add focused fake-driver exception cases and real-cause assertions. Use one bounded context schema containing operation and stage but excluding DSN, SQL parameters, credentials, manifests, and payloads. Set transaction-local timeouts per operation; do not retry indefinitely inside the authority. Add concurrent outcomes to the contract as sets of allowed typed results plus independent safety assertions, not all-success timing expectations.</action>
  <acceptance_criteria>The full contract passes for memory/SQLite and the PostgreSQL driver boundary; every progress class is distinct, typed, bounded, and causally chained.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra postgresql pytest -q tests/contracts/test_lifecycle_authority.py tests/contracts/test_postgresql_lifecycle_authority.py tests/test_lifecycle_authority_contract.py -x -o log_cli=false</automated></verify>
  <done>PostgreSQL authority behavior is complete and its contention contract cannot be mistaken for universal success or corruption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Persisted SQL rows -> semantic authority objects | Malformed/cross-store rows and cursors must fail closed. |
| PostgreSQL errors -> public outcomes | Driver failures require accurate classification and redaction. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-19 | Tampering | clear/reconciliation cursor | high | mitigate | Validate store/run/revision identity and enforce keyset/work bounds. |
| T-05-20 | Tampering | stale cleanup/delete | high | mitigate | Exact lineage/generation/digest predicates before state/debt retirement. |
| T-05-21 | Denial of service | remote scans | high | mitigate | Stable bounded pages; prohibit unbounded remote materialization. |
| T-05-22 | Repudiation | SQLSTATE mapping | medium | mitigate | Table-driven error-class tests preserve cause and stage. |
| T-05-23 | Information disclosure | error context | high | mitigate | Allow-list bounded operation metadata; exclude credentials/data/SQL values. |
</threat_model>

<verification>Run the complete contract and a protocol structural assertion. Search the implementation for advisory-lock calls, loops that retry without a configured bound, and remote use of unbounded `list_entries`; any finding blocks completion.</verification>

<success_criteria>
- PostgreSQL implements every authority method with bounded work.
- Error and contention outcomes are exact and topology-specific.
- The common authority contract does not force parity beyond safety/recovery invariants.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-05-SUMMARY.md` when done.</output>
