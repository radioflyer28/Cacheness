---
phase: 10-remove-sqlcache-pull-through-subsystem
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/run_phase8_packaging.py
  - tests/packaging/test_wheel_matrix.py
autonomous: true
requirements: [CACH-07]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The existing one-artifact harness inspects wheel members and installed metadata, not only checkout imports (D-13, D-16)."
    - "A source-free install retains working local BlobStore and UnifiedCache round trips while retired names fail naturally (D-01, D-16)."
    - "The packaging contract freezes the six retained installable extras and rejects DuckDB or a sql extra/group surface (D-05, D-06, D-07, D-08)."
  artifacts:
    - path: tools/run_phase8_packaging.py
      provides: "Single-wheel member, metadata, import, and local-round-trip probe"
    - path: tests/packaging/test_wheel_matrix.py
      provides: "Integration contract for the exact built artifact"
  key_links:
    - from: tests/packaging/test_wheel_matrix.py
      to: tools/run_phase8_packaging.py
      via: "one WheelArtifact path and SHA-256 across ZIP inspection/install/probe"
      pattern: "WheelArtifact|sha256"
  prohibitions:
    - "flagged-unverified: Packaging proof must not add a second build harness or accept a source-tree-only result."
---

<objective>
Extend the existing source-free wheel harness with complete Phase 10 artifact assertions before dependency/source deletion.

Purpose: Prove the shipped distribution—not merely the checkout—contains only the retained product.
Output: One-artifact wheel membership and installed-metadata contracts.
</objective>

<scope_guard>
Packaging harness/tests only. Do not edit the manifest, lockfile, production surface, or build a compatibility package.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-CONTEXT.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-RESEARCH.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-PATTERNS.md
@tools/run_phase8_packaging.py
@tests/packaging/test_wheel_matrix.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-03-01: Bind retired members and installed metadata to one WheelArtifact</name>
  <files>tools/run_phase8_packaging.py</files>
  <read_first>tools/run_phase8_packaging.py; 10-RESEARCH.md “Wheel member and installed metadata proof”; pyproject.toml</read_first>
  <behavior>
    - "The exact built wheel contains no cacheness/sql_cache.py member."
    - "The source-free installed distribution exposes no DuckDB requirement or sql extra and no retired import/export."
    - "The same artifact path/digest drives ZIP inspection, installation, and local BlobStore/UnifiedCache probes."
  </behavior>
  <action>Per D-01, D-05, D-06, D-13, and D-16, move the retired top-level names out of `BASE_PUBLIC_EXPORTS` and into the exact retired inventory; add stdlib ZIP-member and installed `importlib.metadata` checks to the existing one-artifact flow. Keep the established base probe names, source-free environment sanitization, local store/cache round trips, and exact optional-group inventory. Make expected pre-cut failure attributable to current artifact contents only.</action>
  <acceptance_criteria>The runner can reject a stale member or dependency edge while preserving every retained probe and binding all observations to one digest.</acceptance_criteria>
  <verify><automated>uv run --isolated --group dev --frozen ruff check tools/run_phase8_packaging.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/packaging/test_wheel_matrix.py --collect-only</automated></verify>
  <done>The packaging harness expresses the complete post-cut artifact contract without a parallel build path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-03-02: Freeze wheel absence and retained-round-trip behavior in integration tests</name>
  <files>tests/packaging/test_wheel_matrix.py</files>
  <read_first>tests/packaging/test_wheel_matrix.py; tools/run_phase8_packaging.py after Task 10-03-01; 10-CONTEXT.md D-13/D-16</read_first>
  <behavior>
    - "Unit tests reject a wheel member, DuckDB requirement, sql metadata surface, or retired package export."
    - "The real wheel test still proves the five established base probes from an isolated installation."
  </behavior>
  <action>Add focused synthetic artifact/metadata failure tests and update the real one-wheel integration expectation, per D-13/D-16. Retain all existing optional-group isolation, no-checkout-import, quiet-import, digest, and local native-format assertions. Do not weaken service nonclaims or create another fixture that can drift from `WheelArtifact`.</action>
  <acceptance_criteria>The test module catches stale artifact contents independently from source-tree absence and keeps the retained local journeys mandatory.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q --collect-only tests/packaging/test_wheel_matrix.py &amp;&amp; uv run --isolated --group dev --frozen ruff check tests/packaging/test_wheel_matrix.py</automated></verify>
  <done>Wave 0 has a collectable packaging contract ready to turn green after the source/dependency cut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source checkout -> built wheel | Checkout absence does not prove wheel membership. |
| wheel -> isolated environment | Installed metadata and imports must come from the exact built artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-02 | Tampering | wheel contents/metadata | high | mitigate | Bind ZIP inspection and isolated install to one digest-carrying WheelArtifact. |
| T-10-SC | Tampering | Python dependency resolution | high | mitigate | Install no new package; use the frozen reviewed lock for tests and inspect exact generated distribution metadata. |
</threat_model>

<verification>
- Packaging tests collect and scoped Ruff passes.
- Real wheel execution is deferred until the source/manifest cut makes the new assertions green.
</verification>

<acceptance_criteria>
- D-13 independently proves shipped member absence.
- D-16 binds retained journeys and pruned metadata to one fresh artifact.
</acceptance_criteria>

## Artifacts this phase produces

- Extended packaging qualification behavior in existing files.
- No new production modules or public symbols.

<source_coverage>
- GOAL: Ship one coherent retained product surface.
- REQ: CACH-07 artifact and dependency absence with retained round trips.
- RESEARCH: Extend the Phase 8 one-wheel source-free harness.
- CONTEXT: D-01, D-05, D-06, D-07, D-08, D-13, D-16.
</source_coverage>

<success_criteria>
- A stale wheel cannot pass because source-tree tests are green.
- Retained local BlobStore/UnifiedCache journeys remain mandatory.
</success_criteria>

<output>
Create `.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-03-SUMMARY.md` when done.
</output>
