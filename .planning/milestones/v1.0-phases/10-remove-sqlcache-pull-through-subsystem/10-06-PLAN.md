---
phase: 10-remove-sqlcache-pull-through-subsystem
plan: 06
type: execute
wave: 4
depends_on: [10-03, 10-05]
files_modified:
  - pyproject.toml
  - uv.lock
  - tools/run_phase8_packaging.py
  - tests/packaging/test_wheel_matrix.py
autonomous: true
requirements: [CACH-07]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The uv sql dependency group and every direct/transitive DuckDB edge are absent (D-05, D-06)."
    - "SQLAlchemy, pandas, PyArrow, psycopg, and all six retained installable extras remain organized as before (D-07, D-08)."
    - "A fresh exact wheel proves member/metadata absence and retained local BlobStore/UnifiedCache behavior (D-13, D-16)."
  artifacts:
    - path: pyproject.toml
      provides: "Pruned authoritative dependency declaration"
    - path: uv.lock
      provides: "Converged lock without DuckDB packages/edges"
    - path: tools/run_phase8_packaging.py
      provides: "Green exact-artifact acceptance harness"
  key_links:
    - from: pyproject.toml
      to: uv.lock
      via: "plain uv lock regeneration followed by uv lock --check"
      pattern: "duckdb|sqlalchemy"
    - from: tools/run_phase8_packaging.py
      to: tests/packaging/test_wheel_matrix.py
      via: "fresh WheelArtifact integration execution"
      pattern: "run_base_probe"
  prohibitions:
    - "flagged-unverified: Dependency pruning must not redesign retained SQLAlchemy/PostgreSQL groups or remove dataframe-handler dependencies."
---

<objective>
Prune the exact obsolete dependency surface and make the one-wheel acceptance contract green.

Purpose: Ensure the installed distribution cannot carry the removed product indirectly through files or metadata.
Output: Authoritative manifest/lock cleanup and passing fresh-wheel qualification.
</objective>

<scope_guard>
Packaging only. Do not hand-edit the lockfile, upgrade unrelated packages, change package version, or change retained runtime/backend code.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-CONTEXT.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-RESEARCH.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-03-SUMMARY.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-05-SUMMARY.md
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-06-01: Remove the exact DuckDB and sql-group dependency surface</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>pyproject.toml; 10-RESEARCH.md dependency inventory; current uv tree inverse ownership for duckdb-engine, sqlalchemy, pandas, pyarrow, and psycopg</read_first>
  <behavior>
    - "Both recommended lists omit duckdb-engine and the uv sql group is absent."
    - "Plain uv lock removes duckdb-engine and duckdb without unrelated manifest changes."
    - "Retained SQL/dataframe/PostgreSQL declarations and six project extras are exact."
  </behavior>
  <action>Per D-05 through D-08, edit only the authoritative TOML entries proven exclusive to the deleted subsystem, then regenerate derived state with plain `uv lock`. Review the diff for unrelated resolution churn and retain SQLAlchemy, pandas, PyArrow, psycopg, and all supported extras. Do not introduce an empty installable or uv compatibility group.</action>
  <acceptance_criteria>`uv lock --check` succeeds; manifest/lock contain no DuckDB package or sql dependency group; retained dependency ownership is unchanged.</acceptance_criteria>
  <verify><automated>uv lock --check &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase10_sqlcache_removal.py::test_manifest_lock_dependency_contract tests/test_phase10_sqlcache_removal.py::test_package_version_remains_unchanged -x</automated></verify>
  <done>The dependency graph is pruned exactly to supported owners.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-06-02: Run the fresh source-free wheel cutover proof</name>
  <files>tools/run_phase8_packaging.py, tests/packaging/test_wheel_matrix.py</files>
  <read_first>Plan 10-03 SUMMARY; tools/run_phase8_packaging.py; tests/packaging/test_wheel_matrix.py; pyproject.toml after Task 10-06-01</read_first>
  <behavior>
    - "Fresh wheel ZIP membership omits the removed source module."
    - "Installed metadata omits DuckDB and sql while retained extras remain exact."
    - "Isolated BlobStore and UnifiedCache local round trips pass and retired imports fail naturally."
  </behavior>
  <action>Finish any expectations that could only become green after the source/manifest cut, then run the existing real-wheel integration path per D-13/D-16. Keep one artifact and digest throughout; do not accept cached/previous wheels or checkout imports. Preserve optional-group isolation and remote-service nonclaims.</action>
  <acceptance_criteria>The real wheel test builds a fresh artifact and proves all member, metadata, import, and retained local journey assertions.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/packaging/test_wheel_matrix.py -x &amp;&amp; uv run --isolated --group dev --frozen ruff check tools/run_phase8_packaging.py tests/packaging/test_wheel_matrix.py</automated></verify>
  <done>The shipped artifact—not only the source tree—meets the Phase 10 cutover contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| manifest -> resolved lock | Derived dependency state must converge without unrelated drift. |
| build tree -> wheel -> isolated install | Exact artifact identity must survive every acceptance step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-02 | Tampering | wheel/dependency metadata | high | mitigate | Fresh one-digest artifact inspection plus installed metadata assertions. |
| T-10-10 | Denial of service | retained SQL/dataframe support | high | mitigate | Exact retained dependency assertions and all-extras regressions. |
| T-10-SC | Tampering | lock resolution | high | mitigate | No new package; plain uv lock, frozen tests, lock check, and reviewed diff. |
</threat_model>

<verification>
- `uv lock --check` passes.
- Real wheel integration and scoped Ruff pass.
</verification>

<acceptance_criteria>
- D-05/D-06 remove only the obsolete dependency surface.
- D-07/D-08 preserve proven retained owners.
- D-13/D-16 prove source and distribution absence independently.
</acceptance_criteria>

## Artifacts this phase produces

- Pruned `pyproject.toml` and regenerated `uv.lock`.
- No new production modules or public symbols.

<source_coverage>
- GOAL: First supported artifact contains only the coherent store/cache product.
- REQ: CACH-07 dependency and wheel removal with retained surfaces.
- RESEARCH: Exact manifest edit, plain lock convergence, one-artifact proof.
- CONTEXT: D-04, D-05, D-06, D-07, D-08, D-13, D-16.
</source_coverage>

<success_criteria>
- No DuckDB/sql-group edge remains in manifest, lock, wheel, or installed metadata.
- Retained local storage/cache and optional-group contracts pass.
</success_criteria>

<output>
Create `.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-06-SUMMARY.md` when done.
</output>
