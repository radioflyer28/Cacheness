---
phase: 10-remove-sqlcache-pull-through-subsystem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase10_sqlcache_removal.py
  - tests/test_public_api_contract.py
  - tests/test_phase6_public_api_contract.py
  - tests/test_phase6_suite_isolation.py
autonomous: true
requirements: [CACH-07]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A literal Phase 10 contract proves the removed module, exports, attributes, aliases, hooks, and package version boundary (D-01, D-04, D-13)."
    - "The surviving public-contract suites enforce BlobStore and UnifiedCache ownership without retaining SqlCache as an optional or separate product (D-15)."
    - "Current-surface reference enforcement uses an explicit narrow allowlist rather than rewriting truthful historical records (D-14)."
  artifacts:
    - path: tests/test_phase10_sqlcache_removal.py
      provides: "Fail-closed source, import, manifest, reference, and no-caller-data-mutation contract"
    - path: tests/test_public_api_contract.py
      provides: "Canonical top-level absence and retained error-vocabulary contract"
    - path: tests/test_phase6_public_api_contract.py
      provides: "Retained cache-over-BlobStore public boundary"
    - path: tests/test_phase6_suite_isolation.py
      provides: "Order-isolation coverage for surviving public contracts"
  key_links:
    - from: tests/test_phase10_sqlcache_removal.py
      to: src/cacheness/__init__.py
      via: "literal export, attribute, import-spec, and natural-exception assertions"
      pattern: "SqlCache|SqlCacheAdapter|cacheness.sql_cache"
    - from: tests/test_phase10_sqlcache_removal.py
      to: pyproject.toml
      via: "TOML and lock metadata assertions"
      pattern: "dependency-groups|duckdb"
  prohibitions:
    - "flagged-unverified: Removal must not become a storage-lifecycle, concurrency, recovery, topology, or BlobStore/UnifiedCache composition redesign."
    - "flagged-unverified: Removal must not retain a compatibility shim, tombstone module, tailored exception, alias, or replacement query-cache layer."
---

<objective>
Establish the executable absence and retained-public-surface contracts before the product cut.

Purpose: Make the direct removal diagnosable and prevent a compatibility layer from surviving under another name.
Output: A new Phase 10 negative contract and updated reusable public/suite-isolation contracts.
</objective>

<scope_guard>
Tests only. Do not delete production code yet, mutate caller databases, change package version 0.3.14, or touch storage lifecycle behavior.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-CONTEXT.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-RESEARCH.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-PATTERNS.md
@src/cacheness/__init__.py
@src/cacheness/error_handling.py
@pyproject.toml
</context>

<flagged_assumptions>
- adjacency probe — flagged-unverified classification mismatch: CACH-07 is structural deletion, so equality/touching has no domain behavior; literal inventories and absence assertions are the verification backstop.
- empty probe — flagged-unverified classification mismatch: CACH-07 has no empty/null input contract; absence of module/export/reference surfaces is the verification backstop.
- ordering probe — flagged-unverified classification mismatch: CACH-07 has no equal-element ordering contract; closed literal inventories make order irrelevant without inventing runtime behavior.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-01-01: Add the fail-closed Phase 10 removal contract</name>
  <files>tests/test_phase10_sqlcache_removal.py</files>
  <read_first>10-CONTEXT.md D-01/D-03/D-04/D-05/D-06/D-10/D-13/D-14; tests/test_phase071_contract_verifier.py; tests/test_phase9_documentation.py; pyproject.toml</read_first>
  <behavior>
    - "The source module, six dedicated examples, three dedicated docs, and three dedicated tests are required to be physically absent after the cut."
    - "Top-level exports/attributes and cacheness.sql_cache fail through ordinary Python absence, with no dynamic hook or tombstone."
    - "The exact version remains 0.3.14; the sql dependency group and all DuckDB manifest/lock entries are absent while retained extras remain exact."
    - "A current-facing scanner names roots and permits only the three canonical cutover-note owners plus negative contract text; planning history and dated audits are outside the scan."
    - "No cleanup, migration, export, or discovery path for caller-owned SqlCache tables is introduced."
  </behavior>
  <action>Create one literal negative contract per D-01, D-03, D-04, D-05, D-06, D-10, D-13, and D-14. Use stdlib `importlib`, `pathlib`, and `tomllib`; isolate the definitive import probe in a subprocess so `sys.modules` cannot mask removal. Freeze the deleted-path inventory and current-facing scan roots. Encode the narrow allowlist by exact path and purpose rather than a repository-wide scrub. Give the boundaries stable focused nodes named `test_removed_source_and_dedicated_tests_are_absent`, `test_dedicated_docs_and_examples_are_absent`, `test_public_names_and_module_are_naturally_absent`, `test_package_version_remains_unchanged`, `test_manifest_lock_dependency_contract`, `test_current_facing_references_match_allowlist`, and `test_phase10_has_no_caller_table_tooling`. Keep the tests red only on the still-present pre-cut surface, not on collection or helper errors.</action>
  <acceptance_criteria>The new module collects independently, enumerates every removal boundary, preserves historical exclusions, asserts version 0.3.14, and fails only because the Phase 10 cut has not occurred yet.</acceptance_criteria>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q --collect-only tests/test_phase10_sqlcache_removal.py &amp;&amp; uv run --isolated --group dev --frozen ruff check tests/test_phase10_sqlcache_removal.py</automated>
  </verify>
  <done>Wave 0 has a precise, collectable CACH-07 contract that will turn green only after complete removal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-01-02: Invert reusable public and suite-isolation contracts</name>
  <files>tests/test_public_api_contract.py, tests/test_phase6_public_api_contract.py, tests/test_phase6_suite_isolation.py</files>
  <read_first>tests/test_public_api_contract.py; tests/test_phase6_public_api_contract.py; tests/test_phase6_suite_isolation.py; 10-PATTERNS.md sections “Public contract inversion” and “Phase 6 public/suite contracts”</read_first>
  <behavior>
    - "Retired names are absent from the package barrel while BlobStore and UnifiedCache retain their exact modules and roles."
    - "The exact CacheReason set omits the four orphan values after the cut."
    - "Both relative orders of the two surviving public-contract modules remain exercised without a deleted SqlCache node."
  </behavior>
  <action>Per D-13 and D-15, replace positive optional/separate-product assertions with natural-absence assertions, remove the retired names from canonical tuples, remove the four orphan reason literals from the exact set, and reduce order-isolation to the two surviving public-contract modules in both relative orders. Preserve every unrelated public symbol, optional-import isolation guarantee, and BlobStore/UnifiedCache ownership assertion.</action>
  <acceptance_criteria>The reusable suites express the post-cut public boundary and contain no live test-node dependency on a dedicated SqlCache suite.</acceptance_criteria>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q --collect-only tests/test_public_api_contract.py tests/test_phase6_public_api_contract.py tests/test_phase6_suite_isolation.py &amp;&amp; uv run --isolated --group dev --frozen ruff check tests/test_public_api_contract.py tests/test_phase6_public_api_contract.py tests/test_phase6_suite_isolation.py</automated>
  </verify>
  <done>Public and order-isolation contracts are ready for the direct cut without preserving compatibility behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source tree -> import system | Deleted names must not be synthesized dynamically. |
| current docs/source -> reference scanner | Current claims are enforced without erasing historical evidence. |
| repository -> caller-owned databases | Tests must not discover or mutate application tables. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Spoofing | package import surface | high | mitigate | Assert export, attribute, import-spec, module-file, and dynamic-hook absence in isolated Python. |
| T-10-03 | Repudiation | reference scanner | medium | mitigate | Use explicit current roots and exact allowlist owners while excluding dated/planning history. |
| T-10-04 | Tampering | caller SQL state | high | mitigate | Assert no Phase 10 cleanup/migration entry point and perform no database discovery. |
</threat_model>

<verification>
- All four Wave 0 modules collect and lint.
- Expected RED conditions are only the still-present product surface.
</verification>

<acceptance_criteria>
- D-01/D-13 define natural absence with no compatibility behavior.
- D-04 freezes the current package version.
- D-14/D-15 preserve truthful history and reusable contracts.
</acceptance_criteria>

## Artifacts this phase produces

- New test file: `tests/test_phase10_sqlcache_removal.py`.
- No new production modules or public symbols.

<source_coverage>
- GOAL: Establish a tested coherent BlobStore/UnifiedCache boundary before deletion.
- REQ: CACH-07 public, implementation, dependency, and retained-surface contract.
- RESEARCH: Wave 0 natural-absence, exact-inventory, and current-reference scan architecture.
- CONTEXT: D-01, D-03, D-04, D-13, D-14, D-15.
</source_coverage>

<success_criteria>
- The cut cannot pass by leaving an alias, hook, tombstone, stale node, or version change.
- Historical evidence and caller-owned SQL state remain outside mutation scope.
</success_criteria>

<output>
Create `.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-01-SUMMARY.md` when done.
</output>
