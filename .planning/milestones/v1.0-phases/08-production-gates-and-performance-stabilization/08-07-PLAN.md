---
phase: 08-production-gates-and-performance-stabilization
plan: 07
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - benchmarks/phase8_workloads.py
  - benchmarks/phase8_benchmarks.py
  - tests/performance/test_phase8_benchmarks.py
  - .github/workflows/performance.yml
autonomous: true
requirements:
  - QUAL-03
  - QUAL-06
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Canonical performance workloads cover small generic objects, medium/large NumPy NPZ and Blosc2, and retained pandas/Polars Parquet tiers without a handler-by-topology Cartesian matrix (D-18)."
    - "Handler serialization, BlobStore lifecycle, and UnifiedCache policy overhead are measured as separate distributions (D-18)."
    - "SHA-256 and XXH3 raw throughput plus digest share of matching end-to-end lifecycle time are reported while persisted SHA-256-plus-size semantics remain unchanged (D-19)."
    - "Only a named controlled Linux runner may record or enforce reviewed median/tail envelopes; remote latency remains diagnostic (D-20, D-22)."
  artifacts:
    - path: "benchmarks/phase8_workloads.py"
      provides: "Deterministic format-tier and layer workload definitions"
    - path: "benchmarks/phase8_benchmarks.py"
      provides: "Stdlib distribution, environment, capture, and verification harness"
    - path: ".github/workflows/performance.yml"
      provides: "Controlled-Linux-only performance workflow"
    - path: "tests/performance/test_phase8_benchmarks.py"
      provides: "Harness/schema/envelope/non-policy self-tests"
  key_links:
    - from: "benchmarks/phase8_workloads.py"
      to: "src/cacheness/storage/blob_store.py"
      via: "public current StoreTopology and BlobStore workload path"
      pattern: "BlobStore|StoreTopology"
    - from: ".github/workflows/performance.yml"
      to: "benchmarks/phase8_benchmarks.py"
      via: "controlled runner capture/verify command"
      pattern: "phase8_benchmarks"
---

<objective>
Create the canonical layer-separated performance and hash benchmark harness and controlled-runner workflow.

Purpose: Make QUAL-06 measurable with stdlib distribution patterns, preserving D-18 through D-22 and the canonical SHA-256 contract.
Output: Deterministic workloads, benchmark CLI, self-tests, and controlled Linux orchestration.
</objective>

<scope_guard>
This plan measures the shipped architecture. It may not alter production digest semantics, runtime deadlines, topology guarantees, payload publication, or lifecycle coordination. It adds no benchmark dependency; a new dependency would require a separate blocking decision before any manifest/lock change.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-PATTERNS.md
@benchmarks/lifecycle_authority_benchmark.py
@benchmarks/lifecycle_authority_baseline.json
@src/cacheness/storage/manifest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define representative format tiers and separated layer workloads</name>
  <files>benchmarks/phase8_workloads.py, tests/performance/test_phase8_benchmarks.py</files>
  <read_first>
    - src/cacheness/handlers.py: retained generic, NumPy NPZ/Blosc2, pandas/Polars Parquet handler contracts
    - src/cacheness/storage/composition.py: supported memory and SQLite/filesystem topology construction
    - benchmarks/lifecycle_authority_benchmark.py: deterministic seeding and setup-outside-timing patterns
  </read_first>
  <behavior>
    - "Fixtures are deterministic, created outside timed callbacks, and carry tier/format/size/cold-warm metadata."
    - "Small generic, medium/large NumPy NPZ and Blosc2, and pandas/Polars Parquet workloads are present exactly once at each intended layer."
    - "Handler-only, BlobStore, and UnifiedCache measurements use current public composition and cannot fall back to removed payload paths."
  </behavior>
  <action>Define reviewed workload descriptors and factories for a 4 KiB nested object, 16 MiB and 128 MiB deterministic contiguous arrays, and 100k-row mixed-type pandas/Polars frames per D-18. Pair small/medium work with memory topology and large persistent work with SQLite authority plus local obstore payload, avoiding a topology cross-product. Provide handler staging/serialization, public BlobStore put/get, and UnifiedCache put/lookup callbacks with setup/cleanup outside timing and explicit cold/warm labels. Test the descriptor inventory and current composition paths using reduced fixtures.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/performance/test_phase8_benchmarks.py -k workloads -x</automated>
  </verify>
  <done>The benchmark workload inventory is representative, deterministic, layer-separated, and bound to the current public architecture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stdlib distributions, hash comparison, and immutable baseline verification</name>
  <files>benchmarks/phase8_benchmarks.py, tests/performance/test_phase8_benchmarks.py</files>
  <read_first>
    - benchmarks/lifecycle_authority_benchmark.py: `_distribution`, warmups, raw samples, environment/revision, and atomic baseline replacement
    - src/cacheness/storage/manifest.py: canonical payload digest identifier and SHA-256 semantics
    - src/cacheness/serialization.py: project XXH3 usage
  </read_first>
  <behavior>
    - "Raw samples and p50/p95/p99 distributions retain workload, layer, environment, revision, and runner identity."
    - "Raw SHA-256/XXH3 throughput is measured at 4 KiB, 1 MiB, 16 MiB, and 128 MiB, with each digest's share of the matching lifecycle distribution."
    - "Verify mode is non-mutating; capture/recalibration requires explicit justification and atomic replacement."
    - "A performance regression never changes runtime timeout or public integrity/recovery/progress semantics."
  </behavior>
  <action>Build the canonical CLI with `time.perf_counter_ns`, `statistics`, worker subprocesses, warmups, calibrated loop counts, raw samples, deterministic percentiles, stability metadata, and Linux RSS/environment fingerprints. Add SHA-256 versus XXH3 raw throughput and lifecycle-share reporting per D-19, importing the existing project hash implementation without changing manifest code or identifiers. Support diagnostic, capture, recalibrate-with-justification, and verify modes; comparison requires the same controlled runner identity and reviewed relative median/tail envelopes. Reject malformed, unstable, stale-revision, environment-mismatched, or auto-mutated baselines.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/performance/test_phase8_benchmarks.py -k 'distribution or hash or baseline' -x</automated>
  </verify>
  <done>The harness emits reproducible layer/hash distributions and enforces immutable reviewed baselines without becoming runtime policy.</done>
</task>

<task type="auto">
  <name>Task 3: Restrict the blocking performance workflow to controlled Linux</name>
  <files>.github/workflows/performance.yml, tests/performance/test_phase8_benchmarks.py</files>
  <read_first>
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: D-20 and D-22
    - benchmarks/phase8_benchmarks.py: diagnostic/capture/verify commands and required runner identity
  </read_first>
  <behavior>
    - "The performance workflow requires one explicit 40-character candidate SHA, checks out that commit detached, and refuses head-SHA or environment/fingerprint drift."
    - "The successful job uploads one fixed-name controlled-performance envelope whose revision/source digest match the input SHA."
  </behavior>
  <action>Create a manual/protected release performance workflow with a required exact 40-character `candidate_sha` input. Target the exact durable label `cacheness-perf-linux-x64`, check out that SHA detached, prove `git rev-parse HEAD` equals the input before measurement, validate the runtime machine fingerprint, use frozen project dependencies, and invoke only the canonical harness. Pin third-party actions to reviewed full commit SHAs. Upload a fixed-name sanitized controlled-performance envelope plus raw diagnostic results with bounded retention; the qualifying envelope must bind the input SHA and recomputed relevant-source digest, and the workflow blocks only on the controlled baseline comparison. Encode remote PostgreSQL/S3 timings and ordinary macOS runs as diagnostics that cannot enter this workflow's blocking envelope per D-20/D-22.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/performance/test_phase8_benchmarks.py -k workflow -x</automated>
  </verify>
  <done>The release performance workflow can run only on the named controlled Linux identity and cannot convert remote latency or benchmark thresholds into runtime semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture setup -> timed callback | Setup or cleanup work must not contaminate measured layers. |
| runner host -> baseline identity | Results from another machine must not qualify the controlled envelope. |
| benchmark result -> runtime contract | Measurements must not mutate digest semantics, deadlines, or topology claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-07-01 | Tampering | benchmark distributions | high | mitigate | Preserve raw samples, validate derived percentiles/envelopes, revision, workload, and environment identity. |
| T-08-07-02 | Spoofing | controlled runner | high | mitigate | Require exact runner label plus captured hardware/OS/filesystem/tool fingerprint. |
| T-08-07-03 | Repudiation | baseline changes | high | mitigate | Separate verify/capture/recalibration, require justification, and atomically publish reviewed data. |
| T-08-07-04 | Denial of Service | large workloads | medium | mitigate | Bound fixture sizes, samples, workers, and child timeouts; keep heavy runs off ordinary PR jobs. |
| T-08-07-05 | Tampering | canonical digest | high | mitigate | Tests assert manifest SHA-256 identifiers/bytes are unchanged and XXH3 appears only in comparative evidence. |
</threat_model>

<verification>
- Workload, distribution, hash, malformed-baseline, environment, and workflow self-tests pass.
- Diagnostic reduced workloads run locally without creating a release baseline.
- Git diff confirms no production digest, lifecycle, or cache-policy source changed.
</verification>

<success_criteria>
- QUAL-06 has one unambiguous benchmark entry point and reviewed controlled-runner contract.
- Representative formats and layer overheads are separate distributions.
- SHA-256 versus XXH3 evidence informs SEED-003 without changing persisted integrity semantics.
</success_criteria>

<source_coverage>
- GOAL: Reproducible reviewed latency/throughput evidence for named workloads and environments.
- REQ: QUAL-03 and QUAL-06.
- RESEARCH: Layer-separated workloads, raw samples/distributions, controlled relative envelope, hash comparison, immutable baseline.
- CONTEXT: D-03, D-18, D-19, D-20, D-22.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-07-SUMMARY.md` when done.
</output>
