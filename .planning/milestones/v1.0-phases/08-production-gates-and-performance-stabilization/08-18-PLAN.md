---
phase: 08-production-gates-and-performance-stabilization
plan: 18
type: execute
wave: 10
depends_on:
  - 08-17
files_modified:
  - tests/test_phase3_postreview_concurrency.py
autonomous: true
gap_closure: true
requirements:
  - QUAL-04
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Independent SQLite workers begin only after one explicit authority initialization has completed, matching ADR 0001 rule 7 and the approved Phase 3 D-35 boundary."
    - "After initialization, both released workers complete within the bounded join, report no errors, observe exactly SQLITE_APPLICATION_ID, and leave authority state usable for a subsequent public operation."
    - "The corrected test makes no concurrent-first-creation availability claim, performs no retry of the stale race, and requires no production lifecycle or bootstrap change (D-03, D-04)."
    - "Existing tests continue to own incomplete/foreign/future fail-closed evidence and single-process first-use convenience; this plan identifies and reruns those selectors without duplicating or weakening them."
  artifacts:
    - path: tests/test_phase3_postreview_concurrency.py
      provides: "Deterministic initialized-root shared-worker SQLite authority regression"
  key_links:
    - from: tests/test_phase3_postreview_concurrency.py
      to: docs/STORAGE_INITIALIZATION.md
      via: "explicit initialize-before-sharing sequence"
      pattern: "initialize|SQLITE_APPLICATION_ID|begin_clear"
    - from: .planning/phases/08-production-gates-and-performance-stabilization/08-16-PLAN.md
      to: tests/test_phase3_postreview_concurrency.py
      via: "fixed canonical plan/threat/selector inventory"
      pattern: "test_initialized_root_shared_workers_converge_through_sqlite"
---

<objective>
Replace one stale concurrent-first-creation assertion with the supported explicitly initialized SQLite shared-worker workflow.

Purpose: Unblock Plan 08-16's deterministic local gate while preserving the Phase 3 compatibility decision and avoiding another bootstrap race-fix cycle.
Output: One renamed deterministic regression in the existing post-review concurrency test module.
</objective>

<root_cause>
This gap is a changed-assumption test defect. `test_fresh_root_bootstrap_converges_through_sqlite` still requires two concurrent first users to create one authority successfully, but ADR 0001 rule 7, approved D-35/Plan 03-22, and `docs/STORAGE_INITIALIZATION.md` place initialization before shared workers and explicitly decline that availability guarantee. The observed `CacheBlobMigrationRequiredError` is therefore not evidence for production lifecycle repair. The correction targets the stale premise, not the exception symptom.
</root_cause>

<scope_guard>
This plan changes only `tests/test_phase3_postreview_concurrency.py`. It must not edit production code, lifecycle/bootstrap behavior, dependencies, workflows, qualification gates, selector families, coverage baselines, `pyproject.toml`, `uv.lock`, ADR 0001, or readiness semantics. It must not retry concurrent first creation, add sleeps, accept worker errors, or turn initialization into a process-local coordination mechanism. Existing fail-closed invalid-evidence tests and single-process first-use tests remain unchanged and separately authoritative.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/STORAGE_INITIALIZATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-22-PLAN.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-22-SUMMARY.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@tests/test_phase3_postreview_concurrency.py
@tests/test_sqlite_metadata_bootstrap_atomicity.py
@tests/test_sqlite_lifecycle_authority.py
@tests/test_blob_store_read_contract.py
</context>

<interfaces>
Preserve and use these existing contracts:

- `SqliteLifecycleAuthority.initialize()` is the explicit creation boundary; ordinary shared-worker operations start only after it returns.
- `SQLITE_APPLICATION_ID` is the exact current application identity; worker diagnostics must equal this constant rather than merely equal each other.
- `tests/test_phase3_postreview_concurrency.py::_join(thread)` is the bounded completion assertion.
- `tests/test_sqlite_metadata_bootstrap_atomicity.py::test_foreign_root_is_rejected_without_mutating_evidence` and `::test_incomplete_authority_layout_is_rejected_without_implicit_upgrade` own foreign/incomplete fail-closed behavior.
- `tests/test_sqlite_lifecycle_authority.py::test_sqlite_authority_rejects_wrong_identity_without_mutating` owns foreign-application-ID and future-version rejection.
- `tests/test_blob_store_read_contract.py::test_composed_store_reopens_one_authenticated_canonical_generation` characterizes single-process first-use convenience through an initial `put_entry` without explicit initialization.
- The new canonical Phase 8 selector is exactly `tests/test_phase3_postreview_concurrency.py::test_initialized_root_shared_workers_converge_through_sqlite`; the stale node is renamed rather than retained as a second contract.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Exercise independent workers only after explicit SQLite initialization</name>
  <files>tests/test_phase3_postreview_concurrency.py</files>
  <behavior>
    - "One authority explicitly initializes the fresh root and proves the exact current application ID before any shared-worker operation is released."
    - "Two independently constructed authorities wait on the existing event, then each performs its public operation and finishes within `_join` with no captured exception."
    - "Both workers diagnose exactly `SQLITE_APPLICATION_ID`, share the initialized store identity, and a subsequent authority operation succeeds after the concurrent work."
    - "Invalid evidence still fails closed in its existing tests, and a single-process first `put_entry` still works in its existing convenience test."
  </behavior>
  <action>Rename `test_fresh_root_bootstrap_converges_through_sqlite` to exactly `test_initialized_root_shared_workers_converge_through_sqlite` and update its docstring to describe initialized-root shared-worker progress. Import `SQLITE_APPLICATION_ID` from the SQLite authority module. Create a dedicated initializer for the fresh root, call `initialize()`, assert its diagnostics report exactly `SQLITE_APPLICATION_ID` and a valid store identity, then close it before constructing the two independent worker authorities. Only after that completed initialization may the event release both workers to call `begin_clear()` concurrently. Keep result and exception capture explicit, join each thread through `_join`, and require both operations to return with no captured error. Assert each worker reports exactly `SQLITE_APPLICATION_ID`, both report the same nonempty store identity, and a subsequent public `begin_clear()` succeeds on the initialized authority before closing both handles in `finally`.

Do not catch `CacheBlobMigrationRequiredError` as an allowed worker result, retry a failed first-creation race, preserve the stale node as a duplicate, or alter production code to force an unsupported schedule. Leave the existing foreign, incomplete, future-version, exact-identity, initialized-thread/process, and single-process first-use tests unchanged. Run their exact selectors as companion evidence so this correction cannot absorb or weaken their separate responsibilities.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase3_postreview_concurrency.py::test_initialized_root_shared_workers_converge_through_sqlite tests/test_sqlite_metadata_bootstrap_atomicity.py::test_threaded_initialized_authorities_converge_without_first_use_claims tests/test_sqlite_metadata_bootstrap_atomicity.py::test_spawned_initialized_authorities_converge_without_process_local_state tests/test_sqlite_metadata_bootstrap_atomicity.py::test_foreign_root_is_rejected_without_mutating_evidence tests/test_sqlite_metadata_bootstrap_atomicity.py::test_incomplete_authority_layout_is_rejected_without_implicit_upgrade tests/test_sqlite_lifecycle_authority.py::test_sqlite_authority_rejects_wrong_identity_without_mutating tests/test_blob_store_read_contract.py::test_composed_store_reopens_one_authenticated_canonical_generation -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_phase3_postreview_concurrency.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff format --check tests/test_phase3_postreview_concurrency.py</automated>
  </verify>
  <done>The canonical regression deterministically initializes before sharing, both workers complete without error against the exact valid authority identity, subsequent authority use succeeds, and the existing invalid-evidence and single-process convenience selectors pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| initialization completion -> shared workers | Worker progress is supported only after the authority creation transaction and identity validation complete. |
| worker results -> test verdict | Every worker exception and unfinished thread must remain visible and fail the regression. |
| authority diagnostics -> validity claim | Equality between peers is insufficient; each must match the exact current application ID and initialized store identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-18-01 | Tampering | initialization order | high | mitigate | Call explicit initialization to completion before constructing and releasing independent worker authorities. |
| T-08-18-02 | Repudiation | worker outcome capture | medium | mitigate | Record every result and exception separately, require exact successful cardinality, and use bounded joins. |
| T-08-18-03 | Spoofing | authority identity | high | mitigate | Require every diagnostic application ID to equal `SQLITE_APPLICATION_ID` and require one shared nonempty store identity. |
| T-08-18-04 | Denial of Service | shared-worker regression | medium | mitigate | Use the existing finite event wait and `_join`; add no retry, sleep, scheduler assertion, or unbounded loop. |
| T-08-18-05 | Elevation of Privilege | test contract -> production lifecycle | high | mitigate | Restrict execution to the one test module and preserve the separate fail-closed and first-use tests without production, gate, dependency, workflow, or baseline edits. |
</threat_model>

<verification>
- The renamed canonical selector passes once through the deterministic initialized-root sequence; no repeated race retry is used as evidence.
- Both worker operations finish within the existing bound, produce no errors, and retain exact current SQLite authority identity.
- Existing initialized-thread/process, incomplete/foreign/future fail-closed, and single-process first-use selectors pass unchanged.
- Execution changes only `tests/test_phase3_postreview_concurrency.py`; Plan 08-16 then binds Plan 08-18, its five threats, and the exact renamed selector into the fixed inventory.
</verification>

<acceptance_criteria>
- The stale all-concurrent-first-users-succeed premise is absent from the canonical node.
- The replacement test proves only the supported initialized SQLite/filesystem workflow and its bounded no-error progress.
- Invalid authority evidence remains non-mutating and fail closed in existing tests.
- Single-process first-use convenience remains separately characterized.
- No lifecycle coordination mechanism or readiness-gate exception is added.
</acceptance_criteria>

<source_coverage>
- GOAL: Restore deterministic local-readiness evidence while preserving topology-specific nonclaims.
- REQ: QUAL-04 requires shared-worker fixtures to initialize first and distinguish supported progress from corruption.
- RESEARCH: Existing Phase 8 qualification guidance carries forward initialized-worker fixtures and forbids automatic race-repair loops.
- CONTEXT: D-02 keeps one lifecycle authority; D-03 separates progress from integrity/recovery; D-04 forbids silently stronger topology contracts; D-24 requires the deterministic local suite without changing lifecycle semantics.
- ADR/PHASE 3: ADR 0001 rule 7 and approved D-35/Plan 03-22 define explicit initialization before shared workers and decline concurrent-first-creation availability.
</source_coverage>

<success_criteria>
- The focused deterministic selector passes without exercising concurrent first creation.
- Exact initialized authority identity and subsequent usability are proven after bounded shared-worker completion.
- Existing fail-closed evidence and first-use convenience contracts remain unchanged and green.
- Plan 08-16 can resume without a production race patch or a weakened fixed gate.
</success_criteria>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-18-SUMMARY.md` when done.
</output>
