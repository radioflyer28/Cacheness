---
phase: 08-production-gates-and-performance-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tools/run_phase8_packaging.py
  - tests/packaging/test_wheel_matrix.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One built wheel proves the complete guaranteed base public surface plus generic-object, NumPy pickle, and native NPZ round trips (D-07)."
    - "Each literal advertised optional group installs in its own isolated environment, imports its guaranteed surface, and performs a representative public round trip (D-08)."
    - "The dataframe group exercises retained pandas and Polars Parquet handlers, while Blosc2 and TensorFlow are exercised only where their declared group is installed (D-07, D-08)."
  artifacts:
    - path: "tools/run_phase8_packaging.py"
      provides: "One-wheel, fresh-environment-per-group qualification runner"
    - path: "tests/packaging/test_wheel_matrix.py"
      provides: "Fail-closed base and optional-group package matrix contracts"
  key_links:
    - from: "pyproject.toml"
      to: "tools/run_phase8_packaging.py"
      via: "literal optional-group inventory parsed from wheel/project metadata"
      pattern: "optional-dependencies"
    - from: "tools/run_phase8_packaging.py"
      to: "tools/phase8_evidence.py"
      via: "sanitized packaging evidence for the exact wheel and revision"
      pattern: "phase8_evidence"
---

<objective>
Qualify the base wheel and every advertised optional dependency group in genuinely separate installations.

Purpose: Close QUAL-01 and QUAL-02 without allowing one all-extras environment or source checkout to hide packaging defects, per D-07 and D-08.
Output: An isolated packaging runner and exhaustive self-tests for the literal optional-group matrix.
</objective>

<scope_guard>
This plan verifies retained public formats and backends. It must not redesign handlers, add a format, change dependency groups, add a package, or treat an import-only probe as a representative round trip.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-PATTERNS.md
@pyproject.toml
@tests/test_full_suite_environment.py
@src/cacheness/__init__.py
@src/cacheness/storage/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the base wheel public surface and required formats</name>
  <files>tools/run_phase8_packaging.py, tests/packaging/test_wheel_matrix.py</files>
  <read_first>
    - tests/test_full_suite_environment.py: existing wheel build, isolated uv process, and memory round-trip pattern
    - pyproject.toml: core dependencies, package metadata, and exact optional-group names
    - src/cacheness/storage/__init__.py: guaranteed storage exports
  </read_first>
  <behavior>
    - "The probe builds one wheel and installs that artifact without the repository source path or inherited site packages."
    - "The base installation imports every guaranteed symbol from both public barrels and completes generic-object, NumPy pickle, and native NPZ round trips through public BlobStore/UnifiedCache composition."
    - "Base qualification fails if NumPy or obstore is absent, a source-tree import is used, a promised export disappears, or a round trip changes type/value."
  </behavior>
  <action>Build one wheel with `uv build --wheel`, hash the artifact, and run base probes through `uv run --isolated --no-project --with PATH_TO_WHEEL`. Enumerate guaranteed exports explicitly rather than using wildcard discovery. Construct the supported memory payload plus memory authority `StoreTopology`, then execute generic-object and retained NumPy pickle/native-NPZ round trips through the public facade per D-07. Record wheel digest, Python/platform identity, probe names, and exact source revision in the packaging evidence envelope.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/packaging/test_wheel_matrix.py -k base -x</automated>
  </verify>
  <done>A clean base wheel proves its promised imports and real public memory-backed object/NumPy behavior without source-tree leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify every advertised optional group independently</name>
  <files>tools/run_phase8_packaging.py, tests/packaging/test_wheel_matrix.py</files>
  <read_first>
    - pyproject.toml: `recommended`, `dataframes`, `tensorflow`, `s3`, `postgresql`, and `cloud` optional groups
    - src/cacheness/handlers.py: retained Blosc2, pandas, Polars, and TensorFlow handler selection
    - tests/test_handlers.py: native format round-trip assertions
  </read_first>
  <behavior>
    - "The literal six-group inventory is exact; omission, duplication, renaming, or an unexpected group fails before probing."
    - "Every group gets a fresh environment containing the same wheel plus only that group, imports its guaranteed surface, and completes a public round trip."
    - "Dataframes cover pandas and Polars Parquet; recommended covers Blosc2; TensorFlow runs only on compatible stable minors and is never converted into a skip-based pass."
    - "S3/PostgreSQL/cloud install probes do not claim live-service qualification; BACK-05 remains a separate evidence class."
  </behavior>
  <action>Drive six independent wheel-extra requirements from a fixed reviewed mapping. Use public `BlobStore`/`UnifiedCache` calls for representative behavior: Blosc2 under `recommended`; pandas and Polars Parquet under `dataframes`; a TensorFlow tensor under `tensorflow`; and guaranteed public imports plus a memory-backed round trip under `s3`, `postgresql`, and `cloud`. Per D-08, do not reach into arbitrary handler paths as the only proof. Treat unavailable wheels on a claimed compatible interpreter as failure, and emit explicit compatibility/non-live fields rather than service claims. No new dependency is authorized or required.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/packaging/test_wheel_matrix.py -x</automated>
  </verify>
  <done>All advertised groups install and execute independently, with format-specific retained behavior and no transitive all-extras or live-service overclaim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source checkout -> isolated probe | The source tree must not shadow the installed wheel. |
| package metadata -> environment resolver | Transitive dependencies must not conceal a broken individual extra. |
| serialized fixture -> public handler path | Integrity and type/value checks must complete before a round trip is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-02-01 | Spoofing | installed distribution identity | high | mitigate | Hash one wheel, install it by path in no-project environments, and assert imports resolve inside that wheel. |
| T-08-02-02 | Tampering | optional-group inventory | high | mitigate | Compare exact literal group names and run one fresh environment per group. |
| T-08-02-03 | Information Disclosure | subprocess logs | medium | mitigate | Persist only allow-listed package names, versions, probe labels, and sanitized failure summaries. |
| T-08-02-04 | Denial of Service | package resolution/probes | medium | mitigate | Apply per-environment timeouts and bounded fixtures; fail rather than retry indefinitely. |
| T-08-02-SC | Tampering | locked package installs | high | mitigate | Install only the existing wheel and declared groups under the committed lock/metadata; this plan adds no package or dependency source. |
</threat_model>

<verification>
- Base and per-extra self-tests pass with isolated subprocesses and representative public round trips.
- The matrix rejects source-tree leakage, missing groups, skips, and group-cross-contamination.
- Packaging evidence explicitly leaves real PostgreSQL/Amazon-S3 qualification unresolved.
</verification>

<success_criteria>
- QUAL-01 and deterministic QUAL-02 evidence are reproducible from one wheel.
- NumPy remains core and retained optional handlers remain functional.
- No dependency or handler architecture changes are introduced by qualification.
</success_criteria>

<source_coverage>
- GOAL: Reproducible release evidence across supported installations.
- REQ: QUAL-01, QUAL-02, QUAL-03.
- RESEARCH: One wheel, one fresh environment per literal group, and public round-trip probes.
- CONTEXT: D-07 and D-08.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-02-SUMMARY.md` when done.
</output>
