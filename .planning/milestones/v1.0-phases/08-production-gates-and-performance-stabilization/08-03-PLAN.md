---
phase: 08-production-gates-and-performance-stabilization
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tools/run_phase8_platform_gates.py
  - tests/qualification/test_phase8_platform.py
autonomous: true
requirements:
  - QUAL-03
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Linux qualification covers every supported stable Python minor 3.11 through 3.14; a prerelease minor is advisory only (D-05)."
    - "macOS boundary smoke covers 3.11 and 3.14, while Windows remains explicitly UNAVAILABLE/NOT_QUALIFIED (D-06)."
    - "Feature compatibility is explicit: core/non-TensorFlow groups cover the supported stable matrix and TensorFlow is qualified only on compatible stable minors, never by skip (D-05, D-08)."
  artifacts:
    - path: "tools/run_phase8_platform_gates.py"
      provides: "Exact platform/Python evidence producer and matrix validator"
    - path: "tests/qualification/test_phase8_platform.py"
      provides: "Supported, advisory, unavailable, and feature-compatibility contracts"
  key_links:
    - from: "tools/run_phase8_platform_gates.py"
      to: "tools/run_phase8_local_gates.py"
      via: "deterministic gate invocation on each qualifying interpreter"
      pattern: "run_phase8_local_gates"
    - from: "tools/run_phase8_platform_gates.py"
      to: "tools/phase8_evidence.py"
      via: "platform evidence envelope with exact interpreter and OS identity"
      pattern: "phase8_evidence"
---

<objective>
Define and prove the supported Python/platform qualification matrix without pretending the current host qualifies every target.

Purpose: Turn D-05 and D-06 into machine-readable evidence and preserve Windows as a truthful nonclaim.
Output: A platform gate runner and fail-closed matrix tests.
</objective>

<scope_guard>
This plan records runtime/platform evidence only. It does not add portability shims, weaken an unavailable platform into a pass, or change storage concurrency semantics to satisfy a platform probe.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md
@pyproject.toml
@verify_platform.py
@tools/run_phase8_local_gates.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the stable, advisory, and feature-aware Python matrix</name>
  <files>tools/run_phase8_platform_gates.py, tests/qualification/test_phase8_platform.py</files>
  <read_first>
    - pyproject.toml: Python support metadata and TensorFlow optional group
    - verify_platform.py: current compatibility smoke behavior
    - .planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md: Python 3.14/TensorFlow compatibility finding
  </read_first>
  <behavior>
    - "The fixed stable matrix is 3.11, 3.12, 3.13, and 3.14; missing stable rows fail qualification."
    - "Prerelease Python results are labeled advisory and cannot satisfy or invalidate a stable row."
    - "TensorFlow has an explicit compatible-minor subset through 3.13; a missing wheel is a nonqualifying result, not a skip-based pass."
  </behavior>
  <action>Implement a standalone runner that accepts an expected OS, Python minor, feature profile, and output path; verifies the actual interpreter/platform; invokes fixed smoke or deterministic commands; and emits bounded platform evidence. Define exact stable/advisory and TensorFlow-compatible sets from the researched release support facts per D-05/D-08. Make row aggregation reject duplicates, omissions, contradictory advisory status, and feature profiles outside their published compatibility range.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_platform.py -k python -x</automated>
  </verify>
  <done>The supported and feature-compatible interpreter sets are explicit, complete, and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate Linux qualification, macOS boundary smoke, and Windows nonclaim</name>
  <files>tools/run_phase8_platform_gates.py, tests/qualification/test_phase8_platform.py</files>
  <read_first>
    - tools/capture_phase3_windows_qualification.py: canonical UNAVAILABLE/NOT_QUALIFIED evidence distinction
    - tests/test_phase3_windows_qualification_attestation.py: native-evidence negative contracts
    - docs/adr/0001-topology-specific-storage-guarantees.md: topology-specific claims and typed outcomes
  </read_first>
  <behavior>
    - "All four stable minors require Linux full-gate evidence."
    - "macOS requires oldest/newest boundary smoke only and cannot substitute for Linux full-gate evidence."
    - "Windows always renders UNAVAILABLE/NOT_QUALIFIED for this phase and points to Phase 999.1; non-Windows execution cannot produce native Windows PASS."
    - "Contention success/conflict/typed retryable outcomes remain valid progress classifications rather than portability failures."
  </behavior>
  <action>Add platform-role aggregation and explicit command profiles per D-06. Linux rows execute the deterministic gate; macOS 3.11/3.14 rows execute boundary smoke against public memory and SQLite/filesystem composition; Windows emits canonical non-native evidence and exit code 2 without executing a substitute. Assert ADR 0001 progress labels in results so a typed retryable contention outcome is not rewritten as corruption or a demand for new coordination.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_platform.py -x</automated>
  </verify>
  <done>Platform evidence reflects its real scope: full Linux qualification, macOS boundary smoke, advisory prerelease runs, and no Windows claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow labels -> actual runtime | A claimed Python/OS row must match the executing interpreter and kernel. |
| advisory/unsupported result -> release matrix | Nonblocking or unavailable evidence must not fill a required stable slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-03-01 | Spoofing | interpreter/platform identity | high | mitigate | Compare requested row to runtime facts and persist exact sanitized versions in signed-shape evidence. |
| T-08-03-02 | Tampering | matrix completeness | high | mitigate | Validate a fixed set with exact one-row-per-slot semantics and reject duplicate/extra substitutions. |
| T-08-03-03 | Repudiation | Windows qualification | high | mitigate | Reuse canonical native-evidence fields and require native Windows execution for PASS; Phase 8 emits only nonclaim state. |
| T-08-03-04 | Denial of Service | interpreter jobs | low | accept | Matrix rows are finite and isolated; ordinary CI cancellation is visible as missing evidence and cannot qualify. |
</threat_model>

<verification>
- Platform self-tests cover every stable/advisory/feature/platform role.
- Missing, mislabeled, duplicated, skipped, and non-native rows fail aggregation.
- No portability or lifecycle production source is changed.
</verification>

<success_criteria>
- Supported Python and platform claims are explicit and reproducible.
- TensorFlow limitations are documented in evidence rather than hidden by all-extras resolution.
- Windows remains NOT_QUALIFIED until its dedicated backlog phase.
</success_criteria>

<source_coverage>
- GOAL: Reproducible release evidence across supported Python versions and platforms.
- REQ: QUAL-03.
- RESEARCH: Linux 3.11-3.14, macOS boundary, advisory prerelease, TensorFlow-compatible subset.
- CONTEXT: D-05, D-06, D-08.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-03-SUMMARY.md` when done.
</output>
