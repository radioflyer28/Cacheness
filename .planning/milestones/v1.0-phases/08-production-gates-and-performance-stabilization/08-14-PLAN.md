---
phase: 08-production-gates-and-performance-stabilization
plan: 14
type: execute
wave: 7
depends_on:
  - 08-13
files_modified:
  - tools/verify_phase8_release.py
  - tests/qualification/test_phase8_release.py
  - tools/verify_phase8_contracts.py
  - tests/test_phase8_contract_verifier.py
  - docs/RELEASE_QUALIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - BACK-05
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-07
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Current release collection dispatches only the quality and protected live workflows; the retained controlled-performance workflow remains available for SEED-006 but is not a current release prerequisite (D-23)."
    - "The exact-SHA aggregate requires every non-deferred evidence class and records controlled performance as both DEFERRED and NOT_QUALIFIED with an exact QUAL-06/SEED-006 reference (D-03, D-23)."
    - "A controlled-performance artifact, macOS timing result, omitted deferral record, or missing non-deferred class cannot enter or satisfy current release qualification (D-03, D-20, D-22, D-23)."
    - "The fixed verifier binds Plan 08-14, D-23, the current requirement inventory, and every new threat to literal tests while preserving the implemented benchmark/hashing/preflight capability (D-18, D-19, D-23)."
    - "All publication commands are implemented and adversarially tested before the exact candidate SHA is selected, so publication cannot invalidate D-10 evidence by changing tracked qualification tooling after collection (D-10, D-13, D-23)."
  artifacts:
    - path: "tools/verify_phase8_release.py"
      provides: "Exact current release workflow/artifact inventory plus explicit controlled-performance deferral"
    - path: "tests/qualification/test_phase8_release.py"
      provides: "Adversarial current-required versus deferred evidence contracts"
    - path: "tools/verify_phase8_contracts.py"
      provides: "Literal Plan 08-14, D-23, requirement, threat, and external-status verification"
    - path: "docs/RELEASE_QUALIFICATION.md"
      provides: "Public current release evidence/nonclaim boundary"
  key_links:
    - from: "tools/verify_phase8_release.py"
      to: ".planning/seeds/SEED-006-qualify-controlled-linux-performance.md"
      via: "closed deferred-requirement record in the canonical aggregate"
      pattern: "QUAL-06|SEED-006|DEFERRED|NOT_QUALIFIED"
    - from: "tools/verify_phase8_contracts.py"
      to: "tests/test_phase8_contract_verifier.py"
      via: "literal Plan 14, D-23, requirement, and T-08-14 selector ownership"
      pattern: "08-14|D-23|T-08-14"
    - from: "tools/verify_phase8_release.py"
      to: ".planning/phases/08-production-gates-and-performance-stabilization/08-12-PLAN.md"
      via: "prequalified publication-preflight, prepare-draft, and publish-and-verify commands"
      pattern: "publication-preflight|prepare-draft|publish-and-verify"
---

<objective>
Cut the current release evidence boundary over to the user-approved performance deferral without weakening or fabricating any qualification claim.

Purpose: Make D-23 executable before external evidence collection so the release can proceed on every non-deferred class while QUAL-06 remains visibly unqualified for SEED-006.
Output: Updated release collector/aggregator, complete pre-candidate publication controller, fixed verifier, adversarial tests, and qualification documentation.
</objective>

<scope_guard>
This plan changes qualification policy and evidence tooling only. Preserve the benchmark harness, workload coverage, SHA-256/XXH3 comparison, runner preflight, and performance workflow. Do not capture a baseline, provision a runner, substitute macOS evidence, change a performance threshold, modify TensorFlow support, or alter storage/lifecycle/concurrency code.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/seeds/SEED-006-qualify-controlled-linux-performance.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-10-SUMMARY.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cut exact-SHA collection and aggregation over to the non-deferred evidence set</name>
  <files>tools/verify_phase8_release.py, tests/qualification/test_phase8_release.py</files>
  <read_first>
    - tools/verify_phase8_release.py: WORKFLOW_SPECS, ARTIFACT_EVIDENCE_CLASS, collect_workflow_evidence, write_collection_manifest, aggregate_collection, and publication asset validation
    - tests/qualification/test_phase8_release.py: FakeGh, exact-run collection, aggregate inventory, and publication-asset adversarial patterns
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: D-03, D-10 through D-13, and D-23
    - .planning/seeds/SEED-006-qualify-controlled-linux-performance.md: exact future capability and macOS non-substitution boundary
  </read_first>
  <behavior>
    - `test_preflight_collection_reports_exact_current_inventory`: read-only preflight validates GitHub auth/visibility/dispatch permission, trusted definitions, and the quality/live-only current inventory before dispatch
    - `test_required_release_collection_excludes_deferred_performance`: current release collection dispatches `quality.yml` and `live_qualification.yml` only, while the retained performance workflow specification remains inspectable for SEED-006
    - `test_aggregate_records_exact_deferred_performance_nonclaim`: the collection manifest and aggregate require exactly deterministic, packaging, platform, coverage, structural, and live-service artifacts for one SHA/source digest, plus a closed controlled-performance record with status `DEFERRED`, qualification `NOT_QUALIFIED`, requirement `QUAL-06`, and the exact SEED-006 path
    - `test_deferred_performance_artifacts_and_macos_diagnostics_are_rejected`: a controlled-performance artifact, macOS diagnostic, altered deferral record, missing required artifact, duplicate class, stale SHA/source digest, or non-clean live envelope fails closed
  </behavior>
  <action>Start with the four failing behaviors above. Retain the full performance workflow/artifact schema as future capability, but define a separate literal current-release workflow and artifact inventory that contains only quality plus protected live production. Add a read-only `preflight-collection` CLI path behind the existing injectable Git/GitHub command seam; it must validate `gh auth status`, current-workflow visibility and dispatch permission, trusted workflow definitions, the exact current artifact inventory, and prerequisite local gates without dispatching or mutating anything. Make collection iterate only that current inventory. Change aggregate validation to require exactly the six non-deferred classes and emit one immutable, closed deferral record for controlled performance containing both `DEFERRED` and `NOT_QUALIFIED`, `QUAL-06`, and `.planning/seeds/SEED-006-qualify-controlled-linux-performance.md`. Bump the aggregate schema identifier because the required evidence set and record shape changed and no release aggregate has yet been published. Reject any performance or macOS timing artifact from current collection/aggregation rather than silently ignoring it. Preserve exact run-ID, fixed-name, SHA/source-digest, live QUALIFIED/CLEAN, bounded JSON, sanitization, and atomic-write checks per D-10 through D-13. Do not edit the benchmark or performance workflow.</action>
  <verify>
    <automated>uv run --isolated --group dev --frozen pytest -q -o log_cli=false tests/qualification/test_phase8_release.py::test_preflight_collection_reports_exact_current_inventory tests/qualification/test_phase8_release.py::test_required_release_collection_excludes_deferred_performance tests/qualification/test_phase8_release.py::test_aggregate_records_exact_deferred_performance_nonclaim tests/qualification/test_phase8_release.py::test_deferred_performance_artifacts_and_macos_diagnostics_are_rejected -x</automated>
  </verify>
  <done>One tested exact-SHA path collects and aggregates every current release class, records the exact QUAL-06 deferral, and rejects performance substitution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the revised evidence boundary into the fixed verifier and public qualification contract</name>
  <files>tools/verify_phase8_contracts.py, tests/test_phase8_contract_verifier.py, docs/RELEASE_QUALIFICATION.md</files>
  <read_first>
    - tools/verify_phase8_contracts.py: reviewed plan/requirement/threat inventories, DECISION_NODES, external evidence classes, render_external_statuses, and all-mode exit behavior
    - tests/test_phase8_contract_verifier.py: literal inventory mutation and external nonclaim tests
    - docs/RELEASE_QUALIFICATION.md: evidence matrix, protected service/performance boundaries, and publication rules
    - .planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md: revised evidence classes and Plan 08-14 test map
  </read_first>
  <behavior>
    - `test_fixed_manifest_covers_deferred_performance_decision`: fixed inventory contains Plans 01-14, decisions D-01 through D-23, T-08-14-01 through T-08-14-04, and current requirements excluding deferred QUAL-06
    - `test_deferred_performance_status_is_nonblocking_but_not_qualified`: external status renders controlled performance as `DEFERRED`/`NOT_QUALIFIED` with SEED-006 and does not count that status as a current blocking prerequisite
    - `test_all_mode_still_blocks_on_unavailable_current_evidence`: all-mode still fails truthfully for any unavailable current class, especially live services, while the controlled deferral alone cannot cause or cure another class's result
    - `test_release_documentation_preserves_seed006_nonclaim`: the documentation names the retained future performance machinery, rejects macOS/Linux equivalence, and excludes controlled-performance diagnostics from current release assets
  </behavior>
  <action>Add Plan 08-14, D-23, the seven current requirement IDs, four T-08-14 threats, and their exact selectors to the fixed literal inventories. Keep QUAL-06 present in the requirements document as deferred but outside `PHASE8_REQUIREMENTS`. Render its controlled-performance state as `DEFERRED` plus `NOT_QUALIFIED` and a SEED-006 reference; classify only required current evidence as release blocking. Update all-mode tests so current unavailable classes still return the existing prerequisite outcome while controlled deferral is informational and cannot be converted to PASS. Update the release qualification evidence matrix, protected boundary, aggregate, retention, and publication asset sections to state that the harness/workflow/preflight remain available but controlled performance is not collected or published in this milestone. State explicitly that macOS timings are diagnostic and establish neither Linux equivalence nor a cross-platform budget. Preserve ADR 0001's correctness/progress/performance separation and every live-service rule.</action>
  <verify>
    <automated>uv run --isolated --group dev --frozen pytest -q -o log_cli=false tests/test_phase8_contract_verifier.py::test_fixed_manifest_covers_deferred_performance_decision tests/test_phase8_contract_verifier.py::test_deferred_performance_status_is_nonblocking_but_not_qualified tests/test_phase8_contract_verifier.py::test_all_mode_still_blocks_on_unavailable_current_evidence tests/test_phase8_contract_verifier.py::test_release_documentation_preserves_seed006_nonclaim -x &amp;&amp; uv run --isolated --group dev --frozen ruff check tools/verify_phase8_contracts.py tools/verify_phase8_release.py tests/test_phase8_contract_verifier.py tests/qualification/test_phase8_release.py &amp;&amp; uv run --isolated --group dev --frozen ruff format --check tools/verify_phase8_contracts.py tools/verify_phase8_release.py tests/test_phase8_contract_verifier.py tests/qualification/test_phase8_release.py</automated>
  </verify>
  <done>The fixed verifier and public documentation enforce D-23 without weakening any current evidence class or deleting future performance capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish the publication controller before selecting the exact candidate</name>
  <files>tools/verify_phase8_release.py, tests/qualification/test_phase8_release.py</files>
  <read_first>
    - tools/verify_phase8_release.py: existing Git/GitHub command injection seam, aggregate validation, publication asset validation, and verify-published behavior
    - tests/qualification/test_phase8_release.py: FakeGh mutation recording, asset digest/content checks, and remote-state adversarial cases
    - .planning/phases/08-production-gates-and-performance-stabilization/08-12-PLAN.md: exact prepublication, approval, publication, and final verification contract
    - docs/RELEASE_QUALIFICATION.md: current non-deferred asset allow-list and D-23 deferral record
  </read_first>
  <behavior>
    - `test_publication_preflight_fails_before_mutation`: read-only preflight rejects unavailable immutable policy/permission, wrong tag/SHA, incomplete non-deferred evidence, altered QUAL-06 deferral, or forbidden assets before draft mutation
    - `test_prepare_draft_uploads_exact_non_deferred_asset_set`: draft preparation uploads exactly the aggregate plus its non-deferred referenced assets and rejects controlled-performance, macOS diagnostic, or extra assets
    - `test_prepare_draft_verifies_uploaded_states_digests_and_content`: every uploaded asset has state `uploaded`, matching local/API SHA-256, and valid reparsed content before a canonical prepublication report is written
    - `test_publish_requires_exact_approved_prepublication_digest`: publication requires the exact approved report digest and rechecks unchanged tag, draft, assets, digests, and deferral immediately before mutation
    - `test_publish_and_verify_requires_immutable_exact_remote_state`: final verification requires non-draft immutable state, exact assets, matching digests, and successful GitHub release plus per-asset verification
    - `test_postpublication_mismatch_records_unqualified_incident`: any postpublication state, digest, asset, or deferral mismatch produces an unqualified incident record without repairing or rewriting evidence
  </behavior>
  <action>Implement `publication-preflight`, `prepare-draft`, and `publish-and-verify` behind the existing injectable Git/GitHub command seam now, before Plan 08-11 chooses and qualifies the exact candidate. The read-only preflight must validate authenticated least privilege, immutable-release policy, exact tag/SHA, the complete non-deferred aggregate, the exact D-23 deferral, and the local asset allow-list before mutation. Draft preparation must target the aggregate-bound SHA, upload exactly the aggregate and its non-deferred referenced assets, and verify remote state plus API/local SHA-256 equality before writing the canonical prepublication report. Publication must require an approval-bound report digest, revalidate unchanged state immediately before the one-way transition, publish only that draft, then prove non-draft immutable state and every release/asset verification result without repair. Keep controlled-performance/macOS diagnostics outside the asset set and preserve failed/unavailable/scheduled diagnostic retention. This implementation belongs here because changing tracked qualification tooling after Plan 08-11 collection would invalidate D-10 evidence.</action>
  <verify>
    <automated>uv run --isolated --group dev --frozen pytest -q -o log_cli=false tests/qualification/test_phase8_release.py::test_publication_preflight_fails_before_mutation tests/qualification/test_phase8_release.py::test_prepare_draft_uploads_exact_non_deferred_asset_set tests/qualification/test_phase8_release.py::test_prepare_draft_verifies_uploaded_states_digests_and_content tests/qualification/test_phase8_release.py::test_publish_requires_exact_approved_prepublication_digest tests/qualification/test_phase8_release.py::test_publish_and_verify_requires_immutable_exact_remote_state tests/qualification/test_phase8_release.py::test_postpublication_mismatch_records_unqualified_incident -x &amp;&amp; uv run --isolated --group dev --frozen ruff check tools/verify_phase8_release.py tests/qualification/test_phase8_release.py &amp;&amp; uv run --isolated --group dev --frozen ruff format --check tools/verify_phase8_release.py tests/qualification/test_phase8_release.py</automated>
  </verify>
  <done>The complete publication controller is tested and committed before exact-SHA collection, so Plans 08-11 and 08-12 require no tracked qualification-tool changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| user-approved deferral -> release policy | Only the controlled-Linux/QUAL-06 class is deferred; no other class may inherit the exception. |
| exact workflow artifacts -> release aggregate | Only fixed required quality/live artifacts for the same SHA/source may enter qualification. |
| diagnostic performance data -> public claims | macOS or uncontrolled timing data must remain outside qualifying evidence and publication assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-14-01 | Tampering | release evidence inventory | high | mitigate | Use separate literal current and deferred inventories; adversarially reject missing/extra/cross-class artifacts. |
| T-08-14-02 | Spoofing | macOS performance diagnostic | high | mitigate | Reject timing artifacts from the current aggregate and name controlled Linux as unqualified under SEED-006. |
| T-08-14-03 | Repudiation | deferred requirement record | medium | mitigate | Emit a closed canonical record containing D-23 status, requirement, qualification, and exact seed path. |
| T-08-14-04 | Elevation of Privilege | deferral scope | high | mitigate | Fixed verifier keeps every non-deferred requirement/evidence class mandatory and tests that the exception cannot broaden. |
</threat_model>

<verification>
- Focused release tests prove exact quality/live collection, same-SHA aggregation, and the closed QUAL-06 deferral record.
- Fixed-verifier tests prove literal Plan 14/D-23/threat/current-requirement ownership and truthful external status behavior.
- Publication-controller tests prove fail-before-mutation preflight, exact draft assets/digests, approval binding, immutable publication, and read-only postpublication verification before exact-SHA evidence collection.
- Scoped Ruff lint and format checks pass for every modified Python file.
- No benchmark, performance workflow, storage, lifecycle, concurrency, or TensorFlow file changes.
</verification>

<acceptance_criteria>
- Current release collection never dispatches the performance workflow.
- The release aggregate cannot exist without every non-deferred exact-SHA class and one exact controlled-performance deferral record.
- Controlled performance remains `DEFERRED`/`NOT_QUALIFIED`; macOS evidence is not represented as Linux-equivalent or qualifying.
- Plan 08-11 can collect and aggregate without a Linux runner, while real PostgreSQL/Amazon-S3 evidence remains mandatory.
- Plans 08-11 and 08-12 make no tracked source change after the exact candidate is selected.
</acceptance_criteria>

<success_criteria>
- D-23 is executable and fail-closed before external release evidence is collected.
- QUAL-06 remains deferred to SEED-006 without deleting or weakening its future harness.
- Every non-deferred Phase 8 evidence class retains its prior exact-SHA, source-digest, sanitization, and terminal-state rules.
</success_criteria>

<source_coverage>
- GOAL: Preserve reproducible release evidence while truthfully excluding one explicitly deferred class.
- REQ: BACK-05, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-07.
- RESEARCH: Exact-SHA evidence separation, strict aggregation, diagnostics non-substitution, and immutable publication inputs.
- CONTEXT: D-03, D-10, D-11, D-12, D-13, D-18, D-19, D-20, D-22, D-23.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-14-SUMMARY.md` when done.
</output>
