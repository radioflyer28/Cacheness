---
phase: 11-clean-supplemental-documentation-and-normalize-validation-ev
plan: 13
type: execute
wave: 11
depends_on: [11-12]
files_modified:
  - tests/test_phase9_evidence_metadata.py
  - .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
  - .planning/v1.0-v1.0-MILESTONE-AUDIT.md
autonomous: true
gap_closure: true
requirements: [D-18, D-19, D-20]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A refreshed milestone audit fails closed when the Phase 11 qualified source revision is missing, malformed, stale, or followed by protected-tree drift; the original historical audit branch remains valid (D-19)."
    - "The new committed provenance-test revision receives a fresh, observed bounded local acceptance before the audit's final verdict is re-derived (D-18, D-19)."
    - "The final audit head equals that newly qualified revision, with later commits limited to planning evidence; BACK-05, QUAL-06, native Windows, and immutable publication remain nonpassing (D-19, D-20)."
  artifacts:
    - path: tests/test_phase9_evidence_metadata.py
      provides: "Required refreshed-audit provenance assertion and missing-field regression"
    - path: .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
      provides: "New exact-revision local acceptance record without rewriting earlier runs"
    - path: .planning/v1.0-v1.0-MILESTONE-AUDIT.md
      provides: "Last-derived verdict bound to the newly qualified source/test revision"
  key_links:
    - from: tests/test_phase9_evidence_metadata.py
      to: .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
      via: "The refreshed branch requires qualified_source_revision and always checks Git ancestry and protected-tree cleanliness"
      pattern: "qualified_source_revision|_assert_refreshed_audit_provenance"
    - from: .planning/v1.0-v1.0-MILESTONE-AUDIT.md
      to: .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
      via: "audited_head is exactly the newly qualified_source_revision after successful local acceptance"
      pattern: "audited_head|qualified_source_revision"
  prohibitions:
    - "No lifecycle, concurrency, recovery, topology, or handler-persistence code changes (D-08, D-16)."
    - "No final audit verdict before the new source/test revision's local gates pass (D-18, D-19)."
    - "No promotion of deferred external, platform, performance, or publication claims (D-20)."
---

<objective>
Close the verifier's final provenance gap: a refreshed audit must not pass without a qualified source revision, and the test change itself must be requalified before the audit is finalized.

Purpose: Make the existing milestone verdict fail closed under missing provenance without creating a second evidence authority or reopening storage lifecycle design.
Output: One strict parser regression, one newly qualified local acceptance record, and one last-derived milestone audit.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-CONTEXT.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VERIFICATION.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-11-SUMMARY.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-12-SUMMARY.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@tests/test_phase9_evidence_metadata.py
@docs/adr/0001-topology-specific-storage-guarantees.md

The Phase 11 architecture is already proven; this gap closes a test-guard bypass and rebinds derived evidence, so no new storage tracer or lifecycle work is warranted. The only actionable verifier gap is the optional field in the refreshed-audit branch. Earlier Phase 11 decisions D-01–D-17 are implemented by Plans 11-01–11-10 and remain unchanged.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject missing provenance through the refreshed-audit parser</name>
  <files>tests/test_phase9_evidence_metadata.py</files>
  <behavior>
    - A synthetic refreshed audit with a validated Phase 11 frontmatter lacking qualified_source_revision raises AssertionError.
    - Supplying the exact qualified fixture revision passes; supplying a stale audit head fails.
    - The original audited timestamp/head pair still takes the historical branch without requiring a qualification field.
  </behavior>
  <action>
    Per D-19, write the negative missing-field regression first against the same parsing path used by test_phase11_refreshed_milestone_audit_is_evidence_derived. Factor that refreshed branch's provenance extraction into one focused helper if needed: obtain qualified_source_revision with the required _frontmatter_value lookup, then unconditionally invoke _assert_refreshed_audit_provenance. Use the existing temporary-Git fixture to prove both absent-field rejection and a valid exact-revision case. Leave the original-audit branch and its transition markers intact; do not impose the refreshed-audit equality check on test_phase11_validation_record_matches_final_acceptance_evidence before the audit is rebound. Do not change _assert_qualified_source_tree's protected inventory or weaken any existing malformed, nonancestor, dirty-tree, or stale-head checks. Commit this test-file change before any qualification run so its full Git SHA is the new candidate revision (D-18, D-19). An interim failure of the two real-repository evidence selectors is expected until Task 2 rebinds their artifacts; do not treat that temporary mismatch as a storage defect.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase9_evidence_metadata.py::test_refreshed_audit_rejects_missing_qualified_source_revision tests/test_phase9_evidence_metadata.py::test_qualified_source_provenance_accepts_artifact_only_commit_before_audit -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_phase9_evidence_metadata.py</automated>
  </verify>
  <done>The refreshed parser always requires and checks the revision; a missing-field regression fails before implementation and passes afterward, while historical and exact-revision cases remain valid.</done>
</task>

<task type="auto">
  <name>Task 2: Requalify the committed test tree and derive the audit last</name>
  <files>.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md, .planning/v1.0-v1.0-MILESTONE-AUDIT.md</files>
  <action>
    Per D-18/D-19, capture the full HEAD SHA after Task 1's test commit and assert that the protected tree is otherwise clean. Before changing either evidence artifact, assert it has no pre-existing uncommitted edit, record the exact prior qualified_source_revision and audited_head values and task-owned diff, and prepare a scoped restoration for both files. The existing validation/audit still identify the previous qualified revision, so use a try/finally or equivalent interruption-safe cleanup boundary to make an uncommitted, temporary provenance-only binding of qualified_source_revision in 11-VALIDATION.md and audited_head in the audit to the new SHA. Keep the existing audit status, verdict, scores, timestamp, and validation approval untouched during this gate; do not commit or report the temporary binding as accepted evidence. This permits the exact frozen non-live command to exercise both strict real-repository selectors rather than excluding them. Run uv lock --check; the focused CR-01/WR-01, documentation/example/validation, and fresh digest-bound source-free-wheel contracts; scoped Ruff on the changed test; and the exact frozen non-live command recorded in 11-VALIDATION.md. Only after every gate passes, append a separately dated acceptance section to 11-VALIDATION.md naming the new full revision and only observed command results, preserving the original Plan 11-09 run and Plan 11-11 post-review run as history. Then re-derive the audit as the last substantive write: retain audited_head equal to the new qualified_source_revision, set audited to the actual post-gate UTC derivation time, update the Phase 11 provenance narrative and only evidence-supported scores/verdict, and preserve BACK-05/SEED-007, QUAL-06/SEED-006, native Windows/Phase 999.1, and NOT_PUBLISHED. Confirm later committed differences from the new SHA are planning/evidence only and no staged, unstaged, or untracked protected path exists. Run the final focused parsers before committing either artifact. On any gate, final-parser, or pre-commit failure or interruption, restore both exact prior provenance field values and all other task-owned artifact edits with scoped reverse patches (never a destructive reset), verify the two files match their pre-task content while preserving unrelated changes, and stop without an accepted status/verdict change. On resume after an abrupt termination, inspect for an uncommitted temporary binding and restore it before any new gate or commit. A genuine lifecycle failure follows ADR 0001's D-16 stop rule, not a fix loop. Commit the two final evidence artifacts only after all gates and focused parsers pass (D-18–D-20).
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase9_evidence_metadata.py tests/qualification/test_phase8_release.py tests/test_phase9_documentation.py -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_phase9_evidence_metadata.py</automated>
  </verify>
  <done>The newly committed test revision has a successful exact frozen non-live run and bounded focused/package gates; validation records its actual results, the final audit is later and names precisely that revision, and all four deferred classes remain nonpassing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validation frontmatter → audit verdict | A missing qualified revision must not bypass Git-backed provenance checks in a refreshed audit. |
| qualified source/test commit → planning evidence | Evidence-only artifacts may follow qualification; any protected-tree change invalidates the prior run. |
| local acceptance → external claims | A local pass must not promote remote, controlled-Linux, native-Windows, or immutable-publication status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-38 | Spoofing | refreshed-audit source identity | high | mitigate | Task 1 makes the field required and adds a missing-field negative regression through the real parser path. |
| T-11-39 | Tampering | post-qualification source/test tree | high | mitigate | Task 2 binds to the committed Task 1 SHA and reuses ancestor, committed-diff, staged, unstaged, and untracked protected-path checks. |
| T-11-40 | Repudiation | final local acceptance and audit record | medium | mitigate | Task 2 records observed gates and derives a post-gate audit timestamp and exact revision without rewriting historical runs. |
| T-11-41 | Spoofing | deferred qualification status | high | mitigate | Task 2 preserves all four nonpassing classes and runs focused release/documentation contracts. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Required outcome / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Refreshed local milestone verdict without lifecycle redesign | 11-01–11-13 | COVERED |
| REQ | — | No newly mapped requirement IDs; deferred BACK-05 and QUAL-06 remain unchecked | 11-01–11-13 | COVERED |
| RESEARCH | validation/audit | Existing canonical evidence, fresh wheel, acceptance before final audit | 11-09–11-13 | COVERED |
| RESEARCH | cutover/docs/Phase 3 | Prior phase work and ADR stop boundary remain unchanged | 11-01–11-10 | COVERED |
| CONTEXT | D-01–D-17 | Earlier locked implementation decisions | 11-01–11-10 | COVERED; preserved |
| CONTEXT | D-18 | Bounded local acceptance on the new committed test revision | 11-13 Task 2 | COVERED |
| CONTEXT | D-19 | Fail-closed refreshed-audit provenance, audit derived after gates | 11-13 Tasks 1–2 | COVERED |
| CONTEXT | D-20 | External/platform/performance/publication nonclaims remain nonpassing | 11-13 Task 2 | COVERED |

<verification>
Task 1 first proves the bypass closed with a negative fixture and commits the protected test change. Task 2 requalifies that exact commit, retains only observed local results, and finalizes the audit last. The independent Phase 11 verifier then reruns against the committed artifacts.
</verification>

<success_criteria>
- Missing qualified_source_revision fails the refreshed-audit parser while the historical audit transition remains valid.
- The exact frozen non-live suite passes on the new committed test revision, and focused wheel/documentation/release/evidence checks pass without a new lifecycle change.
- The audit's final timestamp follows that run; audited_head equals qualified_source_revision and the protected tree has not changed since qualification.
- The phase's four explicit external/platform/performance/publication nonclaims remain nonpassing.
</success_criteria>

<output>
Create `.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-13-SUMMARY.md` when done.
</output>
