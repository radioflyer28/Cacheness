---
phase: 02-canonical-storage-and-integrity-contract
plan: "05"
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - src/cacheness/storage/integrity.py
  - src/cacheness/security.py
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store_integrity.py
autonomous: true
requirements: [STOR-02, STOR-08, SECU-03, SECU-04, SECU-05, SECU-08, MIGR-07]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Required HMAC-SHA256 signing accepts exact authorized key/provider and signature inputs, while every invalid configuration produces its stable typed outcome before repository or payload access (SECU-04 planner assumption, unclassified by the generic edge probe; D-09, D-10)."
    - "Authentication precedes critical-field use; committed/key/format/locator validation precedes one snapshot; SHA-256 and size verification precede handler deserialization on that same snapshot (SECU-03 planner assumption, unclassified by the generic edge probe; D-07, D-08)."
    - "Every critical structural field is signed and any mutation fails before payload access (SECU-05 planner assumption, unclassified by the generic edge probe; D-09)."
    - "Tampered, missing, or replaced payload evidence yields a stable typed integrity outcome while pre/post manifest bytes, payload bytes, mtimes, key material, and access bookkeeping remain equal (STOR-08/SECU-08/MIGR-07 planner assumptions; D-05)."
  artifacts:
    - path: src/cacheness/storage/integrity.py
      provides: "Strict manifest key provider, fixed HMAC signer, and one-snapshot SHA-256/size verifier"
    - path: src/cacheness/security.py
      provides: "Explicit separation between legacy CacheEntrySigner and canonical strict signing"
    - path: src/cacheness/storage/blob_store.py
      provides: "Complete authenticated-before-deserialize direct get pipeline"
    - path: tests/test_blob_store_integrity.py
      provides: "Key/signature/tamper/snapshot/order adversarial matrix"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/integrity.py
      via: "Strict key/signature verification and same-snapshot digest/size verification"
      pattern: "verify_(manifest|payload)"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/handlers.py
      via: "Handler is resolved and invoked only after authenticated identity and verified snapshot"
      pattern: "get_handler_by_type"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Complete the fail-closed authenticity and one-snapshot payload integrity boundary for direct BlobStore reads.

Purpose: Ensure no metadata-controlled I/O or trusted-payload deserialization occurs before every applicable storage authenticity and integrity check succeeds.
Output: Strict required signing, complete signed projection, ordered direct reads, and adversarial tamper coverage.
</objective>

## Artifacts this phase produces

- Strict exact-key/provider behavior for canonical manifests.
- Complete fixed HMAC-SHA256 projection with constant-time verification.
- Same-snapshot SHA-256 and byte-size enforcement before handler access.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-PATTERNS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-03-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-12-SUMMARY.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-02-SUMMARY.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-03-SUMMARY.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-04-SUMMARY.md
</context>

<planner_assumptions>
- SECU-03, SECU-04, SECU-05, STOR-02, STOR-08, SECU-08, and MIGR-07 were unclassified by the generic edge probe; the ordered pipeline and strict failure matrix are direct requirement/context acceptance.
- Exact 32-byte application-supplied material or a narrow provider is accepted cross-platform. File-backed required mode must attest a regular file, owner, and restrictive permissions on POSIX; platforms unable to attest that policy return a typed unsupported configuration.
- Payload byte size is an integrity claim, not a new application payload-size ceiling.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict required canonical signing and key provenance</name>
  <depends_on>Plans 02-01 and 02-04 provide canonical bytes plus public typed errors; Plan 02-02 provides persistent local reopen cases.</depends_on>
  <reversibility rating="one-way">D-09 freezes HMAC-SHA256 and the complete schema-1 signed projection as a persisted authenticity contract.</reversibility>
  <files>src/cacheness/storage/integrity.py, src/cacheness/security.py, tests/test_blob_store_integrity.py</files>
  <read_first>
    <file>src/cacheness/security.py</file>
    <file>src/cacheness/config.py</file>
    <file>tests/test_cache_integrity.py</file>
    <file>src/cacheness/storage/path_security.py</file>
  </read_first>
  <behavior>
    - "Exact 32-byte supplied key material signs and verifies canonical unsigned bytes with HMAC-SHA256 and constant-time comparison."
    - "Missing/short/long/unreadable key, unsafe POSIX ownership/mode, symlink/non-regular key path, unsupported algorithm/provider, absent signature, and invalid signature fail typed."
    - "Reopening an existing signed store never creates/replaces a key, switches to ephemeral material, or authorizes unsigned records."
  </behavior>
  <action>Write the strict signing/key matrix in `tests/test_blob_store_integrity.py` first. Implement a canonical strict provider separate from the legacy `CacheEntrySigner`; keep the latter only for Phase 1 compatibility verification. Accept exact application-supplied key bytes and a narrow provider contract. For file-backed required signing, use no-follow regular-file inspection and attest ownership/mode where the platform can truthfully enforce it; return a typed unsupported configuration elsewhere. Allow exclusive creation only for a provably empty new store when explicitly initialized, never during reads or reopen. Sign every canonical field except the signature value and verify with byte-to-byte `hmac.compare_digest`. Never log secrets or inherit configurable signed-field subsets per D-09/D-10.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_integrity.py -k "key or signature or signer or projection" -x &amp;&amp; uv run ruff check src/cacheness/storage/integrity.py src/cacheness/security.py tests/test_blob_store_integrity.py</automated>
  </verify>
  <acceptance_criteria>All key/provider/signature failure modes produce exact public reasons; wrong/absent signatures fail before payload events; existing signed-store reopen causes no key-file mutation or ephemeral fallback.</acceptance_criteria>
  <done>Canonical required signing is strict, complete, deterministic, and non-downgradable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce authenticate-validate-snapshot-digest-deserialize ordering</name>
  <depends_on>Task 1 supplies strict authenticity; Plans 02-03/02-04 supply format support and typed outcomes.</depends_on>
  <files>src/cacheness/storage/integrity.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_integrity.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>src/cacheness/handlers.py</file>
    <file>tests/test_stored_compatibility.py</file>
  </read_first>
  <behavior>
    - "The event sequence is bounded decode/version, manifest authentication, critical validation, one snapshot, streaming digest/size, handler; exactly one snapshot occurs."
    - "Non-committed state, key mismatch, unsafe locator, unsupported handler identity, invalid digest/size, missing payload, same-length replacement, truncation, extension, and snapshot-boundary replacement never invoke the handler."
    - "Digest and handler consume the same live private snapshot; no managed locator is reopened by a handler."
    - "Every failure leaves manifest, payload, key, mtime, and access bookkeeping unchanged."
  </behavior>
  <action>Expand the tracer into the complete D-07 pipeline. Parse only minimal framing/version/algorithm selection before authentication; after successful HMAC, validate logical key, exact committed state, digest/size syntax, contained locator, and exact handler format support. Open one `GuardedHandlerIO` snapshot, stream SHA-256 and count bytes, compare both, then invoke the handler on that same live snapshot. Map a missing manifest to None only; map a missing payload and every tamper/race to the precise integrity/lifecycle/version/backend reason. Remove access-time or cleanup behavior from every failed read, retaining trusted-payload serializer semantics from D-11.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_integrity.py tests/test_blob_store_read_contract.py tests/test_filesystem_containment.py -x &amp;&amp; uv run ruff check src/cacheness/storage/integrity.py src/cacheness/security.py src/cacheness/storage/blob_store.py tests/test_blob_store_integrity.py</automated>
  </verify>
  <acceptance_criteria>Event-order spies prove the exact sequence and one snapshot; all metadata/payload tamper variants fail before handler invocation; failure evidence is byte/mtime/key unchanged; Phase 1 containment stays green.</acceptance_criteria>
  <done>Every canonical direct read is authenticated and verified from one immutable snapshot before trusted deserialization.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V5 File Handling — contained race-resistant access | Authenticated locators feed exactly one guarded snapshot that is hashed and deserialized in one context. | Snapshot replacement, containment, and event-order matrix. |
| V11 Cryptography — approved algorithms and key handling | Exact key material, HMAC-SHA256, SHA-256, complete projection, and constant-time verification. | Key/signature/projection/payload tamper matrix. |
| V13 Configuration — fail-secure cryptographic configuration | Missing/unsafe/unsupported required configuration surfaces typed before repository or payload access. | POSIX/non-POSIX provider and reopen cases. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key source → canonical signer | Missing, substituted, weakly protected, or unsupported key material could authorize attacker-controlled manifests. |
| authenticated manifest → managed locator | Critical fields cross from persisted metadata into filesystem and handler selection. |
| managed payload → private snapshot → handler | Payload replacement and check/use races must not let unverified bytes reach deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Spoofing / Tampering | strict key provider | high | mitigate | Exact key length/provider/algorithm, no-follow file attestation, no reopen regeneration, and blocking adversarial tests. |
| T-02-14 | Tampering | manifest critical fields | high | mitigate | Fixed complete HMAC projection and mutation test for every structural field. |
| T-02-15 | Tampering / Elevation of Privilege | payload snapshot and handler | high | mitigate | One guarded snapshot is hashed and deserialized within one context after authenticated validation; race/tamper tests block execution. |
| T-02-16 | Repudiation | failed read evidence | medium | mitigate | No access update, deletion, rewrite, key rotation, or cleanup on read failure; pre/post byte and mtime assertions. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-13 | `uv run pytest -q -o log_cli=false tests/test_blob_store_integrity.py -k "key or signer or permission or provider" -x` |
| T-02-14 | `uv run pytest -q -o log_cli=false tests/test_blob_store_integrity.py -k "projection or critical_field or signature" -x` |
| T-02-15 | `uv run pytest -q -o log_cli=false tests/test_blob_store_integrity.py tests/test_blob_store_read_contract.py -k "snapshot or tamper or order or handler" -x` |

Each high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run integrity, read-contract, and filesystem-containment suites plus targeted Ruff. All high-severity key/signature/order/tamper cases are blocking: any payload or handler event before successful authentication and verification fails the plan.
</verification>

<success_criteria>
- Required signing cannot downgrade or replace missing/invalid key material.
- The complete critical manifest projection is authenticated.
- One verified private snapshot feeds the handler.
- Corruption and missing payload are typed, non-mutating failures rather than misses.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-05-SUMMARY.md` when done.
</output>
