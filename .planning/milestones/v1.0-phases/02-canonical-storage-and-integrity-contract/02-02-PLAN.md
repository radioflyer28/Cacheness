---
phase: 02-canonical-storage-and-integrity-contract
plan: "02"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_manifest_backends.py
autonomous: true
requirements: [STOR-01, STOR-08]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "JSON, memory, and SQLite repositories round-trip the same canonical bytes and typed manifest semantics (STOR-01 planner assumption, unclassified by the generic edge probe; D-02)."
    - "Persistent JSON and SQLite reopen without field loss; SQLite canonical manifests remain isolated from legacy cache_entries."
    - "Repository absence is distinct from backend failure for direct callers (STOR-08 planner assumption, unclassified by the generic edge probe; D-05)."
  artifacts:
    - path: src/cacheness/storage/manifest_repository.py
      provides: "Lossless JSON, in-memory, and dedicated-table SQLite canonical record repositories"
    - path: tests/test_blob_manifest_backends.py
      provides: "Local repository parity, reopen, isolation, and failure-translation contract"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/manifest_repository.py
      via: "Exact local backend identity selects a lossless canonical repository"
      pattern: "create_manifest_repository"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Make the canonical record backend-neutral across the Phase 2 local metadata scope without stealing Phase 4 composition work.

Purpose: Prevent SQLite column projection or JSON representation from changing lifecycle meaning or signed bytes.
Output: Lossless local repositories plus exact BlobStore selection and error translation.
</objective>

## Artifacts this phase produces

- Exact-byte JSON and memory repositories.
- A dedicated SQLite BLOB table that leaves legacy `cache_entries` untouched.
- Contract coverage for CRUD, reopen, parity, and backend failure taxonomy.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-PATTERNS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VALIDATION.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-01-SUMMARY.md
</context>

<planner_assumptions>
- STOR-01 and STOR-08 were unclassified by the generic edge probe beyond the explicit STOR-01 probes; parity and typed backend-failure behavior are therefore direct requirement/context acceptance, not inferred scope.
- PostgreSQL, S3, arbitrary registered backend composition, CAS, and topology capability claims remain excluded by D-02's Phase 2 boundary and the roadmap's Phase 4/5 ownership.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove exact canonical bytes across local repositories and reopen</name>
  <depends_on>Plan 02-01 freezes the canonical byte codec and raw repository protocol.</depends_on>
  <reversibility rating="costly">D-02 makes the raw repository seam a dependency of later lifecycle and backend adapters.</reversibility>
  <files>src/cacheness/storage/manifest_repository.py, tests/test_blob_manifest_backends.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>tests/test_metadata.py</file>
    <file>tests/test_clear_recovery.py</file>
  </read_first>
  <behavior>
    - "Memory, JSON, and SQLite return exactly the bytes written for a canonical record."
    - "JSON and SQLite preserve Unicode plus nested handler/user metadata after close and reopen."
    - "Absent key, overwrite, remove, and list_keys behave identically; injected backend failures retain causes and typed backend reasons."
    - "SQLite uses a dedicated manifest table and does not alter or reinterpret legacy cache_entries rows."
  </behavior>
  <action>Write `tests/test_blob_manifest_backends.py` as the Wave 0 contract and parametrize the three local repositories. Reuse established locking and durable JSON patterns, but store exact canonical bytes or an exactly reversible byte encoding; never route a canonical record through `SqliteBackend.put_entry`, whose fixed columns lose D-01 fields. Create a dedicated SQLite table keyed by logical key with canonical bytes in a BLOB and prove table isolation plus reopen. Translate narrow I/O/SQLite errors to the public backend failure with `raise ... from exc`; do not decode, authenticate, update access bookkeeping, or clean evidence in repository methods.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_manifest_backends.py -x &amp;&amp; uv run ruff check src/cacheness/storage/manifest_repository.py tests/test_blob_manifest_backends.py</automated>
  </verify>
  <acceptance_criteria>All three repositories pass the same CRUD byte contract; persistent adapters reopen losslessly; injected failures are typed; the legacy table schema/data remain unchanged.</acceptance_criteria>
  <done>Canonical record persistence is demonstrably independent of the local backend representation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select exact local repositories at the BlobStore composition boundary</name>
  <depends_on>Task 1 implements all supported Phase 2 local adapters.</depends_on>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/storage/manifest_repository.py, tests/test_blob_manifest_backends.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/backends/__init__.py</file>
    <file>.planning/phases/01-compatibility-and-security-baseline/01-14-SUMMARY.md</file>
  </read_first>
  <behavior>
    - "BlobStore chooses JSON, SQLite, or in-memory canonical repositories from exact supported local backend identities."
    - "Unsupported/custom/PostgreSQL identities fail during construction with a typed configuration/backend reason before payload staging."
    - "Existing Phase 1 clear-recovery ownership remains intact and separate from manifest persistence."
  </behavior>
  <action>Centralize exact repository selection without advertising generalized backend capability or replacing the caller-injected backend contract owned by Phase 4. Ensure `BlobStore` uses the selected raw repository for canonical records while retaining the exact Phase 1 clear coordinator boundary. Reject unsupported identities before serialization or filesystem mutation and preserve existing public constructor forms where they are supported. Add integration cases for JSON, SQLite, and an established in-memory backend instance plus explicit unsupported identities.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_manifest_backends.py tests/test_clear_recovery.py -x &amp;&amp; uv run ruff check src/cacheness/storage/manifest_repository.py src/cacheness/storage/blob_store.py tests/test_blob_manifest_backends.py</automated>
  </verify>
  <acceptance_criteria>Every supported local BlobStore configuration reaches the same typed manifest path; unsupported identities fail before mutation; clear recovery regressions remain green.</acceptance_criteria>
  <done>The direct BlobStore composition root selects only truthful, lossless Phase 2 repositories.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V1 Encoding and Sanitization — canonical representation integrity | Repositories preserve exact authenticated bytes instead of reconstructing backend-shaped data. | JSON/memory/SQLite exact-byte and reopen contract. |
| V5 File Handling — safe durable local persistence | JSON and SQLite paths use established contained/durable local primitives and isolate legacy tables. | Backend reopen and legacy-table isolation cases. |
| V13 Configuration — fail-secure backend selection | Only exact supported local repository identities are admitted before mutation. | Unsupported/custom identity construction cases. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persistence adapter → canonical bytes | Backend encodings, files, and SQLite rows must not alter authenticated record semantics. |
| backend identity → BlobStore composition | Capability-shaped/custom objects must not inherit guarantees they do not implement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | JSON/SQLite record persistence | high | mitigate | Exact-byte round trips, durable reopen tests, and dedicated SQLite BLOB storage prevent critical field loss or reinterpretation. |
| T-02-06 | Spoofing | repository selection | high | mitigate | Admit exact supported local identities and reject unknown/custom identities before payload or metadata mutation. |
| T-02-07 | Repudiation | repository error translation | medium | mitigate | Preserve original causes and stable backend-failure reasons; never translate operational failure to absence. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-05 | `uv run pytest -q -o log_cli=false tests/test_blob_manifest_backends.py -k "round_trip or reopen or sqlite" -x` |
| T-02-06 | `uv run pytest -q -o log_cli=false tests/test_blob_manifest_backends.py -k "selection or unsupported or custom" -x` |

Each high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run the backend contract, Phase 1 clear recovery regression, and targeted Ruff. Compare canonical bytes before/after JSON and SQLite reopen and verify legacy `cache_entries` remains isolated.
</verification>

<success_criteria>
- JSON, memory, and SQLite preserve identical canonical bytes and semantics.
- BlobStore selects only exact supported local repositories.
- Absence and backend failure remain distinct.
- No Phase 4/5 backend guarantees are claimed.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-02-SUMMARY.md` when done.
</output>
