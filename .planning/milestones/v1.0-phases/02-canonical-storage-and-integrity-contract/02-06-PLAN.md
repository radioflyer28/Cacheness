---
phase: 02-canonical-storage-and-integrity-contract
plan: "06"
type: execute
wave: 4
depends_on: [02-05]
files_modified:
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-02, STOR-08, SECU-03, SECU-04, SECU-05, SECU-08]
estimate:
  tokens: 29000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "get_metadata, exists, and list expose only authenticated committed canonical records and preserve typed failure distinctions (STOR-02/STOR-08 planner assumptions, unclassified by the generic edge probe; D-05, D-06)."
    - "update_metadata can patch only user metadata, then re-canonicalizes and re-signs; immutable locator/handler/format/generation fields cannot be changed through the public patch."
    - "delete and clear authenticate every record before trusting locators and preserve corrupt/conflicted evidence for later reconciliation; no Phase 3 state machine is introduced."
  artifacts:
    - path: src/cacheness/storage/blob_store.py
      provides: "Canonical integrity semantics across every direct public operation"
    - path: tests/test_blob_store_read_contract.py
      provides: "Operation matrix for absence, committed visibility, integrity, conflict, and backend outcomes"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/manifest.py
      via: "Every public operation consumes the same authenticated typed manifest"
      pattern: "(decode|verify).*manifest"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Apply the canonical committed/integrity contract consistently to every direct BlobStore public operation.

Purpose: Prevent weaker metadata, existence, listing, mutation, and cleanup paths from bypassing the secure get contract.
Output: A complete direct-operation outcome matrix without Phase 3 lifecycle or Phase 6 cache policy.
</objective>

## Artifacts this phase produces

- Authenticated committed semantics for `get_metadata`, `exists`, and `list`.
- Safe user-metadata patching and authenticated locator use for `delete`/`clear`.
- Complete public operation outcome tests.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-14-SUMMARY.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-05-SUMMARY.md
</context>

<planner_assumptions>
- STOR-02, STOR-08, SECU-03, SECU-04, SECU-05, and SECU-08 were unclassified by the generic edge probe; the research operation matrix is adopted as concrete direct-API acceptance.
- `exists()` means a valid authenticated committed record with a supported declared handler/format/version and one guarded payload snapshot whose SHA-256 and size match; it verifies bytes without deserializing, returns false only for absent manifest, and raises typed for every other failure.
- Delete/clear are mutation APIs, but must authenticate before locator use; Phase 3 remains owner of generation transitions, CAS, crash reconciliation, and race convergence.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce committed authenticated semantics on read-only public operations</name>
  <depends_on>Plan 02-05 provides the complete get pipeline and stable typed outcomes.</depends_on>
  <files>src/cacheness/storage/blob_store.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>tests/test_filesystem_containment.py</file>
  </read_first>
  <behavior>
    - "get_metadata authenticates and validates committed/key/format/locator fields without deserializing payload bytes."
    - "exists validates supported handler/format/version, verifies authenticated committed state plus one-snapshot payload SHA-256/size without deserializing, and returns false only for true absence."
    - "list authenticates every selected record and returns all-or-error rather than omitting corrupt/conflicted entries."
    - "No read-only operation updates access time, repairs, deletes, rewrites, rotates keys, or invokes a handler unnecessarily."
  </behavior>
  <action>Expand `tests/test_blob_store_read_contract.py` into a parameterized read-only operation matrix and reuse one internal authenticated-manifest pipeline rather than duplicating weaker checks. `get_metadata` stops before payload deserialization but validates every critical manifest field; `exists` validates declared handler/format/version support and uses one snapshot to verify SHA-256/size without deserializing; `list` authenticates each included record and raises on any selected invalid record. Preserve current public return shapes for successful/absent outcomes and propagate exact integrity/conflict/version/backend types per D-05/D-06. Make every failure side-effect-free.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "get_metadata or exists or list" -x &amp;&amp; uv run ruff check src/cacheness/storage/blob_store.py tests/test_blob_store_read_contract.py</automated>
  </verify>
  <acceptance_criteria>All three methods use the canonical contract; only true absence yields None/false; corrupt/conflicted/backend cases retain exact types; event spies show no forbidden side effect.</acceptance_criteria>
  <done>Every direct read-only surface exposes the same authenticated committed truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constrain metadata patches and authenticate mutation locators</name>
  <depends_on>Task 1 centralizes authenticated-manifest loading for reuse.</depends_on>
  <files>src/cacheness/storage/blob_store.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/clear_recovery.py</file>
    <file>tests/test_clear_recovery.py</file>
    <file>tests/test_filesystem_containment.py</file>
  </read_first>
  <behavior>
    - "update_metadata patches only bounded user_metadata, retains immutable fields, and persists a newly canonicalized valid signature."
    - "delete authenticates and validates committed locator before payload/manifest removal."
    - "clear preflights and authenticates the complete selected record set before invoking the existing bounded clear coordinator."
    - "Corrupt, unknown, or conflicted records remain untouched and raise typed; absent update/delete retain existing false result."
  </behavior>
  <action>Separate structural manifest fields, handler metadata, and mutable user metadata at the public boundary. Reject attempts to patch logical key, generation, lifecycle, locator, handler/type, payload format/version, digest, size, or signature fields; valid user metadata is bounded, canonicalized, and signed as one replacement record. Route `delete` and `clear` through authenticated manifest preflight before locator trust, preserving the Phase 1 clear-only coordinator without adding new lifecycle states or recovery policy. Test whole-set preflight so a later corrupt entry causes zero deletion or metadata mutation.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py tests/test_clear_recovery.py tests/test_filesystem_containment.py -x &amp;&amp; uv run ruff check src/cacheness/storage/blob_store.py tests/test_blob_store_read_contract.py</automated>
  </verify>
  <acceptance_criteria>User metadata updates re-sign without changing critical fields; invalid structural patches fail before write; delete/clear trust only authenticated locators; corrupt whole-set preflight causes zero mutation.</acceptance_criteria>
  <done>Every direct BlobStore operation shares one truthful manifest boundary without expanding later-phase lifecycle scope.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V1 Encoding and Sanitization — allowlisted mutable input | `update_metadata` accepts only bounded user metadata and rejects structural manifest fields. | Critical-field patch parameterization. |
| V5 File Handling — authenticated destructive file operations | Delete/clear authenticate and whole-set preflight before locator-driven mutation. | Mutation spies plus clear/containment regressions. |
| V11 Cryptography — authenticity on every operation | Read-only and mutation surfaces consume the same verified canonical signature boundary. | Direct operation outcome matrix. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public operation → authenticated manifest | Weaker metadata/list/exists APIs could otherwise expose or trust unauthenticated records. |
| caller metadata patch → signed record | User metadata must not overwrite immutable security-critical fields. |
| authenticated locator → delete/clear | Mutation APIs must not delete attacker-selected or conflicted evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Tampering | update_metadata | high | mitigate | Separate mutable user metadata, reject structural keys, re-canonicalize/re-sign, and test every critical-field patch. |
| T-02-18 | Tampering / Denial of Service | delete and clear | high | mitigate | Authenticate complete records and whole-set preflight before locator use or any mutation. |
| T-02-19 | Repudiation | exists/list/get_metadata | medium | mitigate | Shared typed pipeline prevents corrupt/conflicted records from appearing absent or silently omitted. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-17 | `uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "update_metadata or patch or structural" -x` |
| T-02-18 | `uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py tests/test_clear_recovery.py tests/test_filesystem_containment.py -k "delete or clear or preflight" -x` |

Each high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run read-contract, clear-recovery, and containment suites plus targeted Ruff. Whole-set mutation spies must remain empty on any authentication, version, lifecycle, or integrity failure.
</verification>

<success_criteria>
- All public read-only operations expose authenticated committed truth.
- Only user metadata can be patched and the result is re-signed.
- Delete and clear authenticate before locator-driven mutation.
- No Phase 3 CAS/recovery or Phase 6 cache behavior is added.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-06-SUMMARY.md` when done.
</output>
