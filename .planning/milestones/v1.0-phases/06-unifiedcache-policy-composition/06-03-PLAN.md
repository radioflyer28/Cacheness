---
phase: 06-unifiedcache-policy-composition
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/cacheness/config.py
  - src/cacheness/cache_policy.py
  - src/cacheness/core.py
  - tests/test_phase6_policy_contract.py
autonomous: true
requirements: [CACH-02, CACH-03, CACH-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03: Cache policy configuration is separate from BlobStore topology, handlers, and security configuration."
    - "D-12: Repeated explicit bounded resumes eventually enforce the configured byte limit for a finite quiescent authoritative inventory; no single call exceeds its work cap."
    - "D-12: Conflicts, stale continuations, unavailable authoritative facts, or authority revision churn return a typed incomplete/retryable result without promising completion under perpetual churn."
    - "Size decisions use authenticated intrinsic catalog facts and never statistics/projections as authority."
    - "The public result of a committed put retains the commit receipt even when post-commit maintenance is partial or retryable."
  artifacts:
    - path: src/cacheness/config.py
      provides: "Validated CachePolicyConfig with finite page/work/size bounds"
    - path: src/cacheness/cache_policy.py
      provides: "Finite inventory/eviction/verification resume protocol and typed maintenance result"
    - path: tests/test_phase6_policy_contract.py
      provides: "Budget exhaustion, continuation, stale-candidate, zero-limit, and committed-put evidence"
  key_links:
    - from: src/cacheness/core.py
      to: src/cacheness/cache_policy.py
      via: "post-commit bounded maintenance that preserves the BlobReceipt"
      pattern: "CachePutResult|maintenance"
    - from: src/cacheness/cache_policy.py
      to: src/cacheness/storage/catalog.py
      via: "bounded authenticated catalog pages and exact candidate expectations"
      pattern: "query_catalog|byte_size"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Size policy must not depend on mutable statistics or projection rows, or claim exact-LRU/global-oldest ordering unsupported by the authority."
    - "[FLAGGED/UNVERIFIED] Maintenance must not hide incomplete work, scan without a bound, or convert a committed write into a rolled-back write after policy cleanup fails."
---

<objective>
Add truthful bounded size enforcement without confusing cache policy with storage correctness.

Purpose: CACH-02 and CACH-03 require eviction policy above BlobStore, while CACH-06 requires explicit, validated policy configuration and coherent results.
Output: CachePolicyConfig, bounded/resumable size maintenance, and a put result that separates canonical commit from maintenance outcome.
</objective>

## Artifacts this phase produces

- `src/cacheness/config.py`: policy-only settings and validation.
- `src/cacheness/cache_policy.py`: bounded size-maintenance state machine.
- `tests/test_phase6_policy_contract.py`: exact policy budget and result contract.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-RESEARCH.md
@.planning/phases/06-unifiedcache-policy-composition/06-PATTERNS.md
@src/cacheness/config.py
@src/cacheness/storage/catalog.py
@src/cacheness/storage/blob_store.py
@src/cacheness/cache_policy.py
@src/cacheness/core.py
@.planning/phases/06-unifiedcache-policy-composition/06-02-SUMMARY.md
</context>

<interfaces>
Plan 02 provides `CacheRemovalReport` and the single bounded exact-removal helper consumed here. `CachePolicyConfig` is a frozen nested policy configuration. `CacheMaintenanceState` is opaque, composition-bound resumable evidence with an explicit `inventory`, `evict`, or `verify` phase, authority revision/cursor, bounded aggregate fields, and configuration fingerprint. `CacheMaintenanceResult` returns `complete`, `retryable`, the truthful removal report, and exactly one of a continuation state or typed restart cause while incomplete. `CachePutResult` carries one unchanged `BlobReceipt` plus that maintenance result. None of these types may authorize lifecycle state independently of BlobStore.
</interfaces>

<source_coverage>
- CACH-02: the policy/authority boundary is explicit in configuration and execution.
- CACH-03: size maintenance is bounded, exact, resumable, truthful, and converges across explicit resumes for a finite quiescent inventory.
- CACH-06: policy configuration rejects invalid/unbounded combinations before storage I/O.
- CONTEXT: D-03, D-10, D-11, D-12, and D-17 are covered; D-05/D-13 types and lifecycle seam are consumed.
- RESEARCH: post-commit partial-success semantics, intrinsic authoritative facts, deterministic authority order, and stale-candidate preservation are covered.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one size limit within a finite maintenance budget</name>
  <files>src/cacheness/config.py, src/cacheness/cache_policy.py, src/cacheness/core.py, tests/test_phase6_policy_contract.py</files>
  <read_first>
    - src/cacheness/config.py
    - src/cacheness/cache_policy.py
    - src/cacheness/core.py
    - src/cacheness/storage/catalog.py
    - src/cacheness/storage/blob_store.py
    - tests/test_config_validation.py
    - tests/test_phase6_removal_contract.py
  </read_first>
  <behavior>
    - "Authoritative bytes exceed configured limit and one exact candidate suffices -> one bounded delete, complete maintenance report, invariant proved."
    - "Finite quiescent inventory spans more than two pages -> repeated caller-driven bounded resumes traverse inventory, evict exact candidates, verify authoritative bytes, and eventually return complete with total bytes at or below the configured limit."
    - "Authority revision changes during inventory/verification or a candidate is replaced before exact delete -> replacement survives and the result is typed incomplete/retryable with continuation or restart guidance."
    - "Zero/negative page or work bounds, negative size limit, or inconsistent finite bounds -> configuration rejection before BlobStore I/O."
  </behavior>
  <action>Write tests first, then add a frozen CachePolicyConfig containing only policy settings such as default TTL, maximum authoritative bytes, catalog page size, and total maintenance work cap; keep StoreTopology, HandlerRegistry, and security settings out per D-03. Validate all bounds as finite and internally consistent before UnifiedCache initializes storage. Implement D-12 as a finite caller-driven resume protocol over authenticated CatalogEntry byte sizes: `inventory` scans stable authority-revision pages within one call's work cap and accumulates authoritative bytes; at end-of-inventory it computes excess bytes. `evict` selects canonical-order candidates through the Plan-02 bounded helper, exact-deletes each expectation, and subtracts bytes only for confirmed removals. Inventory and verification cursors cross only mutation-free stable-revision scans; after any eviction batch, clear the catalog cursor and make the next explicit resume select again from the canonical beginning, carrying only remaining excess and truthful removal evidence so a mutation never reuses a stale page cursor. `verify` performs a fresh stable-revision bounded inventory pass and returns complete only when authoritative bytes are at or below the limit; otherwise it starts another bounded evict/verify cycle from the newly observed facts. Every incomplete call returns a `CacheMaintenanceResult` with one bounded, composition/config-bound `CacheMaintenanceState`; callers must explicitly invoke the resume method for the next step. A stable finite inventory therefore reaches completion after finitely many explicit resumes because each complete scan is finite and each successful eviction removes a positive authenticated byte size. A conflict, malformed/missing intrinsic fact, foreign/stale state, or revision change during inventory/verification returns typed incomplete/retryable continuation or restart guidance, preserving the winning generation. Each invocation counts observations and delete attempts against one configured work cap and never internally drains all pages. The selection promise is canonical authority order, not exact LRU or global oldest. Do not use CacheStatistics or projections as decision inputs, and do not add a scheduler, worker, queue, lease, readiness registry, hidden background resume loop, cache-side lock/coordinator, timing-derived correctness rule, or second lifecycle authority.</action>
  <reversibility rating="costly">The public bounded-maintenance and ordering contract becomes user-visible; changing its continuation or retention semantics later requires coordinated API, test, and documentation updates.</reversibility>
  <acceptance_criteria>
    - "CachePolicyConfig validation rejects every non-finite/unbounded or negative work/size combination before any store method is called."
    - "A one-candidate over-limit case returns complete=true only after authoritative committed bytes are proven within the limit."
    - "A store larger than two catalog pages with a work cap smaller than the inventory requires multiple explicit resumes and deterministically reaches complete=true with authoritative bytes at or below the limit when no external mutation occurs."
    - "Each inventory, eviction, and verification invocation stays within one configured observation-plus-delete work cap; no invocation drains every remaining page internally."
    - "Eviction clears its page cursor after mutation; later resumes reselect from the canonical beginning and never reuse a cursor issued for a pre-delete revision."
    - "Budget exhaustion returns a bounded continuation, while stale/foreign state, missing intrinsic facts, revision churn, and replacement conflict return complete=false and retryable=true with typed continuation or restart detail."
    - "A spy statistics/projection object is never read during size selection."
    - "CacheMaintenanceState has a configured maximum encoded size and carries only phase, composition/config binding, bounded aggregates/candidates, authority revision, and opaque cursor; CacheMaintenanceResult exposes completion/retryability without persisting either as lifecycle authority."
    - "The tested selection order is documented as canonical authority order, with no global-oldest or exact-LRU assertion."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_policy_contract.py -k "config or size or budget or multipage or resume or quiescent or churn or replacement or ordering" -o log_cli=false</automated>
  </verify>
  <done>The explicit finite resume protocol converges for a quiescent multi-page inventory and returns typed incomplete/retryable evidence instead of a false guarantee under conflict or churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve committed put truth across post-commit maintenance</name>
  <files>src/cacheness/cache_policy.py, src/cacheness/core.py, tests/test_phase6_policy_contract.py</files>
  <read_first>
    - src/cacheness/core.py
    - src/cacheness/cache_policy.py
    - src/cacheness/storage/read_contract.py
    - tests/test_phase6_policy_contract.py
    - tests/test_blob_store_atomic_lifecycle.py
  </read_first>
  <behavior>
    - "BlobStore.put commits and maintenance completes -> CachePutResult contains canonical receipt plus complete maintenance report."
    - "BlobStore.put commits and maintenance conflicts/fails/exhausts budget -> receipt remains present and maintenance reports actual partial outcome."
    - "BlobStore.put fails before commit -> canonical typed error propagates and maintenance is not invoked."
    - "Repeatedly resuming returned maintenance state for a finite quiescent multi-page inventory -> each call performs one bounded step and the final verified result enforces the configured byte limit."
    - "Resuming while authority revision churns or an exact delete conflicts -> typed incomplete/retryable result; no completion promise until a stable verification pass proves the invariant."
  </behavior>
  <action>Define an immutable CachePutResult that separates the successful BlobStore receipt from `CacheMaintenanceResult`. Make UnifiedCache.put perform exactly one canonical BlobStore.put, then at most one configured bounded size-maintenance step. A post-commit cleanup conflict, backend failure, revision change, or exhausted budget must not erase, relabel, or pretend to roll back the canonical commit; retain the receipt and return the exact incomplete/retryable result per D-17. A pre-commit storage failure must not run policy. Provide an explicit UnifiedCache maintenance-resume method accepting only opaque CacheMaintenanceState emitted by this cache/store composition; validate its phase, composition/config fingerprint, encoded bound, cursor, revision, and aggregates before mutation. The method executes exactly one `inventory`, `evict`, or `verify` bounded step and returns the next result; it never schedules or loops through later resumes. Keep all deletes routed through the Plan-02 primitive and preserve D-12's finite-quiescent completion versus conflict/churn incomplete distinction.</action>
  <acceptance_criteria>
    - "Every successful canonical write returns exactly one BlobReceipt inside CachePutResult, including when maintenance is incomplete or failed."
    - "A failed canonical write executes zero maintenance queries/deletes and preserves its typed cause."
    - "One put performs no more than one policy work budget; resume performs no more than one further work budget."
    - "A quiescent inventory spanning more than two pages reaches the byte limit after finitely many explicit resume calls, with a final stable verification pass and no duplicate removal claims."
    - "Injected authority revision churn and exact-delete conflict each produce a typed incomplete/retryable maintenance result and preserve the current winner."
    - "Foreign/malformed continuation state is rejected before query or delete."
    - "Completion is emitted only after the configured size invariant is proven from authenticated authority facts."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_policy_contract.py tests/test_blob_store_atomic_lifecycle.py -k "put or maintenance or multipage or resume or quiescent or churn or conflict" -o log_cli=false</automated>
  </verify>
  <done>Committed write truth and bounded policy-maintenance truth remain separately inspectable under every partial-success path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Policy configuration -> storage work | Invalid limits must fail before allocating or scanning authority resources. |
| Catalog facts -> eviction choice | Only authenticated intrinsic authority facts may authorize candidate deletion. |
| Commit receipt -> post-commit maintenance | Policy failure must not falsify a completed canonical write. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-09 | Denial of service | size traversal | high | mitigate | Enforce finite page and total-work caps on every invocation and resume. |
| T-06-10 | Tampering | continuation state | high | mitigate | Make continuation opaque, composition-bound, validated, and stale-safe before mutation. |
| T-06-11 | Repudiation | post-commit result | high | mitigate | Return receipt and maintenance report separately; test partial-success cases. |
| T-06-12 | Information disclosure | policy diagnostics | low | accept | Reports contain counts/typed causes and opaque state, not payload bytes or secrets. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run policy/config tests plus BlobStore atomicity tests. Confirm each invocation stays inside work caps and committed receipts survive every injected maintenance outcome.
</verification>

<success_criteria>
- Policy settings are validated and separate from storage composition.
- Size maintenance is bounded and resumable, with no unsupported ordering guarantee.
- Explicit bounded resumes finish for a finite quiescent multi-page inventory; conflicts or revision churn remain typed incomplete/retryable outcomes with no perpetual-churn guarantee.
- Incomplete work is reported whenever the invariant is not proven.
- Post-commit policy work never rewrites canonical commit truth.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-03-SUMMARY.md` when done.
</output>
