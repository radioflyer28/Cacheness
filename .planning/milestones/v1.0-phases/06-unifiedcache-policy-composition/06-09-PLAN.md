---
phase: 06-unifiedcache-policy-composition
plan: "09"
type: execute
wave: 9
depends_on: ["06-08"]
files_modified:
  - tests/test_blob_manifest.py
  - tests/test_filesystem_containment.py
  - tests/test_cache_signing.py
  - tests/test_legacy_array_security.py
  - tests/test_public_api_contract.py
autonomous: true
gap_closure: true
requirements: [CACH-01, CACH-04, CACH-05, CACH-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Retained manifest, containment, signing, object-array, and public-import tests use the nested CacheConfig vocabulary and never depend on removed flat fields (CACH-06; D-01, D-03, D-04)."
    - "Every UnifiedCache exercised by these retained tests receives an explicit StoreTopology or caller-owned BlobStore and follows deliberate initialize/close ownership (CACH-01, CACH-06; D-02, D-14, D-17)."
    - "Retained cache behavior observes CachePutResult.receipt and CacheLookupResult outcomes, including a present None and non-destructive corrupt evidence, instead of raw keys or a removed get result (CACH-04, CACH-05; D-05, D-06, D-07, D-08)."
    - "Intentional negative cutover assertions remain negative: removed arguments, aliases, factories, and implicit constructors raise or remain absent rather than delegating through a compatibility route (D-01 through D-04)."
  artifacts:
    - path: tests/test_blob_manifest.py
      provides: "Canonical handler-format and signed-manifest regression setup"
    - path: tests/test_filesystem_containment.py
      provides: "Guarded handler I/O containment coverage using nested storage configuration"
    - path: tests/test_cache_signing.py
      provides: "Explicit-composition signing and typed-result regressions"
    - path: tests/test_legacy_array_security.py
      provides: "Object-array authenticity and non-destructive cache outcome regressions"
    - path: tests/test_public_api_contract.py
      provides: "Current positive public imports plus deliberate negative retired-surface assertions"
  key_links:
    - from: tests/test_cache_signing.py
      to: src/cacheness/core.py
      via: "UnifiedCache(config, store=...) followed by CachePutResult.receipt.key and CacheLookupResult"
      pattern: "store=.*StoreTopology|CachePutResult|CacheOutcome"
    - from: tests/test_filesystem_containment.py
      to: src/cacheness/config.py
      via: "CacheConfig(storage=CacheStorageConfig(cache_dir=...)) passed to guarded handler I/O"
      pattern: "CacheStorageConfig"
  prohibitions:
    - "Do not edit production source in this plan. If a canonical migrated assertion exposes a distinct implementation defect, stop and report the exact failing behavior and justification for a separate source-owning fix."
    - "Do not add a compatibility shim, implicit cache constructor, alternate result API, lifecycle lock, queue, coordinator, readiness mechanism, sidecar, or timing-based correctness rule."
    - "Do not weaken, delete, skip, xfail, or replace security/format/containment behavior merely to make the suite green."
---

<objective>
Migrate the retained format, containment, signing, array-security, and public-contract tests to the completed explicit cache surface.

Purpose: Close the first verification gap for test consumers that still encode the removed flat configuration, implicit cache ownership, or raw cache results, while preserving their substantive security and integrity assertions.
Output: Five current test modules that exercise only nested configuration, explicit BlobStore composition, and typed Phase 6 results.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-VERIFICATION.md
@.planning/phases/06-unifiedcache-policy-composition/06-06-SUMMARY.md
@.planning/phases/06-unifiedcache-policy-composition/06-08-SUMMARY.md
@src/cacheness/config.py
@src/cacheness/cache_policy.py
@src/cacheness/core.py
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/composition.py
</context>

<interfaces>
Use CacheConfig(storage=CacheStorageConfig(...), metadata=CacheMetadataConfig(...), compression=CompressionConfig(...), handlers=HandlerConfig(...), security=SecurityConfig(...)). UnifiedCache has one constructor form, UnifiedCache(config, *, store=BlobStore | StoreTopology), and returns CachePutResult from put plus CacheLookupResult from lookup. CachePutResult.receipt.key is the committed cache key; CacheLookupResult.outcome and value express presence. Direct handler tests still receive CacheConfig, but its storage path is nested.
</interfaces>

<source_coverage>
- Verification gap 1: migrates four explicitly named stale behavior modules plus the broader public contract found by the complete isolated-suite reproduction.
- CACH-01/CACH-06 and D-01, D-02, D-03, D-04, D-14, D-17: every cache is explicitly composed, initialized, closed, and imported through the canonical public surface.
- CACH-04/CACH-05 and D-05, D-06, D-07, D-08: raw get/put expectations become presence-bearing typed-result assertions without destructive corruption cleanup.
- RESEARCH: applies the atomic public cutover, explicit cache injection, narrow error translation, and no-new-package recommendation; real PostgreSQL/S3 and native Windows remain excluded.
</source_coverage>

<tasks>

<task type="tracer">
  <name>Task 1: Preserve native-format and guarded-I/O behavior through nested configuration</name>
  <files>tests/test_blob_manifest.py, tests/test_filesystem_containment.py</files>
  <read_first>
    <file>tests/test_blob_manifest.py</file>
    <file>tests/test_filesystem_containment.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/handlers.py</file>
    <file>src/cacheness/storage/guarded_io.py</file>
  </read_first>
  <action>Replace every retained handler-I/O call that constructs CacheConfig with a flat storage path by CacheConfig(storage=CacheStorageConfig(cache_dir=...)), retaining the existing CompressionConfig and handler options exactly. Keep the native NPZ, pickle, Parquet signature, manifest-version, symlink, traversal, hard-link, snapshot-copy, and retarget assertions intact. Do not route these direct handler tests through UnifiedCache, change their fault schedule, relax path checks, or add compatibility acceptance. The deliberate Phase 6 negative CacheConfig assertion lives in tests/test_phase6_public_api_contract.py and is not a migration target.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_blob_manifest.py tests/test_filesystem_containment.py -o log_cli=false</automated>
  </verify>
  <done>All native-format and guarded handler-I/O assertions pass with nested storage configuration, with no removed flat constructor accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve signing, object-array, and public API behavior through explicit typed cache use</name>
  <files>tests/test_cache_signing.py, tests/test_legacy_array_security.py, tests/test_public_api_contract.py</files>
  <read_first>
    <file>tests/test_cache_signing.py</file>
    <file>tests/test_legacy_array_security.py</file>
    <file>tests/test_public_api_contract.py</file>
    <file>tests/test_phase6_public_api_contract.py</file>
    <file>tests/test_phase6_lookup_contract.py</file>
    <file>tests/contracts/test_phase6_topology_policy.py</file>
  </read_first>
  <action>Build each retained cache with nested Storage/Metadata/Handler/Security configuration and an explicit supported memory/memory or SQLite/filesystem StoreTopology chosen to preserve the test's persistence claim. Call initialize and close at deliberate boundaries; caller-injected BlobStore fixtures remain caller-owned. Replace raw put keys with result.receipt.key and removed get calls with lookup assertions over CacheOutcome and value. Signing and object-array corruption cases must still preserve the original typed cause, keep corrupt evidence non-destructive at the cache boundary, and prove that unsafe object arrays never reach ObjectHandler. Rewrite obsolete positive compatibility assertions in test_public_api_contract.py and test_cache_signing.py as current positive imports or explicit absence/TypeError checks only where they add coverage beyond tests/test_phase6_public_api_contract.py; never make a removed alias or flat option work. Keep the intentional negative tests in tests/test_phase6_public_api_contract.py unchanged.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_cache_signing.py tests/test_legacy_array_security.py tests/test_public_api_contract.py tests/test_phase6_public_api_contract.py tests/test_cache_integrity.py -o log_cli=false</automated>
  </verify>
  <done>Signing, object-array, and public-contract tests pass only through explicit composition and typed results; removed routes remain demonstrably absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixtures -> canonical cache constructor | Historical setup must not recreate implicit lifecycle ownership or bypass topology preflight. |
| Signed/corrupt payload evidence -> lookup assertions | Tests must preserve authenticity and typed failure meaning without authorizing destructive cleanup. |
| Historical public expectations -> supported imports | A green test must not spoof support for a removed pre-production route. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-33 | Spoofing | public-contract tests | high | mitigate | Assert the canonical __all__ and explicit constructor; retired names remain absent or TypeError-only negatives. |
| T-06-34 | Tampering | signing/object-array tests | high | mitigate | Retain signed-field, corrupt-evidence, and handler-not-reached assertions through CacheLookupResult. |
| T-06-35 | Elevation of privilege | implicit cache fixture | high | mitigate | Supply exactly one supported StoreTopology or caller-owned BlobStore and explicit lifecycle boundaries. |
| T-06-36 | Information disclosure | signing fixtures | medium | mitigate | Use only generated temporary test keys/roots and never log or persist credential material outside tmp_path. |

No package installation or live service access occurs in this plan.
</threat_model>

<verification>
Run both task commands, then run `uv run --isolated --all-extras --group dev --frozen ruff check tests/test_blob_manifest.py tests/test_filesystem_containment.py tests/test_cache_signing.py tests/test_legacy_array_security.py tests/test_public_api_contract.py`. Confirm git diff contains only the five owned test files and no production source.
</verification>

<success_criteria>
- All five modules collect and pass in an isolated all-extras environment.
- No positive flat CacheConfig, implicit UnifiedCache, raw cache get/put-result, or removed public alias expectation remains.
- Security, format, containment, signing, and object-array behavior coverage is retained.
- Canonical negative cutover assertions still prove the retired surface does not delegate.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-09-SUMMARY.md` when done.
</output>
