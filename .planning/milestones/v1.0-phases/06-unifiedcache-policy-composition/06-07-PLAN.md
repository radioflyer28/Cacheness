---
phase: 06-unifiedcache-policy-composition
plan: "07"
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - docs/CACHE_POLICY.md
  - examples/simple_object_caching.py
  - examples/configurable_serialization_demo.py
  - examples/api_request_caching.py
  - tests/test_phase6_examples.py
autonomous: true
requirements: [CACH-01, CACH-02, CACH-03, CACH-04, CACH-05, CACH-06]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-08: Documentation and examples teach only the canonical API and explain removed development paths without providing adapters."
    - "D-05/D-09/D-10/D-12: Users can distinguish lookup outcome, committed put truth, bounded removal/maintenance completion, and statistics."
    - "D-14/D-17: Examples explicitly initialize and close cache-owned stores and do not close injected caller-owned stores."
    - "Examples execute in isolated subprocesses without network access or hidden global state."
  artifacts:
    - path: docs/CACHE_POLICY.md
      provides: "Canonical cache policy, outcome, bounded-work, ownership, topology, and migration guidance"
    - path: examples/api_request_caching.py
      provides: "Explicit-cache decorator and real cache_clear report example"
    - path: tests/test_phase6_examples.py
      provides: "Executable smoke contract for all rewritten examples"
  key_links:
    - from: docs/CACHE_POLICY.md
      to: src/cacheness/__init__.py
      via: "every shown import is canonical and exported"
      pattern: "UnifiedCache|CacheConfig|cached"
    - from: examples/api_request_caching.py
      to: src/cacheness/decorators.py
      via: "explicit cache injection and outcome-aware recomputation"
      pattern: "cached(cache="
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Documentation must not teach compatibility aliases, implicit ownership, raw-None miss semantics, exact-LRU/global-oldest guarantees, or cross-resource ACID."
    - "[FLAGGED/UNVERIFIED] Mocked PostgreSQL/S3 behavior must not be described as live BACK-05 qualification."
---

<objective>
Make the canonical Phase-6 cache model learnable and executable from public documentation.

Purpose: The cutover is incomplete if examples still teach removed constructors, raw values, hidden cache ownership, or unsupported guarantees.
Output: A cache-policy guide and three subprocess-verified canonical examples.
</objective>

## Artifacts this phase produces

- `docs/CACHE_POLICY.md`: one reference for cache policy composition and outcomes.
- `examples/simple_object_caching.py`: explicit object lifecycle example.
- `examples/configurable_serialization_demo.py`: typed value/handler example using the same result surface.
- `examples/api_request_caching.py`: explicit decorator and invalidation example.
- `tests/test_phase6_examples.py`: deterministic example execution tests.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/STORAGE_INITIALIZATION.md
@docs/CATALOG_AND_TOPOLOGY.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@src/cacheness/__init__.py
@src/cacheness/core.py
@src/cacheness/decorators.py
@examples/simple_object_caching.py
@examples/configurable_serialization_demo.py
@examples/api_request_caching.py
@.planning/phases/06-unifiedcache-policy-composition/06-06-SUMMARY.md
</context>

<interfaces>
Examples and docs must use only the Plan-06 public exports. Every example constructs one explicit StoreTopology or injects one initialized BlobStore, calls `UnifiedCache.initialize()` deliberately, branches on `CacheOutcome`, and closes according to ownership. Size-policy guidance shows `CacheMaintenanceResult` and caller-driven `CacheMaintenanceState` resumes, including finite-quiescent completion and typed incomplete/retryable conflict/churn outcomes. Decorated examples use only `cached(cache=...)` and surface the returned `CacheRemovalReport`.
</interfaces>

<source_coverage>
- CACH-01..CACH-05: topology behavior, policy outcomes, removals, decorators, and statistics are demonstrated.
- CACH-06: the canonical public API/configuration/result surface is the only documented route.
- CONTEXT: D-01 through D-17 are reflected in documentation; D-01's intentional removals, D-08's immutable statistics, and D-12's bounded/incomplete semantics receive explicit guidance.
- RESEARCH: result-oriented examples, explicit lifecycle, topology-qualified guarantees, version rejection, and no overclaiming are covered.
</source_coverage>

<tasks>

<task type="tracer">
  <name>Task 1: Document and execute one canonical object-cache lifecycle</name>
  <files>docs/CACHE_POLICY.md, examples/simple_object_caching.py, tests/test_phase6_examples.py</files>
  <read_first>
    - src/cacheness/__init__.py
    - src/cacheness/core.py
    - src/cacheness/config.py
    - docs/adr/0001-topology-specific-storage-guarantees.md
    - docs/STORAGE_INITIALIZATION.md
    - examples/simple_object_caching.py
  </read_first>
  <action>Write a concise canonical guide and rewrite the simple object example around one explicit memory/memory or temporary SQLite/filesystem StoreTopology, nested CacheConfig, explicit UnifiedCache.initialize, CachePutResult, CacheLookupResult, CacheStatistics, bounded invalidation/maintenance reports, and explicit close. Explain all six D-05 outcomes, including present None; policy-versus-storage responsibility per D-02/D-03/D-13; bounded/resumable D-12 behavior; topology-specific guarantees without cross-resource ACID or universal contender-success claims; cache-owned topology construction versus caller-owned injected BlobStore under the locked ownership resolution; explicit decorator ownership per D-14; and explicit initialization/committed-partial close semantics per D-17. State that unsupported stored versions fail explicitly and offline migration/rebuild arrives in Phase 7. Include a cutover table naming removed paths from D-01 through D-04 and their canonical replacement, but no adapter code. Add a subprocess test using an isolated temporary directory and no network/global cache.</action>
  <acceptance_criteria>
    - "The guide names every CacheOutcome and distinguishes presence, primary outcome, maintenance/removal outcome, and typed cause."
    - "The guide states finite page/work semantics, incomplete/retryable reporting, and canonical authority order without exact-LRU/global-oldest claims."
    - "The object example imports only canonical exports, initializes and closes explicitly, checks result outcomes, and exits zero in an isolated subprocess."
    - "The migration section retains format/schema identification and points future offline conversion/rebuild to Phase 7, never implicit upgrade."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_examples.py -k "simple_object or docs" -o log_cli=false</automated>
  </verify>
  <done>A user can learn and execute the canonical object-cache lifecycle without relying on removed or hidden behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite serialization and API-function examples against the same contract</name>
  <files>examples/configurable_serialization_demo.py, examples/api_request_caching.py, tests/test_phase6_examples.py</files>
  <read_first>
    - src/cacheness/core.py
    - src/cacheness/decorators.py
    - examples/configurable_serialization_demo.py
    - examples/api_request_caching.py
    - tests/test_phase6_public_api_contract.py
    - tests/test_phase6_decorator_contract.py
  </read_first>
  <action>Rewrite the configurable-serialization example to use the same explicit composition, nested config, presence-bearing results, statistics snapshot, and explicit lifecycle as the object example, conditionally reporting a clear capability message only when its documented DataFrame dependency is unavailable. Rewrite the API-request caching example as a deterministic injected-transport demonstration that performs no network call: use only `cached(cache=...)`, demonstrate a stored None hit, show default recomputation only for absent/expired, show an explicit opt-in policy for one failure outcome without hiding the original outcome, and print the actual function-scoped CacheRemovalReport from cache_clear per D-06/D-07/D-15/D-16. Extend subprocess tests to run each example twice in isolated environments, assert deterministic zero exit/output markers, patch sockets/HTTP clients to fail if touched, and ensure each run starts with no hidden global cache state.</action>
  <acceptance_criteria>
    - "All three examples execute in isolated subprocesses using only canonical imports and no external network/service."
    - "The DataFrame example either completes with its optional dependency or emits the documented explicit capability message."
    - "The function example proves None is cached, default failure outcomes do not silently recompute, and cache_clear prints real report counts/completion."
    - "Running examples in different orders produces identical success markers and no shared singleton/cache-directory dependency."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_examples.py -o log_cli=false</automated>
  </verify>
  <done>Object, DataFrame, and function examples all teach and execute the single canonical cache model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation -> user expectations | Guarantees and ownership claims must match implemented topology-specific behavior. |
| Example process -> filesystem/network | Examples must use isolated temporary resources and no external service. |
| Optional dependency -> example result | Unavailable capabilities must be explicit rather than producing misleading success/failure. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-25 | Repudiation | guarantee documentation | high | mitigate | Assert required/forbidden claims in docs tests and align language to ADR topology guarantees. |
| T-06-26 | Information disclosure | examples | medium | mitigate | Use generated temporary data/paths and prohibit credentials or external endpoints. |
| T-06-27 | Denial of service | example maintenance | low | mitigate | Configure finite page/work budgets in every example. |
| T-06-28 | Spoofing | optional capability | medium | mitigate | Emit a feature-specific unavailability result and test both availability branches. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run the example suite in multiple orders and the documentation assertions. Confirm every import resolves against the canonical public API and all storage is isolated under test temp directories.
</verification>

<success_criteria>
- Documentation describes exactly the implemented result, policy, ownership, and topology model.
- All examples are executable and use no removed API.
- Bounded/incomplete semantics and topology limits are explicit.
- No live remote or cross-resource atomicity claim appears.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-07-SUMMARY.md` when done.
</output>
