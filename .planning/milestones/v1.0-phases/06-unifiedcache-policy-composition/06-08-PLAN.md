---
phase: 06-unifiedcache-policy-composition
plan: "08"
type: execute
wave: 8
depends_on: ["06-07"]
files_modified:
  - tools/verify_phase6_contracts.py
  - tests/test_phase6_contract_verifier.py
  - .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
autonomous: true
requirements: [CACH-01, CACH-02, CACH-03, CACH-04, CACH-05, CACH-06]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A deterministic verifier proves every CACH-01..CACH-06 contract and D-01..D-17 invariant from fixed tests and source structure."
    - "The verifier fails if a second lifecycle coordinator, lifecycle lock/admission queue, readiness registry, compatibility projection authority, direct resource deletion, compatibility route, or unbounded policy traversal reappears."
    - "Validation records actual commands/results and explicitly excludes BACK-05 live remote evidence from Phase 6."
    - "Existing Phase-3 through Phase-5 authority, lifecycle, integrity, recovery, and topology contracts remain green after the cutover."
    - "The fixed manifest directly proves malformed direct projection observations are strictly rejected rather than accepted as cache or lifecycle evidence."
  artifacts:
    - path: tools/verify_phase6_contracts.py
      provides: "Fixed-scope Phase-6 contract and regression verifier"
    - path: tests/test_phase6_contract_verifier.py
      provides: "Self-tests proving verifier fail-closed behavior"
    - path: .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
      provides: "Executed Nyquist coverage map and honest evidence ledger"
  key_links:
    - from: tools/verify_phase6_contracts.py
      to: tests/contracts/test_phase6_topology_policy.py
      via: "fixed suite manifest covering cache behavior and retained storage contracts"
      pattern: "pytest|CACH-01"
    - from: .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
      to: tools/verify_phase6_contracts.py
      via: "recorded exact command and output status"
      pattern: "verify_phase6_contracts.py"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Verification must not depend on branch diffs, unbounded repository scans, comments-only grep counts, or fabricated passing evidence."
    - "[FLAGGED/UNVERIFIED] Phase-6 completion must not count mocked PostgreSQL/S3 behavior as BACK-05 live qualification or weaken earlier storage guarantees."
---

<objective>
Close Phase 6 with deterministic, adversarial proof that the canonical cache policy composes without regressing storage authority.

Purpose: Fixed verification prevents a locally green API cutover from hiding duplicate lifecycle authority, unsafe removal, compatibility residue, or earlier-phase regressions.
Output: A self-tested verifier and an executed validation ledger mapped to every requirement and locked decision.
</objective>

## Artifacts this phase produces

- `tools/verify_phase6_contracts.py`: stable fixed-scope verifier.
- `tests/test_phase6_contract_verifier.py`: mutation-style self-tests for the verifier.
- `.planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md`: completed phase evidence and explicit exclusions.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-VERIFICATION.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
@tools/verify_phase5_contracts.py
@tools/verify_phase4_cutover.py
@.planning/phases/06-unifiedcache-policy-composition/06-07-SUMMARY.md
</context>

<interfaces>
The fixed Phase-6 manifest includes `tests/test_phase6_lookup_contract.py`, `tests/test_phase6_removal_contract.py`, `tests/test_phase6_statistics.py`, `tests/test_phase6_decorator_contract.py`, `tests/test_phase6_public_api_contract.py`, `tests/test_phase6_policy_contract.py`, `tests/contracts/test_phase6_topology_policy.py`, the exact retained regression node `tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents`, the separate `tests/test_sql_cache.py` regression, and named retained Phase-3-through-5 lifecycle contracts. `tools/verify_phase6_contracts.py --repo-root PATH` returns zero only when every fixed behavioral and AST check passes.
</interfaces>

<source_coverage>
- GOAL: final verification proves UnifiedCache is policy over the one canonical BlobStore lifecycle.
- CACH-01: supported topology matrix and unsupported-profile preflight tests are fixed inputs.
- CACH-02: one-store/one-engine architecture and no direct mutation are statically and behaviorally checked.
- CACH-03: bounded TTL/size/invalidation, exact races, continuation, and reports are fixed inputs.
- CACH-04: function identity, None, result policy, stats, and clearing are fixed inputs.
- CACH-05: all six statistics outcomes and observational-only behavior are fixed inputs.
- CACH-06: canonical/negative public surface, config, imports, docs, and examples are fixed inputs.
- GOAL SC-06: `tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents` is a fixed input proving strict malformed direct projection observation rejection.
- CONTEXT: D-01 through D-17 each receive an explicit validation row and at least one fixed test/source assertion.
- RESEARCH: prior-phase regression, adversarial authority checks, and evidence provenance are covered.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one fixed Phase-6 contract manifest end to end</name>
  <files>tools/verify_phase6_contracts.py, tests/test_phase6_contract_verifier.py</files>
  <read_first>
    - tools/verify_phase5_contracts.py
    - tools/verify_phase4_cutover.py
    - .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
    - src/cacheness/core.py
    - src/cacheness/cache_policy.py
    - src/cacheness/decorators.py
    - src/cacheness/storage/blob_store.py
    - src/cacheness/storage/lifecycle.py
  </read_first>
  <behavior>
    - "Clean Phase-6 implementation -> verifier executes fixed manifest and returns zero with per-requirement evidence."
    - "Second lifecycle engine/coordinator or direct cache-policy resource delete fixture -> verifier returns nonzero with a specific diagnostic."
    - "Compatibility alias, hidden singleton, decorator ownership, or new lock/queue fixture -> verifier returns nonzero."
    - "Unbounded catalog traversal or policy reliance on statistics/projections fixture -> verifier returns nonzero."
    - "Malformed direct projection observation fixture -> tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents runs by exact node id; a document with format_version 999 makes load_projection_checkpoint raise JsonProjectionError matching incompatible shape."
  </behavior>
  <action>Follow the existing fixed verifier pattern and write fail-closed self-tests first. Build a repository-root-safe verifier with an explicit manifest of Phase-6 suites plus the retained Phase-3-to-5 lifecycle/integrity/recovery/topology suites; name `tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents` exactly in that manifest so malformed direct projection observations remain strict failures, and do not derive scope from git diff or collect the entire repository. Add AST/import/behavior checks narrowly scoped to canonical modules that prove: one UnifiedCache-selected BlobStore and shared AuthorityLifecycleEngine; no direct payload/catalog/projection mutation from core/cache_policy/decorators; no new lifecycle coordinator, admission queue, threading/async lifecycle lock, sidecar, readiness registry, or compatibility projection authority; bounded query_catalog use with configured page/work caps; no hidden global cache, atexit/weakref owner, removed alias/factory/decorator route, or ambiguous ownership boolean; and stable public exports/results. Add documentation checks that benchmark numbers do not become runtime correctness deadlines and that cross-resource ACID/universal contender success/global-oldest claims are absent from the cache contract. Use AST region-aware assertions rather than comment-count grep. Keep checks descriptor-less but map diagnostics to the project prohibitions and D-01..D-17. The verifier must not install packages, access network, or claim live remote evidence.</action>
  <acceptance_criteria>
    - "The clean-tree verifier runs from outside repository cwd and returns zero with CACH-01..CACH-06 labels."
    - "Each prohibited architecture/API/unbounded fixture causes nonzero status and a diagnostic identifying the violated invariant."
    - "Comments/docstrings containing a forbidden token do not cause a false failure when executable AST structure is valid."
    - "The manifest names fixed files/tests and contains no git diff, recursive all-tests discovery, network call, or package install."
    - "The manifest contains the exact node id tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents, and verifier evidence shows format_version 999 raises JsonProjectionError matching incompatible shape rather than being admitted as an observation."
    - "Remote profile output is explicitly mocked-candidate and never BACK-05 live-qualified."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_contract_verifier.py -o log_cli=false</automated>
  </verify>
  <done>A self-tested fixed verifier detects both behavioral and architectural Phase-6 regressions without false green evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the validation matrix and record honest evidence</name>
  <files>.planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
    - .planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-payload-backends-and-supported-topology-qualification/05-VERIFICATION.md
    - tools/verify_phase6_contracts.py
  </read_first>
  <action>Run the fixed verifier, its self-tests, scoped Ruff on the Phase-6 production/test/tool files, the exact regression node `tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents`, and the complete current-environment pytest suite required by the validation strategy. Update 06-VALIDATION.md only with commands actually executed and their exact pass/fail/skip totals, timestamps/runtime labels, and artifact links; do not predeclare success. Map every CACH requirement and D-01..D-17 to concrete test names and static checks, and give strict malformed direct projection observation rejection its own evidence row containing the exact node id and observed rejection result. Record the spec-less edge resolutions: CACH-01/CACH-03/CACH-06 empty-adjacency-ordering predicates and CACH-02/CACH-04/CACH-05 classification predicates. Retain the descriptor-less `[FLAGGED/UNVERIFIED]` project safety/value prohibitions and mark each verified only after its adversarial check passes. Explicitly state that PostgreSQL/S3 is deterministic candidate evidence and BACK-05 live evidence remains Phase 8. If any command fails, leave the relevant row open with the actual failure rather than editing the ledger to green. Do not turn Python-version, packaging, coverage, performance, or live-service matrices into Phase-6 acceptance; those remain Phase 8.</action>
  <acceptance_criteria>
    - "06-VALIDATION.md has one concrete evidence row for every CACH-01..CACH-06 requirement and D-01..D-17 decision."
    - "Each recorded command/result exactly matches an executed verifier, pytest, Ruff, or Python-version run; failed/skipped evidence remains visibly open."
    - "All spec-less edge predicates and project-derived prohibitions have explicit test/check mappings with no silent drops."
    - "A dedicated ledger row records the exact command and observed rejection evidence for tests/test_catalog_projection.py::test_json_projection_rejects_incompatible_derived_documents; the row remains open if that node does not pass."
    - "The ledger explicitly excludes BACK-05 and contains no cross-resource ACID, universal contender-success, exact-LRU/global-oldest, or unbounded-completion claim."
    - "Phase completion is claimed only if the fixed verifier, self-tests, scoped lint, current-environment full suite, and Phase-3-through-5 fixed regressions meet their documented gate."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen python tools/verify_phase6_contracts.py --repo-root .</automated>
  </verify>
  <done>The validation ledger contains reproducible, requirement-complete evidence and honestly preserves every exclusion or open failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source/test tree -> verifier verdict | Scope and checks must be fixed and resistant to comments, cwd, and branch state. |
| Command output -> validation ledger | Only actually executed evidence may support a pass. |
| Mock remote fixture -> requirement status | Candidate evidence must not cross the live-qualification boundary. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-29 | Tampering | verifier scope | high | mitigate | Use a fixed manifest and root-safe paths; self-test prohibited mutations. |
| T-06-30 | Repudiation | evidence ledger | high | mitigate | Record exact executed commands/results and leave failures open. |
| T-06-31 | Spoofing | mocked remote evidence | high | mitigate | Label candidate fixtures and explicitly exclude BACK-05 live acceptance. |
| T-06-32 | Denial of service | verification | low | accept | Fixed focused suites bound runtime; broader Phase-8 live qualification remains separate. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run verifier self-tests, then the fixed Phase-6 verifier from repository and non-repository cwd. Review the completed evidence matrix for every requirement, decision, edge predicate, and prohibition.
</verification>

<success_criteria>
- Fixed tests prove the canonical public behavior and one-authority architecture.
- Adversarial verifier self-tests fail on every prohibited regression.
- Earlier storage lifecycle guarantees remain green.
- Validation is reproducible, honest about runtime/remote limits, and complete for Phase 6.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-08-SUMMARY.md` when done.
</output>
