---
phase: 06-unifiedcache-policy-composition
plan: "06"
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - src/cacheness/config.py
  - src/cacheness/core.py
  - src/cacheness/decorators.py
  - src/cacheness/__init__.py
  - tests/test_phase6_public_api_contract.py
autonomous: true
requirements: [CACH-01, CACH-02, CACH-03, CACH-04, CACH-05, CACH-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: The supported cache API is one canonical surface centered on UnifiedCache, CacheConfig, and cached."
    - "D-03/D-04: Configuration is nested by ownership and optional capability failures are explicit; optional availability does not become cache identity."
    - "D-05: Immutable lookup, put, removal, close, and statistics results are exported coherently and retain typed causes/outcomes."
    - "Pre-production overlapping factories, singleton helpers, aliases, flat compatibility arguments, and alternate decorators are removed rather than wrapped."
    - "Format/schema version identification remains explicit, unsupported old layouts fail clearly, and Phase-7 offline migration/rebuild remains possible."
  artifacts:
    - path: src/cacheness/__init__.py
      provides: "Deliberate canonical exports with honest optional capabilities"
    - path: src/cacheness/config.py
      provides: "Nested ownership-aligned configuration without flat compatibility paths"
    - path: tests/test_phase6_public_api_contract.py
      provides: "Positive/negative surface, optional dependency, SqlCache separation, and version-retention contract"
  key_links:
    - from: src/cacheness/__init__.py
      to: src/cacheness/core.py
      via: "one import path for UnifiedCache and immutable public result types"
      pattern: "__all__|UnifiedCache"
    - from: src/cacheness/config.py
      to: src/cacheness/storage/composition.py
      via: "nested topology, policy, handler, and security ownership boundaries"
      pattern: "CacheConfig|CachePolicyConfig|StoreTopology"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] The public cutover must not preserve removed behavior through aliases, wrappers, hidden singletons, ambiguous flags, or alternate constructors/decorators."
    - "[FLAGGED/UNVERIFIED] The cutover must not remove format/schema identification, imply in-place upgrade, fold SqlCache into UnifiedCache, or claim Phase-8 remote qualification."
---

<objective>
Complete the intentional pre-production public API and configuration cutover to the canonical policy-over-storage model.

Purpose: CACH-06 requires one learnable import/configuration/result surface and explicit optional-feature behavior after the lifecycle changes are proven.
Output: Canonical exports, nested configuration, negative compatibility tests, and retained version/migration boundaries.
</objective>

## Artifacts this phase produces

- `src/cacheness/__init__.py`: supported import surface and optional capability behavior.
- `src/cacheness/config.py`: canonical nested configuration only.
- `tests/test_phase6_public_api_contract.py`: executable public surface and removal declaration.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-RESEARCH.md
@.planning/phases/06-unifiedcache-policy-composition/06-PATTERNS.md
@src/cacheness/__init__.py
@src/cacheness/config.py
@src/cacheness/core.py
@src/cacheness/sql_cache.py
@src/cacheness/storage/read_contract.py
@.planning/phases/06-unifiedcache-policy-composition/06-05-SUMMARY.md
</context>

<interfaces>
The public cache exports are `UnifiedCache`, `CacheConfig`, `CachePolicyConfig`, `cached`, `CacheOutcome`, `CacheLookupResult`, `CacheStatistics`, `CacheRemovalReport`, `CacheMaintenanceState`, `CacheMaintenanceResult`, and `CachePutResult`. Storage exports and `SqlCache` remain separate supported surfaces. Optional exports are capability-gated; they do not select or replace the cache's BlobStore identity.
</interfaces>

<source_coverage>
- CACH-01..CACH-05: the already-proven topology, policy, decorator, and statistics types become the supported public surface.
- CACH-06: canonical exports, nested configuration, intentional removal, optional capability checks, and deterministic import behavior are covered.
- CONTEXT: D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-14, D-16, and D-17 are enforced at the public boundary.
- RESEARCH: pre-production break rather than compatibility adapters, explicit optional-dependency failures, SqlCache separation, and migration/version retention are covered.
- API coverage declaration: `COVERAGE.md` records that Phase 6 introduces no external API, SDK, or service integration.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Import, configure, and run one object through the canonical public surface</name>
  <files>src/cacheness/config.py, src/cacheness/core.py, src/cacheness/__init__.py, tests/test_phase6_public_api_contract.py</files>
  <read_first>
    - src/cacheness/__init__.py
    - src/cacheness/config.py
    - src/cacheness/core.py
    - src/cacheness/error_handling.py
    - src/cacheness/storage/composition.py
    - src/cacheness/storage/read_contract.py
    - tests/test_public_api_contract.py
    - tests/test_config_validation.py
  </read_first>
  <behavior>
    - "Canonical imports -> build nested CacheConfig, explicitly initialize UnifiedCache, put/lookup one object, read statistics, and explicitly close."
    - "No store/topology supplied -> deterministic configuration error before I/O."
    - "Optional feature unavailable -> base import still succeeds; using that feature raises a clear install/capability error."
    - "Repeated optional import order -> identical canonical __all__ and selected BlobStore identity."
  </behavior>
  <action>Write the public end-to-end test first. Finalize the canonical constructor and immutable result types proven in Plans 01-05, including the Plan-03 `CacheMaintenanceResult`/`CacheMaintenanceState` explicit-resume pair, then export the intentionally supported surface from cacheness.__init__ per D-01 and D-05: UnifiedCache, nested CacheConfig/CachePolicyConfig, cached, the cache result/report/statistics types, and the independently supported storage and SqlCache entry points. Organize CacheConfig by topology/storage ownership, policy, handlers, and security per D-03; remove flat overlapping backend selectors and validate before I/O. Make store/topology selection mandatory and unambiguous per D-02 and the locked injected-versus-created ownership resolution. Fix optional exports/capability detection so unavailable dependencies do not break base import, emit the documented optional-export warning where applicable, and raise a typed install-oriented failure only when the caller requests that feature per D-04. Resolve CACH-06 fallback probes: empty means omitted composition is rejected; adjacency means missing optional features and the separate SqlCache boundary remain explicit; ordering means __all__ and feature behavior do not vary with optional import/registration order. Keep primary cache identity the selected BlobStore composition, not capability flags; this is the reasoned no-change outcome for the optional-export assumption delta.</action>
  <reversibility rating="costly">This is the intentional pre-production API cutover; downstream users must migrate to one canonical constructor/config/result vocabulary.</reversibility>
  <acceptance_criteria>
    - "A test importing only documented canonical names performs initialize/put/lookup/statistics/close successfully on memory/memory."
    - "Omitted or ambiguous composition and invalid nested policy settings fail before any storage method is called."
    - "Subprocess tests with optional modules unavailable can import cacheness and receive a clear feature-specific failure only when requesting that feature."
    - "Canonical __all__ order/content and cache store identity are stable across simulated optional import orders."
    - "SqlCache remains separately importable and is not instantiated or routed through UnifiedCache."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_public_api_contract.py -k "canonical or config or optional or ordering or sqlcache" -o log_cli=false</automated>
  </verify>
  <done>One documented import/configuration path exercises the entire canonical cache lifecycle with honest capabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove overlapping pre-production APIs while retaining version boundaries</name>
  <files>src/cacheness/config.py, src/cacheness/core.py, src/cacheness/decorators.py, src/cacheness/__init__.py, tests/test_phase6_public_api_contract.py</files>
  <read_first>
    - src/cacheness/__init__.py
    - src/cacheness/config.py
    - src/cacheness/core.py
    - src/cacheness/decorators.py
    - src/cacheness/storage/catalog.py
    - tests/test_public_api_contract.py
    - tests/test_config_options.py
  </read_first>
  <behavior>
    - "Removed global/factory/alias/flat-config names -> absent from public exports and fail directly, with no compatibility delegation."
    - "Unsupported current stored format/schema version -> explicit typed rejection, never implicit upgrade."
    - "Current canonical format/schema version -> remains discoverable in authoritative records for Phase-7 offline tooling."
    - "SqlCache and explicit supported storage entry points -> remain separate and functional."
  </behavior>
  <action>Delete the overlapping development-only surface identified by D-01 through D-04 rather than adding wrappers: callable `cacheness` alias, get_cache/reset_cache hidden singleton helpers, UnifiedCache.for_api and overlapping cache factories, decorator aliases/builders/CacheContext ownership paths, flat CacheConfig compatibility arguments/properties, and create_cache_config-style parallel construction. Also retire the raw-value `UnifiedCache.get`, dictionary `get_stats`, and unbounded `list_entries` compatibility shapes now replaced by `lookup`, immutable statistics, and bounded catalog-backed policy operations per D-05/D-06/D-08/D-10. Update or replace backward-compatibility tests with negative public-contract tests that prove these names are gone, not delegated. Do not remove explicit catalog/payload format and schema version fields or unsupported-version errors; keep them authoritative so Phase 7 can implement offline migration/rebuild, but add no implicit migration here. Preserve SqlCache as a separate subsystem and do not count the Phase-5 remote candidate as BACK-05 evidence.</action>
  <reversibility rating="costly">Removed development APIs intentionally stop resolving; restoring aliases later would recreate the ambiguity this phase eliminates.</reversibility>
  <acceptance_criteria>
    - "Negative tests prove each enumerated singleton, alias, alternate factory/decorator, context owner, and flat configuration path is absent and has no delegation wrapper."
    - "Canonical imports and the full Phase-6 behavior suites continue to pass after removals."
    - "Authoritative records still expose explicit format/schema identifiers and unsupported versions fail with a typed error."
    - "No runtime code performs an implicit stored-layout upgrade; Phase-7 offline tooling remains the declared migration path."
    - "COVERAGE.md contains the reasoned no-external-API declaration and no fabricated API matrix exists."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_public_api_contract.py tests/test_phase6_lookup_contract.py tests/test_phase6_removal_contract.py tests/test_phase6_policy_contract.py tests/test_phase6_decorator_contract.py tests/contracts/test_phase6_topology_policy.py -o log_cli=false</automated>
  </verify>
  <done>The public package exposes one canonical model, removed APIs do not resolve, and version/migration boundaries remain explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public config/import -> cache construction | Ambiguous or unavailable capabilities must fail before storage mutation. |
| Stored version -> runtime parser | Unsupported layouts must fail closed rather than upgrade implicitly. |
| Optional dependency -> base package | Missing extras must not corrupt base import or masquerade as available. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-21 | Spoofing | optional capability flags | medium | mitigate | Test the dependency/capability itself and fail only feature use with clear guidance. |
| T-06-22 | Tampering | stored schema/version | high | mitigate | Preserve explicit identifiers and reject unsupported versions before decoding/mutation. |
| T-06-23 | Elevation of privilege | compatibility route | high | mitigate | Remove alternate public routes and prove absence with negative import/AST tests. |
| T-06-24 | Denial of service | invalid public config | medium | mitigate | Validate nested configuration and composition before resource allocation or I/O. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run the canonical and negative public API suite, then every Phase-6 behavior suite. Confirm optional-dependency subprocesses and unsupported-version tests fail at the intended boundary.
</verification>

<success_criteria>
- One canonical public import/configuration/result surface remains.
- Removed development APIs have no compatibility implementation.
- Optional dependencies fail explicitly without defining cache identity.
- Version identifiers and future offline migration/rebuild boundaries remain intact.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-06-SUMMARY.md` when done.
</output>
