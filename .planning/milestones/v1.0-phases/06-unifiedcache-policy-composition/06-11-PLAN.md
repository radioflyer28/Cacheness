---
phase: 06-unifiedcache-policy-composition
plan: "11"
type: execute
wave: 10
depends_on: ["06-09", "06-10"]
files_modified:
  - tools/run_phase6_local_suite.py
  - tests/test_phase6_suite_isolation.py
  - tests/test_full_suite_environment.py
  - tests/test_phase3_gap_acceptance.py
  - tools/verify_phase6_contracts.py
  - tests/test_phase6_contract_verifier.py
  - .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [CACH-01, CACH-02, CACH-03, CACH-04, CACH-05, CACH-06, CACH-07]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One fixed isolated all-extras local command runs the complete current repository suite while excluding exactly the three Phase 8 live PostgreSQL/S3 modules by path; missing fixtures, deselected non-live tests, or pytest failure cannot render green."
    - "The canonical Phase 6 public tests and separate SqlCache regression pass both alone in the fixed verifier and in repository suite order (CACH-06, CACH-07; D-01, D-04, D-14, D-15)."
    - "Test-owned import, optional-dependency, environment, registry, and backend state is confined by monkeypatch/context cleanup or subprocess boundaries; no production global reset or autouse product-state mutation is introduced (D-02, D-06, D-08, D-17)."
    - "The Phase 6 verifier covers every migrated module and rejects positive flat CacheConfig, implicit UnifiedCache, legacy raw result/query use while allowing only deliberate TypeError/absence negatives (D-01 through D-17)."
    - "PostgreSQL/Amazon-S3 live qualification and native Windows remain UNAVAILABLE/NOT_QUALIFIED, not skipped successes or Phase 6 evidence."
  artifacts:
    - path: tools/run_phase6_local_suite.py
      provides: "Root-safe fixed local-suite runner with an exact three-module live exclusion"
    - path: tests/test_phase6_suite_isolation.py
      provides: "Mutation-style proof of selection, exit propagation, and ordered canonical/SqlCache isolation"
    - path: tools/verify_phase6_contracts.py
      provides: "Expanded fixed canonical-cutover manifest and AST migration audit"
    - path: .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
      provides: "Executed full-suite, fixed-verifier, Ruff, and unavailable-live evidence"
  key_links:
    - from: tools/run_phase6_local_suite.py
      to: tests/integration/test_postgresql_authority.py
      via: "exact repository-relative --ignore selection shared with the other two live modules"
      pattern: "LIVE_QUALIFICATION_MODULES|--ignore"
    - from: tools/verify_phase6_contracts.py
      to: tests/test_phase6_suite_isolation.py
      via: "fixed regression manifest and source-aware legacy-call audit"
      pattern: "CANONICAL_CUTOVER_NODES|CacheConfig|UnifiedCache"
  prohibitions:
    - "Do not edit production source. If the complete migrated suite exposes a genuine independent defect, preserve the failing evidence and stop for a separately justified source-owning plan."
    - "Do not make live tests pass by skipping, xfail, dummy fixtures, credentials substitution, mocks credited as qualification, or collection suppression outside the exact three path exclusions."
    - "Do not add lifecycle locks, queues, coordinators, readiness mechanisms, sidecars, timing correctness, cross-resource ACID, compatibility routes, or guarantee inflation."
---

<objective>
Make the Phase 6 repository gate deterministic in suite order and truthful about unavailable live qualifications.

Purpose: Close verification gap 2 after the canonical test migrations land, without converting unavailable PostgreSQL/S3/native-Windows evidence into a pass.
Output: A fixed local-suite runner, isolation/self-selection tests, expanded Phase 6 verifier, repaired historical test-node inventory, and executed validation evidence.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-RESEARCH.md
@.planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md
@.planning/phases/06-unifiedcache-policy-composition/06-VERIFICATION.md
@.planning/phases/06-unifiedcache-policy-composition/06-REVIEW-FIX.md
@.planning/phases/06-unifiedcache-policy-composition/06-08-SUMMARY.md
@tools/verify_phase6_contracts.py
@tools/verify_phase5_contracts.py
@tests/test_full_suite_environment.py
@tests/qualification/conftest.py
</context>

<interfaces>
The complete local command is `uv run --isolated --all-extras --group dev --frozen python tools/run_phase6_local_suite.py --repo-root .`. The runner invokes the current interpreter's pytest over the repository testpaths with `-q -o log_cli=false` and exactly three `--ignore` paths: tests/integration/test_postgresql_authority.py, tests/integration/test_s3_generation.py, and tests/integration/test_remote_topology.py. The unchanged Phase 5 live collector remains the only local collection route for those modules, and the Phase 8 qualification fixture remains required for execution.
</interfaces>

## Multi-Source Coverage Audit

| Source | ID/item | Plan(s) | Status | Notes |
|---|---|---|---|---|
| GOAL | One coherent cache-policy API over BlobStore | 09-11 | COVERED | Current tests and final gates exercise only the delivered canonical surface. |
| GOAL | Verification gap 1 — migrate all remaining current test consumers | 09-10 | COVERED | Five format/security/public files and four catalog/query/quality files have exclusive owners. |
| GOAL | Verification gap 2 — deterministic suite order and live selection | 11 | COVERED | Fixed isolated runner, ordered regressions, and exact live exclusions. |
| REQ | CACH-01 BlobStore-only lifecycle | 09-11 | COVERED | Explicit composition and fixed one-engine verifier remain required. |
| REQ | CACH-02 UnifiedCache owns policy | 10-11 | COVERED | Catalog remains BlobStore authority; cache result/policy tests stay fixed. |
| REQ | CACH-03 exact lifecycle removal | 10-11 | COVERED | Query/invalidation coverage and retained Phase 6 removal nodes run. |
| REQ | CACH-04 cached None is distinct | 09, 11 | COVERED | Typed lookup/result migrations plus existing fixed tests. |
| REQ | CACH-05 six-outcome statistics | 09, 11 | COVERED | Raw result assertions migrate; fixed statistics node remains. |
| REQ | CACH-06 coherent public surface | 09-11 | COVERED | Positive canonical and negative removed-route tests run together. |
| REQ | CACH-07 SqlCache remains separate | 11 | COVERED | Fixed verifier and full ordered suite both execute tests/test_sql_cache.py. |
| RESEARCH | Presence-bearing typed results and explicit cache injection | 09 | COVERED | Retained cache tests use receipt/lookup results and one explicit store. |
| RESEARCH | Typed bounded catalog queries and opaque cursors | 10 | COVERED | Retired query tests move to Phase 4 catalog APIs. |
| RESEARCH | Fixed root-safe verifier and no new packages/services | 11 | COVERED | Existing locked all-extras environment and exact manifests only. |
| CONTEXT | D-01 canonical public surface | 09, 11 | COVERED | Removed aliases remain negative-only. |
| CONTEXT | D-02 explicit construction | 09, 11 | COVERED | Every cache has one explicit store composition. |
| CONTEXT | D-03 nested configuration | 09-11 | COVERED | Flat call audit permits only deliberate TypeError negatives. |
| CONTEXT | D-04 honest optional features | 09, 11 | COVERED | Isolated all-extras local evidence is separate from live qualification. |
| CONTEXT | D-05 presence-bearing results | 09, 11 | COVERED | CachePutResult/CacheLookupResult replace raw results. |
| CONTEXT | D-06 one lookup boundary | 09, 11 | COVERED | No migrated test restores get or second reads. |
| CONTEXT | D-07 typed miss/error policy | 09, 11 | COVERED | Signing/corruption tests retain outcome and cause. |
| CONTEXT | D-08 immutable statistics | 09, 11 | COVERED | Existing fixed statistics coverage remains suite-green. |
| CONTEXT | D-09 authoritative TTL facts | 10, 11 | COVERED | Retained TTL/query tests use canonical facts and fixed removal regressions. |
| CONTEXT | D-10 lifecycle-only removal | 10, 11 | COVERED | Catalog tests do not delete backend resources directly. |
| CONTEXT | D-11 structured removal truth | 10, 11 | COVERED | Typed result/report nodes remain in the verifier. |
| CONTEXT | D-12 deterministic bounded size policy | 11 | COVERED | Fixed policy contract remains in both verifier and full suite. |
| CONTEXT | D-13 bounded portable predicates | 10-11 | COVERED | Typed preflight/page/work tests and AST sentinel. |
| CONTEXT | D-14 explicit decorator ownership | 09, 11 | COVERED | Canonical public/decorator regressions run in suite order. |
| CONTEXT | D-15 declared decorator recomputation | 11 | COVERED | Existing fixed decorator node remains required. |
| CONTEXT | D-16 truthful function clear | 11 | COVERED | Existing fixed decorator/removal nodes remain required. |
| CONTEXT | D-17 committed-partial close | 09, 11 | COVERED | Explicit lifecycle tests and retained topology nodes run. |

Deferred policy plugins, distributed coherence, migration tooling, real PostgreSQL/Amazon-S3 qualification, Python/platform release matrices, performance budgets, and SqlCache redesign remain excluded exactly as recorded in 06-CONTEXT.md.

<tasks>

<task type="tracer">
  <name>Task 1: Run the complete local corpus with exact live-module exclusion and clean test-owned state</name>
  <files>tools/run_phase6_local_suite.py, tests/test_phase6_suite_isolation.py, tests/test_full_suite_environment.py, tests/test_phase3_gap_acceptance.py</files>
  <read_first>
    <file>tests/test_full_suite_environment.py</file>
    <file>tests/test_phase3_gap_acceptance.py</file>
    <file>tests/test_phase6_public_api_contract.py</file>
    <file>tests/test_sql_cache.py</file>
    <file>tests/test_handlers.py</file>
    <file>tools/verify_phase5_contracts.py</file>
    <file>tests/qualification/conftest.py</file>
  </read_first>
  <action>Create a root-safe local-suite runner with a literal tuple containing exactly the three Phase 8 live qualification modules. Before spawning pytest, validate that repo-root is a repository directory, each excluded path is repository-relative and exists, and the exclusion tuple has no duplicate or non-live path. Invoke sys.executable -m pytest against the normal repository testpaths with quiet/log settings and one --ignore per live module; propagate timeout, signal, collection, and test failures as nonzero without parsing a pass from output. Print the exact excluded paths and state that PostgreSQL/Amazon-S3 are UNAVAILABLE/NOT_QUALIFIED locally; do not load the live fixture plugin or manufacture skips.

Add runner self-tests for root safety, exact-three selection, missing-path failure, subprocess argv, nonzero propagation, and absence of broad integration-directory exclusion. Add an ordered subprocess regression that runs the public/optional-import tests before tests/test_phase6_public_api_contract.py and tests/test_sql_cache.py, then the reverse order, proving both targets pass without import/registry/environment leakage. Use child-process boundaries inside the owned isolation test; do not edit an unowned polluting test, add a global autouse reset, or mutate production registries. If either ordered probe still exposes pollution outside this plan's owned files, preserve the reproducer and stop for a separately scoped test-owning plan. Update tests/test_full_suite_environment.py to publish the runner command as the Phase 6 local gate while retaining the broader Phase 8 release command separately. Replace stale missing node IDs in tests/test_phase3_gap_acceptance.py with current surviving exact nodes that cover the same integrity/recovery findings; never recreate removed scheduler/readiness tests or turn missing nodes into skips.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase6_suite_isolation.py tests/test_full_suite_environment.py tests/test_phase3_gap_acceptance.py tests/test_phase6_public_api_contract.py tests/test_sql_cache.py -o log_cli=false</automated>
  </verify>
  <done>The fixed runner and ordered probes pass, execute all non-live tests, propagate failures, and exclude only the three unavailable Phase 8 live modules.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the fixed verifier and record the final local evidence</name>
  <files>tools/verify_phase6_contracts.py, tests/test_phase6_contract_verifier.py, .planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md</files>
  <read_first>
    <file>tools/verify_phase6_contracts.py</file>
    <file>tests/test_phase6_contract_verifier.py</file>
    <file>tools/run_phase6_local_suite.py</file>
    <file>.planning/phases/06-unifiedcache-policy-composition/06-VALIDATION.md</file>
    <file>.planning/phases/06-unifiedcache-policy-composition/06-VERIFICATION.md</file>
  </read_first>
  <action>Add the nine Plan 09/10 migrated tests, the suite-isolation test, and the repaired Phase 1/3 gate tests to a fixed canonical-cutover regression group in tools/verify_phase6_contracts.py. Extend its AST audit over that literal inventory so a CacheConfig call with retired flat keywords, a UnifiedCache call without store, or an executable call to removed cache get/query/stats/list surfaces fails. The audit must allow only source-aware deliberate negatives inside pytest.raises(TypeError) or explicit hasattr/signature absence assertions; strings, comments, and the preserved negative tests must not be counted as positive calls. Keep CACH-01 through CACH-07 labels mapped to all supporting static and behavioral checks, and add self-tests that mutate one positive legacy call, one allowed negative, and one omitted migration node.

Run, in order: the complete local runner once; the root-safe fixed Phase 6 verifier once; and Ruff over every Python file modified by Plans 09-11. Update 06-VALIDATION.md with exact commands, timestamps/environment, pass/fail/skip counts, and artifact links actually observed. Mark the Phase 6 full-suite and CACH-07 gates green only if both commands exit zero. Record the three excluded live modules plus native Windows as UNAVAILABLE/NOT_QUALIFIED and leave BACK-05/Phase 8 unchanged. If any canonical migrated test reveals a production defect, keep the row open and report the exact reproducer and source-change justification rather than editing source in this task.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase6_contracts.py --repo-root .</automated>
  </verify>
  <done>The complete non-live local suite, fixed Phase 6/CACH-07 verifier, and scoped Ruff all pass from isolated locked dependencies, while every live/native qualification remains explicitly unpassed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository tests -> local-suite verdict | Exact selection and subprocess status must prevent missing or failing non-live tests from appearing green. |
| Test-owned global state -> later test modules | Import flags, registries, environment, and backend doubles must not leak across suite order. |
| Local/mocked evidence -> release qualification | Candidate behavior cannot impersonate real PostgreSQL, Amazon S3, or native-Windows evidence. |
| Migrated source text -> fixed verifier | AST checks must distinguish executable compatibility use from deliberate negative assertions. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-41 | Tampering | local-suite selection | high | mitigate | Validate a literal exact-three exclusion and fail on missing paths, duplicates, or subprocess errors. |
| T-06-42 | Repudiation | suite-order state | high | mitigate | Run canonical public and SqlCache nodes in both fixed orders and confine mutations to owning tests/subprocesses. |
| T-06-43 | Spoofing | live/native status | high | mitigate | Print and record UNAVAILABLE/NOT_QUALIFIED; never credit skip, mock, or collection as qualification. |
| T-06-44 | Elevation of privilege | verifier allowlist | high | mitigate | Allow legacy syntax only in structural TypeError/absence negatives and self-test positive-call rejection. |
| T-06-45 | Denial of service | complete suite | low | accept | The isolated all-extras suite is finite and its subprocess timeout/failure remains visible; Phase 8 owns broader platform/service runs. |

No package changes are made; uv resolves only the existing locked extras and dev group.
</threat_model>

<verification>
1. Complete local gate: `uv run --isolated --all-extras --group dev --frozen python tools/run_phase6_local_suite.py --repo-root .`.
2. Equivalent explicit pytest selection: `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false --ignore=tests/integration/test_postgresql_authority.py --ignore=tests/integration/test_s3_generation.py --ignore=tests/integration/test_remote_topology.py`.
3. Fixed verifier: `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase6_contracts.py --repo-root .`.
4. Scoped lint: `uv run --isolated --all-extras --group dev --frozen ruff check tools/run_phase6_local_suite.py tools/verify_phase6_contracts.py tests/test_blob_manifest.py tests/test_filesystem_containment.py tests/test_cache_signing.py tests/test_legacy_array_security.py tests/test_public_api_contract.py tests/test_query_meta.py tests/test_store_cache_key_params_config.py tests/test_query_meta_security.py tests/test_phase1_quality_gates.py tests/test_phase6_suite_isolation.py tests/test_full_suite_environment.py tests/test_phase3_gap_acceptance.py tests/test_phase6_contract_verifier.py`.
</verification>

<success_criteria>
- The exact non-live isolated suite passes with no hidden deselection beyond three explicit Phase 8 files.
- The fixed verifier passes CACH-01 through CACH-07 and every migrated cutover node.
- Phase 6 public and SqlCache regressions pass alone and in both defined suite orders.
- Scoped Ruff passes for every changed Python file.
- PostgreSQL/Amazon-S3 and native-Windows remain explicitly unavailable/unqualified rather than falsely passed.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-11-SUMMARY.md` when done.
</output>
