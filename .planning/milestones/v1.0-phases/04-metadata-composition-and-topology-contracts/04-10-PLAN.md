---
phase: 04-metadata-composition-and-topology-contracts
plan: "10"
type: execute
wave: 9
depends_on: [04-09]
files_modified:
  - src/cacheness/storage/composition.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/backends/blob_backends.py
  - src/cacheness/storage/backends/__init__.py
  - src/cacheness/__init__.py
  - tests/test_blob_store_composition.py
  - tests/test_topology_capabilities.py
  - tests/test_public_api_contract.py
  - tests/test_metadata_role_contract.py
autonomous: true
gap_closure: true
requirements: [BACK-02, BACK-03, BACK-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One application-extensible RoleRegistry is part of the StoreTopology composition root and every built-in or application named role reaches BlobStore through that same path (CR-03; BACK-03; D-09, D-10)."
    - "The disconnected blob registry and all package exports are absent, with no synchronization adapter, alias, or second selection path (CR-03; D-17)."
    - "Payload, authority, and projection participants satisfy runtime structural role protocols before BlobStore initialization; invalid objects fail during composition (CR-06; BACK-06; D-11)."
    - "Every store-owned participant, including a just-created or transfer-owned invalid resource, is entered into one identity-deduplicated unwind ledger before validation and is closed exactly once; caller-owned injections remain open (CR-06; D-12)."
    - "The canonical public barrels expose only RoleRegistry/StoreTopology selection while explicit version detection and offline migration/rebuild seams remain intact (D-17, D-18)."
  artifacts:
    - path: src/cacheness/storage/composition.py
      provides: "Single reachable RoleRegistry, structural role validation, and identity-deduplicated ownership guard"
    - path: src/cacheness/storage/backends/blob_backends.py
      provides: "Payload implementations without a disconnected legacy registry"
    - path: tests/test_blob_store_composition.py
      provides: "High-level registered-name, invalid-resource unwind, and exact-instance tests"
    - path: tests/test_public_api_contract.py
      provides: "Canonical absence proof for retired registry exports"
  key_links:
    - from: src/cacheness/storage/composition.py
      to: src/cacheness/storage/blob_store.py
      via: "StoreTopology carries its application RoleRegistry and BlobStore resolves the topology once"
      pattern: "RoleRegistry|resolve"
    - from: src/cacheness/storage/composition.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "authority participants are checked against the runtime LifecycleAuthority protocol"
      pattern: "LifecycleAuthority"
    - from: src/cacheness/__init__.py
      to: src/cacheness/storage/backends/__init__.py
      via: "both public barrels omit the retired blob registry surface"
      pattern: "__all__"
  prohibitions:
    - "No new lock, queue, scheduler, readiness registry/event, lease, sidecar, lifecycle coordinator, second authority, or timing-derived correctness rule."
    - "No universal-success, cross-resource ACID, or multi-host guarantee beyond ADR 0001."
    - "No compatibility shim, old metadata-authority export, dual registry, or implicit migration during normal open."
    - "No projection may authorize lifecycle, cleanup, repair, or canonical query completeness."
    - "Explicit schema/format version detection and offline migration/rebuild tooling remain supported."
---

<objective>
Close the application-registration, structural-validation, and ownership-unwind gaps in the one topology composition root.

Purpose: Ensure every advertised role can be selected at BlobStore construction, is valid before use, and has one explicit close owner without reviving retired metadata APIs.
Output: One reachable registry, no legacy blob registry, structural protocols for all roles, and close-once failure unwind.
</objective>

## Artifacts this phase produces

- A single application-extensible registry carried by StoreTopology and consumed by BlobStore.
- Runtime structural role validation plus an identity-deduplicated ownership ledger for resolution and close.
- A clean package surface whose downstream consumers can migrate directly to RoleRegistry without a compatibility path.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-PATTERNS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-REVIEW.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VERIFICATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-09-SUMMARY.md
</context>

<source_coverage>
| Source | Items covered |
|---|---|
| GOAL | Select advertised backends through one composition root and reject guarantees/participants the active topology cannot supply. |
| REQ | BACK-02 explicit authority/projection roles; BACK-03 exact injected and registered selection; BACK-06 truthful capabilities and construction-time rejection. |
| RESEARCH | One role-aware registry; exact injected identity; explicit caller/store ownership; structural protocols; derived-only projections. |
| CONTEXT | D-09 through D-13, D-16 through D-18. No Phase 5 backend delivery, Phase 6 cache-policy migration, or Phase 7 migration execution is included. |
</source_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route application registrations through StoreTopology into BlobStore</name>
  <reversibility rating="costly">D-09/D-17 establish StoreTopology plus RoleRegistry as the only supported selection contract and remove the disconnected development API.</reversibility>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/backends/blob_backends.py, src/cacheness/storage/backends/__init__.py, tests/test_blob_store_composition.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/backends/blob_backends.py</file>
    <file>src/cacheness/storage/backends/__init__.py</file>
    <file>tests/test_blob_store_composition.py</file>
    <file>.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "StoreTopology accepts or creates exactly one RoleRegistry; BlobStore calls topology.resolve() and reaches application registrations without a separate registry parameter or global mutation."
    - "Built-ins and application registrations use RoleRegistry.register/resolve/construct with the same role/options/ownership rules."
    - "An exact caller-injected object remains exact; a named custom payload/authority/projection selected through the registry is the exact constructed object used by BlobStore."
    - "register_blob_backend, unregister_blob_backend, get_blob_backend, list_blob_backends, and their private global registry state are removed rather than forwarded or synchronized."
  </behavior>
  <action>Make RoleRegistry an explicit field of the immutable StoreTopology composition declaration, with a built-in-populated registry created per topology when the caller does not supply one. Remove the optional registry argument from resolve/capability_report so registry choice cannot bypass the one root. BlobStore continues to receive only StoreTopology and resolves that topology once; it does not gain a second selector. Prove that application code registers named payload, authority, and projection participants on the topology's registry, constructs BlobStore, and observes the exact constructed instances/options/ownership through real put/query/projection behavior.

Delete `_blob_backend_registry` and register_blob_backend/unregister_blob_backend/get_blob_backend/list_blob_backends from blob_backends.py and the storage.backends barrel. Do not delete or rewrite the downstream registry consumer modules in this production plan; Plan 04-12 rewrites those stable paths against RoleRegistry and removes only global-registry-only assertions. Do not copy entries between registries, install a process-global registry, add a fallback lookup, or preserve old names as aliases. Retain explicit format/schema migration detection and rebuild APIs; the registry cutover does not alter normal-open migration behavior.</action>
  <acceptance_criteria>CR-03 closes through a high-level BlobStore test using an application RoleRegistry carried by StoreTopology. Exactly one lookup/construction path exists for built-in and custom roles; the legacy blob registry state/functions/exports are absent and no compatibility or synchronization shim exists. Projections selected by the registry remain derived-only and cannot authorize lifecycle or catalog completeness.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_blob_store_composition.py tests/test_metadata_role_contract.py tests/test_public_api_contract.py -o log_cli=false</automated>
  </verify>
  <done>Application registrations are reachable from BlobStore through the same one-root path as built-ins, and the disconnected registry is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce structural roles and close owned invalid resources exactly once</name>
  <depends_on>Task 1 establishes the only construction path whose resource ledger must be correct.</depends_on>
  <reversibility rating="costly">Construction-time rejection and explicit ownership are observable topology behavior under D-11/D-12.</reversibility>
  <files>src/cacheness/storage/composition.py, src/cacheness/__init__.py, tests/test_topology_capabilities.py, tests/test_public_api_contract.py, tests/test_metadata_role_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/blob_backends.py</file>
    <file>src/cacheness/__init__.py</file>
    <file>tests/test_topology_capabilities.py</file>
    <file>tests/test_public_api_contract.py</file>
    <file>tests/test_metadata_role_contract.py</file>
  </read_first>
  <behavior>
    - "Authority resolution requires the runtime-checkable LifecycleAuthority contract; payload resolution requires Plan 04-09's handler-I/O provider; projection resolution requires the derived ProjectionSink/checkpoint contract."
    - "A named factory result or transfer-owned injection enters one local identity ledger before role validation; any later validation/capability/construction failure closes each owned identity exactly once in reverse first-acquisition order."
    - "Repeated references to the same store-owned object do not produce repeated close calls; caller-owned injections are never added to the close ledger."
    - "Invalid role participants and impossible capabilities fail before BlobStore initialization, payload staging, authority mutation, or projection activity."
  </behavior>
  <action>Replace nominal concrete-class cross-role checks with explicit runtime-checkable structural protocols for payload, LifecycleAuthority, and derived ProjectionSink responsibilities. Keep the protocols narrow: authority supplies the existing transactional catalog/lifecycle operations; payload supplies only guarded immutable-generation I/O; projection supplies only bounded apply/checkpoint/rebuild primitives it advertises. Reject arbitrary objects, missing methods, and cross-role participants during resolution before any store I/O.

Move ownership guarding around factory construction and injected transfer ownership so a created/transferred participant is recorded before validation can fail. Represent the ledger by object identity, preserve reverse first-acquisition close order, and make normal ResolvedTopology.close and exceptional unwind share the close-once rule. Parameterize failures at payload, authority, projection, composed-capability, and duplicate-reference stages; assert exact close counts/order and no close for caller-owned resources. Remove remaining top-level registry exports in the same cutover and retain only RoleRegistry/StoreTopology public selection. No hidden reference count, resource-sharing manager, close scheduler, or lifecycle coordinator is allowed.</action>
  <acceptance_criteria>CR-06 closes: every role is structurally valid before use, every invalid owned resource unwinds exactly once even when referenced twice, and every caller-owned resource remains open. Capability reports are produced only from valid active participants. Public barrels expose no old metadata authority or blob registry names. No coordination primitive, stronger topology guarantee, implicit migration, or projection authority is introduced.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_topology_capabilities.py tests/test_blob_store_composition.py tests/test_metadata_role_contract.py tests/test_public_api_contract.py -o log_cli=false</automated>
  </verify>
  <done>Composition fails closed before I/O and has one identity-based ownership/unwind rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application registry → participant factory | Application factories and options may return structurally invalid or aliased resources. |
| Constructed/injected participant → ownership ledger | Store-owned resources must be captured before validation and released exactly once on every failure path. |
| Public package surface → composition root | Retired selectors can silently create a second authority or select a different participant. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-40 | Elevation of privilege | participant role validation | high | mitigate | Runtime structural protocols reject objects that cannot perform exactly their declared role before any I/O. |
| T-04-41 | Denial of service | ownership unwind | high | mitigate | Identity-deduplicated guard records store-owned objects before validation and closes each once on all failures. |
| T-04-42 | Spoofing | named registry resolution | high | mitigate | One topology-carried RoleRegistry resolves role+name; legacy/global fallback paths are deleted. |
| T-04-43 | Tampering | projection role | high | mitigate | Projection protocol excludes lifecycle mutation/cleanup authority and composition rejects role confusion. |
| T-04-44 | Repudiation | compatibility cutover | medium | mitigate | Canonical public absence tests and explicit rewrite/delete classification preserve the approved D-17/D-18 boundary. |

High-severity findings block completion. Package legitimacy is not applicable because this plan adds no dependency.
</threat_model>

<verification>
Run both task commands and the Phase 4 Ruff delta. Inspect public barrels and source references to confirm there is one RoleRegistry and no removed registry functions. Fault every resolution stage with caller-owned, transfer-owned, factory-owned, and duplicate-identity participants; assertions must use deterministic call records, not timing. Plan 04-12 owns downstream registry and retired-metadata consumer migration after this production contract is fixed.
</verification>

<success_criteria>
- Custom named participants reach BlobStore through StoreTopology's one registry.
- Old blob/metadata registry exports and state are absent without a shim.
- All roles are structurally validated before I/O and all owned resources unwind once by identity.
- Downstream consumers have one unambiguous RoleRegistry/StoreTopology target while version/migration evidence remains explicit.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-10-SUMMARY.md` when done.
</output>
