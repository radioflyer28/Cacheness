---
phase: 04-metadata-composition-and-topology-contracts
plan: "12"
type: execute
wave: 11
depends_on: [04-11]
files_modified:
  - src/cacheness/storage/backends/s3_backend.py
  - tests/test_blob_backend_registry.py
  - tests/test_s3_blob_backend.py
  - tests/test_public_api_contract.py
  - tests/test_blob_store_integrity.py
  - tests/test_unified_cache_lifecycle_authority.py
  - tests/test_core.py
  - tests/test_phase3_gap_acceptance.py
  - tests/test_sqlite_metadata_bootstrap_atomicity.py
autonomous: true
gap_closure: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Meaningful registration, construction-option, fresh-instance identity, collision, and validation behaviors are rewritten in place against RoleRegistry; only assertions whose sole contract is the retired process-global blob registry are removed (CR-03; BACK-03; D-09 through D-11, D-17)."
    - "Mocked S3 tests may prove RoleRegistry registration and S3 factory option forwarding, but they do not claim that the Phase 5 S3 topology is qualified or usable as a Phase 4 lifecycle participant (D-10, D-11, D-17)."
    - "Every named stale regression module remains at its stable path and preserves its applicable public behavior, integrity, recovery, initialization, and resource-ownership assertions without importing retired metadata-authority types or restoring shims (WR-02; D-17, D-18)."
  artifacts:
    - path: tests/test_blob_backend_registry.py
      provides: "Rewrite-in-place RoleRegistry contract coverage retaining meaningful former registry behaviors"
    - path: tests/test_s3_blob_backend.py
      provides: "Local RoleRegistry/S3 factory tests without Phase 5 topology qualification claims"
  key_links:
    - from: tests/test_blob_backend_registry.py
      to: src/cacheness/storage/composition.py
      via: "RoleRegistry register/resolve/construct replaces only meaningful registration behavior"
      pattern: "RoleRegistry|BackendRole"
    - from: tests/test_s3_blob_backend.py
      to: src/cacheness/storage/backends/s3_backend.py
      via: "A local payload-role registration constructs S3 with forwarded options without resolving a production topology"
      pattern: "RoleRegistry|S3BlobBackend"
  prohibitions:
    - "No new lock, queue, scheduler, readiness registry/event, lease, sidecar, lifecycle coordinator, second authority, or timing-derived correctness rule."
    - "No universal-success, cross-resource ACID, or multi-host guarantee beyond ADR 0001."
    - "No compatibility shim, old metadata-authority export, dual registry, or implicit migration during normal open."
    - "No projection may authorize lifecycle, cleanup, repair, or canonical query completeness."
    - "Explicit schema/format version detection and offline migration/rebuild tooling remain supported."
---

<objective>
Rewrite the remaining registry, lifecycle, integrity, initialization, and cache consumers against the final public composition seams.

Purpose: Preserve meaningful stable-path regression coverage without restoring removed development APIs.
Output: Rewrite-in-place RoleRegistry, S3 factory, lifecycle, integrity, initialization, and cache regression consumers.
</objective>

## Artifacts this phase produces

- RoleRegistry consumer tests that preserve construction and validation behavior without recreating a global selector.
- Stable-path regression modules rewritten against BlobStore, StoreTopology, and lifecycle-authority contracts.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-REVIEW.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VERIFICATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-10-SUMMARY.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-11-SUMMARY.md
</context>

<source_coverage>
| Source | Items covered |
|---|---|
| GOAL | Prove that one public BlobStore/topology surface is consumable by the retained stable-path registry and lifecycle regressions without a hidden registry or retired metadata authority. |
| REQ | BACK-02 role separation; BACK-03 registered selection; BACK-06 structural validation/truthful qualification; BACK-07 retained catalog and version evidence. |
| RESEARCH | Approved pre-production cutover, one role registry, exact construction options/identity, and stable current-surface tests. |
| CONTEXT | D-08 through D-12 and D-17/D-18. Live S3/PostgreSQL belongs to Phase 5, cache-policy redesign to Phase 6, migration execution to Phase 7, and pandas/SQL-cache installation qualification to Phase 8. |
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rewrite blob and S3 registry consumers in place against RoleRegistry</name>
  <reversibility rating="reversible">Tests retain the behavioral value of the development registry while binding it to the approved one-root public contract.</reversibility>
  <files>tests/test_blob_backend_registry.py, tests/test_s3_blob_backend.py, src/cacheness/storage/backends/s3_backend.py, tests/test_public_api_contract.py</files>
  <read_first>
    <file>tests/test_blob_backend_registry.py</file>
    <file>tests/test_s3_blob_backend.py</file>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>tests/test_public_api_contract.py</file>
  </read_first>
  <behavior>
    - "test_blob_backend_registry.py remains a collected module and covers per-instance built-ins, typed role/name collisions, replace behavior, callable/factory validation, unknown-name errors, construction-option forwarding, and fresh constructed identity through RoleRegistry."
    - "Assertions about mutating/listing/unregistering one process-global registry or exporting its functions are removed because that state itself is the retired contract; equivalent meaningful collision/construction assertions remain."
    - "The S3 registry case registers S3BlobBackend in a local RoleRegistry, resolves its RoleRegistration, constructs with moto-backed options, and proves exact type/options without calling StoreTopology.resolve or BlobStore."
    - "Public absence tests confirm retired selectors remain absent while RoleRegistry, BackendRole, BackendRef, and StoreTopology stay importable from their canonical barrel."
  </behavior>
  <action>Rewrite tests/test_blob_backend_registry.py in place as the focused RoleRegistry unit/contract suite. Preserve the prior suite's meaningful registration-name validation, duplicate/replace behavior, factory-callability rejection, option forwarding, exception translation, and distinct constructed-instance identity using RoleRegistry.register, resolve, construct, and RoleRegistration.construct as appropriate. Preserve built-in payload-name coverage without depending on private registry dictionaries or process-global reset fixtures. Remove only cases whose entire assertion is global unregister/list ordering, private global mutation, or legacy top-level export presence; do not replace those functions with new aliases or synchronization helpers.

Rewrite only the registry section of tests/test_s3_blob_backend.py to use a fresh local RoleRegistry and the payload role. Resolve the S3 RoleRegistration and construct its factory under moto to verify bucket, endpoint, region, SSL, and option forwarding. Do not feed S3 into StoreTopology or BlobStore in Phase 4 because its immutable-generation lifecycle primitive and live topology qualification belong to Phase 5. Update the s3_backend module guidance to show local RoleRegistry registration and state the topology-qualification boundary. Strengthen test_public_api_contract.py so the canonical composition exports are present and all retired registry selectors are absent, without making string-only negative assertions count as executable consumers.</action>
  <acceptance_criteria>CR-03's downstream registry consumers now exercise the one RoleRegistry contract at their existing paths. Registration, construction options, fresh identity, and validation coverage remain substantive; only global-registry-only assertions disappear. Mocked S3 factory coverage is green without claiming Phase 5 lifecycle qualification, and no global fallback, compatibility alias, or second selector exists.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_blob_backend_registry.py tests/test_s3_blob_backend.py tests/test_public_api_contract.py -o log_cli=false</automated>
  </verify>
  <done>Former registry consumers use the one public role registry while retaining meaningful factory and validation evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair stable-path lifecycle, integrity, initialization, and cache regressions</name>
  <depends_on>Task 1 establishes the final consumer-facing registry and public-export shape.</depends_on>
  <reversibility rating="reversible">The modules remain in place and their assertions are translated to current behavioral seams rather than deleted wholesale.</reversibility>
  <files>tests/test_blob_store_integrity.py, tests/test_unified_cache_lifecycle_authority.py, tests/test_core.py, tests/test_phase3_gap_acceptance.py, tests/test_sqlite_metadata_bootstrap_atomicity.py</files>
  <read_first>
    <file>tests/test_blob_store_integrity.py</file>
    <file>tests/test_unified_cache_lifecycle_authority.py</file>
    <file>tests/test_core.py</file>
    <file>tests/test_phase3_gap_acceptance.py</file>
    <file>tests/test_sqlite_metadata_bootstrap_atomicity.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "Integrity tampering tests use the current authority-manifest key seam and still prove signature failure, fail-closed read behavior, and committed payload preservation/deletion rules."
    - "UnifiedCache tests preserve event-driven distinct-key overlap, exact replacement preservation, invalidation non-deletion, and selected guarded-root round trips without requiring a facade lock or authored path representation."
    - "test_core.py asserts the public statistics/result behavior directly and never conditionally imports an implementation-specific metadata cache wrapper."
    - "Phase 3 acceptance and SQLite bootstrap tests use BlobStore/SqliteLifecycleAuthority explicit initialization, owned-resource cleanup, non-mutating unsupported-layout rejection, and deterministic authority outcomes rather than retired metadata backend classes."
  </behavior>
  <action>Keep all five test modules at their current paths and convert each stale assertion to a current behavioral contract. In the integrity suite, use `_authority_manifest_key()` or a focused test fixture and retain the tamper/signature security assertions. In the UnifiedCache suite, remove the representation check for a facade mutex while preserving barrier/event-controlled distinct-key overlap; require invalidation to preserve a replacement generation and report the current non-deletion result; inspect the selected filesystem participant's resolved guarded root and round-trip data rather than the authored cache_dir spelling. In test_core.py replace the CachedMetadataBackend type branch with assertions over the public statistics shape and values.

Translate the Phase 3 acceptance and SQLite bootstrap modules to BlobStore plus SqliteLifecycleAuthority at the explicit initialize/close boundary. Preserve constructor/bootstrap failure cleanup, absence of destructor-owned correctness, transactional SQLite schema creation, non-mutating rejection of incomplete/foreign/obsolete roots, and typed contention/integrity outcomes that remain part of ADR 0001. Do not weaken a meaningful assertion merely because its old implementation class disappeared, and do not add a facade lock, retry queue, compatibility import, implicit upgrade, or alternate metadata authority.</action>
  <acceptance_criteria>WR-02 closes for these stable modules: all five collect and pass against current public or purpose-built security seams, and the original integrity/recovery/resource assertions remain observable. No test imports retired metadata authority types, no production shim is restored, and concurrency tests separate integrity outcomes from progress/performance claims.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_blob_store_integrity.py tests/test_unified_cache_lifecycle_authority.py tests/test_core.py tests/test_phase3_gap_acceptance.py tests/test_sqlite_metadata_bootstrap_atomicity.py -o log_cli=false</automated>
  </verify>
  <done>The retained lifecycle/security regressions pass at stable paths on current authority and policy seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical tests/examples -> current public API | Stale implementation imports can recreate or demand a second authority instead of testing current behavior. |
| Registered factory/options -> role construction | Application and S3 factories must retain option/identity validation without implying lifecycle qualification. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-50 | Spoofing | retired registry consumers | high | mitigate | Rewrite in place to local RoleRegistry and assert retired public selectors remain absent. |
| T-04-51 | Tampering | regression migration | high | mitigate | Preserve behavior-level integrity, recovery, initialization, version, and cleanup assertions at stable paths. |
| T-04-52 | Elevation of privilege | S3 registry test | high | mitigate | Limit Phase 4 to local registration/factory construction and forbid topology resolution or lifecycle qualification. |

High-severity findings block completion. Package legitimacy is not applicable because this plan adds no dependency.
</threat_model>

<verification>
Run each task command. Confirm every named stable-path module remains present, no production compatibility API was restored, and mocked S3 factory tests do not satisfy Phase 5 service/topology qualification. Plan 04-13 owns the exhaustive executable-tree audit and two-interpreter evidence after these consumers are green.
</verification>

<success_criteria>
- Registry and S3 consumer tests preserve meaningful RoleRegistry registration/construction behavior at stable paths.
- Retained integrity, lifecycle, initialization, and cache modules run on current seams without restored APIs.
- No named stable-path test is deleted or weakened merely to evade collection or verification.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-12-SUMMARY.md` when done; Plan 04-13 consumes that summary for the final cutover audit.
</output>
