---
phase: 04-metadata-composition-and-topology-contracts
plan: "13"
type: execute
wave: 12
depends_on: [04-12]
files_modified:
  - tests/test_cached_query_meta.py
  - tests/test_stored_compatibility.py
  - examples/custom_metadata_demo.py
  - tools/verify_phase4_cutover.py
  - .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The remaining query, stored-format, and custom-metadata consumers stay at their stable paths and use public BlobStore/catalog/version seams without restoring a retired authority, registry, or runtime compatibility adapter (WR-02; BACK-07; D-08, D-17, D-18)."
    - "An executable-tree audit finds removed-symbol imports and attribute use across source, tests, tools, examples, and benchmarks, including consumers ordinary pytest collection may not reach (D-17)."
    - "04-VALIDATION.md contains one exact Phase 4-owned matrix-path section and a separate exact deferred-diagnostic SQL-cache-path section; only the first section can populate PHASE4_MATRIX (D-17, D-18)."
    - "Python 3.11 and 3.13 run the exact Phase 4-owned matrix without filters, while the three deferred pandas-dependent SQL-cache paths are used only to classify an otherwise failing full-tree collection diagnostic (D-17, D-18)."
    - "Stable-path RoleRegistry and mocked S3 coverage from Plan 04-12 remains in the owned matrix and no test is deleted or deselected to manufacture a passing result (BACK-03; D-09 through D-11, D-17)."
  artifacts:
    - path: tests/test_cached_query_meta.py
      provides: "Public query/current-generation/resource-cleanup regression coverage"
    - path: tests/test_stored_compatibility.py
      provides: "Explicit format detection, current-layout reopen, and non-mutating migration-required coverage"
    - path: examples/custom_metadata_demo.py
      provides: "Public CatalogSchema/catalog values/query/update example"
    - path: tools/verify_phase4_cutover.py
      provides: "Executable-consumer audit, exact owned matrix runner, and separate deferred-diagnostic classifier"
    - path: .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
      provides: "Structured owned/deferred path sets plus exact Python 3.11/3.13 evidence"
  key_links:
    - from: tools/verify_phase4_cutover.py
      to: .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
      via: "The verifier parses only the bounded owned-path section into PHASE4_MATRIX and parses the disjoint deferred section only for diagnostic classification"
      pattern: "Phase 4-Owned Matrix Paths|Deferred Diagnostic SQL-Cache Paths|PHASE4_MATRIX"
    - from: tests/test_stored_compatibility.py
      to: src/cacheness/storage/blob_store.py
      via: "Public current-layout reopen and explicit unsupported-layout failure"
      pattern: "BlobStore|MigrationRequired"
  prohibitions:
    - "No new lock, queue, scheduler, readiness registry/event, lease, sidecar, lifecycle coordinator, second authority, or timing-derived correctness rule."
    - "No universal-success, cross-resource ACID, or multi-host guarantee beyond ADR 0001."
    - "No compatibility shim, old metadata-authority export, dual registry, or implicit migration during normal open."
    - "No projection may authorize lifecycle, cleanup, repair, or canonical query completeness."
    - "Explicit schema/format version detection and offline migration/rebuild tooling remain supported."
---

<objective>
Finish the consumer cutover and produce honest, reproducible two-interpreter release evidence.

Purpose: Prove the final public surface is executable across the Phase 4-owned matrix while keeping deferred pandas-dependent SQL-cache diagnostics outside that matrix.
Output: Remaining stable-path migrations, a deterministic cutover verifier, and structured Python 3.11/3.13 validation evidence.
</objective>

## Artifacts this phase produces

- Current-surface query and stored-format regressions plus a public catalog example.
- A checked-in AST audit for retired executable consumers.
- Two disjoint, machine-readable path sets: Phase 4-owned matrix paths and deferred SQL-cache diagnostic paths.
- Reproducible Python 3.11/3.13 matrix and collection-diagnostic evidence.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-REVIEW.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VERIFICATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-12-SUMMARY.md
</context>

<source_coverage>
| Source | Items covered |
|---|---|
| GOAL | Prove one public BlobStore/topology/catalog surface is consumable without hidden registries or retired metadata authorities. |
| REQ | BACK-02 role separation; BACK-03 registered selection evidence; BACK-06 structural validation/truthful qualification; BACK-07 public catalog and version evidence. |
| RESEARCH | Approved pre-production cutover, exact current-format detection, explicit migration boundaries, stable current-surface tests, and honest supported-interpreter evidence. |
| CONTEXT | D-08 through D-12 and D-17/D-18. Live S3/PostgreSQL belongs to Phase 5, cache-policy redesign to Phase 6, migration execution to Phase 7, and pandas/SQL-cache installation qualification to Phase 8. |
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Migrate the remaining query, stored-format, and catalog-example consumers</name>
  <reversibility rating="reversible">The modules remain at their stable paths and their assertions move to the approved public surface.</reversibility>
  <files>tests/test_cached_query_meta.py, tests/test_stored_compatibility.py, examples/custom_metadata_demo.py</files>
  <read_first>
    <file>tests/test_cached_query_meta.py</file>
    <file>tests/test_stored_compatibility.py</file>
    <file>examples/custom_metadata_demo.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/catalog.py</file>
  </read_first>
  <behavior>
    - "test_cached_query_meta.py remains in place and verifies supported public query results, exact current-generation visibility, and resource cleanup without ORM/session/backend type inspection."
    - "test_stored_compatibility.py remains in place and verifies explicit format/schema detection, non-mutating migration-required failures, current-layout reopen, and retained future offline migration/rebuild seams without runtime compatibility adapters."
    - "The custom metadata example uses BlobStore, CatalogSchema, catalog values, query_catalog, and update_catalog rather than a removed ORM authority base."
  </behavior>
  <action>Rewrite test_cached_query_meta.py in place around supported public UnifiedCache/BlobStore catalog-query results, exact current-generation filtering, and externally observable resource cleanup; replace ORM/session/class-shape checks with those behaviors without deleting the module or reducing meaningful assertions. Rewrite test_stored_compatibility.py in place around current format-2 reopen plus explicit version/schema detection and non-mutating migration/rebuild-required failures; preserve corpus corruption and unsupported-layout evidence while removing runtime adapters for development layouts. Convert examples/custom_metadata_demo.py to the public CatalogSchema, catalog_values, query_catalog, and update_catalog workflow. Keep migration and rebuild entry points discoverable for future Phase 7 tooling, but never perform an implicit upgrade during open. Do not restore an old metadata authority, compatibility export, global selector, or alternate lifecycle sequence.</action>
  <acceptance_criteria>The three stable-path consumers execute against the final public surface, preserving query/current-generation, format/corruption, migration-required, and resource-cleanup behavior. No compatibility shim, hidden registry, implicit migration, second authority, or test deletion is introduced.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_cached_query_meta.py tests/test_stored_compatibility.py -o log_cli=false</automated>
  </verify>
  <done>The remaining executable consumers no longer require retired metadata or registry APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce disjoint owned-matrix and deferred-diagnostic sets on Python 3.11 and 3.13</name>
  <depends_on>Task 1 and Plan 04-12 leave every stable-path consumer ready for the final audit and interpreter matrix.</depends_on>
  <reversibility rating="reversible">The structured path sets and verifier may grow as later phases take ownership, while their separation remains an evidence invariant.</reversibility>
  <files>tools/verify_phase4_cutover.py, .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md</file>
    <file>tools/verify_phase4_ruff_delta.py</file>
    <file>pyproject.toml</file>
    <file>uv.lock</file>
  </read_first>
  <behavior>
    - "04-VALIDATION.md has a `Phase 4-Owned Matrix Paths` section bounded by explicit start/end markers and a separately bounded `Deferred Diagnostic SQL-Cache Paths` section; each contains exact normalized tests/test_*.py paths and the sets are disjoint."
    - "The owned section includes every Phase 4 contract path plus all stable-path modules retained by Plans 04-12/04-13, including test_blob_backend_registry.py, test_s3_blob_backend.py, test_public_api_contract.py, test_config_validation.py, and test_phase3_local_workflows.py."
    - "The deferred section contains exactly tests/test_sql_cache.py, tests/test_sql_cache_documentation.py, and tests/test_sql_cache_failure_contract.py."
    - "verify_phase4_cutover.py derives PHASE4_MATRIX only from the bounded owned section, rejects duplicates/non-test paths/set overlap, and proves the three deferred paths are absent from PHASE4_MATRIX before invoking pytest without selection filters."
    - "The verifier parses every tracked Python consumer under src, tests, tools, examples, benchmarks, plus verify_platform.py and rejects executable retired imports, bound aliases, or package attributes while ignoring string literals used by absence tests and unrelated Base classes."
    - "The full-tree collection diagnostic uses only the deferred set to classify absent-pandas errors; any other path/cause fails, and a classified diagnostic is never reported as a green full suite."
  </behavior>
  <action>First update 04-VALIDATION.md with two explicit machine-readable sections. Bound `Phase 4-Owned Matrix Paths` with unique start/end markers and list exact normalized test paths, one per bullet. This owned list must include the Phase 4 contract modules already named by validation and every rewrite-in-place module from Plans 04-12/04-13; include configuration, local-workflow, RoleRegistry, mocked-S3, integrity, lifecycle-authority, core, Phase 3 acceptance, SQLite bootstrap, query metadata, and stored-format coverage. Bound `Deferred Diagnostic SQL-Cache Paths` with different unique start/end markers and list exactly the three pandas-dependent modules: tests/test_sql_cache.py, tests/test_sql_cache_documentation.py, and tests/test_sql_cache_failure_contract.py. Do not place any deferred path in the owned section or describe it as owned matrix membership.

Create tools/verify_phase4_cutover.py with independently runnable consumer-audit, matrix, collection-diagnostic, and aggregate modes. Its validation parser must address the two marker-bounded sections by exact heading/marker identity, normalize and validate each bullet as a repository-relative tests/test_*.py path, reject missing markers, duplicates, unexpected deferred paths, nonexistent files, and any intersection. Set PHASE4_MATRIX from the owned parsed tuple alone; never globally scrape tests/test_*.py references from validation. Assert all mandatory Phase 4 and Plans 04-12/04-13 stable paths are owned and assert every deferred path is absent before passing PHASE4_MATRIX verbatim to pytest without `-k`, ignore, deselect, or synthetic xfail. The full-tree collection diagnostic may consult only the separately parsed deferred set and may tolerate a nonzero result only when every collection error maps to one of those exact paths and the cause is absent pandas; print and record `deferred diagnostic`, never a successful full-suite claim.

Implement the consumer audit by parsing every tracked Python file in src, tests, tools, examples, benchmarks, and verify_platform.py. Fail on imports from retired cacheness metadata/blob selector surfaces, use of aliases bound by those imports, or package/module attribute access to retired symbols. Treat Base as retired only when bound from cacheness.metadata, exclude planning/history, ignore string constants used to assert public absence, and provide no executable-code allowlist. After the documentation update, rerun the parser/audit/matrix deterministically under Python 3.11 and 3.13, then run the Phase 4 Ruff delta under both. Record exact interpreter versions, commands, counts, skips, owned set, deferred diagnostic outcome, and live PostgreSQL/S3/Windows non-claims in validation. Validation is compliant only when both owned matrices and consumer audits pass; the deferred diagnostic stays explicitly non-green until Phase 8 qualifies pandas/SQL-cache installation.</action>
  <acceptance_criteria>The verifier extracts exactly two disjoint path sets from their bounded sections after the documentation update. PHASE4_MATRIX equals only the owned set on Python 3.11 and 3.13; the three deferred SQL-cache paths cannot enter it and are consulted only by the full-tree failure classifier. Stable RoleRegistry/S3 and all named regression coverage remains present, the executable tree has no retired-symbol consumer, and the evidence makes no false full-suite, live-service, Windows, universal-progress, or cross-resource ACID claim.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen --python 3.11 python tools/verify_phase4_cutover.py --all &amp;&amp; uv run --frozen --python 3.13 python tools/verify_phase4_cutover.py --all &amp;&amp; uv run --frozen --python 3.11 python tools/verify_phase4_ruff_delta.py &amp;&amp; uv run --frozen --python 3.13 python tools/verify_phase4_ruff_delta.py</automated>
  </verify>
  <done>The final release gate is reproducible on both supported interpreters and cannot mistake deferred diagnostic modules for Phase 4-owned matrix coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Historical tests/examples -> current public API | Stale implementation imports can recreate or demand a second authority instead of testing current behavior. |
| Validation document -> executable matrix | Ambiguous path extraction can silently add deferred modules to or omit owned modules from the release gate. |
| Repository tree -> release evidence | Uncollected executable consumers and interpreter-specific failures must not be omitted from the cutover claim. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-53 | Repudiation | executable consumer audit | high | mitigate | AST-scan all executable consumer roots independently of pytest collection and reject every retired import/attribute use. |
| T-04-54 | Tampering | validation path extraction | high | mitigate | Parse two separately marker-bounded exact path sets, reject malformed/overlapping membership, and derive PHASE4_MATRIX solely from the owned section. |
| T-04-55 | Repudiation | interpreter/full-collection evidence | high | mitigate | Run the exact owned matrix on both interpreters and use the disjoint deferred set solely for exact absent-pandas diagnostic classification. |
| T-04-56 | Elevation of privilege | unsupported layout | high | mitigate | Preserve typed, non-mutating migration-required failure and explicit offline migration/rebuild boundaries. |

High-severity findings block completion. Package legitimacy is not applicable because this plan adds no dependency.
</threat_model>

<verification>
Run each task command, then inspect the aggregate verifier output. The owned and deferred sections must parse as exact disjoint sets after the documentation update; PHASE4_MATRIX must equal only the owned set; every owned path must execute on both interpreters; no stable-path test may be deleted or filtered; and only the exact absent-pandas SQL-cache diagnostic may remain non-green. Mocked S3 factory coverage remains Phase 4 evidence only and does not satisfy Phase 5 live-service/topology qualification.
</verification>

<success_criteria>
- Remaining query, stored-format, and catalog-example consumers use current public seams without restored APIs.
- The executable consumer tree contains no retired metadata/blob selector use outside non-executable absence strings.
- 04-VALIDATION.md exposes exact, marker-bounded, disjoint owned and deferred path sets.
- Python 3.11 and 3.13 run only the owned matrix, including configuration, local-workflow, RoleRegistry, and mocked-S3 stable paths.
- Deferred pandas-dependent SQL-cache paths are used only for full-tree diagnostic classification and never become owned matrix membership.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-13-SUMMARY.md` when done.
</output>
