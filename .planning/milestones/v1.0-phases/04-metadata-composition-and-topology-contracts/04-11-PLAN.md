---
phase: 04-metadata-composition-and-topology-contracts
plan: "11"
type: execute
wave: 10
depends_on: [04-10]
files_modified:
  - src/cacheness/storage/projections.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/catalog.py
  - tests/test_catalog_projection.py
  - tests/test_catalog_query_contract.py
autonomous: true
gap_closure: true
requirements: [BACK-02, BACK-06, BACK-07]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every ordinary external projection Exception becomes a named dirty outcome or an explicit committed-partial error carrying the exact immutable BlobReceipt; BaseException subclasses still propagate (CR-04; D-13 through D-15)."
    - "A named projection rebuild is qualified by that sink's own capabilities, while the aggregate topology report remains a truthful whole-topology minimum (WR-01; BACK-06; D-16)."
    - "Catalog cursor encoded bytes, decoded bytes, and every string field are bounded before expensive work; rejected cursors never reach authority catalog dispatch (CR-05; D-06, D-08)."
  artifacts:
    - path: src/cacheness/storage/projections.py
      provides: "Exception-safe derived boundary and per-sink rebuild qualification"
    - path: src/cacheness/storage/catalog.py
      provides: "Finite encoded/decoded/field cursor limits enforced before authority dispatch"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/projections.py
      via: "post-commit delivery passes the exact receipt and each selected sink's ParticipantCapabilities"
      pattern: "BlobReceipt|ParticipantCapabilities"
    - from: src/cacheness/storage/catalog.py
      to: src/cacheness/storage/blob_store.py
      via: "CatalogCursor.inspect validates all bounds before query_catalog asks LifecycleAuthority for a page"
      pattern: "inspect|catalog_page"
  prohibitions:
    - "No new lock, queue, scheduler, readiness registry/event, lease, sidecar, lifecycle coordinator, second authority, or timing-derived correctness rule."
    - "No universal-success, cross-resource ACID, or multi-host guarantee beyond ADR 0001."
    - "No compatibility shim, old metadata-authority export, dual registry, or implicit migration during normal open."
    - "No projection may authorize lifecycle, cleanup, repair, or canonical query completeness."
    - "Explicit schema/format version detection and offline migration/rebuild tooling remain supported."
---

<objective>
Harden the post-commit projection and public cursor boundaries.

Purpose: Prevent a successful canonical commit from appearing wholly failed and reject hostile cursor sizes before authority dispatch.
Output: Exact receipt-preserving projection outcomes, per-sink rebuild checks, and bounded cursors.
</objective>

## Artifacts this phase produces

- A fail-safe derived projection boundary that never changes canonical authority state and never loses an ordinary committed result.
- A finite opaque cursor encoding/inspection contract enforced before authority work.
- Production query/projection seams ready for the downstream consumer and interpreter matrix in Plan 04-12.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-PATTERNS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-REVIEW.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VERIFICATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-10-SUMMARY.md
</context>

<source_coverage>
| Source | Items covered |
|---|---|
| GOAL | Preserve committed catalog receipts, advertise only topology-supported projection guarantees, and keep bounded canonical queries authoritative. |
| REQ | BACK-02 projection-only roles; BACK-06 truthful per-participant/aggregate capabilities; BACK-07 safe canonical catalog query and derived failure behavior. |
| RESEARCH | Best-effort vs explicit committed-partial outcomes; isolated capability-qualified rebuild; authenticated revision-bound bounded cursors. |
| CONTEXT | D-05 through D-08 and D-13 through D-18. Plan 04-12 owns current-surface consumer evidence; Phase 8 owns pandas/SQL-cache installation qualification; Phase 5 service topologies and Phase 6 cache-policy redesign remain excluded. |
</source_coverage>

## Multi-Source Coverage Audit

| Source item | Status | Gap-plan coverage |
|---|---|---|
| GOAL-1: explicit authority/projection roles and narrowed local adapter | COVERED | 04-10 Tasks 1-2; 04-11 Task 1 |
| GOAL-2: exact injected and registered selection through one construction path | COVERED | 04-09 Task 2; 04-10 Task 1 |
| GOAL-3: truthful active-pair capabilities | COVERED | 04-09 Task 2; 04-11 Task 1 |
| GOAL-4: construction-time rejection of invalid participants/claims | COVERED | 04-09 Task 2; 04-10 Task 2 |
| GOAL-5: public define/validate/write/update/reopen/query catalog workflow | COVERED | 04-09 Task 1 |
| GOAL-6: canonical query and derived-index failure/rebuild contract | COVERED | 04-11 Tasks 1-2 |
| REQ BACK-02 | COVERED | 04-10 Tasks 1-2; 04-11 Task 1 |
| REQ BACK-03 | COVERED | 04-09 Task 2; 04-10 Task 1 |
| REQ BACK-06 | COVERED | 04-09 Task 2; 04-10 Task 2; 04-11 Task 1 |
| REQ BACK-07 | COVERED | 04-09 Task 1; 04-11 Tasks 1-2 |
| RESEARCH: catalog validation/presence/versioning joins the existing authority visibility switch | COVERED | 04-09 Task 1 |
| RESEARCH: selected payload supplies primitives without lifecycle sequencing | COVERED | 04-09 Task 2 |
| RESEARCH: one role registry, structural validation, exact identity, explicit ownership | COVERED | 04-10 Tasks 1-2 |
| RESEARCH: projections preserve commits, checkpoint explicitly, and rebuild per capability | COVERED | 04-11 Task 1 |
| RESEARCH: opaque portable cursor remains authenticated, revision-bound, and finite | COVERED | 04-11 Task 2 |
| RESEARCH: approved pre-production cutover removes obsolete APIs and migrates consumers while retaining migration evidence | COVERED | 04-10 Tasks 1-2; 04-12 Tasks 1-3 |
| CONTEXT D-01 through D-04 | COVERED | 04-09 Task 1 |
| CONTEXT D-05 through D-08 | COVERED | 04-09 Task 1; 04-11 Task 2 |
| CONTEXT D-09 through D-12 | COVERED | 04-09 Task 2; 04-10 Tasks 1-2 |
| CONTEXT D-13 through D-16 | COVERED | 04-10 Task 2; 04-11 Task 1 |
| CONTEXT D-17 through D-18 | COVERED | 04-09 through 04-12; Plan 04-12 owns exhaustive consumer and interpreter evidence |

No GOAL, REQ, RESEARCH, or CONTEXT item in the Phase 4 gap boundary is missing. Deferred Phase 5 PostgreSQL/S3 topology delivery, Phase 6 UnifiedCache policy coverage, Phase 7 migration execution, and Phase 8 pandas/SQL-cache packaging qualification are exclusions, not gaps. Plan 04-12 is an in-phase downstream cutover plan, not a deferral.

## Verification Gap and Review Finding Coverage

| Grouped gap / finding | Plan task |
|---|---|
| Public catalog write/update/query/reopen; pre-staging validation; exact presence; replace private seeding (Gap 1, CR-01) | 04-09 Task 1 |
| Selected filesystem participant performs real guarded I/O; unsupported payload rejected (Gap 2, CR-02) | 04-09 Task 2 |
| One reachable application RoleRegistry; remove legacy registry/exports (Gap 3, CR-03) | 04-10 Task 1; 04-12 Task 1 migrates registry consumers |
| Structural protocols and identity-deduplicated close-once unwind (Gap 4, CR-06) | 04-10 Task 2 |
| Exception-not-BaseException projection boundary, exact receipt, per-sink rebuild (Gap 5, CR-04, WR-01) | 04-11 Task 1 |
| Encoded/decoded/field cursor bounds before authority dispatch (Gap 6, CR-05) | 04-11 Task 2 |
| Stale cutover/regression tests, exhaustive removed-symbol consumers, and honest Python 3.11/3.13 Phase 4 evidence (Gap 7, WR-02) | 04-12 Tasks 1-3 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve exact committed receipts across every ordinary projection failure</name>
  <reversibility rating="costly">D-15 is a public failure/result contract that callers use to avoid unsafe retries after commit.</reversibility>
  <files>src/cacheness/storage/projections.py, src/cacheness/storage/blob_store.py, tests/test_catalog_projection.py</files>
  <read_first>
    <file>src/cacheness/storage/projections.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/read_contract.py</file>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/test_catalog_projection.py</file>
  </read_first>
  <behavior>
    - "best_effort catches every Exception from external apply/checkpoint/rebuild-related sink code and returns a named DIRTY ProjectionOutcome attached to the already frozen receipt."
    - "Explicit refresh catches every Exception and raises CacheBlobCommittedPartialError whose receipt is the exact object/value committed by BlobStore and whose projection name/cursor identify remaining derived work."
    - "KeyboardInterrupt, SystemExit, GeneratorExit, and custom BaseException subclasses are not translated by the projection boundary."
    - "Each ProjectionController receives ParticipantCapabilities.from_participant for its own sink; rebuilding a capable named sink is not disabled by another incapable sink, and an incapable named sink still fails qualification."
  </behavior>
  <action>Remove the hand-maintained projection exception tuple as the external-boundary classifier. Catch Exception, never BaseException, around best-effort pull/apply/checkpoint and explicit refresh; translate best effort to a named dirty outcome and explicit refresh to CacheBlobCommittedPartialError while preserving the exact immutable BlobReceipt, projection attribution, and last safe remaining cursor. Do not retry automatically, roll back the authority commit, delete payloads, change the receipt generation, or consult projection state for canonical membership.

In BlobStore._create_projection_controllers, continue exposing the aggregate TopologyCapabilities for whole-topology inspection, but pass each controller `ParticipantCapabilities.from_participant(projection, BackendRole.PROJECTION.value)`. Ensure isolated rebuild propagation/discard catches ordinary Exception and leaves BaseException visible, while a named rebuild checks only the selected sink's online/offline declaration. Add custom Exception, LookupError/AttributeError-like sink failures, BaseException passthrough, exact-receipt identity/equality, two-sink mixed-capability, isolated discard, and authority-state-unchanged tests.</action>
  <acceptance_criteria>CR-04 and WR-01 close: after canonical promotion every ordinary sink failure yields the exact committed receipt as a dirty outcome or typed committed-partial error, BaseException is not swallowed, and named rebuild capability is per sink. Projections remain pull/checkpoint/rebuild consumers only; they cannot authorize lifecycle, cleanup, repair, or query completeness. No retry scheduler, queue, worker, readiness event, coordinator, or stronger success guarantee is added.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_catalog_projection.py tests/test_blob_store_composition.py -o log_cli=false</automated>
  </verify>
  <done>Derived failures are truthful post-commit outcomes and mixed projection capabilities behave independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject oversized or malformed cursors before decode, JSON, and authority dispatch</name>
  <depends_on>Task 1 leaves the projection consumer able to report bounded cursor continuation truthfully.</depends_on>
  <reversibility rating="reversible">Finite input limits can be raised compatibly; authenticated cursor meaning and revision binding remain unchanged.</reversibility>
  <files>src/cacheness/storage/catalog.py, src/cacheness/storage/blob_store.py, tests/test_catalog_query_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/catalog.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/test_catalog_query_contract.py</file>
    <file>docs/CATALOG_AND_TOPOLOGY.md</file>
  </read_first>
  <behavior>
    - "CatalogCursor.inspect rejects a non-string/empty or over-limit encoded cursor by UTF-8 byte length before base64 decoding."
    - "Strict URL-safe base64 decoding rejects malformed input, and decoded bytes are bounded before UTF-8 decoding or json.loads."
    - "Every cursor string field is non-empty and UTF-8-byte-bounded, including store/schema/query fingerprints, both last_identity values, and the fixed-format signature."
    - "Every over-limit/malformed cursor raises CatalogCursorError before LifecycleAuthority.catalog_page or canonical manifest loading is called."
  </behavior>
  <action>Define named finite constants for maximum encoded cursor bytes, decoded cursor bytes, and per-field UTF-8 bytes derived from the closed cursor record shape and existing catalog identity bounds. Apply the encoded bound before padding/base64 allocation, use strict URL-safe base64 validation, apply the decoded bound before decode-to-text/json.loads, and then validate exact record keys/types/numeric domains/string lengths/identity lengths/signature encoding before HMAC comparison. Apply matching bounds when creating a cursor so the library never emits a token its inspector rejects.

Keep the cursor opaque, deterministic, HMAC-authenticated, schema/store/query/revision-bound, and keyset ordered per D-06/D-08. Add boundary-minus-one/exact-bound/plus-one tests for encoded, decoded, each scalar string, identity key/generation, malformed base64/UTF-8/JSON/signature, and a very large token. Instrument BlobStore's selected authority to prove catalog_page and manifest loading have zero calls for all rejected inputs. Do not add offset pagination, backend-native query syntax, mutable cursor state, or a pre-query index authority.</action>
  <acceptance_criteria>CR-05 closes: hostile cursor sizes and malformed shapes fail with CatalogCursorError at the earliest finite boundary and authority dispatch is provably untouched. Valid current cursors still paginate deterministically and stale/mismatched revisions retain their typed behavior. No timing rule, secondary authority, implicit migration, or unbounded fallback path exists.</acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_catalog_query_contract.py -o log_cli=false</automated>
  </verify>
  <done>Opaque cursor work is bounded before allocation/parsing and remains bound to one canonical authority snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical commit → external projection sink | Arbitrary application/driver exceptions occur after commit and must not erase the receipt or imply rollback. |
| Public cursor string → decoder/parser | Caller-controlled size and encoding must be bounded before allocation, JSON parsing, or authority access. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-45 | Repudiation | post-commit projection | high | mitigate | Catch ordinary Exception and preserve the exact BlobReceipt plus named dirty/partial outcome. |
| T-04-46 | Denial of service | cursor inspection | high | mitigate | Enforce encoded, decoded, and per-field byte bounds before decode/JSON/authority dispatch. |
| T-04-47 | Tampering | cursor context/signature | high | mitigate | Exact-shape/type/bound validation plus HMAC, schema/store/query/revision binding, and no-dispatch tests. |
| T-04-48 | Elevation of privilege | projection rebuild | high | mitigate | Use only the named sink's declared capability and isolated publish contract; never grant authority operations. |

High-severity findings block completion. Package legitimacy is not applicable because this plan adds no dependency.
</threat_model>

<verification>
Run both task commands and the Phase 4 Ruff delta. Confirm custom Exception and BaseException tests distinguish the boundary, mixed projection capabilities remain independent, and rejected cursors show zero authority calls. Plan 04-12 performs the stable-path consumer migration and final interpreter evidence after these production seams are fixed.
</verification>

<success_criteria>
- Projection failures preserve exact committed results and per-sink rebuild qualification.
- Cursor processing is finite before decode, JSON, and authority dispatch.
- Production projection and cursor seams are ready for the exhaustive downstream cutover matrix in Plan 04-12.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-11-SUMMARY.md` when done.
</output>
