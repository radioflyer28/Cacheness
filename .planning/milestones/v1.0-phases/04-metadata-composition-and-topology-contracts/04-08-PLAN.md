---
phase: 04-metadata-composition-and-topology-contracts
plan: "08"
type: execute
wave: 7
depends_on: [04-07]
files_modified:
  - src/cacheness/core.py
  - src/cacheness/config.py
  - src/cacheness/__init__.py
  - src/cacheness/metadata.py
  - src/cacheness/custom_metadata.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/read_contract.py
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/storage/backends/__init__.py
  - src/cacheness/storage/backends/base.py
  - src/cacheness/storage/backends/postgresql_backend.py
  - .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
autonomous: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BlobStore is the only public payload+catalog lifecycle authority and exposes one StoreTopology-rooted construction path, exact injected instances, explicit ownership, capabilities, canonical query, and frozen BlobReceipt (D-08 through D-13, D-17)."
    - "The atomic cutover removes old metadata selectors/factories/registries, duplicate backend ABC, dictionary list filter, BlobEntryInfo, ManifestRepository surface, and runtime custom-metadata authority/session hooks without aliases or adapters (D-17)."
    - "UnifiedCache remains importable and its retained tests run through a narrow internal BlobStore composition; TTL, eviction, statistics, decorator, and global-cache policy changes remain Phase 6."
    - "The complete suite passes on supported Python 3.11 and 3.13, Ruff adds no findings, and release evidence makes no live PostgreSQL, S3, or Windows claim."
  artifacts:
    - path: src/cacheness/storage/blob_store.py
      provides: "Only public storage lifecycle facade rooted in StoreTopology"
    - path: src/cacheness/__init__.py
      provides: "Clean package exports with obsolete metadata authority surface removed"
    - path: .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
      provides: "Final 3.11/3.13, full-suite, Ruff, docs, and external-service qualification matrix"
  key_links:
    - from: src/cacheness/core.py
      to: src/cacheness/storage/blob_store.py
      via: "Narrow internal composition keeps UnifiedCache policy runnable while BlobStore owns lifecycle"
      pattern: "StoreTopology|BlobStore"
    - from: src/cacheness/__init__.py
      to: src/cacheness/storage/__init__.py
      via: "Both barrels export the same clean current public surface"
      pattern: "__all__"
    - from: src/cacheness/storage/backends/postgresql_backend.py
      to: src/cacheness/storage/projections.py
      via: "PostgreSQL-facing behavior is projection-only in Phase 4"
      pattern: "Projection"
  prohibitions:
    - "No runtime compatibility adapter, alias, hidden registry, legacy constructor branch, or implicit migration path may remain."
    - "No UnifiedCache policy cutover, TTL/eviction/statistics/decorator/global-cache refactor, PostgreSQL authority, S3 delivery, or cross-resource ACID claim belongs here."
    - "No full-suite exclusion or Ruff suppression may be used to manufacture the release matrix."
---

<objective>
Perform the atomic source/public cutover to the clean BlobStore composition and prove the Phase 4 release matrix.

Purpose: Delete every obsolete source and export path only after all consumers are prepared, while preserving runnable policy-above-storage behavior.
Output: One current public API/storage format, clean package imports, consolidated projections, and complete supported-runtime validation evidence.
</objective>

## Artifacts this plan produces

- Atomic source/export/direct-consumer cutover with obsolete files and symbols deleted or consolidated.
- Final catalog/topology/initialization documentation audit and Python 3.11/3.13 release matrix.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/CATALOG_AND_TOPOLOGY.md
@docs/STORAGE_INITIALIZATION.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-07-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: composed store topology. Decision: promote. Public metadata_backend selection disappears atomically; UnifiedCache remains policy above an internally composed BlobStore until Phase 6.
</assumption_delta_decision>

<planner_assumptions>
- STORE_FORMAT_VERSION remains exactly 2 as collision avoidance for development format 1; store epoch, manifest schema, SQLite user_version, and payload format remain independently meaningful.
- Unsupported prior layouts are detected without mutation and require Phase 7 migration/rebuild tooling; this plan executes no historical migration.
- Deterministic external-service API detection is not applicable: Phase 4 adds no remote API/SDK surface. PostgreSQL is projection-only and S3 topology delivery remains Phase 5.
- Descriptor-less prohibition recall remains flagged/unverified; final evidence does not invent a descriptor.
</planner_assumptions>

<source_consumer_classification>
| Sources | Atomic disposition |
|---|---|
| core.py, config.py | Replace direct legacy selector/factory consumption with narrow internal StoreTopology/BlobStore composition; preserve all Phase 6 policy behavior. |
| cacheness/__init__.py, storage/__init__.py | Remove obsolete unconditional/conditional exports together; export only the clean supported storage/catalog surface plus unrelated preserved package APIs. |
| metadata.py, backends/__init__.py, backends/base.py, postgresql_backend.py | Delete old authority factories/registries and duplicate ABC; retain/consolidate projection implementations and optional dependency errors only. |
| blob_store.py, lifecycle.py, read_contract.py | Remove overloaded constructors, private role selectors, dictionary list filtering, and BlobEntryInfo; route all operations through StoreTopology, canonical descriptors, CatalogPage, and BlobReceipt. |
| manifest_repository.py, custom_metadata.py | Consolidate retained canonical descriptor/projection functionality into the Phase 4 modules, then delete the obsolete runtime integration files and all imports. |
</source_consumer_classification>

<requirement_decision_coverage>
| Task | Requirements | Decisions |
|---|---|---|
| 1 | BACK-02, BACK-03, BACK-06, BACK-07 | D-01 through D-18 |
| 2 | BACK-02, BACK-03, BACK-06, BACK-07 | D-01 through D-18 |
</requirement_decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically cut over source consumers and package exports</name>
  <depends_on>Plan 04-07 has prepared every known test consumer; Plans 04-02 through 04-05 provide the complete replacement contract.</depends_on>
  <reversibility rating="costly">D-17 deliberately removes a development-only public/storage surface and stored layouts require an offline rebuild or migration.</reversibility>
  <files>src/cacheness/core.py, src/cacheness/config.py, src/cacheness/__init__.py, src/cacheness/metadata.py, src/cacheness/custom_metadata.py, src/cacheness/storage/__init__.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/read_contract.py, src/cacheness/storage/manifest_repository.py, src/cacheness/storage/backends/__init__.py, src/cacheness/storage/backends/base.py, src/cacheness/storage/backends/postgresql_backend.py</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/__init__.py</file>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/custom_metadata.py</file>
    <file>src/cacheness/storage/__init__.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/read_contract.py</file>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>src/cacheness/storage/backends/__init__.py</file>
    <file>src/cacheness/storage/backends/base.py</file>
    <file>src/cacheness/storage/backends/postgresql_backend.py</file>
  </read_first>
  <behavior>
    - "Package import and direct BlobStore operations work with one StoreTopology-rooted public API."
    - "Every injected participant is exact, ownership is explicit, and close is idempotent/close-once."
    - "No obsolete source symbol or unconditional import survives, and unsupported layouts fail typed before mutation."
    - "UnifiedCache retained policy tests remain runnable through internal BlobStore composition without policy transfer."
  </behavior>
  <action>Make this one atomic source change. In BlobStore/lifecycle/read_contract, delete overloaded backend selectors, authority/projection private selectors, dictionary list filtering, BlobEntryInfo, and old result-shape branches; keep StoreTopology, CatalogPage, and frozen BlobReceipt as the only supported contract. In metadata/backends modules, remove create_metadata_backend/get_metadata_backend, metadata registry state, and the duplicate ABC while retaining only code that serves a current authority or derived projection. Consolidate canonical descriptor behavior out of manifest_repository.py and projection behavior out of custom_metadata.py, then delete those obsolete runtime files and every import. Update both package barrels and every unconditional consumer in the same commit. In core.py/config.py, wire only the narrow internal BlobStore composition needed for existing UnifiedCache tests; preserve its TTL, eviction, statistics, decorator, global-cache, and policy logic unchanged. Keep Postgres projection-only, reject unsupported layouts read-only, and add no alias or adapter.</action>
  <verify>
    <automated>uv run --frozen python -c "import cacheness; from cacheness.storage import BlobStore, StoreTopology, BlobReceipt" &amp;&amp; uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py tests/test_blob_store_composition.py tests/test_topology_capabilities.py tests/test_metadata_role_contract.py tests/test_catalog_projection.py tests/test_public_api_contract.py tests/test_config_validation.py tests/test_phase3_local_workflows.py -x &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>The package imports cleanly, canonical Phase 4 and mixed-scope tests pass, all listed source consumers are updated atomically, and no obsolete factory/registry/list/result/constructor/runtime-ORM path remains.</acceptance_criteria>
  <done>The repository exposes one clean BlobStore composition and one current format with no runtime compatibility layer.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the supported-runtime release matrix and record honest evidence</name>
  <depends_on>Task 1 atomic cutover is complete and importable.</depends_on>
  <files>.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md</file>
    <file>pyproject.toml</file>
    <file>.python-version</file>
    <file>docs/CATALOG_AND_TOPOLOGY.md</file>
    <file>docs/STORAGE_INITIALIZATION.md</file>
    <file>verify_platform.py</file>
  </read_first>
  <action>Run the complete repository suite without exclusions on Python 3.13 and Python 3.11 using the locked environment, plus the Phase 4 Ruff delta and focused canonical suites. Record exact commands/results in 04-VALIDATION.md. Audit the Plan 04-04/04-05 docs against the final exports and record format-2, independent version fields, stored-presence/default semantics, offline SQLite schema boundary, projection-only PostgreSQL/JSON, unsupported acceleration, and topology-qualified guarantees. Mark live PostgreSQL, S3, and Windows qualification as not executed; do not infer it from mocks or portable tests. Record deterministic external-service API coverage as not applicable because no such API was added.</action>
  <verify>
    <automated>uv run --frozen --python 3.13 pytest -q -o log_cli=false &amp;&amp; uv run --frozen --python 3.11 pytest -q -o log_cli=false &amp;&amp; uv run --frozen --python 3.13 python tools/verify_phase4_ruff_delta.py &amp;&amp; uv run --frozen --python 3.11 python tools/verify_phase4_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Both supported interpreter runs pass the entire suite with no exclusion, both Ruff delta runs pass, docs match the exported contract, and external-service/platform non-claims are explicit.</acceptance_criteria>
  <done>Phase 4 has reproducible release evidence for Python 3.11 and 3.13 without overstated PostgreSQL/S3/Windows validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public imports/config → composed store | Legacy names/options must not reopen hidden selection paths. |
| Existing store root → current initialization | Unsupported format/layout evidence is untrusted and must be inspected read-only. |
| UnifiedCache policy → BlobStore lifecycle | Policy may request storage work but cannot own payload/catalog lifecycle. |
| Local validation → release claims | Mocks and portable tests cannot qualify live remote services or Windows. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-30 | Elevation of privilege | legacy selectors/exports | high | mitigate | Atomic deletion across factories, barrels, direct consumers, and tests; canonical absence tests. |
| T-04-31 | Tampering | unsupported layout handling | high | mitigate | Typed pre-mutation format/identity rejection and non-mutation regressions. |
| T-04-32 | Tampering | custom/ORM projection | high | mitigate | Consolidate into projection-only pull/checkpoint contracts with no authority writes. |
| T-04-33 | Repudiation | release matrix | medium | mitigate | Record interpreter, command, result, and explicit unexecuted environments. |
| T-04-34 | Denial of service | package/full-suite gate | low | accept | Local bounded validation cost is appropriate for release gating. |

Package legitimacy: not applicable; no dependency installation or new package is planned.
</threat_model>

<verification>
Run both task commands, then confirm no plan-owned test is skipped or excluded and the final validation record contains exact 3.11/3.13 results plus PostgreSQL/S3/Windows non-claims.
</verification>

<success_criteria>
- BlobStore is the sole public lifecycle authority and StoreTopology the sole composition root.
- Obsolete selectors, factories, registries, shapes, filters, duplicate ABC, and runtime integrations are absent without shims.
- All classified tests pass and mixed-scope policy/public coverage remains intact.
- Full suites and Ruff delta pass on Python 3.11 and 3.13.
- Final docs/evidence state exact topology limits and offline migration boundary.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-08-SUMMARY.md` when done.
</output>
