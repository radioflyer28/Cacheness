---
phase: 04-metadata-composition-and-topology-contracts
plan: "06"
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - tests/test_blob_manifest.py
  - tests/test_blob_manifest_backends.py
  - tests/test_blob_store_atomic_lifecycle.py
  - tests/test_blob_store_close_contract.py
  - tests/test_blob_store_concurrency.py
  - tests/test_blob_store_integrity.py
  - tests/test_blob_store_legacy_contract.py
  - tests/test_blob_store_read_contract.py
  - tests/test_blob_store_reconciliation.py
  - tests/test_clear_recovery.py
  - tests/test_filesystem_containment.py
  - tests/test_manifest_repository_cas.py
  - tests/test_phase3_windows_contract.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct BlobStore lifecycle, recovery, integrity, containment, concurrency, and close invariants run through StoreTopology and the frozen BlobReceipt contract (D-08 through D-12, D-18)."
    - "Mixed legacy suites lose only obsolete constructor, BlobEntryInfo, list-filter, and repository-shape assertions; retained security and lifecycle cases remain executable."
    - "Memory assertions remain same-process and SQLite/local contention accepts only success, exact conflict, or typed retryable timeout while preserving authority/payload agreement."
  artifacts:
    - path: tests/test_blob_store_atomic_lifecycle.py
      provides: "StoreTopology-based atomic promotion/rollback regressions"
    - path: tests/test_blob_store_integrity.py
      provides: "Canonical descriptor signature and payload-integrity regressions"
    - path: tests/test_filesystem_containment.py
      provides: "Path containment regressions preserved across composition reset"
  key_links:
    - from: tests/test_blob_store_atomic_lifecycle.py
      to: tests/test_blob_manifest.py
      via: "Both assert one canonical signed descriptor and frozen receipt for the committed generation"
      pattern: "StoreTopology|BlobReceipt"
    - from: tests/test_blob_store_concurrency.py
      to: docs/adr/0001-topology-specific-storage-guarantees.md
      via: "Topology-qualified progress assertions preserve the ADR safety boundary"
      pattern: "Conflict|Retryable"
  prohibitions:
    - "Do not preserve a test-only constructor or result-shape adapter."
    - "Do not weaken containment, signature, rollback, reconciliation, close-once, or same-key integrity assertions while removing obsolete cases."
    - "Do not add a coordinator, queue, lock, or cross-resource atomicity claim outside BlobStore's existing lifecycle authority."
---

<objective>
Rewrite the first bounded group of direct BlobStore regression consumers onto the composed-store contract.

Purpose: Preserve Phase 1–3 safety evidence while retiring development-only API and shape assumptions before the public cutover.
Output: Thirteen focused lifecycle/security test files expressed through StoreTopology and BlobReceipt.
</objective>

## Artifacts this plan produces

- StoreTopology-based manifest, lifecycle, recovery, close, concurrency, integrity, containment, and Windows-behavior regressions.
- Selective retirement of obsolete constructor/list/result/repository-shape cases without deleting mixed-scope suites.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-05-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: composed store topology. Decision: promote. Every retained direct BlobStore regression constructs one StoreTopology; none selects a metadata_backend.
</assumption_delta_decision>

<consumer_classification>
| Files | Executable disposition |
|---|---|
| test_blob_manifest.py, test_blob_manifest_backends.py, test_blob_store_atomic_lifecycle.py, test_blob_store_close_contract.py, test_blob_store_read_contract.py, test_blob_store_reconciliation.py, test_clear_recovery.py, test_manifest_repository_cas.py | Rewrite retained lifecycle/manifest/CAS/recovery assertions to StoreTopology and canonical signed descriptors; remove only old constructor, repository-internal, list-filter, and BlobEntryInfo cases. |
| test_blob_store_concurrency.py, test_blob_store_integrity.py, test_filesystem_containment.py, test_phase3_windows_contract.py | Rewrite retained concurrency/security/platform invariants to StoreTopology; keep topology-qualified outcomes and portable Windows contract without claiming a live Windows run. |
| test_blob_store_legacy_contract.py | Remove obsolete compatibility assertions and replace the retained non-mutation/security cases with explicit rejection of unsupported layouts and removed shapes. |
</consumer_classification>

<requirement_decision_coverage>
| Task | Requirements | Decisions |
|---|---|---|
| 1 | BACK-02, BACK-03, BACK-07 | D-02, D-03, D-08 through D-12, D-17, D-18 |
| 2 | BACK-02, BACK-03, BACK-06, BACK-07 | D-08 through D-12, D-17, D-18 |
</requirement_decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite retained manifest, atomic lifecycle, close, read, and recovery regressions</name>
  <depends_on>Plan 04-05 completes the canonical descriptor, receipt, topology, and projection behavior consumed here.</depends_on>
  <files>tests/test_blob_manifest.py, tests/test_blob_manifest_backends.py, tests/test_blob_store_atomic_lifecycle.py, tests/test_blob_store_close_contract.py, tests/test_blob_store_read_contract.py, tests/test_blob_store_reconciliation.py, tests/test_clear_recovery.py, tests/test_manifest_repository_cas.py</files>
  <read_first>
    <file>tests/_lifecycle_test_support.py</file>
    <file>tests/test_blob_manifest.py</file>
    <file>tests/test_blob_manifest_backends.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
    <file>tests/test_blob_store_close_contract.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
    <file>tests/test_clear_recovery.py</file>
    <file>tests/test_manifest_repository_cas.py</file>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
  </read_first>
  <behavior>
    - "StoreTopology construction preserves atomic promotion, rollback, recovery, close-once, stale-writer, and canonical read outcomes."
    - "Manifest expectations inspect the canonical signed descriptor and frozen BlobReceipt instead of repository internals or BlobEntryInfo."
    - "Unsupported prior layouts are rejected read-only with migration/rebuild-required evidence."
  </behavior>
  <action>Convert every retained direct-store fixture and call in these eight files to StoreTopology, exact role injection, and the frozen BlobReceipt. Preserve atomic promotion, rollback, close ownership, canonical read, reconciliation, clear-recovery, manifest authenticity, and same-generation CAS assertions. In mixed files, delete only cases whose sole purpose is the overloaded backend selector, metadata factory/getter, dictionary list filter, BlobEntryInfo equality, or ManifestRepository implementation shape; replace security-relevant legacy-layout coverage with typed read-only rejection per D-17/D-18. Do not delete any file wholesale.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_blob_manifest.py tests/test_blob_manifest_backends.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_close_contract.py tests/test_blob_store_read_contract.py tests/test_blob_store_reconciliation.py tests/test_clear_recovery.py tests/test_manifest_repository_cas.py -x &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>All retained lifecycle cases pass through StoreTopology, obsolete assertions alone are gone, and unsupported layouts remain unchanged on failure.</acceptance_criteria>
  <done>The main direct BlobStore lifecycle regression group is expressed only in current Phase 4 terms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite retained concurrency, integrity, containment, and platform regressions</name>
  <depends_on>Task 1 establishes the shared direct-store construction pattern.</depends_on>
  <files>tests/test_blob_store_concurrency.py, tests/test_blob_store_integrity.py, tests/test_blob_store_legacy_contract.py, tests/test_filesystem_containment.py, tests/test_phase3_windows_contract.py</files>
  <read_first>
    <file>tests/_lifecycle_test_support.py</file>
    <file>tests/test_blob_store_concurrency.py</file>
    <file>tests/test_blob_store_integrity.py</file>
    <file>tests/test_blob_store_legacy_contract.py</file>
    <file>tests/test_filesystem_containment.py</file>
    <file>tests/test_phase3_windows_contract.py</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
  </read_first>
  <behavior>
    - "Same-key contention cannot expose metadata/payload disagreement or a partially promoted descriptor."
    - "Signature, descriptor, cursor, and path-containment failures remain fail-closed and non-mutating."
    - "Portable Windows-path assertions remain executable without reporting live Windows qualification."
  </behavior>
  <action>Rewrite direct-store construction in these five suites to StoreTopology and exact injected participants. Preserve signature/integrity, traversal/symlink containment, concurrency, cleanup debt, and platform-neutral path behavior. Qualify memory as same-process and SQLite/local contention as success, exact conflict, or typed retryable timeout; assert integrity rather than every-contender success. In the legacy-contract suite, retire only compatibility behavior and keep explicit unsupported-layout, untrusted-path, and non-mutation regressions. Keep the Windows contract as portable logic evidence and label live Windows validation unverified.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_blob_store_concurrency.py tests/test_blob_store_integrity.py tests/test_blob_store_legacy_contract.py tests/test_filesystem_containment.py tests/test_phase3_windows_contract.py -x &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Security and concurrency invariants pass with topology-qualified outcomes; no compatibility shim, timing-only oracle, or live-Windows claim is introduced.</acceptance_criteria>
  <done>The second retained direct-store regression group is current-contract-only and security-equivalent or stronger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy fixtures → current topology | Old fixtures can accidentally preserve removed constructors or weaken role identity. |
| Filesystem/descriptor bytes → BlobStore read | Paths, signatures, and generations may be hostile or stale. |
| Concurrent callers → one authority transition | Contention must preserve safety without overstating progress. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-22 | Tampering | retained manifest/integrity tests | high | mitigate | Preserve signature, canonical descriptor, rollback, and read-only rejection cases. |
| T-04-23 | Elevation of privilege | topology fixtures | high | mitigate | Exact role injection and no projection-as-authority fixture. |
| T-04-24 | Denial of service | concurrency tests | medium | mitigate | Bounded deterministic barriers and ADR-qualified retry/conflict outcomes. |
| T-04-25 | Information disclosure | containment tests | high | mitigate | Preserve traversal, symlink, and authority-record path validation. |

Package legitimacy: not applicable; this plan changes tests only and adds no dependency.
</threat_model>

<verification>
Run both task commands. Full-suite execution is intentionally deferred to Plan 04-08 because legacy public sources and remaining consumers are still present; no skip, ignore, or compatibility shim is added.
</verification>

<success_criteria>
- All thirteen files have the classification above implemented without wholesale deletion.
- Retained lifecycle/security regressions pass through StoreTopology.
- Obsolete API/shape assertions are removed rather than translated into aliases.
- Topology-specific concurrency and platform evidence remains honest.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-06-SUMMARY.md` when done.
</output>
