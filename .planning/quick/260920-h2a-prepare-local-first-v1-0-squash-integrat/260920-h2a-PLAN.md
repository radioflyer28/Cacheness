---
quick_id: 260920-h2a
status: complete
scope: git-ref-only integration preparation
---

# Local-first v1.0 squash integration preparation

## Goal

Prepare, but do not merge or push, a one-commit branch based on the verified
GitHub `main` tip whose tree exactly matches the audited local `v1.0` tag.
Preserve both histories and report the remote-only material that this snapshot
would replace. Do not change product source, the v1.0 tag, or either `main` ref.

## Tasks

1. Verify the current local tag, fetched remote tip, working-tree status, and
   unoccupied branch names; capture the merge base and divergence.
2. Create local preservation refs for both tips and a single-parent snapshot
   commit on a `codex/` integration branch. Verify its parent equals the remote
   tip and its tree equals `v1.0^{tree}` exactly.
3. Review the remote-only file/feature inventory, package-version mismatch,
   release-workflow implications, and exact-SHA requalification obligations.
   Write a concise report and stop before any merge or push.

## Verification

- `git rev-parse` proves branch parent and tree equality.
- `git status --short` shows only pre-existing `.claude/` plus this task's
  planning artifacts; no source file is changed.
- No remote ref, `main` ref, or `v1.0` tag moves.
