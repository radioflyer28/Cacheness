---
phase: 29-ttl-eviction-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/core.py
  - src/cacheness/metadata/json_backend.py
  - src/cacheness/metadata/sqlite_backend.py
  - src/cacheness/storage/backends/postgresql_backend.py
  - tests/test_core.py
  - tests/test_metadata.py
  - tests/test_backend_parity.py
  - tests/test_storage_mode.py
  - tests/test_postgresql_backend.py
autonomous: true
requirements:
  - TTL-01
requirements_addressed:
  - TTL-01
user_setup: []
must_haves:
  truths:
    - "TTL-01: cache-mode reads honor stored expires_at before caller or config TTL."
    - "TTL-01: JSON, SQLite, and PostgreSQL cleanup delete rows with past stored expires_at."
    - "TTL-01: fallback TTL applies only when an entry has no stored expires_at."
    - "TTL-01: storage-mode reads do not expire data because of stored expires_at."
  artifacts:
    - path: "src/cacheness/core.py"
      provides: "Stored expires_at precedence in UnifiedCache._is_expired and public cleanup scan"
      symbols: ["UnifiedCache._is_expired", "UnifiedCache.cleanup_expired"]
    - path: "src/cacheness/metadata/json_backend.py"
      provides: "JSON cleanup predicate using stored expires_at or created_at fallback"
      symbols: ["JsonBackend.cleanup_expired"]
    - path: "src/cacheness/metadata/sqlite_backend.py"
      provides: "SQLite cleanup predicate using stored expires_at or created_at fallback"
      symbols: ["SqliteBackend.cleanup_expired"]
    - path: "src/cacheness/storage/backends/postgresql_backend.py"
      provides: "PostgreSQL cleanup predicate using stored expires_at or created_at fallback"
      symbols: ["PostgresBackend.cleanup_expired"]
  key_links:
    - from: "src/cacheness/core.py:UnifiedCache.cleanup_expired"
      to: "metadata_backend.cleanup_expired"
      via: "same stored-expiry predicate family"
      pattern: "expires_at"
    - from: "tests/test_storage_mode.py"
      to: "src/cacheness/core.py:UnifiedCache.get"
      via: "storage_mode returns _storage_mode_get before _is_expired"
      pattern: "_storage_mode_get"
---

<objective>
Implement TASK-5 / R5 for TTL-01: stored `expires_at` is the authoritative expiry field when present, and caller or config TTL is only a fallback when an entry has no stored expiry.

Purpose: Make per-entry TTL coherent across cache-mode reads, public cleanup, JSON metadata cleanup, SQLite metadata cleanup, PostgreSQL metadata cleanup, and storage-mode guards.
Output: Verify-first regressions plus source changes that honor `expires_at` end-to-end per D-01 through D-06 and keep Phase 29 scoped per D-24.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-ttl-eviction-consistency/29-CONTEXT.md
@.planning/phases/29-ttl-eviction-consistency/29-RESEARCH.md
@.planning/phases/29-ttl-eviction-consistency/29-VALIDATION.md
@.planning/phases/29-ttl-eviction-consistency/29-PATTERNS.md
@docs/CODE_REVIEW_ACTIONS.md
@docs/CODE_REVIEW_FINDINGS.md
@.planning/codebase/TESTING.md
@src/cacheness/core.py
@src/cacheness/metadata/json_backend.py
@src/cacheness/metadata/sqlite_backend.py
@src/cacheness/storage/backends/postgresql_backend.py
@tests/test_core.py
@tests/test_metadata.py
@tests/test_backend_parity.py
@tests/test_storage_mode.py
@tests/test_postgresql_backend.py

<interfaces>
Existing symbols to use and modify:
- `UnifiedCache._is_expired(cache_key: str, ttl_seconds=_DEFAULT_TTL) -> bool`
- `UnifiedCache.cleanup_expired(ttl_seconds: Optional[float] = None) -> int`
- `JsonBackend.cleanup_expired(ttl_seconds: float) -> int`
- `SqliteBackend.cleanup_expired(ttl_seconds: float) -> int`
- `PostgresBackend.cleanup_expired(ttl_seconds: float) -> int`
- `UnifiedCache.get(...)` returns through `_storage_mode_get(...)` before `_is_expired(...)` when `config.storage_mode` is true.
</interfaces>
</context>

## Artifacts This Phase Produces

- Materially changes `UnifiedCache._is_expired` in `src/cacheness/core.py`.
- Materially changes `UnifiedCache.cleanup_expired` in `src/cacheness/core.py`.
- Materially changes `JsonBackend.cleanup_expired` in `src/cacheness/metadata/json_backend.py`.
- Materially changes `SqliteBackend.cleanup_expired` in `src/cacheness/metadata/sqlite_backend.py`.
- Materially changes `PostgresBackend.cleanup_expired` in `src/cacheness/storage/backends/postgresql_backend.py`.
- Adds stored-expiry precedence tests in `tests/test_core.py`, `tests/test_metadata.py`, `tests/test_backend_parity.py`, `tests/test_storage_mode.py`, and `tests/test_postgresql_backend.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TTL-01 verify-first stored-expiry regressions</name>
  <files>tests/test_core.py, tests/test_metadata.py, tests/test_backend_parity.py, tests/test_storage_mode.py, tests/test_postgresql_backend.py</files>
  <read_first>
    @.planning/phases/29-ttl-eviction-consistency/29-VALIDATION.md
    @.planning/phases/29-ttl-eviction-consistency/29-CONTEXT.md
    @.planning/phases/29-ttl-eviction-consistency/29-PATTERNS.md
    @docs/CODE_REVIEW_ACTIONS.md
    @tests/test_core.py
    @tests/test_metadata.py
    @tests/test_backend_parity.py
    @tests/test_storage_mode.py
    @tests/test_postgresql_backend.py
  </read_first>
  <behavior>
    - RED case 1: an entry with past stored `expires_at` and recent `created_at` is expired by `_is_expired()` and deleted by cleanup even when the fallback TTL is large.
    - RED case 2: an entry with future stored `expires_at` and old `created_at` survives cleanup even when the fallback TTL is small.
    - RED case 3: JSON and SQLite cleanup parity proves the same stored-expiry precedence.
    - RED case 4: PostgreSQL cleanup uses the same stored-expiry precedence through existing Docker-skipping PostgreSQL fixtures.
    - RED case 5: storage mode returns data for an entry whose stored `expires_at` is in the past, proving D-06.
  </behavior>
  <action>Add verify-first tests for TASK-5 / TTL-01. Place core read-expiry tests near existing TTL tests in `tests/test_core.py`; add JSON/SQLite cleanup coverage in `tests/test_metadata.py` and/or the existing `TestBackendParity.test_cleanup_expired_parity` area in `tests/test_backend_parity.py`; add an explicit PostgreSQL stored-expiry cleanup regression in `tests/test_postgresql_backend.py` using the existing backend fixture and Docker-skip behavior; add the storage-mode guard case under `TestNoTTLExpiration` in `tests/test_storage_mode.py`. Use deterministic datetimes instead of sleeps where possible. Cite D-01, D-02, D-03, D-04, D-05, D-06, D-26, and D-27 in test comments only if a short comment clarifies the regression. PostgreSQL tests may skip only via existing service-availability fixture behavior; the planned regression must still exist in the file and assert both past-`expires_at` deletion and future-`expires_at` survival when PostgreSQL is available.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - New TTL-01 tests fail before implementation for the current bug and pass after Task 2.
    - Tests assert both directions of precedence: past `expires_at` wins over recent `created_at`, and future `expires_at` wins over old `created_at`.
    - `tests/test_postgresql_backend.py` contains an explicit PostgreSQL stored-expiry cleanup regression that uses existing Docker-skip behavior rather than being omitted from the plan.
    - Storage-mode test asserts `get()` returns stored data even when metadata contains past `expires_at`.
    - Signing or integrity coverage uses existing cache signing/integrity test files only.
  </acceptance_criteria>
  <done>Wave 0 TTL-01 validation items are represented by deterministic tests in existing test files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stored expires_at precedence across read and cleanup paths</name>
  <files>src/cacheness/core.py, src/cacheness/metadata/json_backend.py, src/cacheness/metadata/sqlite_backend.py, src/cacheness/storage/backends/postgresql_backend.py</files>
  <read_first>
    @.planning/phases/29-ttl-eviction-consistency/29-CONTEXT.md
    @.planning/phases/29-ttl-eviction-consistency/29-RESEARCH.md
    @.planning/phases/29-ttl-eviction-consistency/29-PATTERNS.md
    @docs/CODE_REVIEW_ACTIONS.md
    @src/cacheness/core.py
    @src/cacheness/metadata/json_backend.py
    @src/cacheness/metadata/sqlite_backend.py
    @src/cacheness/storage/backends/postgresql_backend.py
  </read_first>
  <action>Update `_is_expired()` per D-01, D-02, and D-03: after fetching the entry, inspect `entry.get("expires_at")` before the `ttl_seconds is None` early return; parse ISO strings with `datetime.fromisoformat`; treat naive datetimes as UTC; compare to `datetime.now(timezone.utc)`; ignore the caller/config TTL when stored expiry exists. Keep the existing storage-mode guard in `UnifiedCache.get()` untouched per D-06. Update `UnifiedCache.cleanup_expired()` and JSON/SQLite/PostgreSQL `cleanup_expired(ttl_seconds)` per D-04 and D-05 so they delete entries with non-null past `expires_at`, otherwise use `created_at < cutoff` only when `expires_at` is absent. Preserve existing zero or missing TTL no-op behavior for entries without stored expiry. Keep this plan scoped to TASK-5 per D-24 and do not include Phase 30, 31, or 32 items. After source changes, run ruff format/check and ty check on touched Python files per D-28; document unrelated baseline `ty` diagnostics in the summary rather than hiding them.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run ruff format src/cacheness/core.py src/cacheness/metadata/json_backend.py src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py</automated>
    <automated>uv run ruff check --fix src/cacheness/core.py src/cacheness/metadata/json_backend.py src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py</automated>
    <automated>uv run ruff check src/cacheness/core.py src/cacheness/metadata/json_backend.py src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py</automated>
    <automated>uv run ty check src/cacheness/core.py src/cacheness/metadata/json_backend.py src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py</automated>
  </verify>
  <acceptance_criteria>
    - `_is_expired()` returns based on stored `expires_at` when present and does not consult fallback TTL in that branch.
    - Public cleanup and backend cleanup agree on the same expired entry set for stored expiry versus fallback TTL.
    - JSON and SQLite tests pass locally; PostgreSQL has an explicit stored-expiry cleanup regression in `tests/test_postgresql_backend.py` that runs when Docker-backed fixtures are available and skips only through existing service-availability behavior.
    - Storage mode remains protected from cache expiry behavior.
  </acceptance_criteria>
  <done>TTL-01 is implemented and verified: stored `expires_at` controls reads and cleanup across the targeted backend semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata backend -> UnifiedCache read path | Stored metadata timestamps determine whether cached data is returned or treated as expired. |
| UnifiedCache cleanup -> metadata backend cleanup | Public cleanup deletes blobs and backend cleanup deletes metadata; disagreement can orphan data. |
| storage mode -> cache-mode TTL logic | Storage-mode data must not cross into cache expiry behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | `UnifiedCache._is_expired` | mitigate | Parse stored `expires_at` defensively and treat naive datetimes as UTC per D-02. |
| T-29-01-02 | Tampering | storage-mode reads | mitigate | Keep `UnifiedCache.get()` storage-mode return before `_is_expired()` and add regression coverage per D-06. |
| T-29-01-03 | Information Disclosure | remote URI eviction | transfer | This plan does not change size eviction; 29-04 mitigates URI blob leakage through `blob_backend.delete_blob`. |
| T-29-01-04 | Tampering | signature field `created_at` | transfer | This plan does not mutate `created_at`; 29-03 mitigates metadata-only signature invalidation. |
| T-29-SC | Tampering | package installs | accept | No package-manager installs are planned for Phase 29; the package legitimacy audit lists no packages. |
</threat_model>

<verification>
Plan command: `uv run --python 3.12 pytest tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_storage_mode.py tests/test_postgresql_backend.py -x -q --ignore=tests/test_tensorflow_handler.py`

Focused Phase 29 command after this plan if wave verification is requested: `uv run --python 3.12 pytest tests/test_core.py tests/test_metadata.py tests/test_backend_parity.py tests/test_update_operations.py tests/test_storage_mode.py tests/test_blob_store.py tests/test_cache_signing.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py`
</verification>

<success_criteria>
- Requirement `TTL-01` is covered by tests and source behavior.
- D-01 through D-06 are implemented exactly.
- D-24 through D-28 are respected: Phase 29 scope only, verify-first tests, `uv` commands, TensorFlow ignore, and touched-file quality gates.
- Decision citations covered by this plan: D-01, D-02, D-03, D-04, D-05, D-06, D-24, D-25, D-26, D-27, D-28.
</success_criteria>

<output>
Create `.planning/phases/29-ttl-eviction-consistency/29-01-SUMMARY.md` when done.
</output>
