---
phase: 31-security-storage-mode-posture
plan: 06
type: execute
wave: 5
depends_on: [31-03, 31-05, 31-07]
files_modified:
  - src/cacheness/config.py
  - src/cacheness/storage/backends/blob_backends.py
  - src/cacheness/metadata/json_backend.py
  - src/cacheness/write_intent.py
  - docs/TRANSACTION_GUARANTEES.md
  - tests/test_config_options.py
  - tests/test_config_validation.py
  - tests/test_atomic_writes.py
  - tests/test_write_intent.py
  - tests/test_blob_store.py
  - tests/test_metadata.py
autonomous: true
requirements: [STRG-02]
requirements_addressed: [STRG-02]
user_setup: []
must_haves:
  truths:
    - "STRG-02: User can understand storage-mode atomic rename and power-loss durability limits."
    - "STRG-02: User can opt into fsync_on_write without changing the default performance behavior."
    - "STRG-02: Local JSON saves, blob writes, and intent files invoke fsync hooks when feasible and enabled."
  artifacts:
    - path: "src/cacheness/config.py"
      provides: "Opt-in fsync_on_write configuration defaulting to False"
    - path: "src/cacheness/storage/backends/blob_backends.py"
      provides: "Local filesystem blob fsync support"
    - path: "src/cacheness/metadata/json_backend.py"
      provides: "JSON metadata fsync support"
    - path: "src/cacheness/write_intent.py"
      provides: "Intent-file fsync support"
    - path: "docs/TRANSACTION_GUARANTEES.md"
      provides: "Storage-mode durability contract"
  key_links:
    - from: "src/cacheness/config.py"
      to: "src/cacheness/storage/backends/blob_backends.py"
      via: "fsync_on_write policy reaches local blob writes"
    - from: "src/cacheness/config.py"
      to: "src/cacheness/metadata/json_backend.py"
      via: "fsync_on_write policy reaches JSON metadata saves"
    - from: "src/cacheness/config.py"
      to: "src/cacheness/write_intent.py"
      via: "fsync_on_write policy reaches intent files"
---

<objective>
Implement STRG-02: document the storage-mode durability contract and add an opt-in `fsync_on_write` policy where local file descriptors are available.

Purpose: make durability limits explicit while giving storage-mode users a configurable stronger local flush path without forcing a default performance change.
Output: config, local fsync wiring, docs, and tests covering D-35 through D-40.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-security-storage-mode-posture/31-CONTEXT.md
@.planning/phases/31-security-storage-mode-posture/31-VALIDATION.md
@.planning/phases/31-security-storage-mode-posture/31-RESEARCH.md
@.planning/phases/31-security-storage-mode-posture/31-PATTERNS.md
@.planning/phases/28-silent-data-loss-remediation/28-02-SUMMARY.md
@.planning/phases/28-silent-data-loss-remediation/28-03-SUMMARY.md
@src/cacheness/config.py
@src/cacheness/storage/backends/blob_backends.py
@src/cacheness/metadata/json_backend.py
@src/cacheness/write_intent.py
@docs/TRANSACTION_GUARANTEES.md
@tests/test_config_options.py
@tests/test_config_validation.py
@tests/test_atomic_writes.py
@tests/test_write_intent.py
@tests/test_blob_store.py
@tests/test_metadata.py
</context>

<scope_fence>
Do not force always-on fsync, do not claim power-loss simulation in tests, and do not change remote backend durability semantics. This plan implements D-35 through D-40 and preserves D-39 explicitly.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fsync_on_write and durability-doc regressions</name>
  <files>tests/test_config_options.py, tests/test_config_validation.py, tests/test_atomic_writes.py, tests/test_write_intent.py, tests/test_blob_store.py, tests/test_metadata.py, docs/TRANSACTION_GUARANTEES.md</files>
  <behavior>
    - D-36: docs distinguish atomic rename crash consistency from power-loss durability for JSON metadata, local blob writes, and write-intent files.
    - D-37 and D-39: `fsync_on_write` defaults to `False`.
    - D-38 and D-40: when enabled, tests monkeypatch fsync hooks or `os.fsync` to prove local JSON, local blob, and intent-file paths invoke flush logic.
  </behavior>
  <action>Add verify-first tests and doc checks for STRG-02. Tests must prove hook invocation rather than pretending to simulate real power loss. Keep external services behind existing skip gates. Use `CacheStorageConfig` as the planned user-visible config location unless the existing source has a more direct storage config seam.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_config_options.py tests/test_config_validation.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>rg -n "fsync_on_write|power-loss|atomic rename|storage mode" docs/TRANSACTION_GUARANTEES.md</automated>
  </verify>
  <done>Tests and docs define the opt-in fsync policy and durability contract before production wiring is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire opt-in local fsync and update transaction docs</name>
  <files>src/cacheness/config.py, src/cacheness/storage/backends/blob_backends.py, src/cacheness/metadata/json_backend.py, src/cacheness/write_intent.py, docs/TRANSACTION_GUARANTEES.md</files>
  <behavior>
    - `CacheStorageConfig.fsync_on_write` exists and defaults to `False`.
    - Local filesystem blob writes flush and fsync file descriptors before rename and best-effort fsync parent directories after rename when enabled.
    - JSON metadata saves and write-intent files flush local descriptors when enabled.
    - Default cache-mode and storage-mode performance paths do not call fsync unless enabled.
  </behavior>
  <action>Implement D-35, D-36, D-37, D-38, D-39, and D-40. Add `fsync_on_write: bool = False` under `CacheStorageConfig` and thread it to local file-writing components. Prefer a narrow shared helper if it prevents duplicated platform handling. On Windows or filesystems where directory fsync is unsupported, use a best-effort branch that does not fail ordinary writes solely because directory fsync is unavailable. For data-critical writes that already raise on persistence errors, do not silently hide file fsync failures when the opt-in is enabled. Update `docs/TRANSACTION_GUARANTEES.md` to describe JSON metadata, blob writes, and write-intent files, including atomic rename versus power-loss caveats.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_config_options.py tests/test_config_validation.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/config.py src/cacheness/storage/backends/blob_backends.py src/cacheness/metadata/json_backend.py src/cacheness/write_intent.py tests/test_config_options.py tests/test_config_validation.py tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/config.py src/cacheness/storage/backends/blob_backends.py src/cacheness/metadata/json_backend.py src/cacheness/write_intent.py tests/test_config_options.py tests/test_config_validation.py tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/config.py src/cacheness/storage/backends/blob_backends.py src/cacheness/metadata/json_backend.py src/cacheness/write_intent.py tests/test_config_options.py tests/test_config_validation.py tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/config.py src/cacheness/storage/backends/blob_backends.py src/cacheness/metadata/json_backend.py src/cacheness/write_intent.py tests/test_config_options.py tests/test_config_validation.py tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py</automated>
  </verify>
  <done>STRG-02 is implemented with opt-in local fsync paths and docs that accurately describe storage-mode durability limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process write completion to durable storage | A write that reaches `os.replace` is not necessarily power-loss durable without flush/fsync. |
| config to backend file descriptors | User-selected durability policy must reach each local file-writing component. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Repudiation | transaction documentation | mitigate | Docs distinguish atomic rename from power-loss durability per D-36. |
| T-31-12 | Denial of Service | fsync_on_write | mitigate | Keep default `False` per D-37 and D-39; opt-in tests cover local hook invocation. |
| T-31-SC | Tampering | package supply chain | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Plan gate: `uv run --python 3.12 pytest tests/test_config_options.py tests/test_config_validation.py tests/test_atomic_writes.py tests/test_write_intent.py tests/test_blob_store.py tests/test_metadata.py -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
STRG-02 is complete when D-35 through D-40 are implemented and verified without default fsync, external service requirements, or exaggerated durability claims.
</success_criteria>

<output>
Create `.planning/phases/31-security-storage-mode-posture/31-06-SUMMARY.md` when done.
</output>
