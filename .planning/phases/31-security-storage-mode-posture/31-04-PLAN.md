---
phase: 31-security-storage-mode-posture
plan: 04
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - src/cacheness/signing_fields.py
  - src/cacheness/_verification_mixin.py
  - src/cacheness/storage/blob_store.py
  - tests/test_cache_signing.py
  - tests/test_blob_store.py
  - tests/test_namespace_signing.py
  - tests/test_cross_system_compatibility.py
autonomous: true
requirements: [SEC-04]
requirements_addressed: [SEC-04]
user_setup: []
must_haves:
  truths:
    - "SEC-04: UnifiedCache and BlobStore sign the same canonical field set for equivalent new metadata."
    - "SEC-04: Existing old-shape BlobStore signed entries remain readable through explicit compatibility."
    - "SEC-04: Minimum signature-version enforcement does not reject compatible entries unless configured."
  artifacts:
    - path: "src/cacheness/signing_fields.py"
      provides: "Shared canonical signable-field extraction helper"
    - path: "src/cacheness/_verification_mixin.py"
      provides: "UnifiedCache verification uses shared helper"
    - path: "src/cacheness/storage/blob_store.py"
      provides: "BlobStore signing and compatibility verification use shared helper"
  key_links:
    - from: "src/cacheness/_verification_mixin.py"
      to: "src/cacheness/signing_fields.py"
      via: "extract canonical signing fields"
    - from: "src/cacheness/storage/blob_store.py"
      to: "src/cacheness/signing_fields.py"
      via: "new BlobStore writes use canonical fields"
---

<objective>
Implement SEC-04: shared canonical signing fields for UnifiedCache and BlobStore, with explicit compatibility for old BlobStore signatures.

Purpose: prevent signing drift between APIs while preserving existing signed cache entries.
Output: shared helper, call-site convergence, compatibility tests, and no strict migration requirement.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-security-storage-mode-posture/31-CONTEXT.md
@.planning/phases/31-security-storage-mode-posture/31-VALIDATION.md
@.planning/phases/31-security-storage-mode-posture/31-RESEARCH.md
@.planning/phases/31-security-storage-mode-posture/31-PATTERNS.md
@src/cacheness/_verification_mixin.py
@src/cacheness/storage/blob_store.py
@src/cacheness/interfaces.py
@tests/test_cache_signing.py
@tests/test_blob_store.py
@tests/test_namespace_signing.py
@tests/test_cross_system_compatibility.py
</context>

<scope_fence>
Do not bulk re-sign existing caches, do not require users to strict-migrate existing BlobStore entries, and do not add Phase 32 polish items. This plan implements D-23 through D-28 and respects D-46 through D-49.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical-signing parity and compatibility regressions</name>
  <files>tests/test_cache_signing.py, tests/test_blob_store.py, tests/test_namespace_signing.py, tests/test_cross_system_compatibility.py</files>
  <behavior>
    - D-25: equivalent new entries produced by UnifiedCache and BlobStore use the same canonical signable field set.
    - D-26 and D-27: old flattened BlobStore signing shapes remain readable through a named compatibility path.
    - D-28: compatibility entries are not rejected by minimum-version policy unless the user configured a stricter minimum.
  </behavior>
  <action>Add verify-first tests for SEC-04. Include one parity test that compares extracted canonical fields rather than only successful reads, one old-shape BlobStore signature compatibility test, and one minimum-version interaction test. Use existing live test files listed in this plan.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_cache_signing.py tests/test_blob_store.py tests/test_cross_system_compatibility.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <done>Tests visibly cover canonical parity, old-shape compatibility, and SEC-01 interaction for SEC-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Share canonical signing helper and wire BlobStore compatibility</name>
  <files>src/cacheness/signing_fields.py, src/cacheness/_verification_mixin.py, src/cacheness/storage/blob_store.py</files>
  <behavior>
    - `_extract_signable_fields()` logic lives in `src/cacheness/signing_fields.py` or an equivalent shared helper.
    - UnifiedCache and BlobStore new signatures call the shared helper.
    - BlobStore verification tries canonical verification first, then an explicit legacy old-shape verifier for existing entries.
  </behavior>
  <action>Implement D-23, D-24, D-25, D-26, D-27, and D-28. Move the canonical field extraction logic from `VerificationMixin._extract_signable_fields()` into `src/cacheness/signing_fields.py` and import it from both `VerificationMixin` and `BlobStore`. Preserve created-at normalization and the fixed field superset; do not sign mutable access counters or custom metadata accidentally. For existing BlobStore entries, add an explicit legacy verifier path for the old flattened signable shape. Prefer verify-only compatibility; re-sign old shapes only on explicit mutation or rotation if already natural in the touched code path.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_cache_signing.py tests/test_blob_store.py tests/test_cross_system_compatibility.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/signing_fields.py src/cacheness/_verification_mixin.py src/cacheness/storage/blob_store.py tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py tests/test_cross_system_compatibility.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/signing_fields.py src/cacheness/_verification_mixin.py src/cacheness/storage/blob_store.py tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py tests/test_cross_system_compatibility.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/signing_fields.py src/cacheness/_verification_mixin.py src/cacheness/storage/blob_store.py tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py tests/test_cross_system_compatibility.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/signing_fields.py src/cacheness/_verification_mixin.py src/cacheness/storage/blob_store.py tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py tests/test_cross_system_compatibility.py</automated>
  </verify>
  <done>UnifiedCache and BlobStore share canonical signing fields for new entries, and existing BlobStore signatures still verify through an explicit compatibility path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata normalization to signer | Divergent signable field extraction can create false accepts or false rejects across APIs. |
| legacy metadata to compatibility verifier | Existing entries may contain old flattened BlobStore signatures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering | canonical signing helper | mitigate | Use one shared helper for new UnifiedCache and BlobStore signatures per D-23 through D-25. |
| T-31-08 | Denial of Service | legacy signed entries | mitigate | Add explicit compatibility verification so existing old-shape entries remain readable per D-26 and D-27. |
| T-31-SC | Tampering | package supply chain | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Plan gate: `uv run --python 3.12 pytest tests/test_cache_signing.py tests/test_blob_store.py tests/test_namespace_signing.py tests/test_cross_system_compatibility.py -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
SEC-04 is complete when D-23 through D-28 are covered and new writes converge on a shared canonical signing field set without strict migration of existing signed entries.
</success_criteria>

<output>
Create `.planning/phases/31-security-storage-mode-posture/31-04-SUMMARY.md` when done.
</output>
