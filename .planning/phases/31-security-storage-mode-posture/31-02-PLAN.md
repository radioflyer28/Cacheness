---
phase: 31-security-storage-mode-posture
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store.py
  - tests/test_encryption_at_rest.py
  - tests/test_handler_bytes_protocol.py
autonomous: true
requirements: [SEC-02]
requirements_addressed: [SEC-02]
user_setup: []
must_haves:
  truths:
    - "SEC-02: User can read encrypted blobs through the configured blob backend abstraction."
    - "SEC-02: Encrypted reads prefer in-memory handler byte paths over plaintext temp files."
    - "SEC-02: Temp-file fallback is hardened and leaves no stale decrypted plaintext files."
  artifacts:
    - path: "src/cacheness/storage/blob_store.py"
      provides: "Backend-routed encrypted read path and bytes-first handler path"
    - path: "tests/test_blob_store.py"
      provides: "Encrypted InMemoryBlobBackend round-trip regression"
    - path: "tests/test_handler_bytes_protocol.py"
      provides: "Handler byte-path coverage"
  key_links:
    - from: "src/cacheness/storage/blob_store.py"
      to: "BlobBackend.read_blob"
      via: "ciphertext reads use backend abstraction"
    - from: "src/cacheness/storage/blob_store.py"
      to: "CacheHandler.get_bytes"
      via: "plaintext bytes are deserialized in memory before temp fallback"
---

<objective>
Implement SEC-02: encrypted BlobStore reads route through the blob backend and avoid plaintext temp files whenever handlers support byte reads.

Purpose: preserve backend abstraction for memory/S3-style storage and reduce plaintext exposure during encrypted reads.
Output: tests plus BlobStore encrypted read changes covering D-09 through D-14.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-security-storage-mode-posture/31-CONTEXT.md
@.planning/phases/31-security-storage-mode-posture/31-VALIDATION.md
@.planning/phases/31-security-storage-mode-posture/31-RESEARCH.md
@.planning/phases/31-security-storage-mode-posture/31-PATTERNS.md
@docs/CODE_REVIEW_FINDINGS.md
@docs/CODE_REVIEW_ACTIONS.md
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/backends/blob_backends.py
@src/cacheness/interfaces.py
@tests/test_blob_store.py
@tests/test_encryption_at_rest.py
@tests/test_handler_bytes_protocol.py
</context>

<scope_fence>
Do not change encryption algorithms, file-hash semantics, handler public contracts, or backend registry behavior. Preserve S3/PostgreSQL skip gates and use only `uv` commands with the TensorFlow ignore flag per D-50 and D-52. Do not implement Phase 32 items per D-46.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SEC-02 backend-routed and plaintext-temp regressions</name>
  <files>tests/test_blob_store.py, tests/test_encryption_at_rest.py, tests/test_handler_bytes_protocol.py</files>
  <behavior>
    - D-10 and D-14: encrypted `InMemoryBlobBackend` entries round-trip without direct filesystem reads.
    - D-11: handlers with `get_bytes()` deserialize decrypted plaintext in memory.
    - D-12 and D-13: handlers without `get_bytes()` fall back to a cache-directory temp file that is removed in `finally`.
  </behavior>
  <action>Add verify-first tests for encrypted memory-backed reads, byte-handler preference, and no stale decrypt temp files. Use monkeypatching or a small test handler to prove `handler.get_bytes(plaintext, metadata)` is attempted before file fallback. Keep tests in the existing live files named in 31-VALIDATION.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <done>SEC-02 regressions fail before the BlobStore change or explicitly verify already-present compliant behavior before implementation proceeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route encrypted reads through blob backends and prefer bytes</name>
  <files>src/cacheness/storage/blob_store.py</files>
  <behavior>
    - Encrypted `BlobStore.get()` and `_read_blob()` read ciphertext through `self.blob_backend.read_blob(actual_path_str)`.
    - After decrypting, the path tries `handler.get_bytes(plaintext, metadata)` before creating any temp file.
    - Temp fallback uses `tempfile.mkstemp` under the cache directory, sets POSIX mode `0600` when applicable, and unlinks in `finally`.
  </behavior>
  <action>Implement D-09, D-10, D-11, D-12, D-13, and D-14 in `src/cacheness/storage/blob_store.py`. Replace encrypted direct `Path.read_bytes()` calls with `blob_backend.read_blob(...)` using the same backend path string used for existence/read behavior. Preserve hash verification over stored ciphertext. For handlers that raise `NotImplementedError` from `get_bytes()`, use the hardened temp-file fallback in the cache directory and do not leave decrypted temp artifacts after success or failure.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/storage/blob_store.py tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/storage/blob_store.py tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/storage/blob_store.py tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/storage/blob_store.py tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py</automated>
  </verify>
  <done>Encrypted reads work through filesystem and memory-style backends, prefer byte deserialization, and leave no stale plaintext temp files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blob backend to BlobStore | Encrypted bytes may come from filesystem, memory, or S3-like backends. |
| decrypted plaintext to handler | Plaintext exists after AES-GCM decrypt and before handler deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information Disclosure | encrypted read path | mitigate | Prefer in-memory `handler.get_bytes()` and harden temp fallback per D-11 through D-13. |
| T-31-04 | Tampering | blob backend abstraction | mitigate | Use `blob_backend.read_blob()` for ciphertext reads so backend controls and URI stores are honored per D-09 and D-10. |
| T-31-SC | Tampering | package supply chain | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Plan gate: `uv run --python 3.12 pytest tests/test_blob_store.py tests/test_encryption_at_rest.py tests/test_handler_bytes_protocol.py -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
SEC-02 is complete when D-09 through D-14 are covered by tests and encrypted reads no longer require direct local path reads or avoidable plaintext temp files.
</success_criteria>

<output>
Create `.planning/phases/31-security-storage-mode-posture/31-02-SUMMARY.md` when done.
</output>
