---
phase: 31-security-storage-mode-posture
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/core.py
  - src/cacheness/_storage_mode_mixin.py
  - docs/TRANSACTION_GUARANTEES.md
  - tests/test_storage_mode.py
  - tests/test_core.py
  - tests/test_cache_integrity.py
autonomous: true
requirements: [STRG-01]
requirements_addressed: [STRG-01]
user_setup: []
must_haves:
  truths:
    - "STRG-01: Storage-mode destructive cache APIs emit a loud warning by default."
    - "STRG-01: Default behavior remains warning-first rather than hard-raise."
    - "STRG-01: Storage mode does not regain implicit TTL, eviction, or invalid-entry deletion behavior."
    - "D-47: Phase 31 does not re-open Phase 29 TTL/eviction semantics except where needed to warn for storage-mode destructive API calls."
  artifacts:
    - path: "src/cacheness/core.py"
      provides: "Storage-mode destructive API warning policy"
    - path: "tests/test_storage_mode.py"
      provides: "Warning-first and no-implicit-eviction regressions"
    - path: "docs/TRANSACTION_GUARANTEES.md"
      provides: "Storage-mode destructive API warning documentation"
  key_links:
    - from: "src/cacheness/core.py"
      to: "tests/test_storage_mode.py"
      via: "public cleanup/clear warning assertions"
    - from: "src/cacheness/core.py"
      to: "src/cacheness/_storage_mode_mixin.py"
      via: "storage-mode no-implicit-delete invariant"
---

<objective>
Implement STRG-01: storage-mode destructive cache APIs warn loudly by default without changing public API semantics to hard refusal.

Purpose: protect durable storage-mode users from accidental explicit cache-eviction methods while preserving warning-first compatibility.
Output: tests, warning helper/call sites, and docs covering D-29 through D-34.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-security-storage-mode-posture/31-CONTEXT.md
@.planning/phases/31-security-storage-mode-posture/31-VALIDATION.md
@.planning/phases/31-security-storage-mode-posture/31-RESEARCH.md
@.planning/phases/31-security-storage-mode-posture/31-PATTERNS.md
@.planning/phases/29-ttl-eviction-consistency/29-02-SUMMARY.md
@.planning/phases/29-ttl-eviction-consistency/29-04-SUMMARY.md
@docs/CODE_REVIEW_FINDINGS.md
@src/cacheness/core.py
@src/cacheness/_storage_mode_mixin.py
@docs/TRANSACTION_GUARANTEES.md
@tests/test_storage_mode.py
@tests/test_core.py
@tests/test_cache_integrity.py
</context>

<scope_fence>
Do not re-open Phase 29 TTL/eviction semantics except for explicit storage-mode warnings per D-47. Do not hard-raise by default, do not add strict refusal as default behavior, and do not re-open Phase 30 overwrite/blob enumeration behavior per D-48.

Decision citations for translation gate:
- D-47: Do not re-open Phase 29 TTL/eviction semantics except where needed to warn for storage-mode destructive API calls.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add storage-mode destructive warning regressions</name>
  <files>tests/test_storage_mode.py, tests/test_core.py, tests/test_cache_integrity.py</files>
  <behavior>
    - D-29, D-30, and D-32: explicit destructive public APIs warn in storage mode, including `cleanup_expired(ttl_seconds=...)`, `clear_all()`, namespace clearing, and size-limit cleanup paths as applicable.
    - D-31 and D-34: default behavior does not hard-raise.
    - D-33: storage mode still has no implicit TTL/eviction or invalid-entry deletion behavior.
  </behavior>
  <action>Add verify-first tests using `caplog` and/or `pytest.warns(RuntimeWarning)` for storage-mode destructive calls. The chosen warning mechanism for production is both `logger.warning` and `warnings.warn(..., RuntimeWarning, stacklevel=2)` so tests can assert an operational log and a Python-level warning. Keep implicit storage-mode behavior tests separate from explicit destructive-call tests.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_storage_mode.py tests/test_core.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_storage_mode.py tests/test_cache_integrity.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <done>Tests visibly cover warning-first behavior and confirm no implicit storage-mode eviction/deletion was reintroduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement warning-first policy for storage-mode destructive APIs</name>
  <files>src/cacheness/core.py, src/cacheness/_storage_mode_mixin.py, docs/TRANSACTION_GUARANTEES.md</files>
  <behavior>
    - Storage-mode explicit destructive methods emit a loud warning and then keep existing default behavior.
    - Warning text identifies storage mode and the durable-entry deletion risk.
    - Docs explain the warning-first policy and that hard refusal is not the default.
  </behavior>
  <action>Implement D-29, D-30, D-31, D-32, D-33, and D-34. Add a small helper in `src/cacheness/core.py` for storage-mode destructive warnings and call it from public destructive entry points: `cleanup_expired(ttl_seconds=...)`, `clear_all()`, `clear_all_namespaces()` or namespace-clearing APIs, and size-limit cleanup paths if they can be invoked on a storage-mode instance. Preserve existing no-op behavior for implicit TTL/eviction under storage mode. If an explicit strict opt-in already exists, document it as opt-in; otherwise do not add a hard-refusal default. Update docs without editing unrelated Phase 32 polish sections.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_storage_mode.py tests/test_core.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_storage_mode.py tests/test_cache_integrity.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/core.py src/cacheness/_storage_mode_mixin.py tests/test_storage_mode.py tests/test_core.py tests/test_cache_integrity.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/core.py src/cacheness/_storage_mode_mixin.py tests/test_storage_mode.py tests/test_core.py tests/test_cache_integrity.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/core.py src/cacheness/_storage_mode_mixin.py tests/test_storage_mode.py tests/test_core.py tests/test_cache_integrity.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/core.py src/cacheness/_storage_mode_mixin.py tests/test_storage_mode.py tests/test_core.py tests/test_cache_integrity.py</automated>
  </verify>
  <done>Storage-mode destructive public APIs warn loudly by default and storage-mode implicit no-delete/no-evict invariants still hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user code to storage-mode cache API | A durable storage-mode user can call cache-oriented cleanup APIs that delete entries. |
| storage-mode config to cleanup internals | Storage-mode disables implicit cache behavior but explicit APIs can still be invoked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Denial of Service | storage-mode destructive APIs | mitigate | Emit logger and RuntimeWarning signals for explicit destructive calls per D-29 through D-32. |
| T-31-10 | Tampering | storage-mode implicit cleanup | mitigate | Tests prove no implicit TTL, eviction, or invalid-entry deletion behavior returns under D-33. |
| T-31-SC | Tampering | package supply chain | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Plan gate: `uv run --python 3.12 pytest tests/test_storage_mode.py tests/test_core.py tests/test_cache_integrity.py -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
STRG-01 is complete when D-29 through D-34 are implemented, warning-first behavior is tested, and storage-mode implicit durability invariants remain intact.
</success_criteria>

<output>
Create `.planning/phases/31-security-storage-mode-posture/31-05-SUMMARY.md` when done.
</output>
