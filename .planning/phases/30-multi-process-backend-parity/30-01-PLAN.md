---
phase: 30-multi-process-backend-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/storage/backends/blob_backends.py
  - tests/test_blob_namespace.py
autonomous: true
requirements:
  - PAR-01
requirements_addressed:
  - PAR-01
user_setup: []
must_haves:
  truths:
    - "PAR-01: repeated same-blob filesystem writes use unique temp files per D-01, D-02, D-04."
    - "Failed filesystem blob writes clean only their own temp artifact and preserve the committed blob per D-03."
    - "The implementation stays scoped to filesystem blob publication and does not alter JSON metadata save semantics per D-05, D-23, D-24, D-25."
    - "All commands use uv and ignore TensorFlow handler tests on Windows per D-28."
  artifacts:
    - path: "src/cacheness/storage/backends/blob_backends.py"
      provides: "FilesystemBlobBackend.write_blob and write_blob_stream unique temp publication"
    - path: "tests/test_blob_namespace.py"
      provides: "Regressions proving tempfile.mkstemp is used, last write wins, and failed publication preserves the committed blob while removing the unique temp file"
  key_links:
    - from: "FilesystemBlobBackend.write_blob"
      to: "os.replace"
      via: "same-directory unique temp file publication"
      pattern: "tempfile.mkstemp"
    - from: "tests/test_blob_namespace.py"
      to: "FilesystemBlobBackend.write_blob"
      via: "spy on tempfile.mkstemp and repeated same blob_id writes"
      pattern: "same blob_id"
    - from: "tests/test_blob_namespace.py"
      to: "FilesystemBlobBackend.write_blob"
      via: "monkeypatch blob_backends.os.replace failure after an existing blob is committed"
      pattern: "publish failed"
---

<objective>
Implement TASK-9 / PAR-01: replace deterministic filesystem blob temp names with unique same-directory temp files so repeated or concurrent writers do not share the same temp path.

Purpose: Close code-review finding R11 without changing public API semantics or metadata backend behavior.
Output: Updated filesystem blob write paths and a regression proving unique temp creation and last-successful-write readability.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.github/copilot-instructions.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-multi-process-backend-parity/30-CONTEXT.md
@.planning/phases/30-multi-process-backend-parity/30-RESEARCH.md
@.planning/phases/30-multi-process-backend-parity/30-VALIDATION.md
@.planning/phases/30-multi-process-backend-parity/30-PATTERNS.md
@docs/CODE_REVIEW_FINDINGS.md
@docs/CODE_REVIEW_ACTIONS.md
@src/cacheness/storage/backends/blob_backends.py
@tests/test_blob_namespace.py
@tests/test_blob_store.py
</context>

## Artifacts This Phase Produces

- `src/cacheness/storage/backends/blob_backends.py`: `FilesystemBlobBackend.write_blob` uses `tempfile.mkstemp(dir=blob_path.parent, suffix=".tmp")`, writes through the returned fd, publishes with `os.replace`, and cleans only its temp path on failure per D-01 through D-03.
- `src/cacheness/storage/backends/blob_backends.py`: `FilesystemBlobBackend.write_blob_stream` uses the same unique-temp pattern so the backend has one temp-publication contract.
- `tests/test_blob_namespace.py`: regression for same `blob_id` repeated writes using distinct temp names and reading the last successful content per D-04.
- `tests/test_blob_namespace.py`: direct D-03 failure-path regression proving a failed second publish leaves the committed blob readable and removes the unique temp file.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PAR-01 verify-first regression</name>
  <files>tests/test_blob_namespace.py</files>
  <read_first>
    - .planning/phases/30-multi-process-backend-parity/30-CONTEXT.md D-01 through D-05 and D-26 through D-29.
    - .planning/phases/30-multi-process-backend-parity/30-PATTERNS.md section for tests/test_blob_namespace.py.
    - docs/CODE_REVIEW_ACTIONS.md TASK-9.
    - tests/test_blob_namespace.py namespace write/read patterns.
    - src/cacheness/storage/backends/blob_backends.py FilesystemBlobBackend.write_blob current failure cleanup path.
  </read_first>
  <behavior>
    - RED: Before the implementation, a spy on tempfile.mkstemp records zero calls because FilesystemBlobBackend still uses deterministic temp paths.
    - GREEN: Two writes to the same blob_id record two distinct temp paths ending in .tmp and the final read returns the second payload.
    - GREEN: No temp files remain under the backend namespace directory after successful writes.
    - GREEN: After an existing blob is committed, a monkeypatched publish failure on the second write raises the injected exception, leaves the old blob readable, and removes the unique temp file created for the failed write.
  </behavior>
  <action>Add focused tests in tests/test_blob_namespace.py for FilesystemBlobBackend with shard_chars=0. First, add or update a repeated-write regression that monkeypatches stdlib tempfile.mkstemp with a spy delegating to the real function, writes the same blob_id twice with different byte payloads, asserts two distinct temp names ending in .tmp, asserts backend.read_blob(final_path) returns the second payload, and asserts backend.base_dir.rglob("*.tmp") is empty. Second, add `test_filesystem_blob_backend_failed_unique_temp_write_preserves_existing_blob`: commit an initial blob, capture the returned final path and assert it reads the original bytes, monkeypatch tempfile.mkstemp with a recording spy, monkeypatch `src.cacheness.storage.backends.blob_backends.os.replace` to raise RuntimeError("publish failed") for the next write, attempt a second write to the same blob_id, assert the RuntimeError, assert backend.read_blob(first_path) still returns the original bytes and Path(first_path).exists(), assert exactly one failed-write temp path was recorded, assert that temp path no longer exists, and assert no `*.tmp` files remain under backend.base_dir. This implements D-03, D-04, and D-26 without adding a flaky process scheduler dependency. Do not use beads or bd for this GSD phase, and do not touch .planning/config.json.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_blob_namespace.py::test_filesystem_blob_backend_failed_unique_temp_write_preserves_existing_blob -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_blob_namespace.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run pytest tests/test_blob_store.py tests/test_blob_namespace.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>The repeated-write regression fails before source changes because tempfile.mkstemp is not called, and the failure-path regression fails before source changes because the patched os.replace publish failure is not exercised. After Task 2, both pass: distinct temp names are observed, final content equals the last successful write, failed publish leaves the old blob readable, and the failed-write temp file is removed.</acceptance_criteria>
  <done>PAR-01 has executable regression coverage for D-01 through D-04, including direct failed-write cleanup preservation from D-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unique temp publication in FilesystemBlobBackend</name>
  <files>src/cacheness/storage/backends/blob_backends.py, tests/test_blob_namespace.py</files>
  <read_first>
    - src/cacheness/storage/backends/blob_backends.py FilesystemBlobBackend.write_blob and write_blob_stream.
    - tests/test_blob_namespace.py test_filesystem_blob_backend_failed_unique_temp_write_preserves_existing_blob and same-blob repeated write regression.
    - .planning/phases/30-multi-process-backend-parity/30-RESEARCH.md Standard Stack and Common Pitfalls.
    - .planning/phases/28-silent-data-loss-remediation/28-01-SUMMARY.md reserved-aware cleanup pattern.
  </read_first>
  <behavior>
    - write_blob creates its temp file in blob_path.parent with tempfile.mkstemp and suffix .tmp.
    - write_blob writes bytes through the returned file descriptor, closes it, and publishes with os.replace.
    - write_blob_stream uses the same unique-temp and os.replace pattern while preserving chunked reads.
    - Exception cleanup unlinks only the unique temp path if it exists; the previously committed final blob remains untouched.
    - Direct failure-path regression proves that patching os.replace after an existing blob is committed raises the injected error, leaves the old blob readable, and removes the unique temp file for the failed write.
  </behavior>
  <action>Update src/cacheness/storage/backends/blob_backends.py per D-01 through D-03. Add a stdlib tempfile import near the existing stdlib imports. In FilesystemBlobBackend.write_blob, replace deterministic blob_path suffix temp construction with tempfile.mkstemp(dir=blob_path.parent, suffix=".tmp"). Convert the returned path to Path, write through os.fdopen(fd, "wb"), close before os.replace(temp_path, blob_path), and clean temp_path in the exception path. The exception path must unlink only the unique temp path recorded for that write; it must not unlink or replace the already committed final blob when os.replace raises. Apply the same publication pattern to write_blob_stream for backend consistency. Ensure tests/test_blob_namespace.py includes and passes `test_filesystem_blob_backend_failed_unique_temp_write_preserves_existing_blob`, which commits an old blob, monkeypatches `blob_backends.os.replace` to fail on the next write, then asserts the old blob is still readable and the unique temp file was removed. Keep JSON metadata save behavior unchanged per D-05 and keep Phase 29 TTL/eviction untouched per D-23. Do not implement Phase 31 or Phase 32 items per D-24 and D-25.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_blob_namespace.py::test_filesystem_blob_backend_failed_unique_temp_write_preserves_existing_blob -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 pytest tests/test_blob_namespace.py tests/test_blob_store.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 python -c "from pathlib import Path; text=Path('src/cacheness/storage/backends/blob_backends.py').read_text(); assert 'tempfile.mkstemp' in text; assert 'with_suffix(blob_path.suffix + \".tmp\")' not in text"</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/storage/backends/blob_backends.py tests/test_blob_namespace.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/storage/backends/blob_backends.py tests/test_blob_namespace.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/storage/backends/blob_backends.py tests/test_blob_namespace.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/storage/backends/blob_backends.py tests/test_blob_namespace.py</automated>
  </verify>
  <acceptance_criteria>FilesystemBlobBackend no longer contains the deterministic blob_path suffix temp pattern, uses tempfile.mkstemp for write_blob and write_blob_stream, leaves no temp files after success, and passes the direct D-03 regression where a patched os.replace failure after an existing blob commit preserves old final content and removes the failed write's unique temp file.</acceptance_criteria>
  <done>PAR-01 is implemented with targeted tests and touched-file quality gates per D-28 and D-29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller data to filesystem blob backend | Untrusted user data is serialized to bytes and written to cache-controlled filesystem paths. |
| multiple local processes to same namespace directory | Independent writers can target the same final blob path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | FilesystemBlobBackend.write_blob | mitigate | Use race-free unique temp creation and atomic os.replace per D-01 through D-03. |
| T-30-01-D | Denial of service | temp cleanup | mitigate | Exception path unlinks only the unique temp file and leaves committed blobs available. |
| T-30-01-R | Repudiation | test evidence | mitigate | Test spies on tempfile.mkstemp and verifies final readable content per D-04 and D-26. |
</threat_model>

<verification>
Targeted validation: `uv run --python 3.12 pytest tests/test_blob_namespace.py tests/test_blob_store.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Phase quick validation after the wave: `uv run --python 3.12 pytest tests/test_blob_namespace.py tests/test_blob_store.py tests/test_backend_parity.py tests/test_fault_injection.py tests/test_storage_mode.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Full phase gate before verification: `uv run --python 3.12 pytest tests/ -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
PAR-01 is complete when filesystem blob writes no longer use deterministic final-path temp names, repeated same-blob writes leave the final content equal to the last successful write, failed writes clean only their unique temp file, and all listed verification commands either pass or document unrelated baseline ty diagnostics.
</success_criteria>

<output>
Create `.planning/phases/30-multi-process-backend-parity/30-01-SUMMARY.md` when done.
</output>
