---
phase: 30-multi-process-backend-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/metadata/sqlite_backend.py
  - src/cacheness/storage/backends/postgresql_backend.py
  - tests/test_backend_parity.py
autonomous: true
requirements:
  - PAR-02
requirements_addressed:
  - PAR-02
user_setup: []
must_haves:
  truths:
    - "PAR-02: SQLite and PostgreSQL preserve leftover user metadata from BlobStore.put metadata per D-06, D-07, D-08."
    - "User metadata is visible in the nested metadata dict returned by get_metadata in the same shape as JSON per D-09 and D-10."
    - "Metadata filtering works through the existing BlobStore.list(metadata_filter=...) public method; do not add a new list_keys API for this phase."
    - "PostgreSQL parity coverage is explicit and remains behind existing availability skip gates per D-10 and D-30."
    - "The plan excludes Phase 32 metadata dict mutation and other polish items per D-25."
  artifacts:
    - path: "src/cacheness/metadata/sqlite_backend.py"
      provides: "Leftover nested metadata merge into metadata_dict and read/filter exposure"
    - path: "src/cacheness/storage/backends/postgresql_backend.py"
      provides: "PostgreSQL JSONB metadata_dict merge and read/filter exposure"
    - path: "tests/test_backend_parity.py"
      provides: "JSON/SQLite/PostgreSQL public BlobStore user-metadata parity regression"
  key_links:
    - from: "BlobStore.put(metadata={...})"
      to: "SqliteBackend.put_entry"
      via: "leftover metadata merged into metadata_dict"
      pattern: "metadata_dict"
    - from: "BlobStore.list(metadata_filter={...})"
      to: "iter_entry_summaries"
      via: "flattened user metadata fields"
      pattern: "experiment"
---

<objective>
Implement TASK-10 / PAR-02: make SQLite and PostgreSQL preserve and filter user metadata the same way JSON already does for the public BlobStore path.

Purpose: Close code-review finding U2 so production metadata backends do not silently drop custom metadata that works in JSON development caches.
Output: Backend metadata merge/read/filter parity and skip-gated PostgreSQL regression coverage.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.github/copilot-instructions.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-multi-process-backend-parity/30-CONTEXT.md
@.planning/phases/30-multi-process-backend-parity/30-RESEARCH.md
@.planning/phases/30-multi-process-backend-parity/30-VALIDATION.md
@.planning/phases/30-multi-process-backend-parity/30-PATTERNS.md
@docs/CODE_REVIEW_FINDINGS.md
@docs/CODE_REVIEW_ACTIONS.md
@.planning/phases/29-ttl-eviction-consistency/29-03-SUMMARY.md
@src/cacheness/storage/blob_store.py
@src/cacheness/metadata/sqlite_backend.py
@src/cacheness/storage/backends/postgresql_backend.py
@src/cacheness/json_utils.py
@tests/test_backend_parity.py
@tests/test_blob_store.py
@tests/test_metadata.py
@tests/test_postgresql_backend.py
</context>

## Artifacts This Phase Produces

- `src/cacheness/metadata/sqlite_backend.py`: merge helper or local logic that serializes leftover user metadata into `metadata_dict`, preserves existing `metadata_dict` keys on conflicts, deserializes readback, and flattens user keys for filtering per D-06 through D-10.
- `src/cacheness/storage/backends/postgresql_backend.py`: equivalent JSONB-safe metadata merge/read/filter behavior using existing `_ensure_jsonb_value` and `json_utils` patterns.
- `tests/test_backend_parity.py`: public BlobStore parity test for JSON, SQLite, and PostgreSQL with PostgreSQL skip-gated by `_skip_if_pg_unavailable`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public BlobStore user-metadata parity regression</name>
  <files>tests/test_backend_parity.py</files>
  <read_first>
    - .planning/phases/30-multi-process-backend-parity/30-CONTEXT.md D-06 through D-10 and D-30.
    - .planning/phases/30-multi-process-backend-parity/30-PATTERNS.md sections for tests/test_backend_parity.py and BlobStore metadata flow.
    - docs/CODE_REVIEW_ACTIONS.md TASK-10.
    - tests/test_backend_parity.py PostgreSQL skip-gate helpers.
  </read_first>
  <behavior>
    - RED: SQLite and PostgreSQL drop BlobStore user metadata after known fields are popped.
    - GREEN: JSON, SQLite, and skip-gated PostgreSQL expose metadata["experiment"] == "x42" through BlobStore.get_metadata(key).
    - GREEN: BlobStore.list(metadata_filter={"experiment": "x42"}) returns exactly the stored key for JSON and SQLite, and PostgreSQL when available.
  </behavior>
  <action>Add a backend-parametrized public BlobStore parity test in tests/test_backend_parity.py. Parametrize backend values json, sqlite, and postgresql, call _skip_if_pg_unavailable for postgresql, and keep the xdist_group("docker") marker pattern for PostgreSQL-capable tests. Store bytes or a simple object with `store.put(data, key="meta-parity", metadata={"experiment": "x42"})`. Assert the returned entry has nested `entry["metadata"]["experiment"] == "x42"` because that is the shape BlobStore.get_metadata currently exposes. Assert `store.list(metadata_filter={"experiment": "x42"}) == ["meta-parity"]`; this is the existing public equivalent of D-10 list_keys wording in this codebase. Do not add a new public list_keys API and do not require a local PostgreSQL service per D-30.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_backend_parity.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run pytest tests/test_blob_store.py tests/test_metadata.py tests/test_backend_parity.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>The new test fails for SQLite before implementation while JSON demonstrates the expected behavior; PostgreSQL is collected and either runs or skips through the existing availability gate.</acceptance_criteria>
  <done>PAR-02 has a public API regression for D-06 through D-10 and D-30.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve user metadata in SQLite and PostgreSQL backends</name>
  <files>src/cacheness/metadata/sqlite_backend.py, src/cacheness/storage/backends/postgresql_backend.py, tests/test_backend_parity.py</files>
  <read_first>
    - src/cacheness/metadata/sqlite_backend.py put_entry, get_entry, iter_entry_summaries, list_entries.
    - src/cacheness/storage/backends/postgresql_backend.py _upsert_entry, _entry_to_dict, iter_entry_summaries, list_entries.
    - src/cacheness/json_utils.py dumps and loads helpers.
    - .planning/phases/29-ttl-eviction-consistency/29-03-SUMMARY.md access-count/upsert preservation decisions.
  </read_first>
  <behavior>
    - SQLite put_entry merges leftover nested metadata into metadata_dict_value after all known-field pops.
    - PostgreSQL _upsert_entry applies the same merge before JSONB conversion.
    - Existing metadata_dict_value keys win on conflict over leftover user metadata per D-08.
    - Read paths expose deserialized metadata_dict user keys in nested metadata and summary/list paths expose them flat for metadata_filter.
  </behavior>
  <action>Implement D-06 through D-09 in the metadata backends. In SqliteBackend.put_entry, after known fields are popped and after removing redundant data_type, merge any remaining user metadata into metadata_dict_value. If metadata_dict_value is None, serialize the leftovers with json_dumps. If metadata_dict_value already exists, deserialize it when it is a string, merge leftovers first, then overlay existing metadata_dict_value so core-provided keys win per D-08, and serialize the result back for SQLite storage. In SqliteBackend.get_entry, when row.metadata_dict exists, deserialize it when needed, keep `metadata["metadata_dict"]`, and copy its dict keys into nested metadata only when they do not overwrite technical fields. In SqliteBackend.iter_entry_summaries/list_entries, flatten metadata_dict dict keys into the returned entry when the flat key is absent so BlobStore.list(metadata_filter=...) can match. Apply the equivalent behavior in PostgresBackend._upsert_entry before _ensure_jsonb_value, in _entry_to_dict, and in iter_entry_summaries/list_entries using JSONB-native dicts where available. Preserve the access_count behavior completed in Phase 29; do not re-open TTL/eviction semantics per D-23 and do not implement Phase 32 POL-01 metadata dict mutation per D-25.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_backend_parity.py tests/test_blob_store.py tests/test_metadata.py tests/test_postgresql_backend.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_backend_parity.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_backend_parity.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_backend_parity.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/metadata/sqlite_backend.py src/cacheness/storage/backends/postgresql_backend.py tests/test_backend_parity.py</automated>
  </verify>
  <acceptance_criteria>BlobStore user metadata round-trips and filters on JSON and SQLite, PostgreSQL coverage is explicit and skip-gated, and no local PostgreSQL service setup is required.</acceptance_criteria>
  <done>PAR-02 is implemented with backend parity tests and touched-file quality gates per D-28 through D-30.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BlobStore public metadata to SQL metadata backends | User-provided metadata crosses from public API dicts into SQLite text JSON and PostgreSQL JSONB columns. |
| metadata filter input to list path | User filter fields are compared against flattened backend summaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Repudiation | SqliteBackend.put_entry and PostgresBackend._upsert_entry | mitigate | Preserve user metadata instead of silently dropping it, with public parity tests per D-06 through D-10. |
| T-30-02-I | Information disclosure | metadata_dict merge | accept | This phase preserves existing user-provided metadata visibility; it does not add new secrets or external exposure. |
| T-30-02-D | Denial of service | PostgreSQL service availability | mitigate | Keep PostgreSQL tests behind existing skip gates per D-30; do not require local service setup. |
</threat_model>

<verification>
Targeted validation: `uv run --python 3.12 pytest tests/test_backend_parity.py tests/test_blob_store.py tests/test_metadata.py tests/test_postgresql_backend.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Code-review task command: `uv run pytest tests/test_blob_store.py tests/test_metadata.py tests/test_backend_parity.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Phase quick validation after the wave: `uv run --python 3.12 pytest tests/test_blob_namespace.py tests/test_blob_store.py tests/test_backend_parity.py tests/test_fault_injection.py tests/test_storage_mode.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Full phase gate before verification: `uv run --python 3.12 pytest tests/ -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
PAR-02 is complete when JSON and SQLite pass the public BlobStore user-metadata round-trip/filter test, PostgreSQL coverage exists and skips through the existing availability gate when unavailable, and SQLite/PostgreSQL no longer discard leftover nested metadata.
</success_criteria>

<output>
Create `.planning/phases/30-multi-process-backend-parity/30-02-SUMMARY.md` when done.
</output>
