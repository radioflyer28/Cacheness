---
phase: 30-multi-process-backend-parity
plan: 04
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/cacheness/storage/backends/blob_backends.py
  - tests/test_cache_integrity_verification.py
autonomous: true
requirements:
  - PAR-04
requirements_addressed:
  - PAR-04
user_setup: []
must_haves:
  truths:
    - "PAR-04: FilesystemBlobBackend.list_blobs enumerates backend-visible namespace files instead of a hardcoded extension whitelist per D-18."
    - "Custom-handler and inline fallback extensions such as .custom and .bin are visible to integrity checks per D-19 and D-22."
    - "Reserved artifacts, .intents directories, metadata files, signing/key files, and *.tmp temp files remain excluded per D-20."
    - "Reserved matching may use a shared constant when it reduces duplication, but the phase remains scoped to enumeration/integrity behavior per D-21, D-23, D-25."
    - "Temp suffix exclusion coordinates with the .tmp suffix from 30-01 per D-20."
  artifacts:
    - path: "src/cacheness/storage/backends/blob_backends.py"
      provides: "Extension-agnostic filesystem blob enumeration with reserved/temp exclusions"
    - path: "tests/test_cache_integrity_verification.py"
      provides: "Custom-extension orphan detection and temp/reserved exclusion regression"
  key_links:
    - from: "FilesystemBlobBackend.list_blobs"
      to: "BlobStore.verify_integrity"
      via: "backend-visible blob inventory"
      pattern: "orphaned_blobs"
    - from: "tests/test_cache_integrity_verification.py"
      to: "verify_integrity"
      via: "fake custom extension under namespace directory"
      pattern: ".custom"
---

<objective>
Implement TASK-12 / PAR-04: make filesystem blob enumeration backend-driven enough for integrity checks and namespace cleanup to see custom-handler blobs while excluding reserved state.

Purpose: Close code-review finding R12 without changing eviction, S3, or Phase 31/32 policy behavior.
Output: Extension-agnostic list_blobs plus integrity regression for custom-extension orphans and temp exclusions.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.github/copilot-instructions.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-multi-process-backend-parity/30-CONTEXT.md
@.planning/phases/30-multi-process-backend-parity/30-RESEARCH.md
@.planning/phases/30-multi-process-backend-parity/30-VALIDATION.md
@.planning/phases/30-multi-process-backend-parity/30-PATTERNS.md
@docs/CODE_REVIEW_FINDINGS.md
@docs/CODE_REVIEW_ACTIONS.md
@.planning/phases/28-silent-data-loss-remediation/28-01-SUMMARY.md
@src/cacheness/storage/backends/blob_backends.py
@src/cacheness/storage/blob_store.py
@tests/test_cache_integrity_verification.py
@tests/test_blob_store.py
</context>

## Artifacts This Phase Produces

- `src/cacheness/storage/backends/blob_backends.py`: `FilesystemBlobBackend.list_blobs` recursively enumerates files under `self.base_dir`, excludes reserved/temp artifacts, and returns normalized paths.
- `src/cacheness/storage/backends/blob_backends.py`: optional module-level `RESERVED_FILE_PATTERNS` or narrowly scoped helper if it keeps reserved matching readable per D-21.
- `tests/test_cache_integrity_verification.py`: regression proving custom-extension orphan files are reported by `verify_integrity()` and .tmp artifacts are excluded.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PAR-04 custom-extension integrity regression</name>
  <files>tests/test_cache_integrity_verification.py</files>
  <read_first>
    - .planning/phases/30-multi-process-backend-parity/30-CONTEXT.md D-18 through D-22 and D-26 through D-29.
    - .planning/phases/30-multi-process-backend-parity/30-PATTERNS.md section for tests/test_cache_integrity_verification.py.
    - docs/CODE_REVIEW_ACTIONS.md TASK-12.
    - tests/test_cache_integrity_verification.py orphan/dangling blob tests.
  </read_first>
  <behavior>
    - RED: A fake orphan file with extension .custom under the namespace blob directory is not reported because list_blobs uses an extension whitelist.
    - GREEN: verify_integrity reports the .custom orphan path in orphaned_blobs.
    - GREEN: A .tmp file under the same namespace directory is not reported as an orphan.
  </behavior>
  <action>Add a focused integrity test in tests/test_cache_integrity_verification.py. Use the existing _make_cache pattern, put one real entry, create `tmp_path / "default" / "orphan.custom"` with bytes, create `tmp_path / "default" / "ignore.tmp"` with bytes, run cache.verify_integrity(), and assert the normalized custom path is in report["orphaned_blobs"] while the temp path is absent. Keep repair assertions only if the file already has a nearby repair pattern that can be reused without expanding scope. Do not alter Phase 29 TTL/eviction tests per D-23 and do not implement Phase 32 cleanup polish per D-25.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run pytest tests/test_blob_store.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>The custom-extension orphan regression fails before implementation and passes after Task 2; temp files are excluded from orphan reporting.</acceptance_criteria>
  <done>PAR-04 has executable coverage for D-18 through D-22.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace whitelist blob enumeration with reserved-aware directory enumeration</name>
  <files>src/cacheness/storage/backends/blob_backends.py, tests/test_cache_integrity_verification.py</files>
  <read_first>
    - src/cacheness/storage/backends/blob_backends.py FilesystemBlobBackend.list_blobs and Plan 30-01 temp suffix implementation.
    - src/cacheness/storage/blob_store.py verify_integrity inventory consumption.
    - .planning/phases/28-silent-data-loss-remediation/28-01-SUMMARY.md reserved-aware cleanup pattern.
  </read_first>
  <behavior>
    - list_blobs returns normalized paths for all regular files under the namespace directory, regardless of extension.
    - list_blobs excludes directories and any path under .intents.
    - list_blobs excludes files ending in .tmp and reserved metadata/key artifacts.
    - verify_integrity sees custom-extension orphan files through blob_backend.list_blobs without special-casing integrity code.
  </behavior>
  <action>Update FilesystemBlobBackend.list_blobs per D-18 through D-21. Remove the hardcoded blob_extensions and pickle_codecs whitelist. Enumerate `self.base_dir.rglob("*")`, include only regular files, and return sorted normalized string paths. Exclude any path with a `.intents` part, any filename ending in `.tmp`, SQLite sidecars ending `.db`, `.db-wal`, or `.db-shm`, JSON metadata files such as `cache_metadata.json` and namespace-specific `*_metadata.json`, and signing/key files such as `.cache_signing_key` and `.cache_signing_key.*`. If a module-level reserved helper or constant makes these exclusions clearer, create it in src/cacheness/storage/backends/blob_backends.py only; do not move behavior across modules. Keep this dependent on 30-01 so the .tmp exclusion matches the unique temp suffix. Do not change BlobStore.verify_integrity except to adjust tests if existing assumptions require it.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_cache_integrity_verification.py tests/test_blob_store.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/storage/backends/blob_backends.py tests/test_cache_integrity_verification.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/storage/backends/blob_backends.py tests/test_cache_integrity_verification.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/storage/backends/blob_backends.py tests/test_cache_integrity_verification.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/storage/backends/blob_backends.py tests/test_cache_integrity_verification.py</automated>
  </verify>
  <acceptance_criteria>FilesystemBlobBackend.list_blobs no longer filters by handler extension whitelist, custom-extension orphans are reported, temp/reserved artifacts are excluded, and integrity behavior remains backend-driven.</acceptance_criteria>
  <done>PAR-04 is implemented with targeted integrity tests and touched-file quality gates per D-28 and D-29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem namespace directory to integrity verifier | Files in the cache namespace may include user blobs, stale temps, and reserved internal state. |
| backend inventory to repair mode | BlobStore.verify_integrity can delete reported orphans when repair=True. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering | FilesystemBlobBackend.list_blobs | mitigate | Enumerate all backend-visible blob files so custom-handler orphans are detectable per D-18, D-19, and D-22. |
| T-30-05-D | Denial of service | reserved/temp file enumeration | mitigate | Exclude .intents, metadata DB/JSON files, signing/key files, and .tmp files per D-20. |
| T-30-05-I | Information disclosure | integrity report paths | accept | Integrity reports already expose local blob paths; this plan changes completeness, not report audience. |
</threat_model>

<verification>
Targeted validation: `uv run --python 3.12 pytest tests/test_cache_integrity_verification.py tests/test_blob_store.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Code-review task command: `uv run pytest tests/test_blob_store.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Phase quick validation after the wave: `uv run --python 3.12 pytest tests/test_blob_namespace.py tests/test_blob_store.py tests/test_backend_parity.py tests/test_fault_injection.py tests/test_storage_mode.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Full phase gate before verification: `uv run --python 3.12 pytest tests/ -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
PAR-04 is complete when custom-extension and inline fallback-style files are visible to integrity enumeration, temp and reserved files are excluded, no BlobStore integrity special case is introduced, and all listed verification commands either pass or document unrelated baseline ty diagnostics.
</success_criteria>

<output>
Create `.planning/phases/30-multi-process-backend-parity/30-04-SUMMARY.md` when done.
</output>
