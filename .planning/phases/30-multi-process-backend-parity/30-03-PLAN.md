---
phase: 30-multi-process-backend-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/cacheness/core.py
  - src/cacheness/_storage_mode_mixin.py
  - src/cacheness/_put_cleanup.py
  - tests/test_fault_injection.py
  - tests/test_storage_mode.py
autonomous: true
requirements:
  - PAR-03
requirements_addressed:
  - PAR-03
user_setup: []
must_haves:
  truths:
    - "PAR-03: failed same-key overwrite leaves the previous committed value readable in cache mode per D-11, D-12, D-17."
    - "PAR-03: failed same-key overwrite leaves the previous committed value readable in storage_mode=True per D-11 and D-17."
    - "The previous local blob is moved to a .prev snapshot before overwrite and restored on rollback per D-13 through D-15."
    - "Successful metadata commit and cleanup commit delete the .prev snapshot, while rollback restores it to the original path per D-14."
    - "Remote URI and inline entries are excluded from previous-blob snapshot handling per D-16."
    - "No Phase 31 storage-mode policy decisions are implemented here per D-24."
    - "This remains one of four atomic TASK-9 through TASK-12 plans per D-27."
  artifacts:
    - path: "src/cacheness/_put_cleanup.py"
      provides: "Shared previous local blob snapshot, commit cleanup, and rollback restore"
    - path: "src/cacheness/core.py"
      provides: "Cache-mode same-local-path overwrite snapshot wiring"
    - path: "src/cacheness/_storage_mode_mixin.py"
      provides: "Storage-mode same-local-path overwrite snapshot wiring"
    - path: "tests/test_fault_injection.py"
      provides: "Cache-mode metadata failure regression preserving value A"
    - path: "tests/test_storage_mode.py"
      provides: "Storage-mode metadata failure regression preserving value A"
  key_links:
    - from: "UnifiedCache.put"
      to: "_PutCleanup.rollback"
      via: "previous local blob snapshot before _write_blob"
      pattern: "snapshot_previous_blob"
    - from: "_storage_mode_put"
      to: "_PutCleanup.rollback"
      via: "same helper as cache mode"
      pattern: "storage_mode"
---

<objective>
Implement TASK-11 / PAR-03: make failed same-key overwrites non-destructive in both cache mode and storage mode.

Purpose: Close code-review finding R7, the highest-risk Phase 30 item, by preserving the previous committed local blob whenever metadata commit fails after a same-path overwrite.
Output: Shared rollback snapshot behavior plus cache-mode and storage-mode fault-injection regressions.
</objective>

<execution_context>
@C:/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.github/copilot-instructions.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-multi-process-backend-parity/30-CONTEXT.md
@.planning/phases/30-multi-process-backend-parity/30-RESEARCH.md
@.planning/phases/30-multi-process-backend-parity/30-VALIDATION.md
@.planning/phases/30-multi-process-backend-parity/30-PATTERNS.md
@docs/CODE_REVIEW_FINDINGS.md
@docs/CODE_REVIEW_ACTIONS.md
@src/cacheness/core.py
@src/cacheness/_storage_mode_mixin.py
@src/cacheness/_put_cleanup.py
@tests/test_fault_injection.py
@tests/test_storage_mode.py
@tests/test_cache_integrity.py
</context>

## Artifacts This Phase Produces

- `src/cacheness/_put_cleanup.py`: explicit previous-blob tracking API, commit deletion of the .prev snapshot, and rollback restoration after deleting the newly written local blob per D-13 through D-15.
- `src/cacheness/core.py`: cache-mode wiring that snapshots only same-local-path overwrites before `_blob_store._write_blob`.
- `src/cacheness/_storage_mode_mixin.py`: storage-mode wiring using the same helper and the same remote/inline exclusions.
- `tests/test_fault_injection.py`: same-key cache-mode metadata failure regression.
- `tests/test_storage_mode.py`: same-key storage-mode metadata failure regression.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PAR-03 same-key overwrite failure regressions</name>
  <files>tests/test_fault_injection.py, tests/test_storage_mode.py</files>
  <read_first>
    - .planning/phases/30-multi-process-backend-parity/30-CONTEXT.md D-11 through D-17 and D-24.
    - .planning/phases/30-multi-process-backend-parity/30-PATTERNS.md sections for tests/test_fault_injection.py and tests/test_storage_mode.py.
    - docs/CODE_REVIEW_ACTIONS.md TASK-11.
    - tests/test_fault_injection.py metadata_backend.put_entry patch pattern.
    - tests/test_storage_mode.py storage_mode fixture pattern.
  </read_first>
  <behavior>
    - RED: In cache mode, value A is committed, metadata_backend.put_entry is patched to raise on value B, and the failed overwrite leaves get(key) unable to return A before the fix.
    - GREEN: The same cache-mode scenario raises the injected exception, then get(key) returns A and the original blob path exists.
    - GREEN: The same behavior holds with CacheConfig(storage_mode=True).
  </behavior>
  <action>Add two focused regressions. In tests/test_fault_injection.py, commit value A under an explicit cache_key, capture the old actual_path from metadata, patch cache.metadata_backend.put_entry to raise RuntimeError on the next call, attempt value B with the same key, assert the exception, then assert cache.get(cache_key=key) returns A and the captured blob file still exists. In tests/test_storage_mode.py, repeat the same sequence using the existing storage_mode=True cache fixture and assert storage-mode get returns A. Keep the tests local-filesystem only; remote URI and inline exclusions are implementation behavior per D-16, not a test service requirement. Do not use beads or bd, and do not touch .planning/config.json.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_fault_injection.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run pytest tests/test_core.py tests/test_fault_injection.py tests/test_cache_integrity.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>The new cache-mode and storage-mode regressions fail before implementation due to loss of the previous committed blob, then pass after Task 2.</acceptance_criteria>
  <done>PAR-03 has executable coverage for D-11, D-12, and D-17.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement previous local blob snapshot and rollback restore</name>
  <files>src/cacheness/core.py, src/cacheness/_storage_mode_mixin.py, src/cacheness/_put_cleanup.py, tests/test_fault_injection.py, tests/test_storage_mode.py</files>
  <read_first>
    - src/cacheness/_put_cleanup.py _PutCleanup slots, commit, rollback.
    - src/cacheness/core.py UnifiedCache.put existing old_blob_path and planned_blob_path flow.
    - src/cacheness/_storage_mode_mixin.py _storage_mode_put matching old_blob_path and planned_blob_path flow.
    - .planning/phases/30-multi-process-backend-parity/30-RESEARCH.md Common Pitfalls for TASK-11.
  </read_first>
  <behavior>
    - _PutCleanup can move an existing local blob to a path formed by appending .prev before overwrite.
    - cleanup.commit deletes the .prev snapshot and disarms rollback after metadata commit and stale cleanup succeed.
    - cleanup.rollback removes the newly written local blob if present, restores .prev to the original path if present, and remains idempotent.
    - UnifiedCache.put and _storage_mode_put invoke the same snapshot behavior only for existing local actual_path values resolving to the same planned local blob path.
  </behavior>
  <action>Extend _PutCleanup per D-15 with explicit previous-blob tracking, for example previous_blob_path and previous_snapshot_path slots plus a method named snapshot_previous_blob(path: Path). The method should no-op when path does not exist, otherwise move the current blob to `Path(str(path) + ".prev")` with os.replace and record both paths. In commit(), delete the recorded snapshot if it exists before marking committed. In rollback(), keep existing best-effort deletion of the newly written local blob, then restore the recorded snapshot to the original path if it exists, and mark committed so double rollback is safe. In UnifiedCache.put and _storage_mode_put, before calling _blob_store._write_blob, compare an existing entry's actual_path resolved through _resolve_actual_path with the planned local blob path derived from handler.get_file_extension(config). Call cleanup.snapshot_previous_blob only when they match, the existing actual_path is not a URI, and the planned write is a local filesystem path. Keep inline entries and remote URI paths outside this snapshot path per D-16. Preserve Phase 29 TTL/eviction behavior per D-23 and do not implement Phase 31 storage-mode destructive API policy per D-24.</action>
  <verify>
    <automated>uv run --python 3.12 pytest tests/test_core.py tests/test_fault_injection.py tests/test_cache_integrity.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
    <automated>uv run --python 3.12 ruff format src/cacheness/core.py src/cacheness/_storage_mode_mixin.py src/cacheness/_put_cleanup.py tests/test_fault_injection.py tests/test_storage_mode.py</automated>
    <automated>uv run --python 3.12 ruff check --fix src/cacheness/core.py src/cacheness/_storage_mode_mixin.py src/cacheness/_put_cleanup.py tests/test_fault_injection.py tests/test_storage_mode.py</automated>
    <automated>uv run --python 3.12 ruff check src/cacheness/core.py src/cacheness/_storage_mode_mixin.py src/cacheness/_put_cleanup.py tests/test_fault_injection.py tests/test_storage_mode.py</automated>
    <automated>uv run --python 3.12 ty check src/cacheness/core.py src/cacheness/_storage_mode_mixin.py src/cacheness/_put_cleanup.py tests/test_fault_injection.py tests/test_storage_mode.py</automated>
  </verify>
  <acceptance_criteria>A failed same-key metadata write in cache mode or storage mode raises the original exception, leaves value A readable, leaves the old blob file present, and does not leave a stale .prev snapshot after successful overwrite commits.</acceptance_criteria>
  <done>PAR-03 is implemented with shared rollback semantics, cache-mode coverage, storage-mode coverage, and touched-file quality gates per D-28 and D-29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata backend commit | Metadata persistence can fail after the blob backend has already published new bytes. |
| cache mode to storage mode | Storage mode shares put orchestration but has a stronger durable-data contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | UnifiedCache.put rollback | mitigate | Snapshot same-local-path previous blobs and restore on rollback per D-11 through D-15. |
| T-30-04 | Tampering | _storage_mode_put rollback | mitigate | Apply the same snapshot helper in storage mode and test durable value preservation per D-11 and D-17. |
| T-30-03-D | Denial of service | .prev cleanup | mitigate | commit and rollback are idempotent and best-effort so stale cleanup state does not crash callers. |
| T-30-03-S | Spoofing | remote URI handling | accept | Remote URI overwrite rollback is explicitly outside this local-path fix per D-16; Phase 31 handles storage/security policy decisions per D-24. |
</threat_model>

<verification>
Targeted validation: `uv run --python 3.12 pytest tests/test_core.py tests/test_fault_injection.py tests/test_cache_integrity.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Storage-mode focused validation: `uv run --python 3.12 pytest tests/test_storage_mode.py tests/test_fault_injection.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Phase quick validation after the wave: `uv run --python 3.12 pytest tests/test_blob_namespace.py tests/test_blob_store.py tests/test_backend_parity.py tests/test_fault_injection.py tests/test_storage_mode.py tests/test_cache_integrity_verification.py -x -q --ignore=tests/test_tensorflow_handler.py`.

Full phase gate before verification: `uv run --python 3.12 pytest tests/ -x -q --ignore=tests/test_tensorflow_handler.py`.
</verification>

<success_criteria>
PAR-03 is complete when same-key overwrite failure preserves value A and its blob in both cache mode and storage mode, snapshot cleanup is idempotent, remote/inline entries are not pulled into scope, and all listed verification commands either pass or document unrelated baseline ty diagnostics.
</success_criteria>

<output>
Create `.planning/phases/30-multi-process-backend-parity/30-03-SUMMARY.md` when done.
</output>
