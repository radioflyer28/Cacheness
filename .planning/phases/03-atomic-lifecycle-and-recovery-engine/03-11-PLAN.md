---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "11"
type: execute
wave: 10
depends_on: ["03-09"]
files_modified:
  - tools/capture_phase3_windows_qualification.py
  - tests/test_phase3_windows_qualification_attestation.py
  - .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
  - .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The completed 03-09 summary exists before qualification work begins and its structured platform-runner contract is compared field-for-field with the command actually executed."
    - "The fixed current-host command exits 2 with one canonical UNAVAILABLE JSON object; capture preserves the exact stdout bytes atomically and verification rejects stale, edited, malformed, or contradictory evidence (D-32)."
    - "The qualification record states NOT_QUALIFIED, native_evidence=false, and backlog Phase 999.1; it never claims native protected-root, Python 3.11, same-session, different-token, or scheduler-retirement proof (D-22, D-31, D-32)."
    - "03-VALIDATION.md accepts attested UNAVAILABLE/NOT_QUALIFIED only as closure of Phase 3's current-host evidence obligation and keeps native PASS/exit 0 mandatory before any Windows-qualified release (D-32)."
    - "The normal executor-generated 03-11 summary carries a structured qualification mirror for dependent Plan 03-10; no task reads or edits that summary before plan close-out."
  artifacts:
    - path: tools/capture_phase3_windows_qualification.py
      provides: "Fixed-command capture, atomic publication, byte-level verification, and structural summary/validation attestation"
    - path: tests/test_phase3_windows_qualification_attestation.py
      provides: "Tamper, stale-output, malformed-status, atomic-write, summary, validation, and contradiction tests"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md
      provides: "Auditable current-host UNAVAILABLE/NOT_QUALIFIED record and Phase 999.1 handoff"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
      provides: "D-32-aware milestone sign-off separating current-host closure from native Windows qualification"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md
      provides: "Normal post-task qualification summary with a structured mirror consumed by 03-10"
  key_links:
    - from: tools/capture_phase3_windows_qualification.py
      to: verify_platform.py
      via: "Non-overridable argv executes the exact 03-09 platform runner without a shell"
      pattern: "require-system|Windows|require-python|3.11"
    - from: tools/capture_phase3_windows_qualification.py
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md
      via: "Bounded frontmatter parser compares the completed runner contract with the fixed argv and expected UNAVAILABLE semantics"
      pattern: "platform-evidence|UNAVAILABLE|windows_qualified|999.1"
    - from: tools/capture_phase3_windows_qualification.py
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md
      via: "Atomic capture plus fresh byte-for-byte and SHA-256 attestation"
      pattern: "stdout_base64|stdout_sha256|NOT_QUALIFIED|native_evidence"
    - from: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
      to: .planning/ROADMAP.md
      via: "D-32 sign-off names Phase 999.1 as the unsatisfied native PASS obligation"
      pattern: "UNAVAILABLE|NOT_QUALIFIED|999.1|native PASS"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] UNAVAILABLE must not be promoted to PASS or cited as native Windows support (D-32)."
    - "[FLAGGED/UNVERIFIED] The helper must not accept a caller-supplied replacement command."
    - "[FLAGGED/UNVERIFIED] Qualification verification must not rely on prose substring searches when a structured field exists."
---

<objective>
Capture and attest the current host's unavailable native Windows qualification after the platform runner and its normal 03-09 summary are complete.

Purpose: Close Phase 3's attainable current-host evidence obligation without fabricating native Windows proof, while leaving an executable Phase 999.1 qualification path.
Output: A deterministic attestation helper and adversarial tests, an atomically captured UNAVAILABLE/NOT_QUALIFIED artifact, a D-32-correct validation contract, and the normal qualification-specific 03-11 summary.
</objective>

## Artifacts this phase produces

- A fixed-command helper that captures and verifies exact current-host runner bytes.
- Adversarial tests for command, status, parsing, atomicity, staleness, tampering, summary, and validation contradictions.
- An auditable Windows qualification artifact that is explicitly UNAVAILABLE/NOT_QUALIFIED.
- Validation and summary records that preserve native PASS as Phase 999.1's future release prerequisite.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/ROADMAP.md

The 03-09 summary is a completed dependency, not a file this plan may rewrite. Its `platform-evidence` mapping is the structural source for runner command/status comparison. The normal 03-11 summary is created only after both tasks finish.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove fixed-command capture and byte-level attestation end to end</name>
  <reversibility rating="reversible">The evidence format and verifier can be versioned or replaced without changing lifecycle storage semantics.</reversibility>
  <files>tools/capture_phase3_windows_qualification.py, tests/test_phase3_windows_qualification_attestation.py</files>
  <read_first>
    <file>verify_platform.py</file>
    <file>tests/test_phase3_windows_contract.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "The helper owns exactly `uv run --python 3.11 --frozen python verify_platform.py --phase3 --require-system Windows --require-python 3.11`; no CLI option or environment value can replace the argv."
    - "Execution uses an argv sequence, repository-root cwd, no shell, byte capture, actual return code 2, strict UTF-8 decoding, exactly one JSON object, and parsed status UNAVAILABLE."
    - "Capture writes a versioned sibling temporary artifact, flushes and fsyncs it, atomically replaces the destination, fsyncs the parent, reads it back, and reproduces the captured stdout bytes exactly from base64 plus SHA-256."
    - "Verify reruns the command and structurally checks the artifact, completed 03-09 `platform-evidence` mapping, and optional completed 03-11 `windows-qualification` mapping; every field must agree."
    - "Wrong exit, malformed/non-UTF-8/multiple JSON, non-UNAVAILABLE status, byte edits, stale output, invalid digest/base64, failed atomic write, invalid backlog/future-PASS structure, or current native/support-qualified claims fail closed."
  </behavior>
  <action>Create `tools/capture_phase3_windows_qualification.py` with `capture` and `verify` modes and the single fixed argv above; accept paths for the artifact, completed 03-09 runner summary, validation file, and optional completed 03-11 qualification summary, but never accept a replacement command. Resolve the repository root from the helper path. Parse only bounded expected scalar mappings from Markdown/YAML frontmatter without adding PyYAML as a runtime dependency. Encode exact stdout bytes as base64 and bind them with SHA-256, return code, parsed runner object, `milestone_status: NOT_QUALIFIED`, `native_evidence: false`, `backlog_phase: 999.1`, and future `status: PASS`/`exit_code: 0` requirements. Implement atomic sibling publication and post-replace byte verification. Write tests first for the happy path and each behavior above, including a fixture 03-09 summary with the normal executor frontmatter contract and a fixture 03-11 summary for later dependent verification. Use controlled subprocess injection for adversarial cases and one real current-host repeatability test for the fixed command. Keep stderr diagnostic-only and never include account names, secrets, or machine-specific paths in the artifact.</action>
  <verify>
    <automated>uv run --python 3.11 --frozen pytest -q tests/test_phase3_windows_qualification_attestation.py -x</automated>
  </verify>
  <acceptance_criteria>The fixed command is unavoidable; exit 2 and parsed UNAVAILABLE are mandatory; exact bytes survive atomic capture/read-back; the completed 03-09 runner contract is compared structurally; and every tamper, stale, malformed, or support-claim contradiction test fails closed (D-32).</acceptance_criteria>
  <done>One production-quality path runs from the completed platform runner through an atomically published, freshly verifiable UNAVAILABLE artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the real record and revise Phase 3 validation semantics</name>
  <reversibility rating="reversible">An eligible Windows host can replace this unavailable evidence through Phase 999.1 without changing lifecycle semantics (D-32).</reversibility>
  <files>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md, .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md</files>
  <read_first>
    <file>tools/capture_phase3_windows_qualification.py</file>
    <file>tests/test_phase3_windows_qualification_attestation.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <action>Revise only the sampling, Windows failure dimension, manual-only verification, validation sign-off, and approval text in `03-VALIDATION.md`. Per D-32, the current Phase 3 gate requires a freshly verified artifact with `UNAVAILABLE`, `NOT_QUALIFIED`, `native_evidence: false`, and backlog Phase 999.1, while native protected-root, Python 3.11, same-session contention, different-session/service-token denial, and scheduler-retirement PASS remain unsatisfied prerequisites for a future Windows-qualified release. Preserve the exact future PowerShell invocation `uv run --python 3.11 --frozen python verify_platform.py --phase3 --require-system Windows --require-python 3.11 --phase3-root $env:CACHENESS_PHASE3_WINDOWS_ROOT` and name the root and second-token environment variables without inventing credentials. Then run helper capture against the completed 03-09 summary and revised validation file to create `03-09-WINDOWS-QUALIFICATION.md`; immediately run verify to re-execute the command and compare return code, raw bytes, digest, parsed fields, summary contract, and validation disposition. Do not edit the completed 03-09 summary, ROADMAP, CONTEXT, runner, Windows contract tests, source code, or any scheduler-retirement code in this task.</action>
  <verify>
    <automated>.venv/bin/python tools/capture_phase3_windows_qualification.py capture --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; .venv/bin/python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; .venv/bin/pytest -q tests/test_phase3_windows_qualification_attestation.py -x</automated>
  </verify>
  <acceptance_criteria>The real current-host command is freshly executed and atomically recorded; structural verification proves UNAVAILABLE/NOT_QUALIFIED without native evidence or a support claim; validation closes only the attainable Phase 3 evidence obligation; and Phase 999.1 still requires native PASS/exit 0 before any Windows-qualified release (D-32).</acceptance_criteria>
  <done>Phase 3 has an auditable unavailable qualification record and a validation contract that is honest about the unproven Windows boundary.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generations and recoverable incomplete outcomes | 03-02..03-10 | COVERED | Crash/race closure remains 03-09; final acceptance remains 03-10. |
| REQ | STOR-03 | Old-or-new write visibility | 03-02, 03-04, 03-09, 03-10 | COVERED | Qualification does not weaken lifecycle proof. |
| REQ | STOR-04 | Failed-write preservation and recoverable residue | 03-02, 03-04, 03-05, 03-09, 03-10 | COVERED | Crash/reopen evidence remains in 03-09. |
| REQ | STOR-05 | Idempotent overwrite/delete/clear/close cleanup | 03-04, 03-05, 03-09, 03-10 | COVERED | Final full-suite gate remains 03-10. |
| REQ | STOR-06 | Dry-run and resumable reconciliation | 03-05, 03-09, 03-10 | COVERED | Qualification record is evidence-only. |
| REQ | STOR-07 | Deterministic same-key races without global serialization | 03-03, 03-04, 03-09, 03-10 | COVERED | Race/platform proof remains 03-09. |
| RESEARCH | — | Transactional authority, native payloads, bounded recovery, scheduler retirement, Python/platform evidence | 03-01..03-10 | COVERED | 03-11 adds only the post-summary evidence boundary. |
| CONTEXT | D-01..D-31 | Locked lifecycle and replacement architecture decisions | 03-01..03-10 | COVERED | No decision is reopened here. |
| CONTEXT | D-32 | Honest UNAVAILABLE/NOT_QUALIFIED record and Phase 999.1 native-PASS obligation | 03-09, 03-11, 03-10 | COVERED | 03-11 owns capture and validation after 03-09 summary creation. |

Exclusions remain unchanged: PostgreSQL/S3 adapters are Phases 4/5; UnifiedCache policy is Phase 6; stored-format migration is Phase 7; archived file-native artifacts are historical evidence only.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| platform runner → evidence helper | Exit status and stdout are untrusted until strict decoding and structural validation complete. |
| filesystem artifact → release record | Partial writes, edits, stale bytes, or digest mismatch must not survive verification. |
| summaries/validation → qualification claim | Structured mirrors may close current-host evidence only; they cannot manufacture native Windows proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-11-01 | Spoofing | runner status | high | mitigate | Require fixed argv, actual exit 2, strict one-object JSON, and parsed UNAVAILABLE. |
| T-03-11-02 | Tampering | qualification artifact | high | mitigate | Use sibling fsync/replace/parent-fsync publication, base64 exact bytes, SHA-256, read-back, and fresh rerun comparison. |
| T-03-11-03 | Repudiation | summary/validation mirrors | high | mitigate | Compare bounded structured mappings from completed 03-09/03-11 summaries and D-32 validation fields; reject prose-only or contradictory claims. |
| T-03-11-04 | Elevation of Privilege | Windows support claim | high | mitigate | Record `native_evidence: false`, keep NOT_QUALIFIED, and require Phase 999.1 native PASS/exit 0 before any Windows-qualified release. |
| T-03-11-05 | Information Disclosure | captured diagnostics | medium | mitigate | Persist only bounded runner fields and stdout evidence; exclude stderr, account names, secrets, and machine-specific paths. |
</threat_model>

<verification>
Run the attestation suite under real Python 3.11, capture the real current-host fixed-command result, and immediately verify it against the completed 03-09 runner summary and revised D-32 validation contract. Require exit 2, parsed UNAVAILABLE, exact stdout bytes and digest, NOT_QUALIFIED, native_evidence=false, and Phase 999.1. After both tasks pass, create the normal 03-11 summary with the structured qualification mirror described below.
</verification>

<success_criteria>
- The evidence helper and tamper suite are deterministic and pass under Python 3.11.
- The current host is recorded only as UNAVAILABLE/NOT_QUALIFIED with no native evidence or Windows support claim.
- 03-VALIDATION.md closes the current-host Phase 3 obligation while retaining every native Windows proof item for Phase 999.1.
- The completed 03-09 summary is consumed read-only and compared structurally; no task depends on a summary that does not yet exist.
- Plan 03-10 can consume both the qualification artifact and the normal completed 03-11 summary.
</success_criteria>

<output>
Create the normal `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md` only after both tasks complete. Add a `windows-qualification` frontmatter mapping with `artifact_path`, `artifact_sha256`, `runner_status: UNAVAILABLE`, `milestone_status: NOT_QUALIFIED`, `native_evidence: false`, `backlog_phase: 999.1`, `future_required_status: PASS`, and `future_required_exit_code: 0`; populate the digest from the captured artifact. This post-task summary is consumed and structurally verified by Plan 03-10.
</output>
