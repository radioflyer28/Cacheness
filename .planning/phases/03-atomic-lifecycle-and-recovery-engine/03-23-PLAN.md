---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "23"
type: execute
wave: 22
depends_on: ["03-22"]
files_modified:
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/read_contract.py
  - src/cacheness/storage/__init__.py
  - tests/test_blob_store_read_contract.py
  - tests/test_blob_store_atomic_lifecycle.py
  - tests/test_blob_store_close_contract.py
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-07]
estimate:
  tokens: 30000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "A supported BlobStore entry result carries authenticated metadata and an exact expectation for the same verified payload snapshot; stored None is distinct from absence (D-01, D-03, D-19, D-33, D-36)."
    - "Snapshot acquisition and disposal belong to BlobStore; policy can inspect authenticated metadata before deserialization without reopening another generation."
    - "A supported write receipt identifies the actual committed generation; engine-owned cleanup and recoverable debt are independent of projection publication (D-04, D-07, D-08, D-24, D-25, D-34)."
    - "Existing put/get signatures and native formats stay compatible, while direct application metadata round-trips through query/update/reopen."
  artifacts:
    - path: src/cacheness/storage/read_contract.py
      provides: "Supported immutable entry metadata/expectation and committed receipt contracts"
    - path: src/cacheness/storage/blob_store.py
      provides: "open_entry and put_entry with compatible existing wrappers"
    - path: src/cacheness/storage/lifecycle.py
      provides: "One snapshot acquisition path and engine-owned post-commit cleanup"
    - path: tests/test_blob_store_read_contract.py
      provides: "Same-generation, pre-deserialization policy, None and lifetime regressions"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle.py
      via: "Shared read acquisition and complete write orchestration"
      pattern: "open_entry|put_entry"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/read_contract.py
      via: "Verified snapshot and committed receipt without raw authority steps"
      pattern: "expectation|metadata|committed"
  prohibitions:
    - "[UNVERIFIED] No raw connection, prepare/promote/settle protocol, or admission gate in the supported caller interface."
    - "[UNVERIFIED] No DB transaction spans payload I/O or caller snapshot lifetime."
    - "[UNVERIFIED] No new payload wrapper, stored schema, generic policy plugin, or second lifecycle engine."
---

<objective>
Deepen the BlobStore interface before rewiring cache callers. Use existing manifests and lifecycle implementation, not a replacement backend framework.
Output: one supported same-generation entry snapshot and committed-write receipt, with public round-trip/recovery tests. Full backend narrowing stays Phase 4.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/phase3-architecture-audit-2026-09-06.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-GAP-REPLAN.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
</context>

<planner_assumptions>
SQLite/filesystem is initialized before concurrent use; memory remains one process. STOR-03/07 enforce integrity with bounded conflict/timeout outcomes; STOR-04/05 enforce intent/debt recovery. Short catalog transactions own promotion, never caller snapshot lifetime or payload effects. Performance remains separately measured.
The additive interface is specified in 03-GAP-REPLAN.md. Names may be refined without expanding caller obligations; record any name changes. No external API or stored schema push is required.
</planner_assumptions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Return one verified entry snapshot with metadata before deserialization</name>
  <reversibility rating="costly">Additive public interface; preserve legacy wrappers and stored formats.</reversibility>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/read_contract.py, src/cacheness/storage/__init__.py, tests/test_blob_store_read_contract.py, tests/test_blob_store_close_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/read_contract.py</file>
    <file>src/cacheness/storage/__init__.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>tests/test_blob_store_close_contract.py</file>
  </read_first>
  <action>Add open_entry(key) as a context-managed verified entry or explicit absence. Metadata is a detached immutable/deep-frozen view of the authenticated committed manifest, with key and opaque exact-entry expectation. read() deserializes only the already acquired private snapshot; it never looks up another generation. Reuse the existing M1/M2 validation, integrity verification and bounded generation-change retry. Acquire before yielding, release snapshot/admission on every exit (including exceptions), do not expose raw paths/connections as caller obligations, and do not hold an authority transaction across acquisition or caller use. Scope read() to the context and reject use after close. Route get/exists through the shared acquisition path, preserving public return semantics.

Public tracer: initialize, put object plus application metadata, open_entry and inspect metadata, read value, close, reopen store and repeat. Add stored None versus absent, tampered manifest/payload, policy rejection before handler.get, failed acquisition, caller exception, nested close/owned-resource behavior and forced overwrite between observations. A read returns one coherent old/new entry or typed conflict; no mixed metadata/payload and no second facade snapshot is needed.</action>
  <acceptance_criteria>Public snapshot proof passes with one payload snapshot/digest verification per successful acquisition and no deserialization before read(). Snapshot metadata cannot mutate authority through shared dictionaries. Post-context read is rejected; close releases owned resources exactly once; get/exists remain compatible.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_blob_store_read_contract.py tests/test_blob_store_close_contract.py tests/test_blob_store_integrity.py -o log_cli=false</automated></verify>
  <done>The cache can get metadata, value and deletion expectation through one supported seam without reconstructing generation identity.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Return committed writes while keeping cleanup inside the engine</name>
  <depends_on>Task 1 tracer passes.</depends_on>
  <reversibility rating="costly">Additive receipt; no stored format change or facade cutover yet.</reversibility>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/read_contract.py, src/cacheness/storage/__init__.py, tests/test_blob_store_atomic_lifecycle.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/read_contract.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
  </read_first>
  <action>Add put_entry with a small immutable committed receipt: key, exact expectation and committed metadata from the actual promotion result, not a later lookup. Reuse LifecyclePutResult internally without exporting authority rows or cleanup stages. put remains key-returning. The supported path owns admission, serialization, intent, verification, promotion and cleanup; callers never choose defer_cleanup or settle it. Post-commit cleanup failure retains durable debt and conveys committed identity through existing recoverable error context (committed status, key, generation/expectation); preserve its cause and distinguish pre-commit failure. No error hierarchy change is needed.

Retain old private facade entry points only until Plan 24; do not claim them removed here. The additive receipt may expose derived-export diagnostics separately from committed identity. Do not change existing wrapper projection-failure behavior before Plan 24's approval; preserve that compatibility path until cutover. Do not invent a separate debt queue for projections. Pure pre-signing cache metadata transformation may remain internally; it cannot alter immutable identity, digest or locator or write a catalog.

Prove put_entry old/new receipt identity during concurrent overwrite, pre-promotion abort, post-promotion cleanup failure, repeated reconcile and exact expected delete. Direct metadata mapping put/list(filter)/get_metadata/update_metadata/reopen preserves payload bytes and rejects immutable field modification. Do not introduce payload-generation/catalog-revision redesign here.</action>
  <acceptance_criteria>Receipt never identifies a later writer; cleanup runs without waiting for a facade projection. Recoverable cleanup has committed identity and durable debt. Existing put/get behavior, native bytes and manifest version remain unchanged. Application metadata workflow works directly without a custom backend.</acceptance_criteria>
  <verify><automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py -o log_cli=false</automated></verify>
  <done>Supported complete writes and verified reads are sufficient for the next plan's cache slice; sequencing is not exported.</done>
</task>
</tasks>
<threat_model>
Snapshot metadata/payload mixing and delayed stale receipt deletion are integrity threats. Opaque exact expectations, authenticated immutable metadata, bounded reacquisition, private snapshot lifetime and promotion-derived receipts mitigate them. Handler deserialization remains trusted-payload-only and follows verification. Tests cover pre/post-commit failure and no transaction spanning external I/O.
</threat_model>
<verification>Run named tests, scoped Ruff and Phase 3 delta in isolated disposable fixtures. Additive symbols need package export/import tests; no new optional dependency. Stop if the proposed interface exposes lifecycle steps or needs another source of truth.</verification>
<success_criteria>The supported BlobStore interface encapsulates acquisition/publication/cleanup and is proven through direct callers. No whole-engine rewrite, backend expansion or premature full CACH/BACK completion is claimed.</success_criteria>
<output>Create 03-23-SUMMARY.md documenting actual interface/exports, lifetime and failure contracts, public round-trip/race tests and exact commands/commit.</output>
