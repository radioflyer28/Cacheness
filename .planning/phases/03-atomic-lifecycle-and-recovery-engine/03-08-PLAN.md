---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "08"
type: execute
wave: 8
depends_on: [03-07]
files_modified:
  - src/cacheness/storage/coordination.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store_concurrency.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-03, STOR-07]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A refcounted per-key registry orders same-key work and retires entries at zero users; it never becomes the cross-instance correctness boundary (D-17, D-18)."
    - "Forced write/write and write/delete races have one exact-CAS winner; loser cleanup touches only its own candidate and winner bytes remain readable (D-19)."
    - "A blocked key A does not prevent key B from completing, and multi-key acquisition order is deterministic (D-20)."
    - "Reads use only authenticated committed manifests and one private snapshot; exactly one reacquisition is allowed only after authenticated generation movement (D-03, D-19)."
  artifacts:
    - path: src/cacheness/storage/coordination.py
      provides: "KeyCoordinatorRegistry and deterministic multi-key helpers extending Plan 03-05's StoreAdmissionBarrier"
    - path: tests/test_blob_store_concurrency.py
      provides: "Deterministic same-key/distinct-key/read race and registry-retirement contract"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/coordination.py
      via: "ordinary operations hold only logical-key coordination plus instance admission"
      pattern: "KeyCoordinatorRegistry"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/manifest_repository.py
      via: "read reacquisition authenticates both manifests and compares generation"
      pattern: "_load_authenticated_manifest"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Prove deterministic same-key races and complete read acquisition without serializing unrelated keys.

Purpose: Make local coordination bounded and useful while keeping backend exact CAS as the cross-instance winner boundary.
Output: Refcounted per-key coordination, deterministic race matrix, and one-retry committed read acquisition.
</objective>

## Artifacts this phase produces

- Extended internal `coordination.py` symbols: `KeyCoordinatorRegistry`, private key-entry value, and deterministic multi-key context helper. `StoreAdmissionBarrier` already exists from Plan 03-05 and is consumed rather than recreated here.
- New test file `tests/test_blob_store_concurrency.py`.
- Extended `BlobStore` read acquisition helper (internal only); no new public symbol.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-07-SUMMARY.md
</context>

<interfaces>
`threading.Lock` waiter order is unspecified, so tests assert state/CAS invariants rather than winner identity. Plan 03-05 already provides the tested `StoreAdmissionBarrier` consumed by clear and reconciliation. `BlobStore._load_authenticated_manifest` authenticates committed authority. `GuardedHandlerIO.open_snapshot` yields a complete private snapshot. `LifecycleEngine` already performs exact CAS and operation-owned cleanup.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coordinate keys locally and prove backend-CAS race winners</name>
  <depends_on>Plan 03-07 completes all mutation/recovery paths that per-key coordination must order; Plan 03-05's aggregate barrier arrives transitively through Plans 03-06 and 03-07.</depends_on>
  <files>src/cacheness/storage/coordination.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_concurrency.py</files>
  <read_first>
    <file>src/cacheness/storage/clear_recovery.py</file>
    <file>tests/test_clear_recovery.py</file>
    <file>tests/test_sqlite_concurrency.py</file>
    <file>src/cacheness/storage/manifest_repository.py</file>
  </read_first>
  <behavior>
    - "Registry users increment under the registry guard before waiting, decrement in finally, and remove the exact entry at zero; high-cardinality churn returns to baseline."
    - "Two independent stores forced to read the same expected record produce one write/write or write/delete CAS winner and one typed conflict."
    - "Key B finishes while key A is paused at pre-CAS; aggregate admission is not held during ordinary payload work."
    - "Multi-key holds sort stable physical key identities and forced inversions do not deadlock."
  </behavior>
  <action>Extend the existing `coordination.py` and its tested Plan 03-05 `StoreAdmissionBarrier` with a dynamically retained `KeyCoordinatorRegistry` containing an internal guard, exact entry identity, lock, and active/waiter refcount. Acquire registry ownership before waiting and retire in `finally`; deterministic multi-key acquisition sorts the existing stable physical identity. Wire direct lifecycle calls through instance/ordinary barrier admission then per-key coordination, retaining exact backend CAS as the only cross-instance winner. Build event/barrier-controlled write/write, write/delete, distinct-key, clear/post-snapshot, deterministic ordering, and high-cardinality retirement cases with bounded waits/joins and explicit thread error collection; use independent BlobStore instances where correctness could otherwise be hidden by local locks. Do not replace or re-specify the aggregate barrier after Plans 03-05 through 03-07 have consumed it.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_concurrency.py -k "write_write or write_delete or distinct or registry or ordering or clear" -x</automated>
  </verify>
  <acceptance_criteria>Every same-key race has one exact winner and typed loser, key B completes before key A release, no deadlock occurs, and the registry returns to its initial size after high-cardinality and exception paths.</acceptance_criteria>
  <done>Local coordination is bounded and per-key while exact backend CAS remains the race correctness boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reacquire a moved committed generation exactly once</name>
  <depends_on>Task 1 supplies deterministic read/write and read/delete interleaving hooks.</depends_on>
  <files>src/cacheness/storage/blob_store.py, tests/test_blob_store_concurrency.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
  </read_first>
  <behavior>
    - "Read authenticates committed G, acquires one private snapshot, re-authenticates authority, and returns complete G when generation is stable."
    - "If authenticated authority changed during acquisition, the snapshot is closed and one retry may return the new committed generation."
    - "A second movement yields typed lifecycle conflict; same-generation missing/tampered payload and malformed/future/unauthenticated records never retry."
    - "Read/write and read/delete races return a complete snapshot or typed outcome, never mixed bytes/metadata or operation evidence."
  </behavior>
  <action>Refactor direct payload acquisition into an internal loop with exactly two attempts: initial plus one permitted reacquisition. On snapshot missing or second-manifest difference, reload through `_load_authenticated_manifest`; retry only when both records authenticate and generation changed. Preserve Phase 2 ordering and use the first manifest's digest/size/handler against its one live snapshot. Same-generation missing, tamper, invalid signature, malformed/future record, or second generation movement returns the existing precise typed failure without retry/guessing. Add event-controlled read/overwrite and read/delete cases, explicit repository/snapshot/digest/handler call counts, and assertions that operation records are never consulted as read authorities.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_concurrency.py -k "read_write or read_delete or retry" -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "authenticates_validates or missing or tamper or unsupported" -x &amp;&amp; uv run ruff check src/cacheness/storage/coordination.py src/cacheness/storage/lifecycle.py src/cacheness/storage/blob_store.py tests/test_blob_store_concurrency.py tests/test_blob_store_read_contract.py</automated>
  </verify>
  <acceptance_criteria>Call counts prove at most two authenticated acquisitions and one snapshot per attempt; retry occurs only for proven generation change; all Phase 2 same-generation failures preserve their exact type and zero-mutation behavior.</acceptance_criteria>
  <done>Concurrent reads return a complete committed generation or typed lifecycle outcome with one bounded retry and no guessing.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V2 business logic | Exact CAS determines winner and bounded reacquisition determines read result. | Deterministic race matrix. |
| V15 architecture | Refcounted per-key entries and narrow aggregate admission bound synchronization. | Distinct-key and retirement tests. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| local ordering -> shared repository | Local lock ownership cannot be treated as cross-instance authority. |
| first manifest/snapshot -> second authority read | A moved generation may permit one retry; other failures remain fail-closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-21 | Tampering | same-key race | high | mitigate | Independent-instance exact CAS tests with winner-byte and loser-cleanup assertions. |
| T-03-22 | Denial of Service | key registry/admission | high | mitigate | Refcount retirement, bounded waits, distinct-key overlap, and deterministic multi-key ordering. |
| T-03-23 | Tampering | read reacquisition | high | mitigate | Authenticate both generations, retry once only on movement, preserve same-generation integrity failures. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-21 | `uv run pytest -q -o log_cli=false tests/test_blob_store_concurrency.py -k "write_write or write_delete" -x` |
| T-03-22 | `uv run pytest -q -o log_cli=false tests/test_blob_store_concurrency.py -k "distinct or registry or ordering" -x` |
| T-03-23 | `uv run pytest -q -o log_cli=false tests/test_blob_store_concurrency.py -k "read and retry" -x` |
</threat_model>

<verification>
Run complete concurrency and Phase 2 read-contract regressions plus targeted Ruff. Race order comes only from events/barriers with timeouts; no sleep is an oracle.
</verification>

<success_criteria>
- Same-key races have one CAS winner.
- Distinct keys proceed independently.
- Coordination entries retire and multi-key work cannot deadlock.
- Reads retry once only for authenticated generation movement.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-08-SUMMARY.md` when done.
</output>
