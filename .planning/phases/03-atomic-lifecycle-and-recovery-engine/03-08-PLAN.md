---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "08"
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - src/cacheness/storage/coordination.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/error_handling.py
  - tests/test_blob_store_close_contract.py
autonomous: true
requirements: [STOR-05, STOR-07]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`close()` atomically rejects new work, waits up to the explicit configurable deadline for admitted operations, and never releases resources still in use (D-12)."
    - "Timeout/cancellation is deterministic and typed; retrying close converges, resource release occurs exactly once, and close never clears stored data."
    - "Internally owned backends and guarded I/O close once; caller-injected backends remain open (D-12)."
    - "Context-manager exit and repeated close share the same ownership-aware state machine."
  artifacts:
    - path: src/cacheness/storage/coordination.py
      provides: "InstanceAdmission close/drain state machine"
    - path: tests/test_blob_store_close_contract.py
      provides: "Close admission, timeout, ownership, idempotence, and no-clear contract"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/coordination.py
      via: "every public operation enters InstanceAdmission before resource access"
      pattern: "InstanceAdmission"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Finish lifecycle ownership with deterministic instance admission, bounded close drain, and exactly-once resource release.

Purpose: Prevent operations from racing closed resources without transferring ownership or deleting user data.
Output: Idempotent close state machine and complete ownership/timeout contract tests.
</objective>

## Artifacts this phase produces

- New internal `InstanceAdmission` and `InstanceState` symbols in `coordination.py`.
- New public typed `CacheBlobStoreClosedError` and `CacheBlobCloseTimeoutError` with stable reasons, exported through existing error/storage barrels.
- New test file `tests/test_blob_store_close_contract.py`; no other new files or public symbols.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-07-SUMMARY.md
</context>

<interfaces>
`BlobStore` already tracks `_owns_backend`, creates `GuardedHandlerIO`, and cleans constructor failures. `LifecycleLimits.close_wait_seconds` is finite/configurable. Every public direct operation must share one instance admission context before key/store coordination or resource access.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject new operations and drain admitted work deterministically</name>
  <depends_on>Plan 03-07 supplies common operation admission and coordination wiring.</depends_on>
  <files>src/cacheness/storage/coordination.py, src/cacheness/storage/blob_store.py, src/cacheness/error_handling.py, tests/test_blob_store_close_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/coordination.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "An admitted operation paused by an event may finish; once close begins, every new public direct operation fails with CacheBlobStoreClosedError before resource access."
    - "Close waits on a condition/in-flight counter up to injected close_wait_seconds without sleep-based tests."
    - "A deadline expires with CacheBlobCloseTimeoutError, leaves resources open and state closing, and a later close completes after in-flight work ends."
    - "Reentrant/self-deadlocking close is detected and typed rather than waiting forever."
  </behavior>
  <action>Implement `InstanceAdmission` with OPEN/CLOSING/CLOSED state, condition, in-flight count, and operation context that increments/decrements in `finally`. Make every public direct BlobStore operation enter admission before accessing repository, handler I/O, coordination, or keys. `close()` changes state to CLOSING once, rejects later admissions, and waits for zero in-flight up to `LifecycleLimits.close_wait_seconds`; use an injected monotonic clock/wait seam for exact deadline tests. On timeout keep resources live and state CLOSING so retry can converge. Detect close from an admitted operation/thread and fail typed rather than self-deadlocking. Preserve original causes and bounded contexts.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_close_contract.py -k "admission or drain or timeout or retry or reentrant" -x</automated>
  </verify>
  <acceptance_criteria>Events prove second calls fail before resource spies, admitted calls finish safely, exact timeout boundary is deterministic, timed-out close releases nothing, and retry reaches CLOSED.</acceptance_criteria>
  <done>Close admission has a finite configurable deadline and never races resources out from under admitted work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Release only owned resources exactly once without clearing data</name>
  <depends_on>Task 1 reaches CLOSED only after admitted work drains.</depends_on>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/storage/__init__.py, src/cacheness/error_handling.py, tests/test_blob_store_close_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>src/cacheness/storage/operation_repository.py</file>
  </read_first>
  <behavior>
    - "Owned operation repository state is durably flushed, GuardedHandlerIO closes once, and internally created backends close once."
    - "Caller-injected backends are never closed; repeated close and repeated context exit are harmless."
    - "Close never calls clear/delete/reconcile-apply and stored entries remain readable after reopening another store."
    - "Partial resource-close failure remains typed/resumable without double-closing already released resources."
  </behavior>
  <action>Track resource ownership and per-resource released state explicitly from successful construction. After drain, flush only owned durable operation state, close `GuardedHandlerIO`, operation repository handles, and the backend only when `_owns_backend` is true; mark each successful release before advancing so a retry after partial close does not double-close. Do not invoke clear, delete, reconciliation apply, or caller-backend close. Make `__exit__` delegate to this same close state machine. Add spies for internally created JSON/SQLite, injected backends, guarded I/O, operation repositories, clear/delete, and stored bytes; reopen a separate store to prove data persists after close.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_close_contract.py -k "owned or injected or idempotent or no_clear or partial" -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "close or constructor" -x &amp;&amp; uv run ruff check src/cacheness/storage/coordination.py src/cacheness/storage/blob_store.py src/cacheness/storage/__init__.py src/cacheness/error_handling.py tests/test_blob_store_close_contract.py</automated>
  </verify>
  <acceptance_criteria>Every owned close spy fires exactly once, every injected backend spy remains zero, partial release resumes without duplication, clear/delete spies remain zero, and a fresh store reads pre-close data.</acceptance_criteria>
  <done>Close is ownership-aware, idempotent, data-preserving, and safe after partial release failure.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V2 business logic | OPEN/CLOSING/CLOSED transitions and exact resource ownership. | Admission/ownership tests. |
| V15 availability | Finite configurable close waits and retryable partial release. | Timeout/retry tests. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| public operation -> instance resources | New work must be rejected once close begins. |
| BlobStore -> injected resource | Caller ownership must not be transferred by close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-24 | Denial of Service | close drain | high | mitigate | Finite validated deadline, deterministic timeout, reentrant detection, and retryable CLOSING state. |
| T-03-25 | Tampering / Denial of Service | resource ownership | high | mitigate | Explicit ownership/release ledger, exactly-once spies, and no-clear persistence test. |
| T-03-26 | Repudiation | post-close calls | medium | mitigate | Stable typed closed/timeout reasons identify deterministic state. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-24 | `uv run pytest -q -o log_cli=false tests/test_blob_store_close_contract.py -k "timeout or drain or reentrant" -x` |
| T-03-25 | `uv run pytest -q -o log_cli=false tests/test_blob_store_close_contract.py -k "owned or injected or no_clear or partial" -x` |
</threat_model>

<verification>
Run complete close contract, Phase 2 ownership regressions, and targeted Ruff. Deadline tests use injected clocks/events; no sleep determines success.
</verification>

<success_criteria>
- Close rejects new work and drains admitted work to an explicit deadline.
- Timeouts and partial release are typed and retryable.
- Only owned resources close once.
- Stored data remains intact.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-08-SUMMARY.md` when done.
</output>
