---
context: phase
phase: 03-atomic-lifecycle-and-recovery-engine
task: 0
total_tasks: 26
status: blocked
last_updated: 2026-09-06T02:28:37.471Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are confirmed failures, not suggestions. Acknowledge each constraint before continuing.

- [ ] CONSTRAINT: UnifiedCache projection cleanup must be generation-conditional — stale cleanup after a concurrent authority promotion can delete the replacement projection and cascade SQLite custom-metadata links. Use exact authority lineage/CAS, not cache-key removal.
- [ ] CONSTRAINT: Authority tombstones are not live cache entries — never render a tombstoned manifest into a UnifiedCache projection or expose it through query metadata.
- [ ] CONSTRAINT: A returned authority snapshot and compatibility projection must describe the same generation — do not return a pre-repair snapshot alongside a newer projection.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale key-only projection removal | `remove_entry(cache_key)` after a stale absence deletes an independently promoted replacement and SQLite cascades custom links. | blocking | Derive one exact authority snapshot/manifest identity and retire only a matching projection through a conditional generation/locator check. |
| Treating tombstones as live metadata | Facade projection synchronization exposes deleted authority state as a cache entry. | blocking | Filter lifecycle tombstones before any compatibility projection or query result is created. |
| Mixed-generation repair return | Snapshot is read before projection repair while the returned projection may be newer. | blocking | Re-read or construct both return values from one authenticated authority revision after repair. |
| Green broad suite as sole race proof | The frozen isolated full suite passes but did not exercise adversarial interleavings. | advisory | Add deterministic two-instance/barrier regression tests before accepting authority-facade changes. |

<current_state>

Phase 03 has executed Plans 03-01 through 03-13. The final frozen isolated all-extras/dev suite passes at commit `9088df7`, but the phase is blocked by adversarial review and re-verification gaps introduced in the UnifiedCache authority facade. Do not mark Phase 03 complete or update completion tracking.

</current_state>

<completed_work>

- Plan execution through `fe2171c`, including measured lifecycle limits, full-suite isolation, and the honest D-32 Darwin attestation.
- Initial review fixes: `a00f9d7` non-mutating BlobStore Windows preflight; `8642823` Win32 error mapping.
- Authority facade and compatibility repairs: `79a61bb`, `4143242`, `9088df7`.
- Exact release gate at `9088df7`: `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false` exited 0.

</completed_work>

<remaining_work>

- Plan and implement a narrow 03-14 repair for the three final-review projection races.
- Add deterministic regressions for stale replacement deletion/cascade, tombstone rendering, and mixed-generation return.
- Re-run final focused review and goal verification, including the exact isolated full suite.
- Commit current review/verification reports only after deciding their normal GSD tracking treatment; never include protected historical review iterations.

</remaining_work>

<decisions_made>

- BlobStore/LifecycleAuthority remains the sole payload lifecycle authority; UnifiedCache is policy/compatibility projection only.
- Current Darwin Windows result remains `UNAVAILABLE`, `NOT_QUALIFIED`, and `native_evidence: false`; Phase 999.1 requires native Windows Python 3.11 PASS/exit 0.
- Base installs and optional feature dependencies remain distinct; repository-wide release testing uses the frozen isolated all-extras/dev command.

</decisions_made>

<blockers>

- `03-FINAL-REVIEW.md`: CR-01 stale projection/failed-put repair removes concurrent replacement; CR-02 tombstone projected as live; WR-01 mixed snapshot/projection return.
- `03-VERIFICATION.md`: status `gaps_found`, score 3/8 solely because those source-level issues remain, despite the full suite and all retained Phase 3 gates being green.

</blockers>

## Required Reading (in order)

1. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-FINAL-REVIEW.md` — current exact blocker evidence.
2. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md` — current release-gate evidence and gaps verdict.
3. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-POSTFIX-REVIEW.md` — prior façade race findings and failed repair history.
4. `src/cacheness/core.py` — current `_sync_authority_projection`, failed-put repair, and authority snapshot return path.
5. `src/cacheness/storage/blob_store.py`, `src/cacheness/storage/lifecycle.py`, and `src/cacheness/storage/lifecycle_authority.py` — exact authority/tombstone/expectation contracts.
6. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md` — D-22/D-31/D-32 boundaries.

## Infrastructure State

- No background services or async jobs.
- Current host: Darwin. Native Windows tests are intentionally unavailable; do not convert that to a support claim.
- User-owned dirty paths must remain untouched: `.planning/codebase/*`, `.planning/config.json`, `.planning/milestone.lock`, `.claude/`, `CONCERNS.md`, and historical `03-REVIEW*.iter*.md` / `03-REVIEW-FIX*.iter*.md`.

<next_action>

Run `$gsd-resume-work`, read the required reports, then use `$gsd-plan-phase 3 --gaps` (or an equivalently narrow checked plan) to repair the three confirmed projection races before further verification.

</next_action>
