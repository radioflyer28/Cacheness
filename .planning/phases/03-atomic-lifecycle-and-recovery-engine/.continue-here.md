---
context: phase
phase: 03-atomic-lifecycle-and-recovery-engine
task: 4
total_tasks: 8
status: in_progress
last_updated: 2026-09-02T00:52:15.950Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Green tests are not convergence — every full suite was green while deterministic deep review reproduced authority, recovery, pagination, and platform failures — structural mitigation: require a fresh full-scope review after every fix pass and advance only on a clean independent report.
- [x] CONSTRAINT: A bounded result page is not bounded work — `nsmallest` and filtered `scandir` pages returned small results while inspecting the whole namespace or skipping entries — structural mitigation: instrument and assert separate limits for names inspected, bytes read, actions attempted, and terminal no-skip/no-duplicate traversal.
- [x] CONSTRAINT: Cursor namespaces must be stable end to end — mixing lexical cursors with filesystem enumeration order, and full sidecar filenames with bare operation IDs, caused skips and livelock — structural mitigation: use one backend-stable inventory order and one opaque cursor representation through repository, report token, and resume.
- [x] CONSTRAINT: Fix reports are claims, not evidence — thirteen `all_fixed` iterations were followed by independently reproduced blockers — structural mitigation: independently inspect source/callers and reproduce failure boundaries before accepting any report.
- [x] CONSTRAINT: Protected user changes are outside the workflow — seven dirty codebase-map documents and `.planning/milestone.lock` predate this work — structural mitigation: use explicit file lists for every edit/stage/commit and never edit, stage, revert, stash, or delete them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blocking lock before bounded retry | Key initialization enters blocking `flock`/`LockFileEx` before its attempt loop, so the nominal retry bound does not bound elapsed time. | blocking | Add policy deadline plus nonblocking retry/backoff on POSIX and Windows; prove timeout, release-before-deadline, process-loss, and descriptor cleanup. |
| Filesystem-order scan with lexical cursor | `scandir()` order is unspecified, but the cursor rejects lexically earlier names; reverse creation order skipped entries and sidecar cursor namespace mismatch livelocked. | blocking | Implement a genuinely stable backend inventory/index or authenticated snapshot and assert no skip/duplicate under insertion, deletion, restart, and reverse order. |
| Double-charged apply work | Completed orphan sidecars are retired in a pre-pass and charged again from unchanged SAFE findings; exact repository conflicts also escape the per-finding boundary. | blocking | Normalize apply into one action stream, charge once per attempted action, and isolate exact-byte conflicts while continuing later work. |
| Trusting existing test oracles | Tests counted successful deletion effects instead of calls and used lexical creation order, allowing replay and cursor bugs to pass. | advisory | Assert call counts, namespace inspection counts, artifact states, tokens, and later-action progress—not only final state. |

<current_state>

Phase 03 execution is complete: all ten plans have SUMMARY files. Work is paused during post-execution code-review convergence. HEAD is `36a3110 fix(03): harden lifecycle recovery publication`.

The independent reviewer `phase3_rereview14` completed immediately before this handoff; no agent or external job remains running. Current `03-REVIEW.md` is authoritative and uncommitted: iteration 14, `status: issues_found`, 3 Critical, 0 Warning. The prior review was preserved as `03-REVIEW.iter12.md`.

</current_state>

<completed_work>

Completed Tasks:
- Execute Plans 03-01 through 03-10 — Done; final execution tracking commit `1a1fb5d`.
- Resume prior pause and acknowledge constraints — Done; resume commit `1ad69bb`.
- Review/fix convergence through iteration 13 — Done through HEAD `36a3110`; recent commits include `9497a69`, `684a658`, `44dcbb3`, `921324d`, `8ca6209`, and `36a3110`.
- Clarify lifecycle trust contract — Done in D-21/D-22: trusted local-store owner; Windows local coordination limited to one OS user/session.
- Independent review iteration 14 — Done; current report has three blockers.

</completed_work>

<remaining_work>

- Fix all three iteration-14 Critical findings in current `03-REVIEW.md` as fix cycle 14.
- Deep re-review the complete Phase 03 scope plus every newly changed file; repeat fix/review until the report is clean.
- On convergence, remove generated `03-REVIEW*.iter*.md` scratch backups, commit final `03-REVIEW.md` and `03-REVIEW-FIX.md`, and rerun independent Phase 03/full tests, compatibility corpus, Python 3.11, lockfile, and changed-path lint.
- Run Phase 03 Nyquist validation, security audit, prior-phase regression gate, and goal verifier; execute any resulting gap plans/fixes.
- Mark Phase 03 complete and route with `$gsd-next` to Phase 04; continue all remaining phases through milestone audit/completion.

</remaining_work>

<decisions_made>

- D-21: the local store-owning OS principal is trusted not to deliberately delete or rebind every live lifecycle authority object; observable substitution still fails closed.
- D-22: Windows local-store coordination supports one OS user in one interactive or service session; cross-user/service/session sharing is outside this milestone.
- Continue fix/re-review beyond the default cap because independent convergence is a shipping condition.
- Native handlers own payload bytes and formats; Cacheness owns manifests/evidence and must not add a payload wrapper/header.
- Backend exact CAS remains the cross-instance authority; ordinary distinct-key work must not be globally serialized.
- Native Windows execution remains an explicit later CI/platform evidence gate, but source contradictions in the supported topology remain Phase 03 blockers.

</decisions_made>

<blockers>

- CR-01: first-key initialization can wait forever because blocking OS lock acquisition occurs before the bounded attempt loop; the process-global guard also serializes unrelated stores.
- CR-02: pending/sidecar cursors are unstable and not truly bounded; filesystem-order scanning plus lexical cursors can skip entries, the sidecar cursor mixes filename and operation-ID namespaces, and remaining operation/manifest inventories still use whole-namespace `nsmallest` scans.
- CR-03: reconciliation apply double-charges retired orphan sidecars and does not isolate exact lifecycle-conflict races, so safe later actions can be starved or the aggregate apply aborted.
- No human action is required. The next step is a bounded implementation fix followed by fresh independent review.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state and exact next action.
2. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md` — authoritative iteration-14 findings and deterministic probes.
3. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW-FIX.md` — iteration-13 implementation claims and verification.
4. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md` — locked decisions D-01 through D-22.
5. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md` — Phase 03 release gates.
6. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md` and `03-PATTERNS.md` — lifecycle architecture and analogs.
7. `.planning/PROJECT.md` and `AGENTS.md` — project constraints, trust boundary, protected changes.

## Critical Anti-Patterns (do NOT repeat these)

- Do not call a result page bounded unless tests count every directory/key inspected and every byte read.
- Do not combine unspecified backend enumeration order with lexical resume comparisons.
- Do not mix full filenames and operation IDs as cursors for one inventory.
- Do not decrement a shared mutation budget from both a pre-pass and an unchanged later finding.
- Do not allow exact sidecar CAS conflicts to escape the per-finding isolation boundary.
- Do not block on an OS lock before entering the configured bounded retry/deadline policy.
- Do not treat `03-REVIEW-FIX.md` or a green full suite as independent proof.
- Do not touch protected dirty codebase-map files or `.planning/milestone.lock`.

## Infrastructure State

- No servers, watchers, subagents, or async external jobs are running.
- The most recent reviewer completed normally and modified only current `03-REVIEW.md` plus new `03-REVIEW.iter12.md` backup.
- `uv` dependency-enabled tests pass in the supported environment; sandbox-only Unix-socket restrictions have required reruns outside the filesystem sandbox.
- Native Windows execution is unavailable locally and remains assigned to the later CI/platform gate.
- Known unrelated issues already documented in protected `CONCERNS.md`: repository-wide Ruff debt and shutdown-only `SqliteBackend.__del__` import error. The base-install NumPy dependency mismatch is also known and must not be duplicated.

<context>

The broad lifecycle architecture and many earlier fixes are holding: exact authenticated CAS snapshots, native payload formats, canonical manifests, managed no-follow reads/deletes, trusted-owner scope, one-user/session Windows scope, crash-convergent evidence, and tombstone post-effect acknowledgement have survived recent reviews. The remaining work is concentrated in bounded scheduling and per-action isolation. Resume skeptically: fix the exact current three findings, instrument actual work rather than result size, then attempt to falsify the new cursor/action model.

</context>

<next_action>

Start with: read and explicitly acknowledge all five blocking constraints above, then spawn a fresh `gsd-code-fixer` for review iteration 14. Require (1) deadline-aware nonblocking key initialization locks scoped per store/key, (2) a genuinely stable bounded inventory/cursor contract across pending, sidecar, operation, and JSON/memory manifest sources, and (3) one normalized reconciliation action stream with exact conflict isolation and single budget charging. Preserve current review and fix reports before overwrite, commit source/tests only, then deep re-review the full Phase 03 scope.

</next_action>
