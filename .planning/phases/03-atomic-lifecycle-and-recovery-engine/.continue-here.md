---
context: phase
phase: 03-atomic-lifecycle-and-recovery-engine
task: 4
total_tasks: 8
status: in_progress
last_updated: 2026-08-31T14:36:18.064Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Green tests are not sufficient — the existing suite passed while deep review reproduced data loss, deadlocks, reopen poisoning, and Windows/containment failures — structural mitigation: require a fresh deep cross-file review after every fix pass and do not advance until it returns clean.
- [x] CONSTRAINT: Same-process concurrency is not cross-process proof — thread-only tests passed while process-local locks could violate exact CAS — structural mitigation: retain deterministic subprocess races for authority, evidence CAS, admission, and crash/reopen boundaries.
- [x] CONSTRAINT: Review-fix reports are claims, not evidence — two `all_fixed` reports were followed by new blockers — structural mitigation: independently inspect the full changed scope and adversarial failure boundaries after each fixer.
- [x] CONSTRAINT: Protected user changes are outside the workflow — seven dirty codebase-map documents and `milestone.lock` predate this work — structural mitigation: use explicit file lists for every stage/commit and never edit, stage, revert, stash, or delete them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Nested hashed OS locks | Fixed evidence stripes were nested in clear recovery and can self-deadlock or cross-deadlock. | blocking | Use one operation-scoped lease or a rigorously reentrant/totally ordered guard; add forced same-stripe and opposite-order bounded-completion tests. |
| Direct final-name control writes | `O_EXCL` page/checkpoint/chunk writes can leave partial final artifacts after process loss and poison every reopen. | blocking | Publish control evidence through contained temp + fsync + atomic no-replace install + directory fsync; inject partial-write crashes at every record type. |
| Partial cross-platform locking | Admission/evidence gained Win32 support while default JSON manifest authority remained `fcntl`-only. | blocking | Route every authority/evidence lock through one truthful contained POSIX/Win32 abstraction and exercise the actual unavailable-`fcntl` branch. |
| Raw authority lock paths | JSON CAS used raw `mkdir/open`, followed symlinks/reparse points, and could lock different inodes outside containment. | blocking | Open a stable lock descriptor through `ManagedFileOps` no-follow containment; add substitution and two-process exact-CAS tests. |
| Trusting existing oracles | Several tests injected retired metadata/clear seams and passed while no longer exercising authority. | advisory | Test the current manifest-CAS/lifecycle/evidence seams and preserve behavior assertions instead of mechanism mocks. |

<current_state>

Phase 03 execution is complete: all ten plans have SUMMARY files and HEAD contains two review-fix commits (`6905569`, `37c1afb`). The active work is post-execution code-review convergence. The global iteration-4 fixer was interrupted immediately for this pause; it produced no commit and no source/test working-tree changes.

Current `03-REVIEW.md` is uncommitted and authoritative: `status: issues_found`, 4 Critical, 2 Warning. Current HEAD is `37c1afb`.

</current_state>

<completed_work>

Completed Tasks:
- Execute Plans 03-01 through 03-10 — Done; final plan tracking commit `1a1fb5d`.
- Independent full Phase 03 build/test gate — Done; full pytest green before review fixes.
- Deep review pass 1 — Done; 6 Critical + 1 Warning.
- Fix pass 1 — Done in `6905569`.
- Deep review pass 2 — Done; 3 Critical + 3 Warnings.
- Fix pass 2 — Done in `37c1afb`.
- Deep review pass 3 — Done; current report has 4 Critical + 2 Warnings.

</completed_work>

<remaining_work>

- Global fix iteration 4: fix every finding in current `03-REVIEW.md`.
- Deep re-review the full 27-file scope plus any newly changed files; repeat fix/review until clean.
- On convergence, remove spent `03-REVIEW*.iter*.md` scratch backups, commit final `03-REVIEW.md` + `03-REVIEW-FIX.md`, and rerun Phase 03/full pytest, compatibility corpus, and changed-file lint.
- Run Phase 03 Nyquist validation, security audit, prior-phase regression gate, and goal verifier; execute any resulting gap plans/fixes.
- Mark Phase 03 complete and route via `$gsd-next` to Phase 04; continue the milestone lifecycle.

</remaining_work>

<decisions_made>

- Continue fix/re-review beyond the built-in three-iteration auto cap because convergence is a shipping condition.
- Interrupted `PREPARED` clear inventory must abort and retire only non-authoritative evidence; it must never resume live lexical paging after admission epoch loss.
- Post-authority failures are forward cleanup debt, not rollback to live data.
- Reconciliation tokens use bounded nonce-based authenticated encryption with a declared dependency.
- Native handler file formats remain unwrapped; Cacheness owns manifests/evidence, not a payload header.
- Repository lint baseline is documented and nonblocking only where proven pre-existing; changed-path lint must remain green.

</decisions_made>

<blockers>

- Technical blocker: current deep review has four shipping blockers and two warnings. No human action is required; resume the fix/re-review loop.
- Current blockers are detailed in `03-REVIEW.md`: nested evidence-lock deadlocks; crash-nonatomic control evidence creation; Windows-incompatible default JSON authority lock; raw/symlink-following JSON lock path; barrier registration identity race; exception-masking unlock behavior.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — exact machine-readable pause state and next action.
2. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md` — current six findings; authoritative fix scope.
3. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW-FIX.md` — iteration-2 claims and prior implementation context.
4. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md` — locked Phase 03 decisions D-01 through D-20.
5. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md` — validation commands and release evidence.
6. `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md` and `03-PATTERNS.md` — architecture and implementation analogs.
7. `AGENTS.md` — project-wide constraints and protected architecture.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat an `all_fixed` REVIEW-FIX report as convergence; always spawn a fresh deep reviewer over every changed file.
- Do not simulate Windows by patching only one coordination helper while real JSON manifest CAS still imports host `fcntl`.
- Do not use per-transition lock files or independently hashed nested stripes; bound lock state and prove cross-process behavior.
- Do not publish authenticated control evidence directly at its final name; crash atomicity must be part of the write primitive.
- Do not resume a live manifest inventory after losing its admission epoch.
- Do not touch protected dirty planning-map files or `milestone.lock`.

## Infrastructure State

- No servers, watchers, or async external jobs are running.
- The `phase3_review_fix4` subagent was explicitly interrupted and is no longer authoritative.
- Worktree isolation previously degraded to deliberate sequential execution because HEAD diverged from `origin/HEAD`.
- `uv` may require approved elevated access to its existing cache outside the workspace.
- Known unrelated issues: repository-wide Ruff baseline and shutdown-only `SqliteBackend.__del__` import error; both already documented in protected `CONCERNS.md`.

<context>

The phase implementation is broad and existing tests are green, but adversarial deep review repeatedly found failures at process-loss, cross-process, containment, and platform boundaries. Resume with skepticism: fix the exact current findings, then deliberately attempt to falsify the fixes. The desired architecture remains BlobStore-owned storage lifecycle with UnifiedCache as a later policy layer; do not narrow correctness to same-process behavior or reintroduce legacy metadata/clear authority.

</context>

<next_action>

Start with: read and explicitly answer the four blocking-constraint questions (what each anti-pattern is, how it manifested, and what structural mechanism prevents recurrence), then spawn a fresh `gsd-code-fixer` for all 4 Critical + 2 Warning findings in current `03-REVIEW.md` as global iteration 4. After its commits, deep re-review the full 27-file scope plus all newly changed files.

</next_action>
