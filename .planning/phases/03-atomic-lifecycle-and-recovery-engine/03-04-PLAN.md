---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "04"
type: execute
wave: 4
depends_on: [03-02, 03-03]
files_modified:
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/error_handling.py
  - tests/test_blob_store_atomic_lifecycle.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-07]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Create and overwrite use immutable candidates and exact CAS; the loser receives a typed conflict and can reclaim only its own residue (D-01, D-02, D-19)."
    - "Delete publishes a signed tombstone against the exact observed record before reclaiming payload bytes and conditionally retiring the tombstone (D-09, D-11)."
    - "Deleting an already absent key remains an idempotent non-error with the compatible `False` return, while a repeated same-tombstone delete resumes and a newer generation conflicts."
    - "Every ordinary or process-loss fault leaves the last authoritative generation readable or a resumable signed tombstone/cleanup debt (D-04, D-07, D-08)."
  artifacts:
    - path: src/cacheness/storage/lifecycle.py
      provides: "Complete put/overwrite/delete mutation state machine"
    - path: tests/test_blob_store_atomic_lifecycle.py
      provides: "Write/overwrite/delete fault and crash/reopen matrix"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle.py
      via: "put and delete share one coordinator and authenticated expectation path"
      pattern: "(put|delete)"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/manifest_repository.py
      via: "committed/tombstoned authority is conditionally published and retired"
      pattern: "(publish_if_expected|remove_if_expected)"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Expand the tracer into the complete single-key create, overwrite, and tombstone-first delete state machine.

Purpose: Make destructive direct BlobStore mutations converge under faults and stale contenders.
Output: One shared mutation coordinator with exact write/delete authority and resumable cleanup.
</objective>

## Artifacts this phase produces

- Completed internal `LifecycleEngine.put`, `LifecycleEngine.delete`, `LifecycleEngine.recover_operation`, and operation-specific cleanup helpers in `lifecycle.py`.
- New public errors `CacheBlobUnsupportedLifecycleError` and `CacheBlobOperationConflictError` only if existing precise subclasses cannot represent these branches; each carries a stable `CacheReason` and compatible broad base.
- Expanded `tests/test_blob_store_atomic_lifecycle.py`; no new files or other public symbols.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-02-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-03-SUMMARY.md
</context>

<interfaces>
`BlobStore.put(data, key=None, metadata=None)` preserves compatible keys/returns. `BlobStore.delete(key) -> bool` documents `True` when removed and `False` when not found. `BlobManifestV1` already admits `committed` and `tombstoned` states; tombstones remain signed canonical records and retain exact old payload identity for proven reclamation.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete create and overwrite fault convergence</name>
  <depends_on>Plans 03-02 and 03-03 provide exact CAS and complete evidence/policy contracts.</depends_on>
  <files>src/cacheness/storage/lifecycle.py, src/cacheness/storage/blob_store.py, src/cacheness/error_handling.py, tests/test_blob_store_atomic_lifecycle.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/operation_record.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "Creating absent and overwriting committed G each publish one immutable candidate and exact expectation; a stale contender cannot overwrite the winner."
    - "Failures before CAS preserve absence/G and detectable owned candidate; failures after CAS preserve the new generation and explicit old-payload cleanup debt."
    - "CAS acknowledgement ambiguity is resolved on reopen from authenticated authority; the engine rolls forward only when new generation matches."
    - "Loser cleanup never touches the winner, the old locator before winner authority, or another operation's residue."
  </behavior>
  <action>Generalize the tracer into explicit create/overwrite transitions while retaining one `LifecycleEngine`. Load and authenticate the current committed manifest once to construct exact expectation and prior locator; keep payload generations immutable and use operation-specific locators. After a winning CAS, checkpoint authority before reclaiming the previous locator; if checkpoint acknowledgement fails, recovery re-authenticates the manifest rather than rolling it back. A stale CAS raises typed conflict and attempts only provenance-validated cleanup of its own candidate; cleanup failure becomes recoverable debt without masking the conflict/winner. Implement the complete validation matrix's serialization, evidence, candidate stream/durability, verification, CAS compare/write/ack, authority checkpoint, reclamation, and retirement seams with `Exception` and meaningful `BaseException` reopen cases.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_atomic_lifecycle.py -k "write or overwrite or acknowledgement or winner or loser" -x</automated>
  </verify>
  <acceptance_criteria>Each fault row asserts exact authority, payload, record, and cleanup ownership after live failure and fresh reopen; one complete generation remains readable; no test accepts a generic exception without winner-state assertions.</acceptance_criteria>
  <done>Create and overwrite expose only absence/old-complete or new-complete and retain all cleanup debt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish tombstones before delete reclamation and converge repeated calls</name>
  <depends_on>Task 1 completes shared operation transitions and cleanup classification.</depends_on>
  <files>src/cacheness/storage/lifecycle.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_atomic_lifecycle.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>tests/test_clear_recovery.py</file>
  </read_first>
  <behavior>
    - "Delete authenticates committed G, creates evidence, CAS-publishes a signed tombstone retaining G's locator/digest identity, then reclaims and conditionally retires it."
    - "A fault at tombstone CAS leaves G; a fault after tombstone authority leaves resumable absence intent and never restores G metadata over missing bytes."
    - "True absence returns False without error; same validated tombstone resumes; a newer committed generation conflicts and survives."
    - "Missing already-reclaimed payload is complete only when exact tombstone/evidence still proves ownership."
  </behavior>
  <action>Route `BlobStore.delete` through the shared engine. Construct/sign the tombstone from the authenticated committed manifest and publish it against its exact record before any payload delete. Reclaim via contained owned I/O, checkpoint, then `remove_if_expected` the exact tombstone and retire evidence. Preserve compatible `False` for a truly absent key while treating it as successful idempotent convergence. Repeated calls encountering the same authenticated tombstone resume cleanup; calls that observed G but now see another committed generation raise conflict and perform no deletion. Add the complete delete fault/reopen matrix, including late tombstone retirement racing a new generation.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_atomic_lifecycle.py -k "delete or tombstone or absent" -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "delete" -x &amp;&amp; uv run ruff check src/cacheness/storage/lifecycle.py src/cacheness/storage/blob_store.py src/cacheness/error_handling.py tests/test_blob_store_atomic_lifecycle.py</automated>
  </verify>
  <acceptance_criteria>Tombstone bytes are signed and authoritative before payload reclamation; every injected delete boundary converges after reopen; absent returns False; newer generations survive stale finalizers.</acceptance_criteria>
  <done>Delete is tombstone-first, exact-record conditional, compatible, idempotent, and resumable.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V2 business logic | Explicit pre/post-authority state machine and exact expected transitions. | Full fault matrix. |
| V5 file handling | Only provenance-validated contained locators are reclaimed. | Winner/loser and tombstone tests. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| contender expectation -> shared manifest | Stale contenders must not overwrite or revoke a current winner. |
| tombstone authority -> payload cleanup | Signed deletion intent must precede destructive payload work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-11 | Tampering / Denial of Service | losing writer cleanup | high | mitigate | Operation-specific immutable locators, exact CAS, and provenance revalidation prevent winner deletion. |
| T-03-12 | Tampering | delete ordering | high | mitigate | Signed tombstone CAS precedes payload reclamation and retirement is exact-record conditional. |
| T-03-13 | Repudiation | partial mutation result | medium | mitigate | Typed pre/post-authority outcome includes bounded operation/generation context and preserved cause. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-11 | `uv run pytest -q -o log_cli=false tests/test_blob_store_atomic_lifecycle.py -k "loser or winner or overwrite" -x` |
| T-03-12 | `uv run pytest -q -o log_cli=false tests/test_blob_store_atomic_lifecycle.py -k "delete or tombstone" -x` |
</threat_model>

<verification>
Run all write/overwrite/delete fault and reopen cases, Phase 2 mutation regressions, and targeted Ruff. No BaseException case may inspect the interrupted live object's cleanup as its oracle.
</verification>

<success_criteria>
- Writes and overwrites preserve old/new completeness.
- Delete publishes signed intent before reclamation.
- Repeated delete preserves its legacy absent return and converges safely.
- Stale cleanup never removes a winner.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-04-SUMMARY.md` when done.
</output>
