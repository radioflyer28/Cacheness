---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "19"
type: execute
wave: 19
depends_on: ["03-18"]
files_modified:
  - src/cacheness/metadata.py
  - src/cacheness/error_handling.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - tests/test_sqlite_metadata_bootstrap_atomicity.py
  - tests/test_cached_query_meta.py
  - tests/test_phase3_postreview_concurrency.py
  - tests/test_sqlite_concurrency.py
  - tests/test_sqlite_concurrency_temp.py
  - tests/test_public_api_contract.py
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Committed Tasks 1-2 remain the sole implementation history for atomic metadata bootstrap and fail-closed query decoding; executors begin at Task 3 and never replay commits 828d13b, 8439e8c, 2ddbba1, or 80c38ca."
    - "On a fresh authority root, same-instance first-use threads enter the existing per-path writer-admission order before any contention-prone connection PRAGMA or schema bootstrap, and independent instances in the same process share that order without serializing payload work."
    - "One original 0.187-second absolute authority deadline covers bootstrap admission, connection open/configuration, schema initialization or validation, writer dispatch, and SQLite acquisition; exhaustion raises CacheBlobLifecycleTimeoutError with an exact stage and the originating sqlite3 error as cause when SQLite supplied one."
    - "Schema readiness is published only after the initialization transaction commits and the same connection validates the application ID, schema version, and store identity; visible leaf files never by themselves mean preflight-ready authority."
    - "Fresh same-instance threads and independent spawned processes deterministically converge or return the documented bounded typed timeout, never raw OperationalError or the generic lifecycle_authority_open classification; fork-reset semantics and ticket retirement remain exact."
    - "The public concurrent_metadata_access schedules complete with meaningful progress/no-corruption accounting, and the additive metadata_corrupt CacheReason is present in the exact public enum contract."
    - "Focused workspace tests and every final release gate pass from committed HEAD in a temporary tracked-clean worktree while user-owned fixture WAL/SHM sidecars remain untouched; authority defaults, benchmark envelopes, public APIs, Windows status, and live PostgreSQL claims do not change."
  artifacts:
    - path: src/cacheness/metadata.py
      provides: "SQLite-native atomic metadata bootstrap, exact schema validation, bounded busy translation, and fail-closed cache_key_params decoding"
    - path: src/cacheness/error_handling.py
      provides: "Stable machine-readable metadata-corruption reason carried by CacheIntegrityError"
    - path: tests/test_sqlite_metadata_bootstrap_atomicity.py
      provides: "Barrier-driven 64-thread, spawned-process, timeout, wrong-state, and provisional-resource bootstrap evidence"
    - path: tests/test_cached_query_meta.py
      provides: "Cached/unwrapped public malformed/type/size/shape/filter parity and evidence-preservation regressions"
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Admission-aware first-use connection/bootstrap state machine with one deadline and precise timeout stages"
    - path: tests/test_phase3_postreview_concurrency.py
      provides: "Deterministic same-instance, independent-process, readiness-publication, and cause/stage regressions"
    - path: tests/test_sqlite_concurrency.py
      provides: "Strict public concurrent_metadata_access qualification through the real UnifiedCache path"
    - path: tests/test_sqlite_concurrency_temp.py
      provides: "Duplicate historical public concurrency policy kept behaviorally identical"
    - path: tests/test_public_api_contract.py
      provides: "Exact additive CacheReason metadata_corrupt public contract"
  key_links:
    - from: src/cacheness/metadata.py
      to: tests/test_sqlite_metadata_bootstrap_atomicity.py
      via: "BEGIN IMMEDIATE surrounds schema check/create/validate and stats insertion on the same connection across independent engines/processes"
      pattern: "BEGIN IMMEDIATE|create_all|schema_bootstrap|sqlite_errorcode"
    - from: src/cacheness/metadata.py
      to: src/cacheness/error_handling.py
      via: "Persisted cache_key_params bounds/type/decode/root-shape failures become CacheIntegrityError with one stable reason and cache key"
      pattern: "METADATA_CORRUPT|CacheIntegrityError|cache_key_params"
    - from: src/cacheness/metadata.py
      to: tests/test_cached_query_meta.py
      via: "Every exact live pair is decoded and validated inside one backend-owned read transaction before any empty or filtered query can render it"
      pattern: "live_pairs|json_loads|Mapping|checkedout"
    - from: src/cacheness/core.py
      to: src/cacheness/metadata.py
      via: "Core catches only operational CacheMetadataError, so backend integrity failures propagate through the unchanged public facade"
      pattern: "except CacheMetadataError|query_entries_by_key_params"
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_phase3_postreview_concurrency.py
      via: "The existing per-path FIFO admits fresh connection preflight before EXCLUSIVE bootstrap and publishes schema_ready only after commit plus validation"
      pattern: "_admit_writer|_schema_ready|BEGIN EXCLUSIVE|connection_preflight|schema_validate"
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_sqlite_concurrency.py
      via: "Real public UnifiedCache first-use contention receives bounded typed authority outcomes rather than raw lifecycle_authority_open errors"
      pattern: "concurrent_metadata_access|CacheBlobLifecycleTimeoutError|stage"
    - from: src/cacheness/error_handling.py
      to: tests/test_public_api_contract.py
      via: "The already implemented metadata_corrupt reason is asserted by the exhaustive enum-value compatibility set"
      pattern: "METADATA_CORRUPT|metadata_corrupt|expected_reasons"
  prohibitions:
    - "[UNVERIFIED] Schema bootstrap cannot rely on process-global serialization, repeated sleeps/retries, arbitrary OperationalError suppression, or an ORM model/schema change."
    - "[UNVERIFIED] Invalid cache_key_params cannot be rendered as an empty mapping, hidden as an operational miss, or removed/repaired by a query."
    - "[UNVERIFIED] This gap closure cannot change LifecycleAuthority timing, release benchmark evidence, native Windows qualification, or PostgreSQL claims."
    - "[UNVERIFIED] Authority preflight cannot gain a new process-global or whole-cache lock, a second deadline, an automatic retry/sleep loop, or a raw-error swallowing path."
    - "[UNVERIFIED] Final qualification cannot delete, move, modify, or depend on user-owned untracked compatibility fixture sidecars."
---

<objective>
Preserve the completed metadata-bootstrap and corrupt-query fixes, then move LifecycleAuthority's contention-prone first-use connection preflight inside bounded admission and qualify the complete phase from clean tracked state.

Purpose: Commits `828d13b`, `8439e8c`, `2ddbba1`, and `80c38ca` closed the two review findings that originally created Plan 19. Fresh execution evidence then exposed a different authority race: `_transaction()` opens, configures, and validates SQLite before `_admit_writer()`, while initialization uses `PRAGMA journal_mode=DELETE` and `BEGIN EXCLUSIVE`. An overlapping first-use connection can therefore spend about 0.242 seconds blocked at `PRAGMA journal_mode` under the unchanged 0.187-second budget and emerge as an imprecise `lifecycle_authority_open` error. The same-instance `_schema_lock` is acquired only after configuration, so it cannot prevent the collision.
Output: The four completed commits retained without replay, one admission-aware authority bootstrap/preflight task with deterministic thread/process and public-facade schedules, the additive enum contract assertion, and clean-worktree release qualification that preserves workspace fixture sidecars.

Decision fidelity: D-01 through D-04 keep immutable generations and exact committed authority visibility. D-05 through D-08 keep bounded intent, validated provenance, recoverable residue, and typed cleanup outcomes. D-09 through D-12 retain tombstone, clear, idempotent cleanup, and ownership-aware close semantics. D-13 through D-16 require bounded evidence-driven reconciliation without payload guessing. D-17 through D-20 retain retiring local coordination, backend CAS authority, deterministic same-key races, and distinct-key concurrency. D-21 and D-22 preserve the trusted local-owner boundary and limited Windows topology. D-23 through D-31 retain one LifecycleAuthority, stdlib SQLite authority ownership, immutable payload work outside transactions, short database-native commits, measured budgets, and no platform-lock authority. D-32 remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false on Darwin.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-18-SUMMARY.md
@src/cacheness/metadata.py
@src/cacheness/error_handling.py
@src/cacheness/storage/sqlite_lifecycle_authority.py
@src/cacheness/config.py
</context>

## Partial Execution Baseline — Do Not Replay

Tasks 1 and 2 are complete in four commits. They remain in this plan only as immutable implementation history:

| Completed task | RED commit | Production commit | Preserved result |
|---|---|---|---|
| Task 1 — atomic file-backed SQLite metadata bootstrap | `828d13b` | `8439e8c` | Independent constructors/processes converge on one validated metadata schema under a bounded SQLite-native transaction. |
| Task 2 — fail-closed live cache-key parameter decoding | `2ddbba1` | `80c38ca` | Empty and filtered public queries share one typed corruption policy while preserving exact evidence. |

The executor must begin at Task 3. It must not edit, amend, squash, replay, or recommit either completed task or its commits. Task 3 owns only the newly diagnosed LifecycleAuthority preflight race, its public enum-contract assertion, deterministic regression coverage, and final qualification.

<tasks>

<completed_task number="1" commits="828d13b,8439e8c" replay="forbidden">
  <name>Task 1: Serialize fresh metadata schema bootstrap with SQLite itself</name>
  <files>src/cacheness/metadata.py, src/cacheness/error_handling.py, tests/test_sqlite_metadata_bootstrap_atomicity.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/test_cached_query_meta.py</file>
    <file>tests/test_sqlite_bootstrap_concurrency.py</file>
    <file>tests/test_sqlite_authority_admission.py</file>
    <file>tests/test_phase3_postreview_concurrency.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md</file>
  </read_first>
  <behavior>
    - "A Barrier releases 64 independent SqliteBackend constructors against one nonexistent file; all 64 construct and close boundedly with no OperationalError, then the database has WAL mode, exact current cache_entries/cache_stats columns and required indexes, and exactly one stats row with id=1."
    - "A multiprocessing `spawn` Barrier releases at least eight child processes against another truly empty path; every child constructs/closes its own engine, exits zero within bounded joins, and the parent validates the same single schema/stats/WAL outcome without inherited locks or seeded state."
    - "A zero-mutation, WAL-aware read-only preflight distinguishes absent/empty, exact legacy, exact current, and incompatible/foreign/partial state before journal-mode setup. For admissible state, schema absence checks, model DDL, post-DDL validation, and INSERT OR IGNORE stats initialization all use one connection after an explicit SQL `BEGIN IMMEDIATE`; a second constructor waits at SQLite, then revalidates/converges without a table-exists catch or retry."
    - "With one private absolute bootstrap budget patched to immediate expiry, externally owned lock schedules at WAL-aware preflight, WAL acquisition, and schema acquisition raise CacheMetadataError with operation `metadata_bootstrap`, stage exactly `preflight`, `wal_mode`, or `schema_lock`, original budget/elapsed context, and the applicable caused sqlite3 or SQLAlchemy/sqlite3 SQLITE_BUSY or SQLITE_LOCKED error."
    - "Existing exact legacy and already-unsupported cache_entries layouts preserve their current CacheLegacyFormatError policy; a wrong cache_stats object, required-name table/view/index collision, unrelated foreign/partial schema, or truncated/non-database file raises CacheIntegrityError(metadata_corrupt) before publication. Original bytes/object catalog/journal mode/directory contents remain unchanged for preflight rejection, transactional DDL is rolled back for a forced mid-bootstrap failure, and every provisional engine/connection is disposed."
    - "Plan 18's connect listener remains limited to read-safe connection-local PRAGMAs; WAL and optimize remain constructor-owned, no page-size policy changes, and SqliteBackend(':memory:') retains its existing behavior."
  </behavior>
  <action>Create `tests/test_sqlite_metadata_bootstrap_atomicity.py` with RED schedules before changing production. Use a truly nonexistent database path for each schedule. Run the 64-thread race inside one disposable spawned child: release 64 manually created workers from one Barrier, have each construct its own `SqliteBackend`, report either the real exception object or success, capture its engine before close, and close in `finally`; use daemon workers plus a child-local monotonic deadline and bounded joins, never a `ThreadPoolExecutor` context whose shutdown can wait forever. The parent bounds the whole child, records a live child as a hard deadlock failure, and terminates/joins only for cleanup. Separately use `multiprocessing.get_context("spawn")`, a process-safe Barrier, and at least eight children for the independent-process schedule; construct only inside each child, return structured results through a queue/pipe, use bounded joins, and terminate only as failure cleanup. Do not seed WAL, tables, or a first backend before either race, and do not count a skip or terminated worker as evidence.

After each successful race, inspect with an independent sqlite3 connection: `PRAGMA journal_mode` is WAL for file-backed storage; `sqlite_master` contains the two required tables rather than colliding views; `PRAGMA table_xinfo` matches the exact ordered CacheEntry and CacheStats names, declared SQLite types, not-null flags, and primary-key positions; `PRAGMA index_list` plus `PRAGMA index_xinfo` prove all four named CacheEntry indexes have the expected table, uniqueness/partial flags, ordered columns, and DESC direction for `idx_list_entries`; and `SELECT COUNT(*) FROM cache_stats WHERE id=1` is exactly one with no other stats rows. Permit additional custom-metadata tables only when both required core tables are already exact; an otherwise empty database must have no unrelated user object before it can be initialized. Require every constructor/child to succeed, every backend to close, and all captured engines/pools to have zero checked-out connections. Add an existing-file convergence case so repeated construction is idempotent without schema or index mutation.

Refactor `_bootstrap_database(engine)` around three explicit phases without changing ORM models. Start one private absolute deadline before inspection. First, before constructing/opening a writable SQLAlchemy engine for a pre-existing path, replace the current `immutable=1` classification read with a WAL-aware `mode=ro` sqlite3 URI plus `PRAGMA query_only=ON`: immutable SQLite readers ignore WAL frames and therefore cannot classify a concurrently committed or hostile WAL database correctly. Inspect through that read-only connection using the remaining deadline, including the logical state represented by an existing WAL, and classify exact legacy, exact current, truly empty, and incompatible/foreign/partial object catalogs; exact-current validation includes the required index definitions and a cache_stats table that is empty or contains only singleton id=1. A BUSY/LOCKED inability to obtain the preflight snapshot consumes the same deadline and receives stage `preflight`; another read/open failure is not permission to call the file empty. Snapshot the durable main database and WAL bytes when present plus logical object catalog and journal mode before inspection. SQLite may use the existing `-shm` file for transient reader locks, so do not compare its lock-bearing bytes as evidence; require that rejection leaves main/WAL bytes, logical objects, and journal mode unchanged and performs no writable-engine/bootstrap operation. Tests must include a hostile/partial schema committed only in WAL to prove preflight does not repeat the current immutable-reader blind spot. This preflight is a safety gate, while the later locked inspection remains correctness authority. Second, for admissible file-backed state, keep Plan 18 WAL establishment outside a transaction because SQLite cannot change journal mode inside one, apply the remaining milliseconds from the same constructor-owned deadline, execute `PRAGMA journal_mode=WAL`, consume and require the returned `wal` value, and translate only primary BUSY/LOCKED exhaustion as stage `wal_mode`. Third, on one connection apply the remaining deadline-derived busy timeout, execute literal SQL `BEGIN IMMEDIATE` before any decisive schema inspection or `create_all`, and on that same connection: re-inspect the current required objects, accept only wholly absent or exact-current core state, reject any incompatible/partial collision before DDL, call `Base.metadata.create_all(connection)` while the database writer lock makes its check-first sequence atomic, validate the exact required tables/columns/primary keys/index definitions, validate that cache_stats is empty or contains only the singleton id=1 row, insert id=1 with existing conflict-ignore semantics when absent, revalidate exactly one total stats row with id=1, run any retained constructor-owned optimize work, then repeat the exact required table/column/PK/index and singleton-stats validation as the final operation immediately before explicit commit. Never delete or normalize an unexpected stats row. Explicitly roll back on every post-BEGIN failure. Do not rely on `engine.begin()` alone: the implementation and a source/statement-order assertion must prove `BEGIN IMMEDIATE` is sent before SQLAlchemy's schema inspection/DDL on the same `Connection`. Publish `self.engine` and `SessionLocal` only after commit and final validation; otherwise close the active connection and dispose the provisional engine exactly once.

The finite metadata-bootstrap budget is one private 30-second absolute monotonic deadline matching the existing metadata connection policy; it is not LifecycleLimits and must not reuse or change the 0.187-second LifecycleAuthority deadline. Convert only remaining time conservatively to SQLite milliseconds before preflight, WAL, and again immediately before `BEGIN IMMEDIATE`, never minting a per-stage timeout. Tests may monkeypatch the private budget/clock to deterministic immediate expiry while an independent sqlite3 connection owns the relevant lock, without sleeps. Translate only primary SQLite `SQLITE_BUSY`/`SQLITE_LOCKED` acquisition failures into `CacheMetadataError` with `backend=sqlite`, `operation=metadata_bootstrap`, stage `preflight`, `wal_mode`, or `schema_lock`, path, original budget, elapsed/remaining context, and the original database exception as `__cause__`; SQLAlchemy paths retain a `DBAPIError` whose `.orig` is sqlite3.OperationalError, while the pre-engine sqlite3 preflight retains its direct sqlite3.OperationalError cause. Mask extended SQLite codes to their primary code; if the cause lacks a recognized code, propagate it. Never identify busy state by English message, ignore `table already exists`, loop/retry DDL, or catch arbitrary OperationalError as convergence.

Before and after DDL, classify database objects from the locked connection. Add `CacheReason.METADATA_CORRUPT = "metadata_corrupt"` in `error_handling.py` as one additive stable reason shared with Task 2, and use the already-public `CacheIntegrityError` for newly classified incompatible metadata database bytes/schema with `backend=sqlite`, `operation=metadata_bootstrap`, and stage `preflight` or `schema_validation`. Put `reason: CacheReason.METADATA_CORRUPT.value` in the ordinary context mapping; do not pass a new `reason=` keyword to the base `CacheIntegrityError` or change its public constructor. Preserve both the exact read-only legacy-layout path and the existing `CacheLegacyFormatError(unsupported_legacy_layout)` result for a cache_entries column layout that `_detect_legacy_layout` already rejects; do not silently change that public compatibility boundary. A table/view/index-name collision, wrong cache_stats/current index shape, one-present/one-absent required table, foreign user schema without a complete exact core schema, missing required property after DDL, or malformed/non-database file must raise the new integrity result and leave pre-existing main/WAL bytes, logical object catalog, and journal mode unchanged; it is not an empty database to rebuild. Unrelated DBAPI/programming exceptions remain caused/propagated rather than relabeled as convergence. Add deterministic hostile-state fixtures that snapshot all of those durable observables and assert the exact legacy-versus-integrity exception split. For transactional-DDL proof, patch the create boundary to issue one sentinel `CREATE TABLE` through the already locked bootstrap `Connection` and then raise; assert the sentinel and every partial core object are absent after rollback, proving SQLite DDL participated in the explicit transaction rather than merely proving a pre-DDL exception. Capture the provisional engine from the factory and prove zero checkout/disposal. Preserve public constructor signatures, Plan 18 read-safe pool checkout, cached wrapper behavior, no-follow/containment behavior already enforced by callers, and D-18/D-26/D-28 database-native authority and short-transaction constraints.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_metadata_bootstrap_atomicity.py tests/test_cached_query_meta.py::test_public_query_meta_uses_fresh_read_only_pool_connections_under_writer -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>All 64 thread constructors and all spawned-process constructors converge from a genuinely empty path; the final WAL/schema/index/stats state validates exactly; one absolute preflight/WAL/schema budget yields staged typed/caused exhaustion; hostile/partial/foreign state, including a schema committed in WAL, is rejected before journal conversion and leaves durable main/WAL bytes, logical objects, and journal mode unchanged; injected post-DDL failure rolls its table back; provisional resources retire; no process-global lock, retry/sleep loop, ignored DDL error, ORM schema change, or authority-budget change exists.</acceptance_criteria>
  <done>Fresh review CR-01 is closed by one bounded SQLite-native bootstrap transaction that is safe across independent engines and spawned processes.</done>
</completed_task>

<completed_task number="2" commits="2ddbba1,80c38ca" replay="forbidden">
  <name>Task 2: Reject corrupt live cache-key parameters consistently across every query shape</name>
  <files>src/cacheness/metadata.py, tests/test_cached_query_meta.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/json_utils.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>tests/test_cached_query_meta.py</file>
    <file>tests/test_query_meta.py</file>
    <file>tests/test_query_meta_security.py</file>
    <file>tests/test_cache_integrity.py</file>
  </read_first>
  <behavior>
    - "For both enable_memory_cache false and true, replacing one exact live row's cache_key_params with malformed JSON makes query_meta(), query_meta(name='value'), and query_meta(score=1) raise CacheIntegrityError with reason `metadata_corrupt`, operation, field, and affected cache key."
    - "The same typed result occurs before JSON filtering for SQL NULL/integer/real/BLOB storage classes (including valid- and invalid-UTF-8 BLOBs), invalid UTF-8 text bytes, a value larger than exactly MAX_MANIFEST_BYTES, a JSON tree exceeding the canonical depth/item/node/string bounds, and valid JSON whose root is null, scalar, string, list, or another non-dict shape."
    - "Every exact live key/locator row is validated before filters, so corruption cannot disappear because a filter would not match; valid rows retain existing nested/string/bool/numeric comparison and empty-filter result behavior."
    - "After every failed query, direct raw SQL returns the exact original corrupt value/type, authority still reports the same committed generation, no conditional projection mutation/retirement ran, and the metadata pool has zero checked-out connections."
    - "Both projection observation and delegated filtering use one primary-code classifier: only sqlite error-code-classified operational busy/locked/I/O/open/full/read-only/protocol families become CacheMetadataError and retain core's logged-None compatibility; SQLITE_CORRUPT/NOTADB become integrity failures and SQL/programming/unclassified DBAPI errors propagate rather than masquerading as misses."
  </behavior>
  <action>Reuse Task 1's stable `CacheReason.METADATA_CORRUPT = "metadata_corrupt"` reason with the already-public `CacheIntegrityError`; put its string value in the existing error context mapping rather than changing the base exception constructor. Do not add a new public method or change `query_meta`'s signature. In `SqliteBackend`, introduce one focused persisted-parameter decoder accepting the cache key, SQLite `typeof(cache_key_params)`, and raw bytes selected through a BLOB/hex-safe expression so invalid UTF-8 cannot fail in the driver before attribution. The persisted column contract is exactly SQLite TEXT: reject NULL, integer, real, and every BLOB storage class even when its bytes happen to contain valid JSON. Lazy-import the exported bounds from `storage.manifest` inside the decoder after `cacheness.metadata` has finished module initialization: `storage.__init__` imports `BlobStore`, and `BlobStore` imports `cacheness.metadata`, so a new module-level metadata-to-storage import would create a real circular-import regression. For TEXT, measure the retrieved raw byte length before UTF-8 decode/JSON parse and cap it at `MAX_MANIFEST_BYTES` exactly rather than copying the number or inventing a larger allowance. Reject invalid UTF-8 and duplicate JSON object keys before a dict can overwrite them. Catch only the decoder/parser's documented Unicode/`ValueError`/`TypeError` failures, require `type(decoded) is dict` and every mapping key to be a string, and enforce the canonical structural walk exactly: root depth starts at one, every mapping key and value and every sequence item advances depth, keys and values each count toward the total-node bound, every mapping/sequence uses the collection-item bound, and every string (including keys) uses its UTF-8 byte bound. Import and use `MAX_NESTING_DEPTH`, `MAX_COLLECTION_ITEMS`, `MAX_TOTAL_NODES`, and `MAX_STRING_UTF8_BYTES`; do not mutate decoded evidence or import/call manifest semantic/version validation. Every failure raises `CacheIntegrityError` with `reason`, `backend=sqlite`, `operation=query_entries_by_key_params`, `field=cache_key_params`, affected `cache_key`, exact violated bound, and observed SQLite type/byte size where safe in its context; never include the full persisted value in logs/context.

Restructure `query_entries_by_key_params` so one backend-owned session explicitly executes read-only `BEGIN` before its first SELECT (SQLAlchemy's logical autobegin alone is insufficient under sqlite3 legacy transaction control). First select every row matching the exact live `(cache_key, actual_path)` pairs without an `IS NOT NULL` exclusion, retrieve raw parameter bytes plus SQLite storage type, and validate/decode all rows before any filter-dependent SQLite JSON expression executes. Keep that same explicit SQLite read snapshot/session for subsequent filtering, and attach the already validated mapping keyed by the exact pair to returned dictionaries rather than decoding again or using a broad catch. Guard every retained JSON1 extraction with `json_valid`/CASE so SQLite never evaluates `json_extract` against an invalid unrelated/stale row regardless of optimizer predicate order; exact live corruption has already raised from the predecode. Preserve bound path/value parameters and the existing statement shape required by query-security tests. Thus empty, string, numeric, boolean, and nested filters share one corruption decision, a nonmatching filter cannot hide corrupt live evidence, and a same-key projection replacement still cannot substitute for the observed locator. Fully materialize results before explicitly committing/rolling back and leaving the session; integrity, operational, and unclassified exceptions all return the QueuePool checked-out count to zero.

The facade currently computes `live_pairs` through `SqliteBackend.list_entries()` before calling the query protocol, and `_list_entries()` currently materializes the ORM `Text` attribute. An invalid-UTF-8 SQLite TEXT value can therefore fail in the driver before the exact-live decoder runs. Make the normal SQLite `_list_entries()` projection query retrieve `typeof(cache_key_params)` and BLOB-safe raw bytes instead of materializing that Text attribute. Preserve the established listing contract for valid TEXT mappings by permissively decoding those safe bytes; for malformed or wrongly typed parameters, retain the existing listing behavior of omitting `metadata.cache_key_params` rather than raising, so a corrupt stale projection cannot block an unrelated live query and exact authority/locator selection can proceed. This listing parse is not the integrity decision: once the facade has exact live pairs, `query_entries_by_key_params` must run the strict decoder above and raise for corruption on those pairs. Add a stale-corrupt plus valid-live regression proving stale evidence is neither exposed nor allowed to mask the valid result, and an invalid-UTF-8 exact-live regression proving no ORM/driver decode escapes before the typed decoder.

Add one narrow SQLite DBAPI classifier shared by `list_entries` (the projection-observation stage used before every public query) and `query_entries_by_key_params`. Read `DBAPIError.orig.sqlite_errorcode`, mask extended codes to the primary byte, and never use message substrings. Preserve the existing public logged-None policy only for operational `SQLITE_BUSY`, `SQLITE_LOCKED`, `SQLITE_IOERR`, `SQLITE_CANTOPEN`, `SQLITE_FULL`, `SQLITE_READONLY`, and `SQLITE_PROTOCOL` families translated to caused `CacheMetadataError` with the actual observation/filtering operation. Translate primary `SQLITE_CORRUPT`/`SQLITE_NOTADB` to `CacheIntegrityError(metadata_corrupt)` without a cache key when database-wide. Re-raise the original SQLAlchemy `DBAPIError` unchanged when `.orig` has no recognized sqlite code or the primary code is SQL/programming/constraint/schema/interrupt/another unclassified family. Malformed parameter JSON must be caught by the explicit predecode and never reach SQLite JSON1 as a generic operational error. Keep `UnifiedCache.query_meta` catching only CacheMetadataError so CacheIntegrityError and unclassified DBAPI/programming failures propagate unchanged while supported/unsupported/validation behavior remains compatible.

In `tests/test_cached_query_meta.py`, parameterize the real public facade over wrapped and unwrapped file-backed SQLite plus empty/string/boolean/nested/numeric filter shapes. After a normal public put, mutate only that exact live projection column by parameterized raw SQL to NULL, integer, real, valid-JSON BLOB, invalid-UTF-8 BLOB/text, malformed or duplicate-key text, exactly-over-bound text, each canonical structural-bound violation (including key/value node accounting), and valid non-mapping JSON roots. Include a corrupt live row whose requested filter would not match, proving prevalidation still raises, and separately a corrupt stale row beside a valid exact-live row, proving the safe listing observation omits stale bad parameters without either raising early or exposing the stale row. Snapshot `typeof(cache_key_params)` plus `hex(cache_key_params)` (or an exact safe scalar), the complete projection row, and authority generation/manifest digest before the call; install fail-if-called sentinels on projection sync/conditional mutation, metadata removal, and BlobStore retirement/delete; assert the same typed reason/context; then prove raw evidence and authority are unchanged and pool checkout is zero. Add valid controls exactly at each bound and for every existing filter family, and preserve the bound-path/value statement assertions in `test_query_meta_security.py`. Include an import regression that starts a clean spawned interpreter and proves both `import cacheness.metadata` and `import cacheness.storage` succeed, protecting the lazy bounds import from the existing storage/BlobStore/metadata cycle. Inject classified operational, classified CORRUPT/NOTADB, and unclassified SQLITE_ERROR DBAPI failures independently at both projection observation and delegated filtering: only the operational class may log/return None, database corruption must raise CacheIntegrityError, and unclassified/programming failures must propagate their original SQLAlchemy error. Do not retire, rewrite, normalize, truncate, or replace corrupt evidence, and add no retry, sleep, broad catch, key-only query, SessionLocal sniffing, or PostgreSQL claim.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_cached_query_meta.py tests/test_query_meta.py tests/test_query_meta_security.py tests/test_sqlite_metadata_bootstrap_atomicity.py tests/test_sqlite_concurrency.py::TestSQLiteConcurrency::test_concurrent_query_operations tests/test_sqlite_concurrency_temp.py::TestSQLiteConcurrency::test_concurrent_query_operations -x -o log_cli=false &amp;&amp; uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_phase3_postreview_concurrency.py tests/test_sqlite_authority_admission.py tests/test_unified_cache_adversarial_lifecycle.py tests/test_unified_cache_lifecycle_authority.py tests/test_blob_store_reconciliation.py tests/test_cache_integrity.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_sqlite_metadata_bootstrap_atomicity.py tests/test_cached_query_meta.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>Malformed/storage-type/UTF-8/byte-bound/structural-bound/root-shape corruption produces one CacheIntegrityError policy across empty and every filtered public query with cached/unwrapped parity; one explicit read snapshot and guarded JSON1 prevent filter-dependent masking; corrupt bytes and authority remain unchanged; no resources leak; operational DBAPI compatibility stays primary-code narrow at observation and filtering; all focused, full-suite, benchmark, compatibility, qualification, and Ruff gates pass.</acceptance_criteria>
  <done>Fresh review CR-02's implementation is committed without weakening public compatibility, committed-only visibility, or prior lifecycle guarantees; final release qualification is owned by Task 3.</done>
</completed_task>

<task type="tracer" tdd="true">
  <name>Task 3: Admit first-use authority preflight before SQLite bootstrap contention and qualify from clean tracked state</name>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_phase3_postreview_concurrency.py, tests/test_sqlite_concurrency.py, tests/test_sqlite_concurrency_temp.py, tests/test_public_api_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/config.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/test_phase3_postreview_concurrency.py</file>
    <file>tests/test_sqlite_authority_admission.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
    <file>tests/test_sqlite_concurrency.py</file>
    <file>tests/test_sqlite_concurrency_temp.py</file>
    <file>tests/test_public_api_contract.py</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-18-SUMMARY.md</file>
  </read_first>
  <behavior>
    - "A fresh same-instance authority schedule pauses creator A immediately after BEGIN EXCLUSIVE; contender B starts a public mutation, obtains a FIFO ticket but cannot open/configure a competing connection until A releases, and both complete boundedly with one valid authority, an empty admission registry, and no duplicate admission. The same admission-before-connection ordering applies to every ready-state mutation, so no mutation-side PRAGMA can contend before its ticket."
    - "Two independent authority instances in one process share the existing canonical-path admission gate during their first use. The winner initializes; the follower reclassifies and validates the committed schema before publishing its own readiness. Ordinary ready-state transactions retain short connection/BEGIN/callback/commit lifetimes, and distinct-key payload work remains concurrent."
    - "A spawned-process empty-root schedule forces process A to hold the initialization EXCLUSIVE boundary while process B enters its first preflight. B either validates the committed authority after release within the original budget or raises CacheBlobLifecycleTimeoutError with the exact preflight/bootstrap stage, elapsed/budget context, and sqlite3.OperationalError cause; a later fresh child then opens the valid authority, proving convergence without inherited coordination."
    - "One absolute deadline created at public operation entry covers FIFO queue/dispatch, connect, every contention-prone PRAGMA, initialization/migration, schema validation, BEGIN IMMEDIATE, and callback commit. Remaining budget is applied immediately before each SQLite call; no stage resets or extends 0.187 seconds, and the existing scheduler tolerance remains test-only."
    - "`_schema_ready` remains false while the database leaf is merely reserved/visible and while initialization is uncommitted. It becomes true only after COMMIT and same-connection validation of application_id, user_version, and store identity; read observers cannot convert an incomplete visible leaf into absence or readiness by sleeping and retrying."
    - "Read-only operations never acquire writer admission, initialize/migrate schema, publish `_schema_ready`, execute BEGIN IMMEDIATE/EXCLUSIVE, or change journal mode. Missing/pristine pre-leaf state retains the established empty result; a visible but uncommitted/invalid leaf returns the precise bounded typed validation outcome without a filename-poll or recursive sleep/reopen loop."
    - "A forced first-use connection/configuration/initialization/validation failure, a queued contender cancelled by close, and close racing an already granted bootstrap owner each release exactly their own ticket/reference, leave readiness false unless committed validation completed, preserve the active owner, and permit a later fresh operation/instance to converge."
    - "Busy/locked exhaustion is classified as CacheBlobLifecycleTimeoutError with operation `lifecycle_authority` and stage `connection_open`, `connection_configure`, `schema_initialize`, or `schema_validate` as appropriate. Non-timeout SQLite failures preserve their current typed migration/backend policy and original cause; no failure uses the generic `lifecycle_authority_open` operation or escapes as raw OperationalError."
    - "Both historical public `concurrent_metadata_access` modules begin against a fresh root and retain strict bounded completion, per-worker progress, no-corruption, no-leaked-ticket, and meaningful-success assertions. They accept only the already documented bounded authority-timeout reason/stages under genuine extreme contention and never suppress other CacheBlob errors."
    - "The exhaustive public CacheReason value test includes the already implemented additive `metadata_corrupt` value, and focused Python 3.11 authority/admission/public-facade tests pass before Task 3 is committed."
  </behavior>
  <action>Write the RED concurrency schedules first in `tests/test_phase3_postreview_concurrency.py`, using the existing bootstrap boundary, admission observer, injected monotonic/wait seams, Events/Barriers, and `multiprocessing.get_context("spawn")`; add only inert test observers when the existing boundaries cannot expose the post-BEGIN EXCLUSIVE and post-COMMIT/pre-publication points. Observers must execute outside Python registry/Condition locks except for the deliberate SQLite transaction hold. Do not use sleeps for ordering or success. The same-instance test must prove B reaches `writer_admission.enqueued` but not `sqlite.connection_preflight.started` while A owns the first-use ticket and holds EXCLUSIVE, then release A and require A and B to complete within bounded joins. Inspect the existing test-only admission snapshot after completion to prove owner/tickets/references retire exactly. Repeat with two independent authority objects for the same canonical path so process-scoped gate sharing—not instance `_schema_lock`—is exercised. Add a readiness-publication hook assertion that `_schema_ready` is false before initialization commit and after the leaf first appears, and true only after commit plus `_validate_schema` succeeds.

Add a deterministic spawned-process schedule on a truly empty authority root. Construct every authority inside its child. Process A pauses at a post-BEGIN EXCLUSIVE boundary and signals the parent through a pipe/Event; only then release process B into a real public authority mutation. Cover both release-within-budget convergence and held-past-budget exhaustion. For exhaustion, assert total elapsed is no more than the unchanged 0.187-second budget plus the existing 0.025-second scheduler tolerance, the exception is `CacheBlobLifecycleTimeoutError`, its context names `operation=lifecycle_authority` and the exact preflight/bootstrap stage, and `__cause__` is the originating `sqlite3.OperationalError` with BUSY/LOCKED primary code. After A completes, launch a new child authority that opens and validates the store, proving the timeout did not corrupt evidence. Re-run the Plan 18 fork cases where fork occurs while the registry lock or a path gate is owned: the inherited object must still reject by PID, and a newly constructed child must see a fresh registry and lock. Bound every join; terminate only as failed-test cleanup.

Refactor `SQLiteLifecycleAuthority._transaction()` per D-18, D-20, D-26, and D-28. Create `started_at` and the one absolute deadline once at public transaction entry, then acquire the existing process-scoped canonical-path `_admit_writer(deadline, started_at)` before every mutation materializes the leaf, opens SQLite, or executes any connection PRAGMA; this applies to already-ready mutations as well as first use and eliminates all mutation-side pre-admission SQLite contention. Read `_schema_ready` only under `_schema_lock` after admission and recheck it because a queued peer may have completed bootstrap. Carry the one granted ticket as an explicit private `admission_held`/equivalent token through `_connection()` and BEGIN IMMEDIATE/callback/commit so the transaction never requests a second ticket and never releases/requeues between schema validation and the requested mutation. Never acquire the path admission Condition while holding `_schema_lock`: ordering is path admission first, a short `_schema_lock` check/publication second, and SQLite connection/transaction work after the Python lock is released. Close cancellation continues to cancel only a registered queued ticket through the existing ownership path; an already granted owner follows the existing bounded completion policy and is never stolen by close. The gate covers only one method-scoped authority connection/bootstrap-or-validation/short transaction, never handler serialization, payload publication/verification, reclamation, projection export, or inter-call cleanup work.

Treat the existing process-scoped gate as scheduling only, never cross-process or lifecycle authority. Independent processes continue to resolve the winner through SQLite's BEGIN EXCLUSIVE and exact schema validation. A mutator that sees only the exact pristine reserved namespace attempts the existing O_EXCL authority leaf under its admitted ticket instead of polling for another process; a process that finds the reserved leaf but no committed application/schema uses its remaining SQLite busy budget to become the safe initializer or block at SQLite and validate the winner. Remove both `_await_inflight_authority_leaf` polling from mutation bootstrap and the recursive millisecond sleep/reopen branch after connection. Leaf presence, `_bootstrap_started`, or a zero application ID cannot publish `_schema_ready`. Publish readiness under `_schema_lock` only after initialization/migration has committed and the same still-open connection has successfully re-read and validated application ID, exact schema version, and store identity. On rollback, timeout, close, failed validation, or lost ownership it remains false. Ready-state operations retain a short admitted connection plus BEGIN IMMEDIATE transaction, and the gate never spans handler serialization, payload I/O, old-generation cleanup, or distinct-key facade work.

Keep the read path structurally separate. Reads do not enter `_admit_writer`, call `_materialize_database_file`, invoke `_initialize_schema`/`_migrate_schema_layout`, set `_schema_ready`, or run a write transaction. An absent root or exact pristine reserved namespace before authority-leaf publication keeps the existing non-materializing empty result. Once a regular authority leaf is visible, open it read-only, apply only read-safe connection settings, and validate committed identity/schema within the caller's one remaining deadline. If it is still empty/uncommitted or externally EXCLUSIVE-locked, return a typed `CacheBlobLifecycleTimeoutError` at `connection_configure` or `schema_validate` when BUSY/LOCKED/deadline applies, or the existing typed migration/backend result for a stable invalid database; never reinterpret the leaf as absence and never poll/sleep/reopen it. A successful read validation does not publish the mutation-only `_schema_ready` shortcut because it has not run migration.

Apply `_remaining_for_stage()` from the original deadline immediately before `sqlite3.connect`, before each connection/configuration PRAGMA or schema statement that can wait on another SQLite owner, before BEGIN EXCLUSIVE or migration, before every post-commit schema/identity validation group, and before ordinary BEGIN IMMEDIATE. Reset SQLite `busy_timeout` conservatively from the newly measured remainder at each such blocking boundary without rounding above it; never let a timeout calculated for an earlier stage survive long enough to extend the absolute deadline at a later stage. Extend the private error translator to accept an explicit stage: recognized primary-code BUSY/LOCKED exhaustion raises `CacheBlobLifecycleTimeoutError` with reason `blob_lifecycle_timeout`, operation `lifecycle_authority`, stage `connection_open`, `connection_configure`, `schema_initialize`, or `schema_validate`, budget/elapsed/remaining/path context, and `raise ... from error`. Preserve existing typed unsupported/migration/integrity results for schema failures and translate other SQLite backend errors narrowly with their cause; delete the blanket `operation=lifecycle_authority_open` classification. Do not inspect English messages, swallow an error, add automatic retries, add a timeout, raise the 0.187-second default, modify benchmark envelopes, or introduce another registry/lockfile/global/whole-cache coordinator.

Update both duplicate `test_concurrent_metadata_access` modules consistently so their public `UnifiedCache` schedule starts all workers at first use of one genuinely fresh authority root. Retain the Plan 18 strict policy: only `CacheBlobLifecycleTimeoutError` with `CacheReason.BLOB_LIFECYCLE_TIMEOUT` and the exact approved contention stage is an expected extreme-contention outcome; every other CacheBlob error, raw sqlite/SQLAlchemy exception, missing result, corrupt read, thread/process leak, or unretired ticket is a hard failure. Keep success accounting meaningful by requiring the existing minimum success ratio plus progress from every worker, and assert a final reopen/read/stats invariant. Do not broaden accepted reasons merely to cover the new preflight test; the same-process admission design should prevent raw first-use preflight collisions. Add `metadata_corrupt` to the exact `expected_reasons` set in `tests/test_public_api_contract.py`; do not alter `CacheReason` production code because commit `8439e8c` already added the enum member.

Add deterministic cleanup schedules for every newly enlarged ticket lifetime. Force one connection-open/configuration failure, one initialization rollback, and one post-commit validation failure after admission; each must close its connection, release only its ticket/reference, leave `_schema_ready` false unless the commit and validation both succeeded, and allow the next mutation to converge. While creator A owns the first-use ticket, enqueue B and close B: B must wake with `CacheBlobStoreClosedError`, A must remain owner and complete, and the registry must retire after both unwind. Close A only after its ticket is granted and bootstrap is paused: close must not steal/cancel the owner, the already active call follows the existing completion contract, later calls on A fail closed, and a fresh instance validates the committed store. Add a read during the visible-leaf/pre-commit boundary and assert no writer-admission event, no schema mutation/readiness publication, the exact typed read outcome, zero connection/ticket leak, and normal reads after the writer commits. Re-run the existing pre-entry timeout, owned-path-gate fork, and locked-registry fork tests unchanged.

Run the focused Python 3.11 authority/admission/public-concurrency/API-contract tests in the workspace. Snapshot the two user-owned untracked compatibility fixture sidecars, if present, by pathname, file type, inode, size, mtime, and SHA-256 before focused verification, and assert the snapshot is identical afterward. Do not run the complete suite or compatibility corpus in the dirty workspace. Commit Task 3 through the ordinary executor flow only after these focused tests pass; Task 4 begins from that committed HEAD. Never delete, move, recapture, stage, or modify the workspace fixture sidecars or any protected planning artifact.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_phase3_postreview_concurrency.py tests/test_sqlite_authority_admission.py tests/test_sqlite_lifecycle_authority.py tests/test_sqlite_concurrency.py::TestSQLiteConcurrency::test_concurrent_metadata_access tests/test_sqlite_concurrency_temp.py::TestSQLiteConcurrency::test_concurrent_metadata_access tests/test_public_api_contract.py -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>Fresh first-use authority preflight is admitted before contention-prone SQLite work, uses one unchanged absolute deadline and precise typed stages/causes, publishes readiness only after committed validation, converges across same-process instances and spawned processes, preserves fork reset and distinct-key concurrency, passes both real public metadata-contention schedules, exposes the additive enum value, and leaves workspace fixture sidecars unchanged.</acceptance_criteria>
  <done>The newly diagnosed LifecycleAuthority preflight blocker is closed and committed without replaying Tasks 1-2 or changing public APIs, authority limits, benchmarks, protected evidence, Windows status, or PostgreSQL claims.</done>
</task>

<task type="auto">
  <name>Task 4: Qualify committed Task 3 from a disposable tracked-clean worktree</name>
  <files>None — verification only; record results in 03-19-SUMMARY.md after plan completion.</files>
  <read_first>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
    <file>tools/capture_phase3_windows_qualification.py</file>
    <file>tools/verify_phase3_ruff_delta.py</file>
    <file>tests/fixtures/compat/validate_corpus.py</file>
  </read_first>
  <action>Require Task 3's focused-green implementation to be committed and use `git diff --exit-code HEAD -- <five paths>` so both staged and unstaged differences from the commit are rejected. Resolve and retain the exact full Task 3 HEAD OID before creating any worktree. Resolve the latest commit that changed this Plan 03-19 file, require it to be an ancestor strictly before Task 3 HEAD, and inspect the complete committed range: every changed path after the plan revision and through Task 3 HEAD must be one of Task 3's five declared source/test paths. This range allowlist prevents an executor from accidentally absorbing unrelated tracked workspace changes into an otherwise clean-looking commit. Snapshot the two user-owned workspace compatibility fixture sidecars, if present, by repository-relative pathname, lstat file type, inode, size, nanosecond mtime, and SHA-256 of regular-file bytes; encode absence explicitly. Create a fresh temporary directory with `mktemp -d`, attach a detached `git worktree` at that captured OID (not a later symbolic `HEAD`), assert its resolved HEAD equals the captured OID, and assert its initial `git status --porcelain --untracked-files=all` is empty. In that worktree run the compatibility corpus first, before any suite can create SQLite sidecars; then run the verify-only Windows attestation, exact frozen all-extras/dev full suite, established focused Python 3.11 metadata-bootstrap/query plus lifecycle/close/reconciliation/concurrency/containment modules, unchanged lifecycle benchmark verifier, Phase 3 Ruff-delta test and tool, and direct Ruff over all five Task 3 paths. After the gates, assert no tracked file differs from the captured commit so a verifier cannot hide a baseline/evidence rewrite in the disposable checkout; untracked test residue is permitted only inside that disposable worktree. An EXIT trap must first return to the original repository, remove only the registered disposable worktree, report cleanup failure instead of swallowing it, remove the now-empty temporary directory when needed, and compare a fresh workspace-sidecar fingerprint with the original even when a gate fails. Do not stash, clean, copy into the worktree, include, delete, move, recapture, stage, or modify workspace user artifacts; never use the dirty workspace as release evidence. Do not change defaults/baselines or claim native Windows/live PostgreSQL qualification. Darwin D-32 remains UNAVAILABLE/NOT_QUALIFIED with `native_evidence:false`.</action>
  <verify>
    <automated>git diff --exit-code HEAD -- src/cacheness/storage/sqlite_lifecycle_authority.py tests/test_phase3_postreview_concurrency.py tests/test_sqlite_concurrency.py tests/test_sqlite_concurrency_temp.py tests/test_public_api_contract.py &amp;&amp; bash -lc 'set -euo pipefail
_phase3_repo=$(git rev-parse --show-toplevel)
_phase3_head=$(git rev-parse --verify HEAD)
_phase3_plan=.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-19-PLAN.md
_phase3_plan_commit=$(git log -1 --format=%H -- "$_phase3_plan")
test "$_phase3_plan_commit" != "$_phase3_head"
git merge-base --is-ancestor "$_phase3_plan_commit" "$_phase3_head"
while IFS= read -r _phase3_changed; do
  case "$_phase3_changed" in
    src/cacheness/storage/sqlite_lifecycle_authority.py|tests/test_phase3_postreview_concurrency.py|tests/test_sqlite_concurrency.py|tests/test_sqlite_concurrency_temp.py|tests/test_public_api_contract.py) ;;
    *) printf '\''Unexpected committed Task 3 path: %s\n'\'' "$_phase3_changed" >&2; exit 1 ;;
  esac
done < <(git diff --name-only --diff-filter=ACDMRTUXB "$_phase3_plan_commit" "$_phase3_head" --)
_phase3_clean_dir=$(mktemp -d /tmp/cacheness-phase3-clean.XXXXXX)
_phase3_sidecars=(tests/fixtures/compat/sqlite-columns-v0314/metadata.sqlite3-wal tests/fixtures/compat/sqlite-columns-v0314/metadata.sqlite3-shm)
_phase3_fingerprint() {
  python3 -c '\''import hashlib
import os
import stat
import sys
root = sys.argv[1]
for relative in sys.argv[2:]:
    path = os.path.join(root, relative)
    try:
        value = os.lstat(path)
    except FileNotFoundError:
        print(relative + "|absent")
        continue
    kind = "regular" if stat.S_ISREG(value.st_mode) else "symlink" if stat.S_ISLNK(value.st_mode) else "other"
    digest = hashlib.sha256(open(path, "rb").read()).hexdigest() if kind == "regular" else "-"
    target = os.readlink(path) if kind == "symlink" else "-"
    print("|".join((relative, kind, str(value.st_ino), str(value.st_size), str(value.st_mtime_ns), digest, target)))'\'' "$_phase3_repo" "${_phase3_sidecars[@]}"
}
_phase3_before=
_phase3_cleanup() {
  _phase3_status=$?
  trap - EXIT
  cd "$_phase3_repo" || _phase3_status=1
  if git worktree list --porcelain | grep -Fqx "worktree $_phase3_clean_dir"; then
    git worktree remove --force "$_phase3_clean_dir" || _phase3_status=1
  fi
  if [ -d "$_phase3_clean_dir" ]; then
    rmdir "$_phase3_clean_dir" || _phase3_status=1
  fi
  _phase3_after=$(_phase3_fingerprint) || _phase3_status=1
  [ "${_phase3_before:-}" = "${_phase3_after:-}" ] || _phase3_status=1
  exit "$_phase3_status"
}
trap _phase3_cleanup EXIT
_phase3_before=$(_phase3_fingerprint)
git worktree add --detach "$_phase3_clean_dir" "$_phase3_head"
test "$(git -C "$_phase3_clean_dir" rev-parse --verify HEAD)" = "$_phase3_head"
test -z "$(git -C "$_phase3_clean_dir" status --porcelain --untracked-files=all)"
cd "$_phase3_clean_dir"
uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314
uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false
uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_metadata_bootstrap_atomicity.py tests/test_cached_query_meta.py tests/test_query_meta.py tests/test_query_meta_security.py tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_close_contract.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py tests/test_filesystem_containment.py tests/test_unified_cache_adversarial_lifecycle.py tests/test_unified_cache_lifecycle_authority.py tests/test_projection_sql_atomicity.py tests/test_cached_custom_metadata.py tests/test_sqlite_bootstrap_concurrency.py tests/test_sqlite_authority_admission.py tests/test_phase3_gap_acceptance.py tests/test_phase3_postreview_concurrency.py tests/test_sqlite_concurrency.py tests/test_sqlite_concurrency_temp.py -o log_cli=false
uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json
uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false
uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py
uv run --isolated --all-extras --group dev --frozen ruff check src/cacheness/storage/sqlite_lifecycle_authority.py tests/test_phase3_postreview_concurrency.py tests/test_sqlite_concurrency.py tests/test_sqlite_concurrency_temp.py tests/test_public_api_contract.py --output-format concise
git diff --exit-code "$_phase3_head" --
test -z "$(git status --porcelain --untracked-files=no)"'</automated>
  </verify>
  <acceptance_criteria>The exact committed Task 3 HEAD passes compatibility, verify-only qualification, full suite, focused Python 3.11 lifecycle matrix, benchmark, and Ruff gates from an initially tracked-clean disposable worktree; the dirty workspace and its fixture sidecars remain unchanged.</acceptance_criteria>
  <done>Plan 03-19 and Phase 3 release qualification are green from clean committed state with unchanged runtime limits, baseline envelopes, public APIs, native-Windows status, and live-PostgreSQL claims.</done>
</task>

</tasks>

## Gap Coverage Audit

| Fresh review finding | Owner | Deterministic acceptance evidence | Status |
|---|---|---|---|
| CR-01 concurrent fresh metadata create_all race | 03-19 Task 1 | 64-thread and spawned-process empty-path Barriers, pre-WAL hostile rejection, one-deadline same-connection BEGIN IMMEDIATE DDL/validation/stats, transactional-DDL rollback, and final exact schema/WAL/resource audit | COVERED |
| CR-02 corrupt live cache_key_params rendered/suppressed inconsistently | 03-19 Task 2 | Cached/unwrapped storage-type/decode/canonical-bound/shape matrix across every public filter family, one explicit read snapshot, guarded JSON1, narrow DBAPI classification, and immutable-evidence checks | COVERED |
| Post-execution blocker: authority connection preflight occurs before FIFO admission | 03-19 Task 3 | Same-instance and independent-instance EXCLUSIVE/preflight barriers, spawned-process within/over-budget schedules, exact staged cause checks, readiness-publication ordering, and real public concurrent_metadata_access in both historical modules | COVERED |
| Public enum contract omitted implemented metadata_corrupt reason | 03-19 Task 3 | Exhaustive CacheReason value-set regression contains metadata_corrupt without production enum replay | COVERED |
| Dirty compatibility sidecars can contaminate exact fixture qualification | 03-19 Task 4 | Compatibility and release gates execute from an initially tracked-clean detached HEAD worktree; workspace sidecar identity and hashes remain unchanged | COVERED |

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generation with every incomplete outcome recoverable | 03-19 Tasks 1-4 | COVERED | Metadata initialization is complete-or-absent, corrupt projection evidence stays detectable, authority bootstrap cannot leak mixed preflight state, and final evidence is clean. |
| REQ | STOR-03 | Never expose partial payload/metadata state | 03-19 Tasks 1-4 | COVERED | Partial metadata schema is rejected, corrupt committed query metadata never renders as valid, schema readiness follows committed authority validation, and the full suite revalidates visibility. |
| REQ | STOR-04 | Failed writes preserve valid generation and recoverable residue | 03-19 Tasks 1-4 | COVERED | Failed metadata bootstrap rolls back/disposes, failed decode preserves exact evidence, timed-out authority preflight leaves the winner valid, and release gates rerun rollback paths. |
| REQ | STOR-05 | Repeated lifecycle operations converge idempotently | 03-19 Tasks 1 and 3 plus Plan 18 regressions | COVERED | Repeated/concurrent constructors and first-use operations converge through bounded SQLite/admission state with no ticket leak. |
| REQ | STOR-06 | Reconciliation is bounded and evidence-driven | 03-19 Tasks 2-4 plus Plan 18 regressions | COVERED | Query corruption evidence remains canonical and all reconciliation gates run from clean committed state. |
| REQ | STOR-07 | Deterministic races without whole-cache serialization | 03-19 Tasks 1 and 3 | COVERED | Database-native bootstrap plus per-path first-use admission are forced with barriers/process schedules while distinct-key payload work stays concurrent. |
| RESEARCH | — | LifecycleAuthority remains canonical; metadata projection is derived | 03-19 Tasks 1-3 | COVERED | Metadata repairs remain derived, and Task 3 changes only deep authority connection/admission ordering. |
| RESEARCH | — | Database-native atomicity and bounded deterministic schedules | 03-19 Tasks 1-3 | COVERED | BEGIN transactions, primary error codes, Barriers/spawn, one deadline, and exact evidence replace retries/guessing. |
| CONTEXT | D-01–D-16 | Exact committed visibility and recoverable evidence | 03-19 Tasks 2-4 | COVERED | Corruption propagates unchanged, readiness requires committed validation, and prior lifecycle/reconciliation behaviors rerun from clean state. |
| CONTEXT | D-17–D-20 | Backend CAS/race authority and distinct-key concurrency | 03-19 Tasks 1 and 3 | COVERED | Existing per-path admission schedules bootstrap only; SQLite remains correctness authority and no payload-global lock is added. |
| CONTEXT | D-21–D-31 | Trusted topology, one authority, stdlib SQLite, measured bounds | 03-19 Tasks 1-3 | COVERED | LifecycleAuthority remains deep and stdlib SQLite-backed with one 0.187-second deadline and unchanged benchmark envelopes. |
| CONTEXT | D-32 | Native Windows qualification deferred | 03-19 Task 4 gate | COVERED | Darwin remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false; evidence is verify-only. |

<threat_model>
OWASP ASVS L1 applies. Every high-severity item blocks completion until mitigated.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Independent constructor/process → shared SQLite file | Schema absence and creation must be one database-native decision. |
| Existing SQLite objects → current metadata schema | Wrong, partial, legacy, or hostile objects must not be adopted as current. |
| Persisted cache_key_params → JSON decoder/query engine | Stored projection text is untrusted evidence until bounded decode and shape validation. |
| Metadata backend → UnifiedCache query policy | Operational availability errors may retain compatibility; integrity failures must propagate. |
| First-use authority thread/process → SQLite connection preflight | Local FIFO scheduling and cross-process SQLite locking must share one bounded operation deadline. |
| Authority leaf visibility → schema readiness | A reserved or partially initialized file is untrusted until committed schema identity validates. |
| Dirty workspace → release evidence | User-owned SQLite sidecars must not influence or be destroyed by qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-19-01 | Tampering | Metadata schema bootstrap | critical | mitigate | WAL-aware query-only preflight rejects hostile existing state without writable bootstrap; explicit BEGIN IMMEDIATE precedes decisive inspection/create_all/validation/stats on one connection, and injected post-DDL failure proves rollback. |
| T-03-19-02 | Denial of Service | Concurrent constructors | high | mitigate | One absolute metadata deadline spans preflight/WAL/schema acquisition, primary SQLite error-code classification preserves typed causes, and bounded 64-thread/spawn tests prove convergence. |
| T-03-19-03 | Repudiation | Table-exists race handling | high | mitigate | No string suppression or retry; successful convergence requires locked post-DDL exact schema validation. |
| T-03-19-04 | Elevation of Privilege | Hostile database state | high | mitigate | Reject views, wrong columns/PK/nullability/indexes, non-database bytes, and partial state before backend publication. |
| T-03-19-05 | Tampering | cache_key_params decode | critical | mitigate | Require SQLite TEXT, bound raw bytes and decoded structure with canonical constants, catch narrow decode failures, require dict/string-key shape, and raise key-attributed CacheIntegrityError. |
| T-03-19-06 | Information Disclosure | Corruption diagnostics | high | mitigate | Error context records key/type/size/reason but never embeds persisted content. |
| T-03-19-07 | Repudiation | Filter-dependent corruption | high | mitigate | Explicitly BEGIN one read snapshot, validate every exact live pair before any filter expression, guard JSON1 on all rows, and cover empty/string/bool/nested/numeric parity. |
| T-03-19-08 | Denial of Service | Oversized metadata JSON | high | mitigate | Reject encoded input above the canonical manifest bound before parsing and return connections on every path. |
| T-03-19-09 | Tampering | Query-side recovery | high | mitigate | Fail-if-called mutation sentinel plus raw-row/authority snapshots prove no repair, retirement, or evidence rewrite. |
| T-03-19-10 | Spoofing | Operational-vs-integrity DBAPI classification | high | mitigate | Share primary-code classification across observation and filtering; only enumerated availability families become CacheMetadataError/miss policy, corruption becomes integrity, and unclassified errors propagate. |
| T-03-19-11 | Denial of Service | Global serialization | high | mitigate | No process-global or whole-cache lock; only short SQLite schema transaction serializes bootstrap. |
| T-03-19-12 | Tampering | Release policy | high | mitigate | Full suite and unchanged benchmark/compatibility/qualification verifiers block default, evidence, or platform-claim drift. |
| T-03-19-13 | Denial of Service | Pre-admission connection PRAGMAs | critical | mitigate | First-use mutation acquires the existing per-path FIFO before opening/configuring SQLite and every blocking stage consumes the one original deadline. |
| T-03-19-14 | Repudiation | LifecycleAuthority open failure classification | high | mitigate | Primary BUSY/LOCKED errors carry exact connection/configure/initialize/validate stage, elapsed/budget context, and original DBAPI cause; generic lifecycle_authority_open classification is removed. |
| T-03-19-15 | Spoofing | Schema-ready publication | critical | mitigate | `_schema_ready` is guarded and published only after commit plus same-connection application/schema/store-identity validation; visible leaves and zero application IDs are never sufficient. |
| T-03-19-16 | Denial of Service | Admission lock ordering | high | mitigate | Canonical-path admission precedes short schema-state locking, no Python lock is held across SQLite work, first-use carries one admission token, and deterministic completion/ticket-retirement schedules block inversion or double admission. |
| T-03-19-17 | Tampering | Cross-process/bootstrap evidence | high | mitigate | Spawned processes rely on SQLite EXCLUSIVE/validation as correctness authority, preserve fork reset, and prove later convergence after a typed timeout. |
| T-03-19-18 | Tampering | Qualification workspace | high | mitigate | Run exact gates from an initially clean detached worktree at committed HEAD, fingerprint workspace sidecars before/after, and remove only the disposable worktree. |
</threat_model>

<verification>
Tasks 1-2 are immutable committed evidence and are not replayed as implementation tasks. Task 3 first runs focused Python 3.11 authority/admission tests, both real public concurrent_metadata_access schedules, and the enum contract in the workspace while preserving fixture-sidecar fingerprints, then commits the focused-green change. Task 4 runs compatibility first and all remaining exact release gates from an initially tracked-clean detached worktree at that Task 3 HEAD: verify-only Windows attestation, frozen all-extras/dev suite, established Python 3.11 lifecycle/close/reconciliation/concurrency/containment modules, unchanged lifecycle benchmark verifier, Phase 3 Ruff delta test/tool, and direct Ruff for all five Task 3 paths. Do not run exact fixture qualification against the dirty workspace, recapture Windows evidence, or claim live PostgreSQL qualification.
</verification>

<success_criteria>
- Independent thread and spawned-process constructors converge from an empty metadata path with one exact validated schema/stats state and no resource leaks.
- Schema bootstrap is one bounded SQLite-native writer transaction; busy exhaustion is typed/caused and hostile or partial state is rejected unchanged.
- Every malformed, wrongly typed, oversized, or non-mapping live cache_key_params value raises the same key-attributed CacheIntegrityError for empty and filtered public queries.
- Cached and unwrapped SQLite behave identically; corrupt evidence and committed authority remain unchanged and all sessions/connections retire.
- Only explicitly enumerated SQLite operational errors retain logged-None compatibility; integrity and programmer errors never become misses.
- First-use authority threads enter existing per-path admission before SQLite connection preflight, use exactly one ticket/deadline, and publish readiness only after committed identity validation.
- Forced same-instance and spawned-process EXCLUSIVE/preflight races either converge within the budget or expose a precise typed/caused stage; no raw OperationalError, generic open classification, inherited gate, or retained ticket remains.
- Both duplicate public concurrent_metadata_access schedules retain bounded meaningful progress and no-corruption guarantees, and the exact CacheReason contract includes metadata_corrupt.
- Full suite, benchmark, compatibility, qualification verification, and Ruff gates pass from clean committed state without LifecycleAuthority/default/baseline/platform drift or workspace sidecar mutation.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-19-SUMMARY.md` after completion. Preserve Tasks 1-2 with their four exact commits; record Task 3's new commits/work (same-instance/process schedules, admission/preflight ordering, schema-ready publication, timeout stages/causes/bounds, ticket/registry/fork retirement, public concurrency progress, and enum contract) plus Task 4's workspace-sidecar fingerprints, exact clean-worktree HEAD, gate results, and unchanged 0.187-second authority/benchmark/Windows/live-PostgreSQL status.
</output>
