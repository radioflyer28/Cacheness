---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/lifecycle-authority.md
  - tests/_lifecycle_test_support.py
  - tests/test_lifecycle_authority_contract.py
  - tests/test_blob_store_read_contract.py
  - tests/test_phase3_release_evidence.py
  - tests/test_phase3_ruff_delta.py
  - tests/fixtures/phase3_ruff_baseline.json
  - tools/verify_phase3_ruff_delta.py
autonomous: false
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Executors begin from frozen public error reasons, BlobStore metadata shape, and the exact legacy reconciliation report view rather than renaming observable fields accidentally; D-13's richer evidence is delivered later through a versioned additive machine view and an explicit legacy adapter."
    - "The file-native Phase 3 scheduler is classified as unreleased development state, with an explicit disposable-store rebuild instruction and a fail-closed preflight if release evidence contradicts that classification (D-23, D-30)."
    - "The reserved local authority locator and schema identity are confirmed before the first persistent authority database is created (D-26, D-31)."
    - "The empty-store inspection and Windows logon-session deployment contracts are frozen before authority creation: read-only inspection creates nothing, while Windows mutation requires a pre-provisioned protected local NTFS root and absent, unsafe, drifted, or unprovable token/DACL scope fails typed and unchanged with offline provisioning guidance (D-21, D-22)."
    - "A checked-in pre-phase Ruff baseline permits only existing stable-fingerprint findings, requires every Phase 3-new Python file to be clean, and rejects every new or changed finding."
  artifacts:
    - path: docs/lifecycle-authority.md
      provides: "Compatibility characterization, local topology, authority locator, and disposable development-store rebuild contract"
    - path: tests/_lifecycle_test_support.py
      provides: "Deterministic authority, payload, crash, observer, and public-shape fixtures"
    - path: tests/test_lifecycle_authority_contract.py
      provides: "Wave 0 contract skeleton and locked observable shapes"
    - path: tests/test_phase3_release_evidence.py
      provides: "Deterministic fail-closed repository and package evidence gate for scheduler retirement"
    - path: tests/fixtures/phase3_ruff_baseline.json
      provides: "Pre-phase Ruff findings for the exact Phase 3-owned Python scope, stored as stable normalized fingerprints"
    - path: tools/verify_phase3_ruff_delta.py
      provides: "Deterministic Ruff delta gate that permits removed baseline findings and rejects every new finding"
  key_links:
    - from: tests/test_lifecycle_authority_contract.py
      to: src/cacheness/error_handling.py
      via: "Exact existing CacheReason values and typed error inheritance"
      pattern: "CacheReason"
    - from: tests/test_blob_store_reconciliation.py
      to: src/cacheness/storage/reconciliation.py
      via: "Exact ReconciliationReport.to_dict and human_summary compatibility"
      pattern: "to_dict|human_summary"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Native handler payloads must not gain a Cacheness wrapper or header."
    - "[FLAGGED/UNVERIFIED] JSON and file-native scheduler artifacts must not remain independently writable lifecycle authorities."
    - "[FLAGGED/UNVERIFIED] Normal reads must not mutate lifecycle evidence or projections."
---

<objective>
Freeze the observable compatibility and one-way local authority choices before replacing the scheduler.

Purpose: Turn every unresolved Wave 0 question into executable evidence so later plans do not rediscover public contracts or migration policy.
Output: Characterization tests, deterministic test support, a checked-in Ruff delta gate, a documented no-release/rebuild decision, and a confirmed versioned SQLite locator/schema contract.
</objective>

## Artifacts this phase produces

- `docs/lifecycle-authority.md` documenting `.cacheness/lifecycle-authority-v1.sqlite3`, lazy read-only bootstrap, local-only topology, the Windows pre-provisioned-root/logon-SID-bound DACL deployment contract and actionable offline provisioning command, public-shape preservation, and disposable scheduler-store rebuild.
- `tests/_lifecycle_test_support.py` with deterministic barriers/fault hooks and no timing-based race oracle.
- Wave 0 assertions for stable errors, metadata dictionaries, reconciliation reports, and scheduler release history.
- A checked-in pre-phase Ruff baseline plus a tested no-new-findings verifier for the exact Phase 3-owned Python scope.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VERIFICATION.md
</context>

<planner_assumptions>
- STOR-03 unclassified edge probe is flagged/unverified: fault-boundary tests must prove only old-complete or new-complete visibility.
- STOR-04 unclassified edge probe is flagged/unverified: every persistent residue must be indexed and recoverable without filename inference.
- STOR-05 idempotency probe is flagged/unverified: repeating overwrite, delete, clear, close, cleanup, and apply operations must converge.
- STOR-05 concurrency probe is flagged/unverified: interruption and parallel execution must preserve exact lineage and the only valid generation.
- STOR-06 unclassified edge probe is flagged/unverified: dry-run stability, bounded resume, and destructive revalidation define acceptance.
- STOR-07 unclassified edge probe is flagged/unverified: same-key single-winner and distinct-key overlap define acceptance.
- Descriptor-less prohibition recall retained only the three project-specific prohibitions in frontmatter; each remains flagged/unverified until later plans provide executable evidence.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the exact Phase 3 Ruff baseline before production edits</name>
  <files>tests/test_phase3_ruff_delta.py, tests/fixtures/phase3_ruff_baseline.json, tools/verify_phase3_ruff_delta.py</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "The pre-phase Ruff baseline records the exact Phase 3-owned Python scope and a multiset of line-independent normalized fingerprints; removed findings pass, while every unmatched current finding fails."
    - "Python files newly created by Phase 3 have no grandfathered findings and must pass direct Ruff in addition to the delta gate."
  </behavior>
  <action>Before any Phase 3 production edit, run repository-locked Ruff with JSON output and check in `tests/fixtures/phase3_ruff_baseline.json`. The artifact declares three disjoint exact lists. `existing_paths`: `src/cacheness/__init__.py`, `src/cacheness/config.py`, `src/cacheness/error_handling.py`, `src/cacheness/metadata.py`, `src/cacheness/storage/__init__.py`, `src/cacheness/storage/blob_store.py`, `src/cacheness/storage/coordination.py`, `src/cacheness/storage/guarded_handler_io.py`, `src/cacheness/storage/lifecycle.py`, `src/cacheness/storage/manifest_repository.py`, `src/cacheness/storage/path_security.py`, `src/cacheness/storage/reconciliation.py`, `tests/test_blob_store_atomic_lifecycle.py`, `tests/test_blob_store_close_contract.py`, `tests/test_blob_store_concurrency.py`, `tests/test_blob_store_read_contract.py`, `tests/test_blob_store_reconciliation.py`, `tests/test_clear_recovery.py`, `tests/test_filesystem_containment.py`, `tests/test_manifest_repository_cas.py`, `tests/test_public_api_contract.py`, and `verify_platform.py`. `new_paths`: `benchmarks/lifecycle_authority_benchmark.py`, `src/cacheness/storage/lifecycle_authority.py`, `src/cacheness/storage/sqlite_lifecycle_authority.py`, `src/cacheness/storage/memory_lifecycle_authority.py`, `tests/_lifecycle_test_support.py`, `tests/test_lifecycle_authority_contract.py`, `tests/test_sqlite_lifecycle_authority.py`, `tests/test_phase3_release_evidence.py`, `tests/test_phase3_scheduler_retirement.py`, `tests/test_phase3_windows_contract.py`, `tests/test_phase3_ruff_delta.py`, and `tools/verify_phase3_ruff_delta.py`. `expected_retired_paths`: `src/cacheness/storage/operation_repository.py`, `src/cacheness/storage/operation_record.py`, and `src/cacheness/storage/clear_recovery.py`. Capture baseline diagnostics only for currently present `existing_paths` and `expected_retired_paths`. Store a multiset of SHA-256 fingerprints over normalized repository-relative path, Ruff rule code, whitespace-normalized diagnostic message, and whitespace-normalized source snippet, excluding line/column so line movement matches. Implement the verifier to reject scope drift and malformed/duplicate declarations; require every `existing_path` to remain present; permit only `expected_retired_paths` and not-yet-created `new_paths` to be absent; invoke Ruff on all currently present declared paths; compare existing/retired current findings as a multiset subset of baseline; and require every present `new_path` to have zero findings. Allow removed findings and retired paths to disappear, and print unmatched findings deterministically. Tests cover moved lines, duplicates, changed messages/snippets, removals, delayed new-file creation, new-file cleanliness, retired files, and path normalization.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase3_ruff_delta.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>The checked-in baseline names the exact scope and expected-retired paths, uses stable multiset fingerprints, rejects every new/changed finding and scope drift, and allows only removals.</acceptance_criteria>
  <done>All later plan-level Ruff commands have one deterministic no-new-findings gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize release history, public shapes, lazy inspection, and Windows scope</name>
  <files>docs/lifecycle-authority.md, tests/_lifecycle_test_support.py, tests/test_blob_store_read_contract.py, tests/test_lifecycle_authority_contract.py, tests/test_phase3_release_evidence.py</files>
  <read_first>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/reconciliation.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/operation_repository.py</file>
    <file>src/cacheness/storage/path_security.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.file-native-final.md</file>
  </read_first>
  <behavior>
    - "CacheReason lifecycle values, typed error bases, get_metadata dictionaries, zero-argument ReconciliationFinding.to_dict/ReconciliationReport.to_dict, and human_summary retain their current exact public shapes. Characterization labels those reconciliation dictionaries as the legacy v1 compatibility view and reserves a separate versioned additive machine view for every D-13 field in Plan 05."
    - "Git evidence proves the scheduler was introduced after origin/main at a22f4b4, is absent from all tags, and has no released fixture; the documented action for its development stores is explicit rebuild, never scheduler replay."
    - "Deterministic test hooks expose authority transaction begin/end and payload stage/publish/verify/cleanup boundaries without sleeps as ordering evidence."
    - "A never-written compatible root has byte-for-byte identical directory state before and after get, get_metadata, exists, list, close, reopen, and repeated reconciliation dry-run."
    - "Existing payload, metadata, legacy, scheduler, future, corrupt, mixed, or wrong-object evidence without authority is distinguishable from empty and requires rebuild/migration without mutation."
    - "Windows support is limited to the current access token's S-1-5-5-X-Y logon SID on one pre-provisioned local NTFS authority root; persistent account SID, service-plus-interactive, cross-session, and non-local mutation claims are unsupported, and Cacheness never creates or alters that root/DACL."
  </behavior>
  <action>Write failing characterization tests before production changes. Freeze existing lower-snake-case `CacheReason` values/error inheritance, public metadata dictionary keys, and redacted reconciliation dictionary/human-summary fields. Explicitly name the zero-argument reconciliation dictionaries the legacy v1 compatibility view: callers keep their exact keys and values, while Plan 05 must add a separately versioned canonical machine view rather than overload or silently reshape v1. Add deterministic release evidence using `git for-each-ref --contains`, tracked manifests, built artifacts when present, fixtures, changelog, and format docs; emit the exact contradiction and halt if evidence shows release. Record the scheduler as local untagged development state after `origin/main`, document a scoped rebuild, and per D-23/D-30 add no reader/replay/migration executor. Add deterministic barriers, subprocess helpers, fault injection, transaction observers, and payload observers. Characterize absent/empty roots separately from every established authority-missing artifact class using bounded `lstat`/name/type snapshots before mkdir or SQLite. Permit only contained regular existing authority files to receive read-only identity/integrity open without journal/sidecar. Snapshot path/type/mode/mtime/size/digest around inspection and close/reopen. Document D-21/D-22: Windows mutation is scoped to the current token's `S-1-5-5-X-Y` logon SID, never persistent account SID; the local NTFS root must be provisioned before Cacheness starts with inheritance disabled, no account/inherited/unrelated write ACE, and only narrowly documented non-mutating SYSTEM/Administrators entries. Provide an exact actionable offline PowerShell/`icacls.exe` provisioning command that runs only while the store is unavailable. Require Cacheness to validate token/root/DACL before authority database creation/open and every mutation while never creating the Windows root, changing mode, disabling inheritance, or editing ACEs. Characterize read-only absent/empty inspection as zero-mutation empty and every attempted mutation on an absent/unsafe/drifted root as typed, unchanged failure carrying the provisioning command. Require same-session positive and different-session/service-token negative evidence, no non-local sharing, and no custom Win32 lock authority. Finish by running the Task 1 Ruff delta gate.</action>
  <verify>
    <automated>.venv/bin/pytest -q tests/test_phase3_release_evidence.py tests/test_blob_store_read_contract.py tests/test_lifecycle_authority_contract.py -x &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>Public shapes are exact; release/rebuild evidence is fail-closed; deterministic fixtures exist; exact before/after tests distinguish empty from established authority-missing roots; and the Windows pre-provisioned-root/logon-SID/DACL contract includes actionable offline provisioning while proving Cacheness performs no root/mode/ACL mutation.</acceptance_criteria>
  <done>Compatibility, scheduler release, lazy bootstrap, and Windows support boundaries are frozen before authority creation.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Confirm persisted authority identity</name>
  <decision>Confirm the versioned local authority locator and persisted schema identity before database creation</decision>
  <context>The recommended contract stores the sole local authority at `.cacheness/lifecycle-authority-v1.sqlite3`, uses SQLite application ID `0x43414348`, schema user_version `1`, and a generated store identity row. Changing these after stores exist requires a schema/locator migration. D-26 and D-31 already approve SQLite; this checkpoint confirms the exact one-way persisted identity.</context>
  <options>
    <option id="confirm-recommended">
      <name>Confirm recommended contract</name>
      <pros>Versioned reserved namespace, explicit wrong-store rejection, and one concrete cross-platform locator for every later plan.</pros>
      <cons>A later locator or schema-identity change requires migration support.</cons>
    </option>
    <option id="revise-before-write">
      <name>Revise persisted identity</name>
      <pros>Allows a different locator/application identifier before any authority database is created.</pros>
      <cons>Stops execution until this plan and downstream collision tests are updated.</cons>
    </option>
  </options>
  <resume-signal>Select `confirm-recommended` to continue, or provide the replacement locator/application identity.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository history → compatibility decision | Untrusted or incomplete release evidence must not authorize deletion. |
| persisted public data → characterization assertions | Existing error/report/metadata values are caller-visible contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering | release-history classification | high | mitigate | Check tags, remote ancestry, fixtures, and changelog; halt on contradictory evidence. |
| T-03-02 | Information Disclosure | test/log fixtures | medium | mitigate | Use redacted fingerprints and bounded reason codes; never include payloads, keys, raw locators, or exception reprs. |
| T-03-03 | Spoofing | reserved authority identity | high | mitigate | Confirm versioned locator, application ID, user version, and store identity before creation. |
</threat_model>

<verification>
Run the characterization selection and inspect `docs/lifecycle-authority.md`; all six flagged edge probes remain visible and routed to later automated tests.
</verification>

<success_criteria>
- No executor must guess whether scheduler state is released.
- Public errors, reconciliation reports, and JSON-shaped metadata are frozen before refactoring.
- The exact contained authority locator and schema identity are human-confirmed before creation.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-01-SUMMARY.md` when done.
</output>
