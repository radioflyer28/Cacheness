---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "13"
type: execute
wave: 13
depends_on: ["03-11", "03-12"]
files_modified:
  - tests/test_phase1_quality_gates.py
  - tests/test_full_suite_environment.py
  - docs/CROSS_PLATFORM_GUIDE.md
  - .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The complete repository suite has one documented, frozen, all-extras invocation that runs in an isolated uv environment and exits zero without mutating the active project environment."
    - "The Phase 1 Ruff quality gate executes the Ruff binary from the already-running test environment; it never invokes uv dependency resolution from inside pytest."
    - "The observed public-import, S3/botocore, and SQL/pandas failures are classified as one environment-mutation cascade, while any failure that remains in a fresh isolated environment is treated as a repository behavior defect and fixed rather than skipped or hidden."
    - "Plan 03-10 is summarized and marked complete only after the full repository suite and every original Plan 03-10 release gate pass from stable environments."
    - "Darwin evidence remains UNAVAILABLE/NOT_QUALIFIED and cannot be represented as native Windows support per D-32."
  artifacts:
    - path: tests/test_full_suite_environment.py
      provides: "Regression contract for non-mutating in-suite tooling and the documented full-suite command"
    - path: docs/CROSS_PLATFORM_GUIDE.md
      provides: "Exact supported full-suite invocation and distinction from base-install checks"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md
      provides: "Plan 03-10 closeout backed by actually green final gates"
  key_links:
    - from: tests/test_phase1_quality_gates.py
      to: tests/test_full_suite_environment.py
      via: "The regression test proves Ruff is resolved from the current interpreter environment without nested uv synchronization"
      pattern: "ruff|sys.executable|scripts"
    - from: docs/CROSS_PLATFORM_GUIDE.md
      to: pyproject.toml
      via: "The frozen isolated command installs all declared extras plus the dev test group from uv.lock"
      pattern: "--isolated|--all-extras|--group dev|--frozen"
    - from: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md
      via: "Final acceptance records UNAVAILABLE/NOT_QUALIFIED and retains Phase 999.1 as the native Windows gate"
      pattern: "UNAVAILABLE|NOT_QUALIFIED|999.1"
  prohibitions:
    - "Do not add optional SQL, pandas, S3, or TensorFlow dependencies to the base runtime merely to make the repository test suite collect."
    - "Do not skip, deselect, xfail, suppress, or reorder failing tests to manufacture a green result."
    - "Do not treat the post-Ruff cascade as production defects unless the same node fails in a fresh isolated all-extras environment."
    - "Do not edit, stage, or commit protected codebase maps, .planning/config.json, .planning/milestone.lock, .claude, CONCERNS.md, or historical review artifacts."
---

<objective>
Remove the test-environment mutation that invalidated Plan 03-10's repository gate, define one reproducible supported full-suite command, and close Plan 03-10 only on fresh green evidence.

Purpose: The benchmark and configuration work in commits `c5dc418`, `60f1850`, and `e9f6fa6` passed its owned gates, but final acceptance cannot be claimed while the repository command either lacks optional test dependencies or destroys them mid-run.
Output: A regression-tested non-mutating Ruff gate, an exact isolated full-suite command, and an evidence-backed Plan 03-10 summary/tracking closeout.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-PLAN.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@pyproject.toml
@tests/test_phase1_quality_gates.py
@tests/test_public_api_contract.py
@tests/test_s3_blob_backend.py
@tests/test_sql_cache.py
@tests/test_sql_cache_documentation.py
@tests/test_sql_cache_failure_contract.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keep optional dependencies intact across the in-suite Ruff gate</name>
  <files>tests/test_phase1_quality_gates.py, tests/test_full_suite_environment.py, docs/CROSS_PLATFORM_GUIDE.md</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>tests/test_phase1_quality_gates.py</file>
    <file>tests/test_public_api_contract.py</file>
    <file>tests/test_s3_blob_backend.py</file>
    <file>tests/test_sql_cache.py</file>
    <file>tests/test_sql_cache_documentation.py</file>
    <file>tests/test_sql_cache_failure_contract.py</file>
    <file>docs/CROSS_PLATFORM_GUIDE.md</file>
  </read_first>
  <behavior>
    - "A regression test fails against the current `_ruff_findings()` argv because it starts nested dependency resolution with `uv run ruff`."
    - "Ruff is resolved only from the scripts directory belonging to the running `sys.executable`, including the platform executable suffix, and an absent current-environment Ruff fails explicitly."
    - "The Ruff subprocess retains the existing check scope and JSON parsing contract without invoking uv or modifying installed packages."
    - "The regression test records the exact failure classification: bare `uv run pytest --collect-only` lacks SQLAlchemy for `test_custom_metadata.py` and pandas for the three SQL-cache modules; the non-isolated all-extras run then loses live package files after the nested Ruff command and cascades into three public-import failures, 23 S3/botocore setup errors, and 13 SQL/pandas failures."
    - "The documented supported command is exactly `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false`; it neither broadens base runtime dependencies nor excludes any repository test module."
  </behavior>
  <action>Write `tests/test_full_suite_environment.py` first. Make it assert that the Phase 1 quality gate launches the Ruff executable from the same interpreter environment and that its command cannot contain `uv`; also assert the exact documented full-suite command and the pyproject dependency contract needed by `--all-extras --group dev`. Then change `_ruff_findings()` to locate Ruff only in the running interpreter's scripts directory and call that executable directly, preserving the existing exit-code and JSON-result validation. Do not use a PATH-wide lookup that could select a global Ruff, and do not replace the gate with an import-time lint library. Update `docs/CROSS_PLATFORM_GUIDE.md` with the exact frozen isolated command and explain that bare `uv run pytest` is not the complete-suite contract because optional-feature test modules intentionally exercise dependencies from extras. Record the reproduced matrix above as a test-invocation/test-isolation classification, not as evidence of S3, pandas, SQL-cache, or public-API production defects: a fresh isolated all-extras run was green before this plan. If a listed node still fails after the isolation fix in a newly created isolated environment, treat that repeatable result as a real repository behavior failure, add a focused regression test, and fix the responsible implementation without skip/xfail/error suppression; do not change production code for a failure that exists only after the shared environment was mutated.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase1_quality_gates.py tests/test_full_suite_environment.py tests/test_public_api_contract.py tests/test_s3_blob_backend.py tests/test_sql_cache.py tests/test_sql_cache_documentation.py tests/test_sql_cache_failure_contract.py -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The affected post-Ruff slice remains green in one stable isolated environment, the regression proves no nested uv synchronization occurs, and the repository documents one exact complete-suite command.</acceptance_criteria>
  <done>Running the quality gate no longer invalidates imports or optional dependency data needed by later tests.</done>
</task>

<task type="auto">
  <name>Task 2: Run final release gates and close Plan 03-10 on evidence</name>
  <files>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-PLAN.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
    <file>tools/capture_phase3_windows_qualification.py</file>
    <file>tools/verify_phase3_ruff_delta.py</file>
  </read_first>
  <action>Run the exact supported full-suite command in a newly created isolated environment. Require normal pytest collection of the entire `tests/` tree: do not add `--ignore`, `-k`, marker deselection, max-fail, or new skips. Then rerun the original Plan 03-10 benchmark verifier, Python 3.11 focused gate, compatibility corpus validator, Phase 3 Ruff-delta verifier, direct Ruff scope, and qualification-attestation verifier. D-32 remains exact: the fresh Darwin qualification command must return exit 2 with `UNAVAILABLE`, `NOT_QUALIFIED`, and `native_evidence: false`; Phase 999.1 still requires native Windows PASS/exit 0 before a Windows-qualified release. If any fresh stable-environment test fails, stop closeout, classify it as packaging, test isolation, or repository behavior using its independently reproducible node and traceback, and fix it with a focused regression test; never summarize the prior mutating run as green. Only after every command exits as required, create `03-10-SUMMARY.md` from the standard template, attribute the benchmark/configuration implementation to `c5dc418`, `60f1850`, and `e9f6fa6`, record the Plan 03-13 isolation fix and exact full-suite command, and update only ordinary Phase 3 ROADMAP/STATE completion tracking. Do not stage protected user-owned paths.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_qualification_attestation.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check src/cacheness/storage/lifecycle_authority.py src/cacheness/storage/sqlite_lifecycle_authority.py src/cacheness/storage/memory_lifecycle_authority.py tests/_lifecycle_test_support.py tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_phase3_release_evidence.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_contract.py tests/test_phase3_windows_qualification_attestation.py tests/test_phase3_ruff_delta.py tests/test_full_suite_environment.py tools/capture_phase3_windows_qualification.py tools/verify_phase3_ruff_delta.py benchmarks/lifecycle_authority_benchmark.py --output-format concise &amp;&amp; test -f .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>The complete repository suite and all original Plan 03-10 gates pass from frozen isolated environments; only then does Plan 03-10 have a summary and completed tracking, while Windows remains explicitly unqualified per D-32.</acceptance_criteria>
  <done>Plan 03-10 and Phase 3 final acceptance are supported by reproducible stable-environment evidence rather than a waived or corrupted test run.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Old-or-new complete generations and recoverable incomplete outcomes | Plans 03-02 through 03-10 implement the lifecycle; Plan 03-13 restores the required trustworthy repository gate before closeout | COVERED |
| REQ | STOR-03 atomic visibility | Existing implementation plus full stable regression in Task 2 | COVERED |
| REQ | STOR-04 detectable/recoverable residue | Existing implementation plus full stable regression in Task 2 | COVERED |
| REQ | STOR-05 idempotent lifecycle convergence | Existing implementation plus full stable regression in Task 2 | COVERED |
| REQ | STOR-06 bounded resumable reconciliation | Existing implementation plus benchmark/reconciliation gates in Task 2 | COVERED |
| REQ | STOR-07 deterministic races and distinct-key overlap | Existing implementation plus Python 3.11 and concurrency gates in Task 2 | COVERED |
| RESEARCH | Full repository, supported-Python, compatibility, Ruff-delta, and direct-Ruff release gates | Task 2 executes every gate from frozen isolated environments | COVERED |
| RESEARCH | Optional dependency/runtime checks stay aligned with packaging extras | Task 1 defines all-extras plus dev as the full-suite environment without broadening base dependencies | COVERED |
| CONTEXT | D-23 through D-31 transactional lifecycle authority | No lifecycle design changes; Task 2 reruns their existing behavioral gates | COVERED |
| CONTEXT | D-32 honest Windows deferral | Task 2 requires fresh UNAVAILABLE/NOT_QUALIFIED evidence and preserves Phase 999.1 | COVERED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| pytest process to tooling subprocess | A quality-gate subprocess must not replace packages beneath the running interpreter. |
| repository lock to isolated test environment | The full suite must resolve only the checked-in project/extras/dev contract without mutating the active environment. |
| platform evidence to release claim | Darwin UNAVAILABLE evidence cannot cross into a native Windows support claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-13-01 | Tampering | `tests/test_phase1_quality_gates.py::_ruff_findings` | high | mitigate | Execute Ruff from the running interpreter's scripts directory and regression-test that no nested uv command can resynchronize packages. |
| T-03-13-02 | Denial of Service | Full-suite dependency setup | medium | mitigate | Use frozen isolated all-extras plus dev resolution so collection is complete and active project packages cannot be removed mid-run. |
| T-03-13-03 | Repudiation | Plan 03-10 completion record | high | mitigate | Create the summary/tracking only after exact full-suite and release commands exit successfully; record commits and the supported command. |
| T-03-13-04 | Spoofing | Windows qualification status | high | mitigate | Re-run the fixed D-32 verifier and require UNAVAILABLE/NOT_QUALIFIED/native_evidence false on Darwin. |
</threat_model>

<verification>
The affected post-Ruff slice passes first, then the complete repository suite and all Plan 03-10 release gates pass in frozen isolated environments with no hidden collection or deselection.
</verification>

<success_criteria>
- The current-environment Ruff regression is green and cannot invoke uv.
- The exact documented frozen isolated all-extras command passes the complete repository suite.
- No cascade failure is silenced or misclassified as a production defect.
- Plan 03-10 summary/tracking exists only after all gates are green.
- D-32 remains UNAVAILABLE/NOT_QUALIFIED on Darwin, with Phase 999.1 unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-13-SUMMARY.md` after both tasks complete. Its frontmatter must record the exact full-suite command, the stable-environment result, the Plan 03-10 summary path, and the D-32 UNAVAILABLE/NOT_QUALIFIED disposition.
</output>
