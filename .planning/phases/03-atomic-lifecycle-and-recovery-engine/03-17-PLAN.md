---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "17"
type: execute
wave: 17
depends_on: ["03-16"]
files_modified:
  - src/cacheness/metadata.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - tests/test_sqlite_bootstrap_concurrency.py
  - tests/test_phase3_gap_acceptance.py
autonomous: true
gap_closure: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Two independent authority instances and two spawned processes can start mutations against a genuinely absent root; the loser joins only a bounded, reclassified valid winner and both operations reach deterministic authority outcomes."
    - "A wrong-object or symlink substitution at root, reserved directory, or database leaf during bootstrap is rejected unchanged and never joined as a winner."
    - "SqliteBackend has explicit idempotent close ownership and interpreter shutdown emits no destructor exception."
    - "Every CR-01 through CR-09 schedule is forced through real facade/backend paths, all focused/full/compatibility/Ruff gates pass, and current Darwin Windows evidence remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false."
  artifacts:
    - path: src/cacheness/storage/sqlite_lifecycle_authority.py
      provides: "Bounded cross-instance/process fresh-root winner reclassification"
    - path: src/cacheness/metadata.py
      provides: "Explicit idempotent SqliteBackend close without unsafe finalizer work"
    - path: tests/test_sqlite_bootstrap_concurrency.py
      provides: "Unseeded two-instance/process bootstrap and substitution schedules"
    - path: tests/test_phase3_gap_acceptance.py
      provides: "Cross-plan adversarial assertions and release-gate inventory"
  key_links:
    - from: src/cacheness/storage/sqlite_lifecycle_authority.py
      to: tests/test_sqlite_bootstrap_concurrency.py
      via: "Named bootstrap hooks force both actors past missing classification before root/leaf winner selection"
      pattern: "bootstrap.*(hook|classif|winner)|FileExistsError"
    - from: src/cacheness/metadata.py
      to: tests/test_phase3_gap_acceptance.py
      via: "Context-managed explicit close is idempotent and a subprocess exits without finalizer diagnostics"
      pattern: "close|__exit__|Exception ignored"
    - from: tests/test_phase3_gap_acceptance.py
      to: tests/test_unified_cache_adversarial_lifecycle.py
      via: "Final acceptance enumerates and executes the real-path CR-01 through CR-09 schedule matrix"
      pattern: "CR-0[1-9]|gap.*acceptance"
  prohibitions:
    - "[UNVERIFIED] Fresh-root joining cannot accept an unclassified object, symlink, arbitrary established store, or unbounded wait."
    - "[UNVERIFIED] Darwin UNAVAILABLE evidence cannot be promoted to native Windows qualification."
---

<objective>
Close fresh-root authority bootstrap races, remove unsafe SQLite finalization, and run one adversarial release acceptance matrix over all nine blockers.

Purpose: Even correct lifecycle and projection transactions cannot converge if independent first mutators fail at root creation, and shutdown diagnostics obscure process-level evidence. The final plan proves every repaired interleaving through production facade/backend paths and reruns Phase 3's exact release gates.
Output: Safe bounded bootstrap winner joining, explicit SqliteBackend close behavior, complete CR/WR regression coverage, and fresh full-suite/compatibility/Ruff evidence without a Windows qualification claim.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-REVIEW.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VERIFICATION.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-16-SUMMARY.md
@src/cacheness/storage/sqlite_lifecycle_authority.py
@src/cacheness/metadata.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Join only a valid bounded fresh-root SQLite bootstrap winner</name>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, tests/test_sqlite_bootstrap_concurrency.py</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
    <file>tests/test_blob_store_concurrency.py</file>
    <file>tests/_lifecycle_test_support.py</file>
  </read_first>
  <behavior>
    - "Two authority instances pause after classifying one root missing, race root/reserved/database creation, then both finish: one creates the authority and the loser safely reclassifies/joins it within the existing absolute deadline."
    - "Two multiprocessing-spawn workers repeat the same unseeded schedule against a fresh root and distinct keys; both construct their own authority/store and both entries are committed without FileExistsError."
    - "The same schedule for one key yields one expected-generation winner and one typed conflict, not bootstrap failure or corruption."
    - "If the losing actor observes a root/reserved/database wrong object or symlink after FileExistsError/reclassification, it fails with the established typed migration/path outcome and every hostile byte/object remains unchanged."
  </behavior>
  <action>Add a narrow test-only bootstrap observer at classification/root/reserved/leaf boundaries so independent instances and spawn workers can deterministically reach the missing-root race; it must not alter production ordering when unset. Catch root `mkdir(..., exist_ok=False)` FileExistsError and immediately restart exact safe classification under the original absolute deadline. Join only the already-defined recognized bootstrap states: valid contained non-symlink root, valid empty reserved namespace or regular authority leaf with the confirmed application ID/schema/store identity once initialization completes. If the winner's leaf exists but is still initializing, wait/reclassify only to the existing deadline; wrong root, symlink, non-directory reserved object, non-regular database, arbitrary established evidence, or invalid identity fails typed and unchanged. Reuse the O_EXCL database leaf and stdlib SQLite transaction initialization; do not introduce a process/global lockfile, authority sidecar, or second lifecycle authority. Force unseeded two-instance and two-process distinct-key and same-key schedules with `multiprocessing.get_context("spawn")`, Events/Barriers, and no sleep-based success condition. Preserve D-02/D-05/D-06/D-17 through D-21 and the SQLite authority architecture D-23 through D-28/D-31.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_sqlite_bootstrap_concurrency.py -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>Fresh unseeded independent instances/processes join one valid authority within the existing bound, same-key authority CAS remains deterministic, and every wrong-object/symlink substitution is rejected unchanged.</acceptance_criteria>
  <done>CR-05 is closed without adding file-native authority or weakening safe bootstrap classification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove shutdown finalizer noise and execute the complete adversarial release gate</name>
  <files>src/cacheness/metadata.py, tests/test_phase3_gap_acceptance.py</files>
  <read_first>
    <file>src/cacheness/metadata.py</file>
    <file>tests/test_unified_cache_adversarial_lifecycle.py</file>
    <file>tests/test_projection_sql_atomicity.py</file>
    <file>tests/test_cached_custom_metadata.py</file>
    <file>tests/test_sqlite_bootstrap_concurrency.py</file>
    <file>tools/verify_phase3_ruff_delta.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md</file>
  </read_first>
  <behavior>
    - "SqliteBackend.close is idempotent and context-managed; a subprocess that constructs/closes or lets process teardown reclaim a backend exits without `Exception ignored`, import/logging finalizer failure, or leaked database handles."
    - "The acceptance inventory maps CR-01 through CR-09 and WR-01/WR-02 to at least one deterministic real facade/backend test and fails if a required schedule is missing, skipped, xfailed, or replaced only by direct private-helper coverage."
    - "The exact frozen isolated full suite, established Python 3.11 lifecycle/close/reconciliation/concurrency/containment modules, new gap matrix, checked-in lifecycle benchmark baseline verifier, qualification-attestation verifier, compatibility corpus through sqlite-columns-v0314, Phase 3 Ruff delta, and direct Ruff on every new gap test file pass."
    - "D-32 remains Darwin UNAVAILABLE/NOT_QUALIFIED with native_evidence false; no test, summary, or status claims native Windows or live PostgreSQL qualification."
  </behavior>
  <action>Prefer removing `SqliteBackend.__del__`; make explicit/context-managed `close()` idempotently detach/dispose the engine without importing `gc` or logging during interpreter finalization. If an unavoidable compatibility finalizer remains, it may only read already-bound attributes, catch BaseException, perform no imports/logging, and delegate no complex cleanup; the subprocess regression must prove silent shutdown. Add a small acceptance-inventory test that names CR-01 through CR-09 and asserts their deterministic test nodes are collected and active; it supplements rather than substitutes for running the tests. Require the interleavings to exercise real UnifiedCache/BlobStore/backend call paths: admitted put versus close/clear, linked M1 failures, two pending candidates, independent SQL absence/existing races, unseeded authority bootstrap, cached custom metadata, empty canonical first put, hostile locators, and signed PostgreSQL mapping. Run the new gap files plus the established Python 3.11 lifecycle authority, SQLite authority, atomic lifecycle, close, reconciliation, concurrency, and filesystem-containment modules; then run the exact frozen isolated full suite, the checked-in lifecycle benchmark baseline verifier, compatibility corpus, the existing qualification-attestation verifier in verify-only mode, Ruff delta, and direct Ruff on every new gap test module. The attestation verifier must consume the existing Plan 03-09 artifact and completed summaries/validation exactly as checked in; do not invoke capture mode, recapture, rewrite, or update any Windows evidence artifact. Existing production paths remain governed by the checked-in Phase 3 delta baseline; do not regenerate it from post-fix code or grandfather a finding. Treat optional live PostgreSQL and native Windows checks honestly: deterministic PostgreSQL dialect/transaction behavior is required locally, live-service PASS is not claimed, and D-22/D-31/D-32 remain UNAVAILABLE/NOT_QUALIFIED on Darwin with Phase 999.1 requiring native Python 3.11 PASS/exit 0. Preserve bounded reconciliation D-13 through D-16/D-29 and scheduler retirement D-23/D-30.</action>
  <verify>
    <automated>uv run --isolated --python 3.11 --all-extras --group dev --frozen pytest -q tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_close_contract.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py tests/test_filesystem_containment.py tests/test_unified_cache_adversarial_lifecycle.py tests/test_projection_sql_atomicity.py tests/test_cached_custom_metadata.py tests/test_sqlite_bootstrap_concurrency.py tests/test_phase3_gap_acceptance.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase3_ruff_delta.py -x -o log_cli=false &amp;&amp; uv run --isolated --all-extras --group dev --frozen python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_unified_cache_adversarial_lifecycle.py tests/test_projection_sql_atomicity.py tests/test_cached_custom_metadata.py tests/test_sqlite_bootstrap_concurrency.py tests/test_phase3_gap_acceptance.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>No SQLite shutdown diagnostic remains; every fresh finding has a forced real-path regression; established Python 3.11 lifecycle modules, full suite, benchmark baseline, qualification attestation, compatibility, and Ruff gates are green; and existing evidence retains honest Windows/PostgreSQL qualification status without recapture.</acceptance_criteria>
  <done>WR-01 and WR-02 are closed and Phase 3 has fresh integrated evidence for STOR-03 through STOR-07.</done>
</task>

</tasks>

## Gap Coverage Audit

| Finding | Owner | Deterministic acceptance evidence | Status |
|---|---|---|---|
| CR-01 unadmitted facade put | 03-15 Task 1 | Real facade put paused after admission versus close and clear snapshot | COVERED |
| CR-02 destructive pre-promotion links | 03-15 Task 2 | Linked M1 plus signing/verification/promotion failures | COVERED |
| CR-03 peer pending token adoption | 03-15 Task 2 | Two M1-based pending candidates before either promotion | COVERED |
| CR-04 non-atomic SQL CAS | 03-16 Task 1 | Independent SQLite instance/process absent/existing races and PostgreSQL transaction schedules | COVERED |
| CR-05 fresh-root bootstrap race | 03-17 Task 1 | Unseeded two-instance and spawn-process schedules plus substitutions | COVERED |
| CR-06 cached custom metadata disabled | 03-16 Task 2 | Public memory-cached SQLite/PostgreSQL put/get/query/session/replacement workflows | COVERED |
| CR-07 empty canonical legacy inference | 03-15 Task 3 | Two-facade first-put versus clear/invalidate | COVERED |
| CR-08 hostile put locator | 03-15 Task 3 | Top-level/nested absolute/traversal/symlink zero-mutation matrix | COVERED |
| CR-09 PostgreSQL key-param nesting | 03-16 Task 3 | Signed public facade get/list/stats without retirement | COVERED |
| WR-01 destructor exception | 03-17 Task 2 | Explicit close/idempotency and clean subprocess teardown | COVERED |
| WR-02 missing forced schedules | 03-15 through 03-17 | Acceptance inventory plus execution of all real-path nodes | COVERED |

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Old-or-new complete generation with every incomplete outcome recoverable | 03-15 through 03-17 | COVERED | Admission, post-promotion projection ownership, atomic SQL, and safe bootstrap close the fresh review gaps. |
| REQ | STOR-03 | No partial/mixed payload and metadata state | 03-15 Tasks 1-3; 03-16 Tasks 1/3 | COVERED | Exact admission, pure candidate projection, SQL CAS, hostile preflight, and signing parity. |
| REQ | STOR-04 | Failed write preserves valid generation and recoverable residue | 03-15 Task 2; 03-16 Task 2 | COVERED | Linked M1 survives pre-promotion failures; stale links/objects never attach. |
| REQ | STOR-05 | Idempotent overwrite/delete/clear/close cleanup | 03-15 Tasks 1/3; 03-17 Tasks 1/2 | COVERED | Exactly-once admission, canonical empty cleanup, bootstrap convergence, explicit close. |
| REQ | STOR-06 | Safe dry-run/resumable reconciliation | 03-15 Task 2; 03-17 Task 2 | COVERED | No destructive pre-promotion evidence loss; existing bounded authority reconciliation is regression-gated. |
| REQ | STOR-07 | Deterministic same-key races without global distinct-key serialization | 03-15 Tasks 1/2; 03-16 Task 1; 03-17 Task 1 | COVERED | Forced pending, SQL, bootstrap, clear, close, and distinct-key schedules. |
| RESEARCH | — | LifecycleAuthority remains the sole complete transactional authority | 03-15 through 03-17 | COVERED | Projection/cache/link state stays derived; no scheduler or secondary authority returns. |
| RESEARCH | — | Immutable native payload work stays outside short transactions | 03-15 Tasks 1/2; 03-16 Task 1 | COVERED | Admission exclusion is snapshot-only; SQL transactions contain rows/links only. |
| RESEARCH | — | Bounded deterministic fault/process coverage and checked release gates | 03-15 through 03-17 | COVERED | Named hooks/barriers, spawn workers, complete suite, compatibility, Ruff. |
| CONTEXT | D-01–D-04 | Immutable generations, exact CAS, committed-only reads, promotion authority | 03-15 Task 2; 03-16 Tasks 1/3 | COVERED | Projection/link ownership follows exact successful promotion. |
| CONTEXT | D-05–D-08 | Durable intent/provenance and pre/post-promotion failure classification | 03-15 Task 2; 03-17 Task 1 | COVERED | Candidate residue remains authority-indexed; bootstrap joins only proven state. |
| CONTEXT | D-09–D-12 | Tombstone, clear, idempotent cleanup, ownership-aware close | 03-15 Tasks 1/3; 03-17 Task 2 | COVERED | Canonical empty clear and admitted close have forced schedules. |
| CONTEXT | D-13–D-16 | Bounded safe resumable reconciliation without guessing | 03-15 Tasks 2/3; 03-17 Task 2 | COVERED | Repairs retain authority evidence and final suite reruns reconciliation contracts. |
| CONTEXT | D-17–D-20 | Per-key ordering, backend CAS, exact races, distinct-key concurrency | 03-15 Tasks 1/2; 03-16 Task 1; 03-17 Task 1 | COVERED | No whole-cache lock; database and authority CAS arbitrate independent actors. |
| CONTEXT | D-21–D-22 | Trusted local owner and limited Windows topology | 03-15 Task 3; 03-17 Tasks 1/2 | COVERED | Fail-closed paths remain and no native Windows proof is claimed. |
| CONTEXT | D-23–D-31 | Transactional-authority replacement, JSON projection, SQLite, concurrency, retirement | 03-15 through 03-17 | COVERED | Changes deepen existing authority/projection seams and introduce no alternate protocol/ORM schema. |
| CONTEXT | D-32 | Windows qualification deferred without eligible environment | 03-17 Task 2 | COVERED | Darwin remains UNAVAILABLE/NOT_QUALIFIED, native_evidence false; Phase 999.1 unchanged. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Missing filesystem root → authority bootstrap | A loser may join only exact safe winner state under one deadline. |
| Interpreter shutdown → SQL resource cleanup | Finalization cannot import/log/raise or obscure process results. |
| Focused test evidence → release claim | Every blocker needs deterministic production-path evidence; optional platforms/services remain explicitly unqualified. |
| Projection repair → reconciliation evidence | Derived cleanup cannot erase the only canonical intent/debt or last valid generation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-17-01 | Spoofing | Fresh-root winner classification | high | mitigate | Reclassify after FileExistsError and accept only exact contained regular authority state with confirmed identity/schema. |
| T-03-17-02 | Tampering | Symlink/wrong-object bootstrap substitution | high | mitigate | lstat each root/reserved/leaf transition and reject unchanged before SQLite open/adoption. |
| T-03-17-03 | Denial of Service | Bootstrap winner wait | high | mitigate | Reuse one absolute LifecycleLimits deadline and terminate with a typed timeout/migration outcome. |
| T-03-17-04 | Repudiation | Shutdown diagnostics | medium | mitigate | Prefer explicit idempotent close and remove destructor work; subprocess stderr is an automated gate. |
| T-03-17-05 | Repudiation | Windows/PostgreSQL qualification | high | mitigate | Separate deterministic local contract evidence from unavailable native/live qualification and preserve D-32 fields. |
| T-03-17-06 | Tampering | Regression evidence substitution | high | mitigate | Collect named CR nodes, forbid skip/xfail/private-helper-only substitution, and run the exact full/compatibility/Ruff gates. |
</threat_model>

<verification>
First run the unseeded authority bootstrap schedules, then the full CR-01 through CR-09 focused matrix on Python 3.11. Finish with the exact frozen isolated repository suite, compatibility corpus, Phase 3 Ruff-delta verifier, and direct Ruff over every new gap test. Do not edit or promote Windows qualification evidence and do not treat an unavailable live PostgreSQL service as PASS.
</verification>

<success_criteria>
- Fresh-root independent instances/processes converge on one valid SQLite authority or typed same-key CAS, never FileExistsError.
- Wrong-object/symlink bootstrap states remain unchanged and unadopted.
- SqliteBackend explicit close is idempotent and interpreter shutdown is silent.
- Every CR-01 through CR-09 and WR-01/WR-02 has forced production-path coverage.
- Full suite, compatibility corpus, Ruff delta, and direct Ruff gates pass.
- D-32 remains UNAVAILABLE/NOT_QUALIFIED with native_evidence false and no live PostgreSQL qualification claim.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-17-SUMMARY.md` after both tasks complete. Record bootstrap winner/rejection schedules, destructor disposition, CR/WR node map, exact full/compatibility/Ruff results, Python 3.11 evidence, and unchanged D-32/live-PostgreSQL qualification status.
</output>
