---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "10"
type: execute
wave: 12
depends_on: ["03-11"]
files_modified:
  - benchmarks/lifecycle_authority_benchmark.py
  - benchmarks/lifecycle_authority_baseline.json
  - src/cacheness/config.py
  - tests/test_config_validation.py
  - tests/test_sqlite_lifecycle_authority.py
  - tests/test_blob_store_concurrency.py
  - tests/test_blob_store_reconciliation.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Production-schema measurements establish SQLite transaction, exact clear-snapshot, busy, reconciliation-page, and distinct-key-overlap distributions before any threshold becomes a release gate (D-26, D-28, D-29; bounded busy values use the agent's discretion)."
    - "Checked-in baseline provenance names Python, SQLite, platform/filesystem, schema, cardinalities, repetitions, and percentile distributions; no anecdotal number controls behavior."
    - "Configured row/action/byte/time/deadline budgets derive from measured fields with documented safety multipliers and have an explicit recalibration path."
    - "Final tests, scheduler-negative reachability, real Python 3.11/3.13 evidence, the checked-in Ruff baseline-delta gate, and direct Ruff for every Phase 3-new Python file pass without requiring pre-existing touched-file debt to be clean."
    - "Final acceptance consumes Plan 03-12's completed additive command-role contract plus Plan 03-11's honest UNAVAILABLE/NOT_QUALIFIED Windows artifact and completed qualification summary, does not require an unattainable native Windows PASS, and preserves Phase 999.1 native PASS as a prerequisite before any Windows-qualified release (D-32)."
    - "Final acceptance executes Plan 03-11's attestation helper, compares freshly produced runner bytes with the artifact, immutable 03-09 runner summary, completed 03-12 contract artifact/summary, completed 03-11 qualification summary, and validation mapping, and fails stale, edited, swapped-role, or contradictory evidence (D-32)."
  artifacts:
    - path: benchmarks/lifecycle_authority_benchmark.py
      provides: "Reproducible production-schema authority, clear, busy, paging, and overlap measurement harness"
    - path: benchmarks/lifecycle_authority_baseline.json
      provides: "Measured distributions, environment provenance, and derived threshold inputs"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md
      provides: "Plan 03-11 current-host UNAVAILABLE evidence and NOT_QUALIFIED Windows release boundary consumed by final Phase 3 acceptance (D-32)"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md
      provides: "Plan 03-12 additive distinction between repository-runtime evidence and the fixed Python 3.11 qualification command"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md
      provides: "Completed structured command-role contract bound to immutable Plan 03-09 evidence"
    - path: tools/capture_phase3_windows_qualification.py
      provides: "Deterministic fresh-execution verifier for the qualification artifact, completed summaries, and validation disposition"
    - path: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md
      provides: "Completed qualification-specific structured mirror from the dependent evidence plan"
  key_links:
    - from: benchmarks/lifecycle_authority_baseline.json
      to: src/cacheness/config.py
      via: "Named measured fields and documented safety multipliers"
      pattern: "clear|busy|page|overlap|percentile"
    - from: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md
      to: .planning/ROADMAP.md
      via: "Final acceptance verifies the Phase 999.1 handoff and preserves native PASS as the future Windows-qualified release gate"
      pattern: "UNAVAILABLE|NOT_QUALIFIED|999.1|native PASS"
    - from: tools/capture_phase3_windows_qualification.py
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md
      via: "Structural comparison selects the designated qualification-attestation field and verifies the immutable 03-09 source binding"
      pattern: "repository_runtime_command|qualification_attestation_command|source_summary_sha256"
    - from: tools/capture_phase3_windows_qualification.py
      to: .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md
      via: "Fresh command execution and structural comparison bind the artifact to the completed qualification mirror and D-32 validation rule"
      pattern: "verify|windows-qualification|validation"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No numeric timeout, clear, page, or performance threshold may become a gate before benchmark evidence is recorded."
    - "[FLAGGED/UNVERIFIED] Final Ruff acceptance must not fail on untouched repository baseline findings."
    - "[FLAGGED/UNVERIFIED] UNAVAILABLE must not be promoted to PASS or cited as a native Windows support claim (D-32)."
---

<objective>
Calibrate lifecycle budgets from production-schema measurements and close Phase 3 acceptance.

Purpose: Separate noisy benchmark judgment from correctness/platform proof and make every operational threshold evidence-based.
Output: Reproducible benchmark, checked-in baseline, derived configuration gates, final source-coverage evidence, and verified consumption of Plan 03-11's unqualified-Windows release record.
</objective>

## Artifacts this phase produces

- Exact transactional clear `INSERT ... SELECT`, transition, busy, reconciliation, and overlap benchmark.
- Checked-in multi-run percentile baseline with host/schema provenance.
- Configuration defaults and release multipliers derived only after measurement.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure production-schema lifecycle distributions</name>
  <reversibility rating="reversible">The checked-in harness and baseline can be recalibrated without changing authority semantics.</reversibility>
  <files>benchmarks/lifecycle_authority_benchmark.py, benchmarks/lifecycle_authority_baseline.json</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>benchmarks/README.md</file>
    <file>benchmarks/threshold_benchmark.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md</file>
  </read_first>
  <behavior>
    - "The harness invokes the production SqliteLifecycleAuthority schema and exact clear-target INSERT SELECT path at representative cardinalities."
    - "Warmups and repeated samples record p50/p95/p99 for short transitions, clear snapshot writer hold, held-writer busy expiry, bounded reconciliation pages, and synchronized distinct-key overlap."
    - "Baseline JSON records command, commit/schema identity, Python/SQLite/platform/filesystem, cardinalities, repetitions, raw summary distributions, and no payload/key secrets."
    - "The baseline uses stable fields `metrics.authority_transition.seconds`, `metrics.clear_snapshot.seconds_by_target_count`, `metrics.busy_wait.seconds`, `metrics.reconciliation_page.rows/actions/bytes/seconds`, and `metrics.distinct_key_overlap.ratio`, each with the applicable sample arrays and p50/p95/p99 or p05 distributions."
    - "The baseline records `multipliers.busy_deadline`, `multipliers.reconciliation_rows`, `multipliers.reconciliation_actions`, `multipliers.reconciliation_bytes`, `multipliers.reconciliation_time`, and benchmark-only latency/overlap multipliers separately from `derived.configuration` and `derived.release_envelopes`."
    - "Verify-baseline and recalibrate modes are explicit; the first baseline is measurement output rather than a hand-authored threshold."
  </behavior>
  <action>Use `benchmarks/threshold_benchmark.py:28-148` as the assigned analog: retain its standalone executable shape, explicit scenario matrix, controlled temporary fixture creation, repeated raw sample arrays, derived distribution records, consistency assertions, and JSON result emission. Add the stronger provenance and baseline-verification contract required here instead of copying its machine-dependent threshold assumptions. Build the reproducible benchmark directly against `SqliteLifecycleAuthority` and the production schema. Measure warmups plus repeated percentile distributions for ordinary complete transitions, exact one-transaction clear target insertion at representative local-store sizes, held-writer busy expiration, bounded reconciliation pages, and barrier-gated distinct-key payload overlap. Emit the exact `metrics.*`, `multipliers.*`, `derived.configuration`, and `derived.release_envelopes` fields named in this task's behavior; include the executable command, commit/schema identity, Python/SQLite/platform/filesystem, cardinalities, repetitions, and raw samples needed to audit every derivation. Provide separate `--record-baseline` and `--verify-baseline` modes; verification recomputes derivations, validates schema/field/provenance consistency, and enforces measured regression envelopes, while recalibration requires an explicit command and never silently rewrites the baseline.</action>
  <verify>
    <automated>.venv/bin/python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json</automated>
  </verify>
  <acceptance_criteria>Every required budget dimension has repeated production-path distributions and complete provenance; the exact clear snapshot is measured before a threshold exists.</acceptance_criteria>
  <done>Clear-snapshot and lifecycle performance questions are resolved by checked-in evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive gates and run final Phase 3 acceptance</name>
  <reversibility rating="reversible">Measured defaults and multipliers can be recalibrated through the checked-in evidence path.</reversibility>
  <files>src/cacheness/config.py, tests/test_config_validation.py, tests/test_sqlite_lifecycle_authority.py, tests/test_blob_store_concurrency.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
    <file>src/cacheness/config.py</file>
    <file>tests/test_config_validation.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
    <file>tests/test_blob_store_concurrency.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
    <file>tools/capture_phase3_windows_qualification.py</file>
    <file>tests/test_phase3_windows_qualification_attestation.py</file>
  </read_first>
  <behavior>
    - "`LifecycleLimits.operation_page_size` equals `derived.configuration.operation_page_size`, computed from `metrics.reconciliation_page.rows.p99 * multipliers.reconciliation_rows`; `max_reconcile_actions` similarly maps actions.p99 to reconciliation_actions; and `max_operation_record_bytes` maps bytes.p99 to reconciliation_bytes, each rounded up to an integer."
    - "`LifecycleLimits.authority_busy_timeout_seconds` equals `derived.configuration.authority_busy_timeout_seconds`, the millisecond-rounded-up maximum of `metrics.busy_wait.seconds.p99 * multipliers.busy_deadline` and `metrics.reconciliation_page.seconds.p99 * multipliers.reconciliation_time`."
    - "Authority-transition p99, clear-snapshot p99 by target count, and distinct-key overlap p05 remain `derived.release_envelopes` benchmark gates only; they do not become LifecycleLimits defaults. Existing manifest-page, inventory, orphan-grace, close, and key-initialization defaults remain unchanged unless a separately named baseline derivation is added."
    - "Unit tests validate derivation/provenance and deterministic logical bounds without depending on noisy wall-clock timing."
    - "The full repository behavior suite passes; every Phase 3-owned existing Python path has no finding outside the checked-in stable-fingerprint baseline, and every Phase 3-new Python file passes direct Ruff."
    - "Final acceptance requires actual Python 3.11 and repository Python 3.13 runs, Plan 03-12's completed two-command contract, and Plan 03-11's current-host UNAVAILABLE/NOT_QUALIFIED artifact and completed structured summary; it does not require native Windows PASS in this milestone and never treats UNAVAILABLE as a support claim (D-32)."
  </behavior>
  <action>Implement only the four configuration mappings named in this task's behavior, per D-29's explicit reconciliation row/action/byte/time bounds and the agent-discretion choice for the SQLite busy limit. D-28 governs the separate transaction/clear/overlap release envelopes: preserve overlap outside authority transactions and do not convert those noisy performance measurements into runtime policy defaults. Store every formula and multiplier in the baseline, document its margin, and make `tests/test_config_validation.py` compare the `LifecycleLimits` defaults to `derived.configuration`; test the busy deadline in `tests/test_sqlite_lifecycle_authority.py`, deterministic row/action/byte/time enforcement in `tests/test_blob_store_reconciliation.py`, and the benchmark-only overlap envelope in `tests/test_blob_store_concurrency.py`. Keep benchmark regression isolated from normal unit tests. Run full behavioral regression and the benchmark verifier. Run explicit interpreter checks plus focused suites through uv-resolved Python 3.11 and the repository `.venv` Python 3.13; skipped collection or a missing interpreter does not satisfy either runtime gate. Invoke `tools/capture_phase3_windows_qualification.py verify` against the Plan 03-11 artifact, immutable completed 03-09 runner summary, Plan 03-12 additive contract artifact and completed contract summary, completed 03-11 qualification summary, and validation file so the exact fixed command executes again, actual exit 2 and parsed UNAVAILABLE are required, raw JSON bytes match, the repository-runtime and qualification-attestation roles remain distinct, every structured mirror agrees, and current native/support-qualified contradictions fail. This attested D-32 record satisfies Phase 3's current-host evidence rule without satisfying native Windows PASS. Run the Plan 01 checked-in Ruff delta verifier over its exact declared Phase 3-owned scope: a current diagnostic is accepted only when its normalized path/rule/message/source fingerprint is present in the pre-phase multiset; removed findings are allowed, while new, changed, duplicated-above-baseline, out-of-scope, or malformed results fail. Then invoke direct Ruff on all Phase 3-new Python files, including the Plan 03-11 attestation helper and test. Do not require pre-existing findings in touched existing files to disappear; repository-wide cleanup remains Phase 8.</action>
  <verify>
    <automated>uv run --python 3.13 --frozen python -c "import sys; assert sys.version_info[:2] == (3, 13)" &amp;&amp; .venv/bin/python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; .venv/bin/pytest -q tests/test_config_validation.py tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_contract.py tests/test_phase3_windows_qualification_attestation.py tests/test_phase3_ruff_delta.py -o log_cli=false &amp;&amp; uv run --isolated --python 3.11 --frozen python -c "import sys; assert sys.version_info[:2] == (3, 11)" &amp;&amp; uv run --isolated --python 3.11 --frozen pytest -q tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_qualification_attestation.py -x &amp;&amp; uv run pytest -q -o log_cli=false &amp;&amp; uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; .venv/bin/python tools/capture_phase3_windows_qualification.py verify --artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-WINDOWS-QUALIFICATION.md --runner-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md --contract-artifact .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-PLATFORM-EVIDENCE-ADDENDUM.md --contract-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-12-SUMMARY.md --qualification-summary .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-11-SUMMARY.md --validation .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run ruff check src/cacheness/storage/lifecycle_authority.py src/cacheness/storage/sqlite_lifecycle_authority.py src/cacheness/storage/memory_lifecycle_authority.py tests/_lifecycle_test_support.py tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_phase3_release_evidence.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_contract.py tests/test_phase3_windows_qualification_attestation.py tests/test_phase3_ruff_delta.py tools/capture_phase3_windows_qualification.py tools/verify_phase3_ruff_delta.py benchmarks/lifecycle_authority_benchmark.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>Every configured limit traces to measured evidence; deterministic tests and benchmark regression pass; the Ruff delta has no unmatched current fingerprint and all new files pass direct Ruff; real Python 3.11/3.13 runs pass; and Plan 03-11's artifact plus completed qualification summary record Windows as UNAVAILABLE/NOT_QUALIFIED with Phase 999.1 retaining native PASS before a Windows-qualified release. Native Windows PASS is not required for this milestone, and UNAVAILABLE is not a support claim (D-32).</acceptance_criteria>
  <done>Phase 3 has evidence-based budgets and complete lifecycle, compatibility, security, and supported-Python acceptance, with Windows qualification honestly deferred and auditable.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Atomic put/overwrite/delete/clear; deterministic reopen/reconciliation; no metadata/payload disagreement | Plans 02-10; D-32 command contract in Plan 12 and evidence boundary in Plan 11 |
| REQ | STOR-03, STOR-04, STOR-05, STOR-06, STOR-07 | Every plan frontmatter; behavior closure in Plans 04-10 |
| RESEARCH | Deep authority, lazy read-only bootstrap, SQLite DELETE/EXTRA/deadlines/identity, ABA lineage, exact clear, bounded reconciliation, JSON projection, scheduler deletion, adapter seam | Plans 01-10; Plan 12 distinguishes command roles and Plan 11 attests the platform evidence |
| CONTEXT | D-01..D-22 preserved behavior, D-23..D-31 replacement architecture, and D-32 honest Windows deferral | Plans 01-12; Plans 09, 12, 11, and 10 preserve UNAVAILABLE/NOT_QUALIFIED and Phase 999.1 without weakening future native PASS |
| EDGE | STOR-03/STOR-04/STOR-06/STOR-07 unclassified; STOR-05 idempotency/concurrency | Flagged in 03-01; exercised in Plans 02-10 |
| PROHIBITIONS | No wrapper/header; no payload deserialization or unauthenticated locator; no guessed budgets; no scheduler reachability; no false Windows qualification | Plans 02, 05, 07-12 |

Exclusions: concrete PostgreSQL and S3 authority adapters remain Phases 4/5; archived `.file-native.md` artifacts are historical evidence; no Phase SPEC exists.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| benchmark host → release gate | Measurements need explicit environment/schema provenance and controlled recalibration. |
| baseline/config → runtime limits | Only measured named fields may drive operational thresholds. |
| Plan 03-12/03-11 host evidence → release claim | The additive command contract and UNAVAILABLE artifact close the milestone evidence record but cannot authenticate native Windows support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10-01 | Repudiation | benchmark claims | medium | mitigate | Record command, environment, schema, repetitions, distributions, and explicit recalibration. |
| T-03-10-02 | Denial of Service | clear/busy/page defaults | high | mitigate | Derive bounded defaults from production-path measurements plus documented margins. |
| T-03-10-03 | Tampering | baseline rewrite | medium | mitigate | Separate record/verify modes and require explicit recalibration. |
| T-03-10-04 | Spoofing | Windows qualification status | high | mitigate | Execute the Plan 03-11 fixed-command helper and structurally compare raw bytes, artifact fields, immutable 03-09 evidence, completed 03-12/03-11 summaries, and validation disposition; permit a Windows-qualified release only after Phase 999.1 native PASS/exit 0 (D-32). |
</threat_model>

<verification>
Run explicit Python 3.11 and repository Python 3.13 checks, focused and full lifecycle suites, scheduler-negative reachability, benchmark verification, full repository pytest, the checked-in Ruff delta gate, and direct Ruff on every new Python file. Execute Plan 03-11's helper so the exact current-host command runs again and the exit code, parsed status, raw JSON bytes, artifact structure, immutable 03-09 source, completed 03-12 command-role contract, completed 03-11 qualification summary, and validation rule all attest UNAVAILABLE/NOT_QUALIFIED without a support claim; native Windows PASS remains Phase 999.1's release gate (D-32).
</verification>

<success_criteria>
- Clear, transaction, busy, paging, and overlap limits derive from reproducible measurements.
- Full lifecycle/security/concurrency tests and scheduler-negative reachability pass.
- Python 3.11 and repository Python 3.13 evidence actually executes and passes; skipped or missing interpreter evidence fails acceptance.
- Plan 03-12's completed command-role contract and Plan 03-11's Windows artifact/qualification summary are structurally consistent and honestly UNAVAILABLE/NOT_QUALIFIED; they cannot be used as a support claim, and Phase 999.1 still requires real native PASS before a Windows-qualified release (D-32).
- Ruff rejects every new finding in the exact Phase 3-owned scope and requires all Phase 3-new Python files to be clean; repository-wide cleanup remains Phase 8.
- No PostgreSQL/S3 adapter or scheduler semantic is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md` when done.
</output>
