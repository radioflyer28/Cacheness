---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "10"
type: execute
wave: 10
depends_on: ["03-09"]
files_modified:
  - benchmarks/lifecycle_authority_benchmark.py
  - benchmarks/lifecycle_authority_baseline.json
  - src/cacheness/config.py
  - tests/test_sqlite_lifecycle_authority.py
  - tests/test_blob_store_concurrency.py
  - tests/test_blob_store_reconciliation.py
autonomous: true
requirements: [STOR-03, STOR-04, STOR-05, STOR-06, STOR-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Production-schema measurements establish transaction, exact clear-snapshot, busy, reconciliation-page, and distinct-key-overlap distributions before any threshold becomes a release gate (D-16, D-25, D-26)."
    - "Checked-in baseline provenance names Python, SQLite, platform/filesystem, schema, cardinalities, repetitions, and percentile distributions; no anecdotal number controls behavior."
    - "Configured row/action/byte/time/deadline budgets derive from measured fields with documented safety multipliers and have an explicit recalibration path."
    - "Final tests, scheduler-negative reachability, real Python evidence, the checked-in Ruff baseline-delta gate, and direct Ruff for every Phase 3-new Python file pass without requiring pre-existing touched-file debt to be clean."
  artifacts:
    - path: benchmarks/lifecycle_authority_benchmark.py
      provides: "Reproducible production-schema authority, clear, busy, paging, and overlap measurement harness"
    - path: benchmarks/lifecycle_authority_baseline.json
      provides: "Measured distributions, environment provenance, and derived threshold inputs"
  key_links:
    - from: benchmarks/lifecycle_authority_baseline.json
      to: src/cacheness/config.py
      via: "Named measured fields and documented safety multipliers"
      pattern: "clear|busy|page|overlap|percentile"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No numeric timeout, clear, page, or performance threshold may become a gate before benchmark evidence is recorded."
    - "[FLAGGED/UNVERIFIED] Final Ruff acceptance must not fail on untouched repository baseline findings."
---

<objective>
Calibrate lifecycle budgets from production-schema measurements and close Phase 3 acceptance.

Purpose: Separate noisy benchmark judgment from correctness/platform proof and make every operational threshold evidence-based.
Output: Reproducible benchmark, checked-in baseline, derived configuration gates, and final source-coverage evidence.
</objective>

## Artifacts this phase produces

- Exact transactional clear `INSERT ... SELECT`, transition, busy, reconciliation, and overlap benchmark.
- Checked-in multi-run percentile baseline with host/schema provenance.
- Configuration defaults and release multipliers derived only after measurement.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-09-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure production-schema lifecycle distributions</name>
  <reversibility rating="reversible">The checked-in harness and baseline can be recalibrated without changing authority semantics.</reversibility>
  <files>benchmarks/lifecycle_authority_benchmark.py, benchmarks/lifecycle_authority_baseline.json</files>
  <read_first>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>benchmarks/README.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md</file>
  </read_first>
  <behavior>
    - "The harness invokes the production SqliteLifecycleAuthority schema and exact clear-target INSERT SELECT path at representative cardinalities."
    - "Warmups and repeated samples record p50/p95/p99 for short transitions, clear snapshot writer hold, held-writer busy expiry, bounded reconciliation pages, and synchronized distinct-key overlap."
    - "Baseline JSON records command, commit/schema identity, Python/SQLite/platform/filesystem, cardinalities, repetitions, raw summary distributions, and no payload/key secrets."
    - "Verify-baseline and recalibrate modes are explicit; the first baseline is measurement output rather than a hand-authored threshold."
  </behavior>
  <action>Build the reproducible benchmark directly against `SqliteLifecycleAuthority` and the production schema. Measure warmups plus repeated percentile distributions for ordinary complete transitions, exact one-transaction clear target insertion at representative local-store sizes, held-writer busy expiration, bounded reconciliation pages, and gated distinct-key payload overlap. Persist provenance and distributions in JSON. Provide separate `--record-baseline` and `--verify-baseline` modes; verification checks schema/field/provenance consistency and measured regression envelopes, while recalibration requires an explicit command and never silently rewrites the baseline.</action>
  <verify>
    <automated>.venv/bin/python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json</automated>
  </verify>
  <acceptance_criteria>Every required budget dimension has repeated production-path distributions and complete provenance; the exact clear snapshot is measured before a threshold exists.</acceptance_criteria>
  <done>Clear-snapshot and lifecycle performance questions are resolved by checked-in evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive gates and run final Phase 3 acceptance</name>
  <reversibility rating="reversible">Measured defaults and multipliers can be recalibrated through the checked-in evidence path.</reversibility>
  <files>src/cacheness/config.py, tests/test_sqlite_lifecycle_authority.py, tests/test_blob_store_concurrency.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>benchmarks/lifecycle_authority_baseline.json</file>
    <file>src/cacheness/config.py</file>
    <file>tests/test_sqlite_lifecycle_authority.py</file>
    <file>tests/test_blob_store_concurrency.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
  </read_first>
  <behavior>
    - "Busy/page row/action/byte/time, transaction, clear, and overlap gates cite named baseline fields and documented safety multipliers."
    - "Unit tests validate derivation/provenance and deterministic logical bounds without depending on noisy wall-clock timing."
    - "The full repository behavior suite passes; every Phase 3-owned existing Python path has no finding outside the checked-in stable-fingerprint baseline, and every Phase 3-new Python file passes direct Ruff."
    - "Final acceptance retains the Plan 09 real Python 3.11/3.13 and native Windows evidence preconditions."
  </behavior>
  <action>After Task 1 records measurements, derive D-16/D-25/D-26 configuration defaults and release multipliers from named baseline fields; document why each margin is sufficient. Keep deterministic row/action/byte and absolute-deadline semantics as primary correctness checks, with benchmark regression isolated from normal unit tests. Run full behavioral regression and the benchmark verifier. Run the Plan 01 checked-in Ruff delta verifier over its exact declared Phase 3-owned scope: a current diagnostic is accepted only when its normalized path/rule/message/source fingerprint is present in the pre-phase multiset; removed findings are allowed, while new, changed, duplicated-above-baseline, out-of-scope, or malformed results fail. Then invoke direct Ruff on all Phase 3-new Python files: `lifecycle_authority.py`, `sqlite_lifecycle_authority.py`, `memory_lifecycle_authority.py`, `_lifecycle_test_support.py`, `test_lifecycle_authority_contract.py`, `test_sqlite_lifecycle_authority.py`, `test_phase3_release_evidence.py`, `test_phase3_scheduler_retirement.py`, `test_phase3_windows_contract.py`, `test_phase3_ruff_delta.py`, `verify_phase3_ruff_delta.py`, and `lifecycle_authority_benchmark.py`. Do not require pre-existing findings in touched existing files to disappear; repository-wide cleanup remains Phase 8.</action>
  <verify>
    <automated>.venv/bin/python benchmarks/lifecycle_authority_benchmark.py --verify-baseline benchmarks/lifecycle_authority_baseline.json &amp;&amp; .venv/bin/pytest -q tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py tests/test_blob_store_concurrency.py tests/test_blob_store_close_contract.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_contract.py tests/test_phase3_ruff_delta.py -o log_cli=false &amp;&amp; uv run pytest -q -o log_cli=false &amp;&amp; uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; .venv/bin/python tools/verify_phase3_ruff_delta.py &amp;&amp; uv run ruff check src/cacheness/storage/lifecycle_authority.py src/cacheness/storage/sqlite_lifecycle_authority.py src/cacheness/storage/memory_lifecycle_authority.py tests/_lifecycle_test_support.py tests/test_lifecycle_authority_contract.py tests/test_sqlite_lifecycle_authority.py tests/test_phase3_release_evidence.py tests/test_phase3_scheduler_retirement.py tests/test_phase3_windows_contract.py tests/test_phase3_ruff_delta.py tools/verify_phase3_ruff_delta.py benchmarks/lifecycle_authority_benchmark.py --output-format concise</automated>
  </verify>
  <acceptance_criteria>Every configured limit traces to measured evidence; deterministic tests and benchmark regression pass; the Ruff delta has no unmatched current fingerprint and all new files pass direct Ruff; Plan 09 runtime/Windows evidence is present.</acceptance_criteria>
  <done>Phase 3 has evidence-based budgets and complete correctness, compatibility, security, runtime, and platform acceptance.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Atomic put/overwrite/delete/clear; deterministic reopen/reconciliation; no metadata/payload disagreement | Plans 02-10 |
| REQ | STOR-03, STOR-04, STOR-05, STOR-06, STOR-07 | Every plan frontmatter; behavior closure in Plans 04-10 |
| RESEARCH | Deep authority, lazy read-only bootstrap, SQLite DELETE/EXTRA/deadlines/identity, ABA lineage, exact clear, bounded reconciliation, JSON projection, scheduler deletion, adapter seam | Plans 01-10 |
| CONTEXT | D-01..D-22 preserved behavior and D-23..D-31 replacement architecture | Plans 01-10 with decision citations |
| EDGE | STOR-03/STOR-04/STOR-06/STOR-07 unclassified; STOR-05 idempotency/concurrency | Flagged in 03-01; exercised in Plans 02-10 |
| PROHIBITIONS | No wrapper/header; no payload deserialization or unauthenticated locator; no guessed budgets; no scheduler reachability | Plans 02, 05, 07-10 |

Exclusions: concrete PostgreSQL and S3 authority adapters remain Phases 4/5; archived `.file-native.md` artifacts are historical evidence; no Phase SPEC exists.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| benchmark host → release gate | Measurements need explicit environment/schema provenance and controlled recalibration. |
| baseline/config → runtime limits | Only measured named fields may drive operational thresholds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10-01 | Repudiation | benchmark claims | medium | mitigate | Record command, environment, schema, repetitions, distributions, and explicit recalibration. |
| T-03-10-02 | Denial of Service | clear/busy/page defaults | high | mitigate | Derive bounded defaults from production-path measurements plus documented margins. |
| T-03-10-03 | Tampering | baseline rewrite | medium | mitigate | Separate record/verify modes and require explicit recalibration. |
</threat_model>

<verification>
Run benchmark verification, the complete Phase 3 suite, full repository pytest, the checked-in Ruff delta gate plus direct Ruff on every new Python file, and confirm Plan 09 real-runtime/native-Windows evidence remains attached.
</verification>

<success_criteria>
- Clear, transaction, busy, paging, and overlap limits derive from reproducible measurements.
- Full lifecycle/security/concurrency tests and scheduler-negative reachability pass.
- Python 3.11/3.13 and native Windows evidence is real, not skipped.
- Ruff rejects every new finding in the exact Phase 3-owned scope and requires all Phase 3-new Python files to be clean; repository-wide cleanup remains Phase 8.
- No PostgreSQL/S3 adapter or scheduler semantic is introduced.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-10-SUMMARY.md` when done.
</output>
