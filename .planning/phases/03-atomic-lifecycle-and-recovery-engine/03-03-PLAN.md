---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "03"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/cacheness/storage/operation_record.py
  - src/cacheness/storage/operation_repository.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/__init__.py
  - tests/test_blob_store_atomic_lifecycle.py
  - tests/test_blob_store_reconciliation.py
autonomous: true
requirements: [STOR-04, STOR-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Operation evidence is bounded, versioned, authenticated, topology-bound, and provenance-complete before it can authorize recovery (D-05, D-06, D-15)."
    - "Malformed, oversized, duplicate, future-version, wrong-store, wrong-key, escaping-locator, or provenance-free evidence is reported untouched before any destructive callback (D-15, D-16)."
    - "Retention, grace, paging, total-action, and close-wait policy is explicit and injectable; terminal evidence is retained until proven cleanup, never removed by a hidden time ceiling."
  artifacts:
    - path: src/cacheness/storage/operation_record.py
      provides: "Complete bounded authenticated evidence codec and transition validation"
    - path: src/cacheness/storage/operation_repository.py
      provides: "Stable bounded page/cursor and exact checkpoint repository contract"
    - path: src/cacheness/storage/lifecycle.py
      provides: "LifecycleLimits explicit operational policy"
    - path: tests/test_blob_store_reconciliation.py
      provides: "Adversarial evidence and bounded inventory Wave 0 contract"
  key_links:
    - from: src/cacheness/storage/operation_repository.py
      to: src/cacheness/storage/operation_record.py
      via: "repository transports exact bytes; lifecycle authenticates and interprets"
      pattern: "LifecycleOperationRecord"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/operation_repository.py
      via: "all checkpoints use explicit bounded policy and stable cursors"
      pattern: "LifecycleLimits"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Harden operation evidence and operational limits before generalized destructive recovery consumes them.

Purpose: Make recovery decisions safe against hostile control metadata and bounded under large stores.
Output: Exact authenticated evidence codec, stable paged repository, explicit LifecycleLimits, and adversarial Wave 0 tests.
</objective>

## Artifacts this phase produces

- Completed internal `LifecycleOperationRecord` codec, transition validator, and domain-separated authentication projection.
- New internal `OperationPage`, `OperationCursor`, and repository methods `list_page`, `create_exclusive`, `checkpoint_if_exact`, and `retire_if_exact`.
- New public configuration value `LifecycleLimits` exported from `cacheness.storage`, with explicit page/action/record/grace/close-wait fields and validation.
- Updated `src/cacheness/storage/__init__.py` exports only the stable `LifecycleLimits` public policy value.
- New test file `tests/test_blob_store_reconciliation.py`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-PATTERNS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-01-SUMMARY.md
</context>

<interfaces>
Phase 2 `manifest.py` is the bounded canonical-codec analog. Phase 1 `ClearRecoveryCoordinator` bounds journal bytes, entries, and fields before parsing. The operation repository must remain opaque and expose stable bounded pages; lifecycle code alone authenticates and interprets records.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the authenticated bounded operation-record contract</name>
  <depends_on>Plan 03-01 supplies the tracer record and checkpoint skeleton.</depends_on>
  <files>src/cacheness/storage/operation_record.py, src/cacheness/storage/lifecycle.py, tests/test_blob_store_atomic_lifecycle.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest.py</file>
    <file>src/cacheness/storage/clear_recovery.py</file>
    <file>tests/test_blob_manifest.py</file>
    <file>tests/test_clear_recovery.py</file>
  </read_first>
  <behavior>
    - "The exact record fields bind version, owner/store topology, operation ID/kind, logical key, expected exact record, candidate generation, relevant locators, transition, checkpoint, and timestamps without payload bytes."
    - "Raw-byte/field/depth/node/item bounds are enforced before expensive parsing and exact field sets reject duplicates or unknowns."
    - "HMAC domain separation makes a manifest signature invalid as operation evidence and vice versa."
    - "Checkpoint transitions are monotonic and invalid or future transitions fail typed without cleanup."
  </behavior>
  <action>Use the Phase 2 restricted canonical codec pattern to complete `LifecycleOperationRecord`: frozen validated values, exact schema dispatch, duplicate/unknown rejection, UTF-8 and numeric bounds, and a domain-separated HMAC-SHA256 projection using the existing strict store key provider. Include only provenance/control fields required by D-05/D-06; never payload contents. Define an explicit monotonic transition table covering prepared, candidate-published, authority-published, reclaiming, and terminal checkpoints. Add adversarial cases for oversized pre-parse rejection, duplicate fields, wrong owner/store/key/operation, unknown version/state, locator traversal/symlink, bad signature, and cross-domain signature. All failures are typed findings and occur before any mutation spy fires.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py -k "record or bound or provenance or transition or signature" -x</automated>
  </verify>
  <acceptance_criteria>Every record field and boundary has accepted-edge/rejected-next coverage; malicious evidence reaches zero destructive callbacks and retains exact bytes/mtime.</acceptance_criteria>
  <done>Only exact authenticated current operation evidence can authorize a lifecycle recovery decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make operation retention, paging, grace, and wait policy explicit</name>
  <depends_on>Task 1 freezes the record and transition model consumed by repository paging.</depends_on>
  <files>src/cacheness/storage/operation_repository.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/__init__.py, tests/test_blob_store_atomic_lifecycle.py, tests/test_blob_store_reconciliation.py</files>
  <read_first>
    <file>src/cacheness/storage/operation_repository.py</file>
    <file>src/cacheness/storage/path_security.py</file>
    <file>.planning/STATE.md</file>
    <file>.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "Operation listing is stable and cursor-based with an explicit positive page size and total-action ceiling; tests assert bounded calls and memory proxies."
    - "Records/tombstones remain until their exact terminal cleanup succeeds; no background age ceiling deletes recovery evidence."
    - "Authenticated pre-authority candidates become automatically eligible only after the configurable orphan grace and revalidation; provenance-free evidence never becomes eligible by age alone."
    - "LifecycleLimits rejects booleans, non-finite values, negative/zero invalid bounds, and unbounded values; injected edge values drive deterministic tests."
  </behavior>
  <action>Add a frozen public `LifecycleLimits` accepted by `BlobStore`/`LifecycleEngine`, carrying maximum record bytes/field bytes/page size/actions per run, orphan grace seconds, tombstone/operation retention policy, and close wait seconds. Choose documented finite defaults by reusing established manifest/clear bounds where equivalent and by measuring deterministic page/call-count/frozen-clock tests; record the rationale beside each default. Define retention semantically: a tombstone or operation record is retained until exact cleanup/conditional retirement, not removed merely by elapsed time. Make grace/config policy visible through report fields and injectable clocks so tests prove exact boundary behavior without sleep. Add stable `OperationPage`/cursor repository primitives, exact conditional checkpoint/retire, and counters proving at most one bounded page plus a documented constant number of calls per item. Do not materialize all records or silently clamp caller values.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "page or cursor or bound or grace or retention or call_count" -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_blob_store_atomic_lifecycle.py -k "checkpoint or retire" -x &amp;&amp; uv run ruff check src/cacheness/storage/operation_record.py src/cacheness/storage/operation_repository.py src/cacheness/storage/lifecycle.py src/cacheness/storage/__init__.py tests/test_blob_store_atomic_lifecycle.py tests/test_blob_store_reconciliation.py</automated>
  </verify>
  <acceptance_criteria>Every operational limit is named, finite, validated, measurable in tests, and configurable where callers need policy control; retention never sacrifices the only recovery evidence; page/call counters remain bounded.</acceptance_criteria>
  <done>Recovery evidence and aggregate scans have explicit test-justified operational policy rather than hidden ceilings.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V1/V2 input validation | Exact fields, versions, state transitions, signatures, and bounds before use. | Adversarial evidence matrix. |
| V15 secure architecture | Bounded pages, retention, and explicit policy avoid unbounded resource consumption. | Cursor/call-count tests. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| raw operation bytes -> codec | Untrusted persisted control input may be malformed, oversized, forged, or future-version. |
| paged inventory -> recovery decision | Scale and cursor manipulation must not bypass bounds or repeat destructive work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08 | Spoofing / Tampering | operation record | high | mitigate | Domain-separated HMAC, exact owner/topology/key/generation fields, strict transition validation. |
| T-03-09 | Denial of Service | evidence decoder/inventory | high | mitigate | Pre-parse byte bounds, bounded field/node counts, stable finite pages/actions, and call-count tests. |
| T-03-10 | Tampering | age/grace cleanup | high | mitigate | Age alone never proves ownership; exact authenticated provenance and current authority are revalidated. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-08 | `uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "provenance or signature or wrong_store or transition" -x` |
| T-03-09 | `uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "oversize or page or call_count" -x` |
| T-03-10 | `uv run pytest -q -o log_cli=false tests/test_blob_store_reconciliation.py -k "grace or provenance_free" -x` |
</threat_model>

<verification>
Run evidence/adversarial tests, operation checkpoint regressions, and targeted Ruff. Negative cases must assert zero mutation and exact evidence preservation.
</verification>

<success_criteria>
- Only authenticated provenance-complete evidence reaches recovery logic.
- Malformed/future/ambiguous evidence is preserved and reported.
- Pages, actions, grace, retention, and waits are explicit and test bounded.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-03-SUMMARY.md` when done.
</output>
