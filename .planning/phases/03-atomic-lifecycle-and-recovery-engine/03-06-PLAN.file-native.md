---
phase: 03-atomic-lifecycle-and-recovery-engine
plan: "06"
status: superseded
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/clear_recovery.py
  - tests/test_blob_store_atomic_lifecycle.py
  - tests/test_clear_recovery.py
autonomous: true
requirements: [STOR-04, STOR-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "New clear operations create only Phase 3 authenticated operation evidence and run through LifecycleEngine; no parallel journal coordinates new work (D-05, D-06)."
    - "Exact authenticated/topology-bound prepared and committed Phase 1 clear journals reopen through a compatibility-only adapter and converge through current lifecycle primitives (D-11)."
    - "Malformed, future-version, topology-mismatched, ambiguous, or provenance-free legacy evidence is preserved byte-for-byte and causes zero mutation (D-15, D-16)."
    - "Ordinary operations use the Plan 03-05 admission protocol and do not enter the predecessor global mutation path (D-10, D-20)."
  artifacts:
    - path: src/cacheness/storage/clear_recovery.py
      provides: "Compatibility-only LegacyClearEvidenceAdapter for exact predecessor journals"
    - path: src/cacheness/storage/blob_store.py
      provides: "Single new-clear authority with a narrow reopen-only legacy adapter seam"
    - path: tests/test_clear_recovery.py
      provides: "Exact predecessor fixture, malformed evidence, and zero-mutation compatibility matrix"
  key_links:
    - from: src/cacheness/storage/clear_recovery.py
      to: src/cacheness/storage/lifecycle.py
      via: "validated predecessor terminal state is translated into current idempotent lifecycle recovery calls"
      pattern: "LegacyClearEvidenceAdapter"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle.py
      via: "all newly requested clear operations call LifecycleEngine.clear directly"
      pattern: "LifecycleEngine"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/clear_recovery.py
      via: "legacy adapter is reachable only during reopen/recovery of exact predecessor evidence"
      pattern: "LegacyClearEvidenceAdapter"
  prohibitions:
    - "No Cacheness payload wrapper or header may be added around native NPZ, Parquet, pickle/dill, or Blosc2 bytes."
    - "Normal reads must not mutate evidence or guess authority from operation records, filename grammar, or handler payloads."
    - "Ordinary operations must not be serialized by one global lock."
    - "Recovery must not delete provenance-free evidence or the only valid generation."
    - "The plan must not claim PostgreSQL, S3, UnifiedCache, or format-migration support deferred to later phases."
---

<objective>
Preserve exact predecessor clear evidence while retiring its authority over all newly created lifecycle work.

Purpose: Keep compatible on-disk recovery without maintaining two active lifecycle coordinators.
Output: A narrow LegacyClearEvidenceAdapter, one authoritative new-clear path, and fail-closed compatibility tests.
</objective>

## Artifacts this phase produces

- New internal `LegacyClearEvidenceAdapter` in `src/cacheness/storage/clear_recovery.py`; it validates exact Phase 1 evidence and has no new-operation journal API.
- Updated `BlobStore` reopen/recovery wiring that delegates new clears only to `LifecycleEngine.clear` and invokes the adapter only when exact predecessor evidence already exists.
- Expanded `tests/test_clear_recovery.py` and `tests/test_blob_store_atomic_lifecycle.py`; no new public symbol or test file.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-CONTEXT.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-RESEARCH.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-15-SUMMARY.md
@.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-05-SUMMARY.md
</context>

<interfaces>
Plan 03-05 provides `LifecycleEngine.clear`, persisted exact clear-target pages/checkpoints, and `StoreAdmissionBarrier`. Phase 1 `ClearRecoveryCoordinator` defines the only accepted predecessor journal schema. This plan may interpret exact predecessor evidence but must not create another current journal or bypass lifecycle CAS/cleanup authority.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt exact predecessor clear evidence without acquiring new authority</name>
  <depends_on>Plan 03-05 supplies the current clear operation and resumable per-target lifecycle primitives.</depends_on>
  <files>src/cacheness/storage/clear_recovery.py, tests/test_clear_recovery.py</files>
  <read_first>
    <file>src/cacheness/storage/clear_recovery.py</file>
    <file>tests/test_clear_recovery.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>.planning/phases/01-compatibility-and-security-baseline/01-15-SUMMARY.md</file>
  </read_first>
  <behavior>
    - "Exact authenticated prepared/committed Phase 1 fixtures validate owner/store topology, schema version, target records, and terminal state before adaptation."
    - "The adapter exposes reopen/recover behavior only and cannot create a predecessor journal for a new clear request."
    - "Accepted evidence delegates idempotent completion to Phase 3 lifecycle primitives and converges across repeated reopen."
    - "Malformed, oversized, future, topology-mismatched, ambiguous, or provenance-free bytes produce typed fail-closed findings with exact bytes/mtime and mutation spies unchanged."
  </behavior>
  <action>Extract predecessor parsing into internal `LegacyClearEvidenceAdapter` per D-06/D-11. Accept only the exact authenticated, size-bounded, topology-bound Phase 1 prepared/committed schema already frozen by Phase 1 tests; translate its validated terminal state and exact target expectations into current idempotent lifecycle recovery calls. Give the adapter no API for starting a clear or writing a new legacy journal. Reject unknown fields/versions, invalid transitions, owner/topology mismatch, unsafe locators, ambiguous records, and missing provenance before lifecycle mutation, preserving evidence for diagnosis. Extend exact fixture/reopen and adversarial zero-mutation tests without inferring authority from filenames or payload bytes.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_clear_recovery.py -k "legacy or prepared or committed or malformed or topology or provenance" -x</automated>
  </verify>
  <acceptance_criteria>`LegacyClearEvidenceAdapter` has reopen/recover but no new-journal entry point; exact prepared/committed fixtures converge; every invalid fixture preserves exact evidence and invokes zero destructive callbacks.</acceptance_criteria>
  <done>Validated predecessor evidence remains recoverable through a narrow compatibility seam with no independent current authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route new clears exclusively through the Phase 3 engine</name>
  <depends_on>Task 1 provides the only permitted predecessor-evidence entry point.</depends_on>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/storage/clear_recovery.py, tests/test_blob_store_atomic_lifecycle.py, tests/test_clear_recovery.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/coordination.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
  </read_first>
  <behavior>
    - "BlobStore.clear invokes LifecycleEngine.clear and creates one Phase 3 operation record without creating or nesting a predecessor journal."
    - "Reopen calls the legacy adapter only when exact predecessor evidence is present; ordinary reads and new mutations never consult it."
    - "Ordinary distinct-key operations use StoreAdmissionBarrier ordinary admission and do not enter Phase 1 global mutation wrappers."
    - "Repeated new clear, legacy reopen, crash/reopen, and mixed-evidence cases converge to one authoritative state without duplicate destructive calls."
  </behavior>
  <action>Remove new-operation ownership from `ClearRecoveryCoordinator` and the `_clear_coordinated`/`_clear_read_coordinated` wrappers. Wire `BlobStore.clear` only to Plan 03-05 `LifecycleEngine.clear`; keep the Task 1 adapter reachable solely from explicit reopen/recovery discovery of exact predecessor evidence. Use Plan 03-05 `StoreAdmissionBarrier` for bounded aggregate snapshot establishment and ordinary admission, never the predecessor global lock. Add instrumentation covering new clear, valid old reopen, mixed old/current evidence, every crash boundary, and repeated retry; assert one Phase 3 record protocol, monotonic checkpoints, no repeated successful destructive call, and later-generation preservation per D-10/D-20.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_clear_recovery.py tests/test_blob_store_atomic_lifecycle.py -k "clear and (legacy or journal or authority or admission or reopen)" -x &amp;&amp; uv run ruff check src/cacheness/storage/blob_store.py src/cacheness/storage/clear_recovery.py tests/test_blob_store_atomic_lifecycle.py tests/test_clear_recovery.py</automated>
  </verify>
  <acceptance_criteria>Instrumentation observes exactly one new-operation lifecycle record path, exact old journals remain recoverable only on reopen, ordinary methods never enter predecessor global wrappers, and retries preserve later/only-valid generations.</acceptance_criteria>
  <done>One LifecycleEngine owns every new clear while exact predecessor evidence retains compatible recovery.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS category | Phase application | Automated evidence |
|---|---|---|
| V1/V2 input and business validation | Exact predecessor schema, topology, transition, provenance, and byte bounds precede adaptation. | Legacy adversarial zero-mutation matrix. |
| V15 secure architecture | One current operation authority prevents split-brain lifecycle journals and broad lock reuse. | One-journal and admission instrumentation. |

## Trust Boundaries

| Boundary | Description |
|---|---|
| predecessor journal bytes -> adapter | Persisted legacy control input may be forged, malformed, oversized, ambiguous, or from another topology. |
| compatibility adapter -> current lifecycle | Accepted old state must not bypass exact CAS, provenance, or only-valid-generation protections. |
| public clear -> operation authority | A new request must create exactly one current protocol and never revive the predecessor coordinator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-15 | Spoofing / Tampering | legacy clear evidence | high | mitigate | Exact old schema/signature/topology/provenance validation and zero-mutation malformed tests. |
| T-03-30 | Tampering / Repudiation | dual clear coordinators | high | mitigate | Remove predecessor new-operation entry points and instrument one current operation record path. |
| T-03-31 | Denial of Service | predecessor global admission | medium | mitigate | Legacy adapter is reopen-only; ordinary operations use the bounded Plan 03-05 barrier protocol. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-03-15 | `uv run pytest -q -o log_cli=false tests/test_clear_recovery.py -k "legacy or malformed or topology or provenance" -x` |
| T-03-30 | `uv run pytest -q -o log_cli=false tests/test_clear_recovery.py tests/test_blob_store_atomic_lifecycle.py -k "clear and (journal or authority or legacy)" -x` |
</threat_model>

<verification>
Run exact predecessor fixtures, adversarial legacy evidence, one-authority instrumentation, crash/reopen, and targeted Ruff. Invalid evidence must assert exact preservation and zero mutation; accepted evidence must assert exact CAS-based convergence.
</verification>

<success_criteria>
- New clear requests create only Phase 3 operation evidence.
- Exact predecessor prepared/committed evidence remains recoverable.
- Invalid predecessor evidence is preserved and cannot authorize mutation.
- Ordinary operations never use the predecessor global admission wrappers.
</success_criteria>

<output>
Create `.planning/phases/03-atomic-lifecycle-and-recovery-engine/03-06-SUMMARY.md` when done.
</output>
