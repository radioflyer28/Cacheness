---
phase: 07.1-obstore-payload-participant-unification
plan: 08
type: execute
wave: 7
depends_on:
  - 07.1-06
files_modified:
  - src/cacheness/storage/obstore_generation_io.py
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/reconciliation.py
  - tests/test_migration_cutover.py
  - tests/test_migration_remote_contract.py
  - tests/test_migration_run_evidence.py
  - tests/test_rebuild_workflow.py
  - tests/test_payload_reconciliation.py
autonomous: true
requirements:
  - STOR-04
  - STOR-05
  - STOR-06
  - BACK-01
  - MIGR-03
  - MIGR-04
  - MIGR-05
  - MIGR-06
  - QUAL-04
  - QUAL-07
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Offline migration and rebuild publish destination payloads through the same backend-neutral participant as ordinary BlobStore writes (D-08, D-10)."
    - "Unchanged-payload migration/replay preserves authenticated evidence; any republished/transformed/new generation receives fresh evidence in its one verification transition, and tombstones carry none (D-11)."
    - "Resume, abort, reconciliation, and cleanup use authority-attributed exact locators and never listings or backend class/scheme introspection as lifecycle state."
    - "Multipart-upload residue categories disappear without creating a replacement journal or cleanup authority (D-02, D-14)."
  artifacts:
    - path: "src/cacheness/storage/migration.py"
      provides: "Backend-neutral candidate publication and destination evidence regeneration"
    - path: "src/cacheness/storage/reconciliation.py"
      provides: "Authority-owned exact cleanup with bounded report-only obstore inventory"
  key_links:
    - from: "src/cacheness/storage/migration.py"
      to: "src/cacheness/storage/obstore_generation_io.py"
      via: "verified source snapshot to exact conditional candidate publication"
      pattern: "publish_candidate|publish_generation"
    - from: "src/cacheness/storage/reconciliation.py"
      to: "src/cacheness/storage/lifecycle_authority.py"
      via: "only authority mutation/debt pages authorize exact cleanup"
      pattern: "pending|cleanup|reconciliation"
---

<objective>
Route explicit offline maintenance and reconciliation through the unified payload participant.

Purpose: Remove the largest hidden consumer of legacy backend mechanics without altering Phase 7's authority-owned maintenance model.
Output: Shared candidate publication, regenerated destination evidence, exact recovery/cleanup, and migration/reconciliation contracts.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-06-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-24-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@src/cacheness/storage/migration.py
@src/cacheness/storage/reconciliation.py
</context>

<interfaces>
Phase 7 maintenance uses authenticated run evidence plus lifecycle-authority candidate records. Same-format copies originate from a verified private source snapshot; transformations remain store-local path-handler operations. The obstore adapter supplies exact conditional candidate publication and exact delete/snapshot primitives only.
</interfaces>

<artifacts_this_phase>
- One backend-neutral maintenance publication method reuses the participant's conditional create and verification mechanics.
- Reconciliation retains authority-owned state/debt and only consumes bounded listing as non-authoritative diagnostics.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resume one interrupted offline candidate through the unified participant</name>
  <files>src/cacheness/storage/obstore_generation_io.py, src/cacheness/storage/migration.py, tests/test_migration_cutover.py, tests/test_migration_remote_contract.py, tests/test_migration_run_evidence.py</files>
  <read_first>
    - src/cacheness/storage/migration.py: candidate write branches, source snapshots, transform path, response-loss replay
    - tests/test_migration_remote_contract.py: exact locator and non-listing assertions
    - tests/test_migration_run_evidence.py: authenticated resume/source-drift contracts
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: stopped-worker authority decisions
  </read_first>
  <behavior>
    - "A same-format local/memory/S3 candidate streams from the verified source snapshot through one exact participant method without byte-buffer or scheme branching."
    - "A transformed candidate still invokes exactly one registered path handler and publishes its guarded artifact through that method."
    - "Accepted-then-raised publication resumes only from the existing authority operation identity and exact destination locator/digest/size."
    - "Republished destination evidence is newly observed/signed; source evidence is not transplanted."
    - "An unchanged-payload migration/replay preserves byte-identical evidence, while transformed/new-generation publication replaces it and tombstone migration carries none."
  </behavior>
  <action>Replace `write_blob`, S3-only candidate methods, backend-class checks, and `memory://` branches with one internal candidate-publication operation on the participant per D-08/D-10. For exact-format copy, stream the already verified source snapshot via its open descriptor. For transforms, retain the existing store-local handler and guarded path flow. Reuse conditional create and exact canonical verification; let the existing lifecycle-authority operation identity govern replay. Preserve authenticated evidence only when migration/replay retains the exact immutable payload identity. Generate fresh signed evidence in the one existing verification transition for every republished, transformed, or new generation; tombstones carry none. Add explicit cases for unchanged-payload replay, transformed publication, new-generation publication, and tombstone migration. Keep unexplained pre-checkpoint objects invisible and unadopted.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_migration_cutover.py tests/test_migration_remote_contract.py tests/test_migration_run_evidence.py -k 'candidate or response_loss or resume or transform or transport' -x</automated>
  </verify>
  <done>All migration/rebuild candidate publication uses one participant and preserves Phase 7's exact authority-owned recovery contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settle exact cleanup and bounded reconciliation without multipart residue</name>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/reconciliation.py, tests/test_rebuild_workflow.py, tests/test_payload_reconciliation.py, tests/test_migration_remote_contract.py</files>
  <read_first>
    - src/cacheness/storage/reconciliation.py: bounded classification/apply and payload inventory evidence
    - tests/test_rebuild_workflow.py: exact receipt-bound cleanup debt and forward fences
    - .codex/skills/spike-findings-cacheness/references/backend-mechanics.md: exact deletion and bounded listing semantics
  </read_first>
  <behavior>
    - "Abort/rebuild cleanup deletes or proves absence only for authority-attributed immutable locators and preserves changed current ownership."
    - "Bounded payload inventory remains report-only and cannot adopt, promote, or authorize deletion."
    - "Multipart upload/session/residue findings and cleanup paths are absent after D-02 direct-create adoption."
    - "Operational cleanup failure remains exact durable debt; integrity/ownership/evidence disagreement remains fail closed."
  </behavior>
  <action>Route migration abort, rebuild cleanup, and reconciler payload effects through the participant's exact snapshot/delete/inventory primitives. Retain Phase 7's authenticated receipt/debt fences and changed-owner protections. Remove multipart-specific inventory/residue classifications and continuations because D-02 eliminates those effects; do not replace them with a temp-object journal, lease, lock, or new state. Keep bounded list pages diagnostic only and narrow exception handling so arbitrary failures never become absence.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_rebuild_workflow.py tests/test_payload_reconciliation.py tests/test_migration_remote_contract.py -k 'cleanup or reconciliation or inventory or multipart or debt' -x</automated>
  </verify>
  <done>Maintenance and reconciliation settle only exact authority-owned effects and carry no legacy multipart lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated maintenance evidence -> participant effect | Only exact authority-attributed candidates/debt may be observed or mutated. |
| source snapshot -> destination object | Source validity does not authorize destination identity or evidence reuse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-08-01 | Spoofing | migration candidate ownership | high | mitigate | Use exact authority operation/receipt identities and reject unexplained objects. |
| T-07.1-08-02 | Tampering | source evidence transplant | high | mitigate | Observe and sign new destination transport evidence after exact canonical verification. |
| T-07.1-08-03 | Elevation | listing as lifecycle authority | high | mitigate | Keep bounded inventory report-only; authority rows/debt alone authorize effects. |
| T-07.1-08-04 | Repudiation | cleanup failure as absence | high | mitigate | Normalize only exact typed absence and retain operational failures as durable debt. |
| T-07.1-08-05 | Denial of Service | migration/inventory aggregation | medium | mitigate | Preserve existing entry/byte/evidence budgets and bounded list pages. |
</threat_model>

<verification>
- Focused maintenance/reconciliation suites pass and contain no timing-based race expectations.
- No migration path imports a concrete filesystem, memory, S3 backend or branches on locator scheme.
- No multipart residue category or new cleanup authority remains.
</verification>

<success_criteria>
- Ordinary writes and offline maintenance use the same payload mechanics.
- Destination evidence is exact, authenticated, and never copied across republished objects.
- Existing resumable migration/rebuild and reconciliation semantics remain authority-owned.
</success_criteria>

<source_coverage>
- GOAL: Extend the unified participant across ordinary and maintenance payload effects.
- REQ: STOR-04/05/06, BACK-01, MIGR-03/04/05/06, QUAL-04/07.
- RESEARCH: migration/rebuild coupling, exact deletion, bounded listing, response-loss recovery.
- CONTEXT: D-01, D-02, D-08, D-09, D-10, D-11, D-14.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-08-SUMMARY.md` when done.
</output>
