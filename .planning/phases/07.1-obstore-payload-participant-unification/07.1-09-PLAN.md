---
phase: 07.1-obstore-payload-participant-unification
plan: 09
type: execute
wave: 8
depends_on:
  - 07.1-07
  - 07.1-08
files_modified:
  - src/cacheness/storage/composition.py
  - src/cacheness/storage/obstore_generation_io.py
  - src/cacheness/storage/blob_store.py
  - tests/test_blob_store_composition.py
  - tests/test_supported_topologies.py
  - tests/contracts/test_topology_lifecycle.py
  - tests/test_phase6_policy_contract.py
  - tests/test_unified_cache_lifecycle_authority.py
  - tests/test_unified_cache_adversarial_lifecycle.py
  - tests/test_phase6_removal_contract.py
  - tests/test_phase6_decorator_contract.py
  - tests/test_core.py
autonomous: true
requirements:
  - STOR-03
  - STOR-04
  - STOR-05
  - STOR-07
  - BACK-01
  - BACK-03
  - BACK-04
  - BACK-06
  - CACH-01
  - CACH-02
  - CACH-03
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One atomic composition cutover makes obstore the only built-in payload mechanics for filesystem, memory, and S3 (D-04, D-07)."
    - "No runtime selector, dual read/write, boto3 escape hatch, or compatibility read chooses legacy payload mechanics (D-06, D-07, D-08)."
    - "Caller-injected payload providers remain selected and ownership/close behavior remains unchanged (BACK-03)."
    - "Every built-in profile still materializes one AuthorityLifecycleEngine; UnifiedCache continues to use BlobStore as its engine (D-10)."
    - "TTL, eviction, predicate, decorator, single-key, and global-clear invalidation all remove through BlobStore after cutover (CACH-03)."
  artifacts:
    - path: "src/cacheness/storage/composition.py"
      provides: "All built-in payload registrations construct the shared obstore participant"
    - path: "tests/contracts/test_topology_lifecycle.py"
      provides: "End-to-end memory, SQLite/local, and deterministic PostgreSQL/mocked-S3 topology contracts"
  key_links:
    - from: "src/cacheness/storage/composition.py"
      to: "src/cacheness/storage/obstore_generation_io.py"
      via: "all built-in payload factories"
      pattern: "_construct_.*payload|ObstoreGenerationIO"
    - from: "src/cacheness/storage/blob_store.py"
      to: "src/cacheness/storage/lifecycle.py"
      via: "one materialized participant and one lifecycle engine"
      pattern: "AuthorityLifecycleEngine"
---

<objective>
Switch every built-in payload topology to obstore in one composition cutover.

Purpose: Eliminate divergent runtime mechanics without changing the lifecycle authority or cache/storage layering.
Output: Unified built-in factories, preserved injection/ownership semantics, and topology-wide lifecycle contracts.
</objective>

<scope_guard>
This plan may perform only the listed atomic composition cutover, ownership regressions, and cache-boundary qualification. If execution exposes an unexpected repair spanning another subsystem or architectural boundary, stop and create a new dependent split plan; do not expand Plan 09 into cross-cutting lifecycle, migration, or coordination work.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-07-SUMMARY.md
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-08-SUMMARY.md
@src/cacheness/storage/composition.py
@src/cacheness/storage/blob_store.py
@tests/contracts/test_topology_lifecycle.py
</context>

<interfaces>
`RoleRegistry._register_builtin_participants()` owns named factories; `StoreTopology.resolve()` validates identity/capabilities and preserves injected ownership. `BlobStore._materialize_authority_store()` accepts only the five-method participant and instantiates one `AuthorityLifecycleEngine`.
</interfaces>

<artifacts_this_phase>
- The named filesystem, memory, and S3 payload references all materialize the same adapter class around their respective obstore stores.
- Custom registered/injected providers keep the stable `PayloadGenerationIOProvider` seam.
</artifacts_this_phase>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cut over one built-in profile from each topology tier</name>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/obstore_generation_io.py, tests/test_blob_store_composition.py, tests/test_supported_topologies.py, tests/contracts/test_topology_lifecycle.py</files>
  <read_first>
    - src/cacheness/storage/composition.py: built-in participant registration, constructors, role validation, identity, ownership
    - src/cacheness/storage/obstore_generation_io.py: completed provider constructors and capability declarations
    - tests/contracts/test_topology_lifecycle.py: current profile matrix and authority assertions
  </read_first>
  <behavior>
    - "Named memory/memory, SQLite/filesystem, and deterministic PostgreSQL/S3 profiles each resolve one obstore-backed payload provider."
    - "Each profile performs a complete put/read/overwrite/delete flow under its existing topology-specific progress outcomes."
    - "No built-in factory imports or chooses a legacy payload implementation."
  </behavior>
  <action>Change all built-in payload registrations/factories together so filesystem, memory, and S3 construct the Plan 03/04 obstore providers per D-04/D-07. Keep the stable role names and qualified topology identities only where they describe topology rather than implementation. Pass resource bounds and S3 policy through strict provider constructors. Update profile/composition/topology tests as one cutover and assert all three built-ins materialize the same adapter type beneath their store. Do not add a feature flag, selector, dual path, fallback, or second lifecycle engine.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_composition.py tests/test_supported_topologies.py tests/contracts/test_topology_lifecycle.py -x</automated>
  </verify>
  <done>All named built-in payload topologies run through obstore under their existing authority-owned lifecycle contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve custom provider identity and cache-over-BlobStore ownership</name>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/blob_store.py, tests/test_phase6_policy_contract.py, tests/test_unified_cache_lifecycle_authority.py, tests/test_unified_cache_adversarial_lifecycle.py</files>
  <read_first>
    - src/cacheness/storage/composition.py: `_resolve_ref`, `_validate_participant_role`, and owned participant close
    - src/cacheness/storage/blob_store.py: lifecycle materialization and close ownership
    - src/cacheness/core.py: UnifiedCache BlobStore injection/composition ownership
  </read_first>
  <behavior>
    - "An injected custom provider is retained by identity and not replaced by a built-in obstore provider."
    - "A topology-owned provider closes exactly once; caller-owned injected provider remains caller-owned."
    - "UnifiedCache performs payload/catalog lifecycle only through its BlobStore and adds policy above it."
    - "Participant head/list/object state never becomes committed visibility."
  </behavior>
  <action>Keep structural `PayloadGenerationIOProvider` validation and existing ownership ledger intact during the built-in cutover. Update the actual Phase 6 policy and lifecycle suites—`test_phase6_policy_contract.py`, `test_unified_cache_lifecycle_authority.py`, and `test_unified_cache_adversarial_lifecycle.py`—to regress injected provider identity, caller/registry ownership, exactly-once close, and one AuthorityLifecycleEngine per BlobStore. Exercise UnifiedCache through its topology-created BlobStore and assert cache policy does not call obstore directly. Preserve D-10 and ADR 0001; do not create a cache-side transport/lifecycle coordinator.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase6_policy_contract.py tests/test_unified_cache_lifecycle_authority.py tests/test_unified_cache_adversarial_lifecycle.py -x</automated>
  </verify>
  <done>The cutover preserves extension identity, resource ownership, and the cache-over-BlobStore architecture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove every cache invalidation family removes through BlobStore</name>
  <files>tests/test_phase6_removal_contract.py, tests/test_phase6_decorator_contract.py, tests/test_core.py</files>
  <read_first>
    - tests/test_phase6_removal_contract.py: TTL, size eviction, predicate, single-key, and global-clear removal contracts
    - tests/test_phase6_decorator_contract.py: decorator cache-clear semantics
    - tests/test_core.py: compact cache facade expiry/invalidation contract
    - src/cacheness/core.py: UnifiedCache policy-to-BlobStore removal delegation
  </read_first>
  <behavior>
    - "TTL expiry removes the exact generation through BlobStore and reports the typed removal outcome."
    - "Size-policy eviction removes through BlobStore and preserves a concurrent replacement."
    - "Predicate invalidation, decorator clear, and single-key invalidation each delegate exact removal to BlobStore."
    - "Global clear remains bounded/resumable and removes each selected generation through BlobStore."
  </behavior>
  <action>Extend the existing Phase 6 removal/decorator contracts and the relevant `test_core.py` expiry selector so every CACH-03 invalidation family—TTL, eviction, predicate, decorator, single-key, and global clear—records that UnifiedCache delegates deletion to its BlobStore after the obstore cutover. Preserve generation-conflict and bounded-resume behavior. Assert no invalidation path imports, constructs, heads, lists, or deletes through obstore directly; cache policy may select work, but BlobStore remains the storage engine and lifecycle authority facade.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase6_removal_contract.py tests/test_phase6_decorator_contract.py tests/test_core.py::test_expiry_invalidation_and_statistics_use_typed_results -x</automated>
  </verify>
  <done>All six CACH-03 invalidation families demonstrably remove through BlobStore and retain established conflict/progress semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| configuration/registry -> participant construction | A name or injected object must resolve exactly one declared participant and capability tier. |
| UnifiedCache -> BlobStore -> participant | Policy must not bypass storage authority or create another lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07.1-09-01 | Spoofing | participant identity replacement | high | mitigate | Preserve exact injected instances and qualified named identities before construction. |
| T-07.1-09-02 | Elevation | duplicate lifecycle authority | high | mitigate | Assert one AuthorityLifecycleEngine and prohibit participant/cache promotion or debt ownership. |
| T-07.1-09-03 | Tampering | partial runtime cutover | high | mitigate | Switch all three built-in factories together and assert no selector/fallback branch exists. |
| T-07.1-09-04 | Denial of Service | close ownership | medium | mitigate | Preserve caller/registry ownership and exactly-once close contracts. |
</threat_model>

<verification>
- All built-in topology and composition contracts pass after one cutover.
- AST/import assertions find no runtime selector or legacy built-in factory call.
- Cache tests prove transport remains below BlobStore.
- The six CACH-03 invalidation families pass against the cut-over composition.
</verification>

<success_criteria>
- Obstore is the sole built-in payload transport implementation.
- The lifecycle authority and capability tiers remain unchanged.
- Custom providers and cache policy preserve their established seams.
</success_criteria>

<source_coverage>
- GOAL: Replace divergent built-in mechanics with one participant while preserving BlobStore authority.
- REQ: STOR-03/04/05/07, BACK-01/03/04/06, CACH-01/02/03.
- RESEARCH: composition cutover, responsibility map, no runtime fallback.
- CONTEXT: D-04, D-06, D-07, D-08, D-09, D-10, D-14.
</source_coverage>

<output>
Create `.planning/phases/07.1-obstore-payload-participant-unification/07.1-09-SUMMARY.md` when done.
</output>
