---
phase: 11-clean-supplemental-documentation-and-normalize-validation-ev
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - tools/run_phase8_platform_gates.py
  - tools/run_phase8_local_gates.py
  - .github/workflows/quality.yml
autonomous: true
requirements: [D-05, D-08, D-18]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The Phase 8 platform/local gate APIs and quality workflow contain only retained feature profiles and jobs (D-05)."
    - "Linux/macOS boundaries, native-Windows nonqualification, protected-live nonclaims, and retained package/example jobs remain structurally intact (D-08, D-18)."
  artifacts:
    - path: tools/run_phase8_platform_gates.py
      provides: "Retained core/non-TensorFlow platform profile matrix without a dormant TensorFlow branch"
    - path: .github/workflows/quality.yml
      provides: "Retained quality workflow without TensorFlow-only jobs or setup steps"
  key_links:
    - from: .github/workflows/quality.yml
      to: tools/run_phase8_platform_gates.py
      via: "retained platform job command and literal profile choices"
      pattern: "feature-profile"
    - from: tools/run_phase8_local_gates.py
      to: tools/run_phase8_platform_gates.py
      via: "CLI choice forwarding into the fixed local qualification path"
      pattern: "feature_profile"
  prohibitions:
    - "Do not modify BlobStore, UnifiedCache, lifecycle authorities, obstore participation, retained handler behavior, or topology guarantees."
    - "Do not retain a compatibility alias, tombstone, empty extra, dormant branch, or re-enable recipe for the retired capability."
    - "Do not fabricate live PostgreSQL/Amazon-S3, controlled-Linux, native-Windows, or immutable-publication qualification."
    - "Do not create a wrapper validation report or another validation authority."
    - "Do not write or preselect the milestone-audit verdict before the final layered acceptance gate."
---

<objective>
Remove the TensorFlow-only CI and qualification paths while preserving the exact retained workflow and platform evidence contracts.

Purpose: Complete D-05 outside runtime/packaging without weakening any retained qualification or nonclaim boundary.
Output: TensorFlow-free Phase 8 platform/local tooling and quality workflow.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-02-SUMMARY.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-03-SUMMARY.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-CONTEXT.md

spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).

No external API integration: this plan edits repository-local CI and qualification declarations and performs no external service call.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TensorFlow feature-profile branches from platform and local gates</name>
  <files>tools/run_phase8_platform_gates.py, tools/run_phase8_local_gates.py</files>
  <read_first>
    - tools/run_phase8_platform_gates.py — remove `TENSORFLOW_COMPATIBLE_MINORS`, the TensorFlow `FEATURE_PROFILES` member, compatibility/minor branches, special row validation, and TensorFlow aggregation helper only.
    - tools/run_phase8_local_gates.py — remove the TensorFlow CLI choice/forwarding while preserving core and retained non-TensorFlow local gate execution.
    - tests/qualification/test_phase8_platform.py — Plan 11-02's exact retained-profile and platform-nonclaim contract.
    - tests/qualification/test_phase8_release.py — preserve fixed local-readiness, deferred service/performance/publication, and exact evidence inventories.
  </read_first>
  <action>
Delete only TensorFlow-specific constants, choices, branches, compatibility results, and aggregation paths. Preserve the literal retained profile set, Linux full matrix, macOS boundary smoke, Windows `UNAVAILABLE`/`NOT_QUALIFIED` record, source identity, deterministic evidence, and fail-closed input validation. Do not replace the retired profile with a generic extension point or relax unknown-profile rejection.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/qualification/test_phase8_platform.py tests/qualification/test_phase8_release.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Only retained feature profiles are accepted.
    - Unknown profiles still fail closed.
    - Platform and release nonclaims remain exact and green.
  </acceptance_criteria>
  <done>Phase 8 platform/local tooling no longer advertises or computes TensorFlow evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Delete TensorFlow-only workflow jobs and repair retained job structure</name>
  <files>.github/workflows/quality.yml</files>
  <read_first>
    - .github/workflows/quality.yml — delete the complete `tensorflow-compatible` job and every later TensorFlow-specific setup/install/probe step; preserve retained jobs verbatim except required dependency/profile adjustments.
    - tests/qualification/test_phase8_quality_workflow.py — Plan 11-02's post-cutover workflow contract.
    - tests/test_phase9_quality_workflow.py — retained-boundary `_job_block` parser contract.
    - pyproject.toml — final five-extra/dependency-group surface from Plan 11-03.
  </read_first>
  <action>
Remove the exact TensorFlow-only job and setup/install/probe steps from `quality.yml`. Keep stable Linux, package, canonical-example, coverage/quality, and protected-live definitions, markers, permissions, frozen dependency use, and fail-closed remote semantics unchanged. Ensure retained jobs do not request the deleted dependency group and that YAML block structure satisfies the repaired retained-job parser.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/qualification/test_phase8_quality_workflow.py tests/test_phase9_quality_workflow.py -x</automated>
  </verify>
  <acceptance_criteria>
    - No TensorFlow-only job/setup/install/probe remains in the workflow.
    - Retained workflow jobs parse and retain their frozen, non-live, and protected-live semantics.
    - No new job or compatibility profile replaces the deleted one.
  </acceptance_criteria>
  <done>The CI/qualification portion of D-05 is complete without changing D-08's protected boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML → CI job execution | Deleting one job must not alter retained frozen or protected-live execution. |
| CLI profile input → qualification evidence | Unknown or retired profiles must fail rather than become ambiguous evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Tampering | quality workflow job graph | high | mitigate | Exact retained-job structural tests and deletion of only named TensorFlow blocks in Task 2. |
| T-11-11 | Spoofing | feature-profile evidence | high | mitigate | Closed retained profile set and fail-closed unknown-profile tests in Task 1. |
| T-11-12 | Repudiation | remote/platform nonclaims | medium | mitigate | Retain release/platform assertions for Windows, live services, performance, and publication in both tasks. |
</threat_model>

## Artifacts this phase produces

None. This plan only modifies existing CI and qualification artifacts.

<verification>
Run the exact Phase 8 platform/release/workflow and Phase 9 workflow contracts after each owning edit.
</verification>

<success_criteria>
- TensorFlow has no CI job, setup step, CLI profile, row, or aggregate.
- Retained jobs/profiles and explicit nonclaims remain green.
- No external service is contacted.
</success_criteria>

<output>
Create `.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-04-SUMMARY.md` when done.
</output>
