---
phase: 11-clean-supplemental-documentation-and-normalize-validation-ev
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - src/cacheness/handlers.py
  - src/cacheness/config.py
  - src/cacheness/storage/handlers/__init__.py
  - tests/test_tensorflow_handler.py
  - pyproject.toml
  - uv.lock
  - tools/run_phase8_packaging.py
  - tools/phase8_evidence.py
autonomous: true
requirements: [D-05, D-08, D-18]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The retired handler has no implementation, lazy loader, config flag, registry identity, compatibility export, dedicated test, package extra, dependency group, lock entry, or packaging profile (D-05)."
    - "All retained handlers, the store-local registry, private staging, BlobStore, UnifiedCache, and lifecycle/topology boundaries remain unchanged (D-08)."
  artifacts:
    - path: src/cacheness/handlers.py
      provides: "Retained built-in handlers and registry with the dormant TensorFlow surface physically absent"
    - path: pyproject.toml
      provides: "Exact five-extra published package surface"
    - path: uv.lock
      provides: "Generated dependency graph converged from the pruned manifest"
  key_links:
    - from: pyproject.toml
      to: uv.lock
      via: "plain `uv lock`, followed by `uv lock --check`"
      pattern: "optional-dependencies|dependency-groups"
    - from: tools/run_phase8_packaging.py
      to: tests/packaging/test_wheel_matrix.py
      via: "exact retained optional-group probes and one WheelArtifact"
      pattern: "OPTIONAL_GROUPS|run_optional"
  prohibitions:
    - "Do not modify BlobStore, UnifiedCache, lifecycle authorities, obstore participation, retained handler behavior, or topology guarantees."
    - "Do not retain a compatibility alias, tombstone, empty extra, dormant branch, or re-enable recipe for the retired capability."
    - "Do not fabricate live PostgreSQL/Amazon-S3, controlled-Linux, native-Windows, or immutable-publication qualification."
    - "Do not create a wrapper validation report or another validation authority."
    - "Do not write or preselect the milestone-audit verdict before the final layered acceptance gate."
---

<objective>
Perform the costly direct pre-production TensorFlow cutover across runtime, configuration, exports, package metadata, generated lock state, and packaging evidence.

Purpose: Make package truth match the supported product surface while leaving every retained storage and handler boundary intact.
Output: A TensorFlow-free runtime and manifest/lock/package-profile surface satisfying Plan 11-01's negative contracts.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-01-SUMMARY.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-CONTEXT.md
@docs/adr/0001-topology-specific-storage-guarantees.md

spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).

No external API integration: this plan deletes an optional in-process handler and dependency profile; it adds no external API surface.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Physically remove the runtime, config, export, and dedicated-test surface</name>
  <reversibility rating="costly">Per D-05, restoring this capability later requires coordinated reintroduction of its dependency, handler, persisted identity, public/configuration surface, and qualification matrix.</reversibility>
  <files>src/cacheness/handlers.py, src/cacheness/config.py, src/cacheness/storage/handlers/__init__.py, tests/test_tensorflow_handler.py</files>
  <read_first>
    - src/cacheness/handlers.py — remove `_lazy_import_tensorflow`, `TENSORFLOW_AVAILABLE`, `_tensorflow_import_attempted`, `tf`, `TensorFlowTensorHandler`, commented registration branches, `handler_enable_map` member, and built-in identity while preserving all adjacent handlers/registry behavior.
    - src/cacheness/config.py — remove `HandlerConfig.enable_tensorflow_tensors` and the `tensorflow_tensors` priority value only.
    - src/cacheness/storage/handlers/__init__.py — remove the optional import probe, module docstring claim, and conditional `__all__` append only.
    - tests/test_tensorflow_handler.py — dedicated dormant capability test to delete outright.
    - tests/test_handler_registration.py — retained handler-registry regression owner; do not broaden or replace its store-local contract.
    - .codex/skills/spike-findings-cacheness/references/handler-integration.md — preserve private contained staging/snapshot and store-local registration for retained formats.
  </read_first>
  <action>
Delete the complete TensorFlow implementation and reachability surface named above, then delete `tests/test_tensorflow_handler.py`. Preserve imports and availability guards for Blosc2, dill, pandas, Polars, and every retained handler; preserve `FormatHandler`, `HandlerRegistry`, suffix containment, private staging/snapshot I/O, and ordinary natural lookup failure for unsupported persisted identities. Do not replace the class with an alias, disabled implementation, custom missing-format error, migration reader, tombstone, or tailored import hook. Do not edit BlobStore, UnifiedCache, authority, participant, or topology modules.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_handler_registration.py tests/test_config_validation.py tests/packaging/test_wheel_matrix.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The named TensorFlow globals/helper/class/config priority/export and dedicated test file are physically absent.
    - Retained handler registration and configuration validation remain green.
    - No storage lifecycle or retained-handler behavior is changed.
  </acceptance_criteria>
  <done>D-05's runtime/config/export/test cutover is complete, and D-08's architecture boundary remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Converge manifest, lockfile, and package evidence to the retained five-extra surface</name>
  <reversibility rating="costly">Per D-05, restoring the published extra and artifact qualification later is a coordinated package-contract change rather than a local edit.</reversibility>
  <files>pyproject.toml, uv.lock, tools/run_phase8_packaging.py, tools/phase8_evidence.py</files>
  <read_first>
    - pyproject.toml — remove only `[project.optional-dependencies].tensorflow` and `[dependency-groups].tensorflow`; preserve recommended, dataframes, s3, postgresql, cloud, dev, and retained dependencies.
    - uv.lock — generated output only; do not hand-edit package blocks.
    - tools/run_phase8_packaging.py — remove the TensorFlow optional group, compatible-minor constant/function, probe script/result shape, argument, and compatibility branching while preserving one-artifact, base, and retained-extra probes.
    - tools/phase8_evidence.py — remove the retired feature/profile inventory member while preserving evidence schema and nonclaims.
    - tests/packaging/test_wheel_matrix.py — Plan 11-01's exact post-cutover contract is authoritative.
    - tests/test_phase10_sqlcache_removal.py — preserve the SqlCache/DuckDB cutover while updating only its retained-extra tuple through Plan 11-01.
  </read_first>
  <action>
Edit the manifest first, then remove TensorFlow-only packaging/evidence constants, functions, parameters, probes, and result branches. Run plain `uv lock` so the existing resolution is preferred; do not hand-edit `uv.lock`, request upgrades, or prune retained transitive dependencies by name. Ensure the packaging tool exposes only the five literal retained groups and keeps base/source-free import, public-export, installed-metadata, digest binding, and local BlobStore/UnifiedCache probes unchanged.
  </action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/packaging/test_wheel_matrix.py tests/test_phase10_sqlcache_removal.py tests/qualification/test_phase8_release.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock --check` succeeds after plain lock convergence.
    - Published and local groups expose exactly recommended, dataframes, s3, postgresql, cloud, and dev in their respective sections.
    - The fresh-wheel negative contract and retained local round trips pass.
    - No unrelated dependency version is deliberately upgraded.
  </acceptance_criteria>
  <done>The package and generated lock surfaces satisfy D-05 and the installed-artifact portions of D-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted persisted handler identity → registry selection | Removed identities must not reach a handler, while retained identities still resolve through guarded private I/O. |
| manifest → generated lock → wheel metadata | Removed dependency truth must converge without losing retained requirements or introducing unrelated upgrades. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Elevation of Privilege | handler/config/export reachability | high | mitigate | Physical deletion plus Plan 11-01 import/export/wheel tests; ordinary unsupported identity failure only. |
| T-11-08 | Tampering | manifest/lock/Core Metadata | high | mitigate | Manifest-first edit, plain uv convergence, lock freshness, exact installed metadata, and retained round trips. |
| T-11-09 | Denial of Service | retained handler imports | medium | mitigate | Focused registry/config tests preserve lazy optional imports and exclude lifecycle or staging changes. |
</threat_model>

## Artifacts this phase produces

None. This plan only modifies or deletes existing artifacts and symbols.

<verification>
The direct cutover is complete when focused registry/config tests, lock freshness, the source-free wheel matrix, retained release contracts, and the Phase 10 inventory all pass.
</verification>

<success_criteria>
- No TensorFlow runtime/config/export/package/profile path remains in the files owned by this plan.
- Retained handlers and local installed journeys remain green.
- The lock is generated from the pruned manifest and no lifecycle code was touched.
</success_criteria>

<output>
Create `.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-03-SUMMARY.md` when done.
</output>
