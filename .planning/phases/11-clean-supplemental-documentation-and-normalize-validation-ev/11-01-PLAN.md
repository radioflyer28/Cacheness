---
phase: 11-clean-supplemental-documentation-and-normalize-validation-ev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/packaging/test_wheel_matrix.py
  - tests/test_phase9_documentation.py
  - tests/test_phase10_sqlcache_removal.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-18]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The complete negative TensorFlow cutover is executable as a source-free wheel and installed-metadata contract before implementation edits begin (D-05, D-08, D-18)."
    - "Supplemental-document disposition, current-reference ownership, and the four canonical executable examples are exact automated contracts (D-01 through D-06)."
  artifacts:
    - path: tests/packaging/test_wheel_matrix.py
      provides: "Negative wheel member, metadata, import/export, extra, and retained local round-trip assertions"
    - path: tests/test_phase9_documentation.py
      provides: "Exact supplemental-document and current-reference ownership assertions"
    - path: tests/test_phase10_sqlcache_removal.py
      provides: "Closed retained-extra inventory after the direct TensorFlow cutover"
  key_links:
    - from: tests/packaging/test_wheel_matrix.py
      to: pyproject.toml
      via: "literal optional-group and installed Core Metadata inspection"
      pattern: "optional-dependencies|Provides-Extra|Requires-Dist"
    - from: tests/test_phase9_documentation.py
      to: docs/RELEASE_QUALIFICATION.md
      via: "explicit current-document inventory, link resolution, and nonclaim assertions"
      pattern: "RELEASE_QUALIFICATION"
  prohibitions:
    - "Do not modify BlobStore, UnifiedCache, lifecycle authorities, obstore participation, retained handler behavior, or topology guarantees."
    - "Do not retain a compatibility alias, tombstone, empty extra, dormant branch, or re-enable recipe for the retired capability."
    - "Do not fabricate live PostgreSQL/Amazon-S3, controlled-Linux, native-Windows, or immutable-publication qualification."
    - "Do not create a wrapper validation report or another validation authority."
    - "Do not write or preselect the milestone-audit verdict before the final layered acceptance gate."
---

<objective>
Create the leading production contract tracer for Phase 11: one executable negative path spanning built-wheel truth, installed metadata, current documentation ownership, and retained local journeys.

Purpose: Make the cutover and documentation/evidence perimeter fail closed before any implementation surface is removed, per D-01 through D-06, D-08, and D-18.
Output: Durable Wave 0 contracts in the existing package and documentation test owners.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-CONTEXT.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-RESEARCH.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md

spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).

No external API integration: this plan adds local package/document contracts for an in-package capability removal and does not integrate an external service.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Encode the end-to-end negative installed-package cutover</name>
  <files>tests/packaging/test_wheel_matrix.py, tests/test_phase10_sqlcache_removal.py</files>
  <read_first>
    - tests/packaging/test_wheel_matrix.py — reuse WheelArtifact build/digest, ZIP membership, source-free install, Core Metadata, public-export, and BlobStore/UnifiedCache round-trip helpers.
    - tests/test_phase10_sqlcache_removal.py — mirror the direct-removal closed-inventory pattern while preserving its SqlCache assertions.
    - pyproject.toml — current published and local TensorFlow groups that the contract must reject after cutover.
    - tools/run_phase8_packaging.py — current optional-group/profile probe API consumed by the wheel matrix.
    - src/cacheness/handlers.py — current class and persisted identities that must become naturally absent.
  </read_first>
  <behavior>
    - "The exact retained published extras are recommended, dataframes, s3, postgresql, and cloud; no extra or conditional installed requirement survives for the retired integration (D-05)."
    - "A single digest-bound wheel has no retired handler member/export/import surface and still performs representative installed BlobStore and UnifiedCache local round trips (D-05, D-08, D-18)."
    - "The contract rejects source-tree fallback and does not weaken retained handler or public-export inventories."
  </behavior>
  <action>
Extend the existing wheel matrix and Phase 10 closed-inventory test with literal post-cutover expectations. Reuse the current WheelArtifact throughout ZIP inspection, isolated installation, Core Metadata inspection, import probes, and retained local round trips. Add focused tests named `test_tensorflow_surface_is_absent_from_built_wheel_and_metadata` and `test_retained_local_round_trips_survive_tensorflow_cutover`; replace positive optional-profile expectations with the exact five-extra inventory. This is Wave 0: write the final production-quality assertions before modifying runtime, manifest, lock, or qualification tools. Do not add a parallel build harness or infer the expected inventory from the edited manifest.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q --collect-only tests/packaging/test_wheel_matrix.py tests/test_phase10_sqlcache_removal.py</automated>
  </verify>
  <acceptance_criteria>
    - Both named negative package tests collect under the frozen environment.
    - Assertions bind one wheel digest through archive, metadata, import, and retained local-round-trip checks.
    - The expected extra inventory is literal and excludes no retained dependency group.
  </acceptance_criteria>
  <done>The installed-artifact cutover boundary is executable before implementation and will turn green only when the complete D-05 package surface is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode supplemental-document disposition and canonical example ownership</name>
  <files>tests/test_phase9_documentation.py</files>
  <read_first>
    - tests/test_phase9_documentation.py — extend its explicit current-document inventory, reference resolver, task-guide ownership checks, and current-surface scans.
    - tests/test_phase9_examples.py — preserve its exact four-file executable-example allowlist and network-free child execution contract.
    - docs/README.md — current documentation index that must not link deleted supplements.
    - docs/API_REFERENCE.md — canonical destination for the concise verified pandas/Parquet note.
    - docs/RELEASE_QUALIFICATION.md — sole detailed platform evidence owner and required nonclaim destination.
    - docs/PANDAS_COMPATIBILITY.md, docs/CUSTOM_METADATA.md, docs/CROSS_PLATFORM_GUIDE.md, docs/WINDOWS_COMPATIBILITY.md, docs/TENSORFLOW_TENSOR_GUIDE.md, docs/TENSORFLOW_HANDLER_STATUS.md — exact D-01 through D-06 disposition set.
  </read_first>
  <behavior>
    - "All six supplemental files have an exact expected deletion outcome; pandas value survives only as a concise tested API-reference statement and platform truth only in release qualification (D-01 through D-03, D-06)."
    - "Current docs resolve links and point to exactly the four canonical executable examples rather than duplicate runnable snippets (D-04)."
    - "Release qualification keeps Windows, live remote services, controlled-Linux performance, and publication explicitly nonqualified or deferred."
  </behavior>
  <action>
Extend `tests/test_phase9_documentation.py` with `test_supplemental_documentation_is_consolidated_or_deleted` and `test_current_guidance_has_no_supported_tensorflow_claim`. Assert the exact deletion set, the concise pandas/Parquet/index/name/dtype survivor in `docs/API_REFERENCE.md`, the local regression and platform nonclaim owner in `docs/RELEASE_QUALIFICATION.md`, resolved current links, and references to the literal four canonical example files. Reuse `tests/test_phase9_examples.py` as the only runnable-example owner; do not copy supplemental snippets into the test or create another documentation registry.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q --collect-only tests/test_phase9_documentation.py tests/test_phase9_examples.py</automated>
  </verify>
  <acceptance_criteria>
    - Both new documentation-contract tests collect.
    - The deletion inventory is exact and current-link resolution remains fail closed.
    - The four canonical examples remain the only executable documentation owners.
  </acceptance_criteria>
  <done>The D-01 through D-06 documentation outcome is encoded before any guide is edited or deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → built wheel → isolated interpreter | Package truth must not be inferred from checkout imports or stale metadata. |
| supplemental prose → canonical current guidance | Unsupported snippets and platform claims must not acquire authority through consolidation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | wheel members/Core Metadata/public exports | high | mitigate | Bind one WheelArtifact digest through member, metadata, source-free import, and retained round-trip assertions in Task 1. |
| T-11-02 | Spoofing | current documentation ownership | medium | mitigate | Use literal document/example inventories, resolved links, and canonical destination assertions in Task 2. |
| T-11-03 | Information Disclosure | deferred qualification claims | low | accept | This plan handles public nonclaim wording only; no credentials, live calls, or remote evidence are introduced. |
</threat_model>

## Artifacts this phase produces

- New test symbols: `test_tensorflow_surface_is_absent_from_built_wheel_and_metadata`, `test_retained_local_round_trips_survive_tensorflow_cutover`, `test_supplemental_documentation_is_consolidated_or_deleted`, and `test_current_guidance_has_no_supported_tensorflow_claim`.
- New files: None.

<verification>
Wave 0 is complete for package/document boundaries when the named tests collect and later implementation plans make them green without changing their asserted scope.
</verification>

<success_criteria>
- The negative installed-package contract covers wheel membership, Core Metadata, imports/exports, exact extras, and retained local journeys.
- Documentation contracts cover the exact six-file disposition and canonical example/platform ownership.
- No lifecycle or retained-handler implementation file is modified.
</success_criteria>

<output>
Create `.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-01-SUMMARY.md` when done.
</output>
