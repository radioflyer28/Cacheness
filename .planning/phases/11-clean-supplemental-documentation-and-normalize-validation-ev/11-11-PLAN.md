---
phase: 11-clean-supplemental-documentation-and-normalize-validation-ev
plan: 11
type: execute
wave: 9
depends_on: [11-10]
files_modified:
  - tests/test_phase9_evidence_metadata.py
  - .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [D-18, D-19, D-20]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The pre-audit validation contract checks Phase 11's qualified source revision against Git ancestry and the current protected source/test tree; after Plan 11-12 refreshes the audit, its separate contract also binds audited_head to that revision. Later artifact-only commits are accepted, while relevant source drift fails (D-18, D-19)."
    - "A second, clearly dated post-review local acceptance run qualifies the tree containing the release-guide and normal-CI packaging fixes, with actual wheel/metadata/round-trip, Ruff, lock, focused, and frozen non-live results recorded (D-18)."
    - "The earlier one-run acceptance remains historical evidence; no local pass promotes BACK-05, QUAL-06, native Windows, or immutable publication (D-12, D-20)."
  artifacts:
    - path: tests/test_phase9_evidence_metadata.py
      provides: "Git-backed final-tree provenance and stale-audit regression contract"
    - path: .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
      provides: "Exact qualified revision and observed post-review acceptance addendum"
  key_links:
    - from: tests/test_phase9_evidence_metadata.py
      to: .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md
      via: "qualified_source_revision parsed from canonical validation frontmatter"
      pattern: "qualified_source_revision"
    - from: tests/test_phase9_evidence_metadata.py
      to: .planning/v1.0-v1.0-MILESTONE-AUDIT.md
      via: "audited_head equality and protected-tree drift check only in the refreshed-audit test after Plan 11-12"
      pattern: "audited_head"
  prohibitions:
    - "No production lifecycle, concurrency, recovery, backend, handler-persistence, or topology-guarantee edits (D-08, D-16)."
    - "No remote-service, controlled-Linux, native-Windows, or immutable-publication pass claim (D-12, D-20)."
    - "Do not replace or relabel the earlier one-run acceptance; append the post-review run with its own source identity (D-11, D-18)."
---

<objective>
Close the test/evidence half of the verifier's final-tree provenance gap without refreshing the milestone audit yet.

Purpose: Make a stale audit revision or changed qualified source observable, then qualify the post-review tree with bounded local evidence.
Output: Stronger evidence metadata tests and an exact second-run addendum in `11-VALIDATION.md`.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-CONTEXT.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VERIFICATION.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-09-SUMMARY.md
@.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-10-SUMMARY.md
@docs/adr/0001-topology-specific-storage-guarantees.md

No newly mapped requirement IDs exist; the D-18–D-20 decision IDs above are the Phase 11 completion contract. External service qualification is explicitly outside this gap.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one qualified-revision-to-audit path and reject stale provenance</name>
  <files>tests/test_phase9_evidence_metadata.py</files>
  <read_first>
    - tests/test_phase9_evidence_metadata.py — retain its canonical validation/discovery assertions and replace the refreshed-audit branch's original-head inequality with exact qualified-source binding.
    - .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md — current historical acceptance wording and frontmatter.
    - .planning/v1.0-v1.0-MILESTONE-AUDIT.md — current audited/audited_head fields and nonclaim checks.
    - .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VERIFICATION.md — the stale-head failure mode.
  </read_first>
  <behavior>
    - A temporary Git fixture with a committed qualified source revision and a later commit changing only planning artifacts passes the pre-audit validation check even when the audit head is stale; the refreshed-audit check passes only with a matching audit head.
    - The pre-audit check fails if qualified_source_revision is malformed or not an ancestor, or a later commit changes README/docs, workflow, source, tools, tests, examples, manifest/lock, or current codebase-map paths; the refreshed-audit check additionally fails if audited_head names an older commit.
    - Dirty tracked or untracked protected paths also fail; no test contains a fixed expected qualification SHA.
  </behavior>
  <action>
    Add a small Git-backed qualified-source helper in `tests/test_phase9_evidence_metadata.py` that validates full 40-hex `qualified_source_revision`, resolves it to a commit, and requires it to be an ancestor of HEAD. Compare committed revisions plus staged/unstaged/untracked state for a literal protected current-tree inventory: `README.md`, `AGENTS.md`, `docs/`, `.github/workflows/`, `src/`, `tests/`, `tools/`, `examples/`, `pyproject.toml`, `uv.lock`, and `.planning/codebase/`; include `.planning/REQUIREMENTS.md` because it controls the audit ledger. Planning execution/evidence/audit commits outside that inventory may follow qualification. Use `git diff`/`git ls-files` results, not a SHA literal, timestamp comparison, or original-head inequality as proof. Add temporary-repository tests for the passing artifact-only path and each rejection class. When the validation record declares `qualified_source_revision`, connect only the revision format, Git ancestry, and protected-tree cleanliness checks to `test_phase11_validation_record_matches_final_acceptance_evidence`; this pre-audit test must pass while `audited_head` is stale. In the refreshed branch of `test_phase11_refreshed_milestone_audit_is_evidence_derived`, reuse those source checks and additionally require `audited_head == qualified_source_revision` exactly; that branch must fail on a stale audit until Plan 11-12 updates it. Keep the pre-qualification branch runnable while the revision is not yet recorded, so the exact suite can run before the audit-last step (D-18, D-19). Commit the test change before Task 2 chooses the source revision it will qualify.
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase9_evidence_metadata.py -k 'provenance or qualification' -x</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic artifact-only commits pass pre-audit with a stale audited_head; invalid revision, protected committed drift, and protected dirty/untracked paths fail at that stage. The refreshed-audit path additionally rejects stale audited_head.
    - The current checkout's pre-qualification evidence parser remains runnable without treating its old audited_head as final-tree proof.
    - Only the strengthened refreshed-audit branch compares audited_head to qualified_source_revision; both branches validate the current protected tree, and neither uses a hardcoded review-fix or qualification SHA.
  </acceptance_criteria>
  <done>The staged provenance contract proves source revision and cleanliness before audit refresh, then exact source → audit identity in the refreshed-audit test; it is committed before final-tree qualification.</done>
</task>

<task type="auto">
  <name>Task 2: Qualify the post-review tree and record the exact second run</name>
  <files>.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-VALIDATION.md — preserve its first one-run record and append a separately identified post-review acceptance record.
    - tests/test_phase9_evidence_metadata.py — Task 1's revision field and protected-tree contract.
    - tests/packaging/test_wheel_matrix.py — one fresh digest-bound wheel, installed metadata, absence, and retained BlobStore/UnifiedCache round trips.
    - tests/test_phase9_documentation.py, tests/test_full_suite_environment.py, tests/test_phase9_quality_workflow.py, tests/qualification/test_phase8_quality_workflow.py — release-guide command and normal-CI packaging fix contracts.
    - .github/workflows/quality.yml, docs/RELEASE_QUALIFICATION.md, pyproject.toml, uv.lock — post-review tree and exact non-live/lock boundary.
    - docs/adr/0001-topology-specific-storage-guarantees.md — stop on a genuine integrity/recovery failure instead of changing lifecycle code.
  </read_first>
  <action>
    With Task 1 committed, run `uv lock --check`; the focused CR-01/WR-01 release-guide and normal-CI packaging selectors; `tests/packaging/test_wheel_matrix.py` for a fresh source-free wheel, installed metadata, retired-surface absence, and retained local BlobStore/UnifiedCache round trips; and scoped Ruff on `tests/test_phase9_evidence_metadata.py` plus every Python file changed since the first acceptance and any earlier Phase 11 Python file whose previous Ruff result no longer applies. Then run exactly `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false -m 'not (live_postgresql or live_aws_s3 or live_remote)'` on that committed source/test tree. Record each command's actual exit/result and the full-suite observed summary, not an inferred test count. After all gates pass, capture `git rev-parse HEAD` as `qualified_source_revision` (the revision including the review fixes and strengthened tests), verify the protected tree is clean, and append a clearly titled post-review second-run section/frontmatter field in `11-VALIDATION.md`. Keep the earlier Plan 11-09 one-run result labeled historical; do not replace it, preselect a new audit verdict, or edit the audit. Preserve D-12/D-20 nonclaims. On any gate failure, stop with the observed result and no new qualified revision; a genuine lifecycle integrity/recovery failure invokes D-16/ADR 0001 and no production repair in this phase (D-18, D-19).
  </action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase9_evidence_metadata.py::test_phase11_validation_record_matches_final_acceptance_evidence -x</automated>
  </verify>
  <acceptance_criteria>
    - `11-VALIDATION.md` contains one full qualified 40-hex revision and an explicitly separate post-review second-run record with exact observed focused, wheel, Ruff, lock, and frozen non-live outcomes.
    - The source/test revision is committed before its gates run, includes the strengthened provenance test, and the protected paths have no staged, unstaged, or untracked drift at capture.
    - Fresh-wheel archive/installed metadata and retained local round trips pass; the exact frozen non-live command exits zero on that same protected tree.
    - The validation-record test passes with the qualified revision while the milestone audit remains unchanged; the separate strict refreshed-audit test is expected to fail until 11-12. The original one-run record and BACK-05, QUAL-06, native Windows, and publication nonclaims remain intact.
  </acceptance_criteria>
  <done>The final post-review source/test tree is qualified and recorded; only audit derivation remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git source/test revision → validation record | A reported local pass is valid only for the exact committed protected tree on which it ran. |
| Validated local tree → milestone audit | Later planning artifacts may be written, but a changed source, test, documentation, CI, or manifest tree invalidates the earlier qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-32 | Tampering | qualified source identity | high | mitigate | Task 1 validates the revision against Git ancestry, protected committed changes, and dirty/untracked protected paths. |
| T-11-33 | Repudiation | post-review acceptance record | high | mitigate | Task 2 names the exact revision, commands, observed results, and separates its second run from historical one-run evidence. |
| T-11-34 | Spoofing | local result as remote/platform release proof | high | mitigate | Task 2 retains exact marker exclusions and all four explicit nonclaims under D-20. |
</threat_model>

## Artifacts this phase produces

`tests/test_phase9_evidence_metadata.py` gains focused private Git-provenance helper(s) and temporary-repository regression tests; `11-VALIDATION.md` gains `qualified_source_revision` and a post-review second-run section. No production symbol or new package is created.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Phase 11 product cutover and bounded local milestone verdict | 11-01–11-12 | COVERED; 11-11 repairs the final-tree gate |
| REQ | — | No newly mapped requirement IDs; 42 in-scope satisfied, BACK-05/QUAL-06 deferred | 11-01–11-12 | COVERED |
| RESEARCH | CUTOVER/DOCS/VALIDATION/PHASE3 | Existing research scope and constraints | 11-01–11-10 | COVERED; no reopening |
| RESEARCH | AUDIT | Layered acceptance before an evidence-derived current audit | 11-11, 11-12 | COVERED |
| CONTEXT | D-01–D-17 | Original cleanup, cutover, normalization, and ADR boundaries | 11-01–11-10 | COVERED; preserved |
| CONTEXT | D-18 | Final layered acceptance at the qualified tree | 11-11 | COVERED |
| CONTEXT | D-19–D-20 | Audit last with explicit deferrals | 11-11, 11-12 | COVERED |

<verification>
Task 1 exercises real temporary-Git pre-audit and refreshed-audit paths plus negative drift cases. Task 2's validation-record selector checks revision format, ancestry, and protected-tree cleanliness while the audit remains stale; final audit identity is proved by the separate refreshed-audit selector in 11-12.
</verification>

<success_criteria>
- The strengthened refreshed-audit parser cannot pass a stale audited_head; the pre-audit validation parser passes with a qualified clean source tree before the audit is rewritten.
- Post-review acceptance is a separate, exact-revision record with all required bounded gates observed.
- Production lifecycle files and the milestone audit remain untouched in this plan.
</success_criteria>

<output>
Create `.planning/phases/11-clean-supplemental-documentation-and-normalize-validation-ev/11-11-SUMMARY.md` when done.
</output>
