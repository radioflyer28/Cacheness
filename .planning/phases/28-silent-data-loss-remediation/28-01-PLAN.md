---
phase: 28-silent-data-loss-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/storage/blob_store.py
  - src/cacheness/core.py
  - tests/test_core.py
  - tests/test_blob_store.py
  - tests/test_storage_mode.py
autonomous: true
requirements: [REL-01]
requirements_addressed: [REL-01]
must_haves:
  truths:
    - "D-01/D-02: Source this plan from docs/CODE_REVIEW_ACTIONS.md TASK-1 and docs/CODE_REVIEW_FINDINGS.md R1."
    - "D-03/D-04: Keep this as one TASK-1 slice, one beads issue, and one atomic commit before later Wave 1 slices."
    - "D-06: Replace root-only globbing in _clear_blob_files()."
    - "D-07: Choose backend list_blobs(namespace) enumeration only if current APIs support all needed namespaces; otherwise use careful cache_dir.rglob."
    - "D-08: Never delete *.db, *.db-wal, *.db-shm, cache_metadata.json*, .intents/, or .cache_signing_key*."
    - "D-09: Keep _clear_blob_files() no-lock because callers own synchronization."
    - "D-26/D-27/D-28: Run the TASK-1 verify-first repro before implementation and include tests/test_storage_mode.py in Tier-1."
  artifacts:
    - path: "src/cacheness/storage/blob_store.py"
      provides: "Correct namespace-recursive blob cleanup"
      contains: "_clear_blob_files"
    - path: "src/cacheness/core.py"
      provides: "clear_all() and clear_all_namespaces() integration"
      contains: "clear_all_namespaces"
    - path: "tests/test_core.py"
      provides: "Regression for clear_all() and clear_all_namespaces() removing blob files while preserving reserved files"
      contains: "clear_all"
---

<objective>
Fix `clear_all()` and `clear_all_namespaces()` so users can remove cache
entries and all namespace blob files without deleting reserved metadata,
database, intent, or signing-key files.

Purpose: Close REL-01 and code-review finding R1, where metadata is cleared
but blob files under namespace directories survive as orphans.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-silent-data-loss-remediation/28-CONTEXT.md
@.planning/phases/28-silent-data-loss-remediation/28-RESEARCH.md
@docs/CODE_REVIEW_ACTIONS.md
@docs/CODE_REVIEW_FINDINGS.md
@.planning/codebase/TESTING.md

<interfaces>
Current issue:
- `BlobStore._clear_blob_files()` globs only `cache_dir/*.{ext}`.
- Blobs live under namespace directories such as `cache_dir/default/`.
- `UnifiedCache.clear_all_namespaces()` calls `_clear_blob_files()` directly for
  the default namespace, then removes non-default namespace directories.

Reserved files and dirs from TASK-1:
- `*.db`, `*.db-wal`, `*.db-shm`
- `cache_metadata.json*`
- anything under `.intents/`
- `.cache_signing_key*`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the root-only cleanup bug</name>
  <files>src/cacheness/storage/blob_store.py, tests/test_core.py</files>
  <read_first>
    - docs/CODE_REVIEW_ACTIONS.md TASK-1
    - src/cacheness/storage/blob_store.py::_clear_blob_files
    - src/cacheness/core.py::clear_all
    - src/cacheness/core.py::clear_all_namespaces
  </read_first>
  <action>
Run the TASK-1 verify-first repro exactly, using `uv run python -c` as shown
in `docs/CODE_REVIEW_ACTIONS.md`. Confirm it prints leftover `*.pkl*` blobs.

If the repro does not show the documented leftover behavior, stop and report
the mismatch instead of implementing a speculative fix.
  </action>
  <verify>
    <automated>uv run python -c "from cacheness.core import UnifiedCache; from cacheness.config import CacheConfig; import tempfile, pathlib; d = tempfile.mkdtemp(); c = UnifiedCache(CacheConfig(cache_dir=d)); c.put({'a': 1}, key1='x'); c.clear_all(); leftover = [p for p in pathlib.Path(d).rglob('*.pkl*')]; print('LEFTOVER BLOBS:', leftover)"</automated>
  </verify>
  <acceptance_criteria>
    - Verify-first command was run before code changes.
    - Observed behavior matches TASK-1: leftover blob files exist before the fix.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace root-only cleanup with reserved-aware recursive cleanup</name>
  <files>src/cacheness/storage/blob_store.py, src/cacheness/core.py</files>
  <read_first>
    - src/cacheness/storage/blob_store.py::_clear_blob_files
    - src/cacheness/storage/backends/blob_backends.py::FilesystemBlobBackend.list_blobs
    - src/cacheness/core.py::clear_all_namespaces
  </read_first>
  <action>
Implement TASK-1 using the lowest-risk strategy after reading the backend APIs:

1. Prefer backend enumeration only if it can enumerate every namespace directory
   relevant to the call.
2. Otherwise use `self.cache_dir.rglob("*")` and delete only files that are not
   reserved.
3. Preserve the method's no-lock contract.
4. Do not remove `.intents/`, metadata JSON/DB files, WAL/SHM files, or signing
   key files.
5. Confirm `clear_all_namespaces()` still clears default namespace blob files
   and removes non-default namespace directories as intended.
  </action>
  <verify>
    <automated>uv run pytest tests/test_blob_store.py tests/test_core.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - `clear_all()` removes blobs under `cache_dir/default/` and any shard dirs.
    - `clear_all_namespaces()` clears namespace blob directories too.
    - Reserved metadata/security/intent files remain untouched.
    - `_clear_blob_files()` still does not acquire a lock.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for blob cleanup guarantees</name>
  <files>tests/test_core.py, tests/test_blob_store.py, tests/test_storage_mode.py</files>
  <read_first>
    - tests/test_core.py existing clear/cleanup tests
    - tests/test_blob_store.py cleanup-related tests
    - tests/test_storage_mode.py storage-mode invariants
  </read_first>
  <action>
Add regression coverage matching TASK-1 acceptance:

1. Put at least three entries, including default namespace and a custom
   namespace.
2. Call `clear_all()`.
3. Assert no blob files remain under the cache directory via `rglob`.
4. Assert reserved metadata DB/JSON and signing key files remain when present.
5. Add or adjust a `clear_all_namespaces()` test to assert namespace blob dirs
   are cleaned as well.
  </action>
  <verify>
    <automated>uv run pytest tests/test_blob_store.py tests/test_core.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail against the old root-only implementation.
    - Tests pass after the cleanup fix.
    - Storage-mode Tier-1 tests pass with no durable-entry cleanup regression.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Verify-first repro from TASK-1 demonstrates the pre-fix bug.
2. `uv run pytest tests/test_blob_store.py tests/test_core.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py`
3. Quality gates on changed Python files:
   `uv run ruff format <files>; uv run ruff check --fix <files>; uv run ruff check <files>; uv run ty check <files>`
</verification>

<success_criteria>
- REL-01 is satisfied for both `clear_all()` and `clear_all_namespaces()`.
- No reserved metadata, database, intent, or key files are removed.
- TASK-1 remains one beads issue and one atomic commit.
</success_criteria>

<output>
After completion, create `.planning/phases/28-silent-data-loss-remediation/28-01-SUMMARY.md`.
</output>

