---
phase: 28-silent-data-loss-remediation
plan: 03
type: execute
wave: 1
depends_on: ["28-02"]
files_modified:
  - src/cacheness/core.py
  - src/cacheness/_storage_mode_mixin.py
  - tests/test_write_intent.py
  - tests/test_atomic_writes.py
  - tests/test_storage_mode.py
autonomous: true
requirements: [REL-02, REL-03, REL-04]
requirements_addressed: [REL-02, REL-03, REL-04]
must_haves:
  truths:
    - "D-05: SEED-006 is a companion follow-up slice tied to TASK-2, not a silent expansion of TASK-2 acceptance."
    - "D-11/D-12: Include SEED-006 in Phase 28 after TASK-2 lands cleanly."
    - "D-13: Storage-mode changes must not delete or expire committed durable entries."
    - "D-26/D-27/D-28: Verify before implementation and include storage-mode tests in Tier-1."
    - "D-33: Use an atomic commit message for this separate seed slice; add docs only if user-visible behavior beyond task docs appears."
  artifacts:
    - path: "src/cacheness/core.py"
      provides: "Intent recorded before non-inline blob write in cache mode"
      contains: "record_intent"
    - path: "src/cacheness/_storage_mode_mixin.py"
      provides: "Intent recorded before non-inline blob write in storage mode"
      contains: "record_intent"
    - path: "tests/test_write_intent.py"
      provides: "Regression for intent existing before blob write"
---

<objective>
Extend write-intent coverage so non-inline cache-mode and storage-mode writes
record the planned intent before blob I/O begins.

Purpose: Close the SEED-006/R8 coverage gap while keeping it separate from the
TASK-2 path-resolution and committed-entry guard changes.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-silent-data-loss-remediation/28-CONTEXT.md
@.planning/phases/28-silent-data-loss-remediation/28-RESEARCH.md
@.planning/seeds/SEED-006-record-write-intent-before-blob-write.md
@docs/CODE_REVIEW_FINDINGS.md
@docs/CODE_REVIEW_ACTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
Current issue:
- `UnifiedCache.put()` records intent after `_blob_store._write_blob(...)`.
- `_storage_mode_put()` records intent after `_blob_store._write_blob(...)`.
- After plan 28-02, cleanup tolerates missing blob paths and preserves
  committed entries, which makes pre-blob-write intents safe.

Constraint:
- Inline writes should still skip intents because no filesystem blob exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify current after-write intent timing</name>
  <files>src/cacheness/core.py, src/cacheness/_storage_mode_mixin.py, tests/test_write_intent.py</files>
  <read_first>
    - .planning/seeds/SEED-006-record-write-intent-before-blob-write.md
    - src/cacheness/core.py::put
    - src/cacheness/_storage_mode_mixin.py::_storage_mode_put
    - tests/test_write_intent.py
  </read_first>
  <action>
Add or run a focused verify-first check that monkeypatches `_blob_store._write_blob`
to raise before returning and confirms the old implementation leaves no intent.
Repeat for `storage_mode=True`.

If the old implementation already records an intent before the monkeypatched
blob write raises, stop and report because SEED-006 is stale.
  </action>
  <verify>
    <automated>uv run pytest tests/test_write_intent.py tests/test_atomic_writes.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - Verify-first proves current intent timing is after blob write for non-inline writes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Record planned-path intent before blob write in cache mode</name>
  <files>src/cacheness/core.py, tests/test_write_intent.py</files>
  <read_first>
    - src/cacheness/core.py::_get_cache_file_path
    - src/cacheness/core.py::put non-inline path
    - src/cacheness/storage/paths.py
    - src/cacheness/write_intent.py
  </read_first>
  <action>
In `UnifiedCache.put()` non-inline path:

1. Determine the planned blob path before calling `_write_blob()`. Use the same
   base path that `_write_blob()` receives and store the cache-dir-relative form
   expected by the metadata path.
2. Call `self._write_journal.record_intent(cache_key, planned_path)` before
   `_blob_store._write_blob(...)`.
3. After `_write_blob()` returns, keep metadata construction based on
   `result.actual_path`.
4. Ensure exception handling still clears the intent and rolls back any blob
   that was actually created.
5. Do not record intents for direct inline writes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_write_intent.py tests/test_atomic_writes.py tests/test_core.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - A crash/exception during blob write leaves an intent file in cache mode.
    - Stale cleanup handles an intent whose blob was never created.
    - Successful puts still leave no residual intents.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mirror before-write intent timing in storage mode</name>
  <files>src/cacheness/_storage_mode_mixin.py, tests/test_storage_mode.py, tests/test_write_intent.py</files>
  <read_first>
    - src/cacheness/_storage_mode_mixin.py::_storage_mode_put
    - tests/test_storage_mode.py
    - tests/test_write_intent.py
  </read_first>
  <action>
Apply the same planned-path before-write intent logic to `_storage_mode_put()`.
Keep all storage-mode invariants:

1. No TTL, eviction, stats, or automatic deletion of committed entries.
2. Rollback clears the intent but does not delete committed prior entries.
3. Cleanup of stale intents removes only uncommitted orphans.
  </action>
  <verify>
    <automated>uv run pytest tests/test_write_intent.py tests/test_atomic_writes.py tests/test_core.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - Storage-mode blob-write failure leaves a recoverable intent.
    - Storage-mode successful put leaves no residual intent.
    - Existing storage-mode durability tests remain green.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Verify-first timing regression for cache mode and storage mode.
2. `uv run pytest tests/test_write_intent.py tests/test_atomic_writes.py tests/test_core.py tests/test_storage_mode.py -x -q --ignore=tests/test_tensorflow_handler.py`
3. Quality gates on changed Python files.
</verification>

<success_criteria>
- Write intents cover the pre-blob-write crash window for cache mode.
- Write intents cover the pre-blob-write crash window for storage mode.
- Inline writes still skip intent files.
- This seed remains a separate follow-up commit after TASK-2.
</success_criteria>

<output>
After completion, create `.planning/phases/28-silent-data-loss-remediation/28-03-SUMMARY.md`.
</output>

