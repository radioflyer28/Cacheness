---
phase: 28-silent-data-loss-remediation
plan: 04
type: execute
wave: 1
depends_on: ["28-03"]
files_modified:
  - src/cacheness/metadata/json_backend.py
  - tests/test_metadata.py
  - tests/test_json_schema_versioning.py
autonomous: true
requirements: [REL-05, REL-06]
requirements_addressed: [REL-05, REL-06]
must_haves:
  truths:
    - "D-14: Follow TASK-3 exactly, using R3/R4 as rationale."
    - "D-15: Data-critical put_entry() and remove_entry() raise on persistence failure."
    - "D-16: Stats and access-time writes remain best-effort."
    - "D-17: Corrupt JSON metadata is preserved as *.corrupt-<timestamp> and logged at error level before starting empty."
    - "D-18: Missing parent directories get mkdir(parents=True, exist_ok=True) retry before data-critical failure bubbles."
    - "D-19: Do not add broader backend health or dirty-state behavior unless already naturally present."
    - "D-26/D-27: Run TASK-3 verify-first before implementation."
    - "D-32: If old swallow-behavior tests are updated, mention the new persistence contract in the commit message."
  artifacts:
    - path: "src/cacheness/metadata/json_backend.py"
      provides: "JSON data-critical save failure propagation and corrupt-file preservation"
      contains: "_save_to_disk"
    - path: "tests/test_metadata.py"
      provides: "Persistence failure behavior regression"
    - path: "tests/test_json_schema_versioning.py"
      provides: "Corrupt metadata preservation regression"
---

<objective>
Make JSON metadata persistence honest: data-critical writes surface save
failures, best-effort telemetry stays best-effort, missing parent directories
are retried, and corrupt metadata files are preserved before starting empty.

Purpose: Close REL-05, REL-06 and code-review findings R3/R4.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-silent-data-loss-remediation/28-CONTEXT.md
@.planning/phases/28-silent-data-loss-remediation/28-RESEARCH.md
@docs/CODE_REVIEW_ACTIONS.md
@docs/CODE_REVIEW_FINDINGS.md
@.planning/codebase/TESTING.md

<interfaces>
Current issue:
- `JsonBackend._save_to_disk()` catches all exceptions and returns.
- `put_entry()` and `remove_entry()` call `_save_to_disk()` and report success.
- Stats-only methods call `_save_to_disk()` too, but may remain best-effort.
- `_load_from_disk()` logs warning and starts fresh on corrupt JSON without
  preserving the corrupt file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify JSON save-failure and corrupt-file behavior</name>
  <files>tests/test_metadata.py, tests/test_json_schema_versioning.py</files>
  <read_first>
    - docs/CODE_REVIEW_ACTIONS.md TASK-3
    - src/cacheness/metadata/json_backend.py
    - tests/test_metadata.py
    - tests/test_json_schema_versioning.py
    - tests/test_fault_injection.py
  </read_first>
  <action>
Add or run verify-first checks before implementation:

1. Patch `shutil.move` or `os.replace`/filesystem write path to raise
   `OSError`; confirm old `put_entry()` does not raise.
2. Confirm stats-only `update_access_time()` does not raise under the same
   failure.
3. Write invalid JSON to the metadata file and initialize `JsonBackend`; confirm
   old behavior starts empty without a `*.corrupt-*` backup.

If current behavior differs from TASK-3, stop and report.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metadata.py tests/test_json_schema_versioning.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - Verify-first behavior matches R3/R4 before code changes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add raise_on_error persistence contract</name>
  <files>src/cacheness/metadata/json_backend.py, tests/test_metadata.py</files>
  <read_first>
    - src/cacheness/metadata/json_backend.py::_save_to_disk
    - src/cacheness/metadata/json_backend.py::put_entry
    - src/cacheness/metadata/json_backend.py::remove_entry
    - src/cacheness/metadata/json_backend.py::update_access_time
    - src/cacheness/metadata/json_backend.py::increment_hits
    - src/cacheness/metadata/json_backend.py::increment_misses
  </read_first>
  <action>
Refactor `_save_to_disk()`:

1. Add `raise_on_error: bool = False`.
2. On missing parent dir, attempt `self.metadata_file.parent.mkdir(parents=True, exist_ok=True)` once before writing.
3. Keep atomic temp-file write and cleanup behavior.
4. On exception, log the error. If `raise_on_error` is true, re-raise.
5. Call `_save_to_disk(raise_on_error=True)` from `put_entry()` and
   `remove_entry()`.
6. Leave `update_access_time()`, `increment_hits()`, and `increment_misses()`
   best-effort with default `False`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metadata.py tests/test_json_schema_versioning.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - `put_entry()` raises when metadata cannot be persisted.
    - `remove_entry()` raises when metadata cannot be persisted.
    - Stats/access-time failures are logged but do not raise.
    - Missing parent directories are recreated and retried before failure bubbles.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve corrupt JSON metadata files</name>
  <files>src/cacheness/metadata/json_backend.py, tests/test_json_schema_versioning.py</files>
  <read_first>
    - src/cacheness/metadata/json_backend.py::_load_from_disk
    - tests/test_json_schema_versioning.py
  </read_first>
  <action>
In `_load_from_disk()`, when JSON parsing or schema validation fails:

1. Rename the metadata file to `f"{path}.corrupt-{int(time.time())}"` using
   `Path.replace`.
2. Wrap the rename in `try/except OSError` so load does not crash if the backup
   cannot be created.
3. Log at `logger.error` including the backup path when backup succeeds.
4. Start empty after preserving the corrupt file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metadata.py tests/test_json_schema_versioning.py -x -q --ignore=tests/test_tensorflow_handler.py</automated>
  </verify>
  <acceptance_criteria>
    - Invalid JSON produces a `*.corrupt-*` file.
    - Backend starts empty after preserving the corrupt file.
    - Error-level logging includes the backup path.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Verify-first checks for old swallow and corrupt-discard behavior.
2. `uv run pytest tests/test_metadata.py tests/test_json_schema_versioning.py -x -q --ignore=tests/test_tensorflow_handler.py`
3. Quality gates on changed Python files.
</verification>

<success_criteria>
- REL-05: JSON data-critical save failures surface to callers.
- REL-06: corrupt JSON files are preserved as timestamped backups.
- Best-effort stats/access-time writes remain non-throwing.
- No broad backend health/dirty-state feature is added in this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/28-silent-data-loss-remediation/28-04-SUMMARY.md`.
</output>

