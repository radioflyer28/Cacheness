---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "10"
status: superseded
superseded_by: "Phase 8 BACK-05 real-service qualification plan (to be created)"
superseded_reason: "User approved moving the non-substitutable PostgreSQL/Amazon-S3 release gate to Phase 8; Phase 5 retains the harness and UNAVAILABLE evidence without claiming qualification."
type: execute
wave: 7
depends_on: [05-09]
files_modified:
  - .planning/phases/05-payload-backends-and-supported-topology-qualification/05-LIVE-QUALIFICATION.json
  - .planning/phases/05-payload-backends-and-supported-topology-qualification/05-VALIDATION.md
autonomous: true
requirements: [BACK-05]
user_setup:
  - service: PostgreSQL
    why: "Real-service authority qualification"
    env_vars:
      - name: CACHENESS_TEST_POSTGRES_DSN
        source: "Connection URL for an externally supplied test-capable real PostgreSQL service"
  - service: Amazon S3
    why: "Real AWS immutable-generation qualification"
    env_vars:
      - name: CACHENESS_TEST_S3_BUCKET
        source: "Existing AWS bucket where credentials have access to one generated test prefix"
      - name: CACHENESS_TEST_MANIFEST_KEY_B64
        source: "Externally generated shared 32-byte manifest key encoded as base64"
      - name: AWS_REGION
        source: "AWS region for the test bucket; standard boto3 credential provider supplies credentials"
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A fresh exact command passes real PostgreSQL, real AWS S3, and combined independent-client suites with zero skips and exact-run cleanup (BACK-05; D-05-D-08)."
    - "Sanitized evidence status is QUALIFIED and exit code is zero; UNAVAILABLE/NOT_QUALIFIED remains an open phase gate."
    - "Runtime profiles and docs remain immutable requirement declarations; the sanitized evidence file is the exclusive source for observed status, tested revision, service scope, and cleanup result (D-08, D-18-D-22)."
  artifacts:
    - path: .planning/phases/05-payload-backends-and-supported-topology-qualification/05-LIVE-QUALIFICATION.json
      provides: "Sanitized real-service qualification evidence"
      contains: "QUALIFIED"
    - path: .planning/phases/05-payload-backends-and-supported-topology-qualification/05-VALIDATION.md
      provides: "Final task/test map and live gate sign-off"
  key_links:
    - from: tools/run_phase5_qualification.py
      to: .planning/phases/05-payload-backends-and-supported-topology-qualification/05-LIVE-QUALIFICATION.json
      via: "Fixed runner writes schema-valid sanitized evidence and exit status"
      pattern: "QUALIFIED|UNAVAILABLE|NOT_QUALIFIED"
  prohibitions:
    - "No manual approval, mock/emulator/compatible endpoint, ordinary skip, or partial service pass can close the gate."
    - "No credentials, DSN, key material, payload data, or broad inventory enters committed evidence."
    - "No universal multi-host scheduling/availability, cross-resource ACID, or performance-deadline claim."
---

<objective>
Close the non-substitutable real PostgreSQL/Amazon-S3 qualification gate without creating a mutable runtime or documentation status mirror.

Purpose: Turn the remote candidate into a supported reference profile only after genuine service and cleanup evidence exists.
Output: Sanitized QUALIFIED evidence plus a validation-map reference to that single observed-status artifact.
</objective>

## Artifacts this phase produces

- One committed sanitized real-service evidence record for the exact repository revision.
- Final Phase 5 validation map referencing the evidence schema, tested implementation revision, command, and artifact digest without copying mutable service status into runtime/docs.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-COVERAGE.md
@tools/run_phase5_qualification.py
@docs/CATALOG_AND_TOPOLOGY.md
@docs/STORAGE_INITIALIZATION.md
</context>

<source_coverage>GOAL: final supported remote topology evidence. REQ: BACK-05. RESEARCH: real-service hard gate and sanitized evidence. CONTEXT: D-03, D-05 through D-08, D-18 through D-22.</source_coverage>

<tasks>

<task type="tracer">
  <name>Task 1: Qualify two independent BlobStore clients over real PostgreSQL and Amazon S3</name>
  <precondition>`CACHENESS_TEST_POSTGRES_DSN`, `CACHENESS_TEST_S3_BUCKET`, `CACHENESS_TEST_MANIFEST_KEY_B64`, AWS region, and a resolvable standard boto3 credential provider are present; read-only identity probes confirm real PostgreSQL and Amazon S3 without emitting values.</precondition>
  <files>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-LIVE-QUALIFICATION.json, .planning/phases/05-payload-backends-and-supported-topology-qualification/05-VALIDATION.md</files>
  <read_first>
    <file>tools/run_phase5_qualification.py</file>
    <file>tests/integration/test_postgresql_authority.py</file>
    <file>tests/integration/test_s3_generation.py</file>
    <file>tests/integration/test_remote_topology.py</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-VALIDATION.md</file>
  </read_first>
  <behavior>
    - "Before and after the live run, runtime profiles and topology/initialization docs contain the same immutable evidence prerequisites and no latest-run status or tested revision."
    - "The evidence record alone contains observed status, tested implementation revision, service versions/scope, per-guarantee results, and cleanup result."
    - "The tested revision is the clean implementation revision before evidence capture; after capture, only the evidence artifact and validation-map reference may differ without forcing a rerun."
    - "UNAVAILABLE/NOT_QUALIFIED remains nonzero and leaves Plan 10 open; no validation checkbox, docs edit, or runtime mutation can override it."
  </behavior>
  <action>Require a clean implementation/test/tool/doc tree, capture its exact git revision, then run the frozen cloud-extra qualification command against the externally supplied services. Require the fixed PostgreSQL, AWS S3, and combined remote suites to execute with zero skips/deselection and require exact-run schema/prefix cleanup to pass. Inspect only the sanitized evidence allow-list. If setup is missing, preserve/write UNAVAILABLE and stop with Plan 10 and Phase 5 open; if any test, service identity, evidence validation, provenance check, or cleanup fails, preserve NOT_QUALIFIED and stop open. Only QUALIFIED/exit-0 completes the task. Keep the runtime profile and topology/initialization docs unchanged: they declare immutable prerequisites, while `05-LIVE-QUALIFICATION.json` alone records observed status, tested revision, service scope/versions, results, and cleanup. Update `05-VALIDATION.md` only as a task/test coverage map that cites the evidence path, schema, digest, tested-revision rule, and exact command; do not copy a second mutable status field. If any production, test, runner, or topology-doc file changes after the captured revision, invalidate the record and rerun. Keep compatible services unclaimed and performance/non-ACID/typed-progress limitations unchanged.</action>
  <acceptance_criteria>The canonical runner exits zero with schema-valid QUALIFIED evidence from genuine PostgreSQL/AWS S3 and clean exact-run cleanup; that evidence is the only observed-status source and is bound to an otherwise unchanged implementation revision. Any other status is deliberately incomplete rather than a passing plan.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud python tools/run_phase5_qualification.py --output .planning/phases/05-payload-backends-and-supported-topology-qualification/05-LIVE-QUALIFICATION.json &amp;&amp; uv run --frozen --extra cloud python tools/verify_phase5_contracts.py &amp;&amp; python -c "import json; from pathlib import Path; p=Path('.planning/phases/05-payload-backends-and-supported-topology-qualification/05-LIVE-QUALIFICATION.json'); d=json.loads(p.read_text()); assert d['status']=='QUALIFIED'"</automated></verify>
  <done>BACK-05 is backed by real-service evidence and the PostgreSQL/Amazon-S3 reference topology is truthfully qualified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| External credentials/services -> final gate | Identity and results must be genuine without exposing secrets. |
| Live result -> committed support claim | Only complete sanitized QUALIFIED evidence may change the claim. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-44 | Spoofing | service identity | high | mitigate | Read-only real PostgreSQL/AWS identity checks; reject compatible endpoints/mocks. |
| T-05-45 | Information disclosure | committed evidence | high | mitigate | Exact field allow-list, secret sentinels, no values/URLs/bodies/keys. |
| T-05-46 | Tampering | support status | high | mitigate | QUALIFIED plus zero exit, zero skips, complete fixed suite, and clean cleanup required. |
| T-05-47 | Denial of service | final live run | medium | mitigate | Runner and all service work use explicit time/page/byte/action bounds. |
| T-05-48 | Repudiation | release claim | medium | mitigate | Record revision/time/runtime/SDK/service versions and per-class results. |
</threat_model>

<verification>Execute the exact command only. Inspect committed evidence for allowed keys and secret absence, confirm its tested revision predates only evidence/validation-map changes, then run the local architecture gate and assert runtime/docs still contain immutable requirements with no observed-status mirror. If status is not QUALIFIED, report the phase as open; do not edit acceptance language to pass.</verification>

<success_criteria>
- Real PostgreSQL and real AWS S3 suites pass together for one revision.
- Two independent clients prove the remote engine contract.
- Evidence is sanitized, exact, and the sole observed qualification truth; runtime/docs remain immutable requirement declarations.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-10-SUMMARY.md` when done.</output>

> **Superseded 2026-09-08:** This execution gate moved intact to Phase 8 with
> `BACK-05`. The `UNAVAILABLE` evidence remains truthful historical evidence;
> this plan was not executed successfully and must never be counted as a live
> qualification pass.
