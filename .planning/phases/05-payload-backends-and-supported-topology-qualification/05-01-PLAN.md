---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/storage/composition.py
  - tests/test_supported_topologies.py
autonomous: true
requirements: [BACK-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exactly three built-in profiles exist with immutable qualification requirements: memory/memory, SQLite/filesystem, and PostgreSQL/Amazon-S3 (BACK-04; D-01, D-19)."
    - "Every other pairing, including adjacent or participant-sharing combinations, is rejected before participant construction or I/O (D-02, D-03)."
    - "Empty, null, partial, duplicated, and order-varied topology declarations fail deterministically; supported resolution never depends on registry insertion or declaration order."
    - "JSON can appear only as a derived projection on a supported store and never changes the profile identity or becomes an authority (D-04)."
  artifacts:
    - path: src/cacheness/storage/composition.py
      provides: "Immutable qualified-profile declarations, qualification requirements, and pre-I/O support resolution"
      contains: "QualifiedTopologyProfile"
    - path: tests/test_supported_topologies.py
      provides: "Positive, adjacency, empty, ordering, and Cartesian-rejection coverage"
  key_links:
    - from: src/cacheness/storage/composition.py
      to: src/cacheness/storage/blob_store.py
      via: "StoreTopology resolves one qualified profile before BlobStore materializes lifecycle participants"
      pattern: "qualified_profile|resolve"
  prohibitions:
    - "No Cartesian support inference from registration, structural protocol checks, or capability minima."
    - "No descriptor-less topology path may be silently accepted; every such path remains explicitly rejected and flagged by tests."
    - "No registry-order dependence, warning-only downgrade, fallback authority, or second composition root."
    - "No runtime compatibility aliases, selectors, old layout adapters, or implicit migration; exact version detection remains intact."
---

<objective>
Establish the exact, order-independent support boundary before adding remote participants.

Purpose: Make constructibility and capability composition visibly distinct from topology qualification so later S3/PostgreSQL work cannot accidentally advertise a Cartesian product.
Output: One declarative three-profile catalog and exhaustive pre-I/O rejection tests.
</objective>

## Artifacts this phase produces

- `QualifiedTopologyProfile`, `TopologyQualificationRequirements`, and the immutable built-in profile catalog in `composition.py`.
- Public report helpers that distinguish registered/constructible participants from immutable qualification prerequisites without storing the latest observed run status.
- `tests/test_supported_topologies.py` as the canonical exact-support-matrix test.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-PATTERNS.md
@src/cacheness/storage/composition.py
</context>

<source_coverage>
| Source | Items covered |
|---|---|
| GOAL | Explicit supported pairings and topology-specific guarantees, without Cartesian parity. |
| REQ | BACK-04 exact supported-pairing contract. |
| RESEARCH | Exact profile table, registration/support separation, capability-drift correction, early rejection. |
| CONTEXT | D-01, D-02, D-03, D-04, D-19. |
</source_coverage>

## Multi-Source Coverage Audit

| Source | ID/item | Plan(s) | Status |
|---|---|---|---|
| GOAL | Every advertised payload works through the shared engine in explicitly supported pairings | 01-10 | COVERED |
| REQ | BACK-01 payload participants and recovery contracts | 02, 03, 06 | COVERED |
| REQ | BACK-04 exact supported pairings and rejection | 01, 06, 09 | COVERED |
| REQ | BACK-05 real PostgreSQL and AWS S3 evidence | 07, 08, 10 | COVERED |
| RESEARCH | One engine; primitive adapters; immutable S3; exact PostgreSQL CAS | 02-06 | COVERED |
| RESEARCH | Bounded I/O, listing, paging, error typing, shared signer | 03, 05, 06, 08 | COVERED |
| RESEARCH | Sanitized non-passing live evidence and capability documentation | 07, 09, 10 | COVERED |
| CONTEXT | D-01 through D-04 | 01 | COVERED |
| CONTEXT | D-05 through D-08 | 07, 08, 10 | COVERED |
| CONTEXT | D-09 through D-13 | 03, 06 | COVERED |
| CONTEXT | D-14 through D-18 | 04, 05, 06 | COVERED |
| CONTEXT | D-19 through D-22 | 01, 02, 07, 09, 10 | COVERED |

Deferred pairings, non-AWS compatible services, bucket deployment features, cache policy, migration execution, full runtime/platform CI, and final performance budgets remain excluded exactly as recorded in 05-CONTEXT.md; none is an unplanned Phase 5 item.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve one memory/memory store through an explicit qualified profile</name>
  <reversibility rating="costly">The three-profile identity and report vocabulary become the release support boundary consumed by later phases.</reversibility>
  <files>src/cacheness/storage/composition.py, tests/test_supported_topologies.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_topology_capabilities.py</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "The named memory authority plus named memory payload resolves to the memory/memory qualified profile and completes a public BlobStore put/get through AuthorityLifecycleEngine."
    - "Participant identity, coordination scope, durability/atomicity boundary, progress outcomes, service prerequisites, projection availability, and required evidence contract come from one immutable profile record."
    - "The SQLite declaration reports portable canonical query truthfully, correcting the Phase 4 capability drift without changing its authority role."
  </behavior>
  <action>Write the failing public tracer first. Add frozen `QualifiedTopologyProfile` and `TopologyQualificationRequirements` value objects plus one immutable built-in catalog containing exactly the three D-01 identities. The remote record declares the real PostgreSQL/Amazon-S3 evidence schema and service prerequisites as requirements only; it never stores pending, qualified, unavailable, latest-run, or mutable readiness state. Make `StoreTopology` derive explicit authority/payload identity from named `BackendRef` values or an injected participant's declared qualification identity, look up the pair independently of registration order, and reject a missing record before any factory is called. Expose a read-only report containing the exact D-19 contract fields and evidence requirement identifier; observed status belongs exclusively to sanitized release evidence written by Plan 10. Correct the SQLite `portable_query` declaration to match its delivered canonical catalog paging. Prove the memory profile with one real BlobStore round trip and assert its engine is `AuthorityLifecycleEngine`; do not add any lifecycle step to composition.</action>
  <acceptance_criteria>The memory/memory public tracer passes through the existing engine, one immutable catalog names exactly three reference profiles, and the report distinguishes qualification from constructibility.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_supported_topologies.py -x -o log_cli=false</automated></verify>
  <done>One supported profile works end to end and the architecture can expand without inferring additional pairings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject empty, adjacent, Cartesian, and order-dependent topology declarations before I/O</name>
  <files>src/cacheness/storage/composition.py, tests/test_supported_topologies.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>tests/test_topology_capabilities.py</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md</file>
  </read_first>
  <behavior>
    - "None, empty names, a missing authority, a missing payload, duplicated roles, and incomplete injected identities raise `CompositionValidationError` without invoking a participant factory."
    - "PostgreSQL/filesystem, SQLite/S3, memory/filesystem, SQLite/memory, and memory/S3 reject even when both participants are registered and structurally valid."
    - "Permuting registration and projection declaration order does not change the resolved profile or report."
    - "Adding JSON projection leaves the underlying profile unchanged and gives JSON no authority permissions."
  </behavior>
  <action>Complete a table-driven negative matrix that covers the specless-probe empty/null/incomplete, adjacency/shared-participant, and ordering cases as well as every built-in cross-pair. Use factory spies to prove rejection precedes construction and I/O. Require explicit qualification identity on injected participants rather than concrete-class tests. Keep JSON outside profile identity and assert its derived-only role. Preserve application registration as constructibility only; do not let registration mint a supported profile or weaken exact built-in rejection.</action>
  <acceptance_criteria>All descriptor-less and non-recorded pairings fail before I/O, and the same supported declaration produces the same profile regardless of registry/declaration order.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_supported_topologies.py tests/test_topology_capabilities.py -x -o log_cli=false</automated></verify>
  <done>The Phase 5 support boundary is exact, deterministic, and closed under all unqualified combinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Topology declaration -> participant construction | Untrusted names, options, and injected objects must be qualified before factories or I/O run. |
| Capability/profile report -> support claim | Capability strength and importability cannot spoof qualification evidence. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-01 | Spoofing | profile identity | high | mitigate | Use exact declared authority/payload identities and immutable records; test injected and named paths. |
| T-05-02 | Elevation of privilege | JSON projection | high | mitigate | Exclude projections from profile identity and retain derived-only structural authorization tests. |
| T-05-03 | Tampering | registry/declaration order | medium | mitigate | Resolve by normalized pair key and run permutation tests. |
| T-05-04 | Denial of service | invalid topology construction | medium | mitigate | Reject invalid/unsupported declarations before factory calls or participant I/O. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run the focused matrix and existing capability tests. Inspect the profile catalog count and pair keys, then confirm every rejection path records zero factory calls. Confirm the memory tracer's concrete store owns one `AuthorityLifecycleEngine`, not a wrapper coordinator.
</verification>

<success_criteria>
- Exactly three profile records exist and only memory/memory is proven by this tracer.
- Unsupported, empty, adjacent, and order-varied declarations have deterministic pre-I/O outcomes.
- Capability and registration data cannot be mistaken for service qualification.
</success_criteria>

<output>
Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-01-SUMMARY.md` when done.
</output>
