---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "02"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/contracts/test_payload_generation_io.py
  - tests/test_payload_faults.py
autonomous: true
requirements: [BACK-01, BACK-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The complete payload-participant contract is reviewed and exercised for memory and filesystem without widening the five-method lifecycle seam (BACK-01)."
    - "Memory and filesystem publication is immutable, reads use private verified snapshots, and deletion/recovery never chooses visibility independently of the authority."
    - "Integrity, recovery, progress, and performance assertions are separately labeled; performance observations impose no runtime deadline (D-20, D-21)."
  artifacts:
    - path: tests/contracts/test_payload_generation_io.py
      provides: "Reusable five-method payload participant contract"
    - path: tests/test_payload_faults.py
      provides: "Deterministic engine-boundary fault classification"
  key_links:
    - from: tests/contracts/test_payload_generation_io.py
      to: src/cacheness/storage/blob_store.py
      via: "The contract materializes participant I/O exactly as BlobStore does"
      pattern: "materialize_handler_io"
    - from: tests/test_payload_faults.py
      to: src/cacheness/storage/lifecycle.py
      via: "BoundaryHooks inject failures into the sole lifecycle engine"
      pattern: "AuthorityLifecycleEngine|BoundaryHooks"
  prohibitions:
    - "No timing race, sleep-based ordering, universal contender-success assertion, or benchmark-derived correctness deadline."
    - "No payload adapter may prepare/promote authority state or decide committed visibility."
    - "No custom payload container/header or compatibility-only reader/writer is introduced."
---

<objective>
Freeze the reusable payload and fault contracts against the already-qualified local participants.

Purpose: Give S3 an exact target and ensure backend expansion tests safety/recovery rather than restarting the race-fix loop.
Output: Parameterized generation-I/O contract and deterministic engine fault suite.
</objective>

## Artifacts this phase produces

- A reusable payload contract covering all five required generation-I/O operations.
- Named integrity/recovery/progress/performance test markers and deterministic boundary fault cases.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-PATTERNS.md
@src/cacheness/storage/backends/blob_backends.py
@src/cacheness/storage/guarded_handler_io.py
@src/cacheness/storage/lifecycle.py
@tests/_lifecycle_test_support.py
</context>

<source_coverage>
GOAL: common engine behavior at declared tiers. REQ: BACK-01 and local BACK-04 evidence. RESEARCH: complete five-method participant contract and deterministic faults. CONTEXT: D-01, D-03, D-20, D-21, D-22.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one immutable local generation through the complete participant contract</name>
  <files>tests/contracts/test_payload_generation_io.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/backends/blob_backends.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>tests/test_blob_store_read_contract.py</file>
    <file>tests/test_blob_store_integrity.py</file>
  </read_first>
  <behavior>
    - "Memory and filesystem participants structurally satisfy `PayloadGenerationIOProvider` and return an I/O object with stage, publish_generation, open_snapshot, delete_or_prove_absent, and close."
    - "A staged native handler file publishes once at an immutable locator; a second distinct write cannot replace those bytes."
    - "The read path exposes only a mode-restricted private snapshot and verifies signed manifest size/SHA-256 before handler deserialization."
    - "Delete either removes the exact generation or proves it absent and is idempotent."
  </behavior>
  <action>Create a reusable fixture/contract function and first run it against filesystem. Exercise it through a real `BlobStore` round trip as well as the primitive seam so the test proves the participant is actually selected by the engine. Parameterize memory next, explicitly adapting expectations for its process-only/no-crash-durability tier. Inspect and assert every method on the provider-returned object; do not add a broad backend mirror API. Use native handler output and current signed manifest verification. Keep listing expectations capability-tiered rather than requiring an operation absent from the five-method seam.</action>
  <acceptance_criteria>Both local payload families satisfy every applicable immutable-generation, contained-snapshot, verified-read, exact-delete, close, and tier-declaration assertion.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/contracts/test_payload_generation_io.py -x -o log_cli=false</automated></verify>
  <done>The complete BACK-01 participant contract has an executable local reference before S3 implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify lifecycle boundary faults without adding coordination mechanisms</name>
  <files>tests/test_payload_faults.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>tests/_lifecycle_test_support.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
  </read_first>
  <behavior>
    - "Failures before/after stage, prepare, publish, verification, promotion, and deletion expose the prior complete generation or one complete new generation only."
    - "Pre-promotion residue is attributable to intent; post-promotion deletion failure is cleanup debt and never reported as rollback."
    - "Progress cases accept success, exact conflict, or the profile's typed retryable outcome without weakening safety assertions."
    - "Performance cases collect observations separately and do not participate in pass/fail lifecycle deadlines."
  </behavior>
  <action>Build table-driven deterministic failures from `BoundaryHooks` and `InjectedLifecycleFault`; use exact named engine boundaries and state snapshots, never scheduling luck. Label/assert integrity, recovery, progress, and performance independently per ADR 0001 and D-20/D-21. For every case reopen the store or authority as appropriate and prove deterministic convergence plus exact cleanup attribution. Do not change production coordination to satisfy a test; a documented typed retryable timeout is a passing progress outcome when the committed generation remains safe.</action>
  <acceptance_criteria>Every named boundary has a deterministic safety/recovery assertion and no test conflates retryable progress failure with corruption or performance with correctness.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_payload_faults.py tests/contracts/test_payload_generation_io.py -x -o log_cli=false</automated></verify>
  <done>The reusable contract detects lifecycle corruption without encouraging another lock, queue, or success-forcing retry loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Handler stage -> payload participant | Native bytes and locator must remain private/contained until immutable publication. |
| Payload participant -> handler read | Bytes must be snapshotted and authenticated before deserialization. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-05 | Tampering | generation publication | high | mitigate | Contract proves exclusive immutable publication and exact digest/size verification. |
| T-05-06 | Elevation of privilege | handler deserialization | high | mitigate | Expose only contained private snapshots after manifest verification. |
| T-05-07 | Repudiation | failure classification | medium | mitigate | Named deterministic engine boundaries and attributable authority evidence. |
| T-05-08 | Denial of service | contention tests | medium | accept | Bounded typed retryable outcomes are documented valid progress; arbitrary scheduling success is not promised. |
</threat_model>

<verification>
Run both files together and confirm no test uses sleep/poll timing as a correctness oracle. Review the participant contract inventory manually to ensure all five methods and every applicable BACK-01 behavior are represented.
</verification>

<success_criteria>
- Local participants satisfy one reusable contract at their truthful tiers.
- Fault tests preserve old-or-new complete generations and attributable recovery.
- Guarantee categories stay separate.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-02-SUMMARY.md` when done.</output>
