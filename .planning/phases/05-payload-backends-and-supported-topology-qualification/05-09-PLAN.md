---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "09"
type: execute
wave: 6
depends_on: [05-08]
files_modified:
  - .planning/phases/05-payload-backends-and-supported-topology-qualification/05-COVERAGE.md
  - docs/CATALOG_AND_TOPOLOGY.md
  - docs/STORAGE_INITIALIZATION.md
  - tools/verify_phase5_contracts.py
  - tests/test_phase5_contract_verifier.py
autonomous: true
requirements: [BACK-01, BACK-04, BACK-05]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The published matrix names exactly three profiles with coordination, durability/atomicity, progress, configuration, projection, and evidence status fields (D-19)."
    - "S3 and psycopg API coverage is complete for the declared roles; every excluded capability has a reason and no excluded feature is silently implied."
    - "Local contract evidence is reproducible and cannot set the live PostgreSQL/AWS result to qualified (D-03, D-05, D-06)."
    - "Performance evidence remains a separate distribution and Phase 8 owns final budgets/matrix expansion (D-21, D-22)."
  artifacts:
    - path: .planning/phases/05-payload-backends-and-supported-topology-qualification/05-COVERAGE.md
      provides: "Reasoned boto3/psycopg capability integration and opt-out matrix"
    - path: tools/verify_phase5_contracts.py
      provides: "Exact local Phase 5 contract and architecture guardrail runner"
    - path: docs/CATALOG_AND_TOPOLOGY.md
      provides: "Exact topology matrix with truthful pending live status"
  key_links:
    - from: src/cacheness/storage/composition.py
      to: docs/CATALOG_AND_TOPOLOGY.md
      via: "Verifier compares runtime profile records with documented marker-bounded rows"
      pattern: "QualifiedTopologyProfile|PHASE5"
  prohibitions:
    - "No documented Cartesian parity, cross-resource ACID, universal success, compatible-S3 support, or live qualification without QUALIFIED evidence."
    - "No backward-compatibility matrix; retained version/migration seams are documented separately."
    - "No API capability is omitted merely because it is out of implementation scope; opt-outs are explicit and reasoned."
---

<objective>
Publish the exact capability/support contract and a reproducible local architecture gate.

Purpose: Make runtime declarations, tests, and documentation agree while preserving the non-substitutable live gate.
Output: API coverage matrix, topology/initialization docs, and local contract verifier.
</objective>

## Artifacts this phase produces

- `05-COVERAGE.md` with complete relevant boto3/psycopg integration decisions.
- Exact three-row topology and initialization documentation.
- `tools/verify_phase5_contracts.py` plus self-tests for local contract/source guardrails.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@src/cacheness/storage/composition.py
@docs/CATALOG_AND_TOPOLOGY.md
@docs/STORAGE_INITIALIZATION.md
</context>

<source_coverage>GOAL: published topology-specific guarantees. REQ: all three phase requirements. RESEARCH: exact capability matrix, API semantics, architecture scan, validation commands. CONTEXT: D-01 through D-22, with emphasis D-03/D-05/D-06/D-19-D-22.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish one exact topology/API capability source of truth</name>
  <files>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-COVERAGE.md, docs/CATALOG_AND_TOPOLOGY.md, docs/STORAGE_INITIALIZATION.md, tests/test_phase5_contract_verifier.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>docs/CATALOG_AND_TOPOLOGY.md</file>
    <file>docs/STORAGE_INITIALIZATION.md</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "Docs/runtime tests agree on the exact three pair identities and all D-19 columns."
    - "Remote row states contract-complete/live-pending until Plan 10 evidence is QUALIFIED; local rows never inherit remote guarantees."
    - "Coverage integrates relevant boto3 credential/client, conditional put/multipart, head/get/delete/list/object and multipart-list/abort capabilities."
    - "Coverage integrates relevant psycopg connect/transaction, safe SQL composition, bound values, execute/fetch/RETURNING, SQLSTATE/timeout capabilities."
    - "Coverage opts out with reasons from S3 versioning/object lock/replication/bucket management/lifecycle/eventing/presign/copy and PostgreSQL advisory locks/2PC/LISTEN-NOTIFY/async/replication/ORM/implicit migration."
  </behavior>
  <action>Create `05-COVERAGE.md` with one complete capability row per relevant boto3 and psycopg service/driver surface, defaulting to INTEGRATE and giving every OPT-OUT a one-line architectural or scope reason. Explicitly enumerate the named excluded S3/PostgreSQL features rather than omitting them. Update topology docs from the runtime profile vocabulary: exact coordination/ACID boundary, allowed progress, setup, derived JSON availability, and unsupported cross-pairs. Document PostgreSQL explicit initialize/read-only version validation/stopped-worker Phase 7 migration and Amazon S3 external shared signer/prefix requirements. Mark the remote live evidence status pending, not qualified. Add tests that parse marker-bounded doc/coverage tables and compare exact identities/capabilities with runtime records, so declaration order or prose drift fails deterministically.</action>
  <acceptance_criteria>Runtime, docs, and coverage enumerate the same three profiles; every relevant/excluded service capability is reasoned; live-pending remains visibly non-passing.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest -q tests/test_phase5_contract_verifier.py tests/test_supported_topologies.py -x -o log_cli=false</automated></verify>
  <done>The support contract is explicit, complete, and honest before final live evidence exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the exact local Phase 5 contract and architecture verifier</name>
  <files>tools/verify_phase5_contracts.py, tests/test_phase5_contract_verifier.py</files>
  <read_first>
    <file>tools/verify_phase4_cutover.py</file>
    <file>tests/test_phase4_cutover_verifier.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "One command runs the exact Phase 5 contract/topology/evidence-self-test modules and returns nonzero on any failure."
    - "AST/source checks fail on a second lifecycle engine/coordinator, adapter authority transitions, S3-derived visibility, advisory lock calls, unbounded remote listing, ETag authorization, or inline secrets."
    - "The verifier asserts all supported profile stores use `AuthorityLifecycleEngine` and distinguishes local contract green from live status."
    - "It never edits evidence, upgrades remote qualification, or folds performance thresholds into correctness."
  </behavior>
  <action>Implement a fixed owned-file/test inventory and AST-aware architecture checks; avoid broad text greps that can match comments/docstrings. Run exact contract files from Plans 01-08 plus the evidence self-tests, report guarantee classes separately, and inspect runtime profile/report values. Read the live evidence file only to report one of absent/UNAVAILABLE/NOT_QUALIFIED/QUALIFIED; local success must leave remote pending unless status is QUALIFIED and schema-valid. Add table-driven source fixtures proving each prohibited architecture form is detected and identical string/comment text is ignored. Do not modify source/evidence or scan unrelated Phase 3 historical artifacts.</action>
  <acceptance_criteria>The local verifier independently proves shared-engine/backend boundaries and exact contract coverage while refusing to convert local green tests into BACK-05 qualification.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest -q tests/test_phase5_contract_verifier.py -x -o log_cli=false &amp;&amp; uv run --frozen --extra cloud python tools/verify_phase5_contracts.py</automated></verify>
  <done>Agents and release gates have one reproducible local command that enforces the ADR without reopening historical race-fix loops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Runtime declarations -> documentation | Drift can overstate support or hide topology limits. |
| Source/test inventory -> release claim | Incomplete or text-only checks can falsely certify architecture. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-39 | Repudiation | capability/support docs | high | mitigate | Marker-bounded exact rows compared to runtime profile records. |
| T-05-40 | Spoofing | local vs live evidence | high | mitigate | Separate status field; only schema-valid QUALIFIED live evidence can close BACK-05. |
| T-05-41 | Tampering | architecture verifier | high | mitigate | AST-aware fixtures with positive/negative self-tests and fixed owned inventory. |
| T-05-42 | Information disclosure | docs/evidence report | high | mitigate | Only non-secret setup names and sanitized evidence fields are rendered. |
| T-05-43 | Denial of service | verification work | medium | mitigate | Fixed file/test inventory and bounded subprocesses; no repository-wide remote scan. |
</threat_model>

<verification>Run task gates, compare all three marker-bounded profile rows to the runtime catalog, and manually verify every required API capability and named opt-out is present once. Confirm local green output retains remote pending when live evidence is absent/unavailable.</verification>

<success_criteria>
- Support documentation and runtime declarations cannot drift silently.
- API coverage records every relevant integration and explicit opt-out.
- Local evidence remains distinct from live qualification and performance budgets.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-09-SUMMARY.md` when done.</output>
