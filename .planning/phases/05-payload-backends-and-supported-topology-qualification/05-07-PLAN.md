---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "07"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - pyproject.toml
  - tools/run_phase5_qualification.py
  - tests/qualification/conftest.py
  - tests/qualification/test_live_evidence.py
autonomous: true
requirements: [BACK-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One exact rerunnable command produces sanitized QUALIFIED, UNAVAILABLE, or NOT_QUALIFIED evidence; only QUALIFIED exits zero (D-05, D-06)."
    - "PostgreSQL schemas and S3 prefixes are unique, run-owned, bounded, and idempotently cleaned; no cleanup crosses the run namespace (D-07)."
    - "Moto, fakes, Docker-compatible endpoints, and skipped live tests cannot satisfy the AWS/PostgreSQL gate (D-05, D-08)."
    - "Evidence contains no DSN, credentials, tokens, signing keys, payloads, general bucket inventory, or secret fragments."
  artifacts:
    - path: tools/run_phase5_qualification.py
      provides: "Exact non-skippable live qualification command and sanitized evidence writer"
    - path: tests/qualification/conftest.py
      provides: "Externally configured, exact-run PostgreSQL/S3/shared-signer fixtures"
    - path: tests/qualification/test_live_evidence.py
      provides: "Non-passing absence/failure and redaction contract"
  key_links:
    - from: tools/run_phase5_qualification.py
      to: tests/integration
      via: "Runs the fixed live marker suite without accepting skips"
      pattern: "live_postgresql|live_aws_s3|live_remote"
  prohibitions:
    - "No embedded credentials, environment-value logging, auto-created cloud resources outside the exact run namespace, or broad cleanup."
    - "No passing skip, emulator substitution, compatible-endpoint inference, or manually approved evidence."
    - "No test result may claim live qualification merely because contract tests pass."
---

<objective>
Build an honest, automated live-service qualification gate before writing service tests.

Purpose: Make missing credentials visibly non-passing and constrain all destructive test cleanup to exact run-owned resources.
Output: Fixed runner, sanitized evidence schema, strict markers, and qualification fixtures.
</objective>

## Artifacts this phase produces

- `uv run --frozen --extra cloud python tools/run_phase5_qualification.py` as the canonical gate.
- `phase5-live-qualification-v1` evidence schema with three terminal statuses.
- External configuration names and exact-run PostgreSQL/S3/shared-signer fixtures.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@tests/test_phase3_release_evidence.py
@pyproject.toml
</context>

<source_coverage>GOAL: verified real-service guarantees. REQ: BACK-05 evidence gate. RESEARCH: exact runner, external fixtures, sanitized non-passing states. CONTEXT: D-05, D-06, D-07, D-08, D-20, D-22.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Emit sanitized UNAVAILABLE evidence and a non-zero result with no live configuration</name>
  <files>pyproject.toml, tools/run_phase5_qualification.py, tests/qualification/test_live_evidence.py</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>tests/test_phase3_release_evidence.py</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "With required configuration absent, the runner writes schema-valid UNAVAILABLE evidence, names missing variable names only, and exits 2."
    - "A collected live failure/skip/cleanup leak writes NOT_QUALIFIED and exits 1; only a complete passing set writes QUALIFIED and exits 0."
    - "Evidence allow-lists revision, UTC time, runtimes/SDK/server versions, AWS region/service identity, redacted run namespace, guarantee-class results, and cleanup status only."
    - "Credential-like values/fragments, URLs, payloads, signing material, and general inventories are rejected by the evidence serializer/tests."
  </behavior>
  <action>Add strict `live_postgresql`, `live_aws_s3`, and `live_remote` pytest markers. Implement the fixed runner with explicit environment-name checks for `CACHENESS_TEST_POSTGRES_DSN`, `CACHENESS_TEST_S3_BUCKET`, `CACHENESS_TEST_MANIFEST_KEY_B64`, AWS standard provider resolution, optional region/expected-owner, and an output path defaulting to the Phase 5 evidence file. Never print/read back secret values for diagnostics. Invoke only the fixed integration modules/markers, reject deselection/skip/zero-collection as non-passing, enforce a bounded subprocess timeout, and always attempt bounded fixture cleanup. Validate the evidence against an exact allow-list and scan serialized output for supplied sentinel secrets in tests. Implement all three terminal status/exit-code paths.</action>
  <acceptance_criteria>The no-credential path is deterministic UNAVAILABLE/exit-2, every incomplete/failing path is NOT_QUALIFIED/exit-1, and only all-live-pass plus clean cleanup can return QUALIFIED/exit-0.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest -q tests/qualification/test_live_evidence.py -x -o log_cli=false</automated></verify>
  <done>BACK-05 can no longer be accidentally satisfied by skipped or mocked tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create exact-run live fixtures with bounded idempotent cleanup</name>
  <files>tests/qualification/conftest.py, tests/qualification/test_live_evidence.py</files>
  <read_first>
    <file>tests/conftest.py</file>
    <file>tests/test_s3_blob_backend.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
  </read_first>
  <behavior>
    - "Each run uses cryptographically random bounded identifiers for one PostgreSQL schema and one S3 managed prefix plus explicit ownership markers."
    - "SQL identifiers use `psycopg.sql.Identifier`; S3 cleanup lists/deletes only the exact run prefix with page/object/byte/work limits."
    - "The shared manifest key is decoded into an in-memory provider and never written to disk/evidence/logs."
    - "Cleanup is idempotent, bounded, and reports residue as NOT_QUALIFIED rather than deleting outside ownership."
  </behavior>
  <action>Build fixtures that consume external configuration, create only a unique test-owned PostgreSQL schema and S3 prefix, and attach an exact ownership marker before destructive cleanup is allowed. Use the new authority's explicit initialize after schema creation and construct two independent clients with the same in-memory manifest signing provider. Implement bounded finalizers that delete only exact prefix objects/uploads and drop only the exact quoted schema after revalidating ownership. A real Docker PostgreSQL server is acceptable transaction evidence if the supplied DSN points to one, but it does not prove deployment topology and cannot replace real AWS S3. Never create/delete buckets, database instances, users, policies, or unrelated schemas.</action>
  <acceptance_criteria>Fixture setup and cleanup are exact-run, bounded, repeatable, and incapable of crossing the owned schema/prefix; shared signing material remains memory-only.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest -q tests/qualification/test_live_evidence.py -x -o log_cli=false</automated></verify>
  <done>The live suites can safely run against external services without broad destructive authority or secret leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Environment/credential providers -> fixtures | Secrets must remain external and unobservable in evidence/logging. |
| Qualification cleanup -> shared services | Destructive operations require exact run ownership and bounds. |
| Pytest result -> release evidence | Skips/mocks/partial passes must not spoof qualification. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-29 | Information disclosure | evidence/logging | high | mitigate | Allow-list fields and sentinel-secret redaction tests. |
| T-05-30 | Tampering | live cleanup scope | high | mitigate | Exact ownership markers, quoted schema, prefix containment, bounded pages/deletes. |
| T-05-31 | Spoofing | passing skip/mock | high | mitigate | Fixed module/marker set, reject skips/deselection/zero collection, status/exit contract. |
| T-05-32 | Denial of service | runner duration/work | medium | mitigate | Bounded subprocess, page, object, byte, and cleanup limits. |
| T-05-33 | Repudiation | qualification provenance | medium | mitigate | Record git revision, runtime/service versions, UTC, redacted namespace, per-class results. |
</threat_model>

<verification>Run evidence tests in an environment scrubbed of the named variables and confirm exit 2 plus sanitized UNAVAILABLE. Simulate pass/fail/skip/cleanup-leak outputs. Review cleanup code for any bucket-wide listing, unquoted schema name, or unbounded loop.</verification>

<success_criteria>
- The gate has exact terminal statuses and exit codes.
- Live setup/cleanup is constrained to run-owned resources.
- No secret or emulator can become qualification evidence.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-07-SUMMARY.md` when done.</output>
