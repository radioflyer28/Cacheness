---
phase: 05-payload-backends-and-supported-topology-qualification
plan: "06"
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-05]
files_modified:
  - src/cacheness/storage/composition.py
  - src/cacheness/storage/backends/__init__.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/storage/lifecycle.py
  - tests/contracts/test_topology_lifecycle.py
  - tests/test_supported_topologies.py
autonomous: true
requirements: [BACK-01, BACK-04]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All three exact profiles construct through one RoleRegistry/StoreTopology and every lifecycle operation is sequenced by the same AuthorityLifecycleEngine (BACK-01, BACK-04)."
    - "Remote immutable object creation precedes PostgreSQL promotion; cleanup follows promotion and remains durable debt on failure (D-13, D-18)."
    - "S3 inventory is bounded evidence consumed by reconciliation but never visibility, membership, revocation, or deletion authority (D-09, D-12)."
    - "PostgreSQL/S3 reports multi-host scope and typed progress outcomes without cross-resource ACID or every-contender-success claims (D-15, D-16, D-19)."
  artifacts:
    - path: src/cacheness/storage/composition.py
      provides: "Built-in S3/PostgreSQL role registration and exact remote profile resolution"
    - path: tests/contracts/test_topology_lifecycle.py
      provides: "Common lifecycle/fault contract for all exact supported profiles"
  key_links:
    - from: src/cacheness/storage/composition.py
      to: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      via: "The authority role constructs the narrow PostgreSQL authority"
      pattern: "PostgresqlLifecycleAuthority"
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/backends/s3_backend.py
      via: "The existing engine invokes payload primitives and consumes bounded evidence pages"
      pattern: "inventory_page|publish_generation"
  prohibitions:
    - "No remote lifecycle coordinator, queue, global/advisory locking protocol, readiness registry, filesystem authority, or second canonical record."
    - "No list-derived publication/read/delete decision and no ACID claim across PostgreSQL and S3."
    - "No exact-class support check; explicit participant/profile identity governs qualification."
---

<objective>
Compose all exact profiles through the single lifecycle engine and prove topology-tiered behavior.

Purpose: Convert independently tested S3/PostgreSQL primitives into one recoverable remote candidate without creating another lifecycle layer.
Output: Built-in role wiring, bounded reconciliation evidence consumption, and common profile contract tests.
</objective>

## Artifacts this phase produces

- Conditional built-in exports/registrations for `S3BlobBackend` and `PostgresqlLifecycleAuthority`.
- One remote `StoreTopology` profile with truthful multi-host/transaction/progress declarations.
- A common lifecycle contract proving all exact profiles instantiate `AuthorityLifecycleEngine`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-RESEARCH.md
@src/cacheness/storage/composition.py
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/lifecycle.py
</context>

<source_coverage>GOAL: every advertised backend through shared engine in exact pairings. REQ: BACK-01/BACK-04. RESEARCH: primitive composition, exact support profiles, bounded evidence. CONTEXT: D-01 through D-04 and D-09 through D-20.</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Construct the PostgreSQL/Amazon-S3 candidate through the same BlobStore engine</name>
  <reversibility rating="costly">The built-in remote participant names and capability report become application/release-facing composition contracts.</reversibility>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/backends/__init__.py, src/cacheness/storage/__init__.py, tests/test_supported_topologies.py</files>
  <read_first>
    <file>src/cacheness/storage/composition.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/__init__.py</file>
  </read_first>
  <behavior>
    - "The built-in payload name `s3` constructs only the Amazon-S3 generation participant when boto3 is available; the authority name `postgresql` constructs the lifecycle authority when psycopg is available."
    - "Missing optional packages raise actionable typed/import errors without breaking base package import."
    - "Resolved remote capabilities are durable, multi-host, authority-transactional, exact-CAS, immutable, streaming/listing, canonical-scan/query, with declared typed progress outcomes."
    - "Constructed BlobStore contains `AuthorityLifecycleEngine` directly and the selected participant identities match the profile record."
  </behavior>
  <action>Add guarded built-in factories and exports for the completed participants using the existing single RoleRegistry. Keep registration/constructibility independent of the profile/evidence record. Declare exact participant identities/capabilities on instances and registrations; do not use concrete type checks. Add the PostgreSQL progress-outcome branch and preserve local branches. Build the remote StoreTopology with explicit external shared manifest key provider and explicit initialize requirement; do not silently create/migrate the schema or derive a local signing key. Mark report evidence as contract-qualified but live-service-pending until Plan 10. Prove base import without optional packages and exact participant identity/ownership unwind.</action>
  <acceptance_criteria>The remote candidate constructs via the same public composition root and owns exactly one `AuthorityLifecycleEngine`; missing dependencies fail cleanly and no unqualified pair becomes constructible as a supported topology.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest -q tests/test_supported_topologies.py tests/test_blob_store_composition.py -x -o log_cli=false</automated></verify>
  <done>All advertised participant families are wired to their exact role without support or lifecycle duplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove common lifecycle recovery and bounded S3 evidence consumption across all profiles</name>
  <files>src/cacheness/storage/lifecycle.py, tests/contracts/test_topology_lifecycle.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
    <file>tests/test_blob_store_reconciliation.py</file>
    <file>tests/test_payload_faults.py</file>
  </read_first>
  <behavior>
    - "Each profile put/get/overwrite/delete/clear/reconcile path uses `AuthorityLifecycleEngine` and observes old-or-new complete generations only."
    - "Remote interruption after object creation/before promotion leaves durable intent; interruption after promotion/before delete leaves durable cleanup debt."
    - "Reconciliation requests bounded inventory pages only to correlate exact current/pending/debt locators and never deletes an object solely because a listing labels it orphaned."
    - "Unknown inventory objects are reported/resumable evidence; deletion requires authority attribution or explicit run-owned qualification cleanup."
  </behavior>
  <action>Extend the existing engine's reconciliation path only as necessary to consume the participant's optional bounded inventory primitive. Keep the engine as sole ordering owner: page evidence under lifecycle limits, correlate exact locators against authenticated current entries, pending mutations, and cleanup debt, and carry a continuation token in existing reconciliation evidence. Report unowned objects without deleting them; exact authority debt/intent remains the only automatic cleanup authorization. Build the common contract using real memory/filesystem participants and deterministic S3/PostgreSQL test boundaries. Assert engine class identity for every profile and classify safety, recovery, progress, and performance separately.</action>
  <acceptance_criteria>All three profiles execute the same lifecycle sequence, remote faults converge through PostgreSQL intent/debt, and S3 listing remains bounded non-authoritative evidence.</acceptance_criteria>
  <verify><automated>uv run --frozen --extra cloud pytest -q tests/contracts/test_topology_lifecycle.py tests/test_payload_faults.py -x -o log_cli=false</automated></verify>
  <done>The remote candidate differs only in primitives and declared outcomes, not in lifecycle ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Registry/profile -> remote participants | Optional dependencies and identities must not spoof qualification. |
| S3 inventory -> engine reconciliation | Listing is untrusted evidence and cannot authorize canonical changes. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-24 | Elevation of privilege | payload/authority roles | high | mitigate | Structural protocols plus explicit profile identities; test no cross-role methods. |
| T-05-25 | Tampering | inventory-based cleanup | high | mitigate | Require exact authority attribution before deletion; unknown objects are report-only. |
| T-05-26 | Repudiation | remote support status | high | mitigate | Report contract/live evidence states separately and keep live status pending. |
| T-05-27 | Denial of service | reconciliation inventory | high | mitigate | Enforce page/object/byte/work limits and resumable continuation. |
| T-05-28 | Spoofing | shared signer | high | mitigate | Require explicit external key provider; prohibit per-host local default. |
</threat_model>

<verification>Run the combined contract with cloud extras. Assert every supported profile's store `_engine` is exactly `AuthorityLifecycleEngine`, scan adapters for engine/authority sequencing calls, and verify unknown S3 inventory never triggers delete.</verification>

<success_criteria>
- Three exact profiles share one lifecycle owner.
- Remote recovery is intent/debt based, not cross-resource ACID.
- S3 inventory is bounded and non-authoritative.
</success_criteria>

<output>Create `.planning/phases/05-payload-backends-and-supported-topology-qualification/05-06-SUMMARY.md` when done.</output>
