---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "12"
type: execute
wave: 9
depends_on: [07-11]
files_modified:
  - src/cacheness/storage/migration_authority.py
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/migration.py
  - tests/test_migration_cutover.py
autonomous: true
requirements: [MIGR-04, MIGR-05]
gap_closure: true
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A supported same-backend migration checkpoints an authenticated bounded receipt after each external candidate batch, including stores larger than 256 entries (MIGR-04; D-07, D-12, D-19)."
    - "The complete candidate digest, entry count, and byte count are accumulated deterministically across ordered batch receipts without making evidence or candidate presence authoritative (MIGR-04, MIGR-05; D-17, D-20)."
    - "A STAGING run can resume or abort using only exact recorded run-owned candidates; already absent candidates are idempotent success and remaining deletion failures persist as retryable cleanup debt (MIGR-05; D-16, D-21)."
  artifacts:
    - path: src/cacheness/storage/migration_authority.py
      provides: "Bounded candidate-batch receipt and incremental complete-candidate aggregation contracts"
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Authenticated persisted candidate-batch progress and exact cleanup-debt continuation"
    - path: src/cacheness/storage/migration.py
      provides: "Batch-checkpointed stage, STAGING resume, and resumable abort orchestration"
    - path: tests/test_migration_cutover.py
      provides: "More-than-256-entry, interruption, already-absent, and partial-deletion recovery tests"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "stage checkpoints each completed external batch before advancing; resume authenticates and revalidates recorded outputs"
      pattern: "candidate_batches|STAGING"
    - from: src/cacheness/storage/migration_evidence.py
      to: src/cacheness/storage/migration_authority.py
      via: "each evidence batch round-trips the exact bounded candidate-batch receipt schema"
      pattern: "CandidateBatchReceipt"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_authority.py
      via: "ordered batch aggregates produce the one complete VerifiedCandidateReceipt submitted to the sole authority"
      pattern: "VerifiedCandidateReceipt|candidate_digest"
---

<objective>
Close the migration-stage attribution and abort-recovery blocker without changing the single-authority cutover model.

Purpose: Make every staged external effect durably attributable and resumable before whole-store activation, including catalogs larger than the former receipt cap.
Output: Bounded candidate-batch receipts, incremental whole-store aggregation, STAGING resume/abort, and deterministic recovery tests.
</objective>

## Artifacts this phase produces

- `CandidateBatchReceipt` and complete-candidate aggregation helpers in `migration_authority.py`.
- Authenticated candidate-batch and cleanup-debt fields in `MaintenanceRunEvidence`.
- Batch-checkpointed migration staging plus resumable STAGING abort in `OfflineMigrationService`.
- Exact tests named `test_migration_stage_checkpoints_each_bounded_candidate_batch_and_scales_past_256` and `test_resume_and_abort_staging_revalidate_exact_batches_and_persist_partial_cleanup_debt`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-05-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-08-SUMMARY.md
@src/cacheness/storage/migration_authority.py
@src/cacheness/storage/migration_evidence.py
@src/cacheness/storage/migration.py
</context>

<interfaces>
`AuthorityInventoryEntry` is the exact candidate descriptor. `VerifiedCandidateReceipt` currently binds run, plan, source/destination identity and revision, complete digest, count, and bytes. `MaintenanceRunEvidence` is the authenticated non-authoritative continuation record. `OfflineMigrationService.stage()` currently reaches STAGING before writes and STAGED only after one whole-store receipt; `abort()` currently omits STAGING.
</interfaces>

<planner_assumptions>
- MIGR-04 is resolved from CONTEXT/VERIFICATION/ADR as a stopped-worker copy-verify-switch workflow whose external candidate effects are crash-consistent through authenticated evidence; authority activation remains the only visibility point.
- MIGR-05 is resolved as exact run/evidence replay with output revalidation, resumable cleanup debt, and preservation of the source as the only valid visible copy before activation.
- Candidate batching is operational progress, not a new authority, queue, lock, sidecar, or availability promise. SQLite remains sufficient for the declared local topology.
</planner_assumptions>

## Multi-Source Coverage Audit

| Source | ID/item | Plan(s) | Status | Notes |
|---|---|---|---|---|
| GOAL | Explicit stopped-worker migration/rebuild without silent mutation or loss | 12-19 | COVERED | Six verified blockers plus final fixed verification are planned. |
| REQ | MIGR-03 non-mutating human/machine plans | 17-19 | COVERED | Shareable plan confidentiality and exact executable mapping. |
| REQ | MIGR-04 explicit resumable same-backend cutover | 12-13, 16, 18-19 | COVERED | Batch attribution, destination contracts, startup fence, exact verification. |
| REQ | MIGR-05 authenticated interruption recovery | 12-15, 17-19 | COVERED | Migration/rebuild receipts, cleanup debt, plan binding, final evidence. |
| REQ | MIGR-06 explicit confirmed rebuild | 14-16, 18-19 | COVERED | Canonical replay, exact rebuild receipts, and handler-owned transform execution. |
| RESEARCH | One authority; immutable external effects; bounded authenticated evidence | 12-19 | COVERED | No coordination or authority expansion. |
| RESEARCH | Exact directed handler transform and destination contract | 16 | COVERED | Production call path and manifest identity test. |
| CONTEXT | D-01 through D-22 | 12-19 | COVERED | Relevant actions cite decisions; Plan 18 binds every decision to exact tests. |
| CONTEXT | Deferred ideas | NONE | EXCLUDED | CONTEXT declares none. |

The spec-less probe left MIGR-03 through MIGR-06 unresolved/unclassified. The assumptions above and in Plans 13-18 resolve them only from `07-CONTEXT.md`, `07-VERIFICATION.md`, and ADR 0001; no backstop predicate is invented. `07-COVERAGE.md` remains the external-API declaration. Assumption-delta and schema-push checks are advisory and add no frontend or TypeScript ORM work.

## Deferred review warnings

WR-01 directory fsync durability and WR-02 inherited transform-override validation remain follow-up warnings. Neither is required to close the six authoritative verification blockers: this plan uses the existing authenticated evidence replacement boundary, and Plan 16 proves a concrete handler that actually implements its declared transform. The accepted Phase 3 clear/delete snapshot race remains out of scope.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Checkpoint one exact candidate batch and aggregate a store larger than 256 entries</name>
  <reversibility rating="costly">Changing persisted maintenance-evidence batch fields later requires coordinated decoder and resume compatibility work.</reversibility>
  <files>src/cacheness/storage/migration_authority.py, src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
    <file>src/cacheness/storage/migration_authority.py</file>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>tests/test_migration_cutover.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - "A 257-entry source stages in authority-declared bounded batches and persists an authenticated receipt immediately after each completed external batch."
    - "A fault after the first batch leaves STAGING evidence that names the exact completed batch and no later candidate."
    - "The final receipt digest/count/bytes equal the deterministic ordered aggregation of all batches and have no total-entry cap inherited from page size."
  </behavior>
  <action>Write `test_migration_stage_checkpoints_each_bounded_candidate_batch_and_scales_past_256` first and prove its pre-fix failure. Add an immutable `CandidateBatchReceipt` that binds run ID, plan digest, batch ordinal, exact ordered plan range, destination identity/revision, batch digest, entry count, and byte count. Persist a bounded tuple of these receipts in `MaintenanceRunEvidence`, validating sequence continuity, duplicate ranges, per-batch limits, canonical encoding, and total evidence bounds before use. Refactor `candidate_digest()` into deterministic incremental hashing over canonical ordered descriptors so the final digest has no whole-store 256-entry rejection while page/batch work remains bounded. Change `OfflineMigrationService.stage()` to write and authenticate evidence after every completed external batch, revalidate exact recorded candidates before skipping them on resume, then derive the single complete `VerifiedCandidateReceipt` only after every planned entry is represented. Preserve D-07, D-12, D-17, D-19, and D-20: evidence corroborates completed effects, and only the existing authority activation publishes the whole candidate.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_migration_cutover.py::test_migration_stage_checkpoints_each_bounded_candidate_batch_and_scales_past_256 -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named test stages and verifies 257 entries, observes at least two durable bounded batch checkpoints, resumes after a first-batch fault without rewriting or adopting unrecorded candidates, and produces exact final digest/count/bytes accepted by the unchanged authority publication boundary.</acceptance_criteria>
  <done>Every migration candidate external effect is attributable to an authenticated bounded batch before the service can proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume and abort STAGING with exact absence proof and durable cleanup debt</name>
  <files>src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_migration_cutover.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "abort accepts STAGING only when authenticated candidate-batch receipts bind the exact run and plan."
    - "A recorded candidate that is already absent counts as idempotently retired; a generation/locator mismatch is retained as debt and never deletes a current generation."
    - "After one deletion succeeds and the next fails, evidence checkpoints retired progress plus remaining exact debt; retry continues from that evidence."
  </behavior>
  <action>Write `test_resume_and_abort_staging_revalidate_exact_batches_and_persist_partial_cleanup_debt` first. Extend the legal evidence transitions and `OfflineMigrationService.abort()` for D-16 and D-21 so STAGING candidates are reconstructed solely from authenticated batch receipts and plan-bound descriptor digests, not listing or timing. Before each delete, prove the exact run-owned locator/generation; treat exact absence as completed, refuse mismatched live ownership, and checkpoint each successful/absent retirement plus the precise remaining debt before returning a typed retryable cleanup result. Make repeated abort replay-safe and ensure source entries, authority selection, activation state, and audit evidence remain unchanged. Keep deletion outside the authority transaction and do not add process coordination.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_migration_cutover.py::test_resume_and_abort_staging_revalidate_exact_batches_and_persist_partial_cleanup_debt tests/test_migration_cutover.py::test_offline_service_abort_removes_only_its_unactivated_candidate -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named interruption test proves STAGING abort, already-absent idempotence, partial-deletion checkpointing, exact remaining debt, safe retry completion, and unchanged source/authority visibility.</acceptance_criteria>
  <done>Interrupted migration staging can be resumed or cleaned deterministically without losing the valid source or forgetting external cleanup obligations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated evidence -> external candidate cleanup | Untrusted or replayed evidence must not authorize deletion of an unrelated or current generation. |
| external candidate batches -> lifecycle authority | Candidate presence and receipts corroborate work but cannot select visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-G12-01 | Tampering | Candidate batch receipts | high | mitigate | Canonical authenticated receipts bind run, plan, destination, ordered range, digest, count, and bytes; every resume revalidates exact outputs. |
| T-07-G12-02 | Spoofing | Abort ownership | high | mitigate | Delete only exact run-owned locator/generation identities derived from authenticated receipts; mismatches remain debt. |
| T-07-G12-03 | Repudiation | Partial abort | medium | mitigate | Checkpoint each retired or already-absent candidate and retain exact unresolved cleanup debt. |
| T-07-G12-04 | Denial of Service | Whole-store aggregation | medium | mitigate | Keep each page/batch bounded while streaming the complete digest and counters without a page-size-derived total cap. |
| T-07-G12-05 | Elevation of Privilege | Evidence as authority | high | mitigate | Preserve the existing authority-only activation call; batch evidence has no publication operation. |
</threat_model>

<verification>
Run the two named tests plus the complete migration cutover and evidence suites. Safety proves no wrong-generation deletion or partial publication; recovery proves exact resume/debt; progress remains bounded per batch; performance qualification stays in Phase 8.
</verification>

<success_criteria>
- More than 256 entries stage and verify through bounded authenticated batches.
- A fault after any completed batch resumes without rewriting, losing, or adopting unexplained candidates.
- STAGING abort is exact, idempotent for already-absent candidates, and durable across partial deletion failure.
- Authority activation remains the only visibility transition and no coordination mechanism is added.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-SUMMARY.md` when done.
</output>
