---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "12"
type: execute
wave: 9
depends_on: [07-11]
files_modified:
  - src/cacheness/storage/migration_authority.py
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/migration.py
  - tests/test_migration_cutover.py
autonomous: true
requirements: [MIGR-04, MIGR-05]
gap_closure: true
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A supported same-backend migration checkpoints an authenticated confidential bounded receipt after each external candidate batch; every receipt contains the exact ordinal, key, generation, locator, target manifest, payload digest, and byte size for each completed candidate, including stores larger than 256 entries (MIGR-04; D-07, D-12, D-19)."
    - "The complete candidate digest, entry count, and byte count are accumulated deterministically across an ordinal-authenticated chain of separately bounded batch journals without making evidence or candidate presence authoritative (MIGR-04, MIGR-05; D-17, D-20)."
    - "A STAGING run can resume or abort using only exact recorded run-owned candidates; already absent candidates are idempotent success and remaining deletion failures persist as retryable cleanup debt (MIGR-05; D-16, D-21)."
  artifacts:
    - path: src/cacheness/storage/migration_authority.py
      provides: "Exact per-entry candidate descriptor, bounded candidate-batch receipt, and incremental complete-candidate aggregation contracts"
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Authenticated confidential per-batch journal, bounded chain-head progress, and exact cleanup-debt continuation"
    - path: src/cacheness/storage/migration.py
      provides: "Batch-checkpointed stage, STAGING resume, and resumable abort orchestration"
    - path: tests/test_migration_cutover.py
      provides: "More-than-256-entry, interruption, already-absent, and partial-deletion recovery tests"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "stage writes one private authenticated exact-descriptor batch journal before advancing; resume addresses journals by recorded ordinal/count and revalidates their exact outputs"
      pattern: "candidate_batch_count|candidate_batch_chain_digest|STAGING"
    - from: src/cacheness/storage/migration_evidence.py
      to: src/cacheness/storage/migration_authority.py
      via: "each separately bounded journal round-trips CandidateBatchReceipt entries containing lossless AuthorityInventoryEntry material"
      pattern: "CandidateEntryReceipt|CandidateBatchReceipt"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_authority.py
      via: "ordered batch aggregates produce the one complete VerifiedCandidateReceipt submitted to the sole authority"
      pattern: "VerifiedCandidateReceipt|candidate_digest"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No ordinary constructor, open, initialize, read, cache policy, reconciliation, or cleanup path may migrate, rebuild, adopt, activate, or purge a store."
    - "[FLAGGED/UNVERIFIED] No work-directory file, filesystem path, object listing, candidate presence, or derived projection may become lifecycle or cutover authority."
    - "[FLAGGED/UNVERIFIED] No new lock, queue, lease, sidecar, daemon, scheduler, online writer protocol, or second lifecycle state machine may be added."
    - "[FLAGGED/UNVERIFIED] No historical compatibility reader, manufactured persisted version, universal native payload converter, handler stream-API cutover, exposed object-store handle, or force override may be introduced."
    - "[FLAGGED/UNVERIFIED] No partial candidate may activate, no immutable generation locator may be reused, and activation/finalize may not physically delete the prior valid store."
    - "[FLAGGED/UNVERIFIED] No unauthenticated evidence, stale source fingerprint, unexplained candidate, or incomplete catalog may be adopted."
    - "[FLAGGED/UNVERIFIED] No signing key bytes, key-provider/cloud/database credentials, or secret provider paths may appear in maintenance artifacts; sensitive target application/handler metadata may appear only in authenticated owner-private exact batch journals and never in plans, reports, main evidence, diagnostics, or logs."
    - "[FLAGGED/UNVERIFIED] No unbounded inventory/evidence, conditional-publication streaming claim, silent S3 multipart policy, or Phase 8 live PostgreSQL/AWS S3/performance qualification claim may enter Phase 7."
---

<objective>
Close the migration-stage attribution and abort-recovery blocker without changing the single-authority cutover model.

Purpose: Make every staged external effect durably attributable and resumable before whole-store activation, including catalogs larger than the former receipt cap.
Output: Confidential exact-descriptor candidate-batch journals, incremental whole-store aggregation, STAGING resume/abort, and deterministic recovery tests.
</objective>

## Artifacts this phase produces

- `CandidateEntryReceipt(entry_ordinal, key, generation, locator, target_manifest, payload_digest, byte_size)` and `CandidateBatchReceipt(run_id, plan_digest, batch_ordinal, first_entry_ordinal, past_last_entry_ordinal, destination_identity, destination_revision, predecessor_digest, entries, candidate_digest, entry_count, byte_count)` in `migration_authority.py`; each entry is a lossless `AuthorityInventoryEntry` equivalent and existing `candidate_digest()` becomes uncapped and incrementally consumes the canonical ordered entries.
- Deterministically named authenticated confidential candidate-batch journal files below the operator work directory, plus bounded `MaintenanceRunEvidence.candidate_batch_count`, `candidate_batch_chain_digest`, `candidate_retirement_cursor`, and retirement-chain head. `CandidateRetirementBatch` stores per-candidate retired/absent/conflict/debt outcomes beside the exact source batch, bounded to that batch; candidate-scale exact debt does not accumulate in one main-evidence tuple. Batch journal contents never enter `MigrationPlan`, human reports, or logs.
- Batch-checkpointed migration staging plus resumable STAGING abort in `OfflineMigrationService`.
- Exact tests named `test_migration_stage_checkpoints_each_bounded_candidate_batch_and_scales_past_256` and `test_resume_and_abort_staging_revalidate_exact_batches_and_persist_partial_cleanup_debt`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.planning/spikes/MANIFEST.md
@.codex/skills/spike-findings-cacheness/SKILL.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.codex/skills/spike-findings-cacheness/references/backend-mechanics.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-05-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-08-SUMMARY.md
@src/cacheness/storage/migration_authority.py
@src/cacheness/storage/migration_evidence.py
@src/cacheness/storage/migration.py
</context>

<interfaces>
`AuthorityInventoryEntry(key, generation, locator, manifest, payload_digest, byte_size)` is the exact candidate descriptor. `VerifiedCandidateReceipt` currently binds run, plan, source/destination identity and revision, complete digest, count, and bytes. `MaintenanceRunEvidence` is the authenticated non-authoritative continuation record; this plan adds only a bounded count and chain head there, while exact descriptor material lives in separately bounded confidential batch journals addressed by deterministic ordinal. `OfflineMigrationService.stage()` currently reaches STAGING before writes and STAGED only after one whole-store receipt; `abort()` currently omits STAGING.
</interfaces>

<planner_assumptions>
- MIGR-04 is resolved from CONTEXT/VERIFICATION/ADR as a stopped-worker copy-verify-switch workflow whose external candidate effects are crash-consistent through authenticated evidence; authority activation remains the only visibility point.
- MIGR-05 is resolved as exact run/evidence replay with output revalidation, resumable cleanup debt, and preservation of the source as the only valid visible copy before activation.
- Candidate batching is operational progress, not a new authority, queue, lock, sidecar, or availability promise. SQLite remains sufficient for the declared local topology.
- Exact target manifests can contain application or handler metadata. Persist them only in authenticated maintenance storage created with owner-only access (or the existing platform-equivalent private-file contract), one bounded file per recorded ordinal; exclude them from shareable plans, reports, diagnostics, and log rendering. Signing-key material and provider credentials remain outside every maintenance artifact per D-22.
- The completed obstore spike is implementation guidance, not authorization to replace payload backends in this gap cycle. None of the six verified blockers requires a new payload participant, so Plans 07-12 through 07-19 install no package, expose no obstore API, and make no direct-conditional-put versus multipart-copy choice. Existing `BlobStore` participant calls remain the boundary.
- The spike's PARTIAL streaming result creates no Phase 7 decision checkpoint because no active task claims bounded-memory conditional S3 publication through obstore. Payload-participant adoption and large-object throughput policy remain outside these six gaps.
- WR-01 is accepted as a non-blocking warning: a missing evidence directory entry causes fail-closed recovery while the canonical source remains visible, so Phase 7 does not claim directory durability beyond the existing evidence contract. WR-02 is closed narrowly in Plan 07-16 by rejecting a declared transform edge whose resolved handler retains the base rejecting implementation.
</planner_assumptions>

<obstore_spike_disposition>
The validated spike constrains any later payload-participant implementation to a Cacheness-owned seam beneath `BlobStore`, with deterministic never-reused generation locators, create-if-absent publication, digest-plus-size observation, exact deletion, and authority-owned intent/promotion/debt/reconciliation. It does not replace `GuardedHandlerIO`: private staging, retained descriptor identity, suffix validation, path containment, and synchronous snapshot lifetime remain mandatory. Because this gap set does not introduce obstore, path-only obstore deletion and its exact-head absence proof are not new Phase 7 mechanics; current exact-generation participant semantics remain in force.
</obstore_spike_disposition>

<flagged_assumptions>
- `[FLAGGED/UNVERIFIED A-MIGR03]` MIGR-03 permits the shareable machine plan to replace raw authenticated catalog/manifest values with exact digests, provided execution re-reads and authenticates the bound source state before mutation; Plan 07-17 implements and tests this interpretation.
- `[FLAGGED/UNVERIFIED A-MIGR04]` MIGR-04's supported set consists only of exact source-to-destination compatibility edges whose destination contract and concrete handler transformation are executable; unsupported historical layouts fail or route to confirmed rebuild rather than receiving a manufactured converter; Plan 07-16 implements and tests this interpretation.
- `[FLAGGED/UNVERIFIED A-MIGR05]` MIGR-05 maintenance evidence is authenticated continuation/attribution state, never visibility authority: it must durably bind exact batches, operation intents, receipts, and cleanup debt and fail closed when absent or inconsistent; Plans 07-12 through 07-15 implement and test this interpretation without strengthening WR-01.
- `[FLAGGED/UNVERIFIED A-MIGR06]` MIGR-06's explicit confirmed rebuild uses the existing path-based handler registry and canonical BlobStore lifecycle replay; it does not require obstore adoption, a universal cross-backend converter, or a new publication policy; Plans 07-14 through 07-16 implement and test this interpretation.
</flagged_assumptions>

## Multi-Source Coverage Audit

| Source | ID/item | Plan(s) | Status | Notes |
|---|---|---|---|---|
| GOAL | Explicit stopped-worker migration/rebuild without silent mutation or loss | 12-19 | COVERED | Six verified blockers plus final fixed verification are planned. |
| REQ | MIGR-03 non-mutating human/machine plans | 17-19 | COVERED | Shareable plan confidentiality and exact executable mapping. |
| REQ | MIGR-04 explicit resumable same-backend cutover | 12-13, 16, 18-19 | COVERED | Batch attribution, destination contracts, startup fence, exact verification. |
| REQ | MIGR-05 authenticated interruption recovery | 12-15, 17-19 | COVERED | Migration/rebuild receipts, cleanup debt, plan binding, final evidence. |
| REQ | MIGR-06 explicit confirmed rebuild | 14-16, 18-19 | COVERED | Canonical replay, exact rebuild receipts, and handler-owned transform execution. |
| RESEARCH | One authority; immutable external effects; bounded authenticated evidence | 12-19 | COVERED | No coordination or authority expansion. |
| RESEARCH | Exact directed handler transform and destination contract | 16 | COVERED | Production call path and manifest identity test. |
| CONTEXT | D-01 through D-22 | 12-19 | COVERED | Relevant actions cite decisions; Plan 18 binds every decision to exact tests. |
| CONTEXT | Deferred ideas | NONE | EXCLUDED | CONTEXT declares none. |
| VERIFICATION | CR-01 bounded migration attribution and STAGING abort | 12, 16 | COVERED | Confidential per-batch authenticated exact descriptors, uncapped aggregation, exact resumable cleanup debt, and transformed runtime-metadata recovery. |
| VERIFICATION | CR-02 durable rebuild recovery | 14-15 | COVERED | Canonical pre-write intent/replay plus persisted exact rebuild receipts and every-state resume. |
| VERIFICATION | CR-03 destination contracts and handler transform execution | 16 | COVERED | Exact two-ended edge and one guarded handler-owned transform. |
| VERIFICATION | CR-04 shareable-plan confidentiality | 17 | COVERED | Digest-only plan records with authenticated execution-time reread. |
| VERIFICATION | CR-05 post-load PostgreSQL worker fence | 13 | COVERED | Persisted authority state is checked before materialization/readiness. |
| VERIFICATION | CR-06 exact fixed-verifier test-node binding | 18-19 | COVERED | Exact selectors are validated, executed, and recorded. |
| ADR 0001 | Sole metadata authority, external-effect crash consistency, exact debt, no cross-resource ACID | 12-19 | COVERED | Plans retain authority promotion as visibility and keep payload/evidence effects outside authority transactions. |
| SPIKE | Guarded path handlers and custom MCAP-style registration survive below a private participant seam | 15-16, 18 | COVERED | Rebuild/transform plans retain current path APIs, store-local handlers, guarded stages/snapshots, and exact verifier evidence. |
| SPIKE | Deterministic immutable publication, digest+size observation, exact cleanup, no listing authority | 12, 14-16 | COVERED | Only run/authority-bound locators and receipts support recovery or deletion. |
| SPIKE | Streaming and obstore S3 conditional publication remain PARTIAL | NONE | EXCLUDED | No obstore/backend replacement is needed for the six gaps; no package or publication policy is selected. |

The spec-less probe left MIGR-03 through MIGR-06 unresolved/unclassified. The assumptions above and in Plans 13-18 resolve them only from `07-CONTEXT.md`, `07-VERIFICATION.md`, and ADR 0001; no backstop predicate is invented. `07-COVERAGE.md` remains the external-API declaration. Assumption-delta and schema-push checks are advisory and add no frontend or TypeScript ORM work.

## Review-warning adjudication

WR-01 directory fsync durability remains an explicitly accepted non-blocking warning because missing evidence fails closed without revoking the canonical source; this plan does not strengthen the evidence filesystem into a new durability authority. WR-02 is covered in Plan 07-16 by requiring a concrete handler transform implementation whenever a directed edge is declared. The accepted Phase 3 clear/delete snapshot race remains out of scope.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Checkpoint one exact candidate batch and aggregate a store larger than 256 entries</name>
  <reversibility rating="costly">Changing persisted maintenance-evidence batch fields later requires coordinated decoder and resume compatibility work.</reversibility>
  <files>src/cacheness/storage/migration_authority.py, src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
    <file>src/cacheness/storage/migration_authority.py</file>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>tests/test_migration_cutover.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md</file>
    <file>.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md</file>
  </read_first>
  <behavior>
    - "A 257-entry source stages in authority-declared bounded batches and atomically persists one authenticated owner-private journal immediately after each completed external batch; every journal contains exact lossless candidate descriptors, not only an aggregate digest."
    - "A fault after the first batch leaves STAGING evidence whose count and chain head address exactly that ordinal journal; authenticating it recovers each completed candidate's ordinal, key, generation, locator, full target manifest bytes, payload digest, and byte size without listing or re-transforming."
    - "The final receipt digest/count/bytes equal the deterministic ordered streaming aggregation of all ordinal journals and have no total-entry cap inherited from page size or one ever-growing evidence document."
  </behavior>
  <action>Write `test_migration_stage_checkpoints_each_bounded_candidate_batch_and_scales_past_256` first and prove its pre-fix failure. Add immutable `CandidateEntryReceipt(entry_ordinal, key, generation, locator, target_manifest, payload_digest, byte_size)` as a lossless validated equivalent of `AuthorityInventoryEntry`, and add `CandidateBatchReceipt(run_id, plan_digest, batch_ordinal, first_entry_ordinal, past_last_entry_ordinal, destination_identity, destination_revision, predecessor_digest, entries, candidate_digest, entry_count, byte_count)`. Implement dedicated `MaintenanceEvidenceStore.write_candidate_batch/read_candidate_batch` methods that canonicalize and authenticate each bounded receipt, reuse the existing symlink-contained `mkstemp`/file-fsync/atomic-replace mechanics at a deterministic run-and-ordinal path below the operator work directory, verify owner-only POSIX mode (and fail closed when the running platform cannot establish its equivalent confidentiality contract), and never render target manifests into `MigrationPlan`, reports, errors, or logs. Keep only `candidate_batch_count` plus the last `candidate_batch_chain_digest` in the main evidence record; authenticate predecessor linkage, ordinal/range continuity, duplicate/overlapping entries, exact entry-count/byte-count/digest agreement, per-entry manifest bounds, per-batch entry/byte limits, and destination/run/plan binding before use. Resume reads ordinals `0..candidate_batch_count-1` directly rather than listing the directory. Refactor existing `candidate_digest()` into one incremental canonical descriptor accumulator so its final digest has no whole-store 256-entry rejection while each read/write remains batch-bounded; do not add a parallel digest function. Change `OfflineMigrationService.stage()` to file-fsync and atomically replace the exact descriptor journal, then atomically advance the authenticated main evidence count/chain head after every completed external batch; revalidate each recorded candidate from its exact manifest/digest/size before skipping it on resume, and derive the single complete `VerifiedCandidateReceipt` by streaming all authenticated journals only after every planned entry is represented. Preserve D-07, D-12, D-17, D-19, D-20, and D-22: confidential evidence corroborates completed effects, signing/provider secrets are never serialized, missing journal/evidence after a filesystem durability loss fails closed per the existing WR-01 disposition, and only the existing authority activation publishes the whole candidate.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_migration_cutover.py::test_migration_stage_checkpoints_each_bounded_candidate_batch_and_scales_past_256 -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named test stages and verifies 257 entries, observes at least two separately bounded authenticated private journals with exact descriptor material, resumes after a first-batch fault without rewriting, re-transforming, listing, or adopting unrecorded candidates, rejects journal tamper/chain discontinuity, keeps a manifest sentinel out of shareable plan/report/log bytes, and produces exact final digest/count/bytes accepted by the unchanged authority publication boundary.</acceptance_criteria>
  <done>Every migration candidate external effect is exactly reconstructable from one authenticated confidential bounded batch before the service can proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume and abort STAGING with exact absence proof and durable cleanup debt</name>
  <files>src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_migration_cutover.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>.codex/skills/spike-findings-cacheness/references/backend-mechanics.md</file>
  </read_first>
  <behavior>
    - "abort accepts STAGING only when authenticated candidate-batch receipts bind the exact run and plan."
    - "A recorded candidate that is already absent counts as idempotently retired; a generation/locator mismatch is retained as debt and never deletes a current generation."
    - "After one deletion succeeds and the next fails, an authenticated CandidateRetirementBatch checkpoints exact retired progress plus remaining exact debt within that same bounded source batch; retry continues from its deterministic ordinal file and stores larger than one batch never accumulate all retirement descriptors in one document."
  </behavior>
  <action>Write `test_resume_and_abort_staging_revalidate_exact_batches_and_persist_partial_cleanup_debt` first. Extend the legal evidence transitions and `OfflineMigrationService.abort()` for D-16 and D-21 so STAGING candidates are reconstructed solely by authenticating the deterministically addressed exact-descriptor journals bound by `candidate_batch_count` and `candidate_batch_chain_digest`, not listing, aggregate-digest inversion, transformation replay, or timing. Add bounded `CandidateRetirementBatch(run_id, plan_digest, batch_ordinal, candidate_batch_digest, outcomes, predecessor_digest)` with one exact outcome per source-batch entry (`retired`, `already_absent`, `ownership_conflict`, or `cleanup_debt`) and dedicated authenticated owner-private read/write methods at deterministic ordinal paths. Before each delete, use the stored target manifest plus key/generation/locator/payload digest/byte size to prove the exact run-owned never-reused immutable candidate; treat exact absence as completed, refuse mismatched live ownership, and atomically checkpoint the updated retirement batch after each external outcome. Store unresolved exact candidate debt in that bounded retirement journal rather than the main evidence `cleanup_debt` tuple, and advance `MaintenanceRunEvidence.candidate_retirement_cursor` plus retirement-chain head only after the whole source batch is classified. Validate source-batch digest, predecessor chain, outcome uniqueness/completeness, and run/plan binding on every load. Make repeated abort replay-safe and ensure source entries, authority selection, activation state, private journal audit evidence, and shareable plan confidentiality remain unchanged. Keep deletion outside the authority transaction and do not add process coordination or replace the existing payload participant with obstore. If a later obstore participant performs this path, its path-only delete must settle absence through exact head; that conditional implementation is not part of this plan.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_migration_cutover.py::test_resume_and_abort_staging_revalidate_exact_batches_and_persist_partial_cleanup_debt tests/test_migration_cutover.py::test_offline_service_abort_removes_only_its_unactivated_candidate -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named interruption test proves STAGING abort across more than one batch, already-absent idempotence, per-outcome bounded retirement checkpointing, exact remaining debt without a whole-store tuple, safe retry completion, and unchanged source/authority visibility.</acceptance_criteria>
  <done>Interrupted migration staging can be resumed or cleaned deterministically without losing the valid source or forgetting external cleanup obligations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| confidential authenticated batch journal -> external candidate cleanup | Untrusted, disclosed, or replayed descriptor material must neither leak target metadata nor authorize deletion of an unrelated or current generation. |
| external candidate batches -> lifecycle authority | Candidate presence and receipts corroborate work but cannot select visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-G12-01 | Tampering | Confidential candidate batch journals | high | mitigate | Separately bounded owner-private canonical receipts authenticate the chain and every exact ordinal/key/generation/locator/target-manifest/digest/size descriptor; shareable renderers receive only digests and every resume revalidates exact outputs. |
| T-07-G12-02 | Spoofing | Abort ownership | high | mitigate | Delete only exact run-owned locator/generation identities derived from authenticated receipts; mismatches remain debt. |
| T-07-G12-03 | Repudiation | Partial abort | medium | mitigate | Checkpoint each retired/already-absent/conflict/debt outcome in an authenticated retirement journal bounded to its exact source batch and chain completed batch ordinals in main evidence. |
| T-07-G12-04 | Denial of Service | Whole-store aggregation | medium | mitigate | Keep each page/batch bounded while streaming the complete digest and counters without a page-size-derived total cap. |
| T-07-G12-05 | Elevation of Privilege | Evidence as authority | high | mitigate | Preserve the existing authority-only activation call; batch evidence has no publication operation. |
</threat_model>

<verification>
Run the two named tests plus the complete migration cutover and evidence suites. Safety proves no wrong-generation deletion, confidential descriptor disclosure, or partial publication; recovery proves exact manifest-bearing resume/debt; progress and persisted descriptor material remain bounded per batch; performance qualification stays in Phase 8.
</verification>

<success_criteria>
- More than 256 entries stage and verify through separately bounded authenticated confidential batch journals carrying exact descriptors.
- A fault after any completed batch resumes without rewriting, re-transforming, losing, or adopting unexplained candidates.
- STAGING abort is exact, idempotent for already-absent candidates, and durable across partial deletion failure through separately bounded retirement journals.
- Authority activation remains the only visibility transition and no coordination mechanism is added.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-SUMMARY.md` when done.
</output>
