---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 15
type: execute
wave: 11
depends_on:
  - 07-14
files_modified:
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/migration.py
  - tests/test_rebuild_workflow.py
autonomous: true
requirements:
  - MIGR-05
  - MIGR-06
gap_closure: true
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Before each destination write, authenticated evidence persists a deterministic rebuild operation ID; after canonical commit-response loss, resume replays that exact operation and checkpoints its original BlobReceipt before later source work (MIGR-05, MIGR-06; D-17 through D-20)."
    - "Resume handles REBUILDING, REBUILD_STAGED, REBUILD_VERIFYING, and REBUILD_VERIFIED by authenticating evidence and revalidating exact destination receipts rather than discovering or adopting destination state (MIGR-05; D-19, D-20)."
    - "Crash or verification failure cleans only destination entries still matching this run's exact receipts; conflicts and operational failures remain durable retryable debt while source data and accepted output remain untouched (MIGR-05, MIGR-06; D-08, D-16, D-21, D-22)."
  artifacts:
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Bounded authenticated rebuild write intents, exact receipt batches, retirement progress, and cleanup debt"
    - path: src/cacheness/storage/migration.py
      provides: "Receipt-driven rebuild staging, every-state resume, verification, and failure cleanup"
    - path: tests/test_rebuild_workflow.py
      provides: "Post-commit/pre-checkpoint response-loss, rebuild-state resume, and receipt-bound cleanup tests"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "each deterministic destination-write intent is authenticated before put_entry, then its exact receipt is checkpointed before staging continues"
      pattern: "rebuild_write_intents|rebuild_receipts|REBUILDING"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/blob_store.py
      via: "the evidence-bound operation ID is passed through the Plan 07-14 replay seam so retry consults canonical lifecycle authority"
      pattern: "operation_id|put_entry"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/blob_store.py
      via: "resume and cleanup compare exact BlobReceipt key, generation, locator, and expectation before read or delete"
      pattern: "BlobReceipt|get_entry_info|delete"
---

<objective>
Close the interrupted-rebuild attribution, resume, and cleanup blocker on top of the canonical replay seam from Plan 07-14.

Purpose: Ensure a rebuild recovers from every durable state using only authenticated run-owned intents and exact destination receipts, without adopting or deleting unrelated entries.
Output: Persisted rebuild write intents and receipt batches, every-state resume, receipt-bound cleanup/debt, and crash-boundary tests.
</objective>

## Artifacts this phase produces

- `RebuildWriteIntent(run_id, plan_digest, destination_identity, entry_ordinal, entry_key, source_manifest_digest, target_contract_digest, operation_id)` and `RebuildReceiptBatch(batch_ordinal, first_entry_ordinal, past_last_entry_ordinal, receipts, batch_digest, byte_count)` in `migration_evidence.py`.
- `MaintenanceRunEvidence.pending_rebuild_intent`, `MaintenanceRunEvidence.rebuild_receipt_batches`, and `MaintenanceRunEvidence.retired_rebuild_operation_ids`; existing `cleanup_debt` stores canonical exact rebuild operation/key/generation/locator debt records.
- Receipt-driven `stage_rebuild`, `verify_rebuild`, `resume`, and failure cleanup through Plan 07-14's canonical operation replay.
- Exact tests named `test_rebuild_response_loss_after_destination_commit_replays_intent_before_receipt_checkpoint`, `test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state`, and `test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/handler-integration.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-09-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-14-SUMMARY.md
@src/cacheness/storage/migration_evidence.py
@src/cacheness/storage/migration.py
@src/cacheness/storage/blob_store.py
@tests/test_rebuild_workflow.py
</context>

<interfaces>
Plan 07-14 adds `BlobStore.put_entry(..., operation_id=...)` and exact canonical lifecycle replay. `MaintenanceRunEvidence` currently has rebuild states but no persisted per-write intent or receipt collection. `OfflineMigrationService.stage_rebuild()` retains receipts only in a local list, `verify_rebuild()` passes an empty receipt tuple on failure, and `resume()` has no rebuild-state branches.
</interfaces>

<planner_assumptions>
- MIGR-05 requires authenticated durable attribution for each destination write and replay from every declared rebuild state; incidental destination keys never prove progress.
- MIGR-06 keeps rebuild separate from physical migration: reads use authenticated source handlers, writes use destination BlobStore lifecycle, and evidence never becomes destination authority.
- Destination cleanup is an external retryable effect. It preserves exact conflicting/current entries and the complete source rather than claiming cross-resource rollback.
- The response-loss repair uses two existing durable boundaries in order: authenticated maintenance evidence records the deterministic intended operation before BlobStore, and Plan 07-14's single canonical destination authority records and replays the exact `MutationSpec`. Neither record adopts a destination entry from key presence, listing, path, or timing.
- The validated obstore spike fixes the handler boundary regardless of payload participant: source and destination handlers keep `CacheHandler.put(data, Path, config)` / `CacheHandler.get(Path, metadata)`, store-local custom registration, private guarded staging, native suffix checks, descriptor identity, containment, and synchronous snapshot lifetime. This plan does not install or expose obstore.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Persist one rebuild intent before commit and checkpoint its canonical replay receipt</name>
  <reversibility rating="costly">Persisted rebuild intent and receipt fields require coordinated evidence decoder and resume changes in later releases.</reversibility>
  <files>src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_rebuild_workflow.py</files>
  <read_first>
    - src/cacheness/storage/migration_evidence.py: authenticated MaintenanceRunEvidence schema, bounds, canonical encoding, and legal rebuild transitions
    - src/cacheness/storage/migration.py: stage_rebuild, evidence writes, response-loss handling, and destination put calls
    - src/cacheness/storage/blob_store.py: Plan 07-14 deterministic operation-ID put and canonical receipt replay contract
    - tests/test_rebuild_workflow.py: registered-handler rebuild, integrity-failure, and batch-failure fixtures
    - .codex/skills/spike-findings-cacheness/references/handler-integration.md: path-based custom-handler and private staging/snapshot boundary
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: D-17 through D-20 and D-22
  </read_first>
  <behavior>
    - "Before each destination put, authenticated evidence contains a bounded RebuildWriteIntent binding run, plan, batch ordinal, source entry digest, destination identity/revision, destination key, and deterministic operation ID."
    - "A process-loss fault at rebuild.destination_committed_before_receipt_checkpoint leaves the intent plus a canonically promoted destination mutation; resume passes the same operation ID, receives the original receipt, checkpoints it, and creates no second generation."
    - "A catchable response error classifies that same operation before cleanup or leaves REBUILDING intent for retry; evidence contains ownership identifiers and digests but no source-value or signing-material sentinel."
  </behavior>
  <action>Write `test_rebuild_response_loss_after_destination_commit_replays_intent_before_receipt_checkpoint` first. Add bounded canonical `RebuildWriteIntent(run_id, plan_digest, destination_identity, entry_ordinal, entry_key, source_manifest_digest, target_contract_digest, operation_id)` and `RebuildReceiptBatch(batch_ordinal, first_entry_ordinal, past_last_entry_ordinal, receipts, batch_digest, byte_count)`. Persist them only through `MaintenanceRunEvidence.pending_rebuild_intent` and `MaintenanceRunEvidence.rebuild_receipt_batches`, validating run/plan/destination binding, deterministic operation-ID derivation from run ID plus plan digest plus exact ordered entry identity, ordinal/range continuity, uniqueness, and collection byte/count bounds. In `stage_rebuild`, authenticate and checkpoint `pending_rebuild_intent` before invoking `destination.put_entry(..., operation_id=intent.operation_id)`, expose the exact fault label `rebuild.destination_committed_before_receipt_checkpoint` immediately after that call returns and before receipt evidence is written, then append the exact returned `BlobReceipt` to the current receipt batch and clear the pending intent atomically in the next evidence replacement before reading another source entry or batch. Keep handler access on the existing path-based `CacheHandler` API within `GuardedHandlerIO` private snapshots/stages; a custom MCAP-style single-file handler must remain registerable without receiving a payload locator, participant instance, or stream API. Use a process-loss-style `BaseException` or child-process termination in the boundary test so ordinary exception cleanup cannot erase the crash state. On resume, authenticate evidence and call the same operation-ID path for an intent lacking a receipt; accept only the canonical authority replay from Plan 07-14. If a catchable put error may represent committed response loss, classify or retry that exact operation ID before cleanup: checkpoint a recovered receipt, or preserve REBUILDING plus its intent and surface a typed retryable failure when authority state remains unclassified. Never attempt receipt cleanup while an unresolved pre-write intent lacks canonical classification. Preserve D-17 through D-20 and D-22 without destination listing discovery, coordination, entry adoption, obstore introduction, or an S3 conditional-publication policy change.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_response_loss_after_destination_commit_replays_intent_before_receipt_checkpoint -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named fault test proves both process-loss and catchable response-error windows recover through pre-write intent plus canonical authority state, checkpoint the original receipt, create no duplicate generation, and persist no payload or signing secret.</acceptance_criteria>
  <done>Every rebuild write is explained before commit and its canonical result remains recoverable after response loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resume every rebuild state and clean only exact receipt ownership</name>
  <files>src/cacheness/storage/migration_evidence.py, src/cacheness/storage/migration.py, tests/test_rebuild_workflow.py</files>
  <read_first>
    - src/cacheness/storage/migration_evidence.py: persisted rebuild intents, receipts, cleanup outcomes, and state transition validation from Task 1
    - src/cacheness/storage/migration.py: _discard_rebuild_receipts, _abort_rebuild_after_failure, verify_rebuild, accept_rebuild, and resume
    - src/cacheness/storage/blob_store.py: exact BlobReceipt identity and guarded get/delete surfaces
    - tests/test_rebuild_workflow.py: response-loss tracer plus existing failure-cleanup coverage
    - docs/adr/0001-topology-specific-storage-guarantees.md: external cleanup and single-authority stop conditions
    - .codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md: exact immutable identity and cleanup-debt recovery boundary
  </read_first>
  <behavior>
    - "After each bounded batch, evidence contains exact BlobReceipt ownership and resume covers REBUILDING, REBUILD_STAGED, REBUILD_VERIFYING, and REBUILD_VERIFIED."
    - "Resume revalidates every recorded receipt and continues only plan entries represented by neither a completed receipt nor the one exact replayable intent."
    - "Failure cleanup deletes only an exact key/generation/locator/expectation match, treats absence as retired, preserves changed ownership, checkpoints each outcome, and retains operational failure as retryable debt."
  </behavior>
  <action>Write `test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state` and `test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt` first. Revalidate every receipt in `rebuild_receipt_batches` against destination authority and continue only plan entries represented by neither a completed receipt nor `pending_rebuild_intent`. Route REBUILDING, REBUILD_STAGED, REBUILD_VERIFYING, and REBUILD_VERIFIED through their legal next steps. Replace local-list cleanup with evidence-owned receipts: prove exact key, generation, locator, and expectation before delete; treat exact absence as retired and append its operation ID to bounded unique `retired_rebuild_operation_ids`; preserve changed ownership; checkpoint each retirement or conflict; and encode each exact operational failure in existing `cleanup_debt` as `rebuild:<operation_id>:<key>:<generation>:<locator>`. Evidence may reach ABORTED only when every recorded receipt's operation ID is retired or represented by durable debt. Preserve D-08, D-16 through D-21, and D-22: do not scan destination listings, adopt an entry without its pre-write intent, delete source or accepted output, or add a coordinator, queue, lease, or lock.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state tests/test_rebuild_workflow.py::test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt tests/test_rebuild_workflow.py::test_rebuild_discards_only_its_run_owned_destination_entries_after_batch_failure -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The state test covers all four resumable rebuild states, and cleanup tests prove exact retirement/debt handling without duplicate writes, ownership adoption, unrelated deletion, or source damage.</acceptance_criteria>
  <done>Every interrupted rebuild converges through authenticated receipts, a legal resume path, or explicit receipt-bound cleanup debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated rebuild intent -> canonical destination authority | A deterministic operation is recorded before BlobStore commit; only its exact authority mutation may satisfy response-loss retry. |
| authenticated rebuild receipts -> destination BlobStore | Replayed or forged receipts must not authorize completion or cleanup. |
| source handler -> destination lifecycle | Trusted application payloads cross handlers only after source authenticity and integrity checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-15-01 | Tampering | Rebuild intents and receipt batches | high | mitigate | Authenticate a deterministic exact operation intent before each put, then bind the canonical BlobReceipt to run, plan, destination identity, ordinal, and bounded batch. |
| T-07-15-02 | Spoofing | Cleanup ownership | high | mitigate | Compare exact key, generation, locator, and expectation before delete; preserve mismatches as debt. |
| T-07-15-03 | Repudiation | Verification failure | medium | mitigate | Record receipt-backed cleanup outcome and remaining debt before terminal evidence state. |
| T-07-15-04 | Information Disclosure | Rebuild evidence | medium | mitigate | Persist ownership identifiers and digests only and retain signing material behind the configured provider. |
</threat_model>

<verification>
Run the three named rebuild tests plus the complete rebuild and migration-evidence suites. Verify safety, response-loss recovery, every-state resume, bounded checkpoints, exact cleanup ownership, and the ADR authority boundary separately.
</verification>

<success_criteria>
- Every destination write has durable authenticated intent before commit and every completed rebuild batch has durable exact receipts.
- A crash after canonical destination commit but before receipt checkpoint replays the same authority operation and returns the original receipt without another generation.
- Every rebuild state has an authenticated, deterministic resume path.
- Verification and crash cleanup act only on matching run-owned entries and persist partial debt.
- Source data, accepted output, and unrelated destination generations remain intact.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Coverage | Plan(s) |
|---|---|---|---|
| GOAL | Explicit stopped-worker rebuild without silent mutation or loss | COVERED | 07-14, 07-15 |
| REQ | MIGR-05 authenticated interruption recovery | COVERED | 07-14, 07-15 |
| REQ | MIGR-06 explicit confirmed rebuild integrity | COVERED | 07-15 |
| RESEARCH | Canonical authority replay plus bounded authenticated rebuild evidence | COVERED | 07-14, 07-15 |
| CONTEXT | D-08 and D-16 through D-22 rebuild attribution, resume, cleanup, determinism, and secrecy | COVERED | 07-14, 07-15 |
| CONTEXT | Deferred ideas | EXCLUDED | Not planned, as required |

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-15-SUMMARY.md` when done.
</output>
