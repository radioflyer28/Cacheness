---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 21
type: execute
wave: 17
depends_on:
  - 07-20
files_modified:
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/migration_evidence.py
  - tests/test_rebuild_workflow.py
autonomous: true
requirements:
  - MIGR-05
  - MIGR-06
gap_closure: true
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A rebuild cleanup failure remains in its existing resumable rebuild evidence state while any exact receipt-bound debt remains; debt representation alone never authorizes terminal `ABORTED` (MIGR-05; D-16, D-19, D-21)."
    - "Resume authenticates the supplied run/plan/evidence, corroborates each debt item against the existing lifecycle-authority operation and recorded `BlobReceipt`, and retries only that immutable generation; it never discovers work from destination entries, paths, or listings (MIGR-05; D-17, D-19, D-20, D-21)."
    - "A cleanup debt item is removed only after exact deletion or proven absence, and terminal `ABORTED` is written only after every recorded rebuild receipt is retired; operational interruption remains typed and retryable (MIGR-05; D-16, D-21)."
    - "Changed ownership, forged debt, missing authority replay, or receipt disagreement remains fail closed without deleting the current generation or the source store (MIGR-05, MIGR-06; D-08, D-20, D-22)."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "Receipt-bound rebuild cleanup settlement through existing authenticated evidence and lifecycle authority"
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Legal reuse of existing rebuild states until cleanup debt is fully settled, with no new state or authority"
    - path: tests/test_rebuild_workflow.py
      provides: "Failure, participant restoration, retry, exact convergence, and ownership-tamper regressions"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "`_abort_rebuild_after_failure` checkpoints exact debt in the current resumable state and `resume` prioritizes its settlement before normal rebuild work"
      pattern: "_abort_rebuild_after_failure|cleanup_debt|REBUILDING|ABORTED"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "each cleanup debt item must match the authenticated recorded receipt and exact existing `read_mutation(operation_id)` replay before an external delete/absence proof"
      pattern: "read_mutation|BlobReceipt|operation_id|expectation"
    - from: tests/test_rebuild_workflow.py
      to: src/cacheness/storage/migration.py
      via: "restore the failed participant, call explicit resume with exact run/evidence, and prove both payload state and evidence reach terminal convergence"
      pattern: "resume|cleanup_debt|retired_rebuild_operation_ids|ABORTED"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No ordinary constructor, open, initialize, read, cache-policy, reconciliation, or cleanup path may perform an implicit migration or upgrade."
    - "[FLAGGED/UNVERIFIED] No work-directory file, filesystem path, object listing, candidate presence, projection, lock, queue, lease, journal, sidecar, or process-local gate may become a second lifecycle authority."
    - "[FLAGGED/UNVERIFIED] No unauthenticated evidence, unexplained candidate, mismatched receipt, or incomplete catalog may be adopted."
    - "[FLAGGED/UNVERIFIED] No coordinator-owned universal payload conversion, handler stream cutover, managed-locator exposure, or production obstore adoption may be introduced."
    - "[FLAGGED/UNVERIFIED] No plan or test may claim one ACID transaction across metadata authority and filesystem/S3 effects, exact reclamation of invisible pre-checkpoint orphans, or identical progress guarantees across topologies."
---

<objective>
Close the terminal rebuild-cleanup-debt blocker through the existing authenticated receipt and lifecycle-authority model.

Purpose: Ensure operational cleanup failures remain explicitly recoverable until exact settlement, without introducing another lifecycle state machine or deleting changed ownership.
Output: Nonterminal receipt-bound debt, deterministic explicit resume settlement, terminal convergence only after cleanup, and fail-closed ownership tests.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-PATTERNS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-15-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-15-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-20-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.codex/skills/spike-findings-cacheness/references/backend-mechanics.md
@src/cacheness/storage/migration.py
@src/cacheness/storage/migration_evidence.py
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/lifecycle_authority.py
@tests/test_rebuild_workflow.py
</context>

<adr_0001_contract>
- Supported topology: stopped-worker memory/memory and SQLite/filesystem deterministic rebuild recovery. Backend-specific operational exceptions remain typed; live remote qualification is Phase 8.
- Integrity: the existing lifecycle authority's exact operation replay plus authenticated `BlobReceipt` is the only cleanup ownership proof. Changed current ownership is never deleted.
- Recovery: canonical destination mutation is already complete before cleanup; exact payload deletion/proven absence is an external effect outside the authority transaction, and authenticated evidence retains debt until settlement.
- Progress: resume either settles bounded debt, leaves the run in the same recoverable state with a typed operational outcome, or fails closed on evidence/ownership conflict. It does not guarantee every retry succeeds.
- Performance: settlement processes only the existing bounded receipt prefix; no timing or throughput assertion is introduced.
- Blob interruption: a failure after exact delete but before evidence checkpoint is retried by proving exact absence; the immutable locator is never reused.
</adr_0001_contract>

<flagged_assumptions>
- `[FLAGGED/UNVERIFIED A-MIGR03]` Existing non-mutating plans remain unchanged and continue to bind this rebuild run by digest.
- `[FLAGGED/UNVERIFIED A-MIGR04]` Existing copy-verify-switch behavior remains unchanged; this plan only settles already attributed rebuild cleanup.
- `[FLAGGED/UNVERIFIED A-MIGR05]` Existing authenticated receipts and the singular lifecycle authority are sufficient to settle exact cleanup debt without a new state, journal, or discovery source.
- `[FLAGGED/UNVERIFIED A-MIGR06]` The explicit rebuild remains handler-backed and destination-BlobStore-owned; receipt-bound cleanup does not become universal cross-backend migration.
</flagged_assumptions>

<assumption_delta>
Decision: no-change. The singular lifecycle authority remains the primary domain noun and owner; this plan reuses its existing operation replay and does not generalize authority ownership.
</assumption_delta>

<interfaces>
`MaintenanceRunEvidence` already carries bounded `rebuild_receipt_batches`, `retired_rebuild_operation_ids`, and `cleanup_debt`. `_rebuild_cleanup_debt(receipt)` encodes the exact operation/key/generation/locator. `_abort_rebuild_after_failure()` currently calls exact `BlobStore.delete(expected=...)` but treats debt as terminal coverage. `resume()` already handles `REBUILDING`, `REBUILD_STAGED`, `REBUILD_VERIFYING`, and `REBUILD_VERIFIED`. `LifecycleAuthority.read_mutation(operation_id)` supplies the canonical exact promoted operation replay. No new enum value, receipt schema, or authority API is required.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keep one failed rebuild cleanup recoverable and settle it on resume</name>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_evidence.py, tests/test_rebuild_workflow.py</files>
  <read_first>
    - src/cacheness/storage/migration.py: `_abort_rebuild_after_failure`, `_rebuild_cleanup_debt`, `_validate_rebuild_receipts`, `resume`, and destination exact-delete paths
    - src/cacheness/storage/migration_evidence.py: rebuild states, legal transitions, receipt/debt validation, and authenticated checkpoint replacement
    - src/cacheness/storage/lifecycle_authority.py: `read_mutation` replay and promoted entry expectations
    - src/cacheness/storage/blob_store.py: exact receipt identity, `get_entry_info`, expected delete, payload snapshot, and exact payload deletion seams
    - tests/test_rebuild_workflow.py: `test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt` plus state-resume fixtures
    - docs/adr/0001-topology-specific-storage-guarantees.md: one-authority cleanup-debt and immutable-effect rules
  </read_first>
  <behavior>
    - "A deterministic operational delete failure records the exact rebuild debt and leaves evidence in the current resumable rebuild state, not terminal `ABORTED`."
    - "After participant restoration, explicit `resume(plan, run_id, evidence_path)` authenticates the debt, corroborates its operation through lifecycle-authority replay, retries the exact generation, and checkpoints each retirement."
    - "Delete-response loss is idempotent: a retry proves the exact immutable locator absent, clears debt, and does not recreate or adopt a payload."
    - "Only when all recorded receipts are retired does evidence enter `ABORTED`; source entries remain unchanged throughout."
    - "The evidence model rejects an `ABORTED` rebuild record that still contains cleanup debt or lacks retirement coverage for any recorded receipt."
  </behavior>
  <action>Rewrite `test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt` to assert nonterminal evidence while operational debt exists, then add `test_rebuild_cleanup_debt_stays_resumable_until_exact_settlement` and `test_rebuild_evidence_rejects_terminal_aborted_cleanup_debt`; restore the destination participant and resume with the exact run ID/evidence path. Refactor `_abort_rebuild_after_failure()` so successful retirements checkpoint immediately, operational failures append one deduplicated exact debt item, and any remaining debt leaves the evidence in its existing resumable rebuild state. Add a narrow settlement helper called before the normal rebuild-state branch in `resume()`: authenticate plan/evidence, map each debt string to exactly one recorded receipt, require `read_mutation(operation_id)` to equal that receipt's promoted result, and delete only a current exact receipt match or the exact immutable receipt locator. Treat `FileNotFoundError` as proven absence; classify only typed operational participant errors as remaining debt. After each exact success, checkpoint the operation ID in `retired_rebuild_operation_ids` and remove only its matching debt item. Write terminal `ABORTED` only when every recorded receipt is retired and cleanup debt is empty. In `MaintenanceRunEvidence.__post_init__`, reject an `ABORTED` record with rebuild receipt batches unless cleanup debt is empty and every recorded operation ID appears in `retired_rebuild_operation_ids`; this makes the terminal invariant fail closed at the evidence boundary. Reuse the existing enum states, evidence fields, lifecycle authority, and exact payload participant methods; add no cleanup state, intent, authority method, journal, listing, or adoption path.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_verification_failure_cleans_only_exact_receipts_and_persists_debt tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_stays_resumable_until_exact_settlement tests/test_rebuild_workflow.py::test_rebuild_evidence_rejects_terminal_aborted_cleanup_debt tests/test_rebuild_workflow.py::test_rebuild_checkpoints_exact_destination_receipts_and_resumes_each_rebuild_state -x -o log_cli=false -o addopts=</automated>
  </verify>
  <acceptance_criteria>The tests fail against terminal-debt behavior, then prove an operational failure remains recoverable, exact participant restoration/resume retires every receipt, delete-response loss converges by absence proof, and only debt-free evidence becomes `ABORTED`.</acceptance_criteria>
  <done>Every operationally failed rebuild cleanup has an explicit receipt-bound path to terminal exact settlement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject forged debt and preserve changed ownership during settlement</name>
  <files>src/cacheness/storage/migration.py, tests/test_rebuild_workflow.py</files>
  <read_first>
    - src/cacheness/storage/migration.py: Task 1 settlement helper, receipt validation, and typed migration evidence failures
    - tests/test_rebuild_workflow.py: changed-owner setup, evidence-rewrite helpers, and exact source/destination assertions
    - src/cacheness/storage/migration_evidence.py: canonical debt parsing and checkpoint authentication
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md: D-19 through D-22
  </read_first>
  <behavior>
    - "A debt record whose operation ID, key, generation, or locator does not match exactly one authenticated recorded receipt fails before payload access."
    - "A missing, prepared-only, conflicting, or mismatched lifecycle-authority operation cannot authorize cleanup."
    - "When the logical key now names another generation, settlement never deletes that current generation; the receipt debt remains explicit unless the exact old locator is independently deleted or proven absent."
    - "No destination listing, latest-run search, timing guess, or caller-provided receipt can satisfy cleanup ownership."
  </behavior>
  <action>Write `test_rebuild_cleanup_retry_rejects_forged_debt_without_payload_access` and `test_rebuild_cleanup_retry_preserves_changed_current_ownership` first. Mutate each debt identity component under a valid evidence envelope or use focused helper inputs, and assert the existing typed evidence mismatch before any participant read/delete. For changed ownership, overwrite one destination key after the recorded receipt, inject a cleanup failure, and resume: preserve the new current generation and source, retain or settle only the old receipt's exact immutable locator according to observable participant state, and never treat the current key as proof of the old locator. Require canonical lifecycle-authority replay plus the authenticated recorded receipt for every attempt. Let integrity/evidence/ownership conflicts propagate rather than converting them to operational debt, and verify zero listing/adoption calls.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_cleanup_retry_rejects_forged_debt_without_payload_access tests/test_rebuild_workflow.py::test_rebuild_cleanup_retry_preserves_changed_current_ownership tests/test_rebuild_workflow.py::test_rebuild_discards_only_its_run_owned_destination_entries_after_batch_failure -x -o log_cli=false -o addopts=</automated>
  </verify>
  <acceptance_criteria>Forged or authority-mismatched debt fails before external effects, changed current ownership is never deleted, source data remains complete, and only the receipt's exact immutable generation may be retired or proven absent.</acceptance_criteria>
  <done>Rebuild cleanup settlement remains fail closed and cannot become a discovery or current-key deletion path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated rebuild evidence -> lifecycle authority replay | Receipt/debt identity must corroborate one exact canonical promoted operation. |
| lifecycle authority replay -> payload participant | Authority ownership permits only exact immutable-generation cleanup, never current-key discovery. |
| operational cleanup result -> terminal evidence | Debt must remain recoverable until exact deletion or absence is proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-21-01 | Spoofing | Rebuild cleanup ownership | high | mitigate | Require exact recorded receipt plus matching lifecycle-authority replay before payload access. |
| T-07-21-02 | Tampering | Rebuild cleanup debt | high | mitigate | Parse and compare operation, key, generation, and locator; reject any mismatch before deletion. |
| T-07-21-03 | Repudiation | Rebuild terminal state | high | mitigate | Checkpoint each retirement and allow `ABORTED` only when every receipt is retired and debt is empty. |
| T-07-21-04 | Denial of Service | Retryable participant outage | medium | mitigate | Preserve bounded exact debt in the existing resumable state and return a typed retryable outcome without expanding work scope. |
</threat_model>

<verification>
- The five named rebuild selectors pass in the isolated frozen all-extras environment.
- Tests separately prove integrity/ownership refusal, recovery after participant restoration, bounded progress, and absence of performance claims.
- Existing response-loss replay, every-state resume, and projection-suppression tests remain green.
</verification>

<success_criteria>
- Rebuild cleanup debt is never stranded in terminal evidence.
- Resume settles only exact receipt/authority-correlated generations.
- Exact deletion or absence proof is required before debt retirement.
- `ABORTED` is reached only after all recorded receipts settle.
- Forged evidence and changed current ownership remain fail closed and preserved.
</success_criteria>

<source_coverage>
- GOAL: Interrupted explicit rebuild cleanup converges without losing source data or deleting a later destination owner.
- REQ: MIGR-05 and MIGR-06.
- RESEARCH: Authenticated bounded evidence, exact immutable deletion, deterministic recovery, and one transactional authority.
- CONTEXT: D-08, D-16, D-17, D-19, D-20, D-21, and D-22.
- REVIEW: CR-03 is closed through the existing receipt/authority seam.
- EXCLUDED: No new evidence state, authority API, coordination component, production obstore participant, live-service qualification, or stronger cross-resource guarantee.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-21-SUMMARY.md` when done.
</output>
