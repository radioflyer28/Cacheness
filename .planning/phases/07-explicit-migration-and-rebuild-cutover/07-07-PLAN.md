---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "07"
type: execute
wave: 5
depends_on: [07-06]
files_modified:
  - src/cacheness/storage/backends/postgresql_lifecycle_authority.py
  - src/cacheness/storage/backends/s3_backend.py
  - tests/contracts/test_postgresql_lifecycle_authority.py
  - tests/test_migration_remote_contract.py
autonomous: true
requirements: [MIGR-04, MIGR-05]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The PostgreSQL authority implements the same verified whole-store activation/rollback/finalize primitives inside one PostgreSQL transaction and preserves typed serialization/deadlock/timeout outcomes (MIGR-04, MIGR-05)."
    - "S3 payload stage/verify/purge remains an immutable external effect with exact run/candidate ownership and retryable evidence; object listing or existence never authorizes activation or adoption (D-12, D-16, D-20)."
    - "Deterministic adapter tests cover non-live PostgreSQL/S3 behavior only; Phase 8 remains the sole real PostgreSQL/AWS S3 qualification gate."
  artifacts:
    - path: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      provides: "PostgreSQL transactional maintenance activation/rollback/finalize contract"
    - path: src/cacheness/storage/backends/s3_backend.py
      provides: "Exact immutable candidate verification and run-owned cleanup participant behavior"
    - path: tests/test_migration_remote_contract.py
      provides: "Deterministic non-live PostgreSQL/S3 cutover, interruption, and non-claim tests"
  key_links:
    - from: src/cacheness/storage/backends/postgresql_lifecycle_authority.py
      to: src/cacheness/storage/migration_authority.py
      via: "Verified receipts enter one PostgreSQL authority transaction"
      pattern: "activate_verified_candidate|rollback_activation|finalize_activation"
    - from: src/cacheness/storage/backends/s3_backend.py
      to: src/cacheness/storage/migration.py
      via: "S3 immutable candidate operations return verifiable external-effect receipts"
      pattern: "digest|byte_size|candidate"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No PostgreSQL advisory lock, distributed lease, two-phase commit, exported snapshot authority, or transaction spanning S3."
    - "[FLAGGED/UNVERIFIED] No S3 listing/ETag/candidate existence becomes canonical evidence or permits adoption."
    - "[FLAGGED/UNVERIFIED] No mock, fake, collection success, or unavailable service is reported as live PostgreSQL/AWS S3 qualification."
---

<objective>
Extend the proven cutover contract to the deterministic PostgreSQL/AWS S3 adapter tier without upgrading its release qualification.

Purpose: Keep the public maintenance architecture backend-neutral while preserving exact remote transaction, external-effect, and progress boundaries.
Output: PostgreSQL authority maintenance primitives, S3 candidate participant behavior, and deterministic non-live contracts.
</objective>

## Artifacts this phase produces

- `PostgresqlLifecycleAuthority.activate_verified_candidate()`, `rollback_activation()`, and `finalize_activation()` plus persisted authority-owned candidate/prior/active/finalized state only if real functionality requires it.
- S3 candidate operations/receipts needed by the generic `OfflineMigrationService`, reusing immutable object IDs and exact ownership markers.
- `tests/test_migration_remote_contract.py` and expanded deterministic PostgreSQL contract cases.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-06-SUMMARY.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-09-SUMMARY.md
@src/cacheness/storage/backends/postgresql_lifecycle_authority.py
@src/cacheness/storage/backends/s3_backend.py
@tests/contracts/test_postgresql_lifecycle_authority.py
</context>

<interfaces>
The PostgreSQL adapter owns descriptor/intent/visibility/cleanup-debt transactions and maps serialization, deadlock, lock-timeout, and statement-timeout SQLSTATEs to typed retryable outcomes. S3 supplies immutable bounded blob operations only. Use the Plan 06 semantic receipts without reproducing orchestration in either adapter.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PostgreSQL whole-store maintenance transactions</name>
  <files>src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>src/cacheness/storage/migration_authority.py</file>
    <file>tests/contracts/test_postgresql_lifecycle_authority.py</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
  </read_first>
  <behavior>
    - "A complete verified receipt activates all candidate catalog state in one PostgreSQL transaction and retains the prior selection."
    - "Injected transaction faults roll back authority rows; response loss is classified through exact authority receipts; typed SQLSTATE causes survive."
    - "No external S3 call occurs within the authority transaction."
  </behavior>
  <action>Implement the narrow Plan 06 authority semantics for PostgreSQL. If whole-store active/prior/finalized state cannot be represented by the existing tables, add only the smallest versioned authority schema change required by the actual activation/rollback/finalize contract, together with explicit initialize rejection of the previous schema and a declared Phase 7 migration edge; do not bump schema merely to demonstrate migration. All authority state changes occur in one `_transaction()` and bind exact source/candidate receipts. Preserve existing SQLSTATE translation and causal exceptions. Deterministic tests must assert SQL shape/parameters, rollback, uncertain commit classification, idempotence, and zero payload/S3 calls inside the transaction.</action>
  <acceptance_criteria>PostgreSQL matches the semantic authority contract with exact transactional rollback/idempotence and typed progress outcomes; no cross-resource operation or live-service claim is introduced.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/contracts/test_postgresql_lifecycle_authority.py -x -o log_cli=false</automated></verify>
  <done>The remote authority tier can request the same generic cutover safely within PostgreSQL's declared boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove S3 candidate effects remain verifiable and non-authoritative</name>
  <files>src/cacheness/storage/backends/s3_backend.py, tests/test_migration_remote_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/backends/s3_backend.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>tests/test_s3_blob_backend.py</file>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-09-SUMMARY.md</file>
  </read_first>
  <behavior>
    - "Deterministic S3 fakes prove stage, response-loss revalidation, digest/size verification, retryable cleanup debt, and exact run ownership."
    - "An unrecorded matching object, object listing, ETag, or unexplained candidate is never adopted or treated as complete."
    - "Test output labels the remote profile deterministic/non-live and leaves BACK-05 for Phase 8."
  </behavior>
  <action>Use existing immutable S3 object operations to satisfy the generic candidate participant contract. Add a helper only if needed to stream/re-hash exact bytes under existing bounds; do not use ETag as content or authority proof. Bind candidate keys to exact run/candidate receipts, revalidate response-loss through digest/size and evidence, and return retryable cleanup debt on purge failure without changing activation. Build deterministic adapter tests with fakes/moto as appropriate, explicitly rejecting listing-based discovery and candidate adoption. Keep real credentials, buckets, PostgreSQL, and AWS qualification outside this plan.</action>
  <acceptance_criteria>Deterministic tests prove attributable S3 external effects and recovery without authority inference, secret output, unbounded listing, or a release qualification claim.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_remote_contract.py tests/test_s3_blob_backend.py -x -o log_cli=false</automated></verify>
  <done>The remote candidate path preserves the same integrity/recovery contract while remaining explicitly unqualified until Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verified receipt -> PostgreSQL transaction | Remote authority must corroborate state under concurrency and typed retryable failures. |
| maintenance evidence -> S3 object operations | External object effects may be delayed, duplicated, or have response loss; presence is not authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-26 | Tampering | PostgreSQL activation | critical | mitigate | Exact receipt/revision checks plus single PostgreSQL transaction and deterministic rollback/uncertain-commit tests. |
| T-07-27 | Spoofing/Tampering | S3 candidate ownership | high | mitigate | Bind exact run/candidate identity and SHA-256/size; reject listing/ETag/incidental presence as proof. |
| T-07-28 | Information Disclosure | remote diagnostics | high | mitigate | Sanitize DSN, credentials, bucket secrets, signing material, request data and exception context. |
| T-07-29 | Denial of Service | remote inventory/verification | medium | mitigate | Stream bounded chunks and page work; preserve typed timeouts/retryables rather than retry-until-success. |
</threat_model>

<verification>
Run deterministic contract modules only. Integrity/recovery/progress remain separate; no latency threshold is asserted. Explicitly state that passing fakes/moto does not satisfy BACK-05 or Phase 8 real-service qualification.
</verification>

<success_criteria>
The deterministic remote tier implements the same semantic maintenance boundary with PostgreSQL-only transactions, verifiable S3 external effects, typed progress, and honest non-live evidence.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-07-SUMMARY.md` when done.
</output>
