---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "14"
type: execute
wave: 10
depends_on: [07-12, 07-13]
files_modified:
  - src/cacheness/storage/lifecycle_authority.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - src/cacheness/storage/backends/postgresql_lifecycle_authority.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/blob_store.py
  - tests/contracts/test_lifecycle_authority.py
  - tests/contracts/test_postgresql_lifecycle_authority.py
  - tests/test_blob_store_atomic_lifecycle.py
autonomous: true
requirements: [MIGR-05, MIGR-06]
gap_closure: true
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A caller-supplied deterministic operation ID is validated once and replays only the exact canonical lifecycle mutation already owned by memory, SQLite, or PostgreSQL authority state (MIGR-05, MIGR-06; D-19, D-20)."
    - "After canonical promotion succeeds but the caller loses the response, retry returns the original BlobReceipt without a new generation, payload write, authority revision, or derived projection effect (MIGR-05; D-19, D-20)."
    - "Replay never infers ownership from a destination key, listing, path, timing, or caller receipt and introduces no coordinator, queue, lease, lock, entry adoption, or second authority (D-20; ADR 0001)."
  artifacts:
    - path: src/cacheness/storage/lifecycle_authority.py
      provides: "Exact operation-record replay contract owned by the existing canonical lifecycle authority"
    - path: src/cacheness/storage/lifecycle.py
      provides: "Operation-ID idempotent put completion across prepared, externally published, verified, and promoted boundaries"
    - path: src/cacheness/storage/blob_store.py
      provides: "Explicit deterministic operation identity seam for stopped-worker rebuild writes"
    - path: tests/contracts/test_lifecycle_authority.py
      provides: "Memory and SQLite exact operation-replay parity contracts"
    - path: tests/contracts/test_postgresql_lifecycle_authority.py
      provides: "Deterministic PostgreSQL exact operation-replay contract"
    - path: tests/test_blob_store_atomic_lifecycle.py
      provides: "End-to-end response-loss replay proof through BlobStore"
  key_links:
    - from: src/cacheness/storage/lifecycle.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "read the exact operation record and idempotently finish or return its promoted result; the authority mutation row remains the only destination truth"
      pattern: "read_mutation|prepare_mutation|promote_mutation"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/lifecycle.py
      via: "validated optional operation identity reaches the canonical engine and promoted replay is frozen into the original BlobReceipt"
      pattern: "operation_id|put_entry|AuthorityLifecycleEngine"
    - from: memory, SQLite, and PostgreSQL lifecycle authorities
      to: src/cacheness/storage/lifecycle_authority.py
      via: "one bounded side-effect-free read_mutation semantic reconstructs absent, prepared, verified-prepromotion, or promoted state"
      pattern: "read_mutation|MutationReplay"
---

<objective>
Close the canonical operation-intent and idempotent-replay prerequisite for interrupted rebuild recovery.

Purpose: Give stopped-worker rebuild orchestration one backend-neutral way to retry an exact destination mutation after response loss while preserving ADR 0001's single-authority model.
Output: One immutable operation replay contract, authority parity across memory/SQLite/deterministic PostgreSQL, the BlobStore/lifecycle operation-ID seam, and focused contract tests.
</objective>

## Artifacts this phase produces

- `MutationReplay(prepared, state, verification, promotion)` plus `LifecycleAuthority.read_mutation(operation_id)` in the existing lifecycle-authority contract, implemented by memory, SQLite, and PostgreSQL authorities.
- Keyword-only `operation_id` forwarding through `BlobStore.put_entry`, `BlobStore._put_with_result_admitted`, and `AuthorityLifecycleEngine.put`, returning the original canonical result after response loss.
- Exact tests named `test_blobstore_operation_id_retry_returns_committed_receipt_after_response_loss`, `test_local_authorities_expose_exact_operation_replay_without_new_authority_state`, and `test_postgresql_read_mutation_returns_exact_prepared_and_promoted_replay`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-09-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-12-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-13-SUMMARY.md
@src/cacheness/storage/lifecycle_authority.py
@src/cacheness/storage/lifecycle.py
@src/cacheness/storage/blob_store.py
</context>

<interfaces>
`BlobStore.put_entry()` currently returns `BlobReceipt(operation_id, key, generation, locator, expectation, catalog_revision, projections)` but accepts no caller-supplied operation identity. `AuthorityLifecycleEngine.put()` currently generates a fresh operation ID immediately before `LifecycleAuthority.prepare_mutation(MutationSpec)`; memory, SQLite, and PostgreSQL authorities already persist exact operation-keyed mutation records and make prepare/promotion replay-safe for the same exact spec. This plan adds one bounded read primitive for that existing record, not a second authority.
</interfaces>

<planner_assumptions>
- MIGR-05 and MIGR-06 need an exact retry seam before rebuild evidence can persist and resume destination writes; incidental destination keys never prove progress.
- The response-loss repair remains wholly inside the existing lifecycle contract: the single canonical destination authority records the exact `MutationSpec` before immutable payload publication and classifies retry per D-19 and D-20.
- No coordinator, queue, lease, lock, second authority, listing scan, or entry-adoption path is permitted by ADR 0001.
- The obstore spike validates the same prepared-intent -> immutable deterministic locator -> digest-and-size observation -> authority-promotion sequence, but this plan implements the missing replay seam against the existing `BlobStore` participant contract. It introduces no obstore dependency or publication-policy choice.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replay one response-lost BlobStore put through the canonical memory authority</name>
  <reversibility rating="costly">The canonical operation-replay primitive becomes a shared lifecycle-authority contract used by rebuild recovery.</reversibility>
  <files>src/cacheness/storage/lifecycle_authority.py, src/cacheness/storage/memory_lifecycle_authority.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_atomic_lifecycle.py</files>
  <read_first>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/memory_lifecycle_authority.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_blob_store_atomic_lifecycle.py</file>
    <file>.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md</file>
  </read_first>
  <behavior>
    - "`BlobStore.put_entry(..., operation_id=...)` forwards one validated deterministic ID into `AuthorityLifecycleEngine.put`; omitting it preserves fresh-ID behavior."
    - "After a fault raised immediately after the memory authority has promoted the operation but before the caller receives its receipt, retrying with the same ID returns the original canonical `BlobReceipt` without creating a generation, rewriting bytes, or changing authority revision."
    - "`read_mutation(operation_id)` returns the exact immutable prepared or promoted operation record without changing entry visibility or cleanup debt; a different exact spec cannot reuse the ID."
  </behavior>
  <action>Write `test_blobstore_operation_id_retry_returns_committed_receipt_after_response_loss` first against the memory/memory topology, injecting the fault after canonical promotion and before `BlobStore.put_entry` returns. Add one bounded immutable `MutationReplay` value in `lifecycle_authority.py` carrying the exact `PreparedMutation`, persisted state (`prepared` or `promoted`), optional verification proof, and optional canonical `PromotionResult`, with invariants that operation ID/spec/proof/result corroborate. Add `LifecycleAuthority.read_mutation(operation_id) -> MutationReplay | None` as the smallest transactional read primitive needed by the deep lifecycle engine and implement it in `InMemoryLifecycleAuthority` from its existing operation-keyed map. Add a keyword-only optional `operation_id` to `BlobStore.put_entry`, `_put_with_result_admitted`, and `AuthorityLifecycleEngine.put`; validate it through `MutationSpec`'s bounded identifier contract and use it in place of a fresh UUID only for that call. Before creating a new spec, read that exact authority operation. For a promoted replay, return the canonical promotion as the same receipt without another payload or derived-projection effect. For a prepared replay, reuse only the persisted never-reused generation locator, signed manifest, expectation, digest, and byte size; observe the exact locator through the current participant and accept it only when digest plus size match, otherwise publish only after exact absence, then finish verification/promotion. Reject operation/spec/key/expectation mismatches and any prepared candidate/manifest disagreement. Preserve exact-spec collision rejection, and never infer completion from a key, listing, path timing, or caller-supplied receipt. Keep object operations outside the authority transaction and do not add obstore or claim combined metadata-plus-payload atomicity. This is the production-quality single-path tracer for D-19/D-20 and ADR 0001's existing authority.</action>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_blob_store_atomic_lifecycle.py::test_blobstore_operation_id_retry_returns_committed_receipt_after_response_loss -x -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>The named end-to-end test proves an exact response-lost put returns its original receipt on retry with no second generation, payload write, authority revision, projection effect, or lifecycle authority.</acceptance_criteria>
  <done>One real BlobStore path survives the post-commit response-loss boundary through its canonical operation identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the same exact operation replay in SQLite and PostgreSQL authorities</name>
  <files>src/cacheness/storage/sqlite_lifecycle_authority.py, src/cacheness/storage/backends/postgresql_lifecycle_authority.py, tests/contracts/test_lifecycle_authority.py, tests/contracts/test_postgresql_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/sqlite_lifecycle_authority.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>tests/contracts/test_lifecycle_authority.py</file>
    <file>tests/contracts/test_postgresql_lifecycle_authority.py</file>
  </read_first>
  <behavior>
    - "SQLite `read_mutation` returns exact absent, prepared, verified-prepromotion, and promoted replay values from its existing mutation/entry/debt tables without changing state."
    - "PostgreSQL exposes the identical semantic replay through one bounded read-only driver transcript and reconstructs its canonical promoted result without live-service claims."
    - "Both durable authorities reject malformed rows and operation/spec disagreement through the existing typed fail-closed error taxonomy."
  </behavior>
  <action>Write `test_local_authorities_expose_exact_operation_replay_without_new_authority_state` for SQLite and `test_postgresql_read_mutation_returns_exact_prepared_and_promoted_replay` first. Implement `SqliteLifecycleAuthority.read_mutation` and `PostgresqlLifecycleAuthority.read_mutation` against their existing mutations/entries/cleanup-debt tables, using the same bounded row decoders as prepare/verification/promotion. An absent operation returns `None`; prepared and verified-prepromotion rows return their exact `MutationReplay`; promoted rows include the canonical `PromotionResult` needed by the lifecycle tracer without reissuing the external effect. Reads must be side-effect-free and bounded by one exact operation ID, with PostgreSQL covered through deterministic adapter transcripts only. Reject malformed state, proof, entry, debt, or spec corroboration and preserve topology-specific typed progress outcomes. Do not add tables, queues, leases, list scans, or another lifecycle interface beyond the Task 1 primitive.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/contracts/test_lifecycle_authority.py::test_local_authorities_expose_exact_operation_replay_without_new_authority_state tests/contracts/test_postgresql_lifecycle_authority.py::test_postgresql_read_mutation_returns_exact_prepared_and_promoted_replay -x</automated>
  </verify>
  <acceptance_criteria>The named contracts prove memory, SQLite, and PostgreSQL authorities classify the same exact operation states without payload discovery, state mutation, or a duplicate authority seam.</acceptance_criteria>
  <done>The BlobStore tracer's operation replay is backend-neutral across all canonical authority implementations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller operation identity -> canonical destination authority | A retry may address one exact persisted mutation but cannot create a second source of truth. |
| authority replay record -> BlobStore receipt | Malformed or mismatched replay state must not impersonate the original canonical result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-14-01 | Elevation of Privilege | Operation replay and entry adoption | high | mitigate | Resolve only an exact caller operation ID through the canonical authority record, corroborate its immutable spec/proof/result, and never treat key, listing, path, timing, or a caller receipt as completion evidence. |
</threat_model>

<verification>
Run the named local/PostgreSQL operation-replay and BlobStore response-loss tests plus the complete lifecycle-authority and BlobStore lifecycle suites. Verify exact replay safety, side-effect-free authority reads, backend parity, and the ADR authority boundary separately.
</verification>

<success_criteria>
- A retry after canonical destination commit returns the original receipt without another generation, payload publication, revision, or projection effect.
- Memory, SQLite, and deterministic PostgreSQL expose the same exact read-only operation classification through the existing authority.
- Different specs cannot reuse an operation ID, and malformed or mismatched replay rows fail closed.
- No coordinator, queue, lease, lock, listing scan, entry adoption, or second authority is introduced.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-14-SUMMARY.md` when done.
</output>
