---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "11"
type: execute
wave: 8
depends_on: [07-10]
files_modified:
  - tools/verify_phase7_contracts.py
  - tests/test_phase7_contract_verifier.py
  - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
autonomous: true
requirements: [MIGR-03, MIGR-04, MIGR-05, MIGR-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fixed Phase 7 verifier executes every migration/rebuild contract module and audits the exact production/public/documentation inventory for one authority, explicit-only maintenance, bounded evidence/inventory, handler ownership, projection separation, and remote non-claims."
    - "All MIGR-03 through MIGR-06 behaviors and D-01 through D-22 have named executable evidence, including custom handlers, every interruption boundary, evidence tampering/redaction, rollback/finalize/purge/abort, and memory/SQLite/non-live PostgreSQL-S3 tiers."
    - "The full deterministic local suite and scoped Phase 7 Ruff gate pass in the locked environment; live PostgreSQL/AWS S3, full platform/version qualification, and performance distributions remain Phase 8 rather than skips-as-passes."
    - "The four spec-less flagged assumptions and eight descriptor-less prohibitions from Plan 01 have exact no-silent-drop coverage in the final verifier and validation ledger."
  artifacts:
    - path: tools/verify_phase7_contracts.py
      provides: "Fixed Phase 7 behavioral, architecture, decision, prohibition, API coverage, and evidence-security verifier"
    - path: tests/test_phase7_contract_verifier.py
      provides: "Adversarial self-tests for omissions and every forbidden architecture/claim"
    - path: .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
      provides: "Final Nyquist mapping and exact execution evidence"
  key_links:
    - from: tools/verify_phase7_contracts.py
      to: .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
      via: "Fixed manifest maps every task/requirement/decision/threat/assumption/prohibition to exact tests"
      pattern: "MIGR-03|D-22|T-07"
    - from: tests/test_phase7_contract_verifier.py
      to: tools/verify_phase7_contracts.py
      via: "Adversarial temporary fixtures exercise shared AST/document/manifest validators independently of the live tree"
      pattern: "ast.parse|verify"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] The verifier may not derive scope from git diff, test discovery, comments/strings alone, or a mutable latest-run/service observation."
    - "[FLAGGED/UNVERIFIED] Missing optional/live services may not become a green skip or Phase 7 PostgreSQL/AWS S3 qualification."
    - "[FLAGGED/UNVERIFIED] Ruff baselines may not conceal new Phase 7 findings, and full-suite failures may not be reclassified as success."
---

<objective>
Close Phase 7 with a fixed, adversarially self-tested validation and security gate.

Purpose: Prove the complete operator lifecycle and architecture constraints without deriving acceptance from a diff or overstating remote/performance qualification.
Output: Fixed verifier, mutation/self-tests, updated Nyquist ledger, full deterministic local evidence, and scoped Ruff evidence.
</objective>

## Artifacts this phase produces

- `tools/verify_phase7_contracts.py` with fixed `PHASE7_PRODUCTION_PATHS`, `PHASE7_TEST_NODES`, requirement/decision/threat mappings, architecture AST rules, documentation/coverage checks, and `--quick`/`--all` execution modes.
- `tests/test_phase7_contract_verifier.py` with adversarial source, plan-manifest, documentation, coverage, omission, secret, unbounded-operation, candidate-adoption, and false-qualification fixtures.
- Final `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md` with concrete task IDs, exact commands/results/environment, Nyquist status, and explicit Phase 8 non-claims.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-10-SUMMARY.md
@tools/verify_phase6_contracts.py
@tests/test_phase6_contract_verifier.py
</context>

<interfaces>
Follow the repository's fixed-inventory AST verifier pattern: production paths and test nodes are literal reviewed tuples, adversarial tests call the same visitors/checks as repository mode, and comments/strings/historical artifacts do not satisfy or fail executable constraints. Keep behavioral tests authoritative for semantics and AST/document checks as architecture/coverage backstops.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the fixed Phase 7 contract and architecture verifier</name>
  <files>tools/verify_phase7_contracts.py, tests/test_phase7_contract_verifier.py</files>
  <read_first>
    <file>tools/verify_phase6_contracts.py</file>
    <file>tests/test_phase6_contract_verifier.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-01-PLAN.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
  </read_first>
  <behavior>
    - "Removing any required production path, exact test node, MIGR ID, D-01..D-22 mapping, threat, flagged assumption, or prohibition causes the verifier self-test to fail."
    - "Adversarial executable fixtures detect maintenance reachability from constructors/initialize/UnifiedCache/reconciliation, second authority or pointer/listing adoption, new lock/queue/lease/sidecar coordination, unsafe handler ordering, unbounded pages/evidence, hidden exclusions, and false remote qualification."
    - "Benign comments/strings and historical source artifacts do not trigger executable AST gates."
  </behavior>
  <action>Create a root-safe fixed verifier whose reviewed manifest names every Phase 7 production/public/doc artifact and exact focused test nodes from Plans 01-10. Map all four MIGR requirements, D-01 through D-22, eight security contribution threats, the four spec-less assumptions, and exactly the eight Plan 01 prohibitions to semantic tests. Add AST/data-flow rules that fail on maintenance calls from ordinary BlobStore/UnifiedCache/reconciliation paths; candidate/listing/projection authority; new concurrency/sidecar machinery; handler reads reachable before authentication/integrity verification; unbounded inventory/evidence; key/credential literals flowing to outputs; and automatic/remote qualification claims. Use adversarial temporary fixtures to prove every rule and omission check. Run the external-API detector/07-COVERAGE check and schema-push-not-applicable declaration as explicit verifier inputs rather than fabricating gates.</action>
  <acceptance_criteria>The verifier and its self-tests fail for every omitted requirement/decision/threat/assumption/prohibition and every constructed architecture/security regression, pass the clean repository, and use fixed scope rather than diff/discovery.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_phase7_contract_verifier.py -x -o log_cli=false &amp;&amp; uv run --frozen python tools/verify_phase7_contracts.py --quick</automated></verify>
  <done>The phase has one reproducible, independently self-tested acceptance command with no silent coverage drop.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the deterministic phase gate and seal Nyquist evidence</name>
  <files>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md</files>
  <read_first>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md</file>
    <file>tools/verify_phase7_contracts.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md</file>
    <file>.planning/phases/06-unifiedcache-policy-composition/06-11-SUMMARY.md</file>
  </read_first>
  <action>Run the fixed `--all` verifier, all Phase 7 migration/rebuild/public/verifier tests, the full deterministic non-live repository suite in the locked all-extras/dev environment, and Ruff on every Phase 7-created/modified Python path. Record exact commands, interpreter/dependency environment, exit status, counts, and failures in `07-VALIDATION.md`; update every task ID and Wave 0 file status, then set `wave_0_complete` and `nyquist_compliant` true only when all required evidence is green. Keep integrity, recovery, progress, and performance sections separate. Record live PostgreSQL/AWS S3, platform/version matrix, and performance distributions as Phase 8 unqualified work rather than skips or Phase 7 failures.</action>
  <acceptance_criteria>The exact focused/full/ruff commands exit zero, all task/requirement/decision/security mappings contain executable evidence, no required row is silently absent, and validation accurately preserves Phase 8 non-claims.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --all</automated></verify>
  <done>Phase 7 has complete deterministic local, Nyquist, architecture, security, and lint evidence with remote/performance claims honestly excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository code/plans/tests -> release verifier | Mutable implementation and tests must not edit, discover, or weaken the acceptance inventory. |
| verifier output -> Phase 7 completion claim | Local/deterministic success must not imply live service, platform, or performance qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-46 | Tampering | verifier scope | high | mitigate | Fixed literal artifact/test/mapping inventory plus omission mutation tests and root-safe path validation. |
| T-07-47 | Spoofing | completion/support evidence | high | mitigate | Exact command/environment/result ledger; unavailable live services remain Phase 8 and cannot produce green status. |
| T-07-48 | Information Disclosure | verifier output | high | mitigate | Scan structured outputs/logs for injected secret sentinels and prohibit raw key/credential/path values. |
| T-07-49 | Denial of Service | verifier/test inventory | medium | mitigate | Focused quick mode plus bounded deterministic all mode; performance measurements remain separate. |
| T-07-50 | Tampering | security/decision coverage | high | mitigate | Exact no-silent-drop equality for D-01..D-22, MIGR IDs, threats, four assumptions and eight prohibitions. |
</threat_model>

<verification>
The fixed `--quick` command provides sub-minute task feedback; `--all` is the phase gate and may run longer without becoming a runtime deadline. Integrity, recovery and progress tests are distinct; performance is only a Phase 8 evidence category. PostgreSQL/S3 behavior here is deterministic adapter coverage, not live qualification.
</verification>

<success_criteria>
Every planned Phase 7 artifact and behavior is covered by fixed executable evidence, all source/decision/prohibition rows are present, full non-live tests and scoped Ruff pass, and no unsupported remote/platform/performance claim is emitted.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-11-SUMMARY.md` with exact commands/results and Phase 8 non-claims.
</output>
