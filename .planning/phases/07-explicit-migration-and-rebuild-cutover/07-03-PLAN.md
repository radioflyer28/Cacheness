---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "03"
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/migration_evidence.py
  - tests/test_migration_plan_contract.py
  - tests/test_migration_inspection.py
  - tests/test_stored_compatibility.py
autonomous: true
requirements: [MIGR-03, MIGR-04, MIGR-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Compatibility is decided independently for store layout, authority kind/capability/schema, signed manifest schema, handler payload identity/version, and catalog schema transformation; no aggregate version implies another dimension (D-03)."
    - "The current canonical layout is the first release baseline, the default support policy accepts current and immediately previous released steps only, and no historical development layout or manufactured target version is added (D-01, D-02, D-05)."
    - "Inspection remains non-mutating outside the supported window and produces a rebuild-only/refused migration plan with no force override (D-04)."
    - "One immutable plan model produces deterministic bounded JSON and a human report with the same identities, totals, classifications, reasons, actions, and state (MIGR-03; D-06, D-18)."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "ReleaseWindow, VersionEdge, CompatibilityMatrix, complete disposition/reason vocabulary, inspection and plan rendering"
    - path: tests/test_migration_plan_contract.py
      provides: "Exact canonical JSON/human rendering and compatibility matrix contracts"
    - path: tests/test_migration_inspection.py
      provides: "Current/historical/unsupported non-mutating classification tests"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/manifest.py
      via: "Inspection reads independently versioned authenticated manifest dimensions"
      pattern: "manifest_schema_version|payload_format_version|store_format_version"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/handlers.py
      via: "Payload classification resolves against the source store's registered handlers"
      pattern: "resolve_payload_contract"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "Machine JSON and human report render from one validated MigrationPlan"
      pattern: "MigrationPlan"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Do not create compatibility readers for pre-refactor/transitional layouts, a force-migrate override, or a persisted version solely for test demonstration."
    - "[FLAGGED/UNVERIFIED] Do not compare only a top-level store integer or use SQLite schema identity for PostgreSQL."
    - "[FLAGGED/UNVERIFIED] Do not maintain separate machine and human plan models or discard unknown authenticated catalog attributes."
---

<objective>
Publish the exact supported version/compatibility contract and deterministic non-mutating plan model.

Purpose: Make eligibility explainable and future-release disciplined before adding physical topology work.
Output: Independent compatibility dimensions, stable entry dispositions/reasons, deterministic machine/human plans, and historical rebuild-only inspection.
</objective>

## Artifacts this phase produces

- `ReleaseWindow(current_release, immediately_previous_release)` and `VersionEdge` with explicit source/target values for each persisted dimension.
- `CompatibilityDimension`, `CompatibilityOutcome`, `CompatibilityMatrix`, `MigrationDisposition`, `MigrationReason`, `MigrationEntryAssessment`, `MigrationTotals`, and the expanded `MigrationPlan` fields `plan_version`, `plan_id`, `plan_kind`, `source_identity`, `source_revision`, `destination_identity`, `release_window`, `compatibility`, `entries`, `totals`, `intended_actions`, `exclusions`, `state`, and `digest`.
- `MigrationPlan.to_canonical_bytes()` and `render_migration_report(plan)` from the same model.
- `tests/test_migration_plan_contract.py`, `tests/test_migration_inspection.py`, and current-store additions to `tests/test_stored_compatibility.py`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-01-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-02-SUMMARY.md
@src/cacheness/storage/catalog.py
@src/cacheness/storage/manifest.py
@src/cacheness/handlers.py
</context>

<interfaces>
Current exact identifiers include `STORE_FORMAT_VERSION`, signed `BlobManifest` schema/version fields, `SQLITE_USER_VERSION`, and `POSTGRESQL_AUTHORITY_SCHEMA_VERSION` plus `POSTGRESQL_AUTHORITY_CAPABILITY`. `HandlerRegistry.resolve_payload_contract(handler_type, payload_format, payload_format_version)` resolves against the store-local registry without opening bytes. Preserve original authenticated `catalog_values` and transform only fields named by an explicit catalog edge.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the release window and independent compatibility matrix</name>
  <precondition>07-02 Task 1 recorded the blocking D-02 decision exactly as locked before this published promise is implemented.</precondition>
  <files>src/cacheness/storage/migration.py, tests/test_migration_plan_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest.py</file>
    <file>src/cacheness/storage/catalog.py</file>
    <file>src/cacheness/storage/backends/postgresql_lifecycle_authority.py</file>
    <file>src/cacheness/handlers.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-02-SUMMARY.md</file>
  </read_first>
  <behavior>
    - "Default production policy declares the current canonical layout as the first baseline and directly supports only current plus immediately previous released edges; older released sources require successive declared steps."
    - "Changing any one persisted dimension without an explicit directed edge changes eligibility without inferring compatibility from other dimensions."
    - "Tests inject synthetic source/target identities and edges without changing any production constant or on-disk schema."
  </behavior>
  <action>After the recorded D-02 approval, implement D-01 through D-05 as immutable compatibility data. Model store layout, authority kind/capability/schema, manifest schema, handler type/native payload format/version, and application catalog schema/transformation separately. The default registry declares the current baseline and the bounded direct-support rule but contains no invented historical or next-release edge. Permit tests and future releases to register exact directed edges; reject duplicates, gaps, ambiguous wildcards, unsupported older direct jumps, and attempts to infer PostgreSQL identity from SQLite fields. Exact-compatible payload contracts can be byte-preserving only when the resolved handler says so; transformations remain handler-declared. Do not alter current persisted constants.</action>
  <acceptance_criteria>The compatibility matrix deterministically classifies supported, rebuild-only, blocked, and unverifiable combinations across every independent dimension, with current production identifiers unchanged and D-02 approval traceable.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_plan_contract.py -x -o log_cli=false</automated></verify>
  <done>The release window and compatibility matrix are explicit, bounded, topology-neutral, and contain no fabricated migration target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Produce deterministic non-mutating inspection plans and reports</name>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_evidence.py, tests/test_migration_plan_contract.py, tests/test_migration_inspection.py, tests/test_stored_compatibility.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_stored_compatibility.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/migration_evidence.py</file>
  </read_first>
  <behavior>
    - "An initialized current store is recognized through authority-owned identity without requiring a hand-created marker."
    - "Historical/transitional and outside-window stores are inspectable as rebuild-only, while their migration mutation action is absent/refused and no force flag exists."
    - "Canonical JSON round-trips byte-for-byte and the human report contains values rendered from the same validated object."
  </behavior>
  <action>Implement D-04, D-06, D-18 and MIGR-03 inspection over the authority identity and the compatibility matrix. Create stable lower-snake-case reason codes for every disposition, exact aggregate counts/bytes by disposition and total, explicit source/destination identities, and intended action sequence. Keep the model bounded and canonically encoded with duplicate-key/non-canonical-number rejection. Render the report exclusively from the validated `MigrationPlan`; do not store independent report state. Preserve unknown authenticated catalog values unchanged in assessments. Extend compatibility tests so a real initialized SQLite authority is current without `store-format.json`; retain exact read-only historical inspection and byte-tree comparisons.</action>
  <acceptance_criteria>Current, unsupported, historical, corrupt, and unverifiable fixtures yield deterministic complete plans/reports; unsupported mutation is impossible; repeated inspection changes no store/work/key bytes or SQLite data version.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_plan_contract.py tests/test_migration_inspection.py tests/test_stored_compatibility.py -x -o log_cli=false</automated></verify>
  <done>The operator receives one canonical plan with a faithful human rendering and exact, stable eligibility explanations without mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted version/manifest/catalog -> compatibility planner | Authenticated but independently versioned fields may be unknown, malformed, or intentionally ambiguous. |
| canonical JSON -> human report | The report must not diverge from, omit, or reinterpret machine-authoritative plan state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-08 | Tampering | compatibility matrix | high | mitigate | Require exact directed edges for every changed dimension and fail closed on unknown/ambiguous values. |
| T-07-09 | Repudiation | report rendering | medium | mitigate | Render from the validated canonical plan and include plan digest, identities, totals, reasons, and intended actions. |
| T-07-10 | Denial of Service | plan JSON/catalog values | medium | mitigate | Apply byte/depth/node/collection/text bounds before allocation or rendering. |
| T-07-11 | Information Disclosure | plan/report | high | mitigate | Serialize provider identity/fingerprint only; exclude key bytes, raw DSNs, credentials, and secret paths. |
</threat_model>

<verification>
Run contract and non-mutation tests. Integrity covers independent version classification and exact bytes; recovery is not mutated here; progress is bounded model parsing/rendering; performance remains a separate Phase 8 concern.
</verification>

<success_criteria>
The D-01 through D-06 version/inspection contract is executable, current persisted versions remain unchanged, unsupported sources are rebuild-only without override, and human/machine output cannot drift.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-03-SUMMARY.md` when done.
</output>
