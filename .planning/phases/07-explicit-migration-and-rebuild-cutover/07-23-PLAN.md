---
phase: 07-explicit-migration-and-rebuild-cutover
plan: 23
type: execute
wave: 19
depends_on:
  - 07-22
files_modified:
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/migration_evidence.py
  - tests/test_rebuild_workflow.py
  - tools/verify_phase7_contracts.py
  - tests/test_phase7_contract_verifier.py
autonomous: true
requirements:
  - MIGR-05
gap_closure: true
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Authenticated rebuild cleanup debt fences direct `stage_rebuild()`, `verify_rebuild()`, and `accept_rebuild()` before payload access or lifecycle-authority/evidence mutation and directs the operator to explicit `resume()` settlement (MIGR-05; D-16, D-19, D-21)."
    - "`resume()` dispatches cleanup only when authenticated evidence is in `REBUILDING` or `REBUILD_VERIFYING`; debt in `REBUILD_ACCEPTED` or any other non-cleanup state fails closed before an external payload effect (MIGR-05; D-19, D-20, D-21)."
    - "`MaintenanceRunEvidence` cannot represent `REBUILD_ACCEPTED` with cleanup debt, while exact receipt-bound debt in a defined cleanup state still settles idempotently through the existing lifecycle authority (MIGR-05; D-17, D-20)."
    - "The fixed verifier maps the exact forward-fence/resume regression to MIGR-05, A-MIGR05, D-16/D-19/D-20/D-21, the applicable Plan 21 terminal-state threat, and every Plan 23 threat, so the lifecycle invariant cannot false-green."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "Existing coordinator methods reordered around one private rebuild-cleanup-debt guard and state-limited resume settlement"
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Fail-closed REBUILD_ACCEPTED cleanup-debt invariant"
    - path: tests/test_rebuild_workflow.py
      provides: "Authentic-debt forward-fence, pre-effect terminal rejection, exact resume settlement, and evidence-model regressions"
    - path: tools/verify_phase7_contracts.py
      provides: "Literal Plan 23, 54-gap-threat/100-total-threat inventory and exact MIGR-05/decision/threat selector mappings"
    - path: tests/test_phase7_contract_verifier.py
      provides: "Adversarial self-test for the exact rebuild cleanup-debt lifecycle selector and Plan 23 threat ownership"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "each public forward method authenticates current evidence and rejects cleanup debt before source/destination or evidence mutation; resume validates state before calling `_settle_rebuild_cleanup_debt`"
      pattern: "stage_rebuild|verify_rebuild|accept_rebuild|resume|cleanup_debt|REBUILD_ACCEPTED"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/lifecycle_authority.py
      via: "only explicit resume from a defined cleanup state reaches the existing receipt plus `read_mutation(operation_id)` settlement proof"
      pattern: "_settle_rebuild_cleanup_debt|read_mutation|operation_id"
    - from: tools/verify_phase7_contracts.py
      to: tests/test_rebuild_workflow.py
      via: "MIGR-05, A-MIGR05, D-16/D-19/D-20/D-21, T-07-21-03, and Plan 23 threats name the exact forward-fence/resume selector"
      pattern: "test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Ordinary construction, open, initialization, reads, cache policy, reconciliation, and cleanup remain unable to migrate, rebuild, adopt, activate, or purge a store."
    - "[FLAGGED/UNVERIFIED] Work-directory evidence, paths, object listings, payload presence, projections, locks, queues, leases, journals, sidecars, and process-local gates remain outside lifecycle authority."
    - "[FLAGGED/UNVERIFIED] Unauthenticated evidence, unexplained candidates, mismatched receipts, incomplete catalogs, and changed current ownership remain ineligible for adoption or cleanup authorization."
    - "[FLAGGED/UNVERIFIED] The repair makes no cross-resource ACID, perfect invisible-orphan reclamation, universal payload conversion, or identical topology-progress guarantee."
    - "[FLAGGED/UNVERIFIED] Production obstore adoption and live PostgreSQL/Amazon-S3, Windows, supported-Python, packaging, and performance qualification remain outside Phase 7."
---

<objective>
Fence rebuild cleanup debt at the existing maintenance coordinator and evidence seams before it can progress or trigger an external effect from terminal state.

Purpose: Close the remaining MIGR-05 loss path while preserving one lifecycle authority, exact receipt-bound explicit resume, and ADR 0001's topology-specific limits.
Output: One end-to-end production repair, exact regressions, and fixed-verifier ownership through Plan 23.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-PATTERNS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-21-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-21-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-22-PLAN.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-22-SUMMARY.md
@.codex/skills/spike-findings-cacheness/SKILL.md
@.codex/skills/spike-findings-cacheness/references/lifecycle-and-recovery.md
@src/cacheness/storage/migration.py
@src/cacheness/storage/migration_evidence.py
@tests/test_rebuild_workflow.py
@tools/verify_phase7_contracts.py
@tests/test_phase7_contract_verifier.py
</context>

<adr_0001_contract>
- Supported topology/evidence: deterministic stopped-worker memory/memory and SQLite/filesystem behavior; existing fake/Moto remote participant checks remain non-live. Phase 8 still owns live PostgreSQL/Amazon-S3, platform, Python-version, packaging, and performance qualification.
- Integrity: no direct forward rebuild action may carry cleanup debt toward acceptance, and terminal-state debt must fail before payload deletion or authority/evidence mutation.
- Recovery: exact immutable payload cleanup remains an external effect. Only authenticated receipt-bound evidence in `REBUILDING` or `REBUILD_VERIFYING` may enter explicit resume settlement, which corroborates the existing lifecycle-authority operation replay.
- Progress: a caller receives settled success, the existing typed operational retry outcome, or a typed offline-decision/evidence failure. No retry is guaranteed to succeed within a deadline.
- ACID scope: the selected lifecycle authority remains the only lifecycle state/visibility owner; payload deletion and maintenance evidence are not part of its transaction and do not become another authority.
- Stop conditions: add no lock, queue, lease, journal, sidecar, lifecycle state, listing/adoption source, online-worker protocol, or production obstore participant.
</adr_0001_contract>

<flagged_assumptions>
- `[FLAGGED/UNVERIFIED A-MIGR03]` The executed non-mutating inventory and canonical human/machine plan remain the Phase 7 MIGR-03 interpretation; this gap plan does not change or newly claim them.
- `[FLAGGED/UNVERIFIED A-MIGR04]` The executed destination-owned copy/verify/switch contract remains the Phase 7 MIGR-04 interpretation; cleanup-debt fencing does not alter migration compatibility or cutover.
- `[FLAGGED/UNVERIFIED A-MIGR05]` Authenticated run evidence plus exact existing lifecycle-authority replay is sufficient to settle attributed rebuild cleanup debt, provided only explicit resume can perform that settlement and terminal debt is rejected before effects.
- `[FLAGGED/UNVERIFIED A-MIGR06]` The executed registered-handler and destination-BlobStore rebuild remains the Phase 7 MIGR-06 interpretation; this repair adds no converter, backend, or alternative rebuild authority.
</flagged_assumptions>

<assumption_delta>
Decision: no-change. The primary noun remains the existing singular lifecycle authority; this gap neither generalizes its identity nor introduces another lifecycle authority.
</assumption_delta>

<interfaces>
`OfflineMigrationService.stage_rebuild()`, `verify_rebuild()`, and `accept_rebuild()` are the existing direct forward coordinator methods. `_settle_rebuild_cleanup_debt(plan, evidence)` is the existing exact receipt-bound settlement path, and `resume()` is its sole permitted dispatcher. Operational rebuild cleanup failures arise from `_abort_rebuild_after_failure()` while evidence is `REBUILDING` or `REBUILD_VERIFYING`. `MaintenanceRunEvidence.__post_init__()` already rejects debt-bearing terminal `ABORTED` evidence but currently permits `REBUILD_ACCEPTED` debt. The existing error surface includes `CacheBlobMigrationOfflineDecisionRequiredError` and `CacheBlobMigrationEvidenceMismatchError`; no new public exception or state is required.
</interfaces>

## Artifacts this phase produces

- No new production module, public API, evidence field, enum state, schema, backend participant, or lifecycle-authority method is created.
- One private coordinator guard may be added in `src/cacheness/storage/migration.py` to centralize the existing evidence-state/debt check; it does not own or persist lifecycle state.
- New exact test functions/selectors:
  - `tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts`
  - `tests/test_rebuild_workflow.py::test_rebuild_evidence_rejects_accepted_cleanup_debt`
  - `tests/test_phase7_contract_verifier.py::test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly`

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fence authentic rebuild debt end to end and bind it to the fixed verifier</name>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_evidence.py, tests/test_rebuild_workflow.py, tools/verify_phase7_contracts.py, tests/test_phase7_contract_verifier.py</files>
  <read_first>
    - src/cacheness/storage/migration.py: `_abort_rebuild_after_failure`, `_checkpoint_rebuild_cleanup_progress`, `_settle_rebuild_cleanup_debt`, `stage_rebuild`, `verify_rebuild`, `accept_rebuild`, and `resume`
    - src/cacheness/storage/migration_evidence.py: `MaintenanceEvidenceState`, `_LEGAL_TRANSITIONS`, and `MaintenanceRunEvidence.__post_init__`
    - tests/test_rebuild_workflow.py: existing authentic cleanup-debt fixture path, exact settlement tests, forward workflow tests, destination mutation spies, and evidence helpers
    - tools/verify_phase7_contracts.py: `PHASE7_PLAN_PATHS`, `MIGRATION_REQUIREMENT_NODES`, `DECISION_NODES`, `GAP_PLAN_THREAT_IDS`, `GAP_PLAN_THREAT_OWNERS`, `SECURITY_THREAT_NODES`, `FLAGGED_ASSUMPTION_NODES`, and `fixed_pytest_nodes`
    - tests/test_phase7_contract_verifier.py: fixed plan/threat counts, current repair mapping, removed-selector mutation, and false-PASS tests
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-VERIFICATION.md: authoritative remaining MIGR-05 gap and exact reproduced loss sequence
    - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-REVIEW.md: CR-01 direct-method fence and WR-01 exact verifier mapping
    - docs/adr/0001-topology-specific-storage-guarantees.md: sole authority, external effect, cleanup debt, recovery, and stop-condition rules
  </read_first>
  <behavior>
    - "Starting from authentic receipt-bound cleanup debt, each of `stage_rebuild()`, `verify_rebuild()`, and `accept_rebuild()` raises the existing typed offline-decision/evidence error with explicit resume guidance before destination payload access, lifecycle-authority revision/state change, or evidence-byte replacement."
    - "`resume()` invokes `_settle_rebuild_cleanup_debt()` only for `REBUILDING` or `REBUILD_VERIFYING`; a debt-bearing `REBUILD_STAGED`, `REBUILD_VERIFIED`, `REBUILD_ACCEPTED`, or unrelated state fails before destination access."
    - "Explicit resume from each permitted cleanup state authenticates the plan/run/evidence, settles only the exact recorded immutable receipts, checkpoints retirement, and reaches `ABORTED` only when all debt is empty; source data and any later destination owner remain unchanged."
    - "`MaintenanceRunEvidence` construction and canonical evidence loading reject `REBUILD_ACCEPTED` with cleanup debt before coordinator dispatch."
    - "The fixed verifier owns Plans 01-23, exactly 54 gap threats and 100 total Phase 7 threats, and maps the exact main regression to MIGR-05, A-MIGR05, D-16/D-19/D-20/D-21, T-07-21-03, T-07-23-01, and T-07-23-02; the evidence invariant and verifier self-test own T-07-23-03 and T-07-23-04."
  </behavior>
  <action>Write `test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts` and `test_rebuild_evidence_rejects_accepted_cleanup_debt` first. Produce cleanup debt through the existing authenticated receipt/failure path, not raw unsigned fixture data. For each direct forward method, place the same exact receipt/debt into the method's forward-ready authenticated evidence state, snapshot canonical evidence bytes, lifecycle-authority identity/revision, destination entry/locator identity, source entries, and participant call counts, then assert the existing typed error names explicit resume and every snapshot/counter is unchanged. Add or reuse one private coordinator guard that accepts already authenticated evidence; call it immediately after the single evidence read in `stage_rebuild()`, `verify_rebuild()`, and `accept_rebuild()` and before source revalidation, receipt validation, evidence transition, destination read/delete, or authority mutation. Reorder `resume()` so it first classifies evidence state: only `REBUILDING` and `REBUILD_VERIFYING` with debt may call `_settle_rebuild_cleanup_debt()`; debt in every other state raises before that helper or any payload participant call. Preserve the existing receipt plus `read_mutation(operation_id)` proof and operational retry behavior inside settlement. Extend `MaintenanceRunEvidence.__post_init__()` to reject `REBUILD_ACCEPTED` whenever `cleanup_debt` is nonempty, retaining the existing `ABORTED` retirement invariant. Prove permitted resume deletes or proves absence only for the exact recorded receipts and reaches debt-free `ABORTED`. Then add Plan 07-23 to the literal verifier inventory, append `T-07-23-01` through `T-07-23-04` to the gap threat inventory/owner map, and update exact counts from 50/96 to 54/100. Map the main selector to MIGR-05, A-MIGR05, D-16, D-19, D-20, D-21, `T-07-21-03`, `T-07-23-01`, and `T-07-23-02`; map the evidence selector to `T-07-23-03`; map `test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly` to `T-07-23-04`. Extend omission/rename/count/ownership self-tests so removing the plan row, any Plan 23 threat, or the exact behavior selector fails before pytest execution. Preserve the existing MIGR-03/MIGR-04/MIGR-06 selectors, all four flagged assumption keys, descriptor-less prohibitions, quick-mode optional PostgreSQL boundary, and unchanged `07-COVERAGE.md`. Do not add a new state, authority, persistence field, schema, lock, queue, lease, journal, sidecar, listing/adoption path, online writer coordination, cross-resource ACID claim, or production obstore integration.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts tests/test_rebuild_workflow.py::test_rebuild_evidence_rejects_accepted_cleanup_debt tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_stays_resumable_until_exact_settlement tests/test_rebuild_workflow.py::test_rebuild_cleanup_retry_preserves_changed_current_ownership tests/test_phase7_contract_verifier.py::test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly tests/test_phase7_contract_verifier.py::test_fixed_manifest_requires_exact_path_and_test_name_selectors tests/test_phase7_contract_verifier.py::test_fixed_manifest_includes_every_gap_plan_threat_exactly_once tests/test_phase7_contract_verifier.py::test_fixed_manifest_rejects_removed_current_gap_behavior_while_file_remains tests/test_phase7_contract_verifier.py::test_main_never_renders_failed_requirement_as_pass -x -o log_cli=false -o addopts=</automated>
  </verify>
  <acceptance_criteria>All three direct forward methods reject authentic cleanup debt with resume guidance before any payload/authority/evidence mutation; only defined nonterminal cleanup states reach exact resume settlement; REBUILD_ACCEPTED debt is invalid before effects; exact receipts settle without changing source or a later owner; and the fixed 23-plan/54-gap-threat/100-total-threat verifier cannot report MIGR-05 PASS without this selector.</acceptance_criteria>
  <done>The remaining verified MIGR-05 loss path is closed end to end at the existing maintenance coordinator, evidence, receipt, and verifier seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated maintenance evidence -> direct rebuild coordinator methods | Authentic debt is still a mandatory recovery obligation and cannot be carried into forward workflow states. |
| evidence state -> external cleanup participant | State validation must precede exact deletion or absence proof; terminal state cannot authorize cleanup. |
| exact receipt plus lifecycle-authority replay -> payload locator | Only the existing authority-correlated immutable generation can be settled. |
| regression selector -> fixed Phase 7 claim | An adjacent passing rebuild test cannot substitute for the direct-method fence and pre-effect resume ordering. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-23-01 | Tampering | Direct rebuild progression | high | mitigate | Authenticate evidence once and reject any cleanup debt before stage, verify, or accept can read/delete payloads or mutate authority/evidence. |
| T-07-23-02 | Tampering | Resume operation ordering | high | mitigate | Permit receipt settlement only from `REBUILDING` or `REBUILD_VERIFYING`; reject all other debt-bearing states before participant dispatch. |
| T-07-23-03 | Repudiation | Accepted maintenance evidence | high | mitigate | Make the evidence model reject `REBUILD_ACCEPTED` with debt and cover construction plus canonical loading with an exact test. |
| T-07-23-04 | Spoofing | Fixed MIGR-05 completion | high | mitigate | Bind the exact behavior selector to MIGR-05, applicable decisions/assumption/threats, literal Plan 23 ownership, and adversarial removal tests. |
</threat_model>

<verification>
- `uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_rebuild_workflow.py::test_rebuild_cleanup_debt_fences_forward_methods_and_resume_settles_exact_receipts tests/test_rebuild_workflow.py::test_rebuild_evidence_rejects_accepted_cleanup_debt tests/test_phase7_contract_verifier.py::test_fixed_manifest_maps_rebuild_cleanup_debt_fence_exactly -x -o log_cli=false -o addopts=` passes as the narrow deterministic safety/recovery gate.
- `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --quick` validates exact selectors, all 23 plans, 54 gap threats, 100 total threats, scoped Ruff, and the existing optional-PostgreSQL quick boundary.
- `uv run --isolated --all-extras --group dev --frozen python tools/verify_phase7_contracts.py --all` is the deterministic non-live Phase 7 completion gate. Its result must not be represented as live PostgreSQL/Amazon-S3, Windows, supported-Python, packaging, performance, or production-obstore qualification; those remain Phase 8 or later.
</verification>

<success_criteria>
- Direct stage, verify, and accept cannot progress authentic rebuild cleanup debt.
- Resume validates the evidence state before any external cleanup effect and settles debt only from the two states in which rebuild failure cleanup is produced.
- Accepted rebuild evidence cannot contain cleanup debt.
- Exact settlement preserves the source and any later destination owner and reaches `ABORTED` only after all recorded receipts retire.
- The fixed verifier cannot false-green MIGR-05 when the fence, state ordering, evidence invariant, plan row, or threat mapping is removed.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID/item | Plan | Status | Notes |
|---|---|---|---|---|
| GOAL | Explicit stopped-worker migration/rebuild without silent mutation or loss of the only valid copy | 07-01 through 07-23 | COVERED | 07-23 closes the only remaining verified accepted-entry deletion path. |
| REQ | MIGR-03 non-mutating human/machine inventory and plans | 07-01 through 07-22 | COVERED | Retained executed behavior; not newly claimed by 07-23. |
| REQ | MIGR-04 explicit same-backend offline copy/verify/switch | 07-01 through 07-22 | COVERED | Retained executed behavior; not newly claimed by 07-23. |
| REQ | MIGR-05 authenticated interruption recovery without loss/adoption | 07-23 | COVERED | Direct debt fence, state-first resume, evidence invariant, exact settlement, and fixed selector close the verified gap. |
| REQ | MIGR-06 explicit confirmed handler-backed rebuild | 07-09 through 07-22 | COVERED | Retained executed behavior; 07-23 protects its recovery edge without newly claiming MIGR-06. |
| RESEARCH | One lifecycle authority; immutable external effects; exact receipt/debt recovery; state-before-effect validation | 07-23 | COVERED | Uses the existing coordinator, authority replay, evidence, and participant seams only. |
| RESEARCH | Inventory, compatibility matrix, release window, handlers, cutover, retirement, projections, public surface | 07-01 through 07-22 | COVERED | No gap reopened and no source item omitted. |
| RESEARCH | Live/platform/performance qualification and production obstore | Phase 8 or later | EXCLUDED | Explicitly out of this deterministic gap closure. |
| CONTEXT | D-01 through D-15 | 07-01 through 07-22 | COVERED | Existing version, plan, incompatibility, cutover, rollback, and retirement decisions remain unchanged. |
| CONTEXT | D-16 external cleanup debt remains retryable and non-authoritative | 07-23 | COVERED | Debt cannot progress or trigger terminal-state deletion. |
| CONTEXT | D-17 through D-18 evidence is corroborative canonical JSON | 07-01 through 07-22 | COVERED | Existing evidence representation remains unchanged. |
| CONTEXT | D-19 explicit authenticated resume | 07-23 | COVERED | Resume is the only cleanup dispatcher and validates state first. |
| CONTEXT | D-20 fail closed on illegal/mismatched evidence | 07-23 | COVERED | Accepted debt is invalid at model and coordinator boundaries. |
| CONTEXT | D-21 exact owned cleanup only | 07-23 | COVERED | Settlement remains receipt/authority-correlated and preserves later ownership. |
| CONTEXT | D-22 signing material boundary | 07-01 through 07-22 | COVERED | No new evidence field or key-material path is introduced. |
| CONTEXT | Deferred ideas | — | EXCLUDED | None declared in 07-CONTEXT.md. |
| SPEC PROBE | MIGR-03, MIGR-04, MIGR-05, MIGR-06 edge coverage | 07-23 assumptions | COVERED | All four remain explicitly `unclassified`/`unresolved`; only MIGR-05 receives a new concrete acceptance truth from verified evidence. |
| PROHIBITIONS | Value/safety constraints from ADR 0001 and phase context | 07-23 | COVERED | Retained as descriptor-less `[FLAGGED/UNVERIFIED]` statements; no check descriptor is invented. |

<source_coverage>
All GOAL, REQ, RESEARCH, and CONTEXT items are covered by the existing executed plan set or the new 07-23 closure. The new plan claims only MIGR-05, introduces no deferred Phase 8/later work, and leaves 07-COVERAGE.md untouched.
</source_coverage>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-23-SUMMARY.md` when done.
</output>
