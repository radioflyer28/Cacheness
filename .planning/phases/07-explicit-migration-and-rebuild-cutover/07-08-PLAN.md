---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "08"
type: execute
wave: 5
depends_on: [07-02, 07-06]
files_modified:
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/migration_authority.py
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/sqlite_lifecycle_authority.py
  - tests/test_migration_cutover.py
autonomous: true
requirements: [MIGR-04, MIGR-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Offline rollback restores the retained prior verified selection only before any post-activation acknowledged writer transition; afterward rollback fails closed rather than discarding new writes (D-13)."
    - "Prior recovery material persists without timer/background/constructor/reconciliation/cache deletion until explicit operator action (D-14)."
    - "After recorded D-15 approval, finalize ends rollback eligibility without deleting bytes; later separately confirmed purge is idempotent and reports retryable cleanup debt without changing activation success (D-15, D-16)."
    - "Abort removes only a run-owned unactivated candidate and preserves source/audit evidence; after activation it refuses and points to rollback/finalize (D-21)."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "rollback, finalize, purge and abort operator actions with exact state transitions"
    - path: src/cacheness/storage/migration_authority.py
      provides: "RollbackReceipt and FinalizeReceipt authority contracts"
    - path: tests/test_migration_cutover.py
      provides: "Writer-restart, retention, finalize/purge/abort and retryable cleanup contracts"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/migration_authority.py
      via: "First acknowledged ordinary post-activation mutation marks rollback ineligible in the same authority transition"
      pattern: "rollback_eligible|activation"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "Explicit actions require matching authenticated run state and confirmations"
      pattern: "rollback|finalize|purge|abort"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No time-based, constructor, cache eviction, background, ordinary reconciliation, or projection cleanup may retire migration recovery material."
    - "[FLAGGED/UNVERIFIED] Finalize may not delete physical prior state; purge may not run without separate confirmation or rewrite activation on failure."
    - "[FLAGGED/UNVERIFIED] Rollback may not proceed after an acknowledged writer transition, and abort may not operate after activation."
---

<objective>
Complete offline rollback, final acceptance, candidate abort, and separately confirmed physical retirement.

Purpose: Preserve the last valid copy and prevent operator cleanup from being confused with canonical activation.
Output: Authority-backed rollback/finalize state, exact abort/purge ownership, retryable cleanup receipts, and deterministic lifecycle tests.
</objective>

## Artifacts this phase produces

- `RollbackReceipt`, `FinalizeReceipt`, `PurgeReceipt`, and `AbortReceipt` with exact run, plan, active/prior/candidate identities and idempotent state.
- `OfflineMigrationService.rollback()`, `finalize()`, `purge()`, and `abort()`.
- Authority-owned `rollback_eligible`/`finalized` state and the rule that the first acknowledged ordinary post-activation write ends rollback eligibility.
- Expanded local retirement/recovery cases in `tests/test_migration_cutover.py`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-02-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-06-SUMMARY.md
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/lifecycle.py
@tests/test_migration_cutover.py
</context>

<interfaces>
Plan 06 leaves an activated authority receipt plus retained prior verified selection and authenticated run evidence. Ordinary acknowledged writes already cross an authority promotion boundary; mark rollback ineligible there rather than adding worker coordination. Payload deletion remains an external exact-locator effect and failures must become attributable cleanup debt.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce offline rollback eligibility and explicit finalize</name>
  <precondition>07-02 Task 3 recorded the blocking D-15 decision before finalize can permanently end rollback eligibility.</precondition>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_authority.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/memory_lifecycle_authority.py, src/cacheness/storage/sqlite_lifecycle_authority.py</files>
  <read_first>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/migration_authority.py</file>
    <file>src/cacheness/storage/lifecycle.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-02-SUMMARY.md</file>
    <file>tests/test_migration_cutover.py</file>
  </read_first>
  <behavior>
    - "Rollback before writer restart atomically restores the exact prior selection and retains the displaced activated candidate as attributable state."
    - "The first successful ordinary post-activation authority mutation permanently clears rollback eligibility in that same authority transaction; no process/worker registry is consulted."
    - "Finalize is explicit, idempotent, requires D-15 confirmation and authenticated activated evidence, ends rollback, and performs no physical deletion."
  </behavior>
  <action>Implement D-13 through D-15 after the recorded decision gate. Add semantic authority rollback/finalize operations for memory and SQLite, corroborating exact activation/prior receipts and using one authority transaction. Integrate a minimal authority-owned marker into the ordinary mutation promotion path so the first acknowledged write after activation ends rollback eligibility atomically; do not attempt global worker detection or add leases/locks/sidecars. `rollback()` requires stopped-worker acknowledgement and exact activated evidence, and refuses after writer progress or finalize. `finalize()` requires its own explicit confirmation, records operator acceptance, is idempotent for the same receipt, and changes no payload bytes.</action>
  <acceptance_criteria>Rollback succeeds only in the offline pre-writer window, writer progress/finalize permanently blocks it, all authority faults roll back, and finalize leaves prior physical material intact.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_cutover.py tests/test_lifecycle_authority_contract.py -x -o log_cli=false</automated></verify>
  <done>Rollback eligibility and final acceptance are represented by the one authority without online coordination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Abort owned candidates and purge retained prior data separately</name>
  <precondition>07-02 Task 3 recorded the blocking D-15 decision covering the separately confirmed destructive purge.</precondition>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_evidence.py, src/cacheness/storage/blob_store.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>src/cacheness/storage/migration.py</file>
    <file>src/cacheness/storage/migration_evidence.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>tests/test_migration_cutover.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "Abort removes only exact candidate locators owned by the authenticated run before activation; source and evidence persist."
    - "Purge requires finalized state plus a distinct confirmation bound to exact prior identity/entries/counts/bytes, is idempotent, and never deletes a current locator."
    - "Injected deletion failure returns retryable cleanup debt; repeated purge resumes exact remaining work and activation remains successful."
  </behavior>
  <action>Implement D-16 and D-21 external cleanup. `abort()` authenticates run ownership, revalidates each candidate locator is unactivated/currently unowned, deletes or proves absence idempotently, preserves audit evidence, and refuses after activation with rollback/finalize guidance. `purge()` is callable only after finalize and a separate exact confirmation; enumerate retained prior locators from authority receipts/evidence, revalidate none is current, perform bounded deletion, and checkpoint retryable debt. Deletion failure must return a typed partial cleanup result and never alter the successful activation/finalize state. Tests cover foreign-run candidates, changed locators, replay, partial purge, response loss, and current-generation protection.</action>
  <acceptance_criteria>Abort/purge can touch only corroborated non-current run-owned locators, repeat safely, preserve audit/source as required, and expose retryable cleanup without redefining canonical success.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_cutover.py tests/test_migration_run_evidence.py -x -o log_cli=false</automated></verify>
  <done>The full activation-retirement lifecycle is explicit, recoverable, and non-destructive until the separately confirmed purge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ordinary writer transition -> rollback eligibility | Acknowledged post-activation writes must prevent stale rollback without a second coordination system. |
| finalized evidence -> destructive payload purge | Exact retained prior ownership crosses into irreversible external deletion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-31 | Tampering | rollback eligibility | critical | mitigate | Authority-owned marker changes atomically with first acknowledged writer transition; rollback uses exact receipt/state. |
| T-07-32 | Elevation of Privilege | abort/purge ownership | critical | mitigate | Authenticate run, corroborate exact non-current candidate/prior locators, and reject foreign/current identities before deletion. |
| T-07-33 | Repudiation | finalize/purge confirmation | high | mitigate | Bind distinct confirmations to exact plan/run/active/prior digest/count/bytes and persist receipts. |
| T-07-34 | Denial of Service | purge | medium | mitigate | Bounded batches and explicit continuation/debt; no retry-until-success or activation rollback on cleanup failure. |
</threat_model>

<verification>
Run local lifecycle/evidence tests with deterministic authority and payload faults. Integrity prevents rollback data loss/current deletion; recovery proves idempotent retry; progress permits typed cleanup continuation; performance stays outside correctness.
</verification>

<success_criteria>
Rollback, finalize, abort, and purge each have distinct explicit contracts; post-activation writes and finalize end rollback correctly; physical cleanup is exact, confirmed, idempotent, and unable to rewrite canonical success.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-08-SUMMARY.md` when done.
</output>
