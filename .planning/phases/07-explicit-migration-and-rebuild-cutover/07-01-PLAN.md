---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/cacheness/storage/migration.py
  - src/cacheness/storage/migration_authority.py
  - src/cacheness/storage/migration_evidence.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - tests/test_migration_cutover.py
  - tests/test_stored_compatibility.py
autonomous: true
requirements: [MIGR-03, MIGR-04, MIGR-05]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An operator can execute one production-quality memory/memory inspect -> plan -> stage -> verify -> activate path using an explicit run ID, separate work directory, and stopped-worker acknowledgement; no ordinary store path performs any step (MIGR-03, MIGR-04; D-12, D-17)."
    - "The tracer verifies every candidate entry before the authority publishes the whole candidate, and interruption before activation leaves the source as the only visible complete store (MIGR-04, MIGR-05; D-07, D-12)."
    - "A real initialized current store remains validation-only on construction/open/initialize, and historical or unsupported evidence remains byte-for-byte unchanged (MIGR-04; D-01, D-04, D-05)."
    - "Maintenance evidence coordinates only the offline run; candidate presence and the evidence file never authorize visibility (MIGR-05; D-17, D-20)."
  artifacts:
    - path: src/cacheness/storage/migration.py
      provides: "OfflineMigrationService and immutable tracer plan/result vocabulary"
      contains: "class OfflineMigrationService"
    - path: src/cacheness/storage/migration_authority.py
      provides: "Narrow whole-store maintenance protocol and corroborated receipts"
      contains: "class MigrationAuthority"
    - path: src/cacheness/storage/migration_evidence.py
      provides: "Versioned canonical maintenance envelope used by the tracer"
      contains: "class MaintenanceRunEvidence"
    - path: tests/test_migration_cutover.py
      provides: "End-to-end memory tracer and interruption assertions"
    - path: tests/test_stored_compatibility.py
      provides: "Ordinary-open and initialize no-mutation regression"
  key_links:
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_authority.py
      via: "OfflineMigrationService requests stage/verify/activate but only MigrationAuthority changes canonical selection"
      pattern: "MigrationAuthority|activate_verified_candidate"
    - from: src/cacheness/storage/migration.py
      to: src/cacheness/storage/migration_evidence.py
      via: "Every durable tracer boundary writes and rereads evidence bound to run and plan"
      pattern: "MaintenanceRunEvidence|plan_digest"
    - from: src/cacheness/storage/memory_lifecycle_authority.py
      to: src/cacheness/storage/migration_authority.py
      via: "Memory authority supplies the same-process whole-store activation primitive"
      pattern: "activate_verified_candidate"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No ordinary constructor, open, initialize, read, cache policy, reconciliation, or cleanup path may migrate, rebuild, adopt, activate, or purge a store."
    - "[FLAGGED/UNVERIFIED] No work-directory file, filesystem path, symlink, object listing, candidate presence, or derived projection may become lifecycle or cutover authority."
    - "[FLAGGED/UNVERIFIED] No new lock, queue, lease, sidecar, daemon, scheduler, online writer protocol, or second lifecycle state machine may be added."
    - "[FLAGGED/UNVERIFIED] No historical compatibility reader, manufactured persisted version, universal native payload converter, or force override may be introduced."
    - "[FLAGGED/UNVERIFIED] No partial candidate may activate, and activation/finalize may not physically delete the prior valid store."
    - "[FLAGGED/UNVERIFIED] No unauthenticated evidence, stale source fingerprint, unexplained candidate, or incomplete catalog may be adopted."
    - "[FLAGGED/UNVERIFIED] No signing key bytes, raw credentials, or secret provider paths may appear in plans, reports, evidence, or logs."
    - "[FLAGGED/UNVERIFIED] No unbounded inventory/evidence or Phase 8 live PostgreSQL/AWS S3/performance qualification claim may enter Phase 7."
---

<objective>
Prove the thinnest complete offline migration path before expanding version, topology, recovery, and rebuild behavior.

Purpose: Establish the one deep maintenance seam end to end while preserving the existing BlobStore lifecycle authority and no-implicit-upgrade contract.
Output: A real memory/memory inspect-plan-stage-verify-activate tracer, its narrow authority/evidence contracts, and negative ordinary-open coverage.
</objective>

## Artifacts this phase produces

- `src/cacheness/storage/migration.py`: `MigrationDisposition`, `MigrationReason`, `MigrationEntryAssessment`, `MigrationPlan`, `MigrationStepResult`, and `OfflineMigrationService`.
- `src/cacheness/storage/migration_authority.py`: `AuthorityIdentitySnapshot`, `AuthorityInventoryEntry`, `AuthorityInventoryPage`, `VerifiedCandidateReceipt`, `ActivationReceipt`, and `MigrationAuthority`.
- `src/cacheness/storage/migration_evidence.py`: `MaintenanceEvidenceState`, `MaintenanceRunEvidence`, `encode_maintenance_evidence()`, and `decode_maintenance_evidence()` with fields `evidence_version`, `run_id`, `plan_digest`, `source_identity`, `source_revision`, `destination_identity`, `state`, `completed_steps`, `candidate_receipt`, and `activation_receipt`.
- `tests/test_migration_cutover.py` and the Phase 7 additions to `tests/test_stored_compatibility.py`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-PATTERNS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-VALIDATION.md
@src/cacheness/storage/lifecycle_authority.py
@src/cacheness/storage/memory_lifecycle_authority.py
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/integrity.py
</context>

<interfaces>
`LifecycleAuthority` currently owns entry visibility through immutable `EntrySnapshot`, exact `EntryExpectation`, and `AuthorityStateSnapshot.revision`. `BlobStore` exposes the selected authority as `lifecycle_authority`, registered handlers as `handlers`, and the configured signing provider behind `_authority_manifest_key(initialize_new_store=False)`. The new maintenance protocol must remain narrower than ordinary lifecycle orchestration and authority receipts must corroborate store identity, revision, plan digest, candidate digest, entry count, and bytes.
</interfaces>

<assumption_delta_decision>
No change: the detector matched only the negated phrase about a second cutover authority. The primary noun is lifecycle authority. Phase 7 preserves exactly one lifecycle authority; maintenance evidence is coordination evidence and never canonical state.
</assumption_delta_decision>

<schema_push_decision>
The generic TypeScript ORM push gate is not applicable. This Python/SQLAlchemy phase tests any actual authority-schema evolution through explicit topology contracts and never runs a frontend schema-push command.
</schema_push_decision>

<planner_assumptions>
- "[FLAGGED/UNVERIFIED] MIGR-03: entry-complete means every canonical authority row at one bound revision is classified and counted, including schema-incompatible rows; the semantic tests in Plans 03-04 must prove this explicitly."
- "[FLAGGED/UNVERIFIED] MIGR-04: a supported same-backend action exists only for an explicitly declared compatibility edge and requires stopped-worker acknowledgement plus copy, complete verification, and separate authority activation; Plans 01, 06, and 07 own proof."
- "[FLAGGED/UNVERIFIED] MIGR-05: resumability is authorized only by bounded authenticated evidence supplied with an exact run ID/path and revalidated outputs; Plans 05-08 own proof."
- "[FLAGGED/UNVERIFIED] MIGR-06: incompatible or cross-backend work is a distinct confirmed rebuild through source handlers and destination BlobStore with no default exclusions; Plan 09 owns proof."
- The eight `must_haves.prohibitions` rows are descriptor-less adversarial recall. They remain flagged/unverified until semantic tests and the final Plan 11 verifier close them; no row is silently dropped or converted into an inferred positive truth.
</planner_assumptions>

## Multi-Source Coverage Audit

| Source | ID/item | Plan(s) | Status | Notes |
|---|---|---|---|---|
| GOAL | Stopped-worker explicit migration/rebuild without silent mutation or loss of the only valid copy | 01-11 | COVERED | Tracer establishes the seam; expansions cover inventory, authority publication, evidence, retirement, rebuild, docs, and final gates. |
| GOAL | SC-01 non-mutating human/machine inspection with counts, bytes, incompatibilities, actions | 03-04, 10-11 | COVERED | One canonical model renders both forms. |
| GOAL | SC-02 same-backend copy-verify-switch and retained prior | 01, 06-08 | COVERED | Local and deterministic remote authority contracts preserve topology-specific ACID scope. |
| GOAL | SC-03 explicit resumability without empty-catalog adoption | 05-08 | COVERED | Authenticated explicit evidence and interruption tests. |
| GOAL | SC-04 scoped confirmed rebuild | 02, 09 | COVERED | One-way exclusion confirmation precedes rebuild implementation. |
| GOAL | SC-05 explicit version window and derived projection separation | 02-04, 06, 08-10 | COVERED | Independent dimensions and post-cutover derived rebuild. |
| REQ | MIGR-03 inventory and dual rendering | 01, 03-04, 10-11 | COVERED | Non-mutating bounded classification and canonical rendering. |
| REQ | MIGR-04 offline resumable same-backend cutover | 01-02, 04, 06-08, 10-11 | COVERED | Checkpointed contract, narrow authority transactions, no implicit open. |
| REQ | MIGR-05 safe resume, signing preservation, projection separation | 01-02, 05-08, 11 | COVERED | Evidence, prior retention, retirement, recovery and audit. |
| REQ | MIGR-06 explicit confirmed rebuild | 02-03, 09-11 | COVERED | Separate plan and handler-owned data path. |
| RESEARCH | Dedicated offline boundary above BlobStore; one authority | 01, 06-09, 11 | COVERED | No UnifiedCache or ordinary-open migration path. |
| RESEARCH | Raw bounded revision-bound inventory | 03-04 | COVERED | Administrative page is distinct from schema-filtering catalog queries. |
| RESEARCH | Independent compatibility matrix and release window | 02-03 | COVERED | Authority kind/schema, store, manifest, handler payload, catalog edges are separate. |
| RESEARCH | Authenticated canonical evidence and explicit resume | 01, 05-08 | COVERED | Domain-separated bounded envelope; no candidate inference. |
| RESEARCH | Whole-store authority activation and external-effect reconciliation | 01, 06-08 | COVERED | Database/memory authority owns visibility; blob work stays external. |
| RESEARCH | Registered handlers own payload compatibility/transform/rebuild | 03, 09 | COVERED | Custom formats are first-class; no native-format switch in coordinator. |
| RESEARCH | Projection rebuild is derived post-cutover work | 06, 08-09 | COVERED | Projection state never gates activation. |
| RESEARCH | Documentation correction, deterministic topologies, no new packages | 10-11 | COVERED | No install task; live PostgreSQL/S3 remains Phase 8. |
| CONTEXT | D-01 first supported release baseline | 03 | COVERED | Historical layouts remain inspectable rebuild-only. |
| CONTEXT | D-02 current + immediately previous release window | 02-03 | COVERED | One-way checkpoint precedes publication. |
| CONTEXT | D-03 independent compatibility dimensions | 03 | COVERED | Explicit matrix, no aggregate inference. |
| CONTEXT | D-04 unsupported source inspection/refusal | 03 | COVERED | No force override. |
| CONTEXT | D-05 no manufactured migration version | 03, 11 | COVERED | Only test-injected edges exercise generic machinery. |
| CONTEXT | D-06 complete classification/totals/reasons | 03-04 | COVERED | Every raw row included. |
| CONTEXT | D-07 whole-store all-or-nothing publication | 01, 06-07 | COVERED | Verify complete candidate first. |
| CONTEXT | D-08 distinct rebuild and confirmed exclusions | 02, 09 | COVERED | One-way checkpoint precedes exact exclusion plan. |
| CONTEXT | D-09 source identity/revision revalidation | 03, 05-08 | COVERED | Every mutating action rejects stale plans. |
| CONTEXT | D-10 handler-owned transforms | 03, 09 | COVERED | Exact/declarative edges only. |
| CONTEXT | D-11 preserve unknown attributes; projections separate | 03, 06, 08-09 | COVERED | Canonical mappings preserved; derived rebuild follows activation. |
| CONTEXT | D-12 separate stage/verify/activate | 01, 06-07 | COVERED | Activation is explicit. |
| CONTEXT | D-13 rollback only before writer restart | 08 | COVERED | First acknowledged ordinary write ends eligibility. |
| CONTEXT | D-14 explicit prior retention | 06, 08 | COVERED | No timer/background cleanup. |
| CONTEXT | D-15 finalize then separately confirmed purge | 02, 08 | COVERED | One-way checkpoint precedes implementation. |
| CONTEXT | D-16 purge debt cannot revoke activation | 08 | COVERED | Retryable cleanup receipt. |
| CONTEXT | D-17 separate operator work directory, non-authoritative evidence | 01, 05 | COVERED | Overlap and escape rejected. |
| CONTEXT | D-18 one canonical JSON model and rendered report | 03, 05, 10 | COVERED | No second report model. |
| CONTEXT | D-19 explicit run ID/path resume | 05-08 | COVERED | No latest-run search. |
| CONTEXT | D-20 fail-closed evidence and no candidate adoption | 01, 05-08 | COVERED | Narrow diagnostics only. |
| CONTEXT | D-21 owned unactivated abort only | 08 | COVERED | Activated runs direct to rollback/finalize. |
| CONTEXT | D-22 preserve provider-backed signing identity | 01, 05, 09 | COVERED | Fingerprints only; inability blocks. |

No CONTEXT deferred idea exists. Phase 8 real-service qualification, performance budgets, full Python/platform release matrix, hostile-pickle sandboxing, new backend families, universal cross-backend physical migration, and `SqlCache` redesign are explicit exclusions rather than missing Phase 7 work.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute one memory inspect-plan-stage-verify-activate run</name>
  <files>src/cacheness/storage/migration.py, src/cacheness/storage/migration_authority.py, src/cacheness/storage/migration_evidence.py, src/cacheness/storage/memory_lifecycle_authority.py, tests/test_migration_cutover.py</files>
  <read_first>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/storage/memory_lifecycle_authority.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/integrity.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "A one-entry initialized memory source is inspected, classified through one test-injected compatible edge, staged into an invisible candidate, fully verified, and activated only by the memory authority after an explicit activate call."
    - "The work directory is supplied explicitly and is separate from source/destination; evidence carries the exact run ID and plan digest but cannot make the candidate visible."
    - "Faults after inspection, staging, or verification preserve the source as the sole visible complete store; resume never infers completion from candidate presence."
  </behavior>
  <action>Write the failing end-to-end test first, then implement the production-quality single-entry memory path per D-07, D-12, D-17, D-20, and D-22. Define immutable bounded value objects in `migration_authority.py`; receipts must bind source/destination identity, expected authority revision, plan digest, candidate digest, entry count, and byte count. Define the smallest canonical evidence envelope needed by the tracer in `migration_evidence.py`, using a migration-specific domain and provider-supplied key material without serializing it. In `migration.py`, expose an `OfflineMigrationService` requiring source, destination, explicit work directory, explicit run ID, and stopped-worker acknowledgement. Its inspect/plan/stage/verify/activate methods revalidate source identity/revision, preserve immutable payloads and authenticated catalog values, and treat activation as the only visibility transition. Extend `InMemoryLifecycleAuthority` with the narrow protocol primitives; correctness may use same-process authority state but must not add a separate coordinator. Exercise a test-only injected compatibility edge between identical current persisted dimensions so the generic cutover path is real without adding or bumping a production format per D-05.</action>
  <acceptance_criteria>The one-entry memory run completes every named action, activation is separately requested after whole-candidate verification, source remains visible at every pre-activation fault, evidence/candidate state alone never publishes, and no persisted production version is introduced.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_cutover.py -x -o log_cli=false</automated></verify>
  <done>The first complete operator path is executable end to end and establishes the reusable one-authority skeleton for all expansion plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve validation-only ordinary store entry points</name>
  <files>tests/test_stored_compatibility.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/manifest.py</file>
    <file>tests/test_stored_compatibility.py</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "Constructing, opening, initializing, reading, reconciling, or composing UnifiedCache over an unsupported store does not create authority files, journals, format markers, candidates, evidence, keys, or projections."
    - "An initialized current store reopens and repeat-initializes through validation only."
  </behavior>
  <action>Extend the stored-compatibility regression matrix for D-01, D-04, D-05, and MIGR-04. Snapshot complete disposable fixture trees before each ordinary operation and compare after failure or validation-only success. Include an actual initialized current memory and SQLite store, historical fixtures, retired scheduler evidence, unsupported/future dimensions, corrupt evidence, and foreign non-empty roots. Tests may call the offline service only in the explicit tracer; no production ordinary path should need modification unless a regression exposes accidental maintenance reachability.</action>
  <acceptance_criteria>Ordinary store entry points remain free of migration/rebuild/adoption/deletion side effects and current repeat initialization changes no persisted version or canonical entry.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_stored_compatibility.py tests/test_migration_cutover.py -x -o log_cli=false</automated></verify>
  <done>The tracer is isolated behind an explicit offline boundary and the pre-existing ordinary lifecycle keeps its fail-closed behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator paths/run request -> offline service | Untrusted paths, run IDs, acknowledgements, and state-transition requests enter bounded maintenance logic. |
| evidence/candidate -> lifecycle authority | Non-authoritative artifacts may request, but never authorize, the one whole-store visibility transition. |
| signed manifest/payload -> handler | Authentication plus digest/size verification must precede any handler-owned deserialization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | tracer evidence/candidate | high | mitigate | Bind canonical evidence and candidate receipts to exact run, plan, identities, revisions, counts, bytes and digests; authority validates the verified receipt before activation. |
| T-07-02 | Elevation of Privilege | work-directory/candidate paths | high | mitigate | Resolve roots, reject overlap/symlink escape, and keep locators opaque through existing participant containment. |
| T-07-03 | Tampering | payload handler boundary | high | mitigate | Authenticate manifest and verify one private payload snapshot before any registered handler read; the tracer uses exact-compatible byte copy. |
| T-07-04 | Denial of Service | evidence and inventory values | medium | mitigate | Bound strings, records, JSON bytes/depth/nodes and per-call work; later plans add resumable pages. |
| T-07-05 | Information Disclosure | signing provider | high | mitigate | Use provider-backed bytes only in memory and emit fingerprints/identities, never keys or secret paths. |
</threat_model>

<verification>
Run the focused memory tracer and stored-compatibility tests after each task. Integrity assertions prove no partial visibility; recovery assertions prove source preservation; progress is bounded typed completion/continuation; performance is not asserted. The topology is memory/memory only and makes no crash-durability or live-service claim.
</verification>

<success_criteria>
The memory tracer proves the entire explicit operator sequence; every pre-activation fault preserves the source; ordinary paths remain validation-only; work evidence is demonstrably non-authoritative; all four spec-less assumptions and eight prohibitions are retained for later closure.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-01-SUMMARY.md` when done.
</output>
