---
phase: 07-explicit-migration-and-rebuild-cutover
plan: "10"
type: execute
wave: 7
depends_on: [07-07, 07-09]
files_modified:
  - src/cacheness/storage/__init__.py
  - src/cacheness/error_handling.py
  - docs/BACKEND_SELECTION.md
  - docs/STORAGE_MIGRATION.md
  - .planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md
  - tests/test_migration_public_contract.py
autonomous: true
requirements: [MIGR-03, MIGR-04, MIGR-05, MIGR-06]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Users have one documented public Python maintenance surface over the same canonical models/service and an exact operator sequence for inspect/plan/stage/verify/activate, rollback-or-finalize, and later purge/rebuild."
    - "Documentation states the first baseline, previous-release window, stopped-worker scope, topology-specific transaction boundaries, explicit evidence/run paths, signing provider, retained prior store, confirmation points, derived projections, and no implicit open/initialize behavior."
    - "The API coverage declaration stores the actual detector result and explains that Phase 7 adds no external integration and uses existing PostgreSQL/S3 authority adapters offline; semantic tests require the adapter/test rationale and Phase 8 live-service non-qualification boundary."
  artifacts:
    - path: src/cacheness/storage/__init__.py
      provides: "Supported offline maintenance public exports"
    - path: docs/STORAGE_MIGRATION.md
      provides: "Canonical operator workflow, recovery diagnostics, topology guarantees, and non-claims"
    - path: .planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md
      provides: "Actual detector result plus reasoned no-external-API declaration and remote non-qualification boundary"
    - path: tests/test_migration_public_contract.py
      provides: "Public contract assertions plus executable detector/result/declaration semantic checks"
  key_links:
    - from: src/cacheness/storage/__init__.py
      to: src/cacheness/storage/migration.py
      via: "Public exports point to the sole OfflineMigrationService/model implementations"
      pattern: "OfflineMigrationService|MigrationPlan"
    - from: docs/STORAGE_MIGRATION.md
      to: src/cacheness/storage/migration.py
      via: "Executable examples use exact public methods and required arguments"
      pattern: "inspect|stage|verify|activate|resume"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] No CLI/session/global service may maintain an independent plan/evidence model or infer paths/run IDs."
    - "[FLAGGED/UNVERIFIED] Documentation may not promise automatic/seamless migration, online writers, cross-resource ACID, universal conversion, remote qualification, or deletion on finalize."
    - "[FLAGGED/UNVERIFIED] Public exports may not expose backend tables, secret material, candidate adoption, or a second lifecycle facade."
---

<objective>
Publish one accurate operator-facing migration/rebuild API and documentation contract.

Purpose: Make every safe action, stop condition, topology guarantee, confirmation, and non-claim usable without preserving outdated automatic-migration promises.
Output: Public exports/errors, migration guide, corrected backend guide, deterministic public contract tests, and reasoned API coverage declaration.
</objective>

## Artifacts this phase produces

- Public storage exports for `OfflineMigrationService`, plan/assessment/disposition/reason models, authority/evidence receipts intended for callers, and typed migration errors; no backend table objects.
- `docs/STORAGE_MIGRATION.md` with the complete operator runbook and recovery/failure taxonomy.
- Updated `docs/BACKEND_SELECTION.md` migration section replacing automatic/seamless language.
- `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md` reasoned `no new external API integration` declaration.
- `tests/test_migration_public_contract.py`.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-07-SUMMARY.md
@.planning/phases/07-explicit-migration-and-rebuild-cutover/07-09-SUMMARY.md
@src/cacheness/storage/__init__.py
@docs/BACKEND_SELECTION.md
@docs/STORAGE_INITIALIZATION.md
</context>

<interfaces>
The supported user entry point is a Python library API. If an executable adapter is later added, it must be a stateless renderer over these exact models and require explicit paths/run IDs. Follow the storage package's deliberate `__all__` barrel and existing domain error hierarchy. PostgreSQL/S3 are already modeled participants; Phase 7 introduces no new external API/service integration.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish the single maintenance API and operator runbook</name>
  <files>src/cacheness/storage/__init__.py, src/cacheness/error_handling.py, docs/BACKEND_SELECTION.md, docs/STORAGE_MIGRATION.md, tests/test_migration_public_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/__init__.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/migration.py</file>
    <file>docs/BACKEND_SELECTION.md</file>
    <file>docs/STORAGE_INITIALIZATION.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "All documented public imports resolve without optional live services and method signatures require explicit work directory/run ID/stopped-worker acknowledgement/confirmation where applicable."
    - "Examples render JSON/human plans from one model and exercise migration and rebuild without implicit open/initialize behavior."
    - "Documentation names exact topology guarantees/non-claims and marks PostgreSQL/AWS S3 real qualification as Phase 8."
  </behavior>
  <action>Export the supported maintenance service, immutable caller result models, stable enums/reasons, and typed domain errors through `cacheness.storage.__all__`. Keep backend implementation details and signing material private. Write `docs/STORAGE_MIGRATION.md` as an executable Python API runbook covering D-01 through D-22: inspect/plan, supported window/matrix, stage/verify/explicit activate, explicit resume evidence, the activated-offline handoff that blocks every ordinary worker open/read/query/mutation entry point until rollback or finalize seals rollback before restart, separate confirm/purge, abort, rebuild/exclusions, custom handlers, catalog preservation, projection rebuild, diagnostics, topology-specific ACID/progress, and Phase 8 non-claims. Correct `BACKEND_SELECTION.md` automatic/seamless wording. Tests inspect public signatures/examples/markers and prove ordinary constructors/initialize expose no migration switch. Per resolved RQ-02, expose a Python library API only: do not add a CLI module or a `[project.scripts]` entry; any later CLI must remain a stateless adapter over these same models.</action>
  <acceptance_criteria>One importable Python surface and one canonical guide describe every action/decision accurately, examples execute against deterministic fixtures, outdated automatic/online/cross-resource claims are absent from the migration sections, and neither a CLI module nor a `[project.scripts]` entry is added.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_public_contract.py tests/test_stored_compatibility.py -x -o log_cli=false</automated></verify>
  <done>Operators can discover and run the complete explicit workflow without an alternate API or misleading guarantee.</done>
</task>

<task type="auto">
  <name>Task 2: Declare deterministic external-API coverage scope</name>
  <files>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-COVERAGE.md, tests/test_migration_public_contract.py</files>
  <read_first>
    <file>.planning/phases/05-payload-backends-and-supported-topology-qualification/05-COVERAGE.md</file>
    <file>.planning/phases/07-explicit-migration-and-rebuild-cutover/07-RESEARCH.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>Resolve the active GSD runtime's real `gsd-core/bin/lib/api-coverage.cjs` module and run it with `--json` over the concatenation of the Phase 7 ROADMAP section and every `07-*-PLAN.md` body; fail if that module cannot be found rather than substituting a local detector. Record its exact typed `detected`, `signals`, and `terms` result in `07-COVERAGE.md`, followed by the reasoned declaration `No external API integration:` explaining that Phase 7 adds offline maintenance over the existing `PostgresqlLifecycleAuthority` and `S3BlobBackend` adapters. Name the deterministic references `tests/contracts/test_postgresql_lifecycle_authority.py`, `tests/test_migration_remote_contract.py`, and `tests/test_s3_blob_backend.py`, and state that they do not qualify live PostgreSQL/AWS S3; Phase 8 alone owns that real-service boundary. Add `test_external_api_coverage_declaration_is_detector_backed` to `tests/test_migration_public_contract.py`; it must assemble the same scope, invoke that real detector as a subprocess, compare the stored typed result, assert the declaration contains the adapter/test references and Phase 8 non-qualification boundary, and fail on a fabricated capability matrix or live-service support claim.</action>
  <acceptance_criteria>The coverage artifact records detector input/result, exact reason, existing adapter surfaces/tests, and Phase 8 ownership; every triggered term is resolved without a false API capability claim.</acceptance_criteria>
  <verify><automated>uv run --frozen pytest -q tests/test_migration_public_contract.py::test_external_api_coverage_declaration_is_detector_backed -x -o log_cli=false</automated></verify>
  <done>The API coverage gate is explicit and honest: no new external integration and no fabricated matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public caller/docs -> maintenance service | Signatures and examples must require every safety input and not expose hidden unsafe defaults. |
| coverage vocabulary -> support claims | PostgreSQL/S3 terminology must not turn deterministic adapter use into real-service qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-42 | Elevation of Privilege | public action signatures | high | mitigate | Require explicit work/run/stopped/confirmation inputs and expose no force/adoption/implicit switch. |
| T-07-43 | Information Disclosure | docs/errors/examples | high | mitigate | Use provider/fingerprint placeholders and redaction tests; never print keys, DSNs, credentials, or secret paths. |
| T-07-44 | Repudiation | operator guide | medium | mitigate | Document canonical JSON digest, exact confirmations/receipts, and human rendering from the same model. |
| T-07-45 | Spoofing | API coverage/support claim | medium | mitigate | Reasoned no-new-integration declaration and explicit Phase 8 real-service qualification ownership. |
</threat_model>

<verification>
Run public import/signature/example tests plus stored-compatibility regressions. Documentation tests validate marker-bounded sections semantically without making prose a second source of truth.
</verification>

<success_criteria>
Every Phase 7 behavior is reachable through one safe public API and accurately documented; coverage declares no new external integration; no automatic migration, secret, or unsupported qualification claim remains.
</success_criteria>

<output>
Create `.planning/phases/07-explicit-migration-and-rebuild-cutover/07-10-SUMMARY.md` when done.
</output>
