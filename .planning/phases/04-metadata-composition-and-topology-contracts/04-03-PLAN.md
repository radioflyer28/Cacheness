---
phase: 04-metadata-composition-and-topology-contracts
plan: 03
type: execute
wave: 2
depends_on: [04-02]
files_modified:
  - src/cacheness/storage/composition.py
  - src/cacheness/storage/backends/__init__.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/memory_lifecycle_authority.py
  - src/cacheness/storage/backends/blob_backends.py
  - tests/test_blob_store_composition.py
  - tests/test_topology_capabilities.py
  - tests/test_metadata_role_contract.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "StoreTopology is the sole typed composition root; name XOR exact instance resolution follows one role-aware registry path per D-09 and D-10."
    - "Caller-owned resources stay open, owned resources close once in reverse creation order, and partial construction unwinds owned resources per D-12."
    - "Participant and composed capabilities are truthful values; invalid minima/topologies fail before I/O per D-11."
    - "The first executable BlobStore tracer resolves through the completed registry and is explicitly memory-only/same-process; persistent SQLite proof waits for Plan 04."
  artifacts:
    - {path: src/cacheness/storage/composition.py, provides: "BackendRef, StoreTopology, role-aware registry/resolver, Ownership, capabilities"}
    - {path: tests/test_blob_store_composition.py, provides: "Registered-name/exact-injection/ownership plus memory-only tracer coverage"}
    - {path: tests/test_topology_capabilities.py, provides: "Capability applicability/minima/incoherence matrix"}
  key_links:
    - {from: src/cacheness/storage/blob_store.py, to: src/cacheness/storage/composition.py, via: "all construction resolves the typed topology"}
    - {from: src/cacheness/storage/composition.py, to: src/cacheness/storage/lifecycle_authority.py, via: "role validation and composed capability intersection"}
    - {from: tests/test_blob_store_composition.py, to: src/cacheness/storage/memory_lifecycle_authority.py, via: "same-process memory tracer"}
  prohibitions:
    - "No legacy metadata factory/selector path, option merge for injected instances, or hidden fallback."
    - "No lifecycle coordinator, added lock, queue, or cross-resource transaction claim."
    - "No durable/multi-host capability assigned to memory."
    - "No PostgreSQL authority or S3 topology delivery in Phase 4."
---

<objective>
Build and prove the single typed topology resolver, ownership/capability contracts, and first executable direct-BlobStore path.

Purpose: Resolve composition before persistence expansions so the tracer exercises the architecture users will actually call.
Output: StoreTopology registry/resolver, close ledger, capability validation, and memory-only same-process tracer.
</objective>

<requirement_decision_coverage>
BACK-03: registered-name resolution and exact injected-instance selection. BACK-06: D-09, D-10, D-11, D-12 topology composition. BACK-07: same-process memory scope. BACK-02: metadata role/capability applicability.
</requirement_decision_coverage>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement one role-aware registry and resolver</name>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/backends/__init__.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_composition.py, tests/test_metadata_role_contract.py</files>
  <read_first>tests/test_blob_store_composition.py; tests/test_metadata_role_contract.py; src/cacheness/storage/blob_store.py; src/cacheness/storage/backends/__init__.py; src/cacheness/storage/lifecycle_authority.py</read_first>
  <behavior>
    - "BackendRef accepts exactly one registered name or instance; instance plus name/options fails before construction (D-10)."
    - "Injected instances are preserved by identity and factories run only for named references."
    - "Registry entries declare payload/authority/projection role and protocol conformance; role mismatch fails typed before I/O."
    - "StoreTopology is the only root consumed by BlobStore (D-09)."
  </behavior>
  <action>Implement BackendRef and StoreTopology plus one role-aware registry/resolver. Put built-in registration and user registration through the same path. Preserve exact injected object identity and reject any options/name alongside an instance. Resolve all direct BlobStore construction through this root. Distinguish lifecycle-authority and projection roles now so JSON/ORM/PostgreSQL-facing outputs cannot be treated as Phase 4 authority. Stop using create_metadata_backend/get_metadata_backend on the direct BlobStore path, but leave their atomic source/export deletion to final cutover Plan 08.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_blob_store_composition.py tests/test_metadata_role_contract.py -k "registered or injected or role or option or resolve" -x &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated></verify>
  <acceptance_criteria>Named and injected participants resolve through one path; exact instances survive; ambiguous input and wrong roles fail before I/O.</acceptance_criteria>
  <done>The resolver is complete before any tracer uses it.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Enforce ownership/capabilities and execute the memory-only tracer</name>
  <files>src/cacheness/storage/composition.py, src/cacheness/storage/backends/blob_backends.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/memory_lifecycle_authority.py, tests/test_topology_capabilities.py</files>
  <read_first>src/cacheness/storage/composition.py; src/cacheness/storage/lifecycle_authority.py; src/cacheness/storage/backends/blob_backends.py; tests/test_topology_capabilities.py; docs/adr/0001-topology-specific-storage-guarantees.md</read_first>
  <behavior>
    - "D-12: injected resources default caller-owned; resolver-created resources are owned; explicit override is visible; close is once/reverse-order and unwind-safe."
    - "D-11: capabilities separately report durability, sharing scope, transaction scope, exact CAS, immutable generations, streaming, listing, portable query, acceleration, projection refresh/rebuild."
    - "Composed capabilities never exceed the weakest applicable participant and invalid required minima fail at construction."
    - "Memory authority/payload advertises same-process scope and portable canonical scan, not durable persistence or index acceleration."
    - "After Task 1 resolution exists, one real direct BlobStore put→info→close path uses registered memory participants and the exact injected instances."
  </behavior>
  <action>Add frozen Ownership, ParticipantCapabilities, TopologyCapabilities, and requirement-minimum types. Compute composition conservatively and implement reverse-order close/partial-construction unwind. Treat capability applicability explicitly for BACK-02/BACK-06; report canonical scan support independently from unsupported acceleration. Then wire one production-quality memory-only direct BlobStore put→info→close tracer through the Task 1 resolver twice: once via registered names and once with exact injected instances. Assert same-process semantics and no durability/persistent-reopen claim. Keep progress properties aligned to the ADR and do not touch SQLite in this plan.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_topology_capabilities.py tests/test_blob_store_composition.py -k "capability or minimum or ownership or close or unwind or memory_tracer or same_process" -x &amp;&amp; uv run --frozen ruff check src/cacheness/storage/composition.py src/cacheness/storage/blob_store.py src/cacheness/storage/memory_lifecycle_authority.py</automated></verify>
  <acceptance_criteria>Ownership/capability cases and the memory-only tracer pass through registered-name and exact-injection paths without SQLite/persistence claims.</acceptance_criteria>
  <done>The resolved same-process topology works end to end and advertises only its actual guarantees.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Trust Boundaries
| Boundary | Description |
|---|---|
| Configuration → resolver | Names, instances, options, minima, ownership enter composition. |
| Resolver → backend participant | Factory output crosses role/capability validation. |
| BlobStore → authority/payload | Lifecycle requests cross participant boundaries. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-03-01 | Spoofing | registry | high | mitigate | Role/protocol checks and deterministic duplicate registration. |
| T-04-03-02 | Elevation of Privilege | capability minima | high | mitigate | Conservative composed capabilities and pre-I/O rejection. |
| T-04-03-03 | Repudiation | ownership close | medium | mitigate | Explicit ledger and close/unwind tests. |
| T-04-03-04 | Tampering | duplicate coordinator | high | mitigate | Tracer delegates only to Phase 3 lifecycle authority. |
| T-04-03-SC | Tampering | package supply chain | low | accept | No package task. |
</threat_model>
<verification>
`uv run --frozen pytest -q tests/test_blob_store_composition.py tests/test_topology_capabilities.py tests/test_metadata_role_contract.py -o log_cli=false`
</verification>
<success_criteria>One resolver precedes and powers the memory-only tracer; ownership, capabilities, exact injection, registered names, same-process scope, and rejection contracts pass.</success_criteria>
<artifacts_produced>`composition.py`, updated direct `BlobStore`, capability/role tests, and same-process memory tracer evidence.</artifacts_produced>
<output>Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-03-SUMMARY.md` when done.</output>
