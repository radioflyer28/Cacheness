---
phase: 04-metadata-composition-and-topology-contracts
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/cacheness/storage/catalog.py
  - src/cacheness/storage/manifest.py
  - src/cacheness/storage/read_contract.py
  - src/cacheness/error_handling.py
  - src/cacheness/storage/__init__.py
  - tests/test_catalog_schema.py
  - tests/test_catalog_query_contract.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-07]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "CatalogSchema is native, optional, immutable, and validates only declared fields while opaque undeclared metadata round-trips per D-01 through D-03."
    - "Every new write materializes declared defaults; query predicates later operate on stored presence and Phase 4 adds no effective-value query per D-04."
    - "STORE_FORMAT_VERSION is exactly 2 to avoid collision with development format 1, while store epoch, manifest schema, SQLite user_version, and payload format remain independent versions."
    - "Unsupported layouts are detected read-only and fail with typed migration/rebuild-required evidence; no historical migration executes per D-17 and D-18."
    - "One frozen BlobReceipt is the public semantic result, without a BlobEntryInfo compatibility alias."
  artifacts:
    - {path: src/cacheness/storage/catalog.py, provides: "Native schema/value/predicate/page/cursor types and bounds"}
    - {path: src/cacheness/storage/manifest.py, provides: "Current format-2 manifest and non-mutating layout classifier"}
    - {path: src/cacheness/storage/read_contract.py, provides: "Frozen BlobReceipt semantic result"}
  key_links:
    - {from: src/cacheness/storage/catalog.py, to: src/cacheness/storage/manifest.py, via: "schema fingerprint plus canonical signed user metadata"}
    - {from: src/cacheness/storage/manifest.py, to: src/cacheness/error_handling.py, via: "typed unsupported-layout evidence before mutation"}
  prohibitions:
    - "No Pydantic, dataclass-as-wire-format, SQLAlchemy canonical model, or TypeScript schema tooling."
    - "No implicit upgrade, dual reader/writer, legacy result alias, or format-1 mutation."
    - "No version lockstep across store epoch, manifest schema, SQLite user_version, or payload format."
    - "No absent-field equals default behavior and no effective-value query mode in Phase 4."
---

<objective>
Create the native catalog, independent version model, typed query values, and one public receipt required by the Wave 0 contracts.

Purpose: Establish the stable semantic vocabulary before composition or persistence work.
Output: Current format-2 manifest, CatalogSchema/query value objects, BlobReceipt, bounds, and typed errors.
</objective>

<requirement_decision_coverage>
BACK-02: D-01, D-02, D-03, D-04. BACK-03: D-05, D-06, D-07, D-08. BACK-07: D-17, D-18.
</requirement_decision_coverage>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement native schema and portable query value contracts</name>
  <files>src/cacheness/storage/catalog.py, src/cacheness/error_handling.py, src/cacheness/storage/__init__.py, tests/test_catalog_schema.py, tests/test_catalog_query_contract.py</files>
  <read_first>tests/test_catalog_schema.py; tests/test_catalog_query_contract.py; src/cacheness/security.py; src/cacheness/json_utils.py; src/cacheness/storage/__init__.py</read_first>
  <behavior>
    - "D-01/D-03: mappings work without a schema; declared string, signed-64 integer, boolean, nullable fields use native frozen values with serializable constraints and no bool-as-int coercion."
    - "D-02: undeclared bounded metadata is preserved, authenticated, and round-tripped but cannot receive portable query/index guarantees."
    - "D-04: defaults are materialized into new records; predicate matching uses stored presence, so absent is distinct from default and null."
    - "D-05 through D-08: equality/range/membership/existence predicates are typed, AND-only, bounded, and represented with stable revision-bound opaque cursor/page types."
  </behavior>
  <action>Implement CatalogField, CatalogSchema, native tagged scalar values, MISSING as an internal stored-presence marker, CatalogPredicate, CatalogQuery, CatalogCursor, CatalogPage, and typed validation/query errors. Validate declared fields and materialize defaults before signing a new record. Preserve undeclared metadata within explicit size/depth bounds. Keep queryable/indexed intent in declarations but do not promise physical secondary indexes. Define equality, range, membership, and existence predicates combined only by AND, with Wave 0 limits. Predicates inspect stored presence; do not expose effective-default evaluation. Export only current native types.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py -k "schema or field or default or presence or predicate or cursor or bound" -x &amp;&amp; uv run --frozen ruff check src/cacheness/storage/catalog.py src/cacheness/error_handling.py src/cacheness/storage/__init__.py</automated></verify>
  <acceptance_criteria>Wave 0 contracts pass for mappings, declared validation, opaque round trips, materialized defaults, stored-presence predicates, bounds, and immutable values.</acceptance_criteria>
  <done>The native in-memory catalog/query contract is complete without persistence or acceleration assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish format 2, independent version dimensions, and BlobReceipt</name>
  <files>src/cacheness/storage/manifest.py, src/cacheness/storage/read_contract.py, src/cacheness/error_handling.py, src/cacheness/storage/__init__.py, tests/test_catalog_schema.py</files>
  <read_first>src/cacheness/storage/manifest.py; src/cacheness/storage/read_contract.py; src/cacheness/storage/lifecycle.py; tests/test_catalog_schema.py; .planning/phases/03-atomic-lifecycle-and-recovery-engine/03-05-SUMMARY.md</read_first>
  <behavior>
    - "STORE_FORMAT_VERSION equals 2 specifically because development format 1 already exists."
    - "Store epoch, manifest schema version, SQLite user_version, and handler payload format are distinct fields with independent tests and no forced numeric equality."
    - "Current manifests bind format, schema identity/revision/fingerprint, signed authoritative metadata, generation/locator, and payload integrity."
    - "Format 1, unknown future, mixed, incomplete, or foreign layouts raise MigrationOrRebuildRequired before any mutation."
    - "BlobReceipt is frozen and semantic, with no legacy shape alias."
  </behavior>
  <action>Set STORE_FORMAT_VERSION = 2 as an intentional collision-avoidance boundary. Model store epoch, manifest schema version, SQLite user_version, and payload/handler format separately; prove they can differ. Implement the sole current manifest encoder/decoder using canonical JSON/HMAC primitives and a read-only layout classifier that produces typed evidence before initialization or staging. Raise MigrationOrRebuildRequired for unsupported layouts and retain explicit future Phase 7 migration/rebuild interfaces without an executor. Publish one frozen BlobReceipt and no BlobEntryInfo alias.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_catalog_schema.py -k "format or version or manifest or receipt or unsupported or mutation" -x &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated></verify>
  <acceptance_criteria>Format 2 round-trips; independent versions are observable; unsupported layouts remain byte/path identical; BlobReceipt is the sole result.</acceptance_criteria>
  <done>The current versioned format and migration boundary are explicit, testable, and non-mutating.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Trust Boundaries
| Boundary | Description |
|---|---|
| Caller metadata/schema → catalog values | Untrusted structure and bounds enter canonical serialization. |
| Persisted bytes → manifest classifier | Corrupt/old bytes reach format dispatch before mutation. |
| Cursor bytes → query values | Opaque state influences revision selection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-02-01 | Tampering | manifest/catalog | high | mitigate | Canonical typed serialization, fingerprint, HMAC, strict scalar checks. |
| T-04-02-02 | Denial of Service | metadata/query | medium | mitigate | Size, depth, page, predicate, and membership bounds. |
| T-04-02-03 | Spoofing | format identity | high | mitigate | Independent identifier checks and read-only mixed/foreign rejection. |
| T-04-02-04 | Repudiation | BlobReceipt | medium | mitigate | Frozen operation/generation/expectation/projection facts. |
| T-04-02-SC | Tampering | package supply chain | low | accept | No package task. |
</threat_model>

<verification>
`uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py -o log_cli=false`
`uv run --frozen python tools/verify_phase4_ruff_delta.py`
</verification>
<success_criteria>Native schema/query values, format 2, independent versions, no-mutation rejection, and frozen BlobReceipt satisfy Wave 0.</success_criteria>
<artifacts_produced>`catalog.py`, current `manifest.py`, frozen receipt/error types, and passing schema/query contracts.</artifacts_produced>
<output>Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-02-SUMMARY.md` when done.</output>
