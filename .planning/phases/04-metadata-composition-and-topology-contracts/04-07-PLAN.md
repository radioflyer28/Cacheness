---
phase: 04-metadata-composition-and-topology-contracts
plan: 07
type: execute
wave: 6
depends_on: [04-06]
files_modified:
  - src/cacheness/metadata.py
  - src/cacheness/storage/backends/__init__.py
  - src/cacheness/storage/backends/base.py
  - src/cacheness/__init__.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/storage/lifecycle.py
  - src/cacheness/storage/read_contract.py
  - docs/CATALOG_AND_TOPOLOGY.md
  - .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
  - tests/test_metadata_backend_registry.py
  - tests/test_metadata.py
  - tests/test_blob_store_read_contract.py
  - tests/test_phase3_local_workflows.py
  - tests/test_public_api_contract.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The final public direct-BlobStore surface contains StoreTopology, native catalog/query types, capabilities, projections, typed errors, and one frozen BlobReceipt."
    - "Legacy metadata/backend selectors, create/get factories, registries, duplicate ABC, old BlobStore constructors, metadata_filter list path, BlobEntryInfo, exports, consumers, and obsolete tests disappear in one atomic task per D-17."
    - "Package import and canonical direct BlobStore suites pass immediately after the atomic cutover; no runtime shim restores a removed path."
    - "UnifiedCache policy remains Phase 6; Python 3.11/3.13 full suites are executed and any failures caused solely by intentionally unported policy tests are recorded explicitly rather than reported green."
    - "Documentation and validation state current format/independent versions, sparse scan continuation, unsupported acceleration, offline SQLite boundary, and no live PostgreSQL/S3/Windows claims."
  artifacts:
    - {path: src/cacheness/__init__.py, provides: "Curated current direct-BlobStore exports"}
    - {path: docs/CATALOG_AND_TOPOLOGY.md, provides: "Final current composition/catalog/projection contract"}
    - {path: .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md, provides: "Focused/full runtime and Ruff evidence with explicit limitations"}
  key_links:
    - {from: src/cacheness/storage/__init__.py, to: src/cacheness/storage/blob_store.py, via: "one direct public constructor/result surface"}
    - {from: src/cacheness/storage/lifecycle.py, to: src/cacheness/storage/read_contract.py, via: "BlobReceipt replaces legacy info shape"}
    - {from: tests/test_public_api_contract.py, to: src/cacheness/__init__.py, via: "obsolete API tests deleted; canonical Wave 0 tests enforce absence"}
  prohibitions:
    - "No compatibility alias, warning shim, legacy dispatch, or parallel backend registry."
    - "No broad UnifiedCache/CacheConfig/decorator/global-cache/TTL/eviction/statistics migration."
    - "No normalized catalog rows or physical metadata index/acceleration claim."
    - "No historical migration execution, TypeScript schema tooling, extra coordinator/lock/queue, or cross-resource ACID fiction."
    - "No false full-suite-green or live PostgreSQL/S3/Windows claim."
---

<objective>
Execute the atomic public direct-BlobStore cutover, delete all remaining obsolete consumers/tests, finalize docs, and record an honest release matrix.

Purpose: Complete D-17 only after Plan 06 made source preparation import-safe, without absorbing Phase 6 cache-policy work.
Output: One current public surface, deleted compatibility tests, finalized contract docs, canonical focused green suite, and scoped 3.11/3.13/full-suite evidence.
</objective>
<requirement_decision_coverage>All BACK-02/03/06/07 and D-01 through D-18 are sealed. BACK-03's registered-name/exact-instance implementation remains Plan 04-03; this plan only publishes and verifies it.</requirement_decision_coverage>
<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-PATTERNS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-06-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Atomically remove every remaining public/direct legacy surface</name>
  <files>src/cacheness/metadata.py, src/cacheness/storage/backends/__init__.py, src/cacheness/storage/backends/base.py, src/cacheness/__init__.py, src/cacheness/storage/__init__.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/lifecycle.py, src/cacheness/storage/read_contract.py, tests/test_metadata_backend_registry.py, tests/test_metadata.py, tests/test_blob_store_read_contract.py, tests/test_phase3_local_workflows.py, tests/test_public_api_contract.py</files>
  <read_first>src/cacheness/metadata.py; src/cacheness/storage/backends/__init__.py; src/cacheness/storage/backends/base.py; src/cacheness/__init__.py; src/cacheness/storage/__init__.py; src/cacheness/storage/blob_store.py; src/cacheness/storage/lifecycle.py; src/cacheness/storage/read_contract.py; tests/test_metadata_backend_registry.py; tests/test_metadata.py; tests/test_blob_store_read_contract.py; tests/test_phase3_local_workflows.py; tests/test_public_api_contract.py; 04-06-SUMMARY.md</read_first>
  <behavior>
    - "create_metadata_backend/get_metadata_backend/register/unregister/list metadata registry, duplicate selector exports, old BlobStore selector constructors, metadata_filter, and BlobEntryInfo are absent together."
    - "All unconditional barrels and direct lifecycle consumers use StoreTopology/CatalogQuery/BlobReceipt in the same commit."
    - "Five obsolete test files are deleted rather than rewritten; storage/security invariants already co-located in canonical Phase 4 suites remain green."
    - "Package import and direct BlobStore operation work; removed calls fail normally by absent import/signature rather than adapter behavior."
  </behavior>
  <action>Perform one atomic D-17 cutover across the listed source and test files. Remove the metadata factory/registry family, duplicate base ABC, backend selector exports, old direct BlobStore constructors, legacy list metadata_filter, BlobEntryInfo shape/alias, and all unconditional/direct references. Export only StoreTopology/resolver, capabilities, CatalogSchema/query/page, projection contracts, BlobStore, frozen BlobReceipt, and typed errors. Delete the five obsolete API/shape/compatibility test files; do not translate them. Rely on canonical Wave 0 suites for retained signed-authority, no-mutation format, exact injection/ownership, sparse canonical scan, and projection-partial invariants. Do not add a shim or migrate UnifiedCache policy.</action>
  <verify><automated>uv run --frozen python -c "import cacheness; from cacheness.storage import BlobStore, BlobReceipt, StoreTopology" &amp;&amp; uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py tests/test_metadata_role_contract.py tests/test_blob_store_composition.py tests/test_topology_capabilities.py tests/test_catalog_projection.py -o log_cli=false &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated></verify>
  <acceptance_criteria>Atomic commit leaves no removed name/import/signature/test file, package and direct BlobStore imports work, and all canonical Phase 4 tests pass without shims.</acceptance_criteria>
  <done>The pre-production compatibility reset is complete at the public/direct storage boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize contract documentation and record the honest release matrix</name>
  <files>docs/CATALOG_AND_TOPOLOGY.md, .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md</files>
  <read_first>docs/CATALOG_AND_TOPOLOGY.md; docs/STORAGE_INITIALIZATION.md; 04-VALIDATION.md; pyproject.toml; .python-version; tests/fixtures/phase4_ruff_baseline.json; tools/verify_phase4_ruff_delta.py</read_first>
  <action>Finalize docs with STORE_FORMAT_VERSION=2 collision rationale; independent store epoch/manifest schema/SQLite user_version/payload format; materialized write defaults versus stored-presence predicates; canonical signed descriptor state; declared index intent with physical acceleration unsupported; bounded sparse scan continuation advancing the last examined identity even on zero-match pages; topology registry/exact injection/ownership/capabilities; memory same-process and SQLite persistent-reopen scopes; projection partial/refresh/rebuild; and Phase 5/6/7 boundaries. Run canonical focused tests and Ruff as blocking gates, then execute full Python 3.13/3.11 suites after Task 1. Record exact counts/skips and every nonzero result; only the two explicitly inventoried Phase 6 UnifiedCache policy files may remain deferred, and no failure may be described as green. Record no live PostgreSQL/S3/Windows evidence and keep descriptor-less prohibitions `[FLAGGED/UNVERIFIED]`.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py tests/test_metadata_role_contract.py tests/test_blob_store_composition.py tests/test_topology_capabilities.py tests/test_catalog_projection.py -o log_cli=false &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py &amp;&amp; uv run --python 3.13 --all-extras --group dev --frozen pytest -q -o log_cli=false --ignore=tests/test_unified_cache_adversarial_lifecycle.py --ignore=tests/test_unified_cache_lifecycle_authority.py &amp;&amp; uv run --python 3.11 --all-extras --group dev --frozen pytest -q -o log_cli=false --ignore=tests/test_unified_cache_adversarial_lifecycle.py --ignore=tests/test_unified_cache_lifecycle_authority.py &amp;&amp; bash -c 'set +e; uv run --python 3.13 --all-extras --group dev --frozen pytest -q -o log_cli=false; status_313=$?; uv run --python 3.11 --all-extras --group dev --frozen pytest -q -o log_cli=false; status_311=$?; test "$status_313" -le 1 &amp;&amp; test "$status_311" -le 1'</automated></verify>
  <acceptance_criteria>Focused canonical and Ruff gates are green; both full suites complete without collection/internal error; all failures are itemized and limited to the two explicit Phase 6 policy-deferred files, with no false success claim.</acceptance_criteria>
  <done>Phase 4 has a truthful cross-runtime release record and final current-contract documentation.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Trust Boundaries
| Boundary | Description |
|---|---|
| Public imports/config → BlobStore | Caller inputs enter sole supported storage surface. |
| Old layouts/cursors → classifiers | Untrusted persisted state reaches fail-closed parsing. |
| Validation output → release claim | Test observations become documented guarantees. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-07-01 | Elevation of Privilege | removed selector | high | mitigate | Atomic source/export/test deletion plus canonical negative contracts. |
| T-04-07-02 | Tampering | signed descriptor/cursor | high | mitigate | Retain HMAC, revision, schema/query, and identity binding tests. |
| T-04-07-03 | Denial of Service | sparse scan | medium | mitigate | Work cap with last-examined cursor continuation. |
| T-04-07-04 | Repudiation | release matrix | medium | mitigate | Record exact runtime/status/failure classification; no false green. |
| T-04-07-SC | Tampering | package supply chain | low | accept | Frozen lockfile; no package change. |
</threat_model>
<verification>
- Canonical focused Phase 4 suite and Ruff delta are blocking.
- Python 3.11/3.13 full suites run only after the atomic cutover; canonical storage failures block, while the two inventoried Phase 6 policy-test files are recorded as deferred and never presented as green.
- External-API detector is not applicable: in-process storage interfaces only; PostgreSQL/S3 service work remains Phase 5.
</verification>
<multi_source_coverage_audit>
GOAL and BACK-02/03/06/07: COVERED by 04-01..04-07. D-01..D-04: 02/04. D-05..D-08: 02/04/05. D-09..D-12 and BACK-03 registered-name/exact injection: 03. D-13..D-16: 05/06. D-17..D-18: 02/06/07. PostgreSQL/S3 authority, UnifiedCache policy, historical migration: EXCLUDED to Phases 5/6/7.
</multi_source_coverage_audit>
<success_criteria>One public direct BlobStore contract remains; old surfaces/tests are atomically absent; canonical tests/Ruff pass; full-suite evidence is truthful and classified.</success_criteria>
<artifacts_produced>Clean package/storage exports, deleted compatibility tests, finalized catalog/topology docs, and executed validation evidence.</artifacts_produced>
<output>Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-07-SUMMARY.md` when done.</output>
