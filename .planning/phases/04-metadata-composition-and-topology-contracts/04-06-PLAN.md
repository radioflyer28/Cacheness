---
phase: 04-metadata-composition-and-topology-contracts
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/custom_metadata.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/core.py
  - src/cacheness/metadata.py
  - src/cacheness/storage/backends/postgresql_backend.py
  - tests/test_blob_manifest_backends.py
  - tests/test_manifest_repository_cas.py
  - tests/test_phase3_scheduler_retirement.py
  - tests/test_custom_metadata.py
  - tests/test_cached_custom_metadata.py
  - tests/test_projection_mutation_contract.py
  - tests/test_projection_sql_atomicity.py
  - tests/test_postgresql_backend.py
autonomous: true
requirements: [BACK-02, BACK-06, BACK-07]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Legacy manifest_repository behavior is consolidated into canonical catalog/projection paths and the module plus obsolete tests are removed without breaking package import."
    - "Legacy custom_metadata ORM runtime integration is removed from direct runtime consumers; only native CatalogSchema plus shared projection roles remain per D-03/D-13."
    - "Duplicate metadata ABC responsibility is prepared for the single composition role protocol; public factories/exports remain intact until Plan 07's atomic cutover."
    - "Intermediate verification is targeted and honest; the full suite is explicitly deferred because remaining legacy public consumers are retired atomically in Plan 07."
  artifacts:
    - {path: src/cacheness/storage/blob_store.py, provides: "Canonical replacement for manifest_repository usage"}
    - {path: src/cacheness/core.py, provides: "Minimal import-safe removal of legacy custom metadata calls, without policy migration"}
    - {path: tests/test_catalog_projection.py, provides: "Retained projection invariants already co-located with Plan 05 source"}
  key_links:
    - {from: src/cacheness/storage/blob_store.py, to: src/cacheness/storage/projections.py, via: "shared canonical projection controller replaces JsonProjectionExporter"}
    - {from: src/cacheness/metadata.py, to: src/cacheness/storage/catalog.py, via: "native schema/projection roles replace ORM authority hooks"}
  prohibitions:
    - "No compatibility shim for manifest_repository, custom_metadata, duplicate ABC, or ORM session hooks."
    - "No broad UnifiedCache/CacheConfig/decorator/global-cache/TTL/eviction/statistics migration."
    - "No removal of public factory/export names until Plan 07 can update all unconditional consumers atomically."
    - "No full-suite-green claim at this preparation boundary."
---

<objective>
Prepare the one-way cutover by consolidating legacy manifest/custom-metadata implementation and retiring its direct tests while keeping imports executable.

Purpose: Separate bounded implementation/test preparation from the final atomic public deletion.
Output: Removed manifest/custom runtime modules, prepared role protocol, deleted obsolete tests, and a complete consumer inventory for Plan 07.
</objective>
<requirement_decision_coverage>BACK-02/BACK-06/BACK-07; D-03, D-13, D-17, D-18. BACK-03 registered-name/exact-injection delivery remains owned by Plan 04-03.</requirement_decision_coverage>
<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-05-SUMMARY.md
</context>

<consumer_inventory>
- Manifest repository implementation/direct tests: `storage/manifest_repository.py`, `storage/blob_store.py`, `test_blob_manifest_backends.py`, `test_manifest_repository_cas.py`, `test_phase3_scheduler_retirement.py` → consolidate/delete here.
- Custom metadata runtime/direct tests: `custom_metadata.py`, `core.py`, `metadata.py`, `postgresql_backend.py`, `test_custom_metadata.py`, `test_cached_custom_metadata.py`, `test_projection_mutation_contract.py`, `test_projection_sql_atomicity.py`, `test_postgresql_backend.py` → remove obsolete integration/tests here; retain only invariants already represented by canonical Phase 4 schema/projection suites.
- Public factory/shape/filter consumers: package barrels, metadata/backend registry, BlobStore/lifecycle/read contract, `test_metadata_backend_registry.py`, `test_metadata.py`, `test_blob_store_read_contract.py`, `test_phase3_local_workflows.py`, `test_public_api_contract.py` → Plan 07 atomic cutover.
- `test_backend_compatibility.py` specifies CacheConfig/UnifiedCache policy selectors and is left unchanged for Phase 6; Phase 4 neither ports nor deletes it.
- `test_config_validation.py` contains only a stale explanatory comment for `get_metadata_backend`, not an executable consumer; it receives no Phase 4 behavioral port.
- `test_unified_cache_adversarial_lifecycle.py` and `test_unified_cache_lifecycle_authority.py` import the removed ORM custom-metadata surface but principally specify UnifiedCache policy. They are neither ported nor silently counted green here; their retirement/replacement is explicitly a Phase 6 full-suite delta recorded by Plan 07.
</consumer_inventory>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Consolidate manifest repository behavior and retire duplicate tests</name>
  <files>src/cacheness/storage/manifest_repository.py, src/cacheness/storage/blob_store.py, tests/test_blob_manifest_backends.py, tests/test_manifest_repository_cas.py, tests/test_phase3_scheduler_retirement.py</files>
  <read_first>src/cacheness/storage/manifest_repository.py; src/cacheness/storage/blob_store.py; tests/test_blob_manifest_backends.py; tests/test_manifest_repository_cas.py; tests/test_phase3_scheduler_retirement.py; tests/test_catalog_projection.py</read_first>
  <behavior>
    - "BlobStore uses the canonical authority CatalogPage and Plan 05 projection controller; no private JsonProjectionExporter/repository lifecycle remains."
    - "Useful bounded snapshot/checkpoint/CAS invariants already covered in canonical catalog/projection tests remain green."
    - "Obsolete implementation-shape tests are deleted rather than translated."
  </behavior>
  <action>Remove `manifest_repository.py` after replacing BlobStore's direct exporter/repository usage with Plan 04-05's shared projection controller and canonical authority snapshot. Delete the three tests whose purpose is the private repository/exporter shape. Do not port private class/attribute assertions; canonical bounded pull, checkpoint, and commit invariants stay in `test_catalog_projection.py`. Update imports in this task so `python -c 'import cacheness'` and canonical direct BlobStore tests still collect.</action>
  <verify><automated>uv run --frozen python -c "import cacheness; import cacheness.storage" &amp;&amp; uv run --frozen pytest -q tests/test_catalog_projection.py tests/test_blob_store_composition.py -o log_cli=false &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated></verify>
  <acceptance_criteria>The private module/tests are absent; package import works; canonical projection/composition tests pass; no replacement shim exists.</acceptance_criteria>
  <done>Manifest projection functionality has one canonical implementation and no duplicate test contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove legacy custom ORM runtime integration and prepare the single role protocol</name>
  <files>src/cacheness/custom_metadata.py, src/cacheness/core.py, src/cacheness/metadata.py, src/cacheness/storage/backends/postgresql_backend.py, tests/test_custom_metadata.py, tests/test_cached_custom_metadata.py, tests/test_projection_mutation_contract.py, tests/test_projection_sql_atomicity.py, tests/test_postgresql_backend.py</files>
  <read_first>src/cacheness/custom_metadata.py; src/cacheness/core.py; src/cacheness/metadata.py; src/cacheness/storage/backends/postgresql_backend.py; tests/test_custom_metadata.py; tests/test_cached_custom_metadata.py; tests/test_projection_mutation_contract.py; tests/test_projection_sql_atomicity.py; tests/test_postgresql_backend.py; tests/test_catalog_schema.py; tests/test_metadata_role_contract.py</read_first>
  <behavior>
    - "Custom ORM model/session hooks cannot authorize or mutate canonical state; native schema and the shared composition projection role are the surviving semantics."
    - "PostgreSQL-facing code advertises projection-only capability without live service validation."
    - "Core receives only import/call-site removal needed for package safety; cache policy behavior is untouched."
    - "Obsolete custom-metadata/SQL-atomicity/registry tests are deleted, not translated."
  </behavior>
  <action>Remove `custom_metadata.py` and direct ORM-session/model-registry hooks from core, metadata, and the PostgreSQL-facing backend. Reuse `composition.py`'s projection protocol for surviving projection-facing behavior. Leave the duplicate base ABC and public factory names intact until Plan 07 can remove them with all unconditional imports. Delete the five obsolete integration tests; retain schema validation, projection role, committed-partial, topology, and no-cross-resource-atomicity invariants only in canonical tests from Plans 02/03/05. Keep core changes strictly import/call-site cleanup—no constructor, decorator, TTL, eviction, statistics, or policy redesign.</action>
  <verify><automated>uv run --frozen python -c "import cacheness; import cacheness.storage" &amp;&amp; uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_metadata_role_contract.py tests/test_catalog_projection.py -o log_cli=false &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated></verify>
  <acceptance_criteria>Package import and canonical suites pass; removed ORM/session integration and obsolete tests are absent; public cutoff remains sequenced for Plan 07.</acceptance_criteria>
  <done>Legacy custom metadata no longer forms a runtime authority or test obligation.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Trust Boundaries
| Boundary | Description |
|---|---|
| Legacy repository/ORM state → canonical authority | Old derived state must not become authority during consolidation. |
| Preparation → public cutover | Imports must remain executable until atomic name deletion. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-06-01 | Elevation of Privilege | ORM projection | high | mitigate | Remove authority/session hooks; shared projection role only. |
| T-04-06-02 | Tampering | manifest export | high | mitigate | Route only from authenticated canonical pages. |
| T-04-06-03 | Denial of Service | intermediate imports | medium | mitigate | Run package import plus targeted canonical tests after each task. |
| T-04-06-SC | Tampering | package supply chain | low | accept | No package change. |
</threat_model>
<verification>Targeted import and canonical test gates only. Full suite is intentionally deferred to Plan 07 after remaining public consumers are deleted atomically.</verification>
<success_criteria>Legacy manifest/custom runtime integrations and their obsolete tests are gone, canonical invariants pass, and package imports remain executable.</success_criteria>
<artifacts_produced>Consolidated BlobStore projection path, removed legacy modules/tests, single role protocol preparation, and explicit consumer inventory.</artifacts_produced>
<output>Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-06-SUMMARY.md` when done.</output>
