---
phase: 04-metadata-composition-and-topology-contracts
plan: 05
type: execute
wave: 4
depends_on: [04-04]
files_modified:
  - src/cacheness/storage/projections.py
  - src/cacheness/storage/catalog.py
  - src/cacheness/storage/lifecycle_authority.py
  - src/cacheness/storage/read_contract.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/error_handling.py
  - tests/test_catalog_projection.py
  - tests/test_metadata_role_contract.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "JSON/ORM/PostgreSQL-facing outputs have projection-only roles in Phase 4 and cannot authorize lifecycle or canonical query results per D-13."
    - "Projection pulls consume bounded canonical pages tied to one authority revision and advance idempotent checkpoints only after apply per D-08 and D-14."
    - "Projection failure never revokes authority commit; frozen BlobReceipt reports per-projection status and typed committed-partial retains that receipt per D-15."
    - "Refresh and isolated rebuild are explicit capability-gated operations; SQLite publication/schema change is offline and no historical migration executes per D-16/D-18."
  artifacts:
    - {path: src/cacheness/storage/projections.py, provides: "Projection role, bounded pull/checkpoint, refresh, isolated rebuild contracts"}
    - {path: tests/test_catalog_projection.py, provides: "Revision, interruption, partial outcome, refresh/rebuild coverage"}
    - {path: tests/test_metadata_role_contract.py, provides: "Projection cannot satisfy authority role"}
  key_links:
    - {from: src/cacheness/storage/projections.py, to: src/cacheness/storage/catalog.py, via: "revision-bound canonical CatalogPage pulls"}
    - {from: src/cacheness/storage/blob_store.py, to: src/cacheness/storage/read_contract.py, via: "post-commit projection status in BlobReceipt"}
  prohibitions:
    - "No projection authority, background queue/worker, or cross-resource transaction fiction."
    - "No live PostgreSQL/S3 delivery claim."
    - "No historical layout migration execution."
    - "No unbounded pull, offset cursor, or projection-only query completeness."
---

<objective>
Add honest derived projection delivery, bounded resumable pull, and explicit recovery operations without weakening canonical authority.

Purpose: Satisfy D-13 through D-16 using the ADR's topology-specific transaction boundary.
Output: Projection protocol/controller, checkpoints, committed-partial receipt semantics, refresh, and isolated rebuild contracts.
</objective>
<requirement_decision_coverage>
BACK-02/BACK-03: derived outputs consume complete typed catalog snapshots. BACK-06: role/capability validation. BACK-07: idempotent checkpoints and topology-qualified interruption. D-08, D-13, D-14, D-15, D-16, D-18.
</requirement_decision_coverage>
<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-04-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Pull bounded revision snapshots into role-checked projections</name>
  <files>src/cacheness/storage/projections.py, src/cacheness/storage/catalog.py, src/cacheness/storage/lifecycle_authority.py, tests/test_catalog_projection.py, tests/test_metadata_role_contract.py</files>
  <read_first>tests/test_catalog_projection.py; tests/test_metadata_role_contract.py; src/cacheness/storage/catalog.py; src/cacheness/storage/lifecycle_authority.py; docs/adr/0001-topology-specific-storage-guarantees.md</read_first>
  <behavior>
    - "ProjectionSink is projection-only; JSON/ORM/PostgreSQL-facing fakes fail lifecycle-authority minima (D-13)."
    - "First page captures source store/epoch/schema/query/revision; all later pages remain bound or fail stale/retryable (D-08/D-14)."
    - "Apply then checkpoint makes page replay idempotent; interruption before/after advancement resumes deterministically."
    - "Page size, predicates, membership, metadata bytes, and work per pull are bounded; source is canonical scan, not acceleration."
  </behavior>
  <action>Implement ProjectionRole, ProjectionSink, ProjectionCheckpoint, ProjectionBatch, ProjectionStatus, and ProjectionController. Pull only through BlobStore's canonical CatalogPage interface and pin the first revision across the run. Use idempotent sink identities and apply-before-checkpoint ordering. Enforce a maximum page/work budget per call. Model JSON/ORM/PostgreSQL as projection roles/capability examples only; do not implement PostgreSQL authority or require live services. Ensure a projection cannot expose lifecycle mutation or claim portable query authority.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_catalog_projection.py tests/test_metadata_role_contract.py -k "role or pull or bounded or checkpoint or interruption or revision" -x &amp;&amp; uv run --frozen ruff check src/cacheness/storage/projections.py tests/test_catalog_projection.py tests/test_metadata_role_contract.py</automated></verify>
  <acceptance_criteria>Projection role rejection, revision pinning, bounded pull, idempotent replay, and interruption resume all pass without acceleration or external services.</acceptance_criteria>
  <done>Derived consumers can copy one complete bounded canonical revision without becoming authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve commits through projection failure and expose refresh/rebuild</name>
  <files>src/cacheness/storage/read_contract.py, src/cacheness/storage/blob_store.py, src/cacheness/storage/projections.py, src/cacheness/error_handling.py, tests/test_catalog_projection.py</files>
  <read_first>src/cacheness/storage/read_contract.py; src/cacheness/storage/blob_store.py; src/cacheness/storage/projections.py; src/cacheness/error_handling.py; tests/test_catalog_projection.py</read_first>
  <behavior>
    - "Authority success plus projection failure remains committed and returns best-effort BlobReceipt status or raises committed-partial carrying the same receipt (D-15)."
    - "Refresh resumes bounded canonical pages from a checkpoint and is repeatable."
    - "Rebuild writes an isolated destination and publishes only under the advertised online/offline capability (D-16)."
    - "SQLite rebuild/schema publication is offline; unsupported source versions raise migration/rebuild-required and are never converted (D-18)."
  </behavior>
  <action>Extend frozen BlobReceipt with immutable named projection outcomes and create a typed committed-partial exception carrying it. Invoke configured projections only after the authority commit; a failure cannot roll back, conceal, or redefine the committed generation. Add explicit refresh and isolated rebuild methods to ProjectionController, validated against topology capabilities. Require stopped-worker/offline publication for SQLite and leave historical migration execution to Phase 7. Add no retry queue, sidecar coordinator, extra lifecycle lock, or filesystem/SQLite transaction claim.</action>
  <verify><automated>uv run --frozen pytest -q tests/test_catalog_projection.py -k "receipt or committed_partial or failure or refresh or rebuild or offline" -x &amp;&amp; uv run --frozen python tools/verify_phase4_ruff_delta.py</automated></verify>
  <acceptance_criteria>Tests preserve the authority receipt on projection failure and prove explicit refresh/rebuild isolation, capability gating, and offline SQLite semantics.</acceptance_criteria>
  <done>Projection failures are attributable recovery work, never false rollback or canonical corruption.</done>
</task>
</tasks>

<threat_model>
## ASVS Level 1 Trust Boundaries
| Boundary | Description |
|---|---|
| Authority snapshot → projection | Authenticated canonical data enters a less-trusted derived sink. |
| Checkpoint → resume | Persistent cursor state controls continuation. |
| Projection outcome → caller | Derived failure is reported alongside committed authority state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-05-01 | Tampering | checkpoint | high | mitigate | Bind identity/epoch/schema/query/revision/cursor and reject stale state. |
| T-04-05-02 | Elevation of Privilege | projection role | high | mitigate | Capability minima deny authority methods. |
| T-04-05-03 | Denial of Service | pull | medium | mitigate | Bounded pages and invocation work. |
| T-04-05-04 | Repudiation | partial result | medium | mitigate | Frozen receipt names authority revision and projection outcomes. |
| T-04-05-SC | Tampering | package supply chain | low | accept | No package task. |
</threat_model>
<verification>`uv run --frozen pytest -q tests/test_catalog_projection.py tests/test_metadata_role_contract.py -o log_cli=false`</verification>
<success_criteria>Projection pull, checkpoints, partial receipts, refresh, and rebuild remain bounded, explicit, and subordinate to authority.</success_criteria>
<artifacts_produced>`projections.py`, updated frozen receipt/error contracts, and passing canonical projection/role tests.</artifacts_produced>
<output>Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-05-SUMMARY.md` when done.</output>
