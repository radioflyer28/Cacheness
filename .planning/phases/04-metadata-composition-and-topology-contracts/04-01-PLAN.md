---
phase: 04-metadata-composition-and-topology-contracts
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/fixtures/phase4_ruff_baseline.json
  - tools/verify_phase4_ruff_delta.py
  - tests/test_catalog_schema.py
  - tests/test_catalog_query_contract.py
  - tests/test_blob_store_composition.py
  - tests/test_topology_capabilities.py
  - tests/test_metadata_role_contract.py
  - tests/test_catalog_projection.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-06, BACK-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Before production edits, executable contracts describe the clean store-format, catalog, composition, topology, and projection outcomes for BACK-02, BACK-03, BACK-06, and BACK-07 (D-01 through D-18)."
    - "The contract suite treats the composed store topology as the primary noun and rejects the legacy metadata_backend selector model rather than preserving it beside the new root (D-09, D-17)."
    - "The pre-phase Ruff fingerprint is frozen before implementation so Phase 4 can prove a no-new-findings delta without pretending the repository baseline is clean."
  artifacts:
    - path: tests/test_catalog_schema.py
      provides: "Wave 0 schema, format-version, validation, and unsupported-layout contract"
    - path: tests/test_catalog_query_contract.py
      provides: "Wave 0 typed predicate, complete-page, and revision-bound cursor contract"
    - path: tests/test_blob_store_composition.py
      provides: "Wave 0 one-root, exact-instance, ownership, and selector-retirement contract"
    - path: tests/test_catalog_projection.py
      provides: "Wave 0 pull/checkpoint/partial/rebuild contract"
    - path: tools/verify_phase4_ruff_delta.py
      provides: "Stable-fingerprint Ruff delta gate for the complete Phase 4 scope"
  key_links:
    - from: tests/test_catalog_query_contract.py
      to: tests/test_catalog_schema.py
      via: "Shared public schema/query values exercised without a private helper contract"
      pattern: "Catalog(Schema|Query|Predicate)"
    - from: tests/test_blob_store_composition.py
      to: tests/test_topology_capabilities.py
      via: "The same fake participants prove selection identity and composed semantic minima"
      pattern: "Fake.*(Payload|Authority|Projection)"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Phase 4 must not add another lifecycle coordinator, lock, queue, sidecar, or cross-resource atomicity claim."
    - "[FLAGGED/UNVERIFIED] A derived projection must not authorize canonical read, query completeness, delete, cleanup, reconciliation, or repair."
    - "[FLAGGED/UNVERIFIED] Unsupported development layouts must not be rewritten, deleted, or reopened through a runtime compatibility path."
---

<objective>
Create the complete Wave 0 contract and quality baseline before any Phase 4 production implementation.

Purpose: Make the compatibility reset, first supported format, catalog/query semantics, topology guarantees, and finite failure cases executable before code can accidentally preserve the superseded surface.
Output: Six focused contract suites and a checked-in Ruff delta gate.
</objective>

## Artifacts this plan produces

- Contract tests for D-01 through D-18 and all four Phase 4 requirements.
- Deterministic memory/SQLite fakes and transaction/projection fault seams inside the six canonical suites.
- A pre-edit Phase 4 Ruff baseline and verifier.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-CONTEXT.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-RESEARCH.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-PATTERNS.md
@.planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md
@docs/adr/0001-topology-specific-storage-guarantees.md
</context>

<execution_shape>
This is the explicit pre-implementation Wave 0 exception. Its tests begin red; the first executable tracer is Plan 04-03 Task 2, after registry resolution exists.
</execution_shape>

<assumption_delta_decision>
Primary noun: composed store topology. Decision: promote. `StoreTopology` becomes the construction and identity anchor; the legacy `metadata_backend` selector is removed rather than added alongside it.
</assumption_delta_decision>

<planner_assumptions>
- BACK-02/BACK-03/BACK-06 applicability is explicit: JSON and PostgreSQL-facing metadata are projections in Phase 4; memory and SQLite are authorities at their declared scopes; exact injection/registration and semantic capability minima apply to every composed role.
- BACK-07 idempotency is explicit: repeated initialization of a valid current-format store validates unchanged state; it is not schema migration and performs no repeated DDL.
- BACK-07 interruption/concurrency is topology-qualified: memory is same-process only; SQLite/local is one-host and may return exact conflict or typed retryable timeout while preserving integrity; PostgreSQL/S3 service qualification is not claimed.
- The three `must_haves.prohibitions` entries are descriptor-less recall and remain flagged/unverified; this plan does not invent check descriptors for them.
- Deterministic external-service API detection is not applicable to this in-process Python storage contract: no remote endpoint, webhook, or new third-party SDK is introduced, and live PostgreSQL/S3 qualification remains Phase 5.
</planner_assumptions>

<requirement_decision_coverage>
| Task | Requirements | Decisions |
|---|---|---|
| 1 | BACK-02, BACK-03, BACK-06, BACK-07 quality gate | D-17, D-18 |
| 2 | BACK-02, BACK-03, BACK-06, BACK-07 | D-01 through D-18 |
</requirement_decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the Phase 4 Ruff delta before source changes</name>
  <depends_on>None.</depends_on>
  <files>tests/fixtures/phase4_ruff_baseline.json, tools/verify_phase4_ruff_delta.py</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>tests/fixtures/phase3_ruff_baseline.json</file>
    <file>tools/verify_phase3_ruff_delta.py</file>
    <file>.planning/phases/04-metadata-composition-and-topology-contracts/04-PATTERNS.md</file>
  </read_first>
  <behavior>
    - "The baseline records stable, line-independent fingerprints for every existing Phase 4-owned Python file before edits."
    - "Removed findings and intentionally deleted files pass; every new or changed finding, malformed baseline, duplicated path, or undeclared scope drift fails."
    - "Every Phase 4-new Python file must be Ruff-clean and receives no grandfathered finding."
  </behavior>
  <action>Adapt the Phase 3 stable-multiset Ruff gate to the exact Phase 4 source/test/tool inventory and the legacy deletion targets in Plans 04-06/04-07. Capture the baseline before any production edit. Separate existing paths, new paths, and expected deletion targets; include old metadata registry/factory, read-contract, and projection-exporter files as deletion/consolidation targets per D-17 while retaining absent-new-file handling. Use repository-relative normalized paths and rule/message/source fingerprints, not line numbers. Do not alter Ruff configuration or add blanket suppressions.</action>
  <verify>
    <automated>uv run --frozen python tools/verify_phase4_ruff_delta.py</automated>
  </verify>
  <acceptance_criteria>The baseline is captured at the unmodified Phase 4 start and the verifier fails on synthetic new findings, changed diagnostics, undeclared files, or dirty new files while permitting removals.</acceptance_criteria>
  <done>Phase 4 has a trustworthy no-new-Ruff-regression oracle before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify the current-format catalog, composition, topology, and projection contracts</name>
  <depends_on>None; may run after Task 1 in the same plan.</depends_on>
  <files>tests/test_catalog_schema.py, tests/test_catalog_query_contract.py, tests/test_blob_store_composition.py, tests/test_topology_capabilities.py, tests/test_metadata_role_contract.py, tests/test_catalog_projection.py</files>
  <read_first>
    <file>src/cacheness/storage/manifest.py</file>
    <file>src/cacheness/storage/lifecycle_authority.py</file>
    <file>src/cacheness/query_validation.py</file>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/backends/__init__.py</file>
    <file>src/cacheness/storage/backends/base.py</file>
    <file>src/cacheness/storage/manifest_repository.py</file>
    <file>docs/adr/0001-topology-specific-storage-guarantees.md</file>
  </read_first>
  <behavior>
    - "The only supported current store/catalog format is distinguishable version 2; a development-version-1 root or manifest is detected read-only and raises typed migration/rebuild-required evidence without mutation (D-04, D-17, D-18)."
    - "Mapping metadata works without a schema; a native schema validates only declared string/signed-64-integer/boolean/nullable scalar fields, materializes defaults on every new write, preserves undeclared bounded values, and never coerces bool to integer (D-01 through D-04)."
    - "Predicates evaluate stored presence: an absent field never equals its declared default, and Phase 4 exposes no effective-value query mode (D-04)."
    - "Typed equality, range/comparison, membership, and existence predicates compose with AND over declared queryable fields; validation and bounds occur before authority dispatch (D-05, D-07)."
    - "Pages are bounded, ordered by key then generation, complete at one authority revision, and use an authenticated opaque cursor bound to store, format, schema, query, revision, and last identity (D-06, D-08)."
    - "A sparse scan stopped by work cap succeeds with exhausted=false and advances its authenticated cursor to the last examined canonical identity even when zero entries matched."
    - "One `StoreTopology` chooses payload, authority, projections, ownership, schema, and capability minima; name+instance or options+instance fail before initialization (D-09, D-10)."
    - "Injected objects remain identical and caller-owned unless ownership transfer is explicit; constructed/transfer-owned objects close exactly once, including failed construction (D-10, D-12)."
    - "Capabilities come from actual participants and reject impossible durability/sharing/CAS/streaming/listing/query claims before payload staging (D-11)."
    - "JSON and PostgreSQL-facing metadata are projection-only; memory and SQLite are authority implementations at different scopes; projections never satisfy authority minima (D-13)."
    - "Projection pull is bounded/idempotent/checkpointed, explicit refresh preserves a committed receipt on failure, and interrupted rebuild never replaces the prior published projection (D-14 through D-16)."
  </behavior>
  <action>Create all six red contract suites for the exact clean semantics. Lock `STORE_FORMAT_VERSION = 2`, page/membership/predicate limits, and a distinct scan work cap. Exercise null, stored absence, materialized defaults, unknown fields, every operator/type mismatch, tampered cursors, revision change, and sparse scans. Assert a stale cursor is retryable and a work-capped non-exhausted page advances to the last examined canonical identity even with zero matches. Test one immutable BlobReceipt result. With local fake participants, cover built-in/registered-name parity, exact injection, options isolation, ownership transfer, close races, construction-failure cleanup, role confusion, capability minima, duplicate/interrupted projection pages, monotonic checkpoints, committed-partial attribution, and online/offline rebuild capability. Add AST/public-surface assertions for the final D-17 cutover: overloaded backend selection, private authority/projection selectors, metadata factory/getter paths, dictionary list filtering, BlobEntryInfo, and runtime ORM-session hooks are absent. Tests must fail as assertions, not collection errors; require neither live PostgreSQL/S3 nor universal contention success.</action>
  <verify>
    <automated>bash -c 'uv run --frozen pytest --collect-only -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py tests/test_blob_store_composition.py tests/test_topology_capabilities.py tests/test_metadata_role_contract.py tests/test_catalog_projection.py &amp;&amp; uv run --frozen pytest -q tests/test_catalog_schema.py tests/test_catalog_query_contract.py tests/test_blob_store_composition.py tests/test_topology_capabilities.py tests/test_metadata_role_contract.py tests/test_catalog_projection.py; code=$?; test "$code" -eq 1'</automated>
  </verify>
  <acceptance_criteria>All six suites collect, then fail red on missing Phase 4 behavior while naming every finite schema/query/cursor/version/composition/projection outcome above.</acceptance_criteria>
  <done>The entire clean Phase 4 contract exists before implementation and cannot be weakened into legacy layout, list-filter, constructor, or result-shape compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test fixtures → schema/query interfaces | Hostile types, sizes, cursor bytes, and role declarations must be rejected before backend work. |
| Existing repository → quality baseline | Pre-existing findings may be grandfathered exactly; changed findings must not be hidden. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | schema/query/cursor contracts | high | mitigate | Exact-type, bounded-input, HMAC-context, pre-dispatch, and non-mutating-layout tests. |
| T-04-02 | Elevation of privilege | role/projection contracts | high | mitigate | Tests deny projection authority and construction-time capability inflation. |
| T-04-03 | Repudiation | committed-partial outcomes | medium | mitigate | Require receipt, projection identity, checkpoint, and remaining-work attribution. |
| T-04-04 | Denial of service | Ruff baseline | low | accept | Local deterministic gate only; bounded path inventory and no external input. |

Package legitimacy: not applicable; this plan adds no package-manager dependency.
</threat_model>

<verification>
Run the three task commands. Confirm all new suites collect; Wave 0 behavior suites exit with pytest code 1, never code 2+, and the Ruff delta self-tests pass. Do not run production behavior tests as green acceptance yet.
</verification>

<success_criteria>
- Every 04-VALIDATION Wave 0 file exists and collects.
- Each D-01 through D-18 behavior is named by a test before production edits.
- Ruff delta evidence predates source changes.
- The Wave 0 contracts claim neither PostgreSQL/S3 live qualification nor cross-resource ACID.
</success_criteria>

<output>
Create `.planning/phases/04-metadata-composition-and-topology-contracts/04-01-SUMMARY.md` when done.
</output>
