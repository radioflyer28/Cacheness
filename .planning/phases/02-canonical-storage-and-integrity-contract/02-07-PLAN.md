---
phase: 02-canonical-storage-and-integrity-contract
plan: "07"
type: execute
wave: 5
depends_on: [02-06]
files_modified:
  - src/cacheness/storage/legacy_manifest.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/security.py
  - tests/test_blob_store_legacy_contract.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-01, STOR-02, STOR-08, SECU-03, SECU-04, SECU-05, SECU-08, MIGR-02, MIGR-07]
estimate:
  tokens: 31000
  raw_tokens: 31000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Exact Phase 1 legacy identities are recognized read-only, attached in memory, and either read safely or return typed migration-required/unsupported outcomes (MIGR-07 planner assumption, unclassified by the generic edge probe; D-12, D-13)."
    - "All eight compatibility fixture trees retain identical bytes and mtimes after get, metadata, exists, list, classification, and failure paths."
    - "Canonical schema, committed visibility, typed outcomes, authenticity, complete signed fields, and independent payload versions remain green together (all Phase 2 requirement assumptions)."
    - "Unknown-future and corrupt records yield typed outcomes with identical pre/post bytes, mtimes, keys, and access metadata."
  artifacts:
    - path: src/cacheness/storage/legacy_manifest.py
      provides: "Exact read-only recognition and in-memory identity for supported Phase 1 legacy layouts"
    - path: tests/test_blob_store_legacy_contract.py
      provides: "Eight-fixture non-mutation and migration/version outcome contract"
    - path: tests/test_blob_store_read_contract.py
      provides: "Final cross-surface ordered outcome regression"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/legacy_manifest.py
      via: "Exact legacy discriminator runs without canonical guessing or persistence"
      pattern: "(recognize|adapt)_legacy"
    - from: tests/test_blob_store_legacy_contract.py
      to: tests/fixtures/compat/validate_corpus.py
      via: "Normative eight-fixture source/copy digest and mtime proof"
      pattern: "validate_corpus"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Seal the compatibility edge and prove the complete Phase 2 contract without mutating legacy or future-version evidence.

Purpose: Preserve the milestone compatibility promise while making canonical writes authoritative and migration explicitly inspectable.
Output: Exact legacy adapters, eight-fixture immutability proof, and a complete cross-requirement phase gate.
</objective>

## Artifacts this phase produces

- Exact read-only identities for the supported Phase 1 compatibility corpus.
- Typed migration-required and unsupported-version outcomes with no read-time conversion.
- A complete automated Phase 2 requirement/threat regression gate.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-12-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-VERIFICATION.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-06-SUMMARY.md
</context>

<planner_assumptions>
- MIGR-07 and the other eight Phase 2 requirements were unclassified by the generic shape probe; this plan treats exact legacy classification, typed conversion-needed outcomes, and the final complete regression as direct requirement/context acceptance.
- The three must_haves.prohibitions remain descriptor-less and flagged-unverified until Task 2's semantic tests pass; they are not converted into inferred positive truths.
- Phase 7 owns inventory, copy-verify-switch, resumption, and rebuild confirmation. Phase 2 only identifies exact legacy evidence and reports conversion requirements.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recognize exact legacy records and preserve every fixture byte</name>
  <depends_on>Plan 02-06 completes canonical operation semantics and Plan 02-04 supplies migration/version reasons.</depends_on>
  <files>src/cacheness/storage/legacy_manifest.py, src/cacheness/storage/blob_store.py, src/cacheness/security.py, tests/test_blob_store_legacy_contract.py</files>
  <read_first>
    <file>tests/fixtures/compat/validate_corpus.py</file>
    <file>tests/test_stored_compatibility.py</file>
    <file>src/cacheness/metadata.py</file>
    <file>src/cacheness/security.py</file>
  </read_first>
  <behavior>
    - "Each of the exact eight fixture identities is recognized without directory scanning or speculative fallback."
    - "Supported safe legacy reads use Phase 1 bounded/native readers; incompatible conversion-needed entries raise migration-required or unsupported-version with explicit in-memory identity."
    - "Wrong/malformed legacy signature and unknown lookalike layouts fail typed and do not fall through to canonical or another legacy reader."
    - "Source and copied fixture bytes plus mtimes remain unchanged after every operation and failure."
  </behavior>
  <action>Build `tests/test_blob_store_legacy_contract.py` from copies of the immutable eight-fixture corpus, recording recursive SHA-256 and mtimes before and after. Add a narrow exact-layout recognizer that attaches explicit schema/payload identities only in memory and delegates only to the already verified Phase 1 bounded readers/signature verifier. Do not enumerate for guesses, write canonical manifests, rotate keys, update access time, or delete invalid evidence. Return deprecation warnings for safely readable legacy paths and exact migration-required/unsupported outcomes where D-13 requires conversion. Keep new writes exclusively canonical per D-12 and keep migration execution absent.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_legacy_contract.py -x &amp;&amp; uv run ruff check src/cacheness/storage/legacy_manifest.py src/cacheness/storage/blob_store.py src/cacheness/security.py tests/test_blob_store_legacy_contract.py</automated>
  </verify>
  <acceptance_criteria>All eight fixture cases have exact identities/outcomes; source/copy digests and mtimes are unchanged; malformed or lookalike evidence never triggers fallback, mutation, or deletion.</acceptance_criteria>
  <done>Legacy compatibility is explicit, bounded, inspectable, and read-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the full canonical, integrity, and non-mutation contract</name>
  <depends_on>Task 1 completes the final compatibility edge; Plans 02-01 through 02-06 supply all canonical paths.</depends_on>
  <files>src/cacheness/storage/blob_store.py, tests/test_blob_store_legacy_contract.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>.planning/phases/02-canonical-storage-and-integrity-contract/02-VALIDATION.md</file>
    <file>tests/test_blob_manifest.py</file>
    <file>tests/test_blob_manifest_backends.py</file>
    <file>tests/test_blob_store_integrity.py</file>
    <file>tests/test_blob_store_translation_seam.py</file>
  </read_first>
  <behavior>
    - "All six Wave 0 files jointly cover every Phase 2 requirement and required adversarial dimension."
    - "Representative native payloads contain no added Cacheness framing and every failure path preserves evidence."
    - "Full Phase 1 compatibility, containment, cache-integrity, and clear-recovery regressions remain green."
  </behavior>
  <action>Audit the six Wave 0 suites against `02-VALIDATION.md` and add only missing cross-surface assertions: canonical byte parity after reopen, full signed-field mutation, exact ordered events, same-snapshot races, local repository failure translation, eight-fixture immutability, and pure cache classification. Exercise all direct read surfaces against absent, corrupt, conflict, unsupported, and backend failures. Retain all scope fences: no CAS/reconciliation generalization, PostgreSQL/S3 matrix, UnifiedCache rewiring, or migration runner. Run the independent corpus validator and final phase regression commands without suppressing baseline failures.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_manifest.py tests/test_blob_manifest_backends.py tests/test_blob_store_read_contract.py tests/test_blob_store_integrity.py tests/test_blob_store_legacy_contract.py tests/test_blob_store_translation_seam.py -x &amp;&amp; uv run pytest -q -o log_cli=false tests/test_stored_compatibility.py tests/test_filesystem_containment.py tests/test_cache_integrity.py tests/test_clear_recovery.py -x &amp;&amp; uv run python tests/fixtures/compat/validate_corpus.py --expected-through sqlite-columns-v0314 &amp;&amp; uv run pytest -q -o log_cli=false &amp;&amp; uv run ruff check src/cacheness/storage/manifest.py src/cacheness/storage/manifest_repository.py src/cacheness/storage/integrity.py src/cacheness/storage/read_contract.py src/cacheness/storage/legacy_manifest.py src/cacheness/storage/blob_store.py src/cacheness/storage/__init__.py src/cacheness/error_handling.py src/cacheness/security.py src/cacheness/interfaces.py src/cacheness/handlers.py tests/test_blob_manifest.py tests/test_blob_manifest_backends.py tests/test_blob_store_read_contract.py tests/test_blob_store_integrity.py tests/test_blob_store_legacy_contract.py tests/test_blob_store_translation_seam.py</automated>
  </verify>
  <acceptance_criteria>All Wave 0, Phase 1 regression, corpus, and changed-file Ruff gates pass; requirement/threat mapping has no uncovered case; the three descriptor-less prohibitions have semantic executable proof.</acceptance_criteria>
  <done>Direct BlobStore callers receive the complete canonical, committed, fail-closed, version-aware, non-mutating Phase 2 contract.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V1 Encoding and Sanitization — exact legacy format recognition | Only exact bounded Phase 1 layouts select compatibility behavior; lookalikes receive typed outcomes. | Eight-fixture and malformed-lookalike matrix. |
| V5 File Handling — read-only evidence preservation | Compatibility reads operate on copies/guarded snapshots and preserve recursive hashes plus mtimes. | Corpus pre/post evidence assertions. |
| V11 Cryptography — legacy/canonical authenticity separation | Exact historical signature verification stays isolated from canonical HMAC and never authorizes another layout. | Wrong-key, malformed-signature, and cross-contract cases. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy/unknown record → format recognizer | Historical bytes and lookalikes must not select unsafe or mutating compatibility behavior. |
| compatibility reader → canonical lifecycle | Reading old evidence must not become implicit migration, signing, deletion, or canonical authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Tampering / Elevation of Privilege | legacy discriminator | high | mitigate | Exact layout/signature/identity matching, bounded Phase 1 readers, and no speculative fallback or payload access for lookalikes. |
| T-02-21 | Repudiation | legacy/future read evidence | high | mitigate | Recursive pre/post hashes and mtimes plus mutation spies prove reads never alter the only copy. |
| T-02-22 | Tampering | cross-contract regression | high | mitigate | Full requirement/adversarial suite blocks any bypass of canonical auth, committed visibility, format dispatch, or same-snapshot verification. |
| T-02-23 | Elevation of Privilege | trusted pickle/dill payload | medium | accept | Application payloads remain explicitly trusted per D-11 and Phase 1 security documentation; authenticity/integrity do not claim sandboxing. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-20 | `uv run pytest -q -o log_cli=false tests/test_blob_store_legacy_contract.py -k "identity or lookalike or signature" -x` |
| T-02-21 | `uv run pytest -q -o log_cli=false tests/test_blob_store_legacy_contract.py -k "hash or mtime or non_mutating" -x` |
| T-02-22 | `uv run pytest -q -o log_cli=false tests/test_blob_manifest.py tests/test_blob_manifest_backends.py tests/test_blob_store_read_contract.py tests/test_blob_store_integrity.py tests/test_blob_store_legacy_contract.py tests/test_blob_store_translation_seam.py -x` |

Each high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run all six Phase 2 Wave 0 files, Phase 1 compatibility/security/recovery regressions, the independent eight-fixture validator, and Ruff on Phase 2-created files. High-severity legacy mutation, guessing, auth-order, and payload-tamper cases are blocking.
</verification>

<success_criteria>
- Exact legacy layouts are read-only or produce typed conversion-needed outcomes.
- Unknown future and corrupt evidence remain untouched and explicit.
- Every Phase 2 requirement and adversarial dimension has an automated passing case.
- Full compatibility, containment, and recovery regressions remain green.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-07-SUMMARY.md` when done.
</output>
