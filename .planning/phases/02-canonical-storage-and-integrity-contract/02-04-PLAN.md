---
phase: 02-canonical-storage-and-integrity-contract
plan: "04"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/cacheness/error_handling.py
  - src/cacheness/storage/read_contract.py
  - src/cacheness/storage/__init__.py
  - tests/test_blob_store_translation_seam.py
autonomous: true
requirements: [STOR-08, SECU-08, MIGR-07]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct operations distinguish absence, corruption/authenticity, lifecycle conflict, unsupported version, migration required, and backend failure with stable public types/reasons (STOR-08 planner assumption, unclassified by the generic edge probe; D-05)."
    - "BlobStore integrity failures are classifiable through a pure seam without importing, constructing, mutating, or recounting UnifiedCache (SECU-08 planner assumption, unclassified by the generic edge probe; D-14)."
    - "Unknown or legacy conversion-needed records remain inspectable typed outcomes, not misses or read-side migration (MIGR-07 planner assumption, unclassified by the generic edge probe; D-04, D-13)."
  artifacts:
    - path: src/cacheness/error_handling.py
      provides: "Public BlobStore error hierarchy and stable CacheReason values"
    - path: src/cacheness/storage/read_contract.py
      provides: "Pure direct-read outcome and future cache-translation classifier"
    - path: tests/test_blob_store_translation_seam.py
      provides: "Exhaustive taxonomy and no-cache-mutation contract"
  key_links:
    - from: src/cacheness/storage/read_contract.py
      to: src/cacheness/error_handling.py
      via: "Pure classification over public typed BlobStore exceptions"
      pattern: "classify_cache_read_failure"
    - from: src/cacheness/storage/__init__.py
      to: src/cacheness/error_handling.py
      via: "Supported direct storage exceptions are exported"
      pattern: "Blob.*Error"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Publish a stable direct-storage failure taxonomy and the narrow pure seam that later cache policy can consume.

Purpose: Keep absence, integrity, conflict, version, migration, and backend outcomes observable without changing UnifiedCache behavior in Phase 2.
Output: Public errors/reasons, classifier categories, exports, and exhaustive unit tests.
</objective>

## Artifacts this phase produces

- BlobStore-specific public exceptions retaining compatible cache base classes.
- Stable machine-readable reasons for every Phase 2 direct outcome.
- A side-effect-free classification seam for Phase 6.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-PATTERNS.md
@.planning/phases/01-compatibility-and-security-baseline/01-01-SUMMARY.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-01-SUMMARY.md
</context>

<planner_assumptions>
- STOR-08, SECU-08, and MIGR-07 were unclassified by the generic shape probe; the exact public categories and pure-classifier boundary are direct requirement and D-04/D-05/D-13/D-14 acceptance.
- Compatible absence remains represented by `None` only at `BlobStore.get`; absence is not an exception category and is never eligible for integrity translation.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze public BlobStore exception types and stable reasons</name>
  <depends_on>Plan 02-01 supplies initial manifest failures and canonical parsing behavior.</depends_on>
  <reversibility rating="costly">D-05 makes exception classes and reason strings caller branching and observability contracts.</reversibility>
  <files>src/cacheness/error_handling.py, src/cacheness/storage/__init__.py, tests/test_blob_store_translation_seam.py</files>
  <read_first>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/__init__.py</file>
    <file>tests/test_error_handling.py</file>
    <file>tests/test_public_api_contract.py</file>
  </read_first>
  <behavior>
    - "Integrity/authenticity errors retain CacheIntegrityError compatibility; lifecycle/backend/migration/version errors retain the appropriate CacheStorageError or serialization/configuration base."
    - "Every failure carries a stable lower-snake-case reason in context and preserves an operational cause where translated."
    - "Supported exceptions import from the storage barrel without exposing private codec helpers."
  </behavior>
  <action>Extend `CacheReason` and the existing public hierarchy with precise BlobStore types for malformed/unauthenticated manifest, missing/tampered payload, unsupported manifest/payload version, non-committed lifecycle conflict, backend failure, and migration required. Retain compatible base catches, authoritative `context['reason']`, narrow catches, and cause chaining. Add import and inheritance tests. Exact prose can remain actionable without becoming contractual; classes and reason strings are contractual per Phase 1 D-08 and Phase 2 D-05.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_translation_seam.py -k "error or reason or export" -x &amp;&amp; uv run ruff check src/cacheness/error_handling.py src/cacheness/storage/__init__.py tests/test_blob_store_translation_seam.py</automated>
  </verify>
  <acceptance_criteria>Every required direct failure has one stable public type/reason, compatible base inheritance, cause preservation, and supported barrel export.</acceptance_criteria>
  <done>Direct callers can branch on outcomes without parsing prose or confusing failure with absence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure future cache-translation classifier</name>
  <depends_on>Task 1 freezes the exception taxonomy consumed by classification.</depends_on>
  <files>src/cacheness/storage/read_contract.py, tests/test_blob_store_translation_seam.py</files>
  <read_first>
    <file>src/cacheness/core.py</file>
    <file>src/cacheness/error_handling.py</file>
    <file>tests/test_cache_integrity.py</file>
  </read_first>
  <behavior>
    - "The classifier recognizes the intended integrity category and preserves all other categories distinctly."
    - "Absence is not classified as integrity failure."
    - "Calling classification imports or mutates no UnifiedCache object, metadata, counters, decorators, TTL, or eviction state."
  </behavior>
  <action>Create a closed classification enum/value and `classify_cache_read_failure(error)` as a pure transform over the public exception hierarchy. Cover every known subtype exhaustively and return no cache behavior; D-14 permits only the seam, while Phase 6 owns translation, counters, and policy wiring. Tests must detect any `UnifiedCache` import/construction, backend mutation, or counter change and fail if present. Keep `core.py` untouched.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_translation_seam.py tests/test_public_api_contract.py -x &amp;&amp; uv run ruff check src/cacheness/error_handling.py src/cacheness/storage/read_contract.py src/cacheness/storage/__init__.py tests/test_blob_store_translation_seam.py</automated>
  </verify>
  <acceptance_criteria>The classifier is exhaustive and pure; selected integrity failures are distinguishable; absence and every non-integrity category remain distinct; UnifiedCache behavior is unchanged.</acceptance_criteria>
  <done>The Phase 6 cache-policy seam exists without prematurely rewiring cache behavior.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V1 Encoding and Sanitization — typed boundary validation | Every internal failure is translated to one closed public type/reason rather than ambiguous text or absence. | Exhaustive subtype/reason parameterization. |
| V13 Configuration — fail-secure policy separation | The translation seam is a pure classifier and cannot activate cache policy in this phase. | Import, construction, mutation, and counter spies. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend/codec/handler failures → public caller | Internal failures must retain category and cause without collapsing into benign absence. |
| BlobStore outcomes → future cache policy | Classification must not create side effects or silently authorize policy translation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Repudiation | public error taxonomy | high | mitigate | Stable reason-coded exception categories and exhaustive tests prevent corruption/backend failures from being reported as misses. |
| T-02-11 | Tampering | cache translation seam | high | mitigate | Pure closed classifier, no `UnifiedCache` dependency, and mutation-spy tests prevent Phase 2 from changing cache policy. |
| T-02-12 | Information Disclosure | error context | low | accept | Context contains operation/key/category and exception type but no payload bytes or secret material; actionable detail is needed for local callers. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-10 | `uv run pytest -q -o log_cli=false tests/test_blob_store_translation_seam.py -k "error or reason or cause" -x` |
| T-02-11 | `uv run pytest -q -o log_cli=false tests/test_blob_store_translation_seam.py -k "classifier or pure or mutation" -x` |

Each high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run the translation seam and public API contracts plus targeted Ruff. Verify exhaustive type/reason cases, cause preservation, and zero cache/backend/counter side effects.
</verification>

<success_criteria>
- Direct failure categories and stable reasons are public and compatible.
- Absence remains separate from failure.
- The future cache classifier is pure and exhaustive.
- `UnifiedCache` source and runtime behavior are unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-04-SUMMARY.md` when done.
</output>
