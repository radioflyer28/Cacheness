---
phase: 02-canonical-storage-and-integrity-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/storage/manifest.py
  - src/cacheness/storage/manifest_repository.py
  - src/cacheness/storage/integrity.py
  - src/cacheness/storage/blob_store.py
  - src/cacheness/error_handling.py
  - src/cacheness/storage/__init__.py
  - tests/test_blob_manifest.py
  - tests/test_blob_store_read_contract.py
autonomous: true
requirements: [STOR-01, STOR-02, STOR-08, SECU-03, SECU-04, SECU-05, MIGR-02, MIGR-07]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A direct BlobStore caller can put and get one ordinary object through a signed, committed, canonical manifest path end to end (D-01, D-02, D-06, D-07)."
    - "Logically identical manifest field sets produce byte-identical canonical JSON independent of insertion order, with deterministic field ordering (STOR-01 adjacency and ordering probes; D-02, D-09)."
    - "Only a genuinely absent repository record maps to None; empty, null, malformed, unsupported, or unauthenticated records fail with typed reasons before payload access (STOR-01 empty probe; D-04, D-05)."
    - "Manifest schema and handler-owned payload format identifiers are independent, explicit fields (MIGR-02 planner assumption, unclassified by the generic edge probe; D-01, D-03)."
  artifacts:
    - path: src/cacheness/storage/manifest.py
      provides: "Frozen BlobManifestV1 model, restricted canonical codec, and exact version dispatch"
    - path: src/cacheness/storage/manifest_repository.py
      provides: "Raw canonical-record repository seam and the first local adapter"
    - path: src/cacheness/storage/integrity.py
      provides: "Canonical HMAC-SHA256 and streaming SHA-256/size primitives"
    - path: src/cacheness/storage/blob_store.py
      provides: "Production direct put/get tracer over canonical records"
    - path: tests/test_blob_manifest.py
      provides: "Golden encoding, parser-bound, version, and signed-projection contract"
    - path: tests/test_blob_store_read_contract.py
      provides: "End-to-end committed direct-read tracer"
  key_links:
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/manifest_repository.py
      via: "BlobStore persists and fetches exact canonical manifest bytes"
      pattern: "(put_raw|get_raw)"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/integrity.py
      via: "Manifest authentication and payload digest/size verification"
      pattern: "(sign|verify|sha256|digest)"
    - from: src/cacheness/storage/blob_store.py
      to: src/cacheness/storage/guarded_handler_io.py
      via: "One live private snapshot is verified and passed to the handler"
      pattern: "open_snapshot"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Prove the canonical storage architecture with one production direct-BlobStore path, then freeze the manifest wire and failure boundary that every expansion plan consumes.

Purpose: Retire backend-shaped dictionaries as the source of truth before broadening backend and operation coverage.
Output: A signed canonical record for one real write/read path plus exhaustive model/codec contract tests.
</objective>

## Artifacts this phase produces

- A typed, backend-neutral `BlobManifestV1` with independently versioned schema and native payload identity.
- Deterministic bounded canonical JSON and complete HMAC-SHA256 projection.
- The first raw-record repository seam and a direct `BlobStore` committed-read tracer.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-PATTERNS.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-03-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-15-SUMMARY.md
</context>

<planner_assumptions>
- STOR-01 edge fallback is explicit: identical field sets encode identically across insertion order and backend, missing repository entry alone is absence, and canonical field ordering is deterministic.
- STOR-02, STOR-08, SECU-03, SECU-04, SECU-05, MIGR-02, and MIGR-07 were unclassified by the generic shape probe; their requirement text and D-01 through D-10 define acceptance without reducing scope.
- The three must_haves.prohibitions are descriptor-less recall and remain flagged-unverified until executable tests prove them.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Store and retrieve one authenticated committed object end to end</name>
  <depends_on>None — this is the phase tracer.</depends_on>
  <reversibility rating="one-way">D-01 and D-09 freeze persisted schema-1 fields and canonical signed bytes; changing them later requires a compatibility reader and migration.</reversibility>
  <files>src/cacheness/storage/manifest.py, src/cacheness/storage/manifest_repository.py, src/cacheness/storage/integrity.py, src/cacheness/storage/blob_store.py, tests/test_blob_store_read_contract.py</files>
  <read_first>
    <file>src/cacheness/storage/blob_store.py</file>
    <file>src/cacheness/storage/guarded_handler_io.py</file>
    <file>src/cacheness/interfaces.py</file>
    <file>.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "A JSON-backed BlobStore put of one ObjectHandler-compatible value publishes native handler bytes, computes SHA-256 and exact size, writes one signed committed manifest, and returns the logical key."
    - "A subsequent get fetches the exact record, authenticates it, validates committed state and the lookup key, takes one guarded snapshot, verifies digest and size, and deserializes from that same snapshot."
    - "A missing raw record returns None without handler lookup or snapshot access."
  </behavior>
  <action>Begin with the failing end-to-end cases in `tests/test_blob_store_read_contract.py`, instrumenting repository, snapshot, digest, and handler events. Add a frozen manifest model carrying every D-01 field, a raw `get_raw`/`put_raw`/`remove`/`list_keys` repository protocol with an exact JSON-backed adapter, and stdlib HMAC-SHA256 plus streaming SHA-256 helpers. Wire only the ordinary object happy path through every real layer, retaining Phase 1 candidate publication and guarded one-snapshot I/O. Use explicit manifest schema number 1 and payload format version 1 as independent fields; persist one committed state without adding Phase 3 transitions or CAS. Native ObjectHandler output remains byte-for-byte handler-owned per D-03. Do not touch `UnifiedCache`, S3, PostgreSQL, generalized recovery, or migration execution.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -k "tracer or absent" -x &amp;&amp; uv run ruff check src/cacheness/storage/manifest.py src/cacheness/storage/manifest_repository.py src/cacheness/storage/integrity.py src/cacheness/storage/blob_store.py tests/test_blob_store_read_contract.py</automated>
  </verify>
  <acceptance_criteria>The tracer test proves a real put/get through handler serialization, exact raw manifest persistence, HMAC authentication, committed-state validation, one snapshot, SHA-256/size verification, and handler deserialization; only an absent record returns None.</acceptance_criteria>
  <done>One direct BlobStore happy path is production-quality end to end and becomes the skeleton expanded by Plans 02-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze canonical bytes, bounds, versions, and typed early failures</name>
  <depends_on>Task 1 tracer establishes the manifest and raw repository seams.</depends_on>
  <reversibility rating="one-way">D-01, D-02, and D-09 make field names, restricted values, ordering, and signed projection a persisted compatibility contract.</reversibility>
  <files>src/cacheness/storage/manifest.py, src/cacheness/error_handling.py, src/cacheness/storage/__init__.py, tests/test_blob_manifest.py</files>
  <read_first>
    <file>src/cacheness/error_handling.py</file>
    <file>src/cacheness/storage/__init__.py</file>
    <file>tests/test_interfaces.py</file>
    <file>tests/test_config_validation.py</file>
  </read_first>
  <behavior>
    - "Equivalent manifests with reordered input maps, Unicode, and empty optional metadata produce the same golden UTF-8 bytes."
    - "Boundary values pass and the next value fails for 1,048,576 raw bytes, depth 16, 4,096 items per collection, 16,384 total nodes, 262,144 UTF-8 bytes per string, signed-64 integers, and non-negative signed-64 byte_size."
    - "Empty/null bytes, invalid UTF-8, duplicates, excessive bytes/depth/count/string length, floats/non-finite values, boolean-integer confusion, invalid sizes/digests/signatures, and conflicting representations fail typed before payload access."
    - "Unknown manifest or payload format versions fail as unsupported and do not trigger lookup, snapshot, handler, rewrite, or delete behavior."
    - "Mutating any schema, key, generation, state, locator, handler/type, payload format/version, digest algorithm/value, size, or creation field breaks the signature."
  </behavior>
  <action>Write the complete Wave 0 manifest suite before broadening the implementation. Freeze `MAX_MANIFEST_BYTES = 1_048_576`, `MAX_NESTING_DEPTH = 16`, `MAX_COLLECTION_ITEMS = 4_096` per map/list, `MAX_TOTAL_NODES = 16_384`, `MAX_STRING_UTF8_BYTES = 262_144`, integer range `[-(2**63), 2**63 - 1]`, and `byte_size` range `[0, 2**63 - 1]`; test each exact boundary as accepted and the next value as rejected, independently of payload size. Restrict canonical values to bounded JSON-compatible primitives and string-keyed maps; reject duplicate keys and unknown/conflicting representations instead of merging them. Encode with stdlib JSON using deterministic key order, compact separators, Unicode UTF-8, and non-finite-number rejection so backend choice cannot change signed bytes per D-02 and the STOR-01 probes. Add public reason-coded manifest integrity and unsupported-version errors beneath compatible existing bases, preserving causes. Export only supported public model/error symbols. Dispatch exact schema and native payload versions; never infer an omitted version, use unauthenticated locator/state/handler fields for I/O, or mutate evidence on failure per D-04, D-05, and D-07.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_manifest.py tests/test_blob_store_read_contract.py -x &amp;&amp; uv run ruff check src/cacheness/storage/manifest.py src/cacheness/storage/manifest_repository.py src/cacheness/storage/integrity.py src/cacheness/error_handling.py tests/test_blob_manifest.py tests/test_blob_store_read_contract.py</automated>
  </verify>
  <acceptance_criteria>Golden bytes and the exact 1,048,576-byte/16-depth/4,096-per-collection/16,384-node/262,144-string-byte/signed-64 limits are frozen with boundary-plus-one tests; every signed critical field is covered; unknown versions and malformed records fail typed without any payload or handler event; public imports succeed.</acceptance_criteria>
  <done>The canonical schema-1 model and authenticated byte contract are deterministic, bounded, independently versioned, and safe to consume in later plans.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V1 Encoding and Sanitization — structured input validation | Restricted canonical JSON, duplicate rejection, and exact parser bounds before model construction. | `tests/test_blob_manifest.py` boundary/malformed matrix. |
| V5 File Handling — safe file access | Authenticated contained locator and one guarded private snapshot before handler access. | `tests/test_blob_store_read_contract.py` event-order tracer. |
| V11 Cryptography — approved integrity/authenticity primitives | SHA-256 plus HMAC-SHA256 with a complete projection and constant-time comparison. | Manifest mutation and read-order tests. |
| V13 Configuration — fail-secure security configuration | Required signer/version/algorithm inputs fail typed before payload work. | Unsupported and absent configuration cases in both Wave 0 files. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository bytes → manifest codec | Untrusted persisted bytes enter bounded parsing and version dispatch. |
| authenticated manifest → payload I/O | Locator, handler, lifecycle, digest, and size claims may influence filesystem and deserialization only after authentication. |
| private snapshot → handler | Trusted-application payload bytes become executable serializer input only after digest and size verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | canonical manifest and signer | high | mitigate | Fixed complete signed projection, deterministic bytes, constant-time HMAC verification, and per-field mutation tests block execution. |
| T-02-02 | Tampering / Elevation of Privilege | BlobStore read ordering | high | mitigate | Authenticate before critical-field use, then verify one private snapshot before handler invocation; event-order tests block the plan. |
| T-02-03 | Denial of Service | manifest decoder | medium | mitigate | Enforce independent raw-byte, depth, collection, string, and integer bounds before typed model construction. |
| T-02-04 | Repudiation | direct-read failure evidence | medium | mitigate | Typed reason codes preserve failure category and every read failure remains non-mutating. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-01 | `uv run pytest -q -o log_cli=false tests/test_blob_manifest.py -x` |
| T-02-02 | `uv run pytest -q -o log_cli=false tests/test_blob_store_read_contract.py -x` |

Each high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run `uv run pytest -q -o log_cli=false tests/test_blob_manifest.py tests/test_blob_store_read_contract.py -x` and targeted Ruff. The event log must show authenticate before snapshot, exactly one snapshot, digest/size before handler, and no events after an earlier failure.
</verification>

<success_criteria>
- A real direct BlobStore put/get uses the canonical committed record path.
- Canonical bytes are golden, deterministic, bounded, and fully signed.
- Only repository absence returns None; malformed and unsupported records are typed failures.
- No native payload bytes gain a Cacheness wrapper or header.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-01-SUMMARY.md` when done.
</output>
