---
phase: 02-canonical-storage-and-integrity-contract
plan: "03"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/cacheness/interfaces.py
  - src/cacheness/handlers.py
  - tests/test_blob_manifest.py
autonomous: true
requirements: [STOR-01, SECU-03, MIGR-02, MIGR-07]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every built-in handler declares an exact handler-owned payload format and Cacheness contract version without opening payload bytes (MIGR-02 planner assumption, unclassified by the generic edge probe; D-03)."
    - "Unknown payload versions fail typed before snapshot or deserialization (MIGR-07 planner assumption, unclassified by the generic edge probe; D-04)."
    - "Representative NPZ, Parquet, pickle/dill, and Blosc2 payloads retain native file signatures and can be consumed through their native handler/library paths."
  artifacts:
    - path: src/cacheness/interfaces.py
      provides: "Explicit payload format/version write-result and support-check contracts"
    - path: src/cacheness/handlers.py
      provides: "Stable built-in native payload identities and non-deserializing support checks"
    - path: tests/test_blob_manifest.py
      provides: "Independent format-version and native-container coverage"
  key_links:
    - from: src/cacheness/handlers.py
      to: src/cacheness/interfaces.py
      via: "Built-in handlers implement exact format identity and support contracts"
      pattern: "payload_format(_version)?"
  prohibitions:
    - "Cacheness must not introduce or retain a custom wrapper/header around native handler payload formats."
    - "A read path must not mutate, rewrite, quarantine, or delete legacy, corrupt, or unknown-future entries as a side effect."
    - "Required signing must not silently downgrade, invent an ephemeral replacement key, or accept unsigned manifests."
---

<objective>
Make native handler format identity explicit and independently versioned before the read pipeline trusts handler selection.

Purpose: Satisfy schema/payload version independence without inventing another payload format.
Output: Handler format/version contracts and representative native-container proof.
</objective>

## Artifacts this phase produces

- Stable `(handler_type, payload_format, payload_format_version)` identities.
- Non-deserializing support checks for all enabled built-in handlers.
- Executable proof that handler-native payload bytes remain unchanged in kind.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-CONTEXT.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-RESEARCH.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-PATTERNS.md
@.planning/phases/01-compatibility-and-security-baseline/01-04-SUMMARY.md
@.planning/phases/02-canonical-storage-and-integrity-contract/02-01-SUMMARY.md
</context>

<planner_assumptions>
- MIGR-02 and MIGR-07 were unclassified by the generic shape probe; exact identifiers and reject-before-open behavior are direct acceptance from the requirements and D-03/D-04.
- Format versions describe the Cacheness serialization contract, not installed dependency versions; support tables decide compatibility explicitly.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit native payload identity to handler contracts</name>
  <depends_on>Plan 02-01 defines the manifest fields that consume handler identity.</depends_on>
  <reversibility rating="costly">The handler identity tuple becomes persisted data and a dispatch contract across later phases.</reversibility>
  <files>src/cacheness/interfaces.py, src/cacheness/handlers.py, tests/test_blob_manifest.py</files>
  <read_first>
    <file>src/cacheness/interfaces.py</file>
    <file>src/cacheness/handlers.py</file>
    <file>tests/test_handlers.py</file>
    <file>tests/test_interfaces.py</file>
  </read_first>
  <behavior>
    - "GuardedWriteResult carries payload_format and payload_format_version for every successful built-in write."
    - "A support check answers from handler identity and declared versions without opening or deserializing payload bytes."
    - "Existing put(data, Path, config) and get(Path, metadata) call shapes stay compatible."
  </behavior>
  <action>Extend `GuardedWriteResult` and the handler contract with explicit native payload identity while preserving existing method signatures and compatibility keys. Implement stable Cacheness contract versions for pandas/polars Parquet, ordinary NPZ, legacy read-only Blosc2, TensorFlow native storage, and object pickle/dill variants. Add an exact support predicate or registry resolution path that does not inspect payload bytes. Make manifest construction consume these explicit values instead of deriving a default from a filename or installed library version per D-03 and MIGR-02.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_manifest.py -k "payload_format or handler_identity or independent_version" -x &amp;&amp; uv run ruff check src/cacheness/interfaces.py src/cacheness/handlers.py tests/test_blob_manifest.py</automated>
  </verify>
  <acceptance_criteria>Every enabled built-in write returns explicit format/version identity; exact support resolution is non-deserializing; legacy public handler call signatures remain green.</acceptance_criteria>
  <done>The manifest can bind an unambiguous native handler contract without guessing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove native containers and reject unknown payload contracts</name>
  <depends_on>Task 1 exposes exact format identities and support checks.</depends_on>
  <files>src/cacheness/handlers.py, tests/test_blob_manifest.py</files>
  <read_first>
    <file>tests/test_stored_compatibility.py</file>
    <file>tests/fixtures/compat/validate_corpus.py</file>
    <file>.planning/phases/01-compatibility-and-security-baseline/01-08-SUMMARY.md</file>
  </read_first>
  <behavior>
    - "Representative NPZ, Parquet, pickle/dill, and Blosc2 paths retain their native magic/framing and round-trip behavior."
    - "Unknown format identifiers or versions produce the public unsupported-version reason before snapshot creation or handler invocation."
    - "Legacy Blosc2 remains read-only and ordinary arrays remain pickle-disabled NPZ."
  </behavior>
  <action>Add representative native-format assertions to the Wave 0 manifest suite, including spies proving unsupported identity rejection precedes payload open. Preserve Phase 1 ordinary-array and trusted-object guards, exact read-only legacy Blosc2 dispatch, and native library ownership. Ensure no handler attempts another format when a canonical manifest declares an unsupported or malformed identity per D-04; return a typed version outcome without rewrite, deletion, or fallback deserialization.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_blob_manifest.py tests/test_handlers.py tests/test_stored_compatibility.py -x &amp;&amp; uv run ruff check src/cacheness/interfaces.py src/cacheness/handlers.py tests/test_blob_manifest.py</automated>
  </verify>
  <acceptance_criteria>Native format signatures and round trips pass; unsupported versions create no payload/handler event; Phase 1 NPZ, object-array, and Blosc2 compatibility tests remain green.</acceptance_criteria>
  <done>Handler formats are explicit, versioned independently, and never wrapped or guessed.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Control Mapping

| ASVS 5.0 L1 category/control | Phase application | Automated evidence |
|---|---|---|
| V1 Encoding and Sanitization — explicit type/format validation | Exact authenticated handler/format/version tuples are validated without deserializing bytes. | Unknown identity/version event-spy cases. |
| V5 File Handling — safe handler file boundary | Native handler payloads are accessed only through guarded snapshots after support validation. | Native-format and stored-compatibility regression cases. |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated format identity → handler registry | Persisted handler/type/format/version selects executable deserialization code. |
| handler writer → native payload | The library must not add ambiguous framing around third-party/native containers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege / Tampering | handler dispatch | high | mitigate | Exact authenticated identity tuple and non-deserializing support checks reject unknown versions before payload access. |
| T-02-09 | Tampering | native payload formats | medium | mitigate | Representative magic/framing tests and Phase 1 compatibility regressions prove handlers retain format ownership. |

## Blocking High-Severity Gates

| Threat | Blocking automated gate |
|---|---|
| T-02-08 | `uv run pytest -q -o log_cli=false tests/test_blob_manifest.py -k "payload_format or handler_identity or unsupported" -x` |

The high-severity command must exit zero before the plan can complete; the corresponding task acceptance criteria are blocking.
</threat_model>

<verification>
Run manifest, handler, and stored-compatibility tests plus targeted Ruff. Unsupported versions must have zero snapshot/handler events, and native payload checks must cover NPZ, Parquet, pickle/dill, and Blosc2.
</verification>

<success_criteria>
- Every built-in handler publishes a stable native format/version identity.
- Format support is resolved before payload deserialization.
- Unknown versions fail explicitly and non-mutatingly.
- Phase 1 native/legacy format guarantees remain intact.
</success_criteria>

<output>
Create `.planning/phases/02-canonical-storage-and-integrity-contract/02-03-SUMMARY.md` when done.
</output>
