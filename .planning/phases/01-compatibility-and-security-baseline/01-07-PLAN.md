---
phase: 01-compatibility-and-security-baseline
plan: "07"
type: execute
wave: 7
depends_on:
  - 01-03
  - 01-04
  - 01-05
  - 01-06
  - 01-12
files_modified:
  - docs/SECURITY.md
  - README.md
  - tests/test_security_documentation.py
  - tests/test_phase1_quality_gates.py
  - .planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
autonomous: true
requirements:
  - SECU-07
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-14 and SECU-07, public security documentation distinguishes trusted application payloads from hostile cache metadata/files and names the explicit trusted object-array configuration required to cross that boundary."
    - "Documentation states that pickle and dill can execute code, that integrity/HMAC proves authenticity rather than serializer safety, that ordinary NumPy loading disables pickle, and that object-array opt-in requires signing, integrity verification, and unsigned-entry rejection."
    - "Documentation accurately separates native handler/container ownership per D-16 from the read-only legacy Cacheness raw-array header and does not promise a Phase 2 format or Phase 7 migration workflow."
    - "The exact boundary/precision documentation assertions are automated; human wording review is optional and not a completion dependency."
    - "The complete focused phase suite and full pytest suite pass, while Ruff findings do not exceed the measured 123-finding `src tests` baseline and all newly created phase files are Ruff-clean."
    - "AST/source quality checks prove the legacy reader has no executable metadata evaluator, ordinary ArrayHandler code has no pickle-enabled NumPy load, query_meta has no caller-field interpolation, and SqlCache has no direct print failure path."
  prohibitions:
    - statement: "Security documentation must not claim that a valid signature, HMAC, or content digest makes hostile pickle or dill deserialization safe."
      status: resolved
      verification: judgment
  artifacts:
    - path: docs/SECURITY.md
      provides: "Canonical trusted-payload and unsafe-serializer boundary"
    - path: tests/test_security_documentation.py
      provides: "Boundary and precision documentation contract"
    - path: tests/test_phase1_quality_gates.py
      provides: "Ruff no-new-findings and unsafe-construct AST/source gates"
    - path: .planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
      provides: "Completed Nyquist validation status after every gate passes"
  key_links:
    - from: tests/test_security_documentation.py
      to: docs/SECURITY.md
      via: "asserts trusted payload, executable serializer risk, integrity limitation, and explicit config requirements"
      pattern: "pickle|dill|allow_trusted_object_arrays|integrity"
    - from: tests/test_phase1_quality_gates.py
      to: src/cacheness/handlers.py
      via: "AST scans the exact legacy and ordinary array handler regions"
      pattern: "ast"
    - from: tests/test_phase1_quality_gates.py
      to: src/cacheness/core.py
      via: "AST/source scan constrains query_meta construction"
      pattern: "query_meta"
---

<objective>
Publish the serializer trust boundary and seal Phase 1 with deterministic security, compatibility, test, and lint evidence.

Purpose: Ensure callers understand what Cacheness protects and make every Phase 1 regression mechanically visible before lifecycle ownership work begins.
Output: Accurate security docs, documentation contract tests, AST/Ruff phase gates, completed validation status, and a source-coverage audit.
</objective>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compatibility-and-security-baseline/01-CONTEXT.md
@.planning/phases/01-compatibility-and-security-baseline/01-RESEARCH.md
@.planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md
@.planning/phases/01-compatibility-and-security-baseline/01-03-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-04-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-05-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-06-SUMMARY.md
@.planning/phases/01-compatibility-and-security-baseline/01-12-SUMMARY.md
@docs/SECURITY.md
@README.md

<interfaces>
Document the exact public names produced by prior plans: `CacheReason`, typed boundary exceptions, `HandlerConfig.allow_trusted_object_arrays`, `SqlCacheResult`, and the strict/best-effort behavior. Do not describe unimplemented canonical manifests, lifecycle generations, migration runners, or native array formats.
</interfaces>
</context>

## Artifacts this phase produces

Complete phase artifact inventory:

- Public symbols: `CacheReason`, `CacheUnsafePathError`, `CacheQueryValidationError`, `CacheLegacyFormatError`, `HandlerConfig.allow_trusted_object_arrays`, `SqlCacheFailure`, `SqlCacheResult`, `SqlCacheFetchError`, `SqlCacheGapDetectionError`, and `SqlCacheWriteError`.
- Internal symbols: `resolve_storage_root`, `encode_physical_name`, `validate_blob_id`, `resolve_managed_locator`, `ManagedFileOps`, `GuardedHandlerIO`, `GuardedReadSnapshot`, `BlobStore._storage_id_for_key`, `BlobStore._resolve_entry_locator`, `_parse_legacy_array_shape`, exact legacy metadata/signature/decorator adapters, `validate_query_fields`, and `to_sqlite_json_path`.
- New source files: `src/cacheness/storage/path_security.py`; `src/cacheness/storage/guarded_handler_io.py`; `src/cacheness/query_validation.py`.
- Modified existing source files: `src/cacheness/error_handling.py`; `src/cacheness/__init__.py`; `src/cacheness/config.py`; `src/cacheness/interfaces.py`; `src/cacheness/storage/backends/blob_backends.py`; `src/cacheness/storage/blob_store.py`; `src/cacheness/core.py`; `src/cacheness/handlers.py`; `src/cacheness/metadata.py`; `src/cacheness/security.py`; `src/cacheness/decorators.py`; `src/cacheness/sql_cache.py`.
- New test files: `tests/test_public_api_contract.py`; `tests/test_stored_compatibility.py`; `tests/test_filesystem_containment.py`; `tests/test_legacy_array_security.py`; `tests/test_query_meta_security.py`; `tests/test_sql_cache_failure_contract.py`; `tests/test_security_documentation.py`; `tests/test_phase1_quality_gates.py`.
- Modified existing test files: `tests/test_config_validation.py`; `tests/test_directory_sharding.py`; `tests/test_query_meta.py`; `tests/test_sql_cache.py`.
- New fixture index/documentation/validation files: `tests/fixtures/compat/manifest.json`; `tests/fixtures/compat/README.md`; `tests/fixtures/compat/validate_corpus.py`.
- New resolved historical fixture files: `tests/fixtures/compat/array-raw-v035-compress/payload.b2nd`; `tests/fixtures/compat/array-raw-v035-compress/provenance.json`; `tests/fixtures/compat/array-raw-v037-compress2/payload.b2nd`; `tests/fixtures/compat/array-raw-v037-compress2/provenance.json`; `tests/fixtures/compat/json-split-unsigned-v037/metadata.json`; `tests/fixtures/compat/json-split-unsigned-v037/payload.npz`; `tests/fixtures/compat/json-split-unsigned-v037/provenance.json`; `tests/fixtures/compat/json-split-signed-v038/metadata.json`; `tests/fixtures/compat/json-split-signed-v038/payload.npz`; `tests/fixtures/compat/json-split-signed-v038/provenance.json`; `tests/fixtures/compat/sqlite-metadata-json-v039/metadata.sqlite3`; `tests/fixtures/compat/sqlite-metadata-json-v039/payload.npz`; `tests/fixtures/compat/sqlite-metadata-json-v039/provenance.json`; `tests/fixtures/compat/decorator-key-v0313/metadata.json`; `tests/fixtures/compat/decorator-key-v0313/payload.npz`; `tests/fixtures/compat/decorator-key-v0313/provenance.json`; `tests/fixtures/compat/json-nested-v0314/metadata.json`; `tests/fixtures/compat/json-nested-v0314/payload.npz`; `tests/fixtures/compat/json-nested-v0314/provenance.json`; `tests/fixtures/compat/sqlite-columns-v0314/metadata.sqlite3`; `tests/fixtures/compat/sqlite-columns-v0314/payload.npz`; `tests/fixtures/compat/sqlite-columns-v0314/provenance.json`.
- Modified documentation/validation files: `docs/SECURITY.md`; `README.md`; `.planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish and test the trusted-payload serializer boundary</name>
  <files>docs/SECURITY.md, README.md, tests/test_security_documentation.py</files>
  <read_first>
    - `docs/SECURITY.md`: existing protected/not-protected claims and signing guidance.
    - `README.md`: serializer, array, security, and configuration examples.
    - `src/cacheness/config.py`: exact object-array opt-in and strict predicates from Plan 04.
    - `src/cacheness/handlers.py`: ordinary NPZ, ObjectHandler, and read-only legacy paths.
    - `src/cacheness/security.py`: actual HMAC/integrity guarantees and limitations.
  </read_first>
  <behavior>
    - Docs name pickle and dill arbitrary-code risk and state application payloads must come from a trusted producer.
    - Docs state authenticity/integrity does not convert hostile executable serialization into safe data.
    - Docs state ordinary NumPy paths keep pickle disabled and list every required object-array opt-in predicate.
    - Docs distinguish legacy read-only custom headers from native write ownership without promising a new format or migration.
  </behavior>
  <action>Create `tests/test_security_documentation.py` first with semantic section assertions rather than brittle full-paragraph snapshots. Update `docs/SECURITY.md` as the canonical boundary and keep README's security/serializer section aligned. State that pickle and dill can execute code; only trusted application payloads may use ObjectHandler; HMAC/signatures/digests detect unauthorized change but do not sandbox deserialization; ordinary arrays use native NPZ with pickle disabled; object arrays require `allow_trusted_object_arrays=True`, object pickle, signing, integrity verification, and unsigned-entry rejection; malformed metadata and legacy headers are treated as untrusted and parsed fail-closed. Per D-16, identify the old raw-array header as read-only compatibility and native handlers as format owners. Per SECU-07, include risks, configuration requirements, safe defaults, and an actionable deployment checklist. Do not claim hostile pickle support, canonical manifests, migration, or an unselected native array format.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_security_documentation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests locate one canonical trusted-payload section and verify every named risk, limitation, default, and configuration predicate.
    - README points to the canonical security section and contains no contradictory serializer claim.
    - Legacy/custom versus native/read-only ownership is accurate to Plan 04 behavior.
    - Documentation contains no promise of hostile-object safety or unimplemented later-phase machinery.
  </acceptance_criteria>
  <done>Users can identify the exact trust boundary, unsafe serializer risk, safe array default, and explicit object-array requirements from tested public documentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal the phase with full regression, Ruff, and unsafe-construct gates</name>
  <files>tests/test_phase1_quality_gates.py, .planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/01-compatibility-and-security-baseline/01-VALIDATION.md`: Wave 0 files, sampling commands, and sign-off flags.
    - `pyproject.toml`: pytest and Ruff configuration.
    - `src/cacheness/handlers.py`: legacy and ordinary array regions for AST checks.
    - `src/cacheness/core.py`: query_meta region for field-construction checks.
    - `src/cacheness/sql_cache.py`: failure/fallback regions for logging checks.
  </read_first>
  <behavior>
    - Every Wave 0 file exists and the focused phase suite passes.
    - Full pytest passes with no hidden baseline exemptions.
    - Ruff JSON executes successfully, reports no more than the measured 123 findings across `src tests`, and reports zero findings in files newly created by this phase.
    - AST/source scans fail on executable legacy metadata evaluation, ordinary permissive array loading, query-field interpolation, or direct SqlCache print calls.
    - `01-VALIDATION.md` uses the final threat register: CACH-07 maps to T-01-20..23, SECU-02 to T-01-13..16, SECU-06 to T-01-17..19, SECU-07 to T-01-24, and every remaining requirement/phase-gate row is reconciled to its final plan register.
    - Validation status changes to complete only after all gates pass.
  </behavior>
  <action>Create `tests/test_phase1_quality_gates.py` as a fail-closed subprocess/AST gate. Run Ruff with JSON output, assert the process outcome is a valid Ruff result, parse every finding, cap the repository's measured `src tests` baseline at 123, and require every phase-created Python file to have zero findings. Use `ast` plus function/class region selection to assert the legacy array reader contains no dynamic expression evaluator, ordinary ArrayHandler NumPy loads cannot enable object deserialization, query_meta does not build query text from caller field variables, and SqlCache contains no direct print call in fetch/gap/upsert failure paths. Also assert every Wave 0 test file exists. Reconcile `01-VALIDATION.md` against the final plan registers rather than retaining its draft threat numbers: set CACH-07 to `T-01-20..T-01-23`, SECU-02 to `T-01-13..T-01-16`, SECU-06 to `T-01-17..T-01-19`, and SECU-07 to `T-01-24`; align MIGR-01 with `T-01-09..T-01-11` and `T-01-28..T-01-36`, SECU-01 with `T-01-01..T-01-07`, `T-01-12`, and `T-01-37`, and record phase-gate threats `T-01-25..T-01-27` separately rather than misattributing them to a requirement. Run the focused phase suite, then full pytest, then the quality gate. Only after all commands pass, set `nyquist_compliant: true`, `wave_0_complete: true`, every validation map row to green, and Approval to passed in `01-VALIDATION.md`; record actual full-suite and Ruff counts without suppressing failures.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_public_api_contract.py tests/test_stored_compatibility.py tests/test_filesystem_containment.py tests/test_legacy_array_security.py tests/test_query_meta.py tests/test_query_meta_security.py tests/test_sql_cache.py tests/test_sql_cache_failure_contract.py tests/test_security_documentation.py tests/test_phase1_quality_gates.py && uv run pytest -q -o log_cli=false</automated>
  </verify>
  <acceptance_criteria>
    - All seven Wave 0 files from 01-VALIDATION.md exist and pass their mapped focused commands.
    - Full pytest exits zero.
    - Ruff runs non-vacuously, total findings are at most 123, and new phase Python files have zero findings.
    - Each unsafe-construct AST/source sentinel fails against a synthetic violating snippet and passes against the production region, proving the gate is not vacuous.
    - Validation threat references exactly match the final requirement registers and keep T-01-25..27 identified as phase-gate threats.
    - Validation flags/rows are marked complete only after evidence exists.
  </acceptance_criteria>
  <done>The full compatibility/security suite, full pytest, no-new-Ruff discipline, and unsafe-construct scans pass, and the validation artifact records the evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Security documentation to operator configuration | Inaccurate claims can lead operators to deserialize hostile executable payloads. |
| Source changes to release gate | A broad green test suite can miss reintroduced dangerous constructs or add lint debt. |
| Validation status to later lifecycle phases | Later phases rely on Phase 1 being genuinely complete, not administratively marked complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-24 | Elevation of Privilege | serializer trust documentation | high | mitigate | Tested explicit pickle/dill execution risk, trusted-producer boundary, integrity limitation, and strict object-array checklist. |
| T-01-25 | Repudiation | validation sign-off | high | mitigate | Update status only after focused/full/quality commands pass and record actual counts. |
| T-01-26 | Tampering / Elevation of Privilege | unsafe construct regression | high | mitigate | Non-vacuous AST/source gates for legacy evaluator, ordinary object loading, query interpolation, and SqlCache print suppression. |
| T-01-27 | Denial of Service / quality degradation | Ruff baseline | medium | mitigate | Parse Ruff JSON, cap existing findings at the measured 123 baseline, and require phase-created files to be clean. |
</threat_model>

## Edge Resolution Audit

| Probe item | Resolution | Plan truth/evidence |
|------------|------------|---------------------|
| MIGR-01 empty-input behavior | resolved explicit | 01-01 characterizes empty/omitted input only for callables that accept it; required arguments are signature contracts. |
| MIGR-01 encoding/equality definition | resolved explicit | 01-08 owns an independent exact-matrix validator run after every 01-08..01-11 fixture task; it checks fixed semantic inputs, complete format discriminators, framing/decompressed bytes, pickle-disabled NPZ, safe paths, per-file digests, and source/copy invariance before 01-12 production-reader semantic equality. 01-01 preserves exact authored-path strings. |
| CACH-07 unclassified manual review | resolved explicit | 01-06 automates import, success, strict, best-effort, gap, and fallback behavior; no subjective residue remains. |
| SECU-01 unclassified manual review | resolved explicit | 01-02/01-03 automate host-independent paths, logical-key/prefix encoding, guarded handler staging/snapshots, no-mutation, static/stale/intra-operation retarget tiers, high-level Unix swap stress, Windows junction/reparse coverage, and all-or-nothing preflight for BlobStore overwrite/get/get_metadata/update_metadata/exists/delete/list/clear plus UnifiedCache get/overwrite/invalidate/list_entries/clear_all/startup expiry/size cleanup. |
| SECU-02 unclassified manual review | resolved explicit | 01-04 automates every required framing, grammar, dtype, decompression, byte, sidecar, and object-array case. |
| SECU-06 unclassified manual review | resolved explicit | 01-05 automates field grammar, pre-session ordering, path/value binding, and numeric/string/bool semantics. |
| SECU-07 boundary review | resolved explicit | 01-07 tests the trusted application payload versus hostile metadata/file boundary. |
| SECU-07 precision review | resolved explicit | 01-07 tests named serializers, safe defaults, integrity limitation, and every opt-in configuration predicate. |

Adversarial prohibition recall kept three project-specific constraints in plan `must_haves.prohibitions`: do not freeze defects/private quirks (01-01), do not merge SqlCache or label partial results complete (01-06), and do not claim integrity makes hostile executable serialization safe (01-07). Traversal, query injection, unsafe deserialization, and evidence mutation are canonical security concerns and are covered in the STRIDE registers and automated negative tests rather than duplicated as probe prohibitions.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Frozen compatibility baseline and safe boundary behavior before ownership changes | 01-01..01-12 | COVERED | Final gate requires the whole phase suite. |
| REQ | MIGR-01 | Public/stored compatibility characterization | 01-01, 01-08, 01-09, 01-10, 01-11, 01-12 | COVERED | Public/config contract, exact corpus, and production adapters. |
| REQ | CACH-07 | Independent SqlCache regression/completeness | 01-06 | COVERED | Success, strict, best effort, gap, upsert. |
| REQ | SECU-01 | Filesystem containment across operations | 01-02, 01-03 | COVERED | Low-level and persisted-locator paths. |
| REQ | SECU-02 | Typed safe parsing; no metadata evaluator | 01-04 | COVERED | Bounded legacy reader and safe array defaults. |
| REQ | SECU-06 | Validated, safely constructed query fields | 01-05 | COVERED | Whole-set validation and bound expressions. |
| REQ | SECU-07 | Trusted payload/unsafe serializer docs | 01-07 | COVERED | Tested canonical documentation. |
| RESEARCH | R-01 | Validate before touching state | 01-02, 01-03, 01-05, 01-12 | COVERED | Backend/high-level guarded I/O, query call-order, and exact legacy schema gates. |
| RESEARCH | R-02 | Declared format selects one reader | 01-04 | COVERED | No alternate sidecar fallback. |
| RESEARCH | R-03 | Exact eight-variant resolved historical fixture matrix and immutable read-only protocol | 01-08, 01-09, 01-10, 01-11, 01-04, 01-12 | COVERED | Plan 08 owns a reusable production-independent validator run after every fixture task and at full-corpus consumption; it enforces exact IDs/commits/variants, provenance schema, safe paths, fixed semantics, complete JSON/SQLite/raw discriminators, raw framing/decompressed bytes, pickle-disabled NPZ, per-file source/copied/current SHA-256, and source/copy/pre-post schema/data-version invariance before production adapters/readers. |
| RESEARCH | R-04 | Strict default, explicit partial result | 01-06 | COVERED | Distinct result/failure channel. |
| RESEARCH | R-05 | No new dependency/package install | all | COVERED | Standard library and existing locked stack only. |
| RESEARCH | R-06 | No canonical manifest, migration runner, or new raw-array container | all | COVERED | Scope fence repeated in task actions. |
| RESEARCH | R-07 | Full focused/pytest/Ruff/unsafe scans | 01-07 | COVERED | Non-vacuous quality gate. |
| RESEARCH | R-08 | Resolved cross-platform symlink/TOCTOU mechanism, five proof tiers, and trusted-host residual | 01-02, 01-03 | COVERED | Canonical root anchoring, Unix descriptor operations, Windows locked reparse checks, private handler stage/snapshot containment, and high-level deterministic/stress tests. |
| CONTEXT | D-01 | Supported public baseline definition | 01-01 | COVERED | Public matrix. |
| CONTEXT | D-02 | 0.3.14 plus safe representative 0.3.x layouts | 01-08, 01-09, 01-10, 01-11, 01-12 | COVERED | Exact immutable corpus validated independently after every fixture task, then consumed through production readers/adapters with read-only compatibility bookkeeping. |
| CONTEXT | D-03 | Adapters/deprecations; unsafe inputs typed | 01-01, 01-04, 01-12 | COVERED | Public, array, metadata, signature, and decorator adapters. |
| CONTEXT | D-04 | Optional symbols import without dependencies | 01-01 | COVERED | Blocked-import subprocesses. |
| CONTEXT | D-05 | Compatibility window through milestone | 01-01, 01-04, 01-12 | COVERED | One-way ratings retained; no checkpoint by autonomous override. |
| CONTEXT | D-06 | Corrected behavior, not defect freezing | 01-01, 01-02, 01-08, 01-09, 01-10, 01-11, 01-12 | COVERED | Broken star import/sanitization corrected and only exact safe historical layouts/readers promised. |
| CONTEXT | D-07 | Preserve relative config paths | 01-01 | COVERED | JSON/YAML exact strings. |
| CONTEXT | D-08 | Exception classes/reason codes contractual | 01-01 | COVERED | CacheReason and subclasses. |
| CONTEXT | D-09 | Unsafe path typed for every operation | 01-02, 01-03 | COVERED | Backend plus guarded handler I/O and explicit BlobStore/UnifiedCache locator exposure, metadata mutation, expiry, and size-cleanup paths. |
| CONTEXT | D-10 | Outside-root metadata untouched | 01-03 | COVERED | Complete affected-set preflight and snapshots prove zero metadata/counter/safe-sibling/attempted-overwrite/outside-byte mutation. |
| CONTEXT | D-11 | Root symlink allowed; managed symlinks rejected | 01-02, 01-03 | COVERED | No-follow and retarget tests. |
| CONTEXT | D-12 | Logical keys/prefixes encoded before filesystem persistence; backend IDs remain restricted | 01-02, 01-03 | COVERED | Length-framed domain-separated hashes, exact public logical values, hostile-prefix API tests, and strict opaque grammar. |
| CONTEXT | D-13 | Safe read-only legacy Blosc2 header | 01-04 | COVERED | Bounded parser. |
| CONTEXT | D-14 | Ordinary no-pickle; explicit trusted object path | 01-04, 01-07 | COVERED | Registry routing and docs. |
| CONTEXT | D-15 | Malformed declared artifact fails typed | 01-04 | COVERED | No sidecar guess. |
| CONTEXT | D-16 | Native handler/container ownership | 01-04, 01-07 | COVERED | NPZ writes; legacy only read. |
| CONTEXT | D-17 | Header/byte checks without payload ceiling | 01-04 | COVERED | Metadata-only bounds. |
| CONTEXT | D-18 | Numeric >=, string exact, nested query semantics | 01-05 | COVERED | Adds explicit bool precision. |
| CONTEXT | D-19 | Whole invalid query rejected pre-DB | 01-05 | COVERED | Call-order spies. |
| CONTEXT | D-20 | SqlCache strict default; explicit partial | 01-06 | COVERED | Staged transaction/result type. |
| CONTEXT | D-21 | Equivalent internal fallback observable | 01-06 | COVERED | Structured logs and typed double failure. |

<verification>
Run the exact combined focused/full command from Task 2. `tests/test_phase1_quality_gates.py` must independently run and parse Ruff, prove its four unsafe-construct sentinels fail-first against violating snippets, and verify every Wave 0 file. The deterministic external-API detector returned `detected: false`, so no `COVERAGE.md` is created. The assumption-delta detector returned `detected: false`, so no identity-model decision is added. This Python phase has no listed JavaScript ORM schema surface, so no schema-push task exists.
</verification>

<success_criteria>
- All six phase requirement IDs and D-01 through D-21 have executable coverage shown in the source audit.
- All eight spec-less edge rows are resolved into explicit truths/tests with no silent drop.
- The three non-canonical project-value prohibitions are recorded descriptor-less; canonical security concerns remain in STRIDE/negative tests.
- Focused phase tests, full pytest, Ruff no-new-findings discipline, and all four unsafe-construct scans pass.
- `01-VALIDATION.md` is marked complete only after the evidence exists.
</success_criteria>

<output>
Create `.planning/phases/01-compatibility-and-security-baseline/01-07-SUMMARY.md` when done.
</output>
