---
phase: 06-unifiedcache-policy-composition
plan: "05"
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - src/cacheness/config.py
  - src/cacheness/core.py
  - tests/contracts/test_phase6_topology_policy.py
autonomous: true
requirements: [CACH-01, CACH-02, CACH-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02: UnifiedCache accepts exactly one selected BlobStore composition, directly injected or created from one validated StoreTopology."
    - "The locked ownership resolution is structural: an injected BlobStore is caller-owned; a topology-created BlobStore is cache-owned, with no ambiguous ownership boolean."
    - "D-17: Explicit initialize and close semantics preserve topology-specific guarantees and retain committed-partial evidence without a cache-side participant coordinator."
    - "CACH-01: Memory/memory and SQLite/filesystem run the same cache contract; PostgreSQL/S3 stays a deterministic candidate profile in this phase."
  artifacts:
    - path: src/cacheness/core.py
      provides: "Explicit BlobStore composition, ownership, initialization, and thin close delegation"
    - path: src/cacheness/config.py
      provides: "Validated canonical StoreTopology selection without parallel backend knobs"
    - path: tests/contracts/test_phase6_topology_policy.py
      provides: "Shared supported-profile contract and ownership/partial-close proofs"
  key_links:
    - from: src/cacheness/core.py
      to: src/cacheness/storage/blob_store.py
      via: "the single selected store instance for every read/write/remove/policy path"
      pattern: "BlobStore|StoreTopology"
    - from: src/cacheness/core.py
      to: src/cacheness/storage/composition.py
      via: "topology preflight and composition factory only when cache owns creation"
      pattern: "preflight|create"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] UnifiedCache must not construct parallel metadata/blob backends, duplicate AuthorityLifecycleEngine, or add a second coordinator/lock layer."
    - "[FLAGGED/UNVERIFIED] Ownership must not be inferred through a compatibility boolean, and remote-candidate mocks must not be presented as BACK-05 live evidence."
---

<objective>
Qualify the canonical cache policy against supported BlobStore compositions with explicit ownership and lifecycle.

Purpose: CACH-01 and CACH-02 require one policy implementation over all supported topologies, while CACH-06 requires predictable construction and cleanup.
Output: One composition constructor, supported-profile contract tests, and explicit cache-owned versus caller-owned lifecycle behavior.
</objective>

## Artifacts this phase produces

- `src/cacheness/core.py`: one-store composition and lifecycle ownership.
- `src/cacheness/config.py`: canonical topology selection/preflight inputs.
- `tests/contracts/test_phase6_topology_policy.py`: shared local contracts and deterministic remote-candidate evidence.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@docs/STORAGE_INITIALIZATION.md
@docs/CATALOG_AND_TOPOLOGY.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-CONTEXT.md
@.planning/phases/05-payload-backends-and-supported-topology-qualification/05-VERIFICATION.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-RESEARCH.md
@src/cacheness/config.py
@src/cacheness/core.py
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/composition.py
@.planning/phases/06-unifiedcache-policy-composition/06-04-SUMMARY.md
</context>

<interfaces>
The canonical constructor is one `UnifiedCache(config: CacheConfig, *, store: BlobStore | StoreTopology)` path. Passing `BlobStore` preserves caller ownership and identity; passing `StoreTopology` creates one cache-owned BlobStore. `UnifiedCache.initialize()` delegates deliberate initialization for the owned form, and `UnifiedCache.close()` delegates only to the owned BlobStore while closing facade-local observers for both forms.
</interfaces>

<source_coverage>
- CACH-01: memory/memory, SQLite/filesystem, and deterministic PostgreSQL/S3 candidate run one parameterized cache contract.
- CACH-02: all cache behavior passes through exactly one selected BlobStore and its AuthorityLifecycleEngine.
- CACH-06: construction rejects missing/ambiguous/unsupported profiles and lifecycle is explicit.
- CONTEXT: D-02, D-03, D-14, and D-17 are directly covered; D-14 governs explicit decorator ownership while the cache-created/injected-store ownership resolution is enforced structurally.
- RESEARCH: topology-safe routing, preflight-before-write, resource ownership, construction unwind, and partial close are covered.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one cache lifecycle through each declared topology profile</name>
  <files>src/cacheness/config.py, src/cacheness/core.py, tests/contracts/test_phase6_topology_policy.py</files>
  <read_first>
    - src/cacheness/core.py
    - src/cacheness/config.py
    - src/cacheness/storage/blob_store.py
    - src/cacheness/storage/composition.py
    - tests/test_blob_store_composition.py
    - tests/contracts/test_topology_lifecycle.py
    - tests/test_unified_cache_lifecycle_authority.py
    - docs/adr/0001-topology-specific-storage-guarantees.md
  </read_first>
  <behavior>
    - "Memory/memory and SQLite/filesystem -> initialize, put, lookup, invalidate, and close through the same UnifiedCache methods/results."
    - "Deterministic PostgreSQL/S3 candidate -> same policy call graph and typed outcomes, without claiming live qualification."
    - "Missing topology/store, unsupported adjacency, or mismatched profile roles -> reject during preflight before payload/catalog/projection writes."
    - "Profile enumeration -> stable StoreTopology authority-role order, never backend registration/import order."
  </behavior>
  <action>Parameterize one end-to-end cache lifecycle over the two Phase-5 qualified local profiles and the deterministic remote candidate. Refactor UnifiedCache construction so its only storage identity is either a directly injected BlobStore or one BlobStore created from a validated StoreTopology per D-02; remove private metadata/blob backend construction and do not instantiate an additional lifecycle engine. Keep CachePolicyConfig separate from topology, handler, and security configuration per D-03. Require explicit initialize before operations and run topology preflight before any resource mutation. Resolve CACH-01 fallback probes as tests: empty means missing composition is rejected pre-I/O; adjacency means payload/catalog/projection role mismatch is rejected; ordering means stable declared profile/role order independent of registry or optional-import order. Mark PostgreSQL/S3 evidence as deterministic candidate only; BACK-05 live qualification remains Phase 8.</action>
  <reversibility rating="costly">The constructor collapses overlapping storage routes onto the Phase-5 topology contract; reversal would restore unsafe dual lifecycle ownership.</reversibility>
  <acceptance_criteria>
    - "The same parameterized initialize/put/lookup/invalidate/close assertions pass for memory/memory and SQLite/filesystem."
    - "The deterministic PostgreSQL/S3 candidate follows the same policy methods and is labeled candidate rather than live-qualified."
    - "Missing, unsupported, and role-mismatched compositions make zero resource writes and return typed preflight failures."
    - "One UnifiedCache instance has one BlobStore and the same AuthorityLifecycleEngine identity observed by its mutation path."
    - "No BACK-05 or live remote acceptance is recorded by these tests."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/contracts/test_phase6_topology_policy.py -k "profile or preflight or identity or candidate" -o log_cli=false</automated>
  </verify>
  <done>All declared profiles exercise one policy/lifecycle path, with unsupported topology stopped before mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make store ownership and close outcomes explicit</name>
  <files>src/cacheness/core.py, tests/contracts/test_phase6_topology_policy.py</files>
  <read_first>
    - src/cacheness/core.py
    - src/cacheness/storage/blob_store.py
    - src/cacheness/storage/composition.py
    - src/cacheness/error_handling.py
    - tests/test_blob_store_composition.py
    - tests/test_blob_store_close_contract.py
    - tests/test_cached_custom_metadata.py
  </read_first>
  <behavior>
    - "Injected initialized BlobStore -> caller-owned; UnifiedCache.close does not close it."
    - "StoreTopology-created BlobStore -> cache-owned; UnifiedCache initializes and closes it exactly once."
    - "Owned construction fails midway -> only resources successfully created by this attempt unwind in reverse order."
    - "Owned BlobStore close returns or raises its declared typed outcome; UnifiedCache does not close topology participants individually or reinterpret storage truth."
    - "A requested projection/statistics failure after a canonical commit, including after engine close, retains the original BlobReceipt in CacheBlobCommittedPartialError evidence."
  </behavior>
  <action>Encode ownership by constructor form, not a boolean: a directly injected BlobStore is caller-owned, while a BlobStore produced from StoreTopology is cache-owned under the locked ownership resolution. For injection, validate compatibility but never close the store from UnifiedCache. For owned construction, initialize explicitly, unwind only the cache-created BlobStore on failure, and delegate close exactly once to BlobStore; UnifiedCache must not enumerate or close topology participants itself. Preserve BlobStore's typed close/failure semantics rather than inventing cache-side aggregate lifecycle truth. If explicitly requested external metadata or derived statistics fails after a canonical commit or after engine close, preserve the original receipt and existing CacheBlobCommittedPartialError evidence per D-17. Reject policy operations after facade close with the existing typed cache/storage lifecycle error. Do not add context-manager ownership magic, a lifecycle lock, readiness registry, or participant coordinator.</action>
  <acceptance_criteria>
    - "Closing a cache with an injected store invokes that store's close zero times and leaves it usable by its caller."
    - "Closing a cache-created store invokes BlobStore.close exactly once and never invokes participant close methods directly from UnifiedCache."
    - "A construction failure unwinds only the cache-created BlobStore and never touches injected resources."
    - "Typed BlobStore close failures propagate unchanged; no cache-side report claims participant-level or cross-resource atomic close."
    - "Requested derived-state failure after commit/engine close preserves the original receipt and typed committed-partial evidence."
    - "No public or private ownership boolean exists; ownership follows the constructor form."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/contracts/test_phase6_topology_policy.py tests/test_blob_store_close_contract.py -o log_cli=false</automated>
  </verify>
  <done>Cache-created and injected stores have unambiguous ownership, explicit initialization, thin BlobStore close delegation, and preserved committed-partial semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Constructor input -> topology factory | Invalid or mismatched roles must stop before any backend is mutated. |
| UnifiedCache -> injected store | Cache policy may use but must not assume lifecycle ownership of caller resources. |
| Owned cache -> BlobStore close | The facade delegates once and must not become a participant-level close coordinator. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-17 | Tampering | topology composition | high | mitigate | Validate declared profile/roles and preflight all participants before first mutation. |
| T-06-18 | Elevation of privilege | caller-owned store | high | mitigate | Encode ownership by constructor form and prove cache close never closes injected stores. |
| T-06-19 | Denial of service | close delegation | medium | mitigate | Delegate one close to BlobStore, preserve its typed failure, and forbid participant iteration in UnifiedCache. |
| T-06-20 | Repudiation | remote qualification | medium | mitigate | Label remote tests mocked-candidate and exclude BACK-05 live evidence from phase success. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run the shared topology contract, initialization, and composition-factory suites. Confirm no code path constructs a second metadata/payload pair or lifecycle engine.
</verification>

<success_criteria>
- One selected BlobStore is the cache's primary storage identity.
- Supported local profiles run identical policy behavior; remote remains candidate evidence.
- Ownership follows constructor form with no compatibility switch.
- Initialization, unwind, and close preserve topology-specific guarantees.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-05-SUMMARY.md` when done.
</output>
