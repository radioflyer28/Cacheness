---
phase: 06-unifiedcache-policy-composition
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/cacheness/core.py
  - src/cacheness/decorators.py
  - tests/test_phase6_decorator_contract.py
autonomous: true
requirements: [CACH-02, CACH-03, CACH-04, CACH-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-14: Function decorators receive an explicit UnifiedCache; they do not create or discover hidden global cache ownership."
    - "D-15: Function identity and argument normalization are policy owned by UnifiedCache and persist a queryable function namespace in canonical metadata."
    - "D-15: Decorators recompute by default only for ABSENT and EXPIRED; CORRUPT, CONFLICT, and BACKEND_ERROR require explicit policy."
    - "D-15/D-16: None and arbitrary Python results are valid stored hits, and function invalidation returns the real CacheRemovalReport."
  artifacts:
    - path: src/cacheness/decorators.py
      provides: "Thin explicit-cache decorator driven by CacheLookupResult"
    - path: src/cacheness/core.py
      provides: "Canonical function key/namespace policy and function-scoped invalidation"
    - path: tests/test_phase6_decorator_contract.py
      provides: "None, exception-outcome, key normalization, isolation, and cache_clear result tests"
  key_links:
    - from: src/cacheness/decorators.py
      to: src/cacheness/core.py
      via: "lookup_call/put_call/invalidate_function policy methods"
      pattern: "lookup_call|put_call|invalidate_function"
    - from: src/cacheness/core.py
      to: src/cacheness/storage/catalog.py
      via: "function namespace persisted as an authoritative queryable field"
      pattern: "function_namespace"
  prohibitions:
    - "[FLAGGED/UNVERIFIED] Decorators must not own cache lifecycle, retain a weakref/atexit registry, or use a hidden singleton."
    - "[FLAGGED/UNVERIFIED] A failure outcome must not be collapsed into a miss or silently recomputed under the default decorator policy."
---

<objective>
Make function caching a thin, explicit policy facade over the same UnifiedCache result and removal contracts.

Purpose: CACH-04 requires stored None, function isolation, real statistics, and real invalidation results without a second lifecycle path.
Output: Explicit-cache decorators, canonical function-key ownership, and explicit recompute/suppression policy.
</objective>

## Artifacts this phase produces

- `src/cacheness/core.py`: function namespace/key policy atop canonical storage.
- `src/cacheness/decorators.py`: explicit decorator surface with no independent ownership.
- `tests/test_phase6_decorator_contract.py`: behavioral proof for values, failures, isolation, and clearing.

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/06-unifiedcache-policy-composition/06-CONTEXT.md
@.planning/phases/06-unifiedcache-policy-composition/06-RESEARCH.md
@.planning/phases/06-unifiedcache-policy-composition/06-PATTERNS.md
@src/cacheness/core.py
@src/cacheness/decorators.py
@src/cacheness/serialization.py
@src/cacheness/storage/catalog.py
@.planning/phases/06-unifiedcache-policy-composition/06-03-SUMMARY.md
</context>

<interfaces>
Consume the shared `CacheLookupResult.outcome` exactly once. The sole decorator is `cached(*, cache: UnifiedCache, recompute_on: frozenset[CacheOutcome] = {ABSENT, EXPIRED})`; it preserves `functools.wraps`, delegates deterministic function keying and writes to UnifiedCache, and exposes `wrapper.cache_clear(...) -> CacheRemovalReport` for the function's authoritative namespace.
</interfaces>

<source_coverage>
- CACH-02: decorators are consumers of policy, not storage/lifecycle owners.
- CACH-03: function invalidation reuses bounded exact removal.
- CACH-04: None hits, arbitrary values, stable function identity, isolation, stats, and real clear results are covered.
- CACH-06: only the canonical explicit decorator survives the cutover.
- CONTEXT: D-06, D-07, D-10, D-11, D-14, D-15, and D-16 are implemented.
</source_coverage>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cache one None-returning function through an explicitly supplied cache</name>
  <files>src/cacheness/core.py, src/cacheness/decorators.py, tests/test_phase6_decorator_contract.py</files>
  <read_first>
    - src/cacheness/decorators.py
    - src/cacheness/core.py
    - src/cacheness/serialization.py
    - src/cacheness/storage/catalog.py
    - tests/test_decorators.py
    - tests/test_cache_key_consistency.py
  </read_first>
  <behavior>
    - "First call returning None -> function executes once and None is committed through cache.put_call."
    - "Second identical call -> lookup outcome HIT with value=None; function does not execute."
    - "Same arguments on two different functions -> distinct keys/namespaces and independent values/statistics."
    - "Equivalent positional/keyword/default argument forms -> one stable key for the same function call."
  </behavior>
  <action>Write the end-to-end tests first. Move function qualification, signature binding/default application, argument normalization, and function namespace construction into focused UnifiedCache policy methods per D-15; persist the stable function namespace as an authenticated queryable catalog field rather than reverse-parsing an opaque key. Keep the existing deterministic serializer/hash primitive where valid. Replace decorator-side keying and cache construction with one canonical cached decorator that requires `cache=UnifiedCache` explicitly per D-14. Drive hit detection only from CacheLookupResult.outcome, so a stored None or other arbitrary value is returned without recomputation per D-15. The decorator must not register atexit cleanup, own weak references, or close the supplied cache.</action>
  <acceptance_criteria>
    - "Two identical calls to a None-returning function invoke user code exactly once and record one miss plus one hit."
    - "Different qualified functions with identical arguments produce different canonical function namespaces and never cross-hit."
    - "Equivalent Python call forms bind to the same key; a changed bound argument produces a different key."
    - "Constructing/collecting a decorated function creates no cache, atexit callback, weakref registry entry, or close call."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_decorator_contract.py -k "none or explicit or identity or normalization or isolation" -o log_cli=false</automated>
  </verify>
  <done>One explicit decorator path correctly caches None and delegates identity, lookup, put, and ownership to UnifiedCache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce outcome-aware recomputation and truthful function clearing</name>
  <files>src/cacheness/core.py, src/cacheness/decorators.py, tests/test_phase6_decorator_contract.py</files>
  <read_first>
    - src/cacheness/decorators.py
    - src/cacheness/core.py
    - src/cacheness/cache_policy.py
    - tests/test_phase6_decorator_contract.py
    - tests/test_phase6_removal_contract.py
  </read_first>
  <behavior>
    - "ABSENT or EXPIRED -> default decorator recomputes once, writes result, and retains the original lookup outcome in call diagnostics."
    - "CORRUPT, CONFLICT, or BACKEND_ERROR -> default decorator raises/preserves typed cause and does not call the function."
    - "Explicit recompute_on/suppression policy names a failure outcome -> function may run, but actual lookup outcome/cause remains inspectable."
    - "cache_clear -> function-scoped exact bounded invalidation and the actual CacheRemovalReport."
  </behavior>
  <action>Add a validated immutable decorator failure policy. Default recompute eligibility is exactly ABSENT and EXPIRED per D-15. CORRUPT, CONFLICT, and BACKEND_ERROR must preserve and expose their typed cause unless the caller explicitly opts that outcome into recomputation/suppression; even then, retain the original CacheLookupResult in an inspectable per-call result/diagnostic rather than relabeling it as a miss. Remove broad `ignore_errors` semantics. Implement cache_clear by calling UnifiedCache.invalidate_function for the persisted namespace and return CacheRemovalReport per D-16; reuse Plan-02 bounded exact removal, including incomplete continuation and conflict accounting. Prove concurrent/repeated function invocations cannot corrupt canonical storage; rely on BlobStore guarantees rather than adding a decorator lock layer.</action>
  <acceptance_criteria>
    - "Default policy recomputes for exactly ABSENT and EXPIRED in parameterized tests."
    - "Default policy invokes user code zero times for CORRUPT, CONFLICT, and BACKEND_ERROR and preserves each typed cause."
    - "Explicit policy recomputation retains the original failure outcome/cause in the observable call record."
    - "cache_clear returns exact attempted/removed/conflict/failure/completion counts and never returns a constant or guessed count."
    - "Function-scoped clear cannot remove an adjacent function namespace and preserves a concurrently replaced generation."
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest -q tests/test_phase6_decorator_contract.py tests/test_phase6_removal_contract.py -o log_cli=false</automated>
  </verify>
  <done>Decorator recomputation is explicit by outcome and function clearing reports canonical removal truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Python call arguments -> cache key | Equivalent calls must normalize deterministically without conflating different functions. |
| Storage outcome -> user function execution | Failure classes must not trigger silent user-code execution unless explicitly authorized. |
| Function namespace -> bounded invalidation | One decorated function must not authorize deletion of another namespace. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-13 | Spoofing | function identity | high | mitigate | Use qualified function identity plus canonical bound arguments and test cross-function isolation. |
| T-06-14 | Tampering | failure recomputation | high | mitigate | Default only absent/expired; require explicit enumerated policy for failure outcomes. |
| T-06-15 | Repudiation | suppressed lookup result | high | mitigate | Retain original outcome/cause in observable call diagnostics even after explicit recompute. |
| T-06-16 | Denial of service | function clear | medium | mitigate | Reuse bounded predicate invalidation and opaque continuation. |

No package installation occurs in this plan.
</threat_model>

<verification>
Run all decorator tests plus bounded removal tests. Inspect the module with AST-based tests to prove there is no atexit, weakref registry, implicit UnifiedCache construction, or lock primitive.
</verification>

<success_criteria>
- Decorators require an explicit cache and never own it.
- None is a valid hit and function identities remain isolated.
- Default recomputation is exactly absent/expired.
- Function clear returns real bounded removal outcomes.
</success_criteria>

<output>
Create `.planning/phases/06-unifiedcache-policy-composition/06-04-SUMMARY.md` when done.
</output>
