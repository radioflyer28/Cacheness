---
phase: 09-adoption-and-release-surface-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/interfaces.py
  - src/cacheness/handlers.py
  - src/cacheness/error_handling.py
  - src/cacheness/storage/__init__.py
  - src/cacheness/storage/handlers/__init__.py
  - tests/test_interfaces.py
  - tests/test_handler_registration.py
  - tests/test_error_handling.py
autonomous: true
requirements:
  - CACH-06
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Users import and subclass FormatHandler from the storage-oriented public barrels, while CacheHandler and CacheHandlerError are absent rather than retained as aliases (D-15)."
    - "Built-in and custom handlers continue to use HandlerRegistry and store.handlers.register_handler(...) without another registry API (D-17)."
    - "The Python rename leaves every built-in data_type, payload_format, payload_format_version, and registration rule unchanged (D-16)."
    - "The generic format protocol is promoted as the primary identity; optional integrations remain request-bound capabilities rather than identity anchors."
  artifacts:
    - path: src/cacheness/interfaces.py
      provides: "FormatHandler protocol and FormatHandlerError hierarchy"
      contains: "class FormatHandler"
    - path: src/cacheness/handlers.py
      provides: "Built-in FormatHandler subclasses and unchanged HandlerRegistry registration seam"
      contains: "FormatHandler"
    - path: src/cacheness/storage/__init__.py
      provides: "Storage-oriented public FormatHandler export"
      contains: '"FormatHandler"'
  key_links:
    - from: src/cacheness/storage/__init__.py
      to: src/cacheness/interfaces.py
      via: "explicit FormatHandler and FormatHandlerError re-export"
      pattern: "FormatHandler"
    - from: src/cacheness/handlers.py
      to: src/cacheness/interfaces.py
      via: "built-in subclasses and registry annotations use the generic format protocol"
      pattern: "class .*Handler\(FormatHandler\)"
    - from: src/cacheness/storage/handlers/__init__.py
      to: src/cacheness/interfaces.py
      via: "the compatibility/public storage-handlers barrel changes in the same atomic task as the defining protocol"
      pattern: "FormatHandler|FormatHandlerError"
---

<objective>
Cut the live Python public surface from the misleading cache-specific protocol name to the generic format-handler contract without changing stored data.

Purpose: Complete the costly pre-release D-15 naming correction while preserving D-16 durable identities and D-17 store-local registration.
Output: Alias-free `FormatHandler`/`FormatHandlerError` contracts, updated built-ins, both public barrels, and an internally coherent active handler test suite.
</objective>

<scope_guard>
This plan is a source-level public-name cutover only. It must not change lifecycle sequencing, handler staging, registry semantics, payload bytes, manifest fields, schema/store versions, migration behavior, backend composition, or ADR 0001 guarantees. It must not add a compatibility alias or rewrite historical `.planning` evidence.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/09-adoption-and-release-surface-closure/09-CONTEXT.md
@.planning/phases/09-adoption-and-release-surface-closure/09-RESEARCH.md
@.planning/phases/09-adoption-and-release-surface-closure/09-PATTERNS.md
@src/cacheness/interfaces.py
@src/cacheness/handlers.py
@src/cacheness/storage/__init__.py
@tests/test_handler_registration.py
@tests/test_stored_compatibility.py
</context>

<interfaces>
Preserve these contracts while changing only the public Python noun:

- `FormatHandler` replaces `CacheHandler` and retains `PAYLOAD_FORMAT_VERSION`, `data_type`, `payload_format`, `payload_format_version`, `payload_identity()`, `supports_payload_contract()`, and the path-based `put`/`get` methods.
- `HandlerRegistry.register_handler(handler, priority=None, name=None)`, `get_handler(data)`, `get_handler_by_type(data_type)`, and `store.handlers.register_handler(...)` retain their behavior and names.
- `CacheWriteError`, `CacheReadError`, `CacheFormatError`, and `CacheValidationError` retain their names but inherit from `FormatHandlerError`.
- Persisted `handler_type` continues to receive `handler.data_type`; no Python class name enters the manifest.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cut the independent cross-cutting error vocabulary to FormatHandlerError</name>
  <reversibility rating="costly">Undoing D-15 would require another coordinated edit across public imports, subclasses, annotations, tests, examples, and documentation.</reversibility>
  <files>src/cacheness/error_handling.py, tests/test_error_handling.py</files>
  <behavior>
    - "The independent cross-cutting handler-operation base is FormatHandlerError and preserves CacheError inheritance, causes, decorator translation, and structured context."
    - "No compatibility alias is introduced; focused error tests use the new public noun directly."
  </behavior>
  <action>Rename the independent `error_handling.py` base and its focused tests together, preserving CacheError inheritance, decorator conversion, original causes, and structured context. This class does not participate in either storage barrel, so the task is independently coherent. Do not add an alias or alter narrower failure-kind classes elsewhere.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_error_handling.py -x</automated>
  </verify>
  <done>The cross-cutting error base and all of its direct consumers use FormatHandlerError without an alias or behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomically cut the core protocol, interface error, both barrels, and importing tests</name>
  <files>src/cacheness/interfaces.py, src/cacheness/handlers.py, src/cacheness/storage/__init__.py, src/cacheness/storage/handlers/__init__.py, tests/test_interfaces.py, tests/test_handler_registration.py</files>
  <behavior>
    - "`from cacheness.storage import FormatHandler` succeeds and a custom subclass registers through `store.handlers.register_handler(..., priority=0)` for a round trip."
    - "Both storage barrels expose FormatHandler/FormatHandlerError and import/export neither retired name."
    - "Built-ins remain FormatHandler subclasses with unchanged data/payload/version identity tuples."
    - "Interface CacheWriteError, CacheReadError, CacheFormatError, and CacheValidationError retain names/context below FormatHandlerError."
    - "Registry selection, priorities, transformation validation, optional behavior, and exact payload-contract matching are unchanged."
  </behavior>
  <action>Change the four mutually dependent source/barrel files and both importing tests in one commit so no completed task imports a deleted symbol. Rename the protocol, interface error base, built-in bases/annotations/comparisons, and both explicit barrels together. Update protocol-facing text and remove import-time dataframe availability logging while preserving request-time capability errors. Do not create an alias, rename HandlerRegistry, or change data_type, payload format, version, transformation, suffix, exception cause, or registration semantics. Per D-15/D-17, FormatHandler is the one native-serialization protocol; optional integrations stay request-bound.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_interfaces.py tests/test_handler_registration.py -x &amp;&amp; uv run python -c "import cacheness.storage as s; import cacheness.storage.handlers as h; assert s.FormatHandler is h.FormatHandler; assert not hasattr(s, 'CacheHandler'); assert not hasattr(h, 'CacheHandler')"</automated>
  </verify>
  <done>The core, both barrels, and every test importing the protocol are coherent in the same commit; a store-local round trip works and no alias exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| application custom handler -> HandlerRegistry | User-defined code enters the existing store-local format selection seam. |
| handler result -> guarded staging/manifest | Native suffix, regular-file output, and stable identity cross into managed storage. |
| Python public name -> persisted manifest | A source rename must not alter durable data/format/version identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Tampering | persisted handler and payload identity | high | mitigate | Freeze exact identity tuples and prove filesystem-plus-SQLite close/reopen without migration or rewrite. |
| T-09-02 | Spoofing | public protocol exports | medium | mitigate | Export only FormatHandler/FormatHandlerError from current barrels and assert retired attributes and `__all__` entries are absent. |
| T-09-03 | Elevation of Privilege | custom handler filesystem output | high | mitigate | Preserve guarded staging tests for suffix containment, regular files, descriptors, symlinks, and path substitution; make no I/O implementation change. |
| T-09-04 | Repudiation | registry selection and error context | low | accept | Existing deterministic registry order and structured exception context remain under focused tests; this plan adds no new event or audit boundary. |
</threat_model>

<verification>
- Focused interface, registry, built-in handler, and error tests pass in the atomic cutover commit.
- Both storage barrels import successfully immediately after the task; no completed task references a deleted source symbol.
- The live source has one generic protocol and one existing registry seam; no compatibility alias or second abstraction is introduced.
- No lifecycle, backend, manifest, schema, migration, or payload implementation file changes.
</verification>

<acceptance_criteria>
- D-15 is implemented in the live Python API and active tests.
- D-16 is preserved in source identities here and proven by exact persisted identity/durable reopen assertions in Plan 09-02.
- D-17 remains the only documented/implemented custom registration shape.
- CACH-06 now has a coherent generic serialization extension surface shared by direct storage and cache composition.
</acceptance_criteria>

<source_coverage>
- GOAL: Current public format extension accurately reflects the completed BlobStore/UnifiedCache architecture without lifecycle redesign.
- REQ: CACH-06 coherent public import/constructor/result surface.
- RESEARCH: Alias-free active-tree cutover, both error hierarchies, storage barrels, guarded staging, and identity-preserving durable reopen.
- CONTEXT: D-14, D-15, D-16, D-17.
</source_coverage>

<success_criteria>
- Users can import, subclass, register, and round-trip through FormatHandler.
- No active Python compatibility alias preserves the old cache-specific protocol name.
- Built-in format identities remain unchanged; Plan 09-02 adds the durable reopen and source-free wheel proof.
- ADR 0001's authority and guarded-I/O boundaries are untouched.
</success_criteria>

<output>
Create `.planning/phases/09-adoption-and-release-surface-closure/09-01-SUMMARY.md` when done.
</output>
