---
phase: 09-adoption-and-release-surface-closure
plan: 07
type: execute
wave: 4
depends_on: [09-02, 09-04, 09-05]
files_modified:
  - tests/test_phase9_documentation.py
  - README.md
  - docs/README.md
  - docs/BLOB_STORE.md
  - docs/CACHE_POLICY.md
  - docs/STORAGE_INITIALIZATION.md
  - docs/STORAGE_MIGRATION.md
autonomous: true
requirements: [CACH-06]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "README is a concise BlobStore-first gateway with exactly two current quick starts (D-01, D-05)."
    - "README labels the checkout a local-ready development version and does not imply publication or remote qualification (D-04, D-07, D-08)."
    - "Checkout plus base-only uv sync is primary, a local wheel secondary, and extras appear only when requested by the relevant task (D-06, D-09)."
    - "Task-first navigation covers store, cache, add-format, and operate/migrate without SqlCache promotion (D-02, D-05)."
  artifacts:
    - path: tests/test_phase9_documentation.py
      provides: "Adoption-surface documentation contract"
    - path: README.md
      provides: "Concise gateway, status box, two quick starts, and task links"
    - path: docs/BLOB_STORE.md
      provides: "Direct storage/catalog guide"
    - path: docs/CACHE_POLICY.md
      provides: "UnifiedCache/decorator/result guide"
  key_links:
    - from: README.md
      to: examples/memory_blob_store.py
      via: "direct-storage quick start"
      pattern: "BlobStore|memory_blob_store.py"
    - from: README.md
      to: examples/unified_cache.py
      via: "cache-policy quick start"
      pattern: "UnifiedCache|cached\(cache="
    - from: docs/README.md
      to: docs/RELEASE_QUALIFICATION.md
      via: "single qualification owner link"
      pattern: "RELEASE_QUALIFICATION"
    - from: README.md
      to: uv.lock
      via: "exact frozen base-only checkout installation command"
      pattern: "uv sync --frozen --no-default-groups"
---

<objective>
Replace stale onboarding with a concise gateway and task-first current local workflow guides.

Purpose: Let a developer install from checkout and complete storage/cache work without removed constructors or unsupported claims.
Output: README quick starts, task navigation, direct store/cache guides, and operator initialization/migration guidance.
</objective>

<scope_guard>
Describe only existing APIs and qualified evidence. Do not promote SqlCache, remote topologies, Windows, controlled performance, or publication, and do not change production lifecycle behavior.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-adoption-and-release-surface-closure/09-CONTEXT.md
@.planning/phases/09-adoption-and-release-surface-closure/09-RESEARCH.md
@.planning/phases/09-adoption-and-release-surface-closure/09-PATTERNS.md
@.planning/phases/09-adoption-and-release-surface-closure/09-02-SUMMARY.md
@.planning/phases/09-adoption-and-release-surface-closure/09-03-SUMMARY.md
@README.md
@docs/RELEASE_QUALIFICATION.md
@examples/memory_blob_store.py
@examples/unified_cache.py
</context>

<interfaces>
- README snippets use canonical public signatures with explicit topology/initialization.
- README installation uses `uv sync --frozen --no-default-groups`; optional extras are introduced only by the task guide that consumes the capability.
- Cache docs preserve outcomes `hit`, `absent`, `expired`, `corrupt`, `conflict`, `backend_error` and `@cached(cache=cache)`.
- Operator docs preserve stopped-worker maintenance and no implicit upgrade.
- `docs/RELEASE_QUALIFICATION.md` stays the sole detailed matrix; Plan 09-08 rewrites it.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the README gateway and task navigation execute the current story</name>
  <files>tests/test_phase9_documentation.py, README.md, docs/README.md</files>
  <behavior>
    - "The contract detects removed APIs, SqlCache promotion, install-everything advice, and remote qualification in gateway/navigation."
    - "README has a local-ready box, checkout-plus-uv primary install, secondary wheel instructions, and exactly two quick starts."
    - "The primary checkout command is exactly `uv sync --frozen --no-default-groups`; capability extras are absent from the base path and introduced only in the guide that needs them."
    - "README establishes BlobStore lifecycle and UnifiedCache policy over a caller-selected store."
    - "docs/README.md orders task journeys before component references."
  </behavior>
  <action>Create the documentation contract first and assert that the primary checkout command is exactly `uv sync --frozen --no-default-groups`, that the base installation section contains no optional-extra or dependency-group opt-in, and that capability extras appear only in the task guide that requires them. Then replace README with a concise gateway sourced from exact examples: architecture statement, status box, checkout plus the base-only command, secondary local-wheel instructions, direct memory BlobStore start, UnifiedCache plus decorator start, and task/qualification links. Rewrite docs navigation task-first. Do not duplicate the full matrix, recommend all extras, claim frozen v1.0, or promote SqlCache.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase9_documentation.py tests/test_phase9_examples.py tests/test_public_api_contract.py -x</automated>
  </verify>
  <done>A user can install the base checkout without development/default groups, follow either current quick start, and reach task-first guides whose capability extras are introduced only where needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite store, cache, initialization, and migration guides</name>
  <files>docs/BLOB_STORE.md, docs/CACHE_POLICY.md, docs/STORAGE_INITIALIZATION.md, docs/STORAGE_MIGRATION.md</files>
  <behavior>
    - "Store guidance uses explicit topology/initialize, receipts/results, catalog query/update, close, and reopen."
    - "Cache guidance shows policy over BlobStore, explicit decorator, cached None, typed outcomes, invalidation/clear, and complete-entry deletion."
    - "Initialization is deliberate and ordinary opens do not silently bootstrap/migrate shared stores."
    - "Migration/rebuild is explicit stopped-worker offline inventory/copy-verify-switch or confirmed rebuild."
  </behavior>
  <action>Rewrite direct storage/cache guides against actual signatures and exact examples. Update initialization/migration only enough to remove stale constructors/navigation. Use ADR 0001 vocabulary: SQLite plus filesystem is crash-consistent through immutable generations/reconciliation, not cross-resource ACID; typed contention outcomes remain valid. Introduce extras only where requested and keep SqlCache outside the journey pending Phase 10.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase9_documentation.py tests/test_phase9_examples.py tests/test_public_api_contract.py -x</automated>
  </verify>
  <done>Storage, cache policy, initialization, and maintenance guides use current APIs and explicit guarantee boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| documentation -> adopter code | Imports/constructors must map to executable APIs. |
| local readiness -> remote/release claim | Onboarding must not strengthen qualification. |
| operator guide -> durable store | Instructions preserve initialization/stopped-worker maintenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-17 | Spoofing | README API | high | mitigate | Bind snippets to exact examples/public tests. |
| T-09-18 | Elevation of Privilege | qualification language | high | mitigate | Use local-ready/NOT_PUBLISHED status and exclude remote quick starts. |
| T-09-19 | Tampering | maintenance guidance | high | mitigate | Preserve explicit initialization and stopped-worker operations. |
| T-09-20 | Repudiation | duplicated claims | medium | mitigate | Link one detailed owner under tests. |
</threat_model>

<verification>
- Documentation, exact examples, and public API contracts pass.
- README contains exactly two locked quick-start roles and a bounded status box.
- Navigation contains all four tasks without promoted SqlCache/remote workflow.
</verification>

<acceptance_criteria>
- D-01 through D-09 are implemented in the primary adoption path.
- D-09 is enforced by a documentation contract for `uv sync --frozen --no-default-groups`, with task-local capability extras only.
- CACH-06 constructor/decorator/result model is accurately published.
- Operations remain truthful about local scope and migration discipline.
</acceptance_criteria>

<source_coverage>
- GOAL: Primary docs make the architecture accurately usable.
- REQ: CACH-06 published import, constructor, configuration, decorator, result surface.
- RESEARCH: Concise gateway, task-first map, checkout install, example linkage, typed outcomes, explicit maintenance.
- CONTEXT: D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-09.
</source_coverage>

<success_criteria>
- README and task guides work without refactor-history knowledge.
- Product story is BlobStore plus optional UnifiedCache policy.
- Limits are linked without overclaim.
</success_criteria>

<output>
Create `.planning/phases/09-adoption-and-release-surface-closure/09-07-SUMMARY.md` when done.
</output>
