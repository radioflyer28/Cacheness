# Phase 11: put_batch() API - Plan

## Plan 11-01: Implement put_batch()

### Goal
Add the missing `put_batch()` method to UnifiedCache, completing the batch operations surface.

### Tasks
1. Implement `put_batch(items: List[Tuple[Any, Dict[str, Any]]]) -> int` on UnifiedCache
2. Follow existing batch patterns: `with self._lock`, iterate calling `self.put()`, return count
3. Allow partial success with logging on individual failures
4. Add tests: basic, empty, mixed types, overwrite, all backends, roundtrip with get_batch

### Verification
- All 8 new tests pass
- Existing test suite unaffected (117 passed in test_update_operations + test_core)
