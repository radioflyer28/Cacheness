---
phase: 21-retroactive-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-configurable-key-fallback/15-VERIFICATION.md
  - .planning/phases/18-encryption-at-rest/18-VERIFICATION.md
  - .planning/phases/19-core-decomposition-ii/19-VERIFICATION.md
  - src/cacheness/__init__.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [SEC-02, SEC-03, SEC-04, ARCH-01]
gap_closure: true

must_haves:
  truths:
    - "VERIFICATION.md exists for Phase 15 confirming SEC-02 is satisfied"
    - "VERIFICATION.md exists for Phase 18 confirming SEC-03 is satisfied"
    - "VERIFICATION.md exists for Phase 19 confirming ARCH-01 is satisfied"
    - "RotationResult is importable from cacheness top-level package"
    - "REQUIREMENTS.md checkboxes show [x] for SEC-02, SEC-03, SEC-04, ARCH-01"
    - "ROADMAP.md marks Phase 18 and Phase 19 as Complete with dates"
  artifacts:
    - path: ".planning/phases/15-configurable-key-fallback/15-VERIFICATION.md"
      provides: "Formal verification of SEC-02 requirement"
      contains: "status: passed"
    - path: ".planning/phases/18-encryption-at-rest/18-VERIFICATION.md"
      provides: "Formal verification of SEC-03 requirement"
      contains: "status: passed"
    - path: ".planning/phases/19-core-decomposition-ii/19-VERIFICATION.md"
      provides: "Formal verification of ARCH-01 requirement"
      contains: "status: passed"
    - path: "src/cacheness/__init__.py"
      provides: "RotationResult in public API exports"
      contains: "RotationResult"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "VERIFICATION.md files"
      via: "checkbox status matches verification status"
      pattern: "\\[x\\].*SEC-0[234]|\\[x\\].*ARCH-01"
---

<objective>
Formally verify and document 4 code-complete requirements (SEC-02, SEC-03, SEC-04, ARCH-01) that the v0.10.0 milestone audit identified as having documentation gaps. Fix one minor integration gap (RotationResult export). Update all stale planning documents.

Purpose: Close the "partial" and "checkbox stale" gaps from the v0.10.0 milestone audit so re-audit can pass.
Output: 3 VERIFICATION.md files, 1 code fix, updated planning docs.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v0.10.0-MILESTONE-AUDIT.md
@.planning/phases/16-hkdf-key-derivation/16-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VERIFICATION.md for Phases 15, 18, 19</name>
  <files>
    .planning/phases/15-configurable-key-fallback/15-VERIFICATION.md
    .planning/phases/18-encryption-at-rest/18-VERIFICATION.md
    .planning/phases/19-core-decomposition-ii/19-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/16-hkdf-key-derivation/16-VERIFICATION.md
    .planning/v0.10.0-MILESTONE-AUDIT.md
    .planning/ROADMAP.md
    src/cacheness/security.py
    src/cacheness/encryption.py
    src/cacheness/core.py
    src/cacheness/config.py
    tests/test_key_fallback_policy.py
    tests/test_encryption_at_rest.py
  </read_first>
  <action>
    Run targeted tests to confirm all 3 requirements still pass:
    ```bash
    uv run pytest tests/test_key_fallback_policy.py tests/test_encryption_at_rest.py -x -q --override-ini="addopts="
    ```

    Then create 3 VERIFICATION.md files following the exact format of `16-VERIFICATION.md`.

    **15-VERIFICATION.md** (SEC-02: Configurable key fallback behavior):
    - YAML frontmatter: `status: passed`, `phase: 15-configurable-key-fallback`, `requirement_ids: [SEC-02]`, `verified: {today}`
    - Requirement Coverage table verifying:
      - `key_fallback_policy` field exists in `SecurityConfig` in config.py (accepts "raise", "warn", "fallback")
      - `raise` policy triggers `CacheSecurityError` when key file is missing
      - `warn` policy logs warning and falls back to in-memory key
      - `fallback` policy silently uses in-memory key
      - Backward-compatible deprecation shim for `raise_on_key_fallback`
    - Evidence: grep for `key_fallback_policy` in config.py, security.py, core.py
    - Test results: tests/test_key_fallback_policy.py (13 tests)

    **18-VERIFICATION.md** (SEC-03: Encryption at rest):
    - YAML frontmatter: `status: passed`, `phase: 18-encryption-at-rest`, `requirement_ids: [SEC-03]`, `verified: {today}`
    - Requirement Coverage table verifying:
      - `enable_content_encryption` config field exists in SecurityConfig
      - AES-256-GCM encryption implemented in encryption.py (107 lines)
      - `_write_blob()` encrypts when enabled, `_read_blob()` decrypts transparently
      - Disabled by default (no behavior change for existing users)
      - Unencrypted entries remain readable when encryption is later enabled
      - `derive_encryption_key()` uses HKDF with info `b"cacheness-aes-gcm-v1:{ns}"`
    - Evidence: grep for `encrypt_blob`, `decrypt_blob` in encryption.py; `enable_content_encryption` in config.py
    - Test results: tests/test_encryption_at_rest.py (18+ tests)

    **19-VERIFICATION.md** (ARCH-01: Core decomposition):
    - YAML frontmatter: `status: passed`, `phase: 19-core-decomposition-ii`, `requirement_ids: [ARCH-01]`, `verified: {today}`
    - Must create a Phase 19 directory first: `mkdir -p .planning/phases/19-core-decomposition-ii`
    - Requirement Coverage table verifying:
      - core.py line count is 1422 (target was ≤1500) ✅
      - 11 mixin files + _put_cleanup.py extracted as flat sibling files
      - List all mixin files: _batch_mixin.py, _convenience_mixin.py, _custom_metadata_mixin.py, _file_ops_mixin.py, _get_variants_mixin.py, _inline_blob_mixin.py, _query_mixin.py, _stats_mixin.py, _storage_mode_mixin.py, _update_mixin.py, _verification_mixin.py, _put_cleanup.py
      - `from cacheness.core import UnifiedCache` still works
      - All existing tests pass unchanged
    - Evidence: `(Get-Content src/cacheness/core.py).Count` shows ≤1500; `Get-ChildItem src/cacheness/_*mixin*.py` lists 11 files
  </action>
  <verify>
    <automated>
      uv run pytest tests/test_key_fallback_policy.py tests/test_encryption_at_rest.py -x -q --override-ini="addopts="
    </automated>
    Verify all 3 VERIFICATION.md files exist:
    - .planning/phases/15-configurable-key-fallback/15-VERIFICATION.md contains "status: passed"
    - .planning/phases/18-encryption-at-rest/18-VERIFICATION.md contains "status: passed"
    - .planning/phases/19-core-decomposition-ii/19-VERIFICATION.md contains "status: passed"
  </verify>
  <acceptance_criteria>
    - 15-VERIFICATION.md contains `status: passed` and `requirement_ids: [SEC-02]`
    - 18-VERIFICATION.md contains `status: passed` and `requirement_ids: [SEC-03]`
    - 19-VERIFICATION.md contains `status: passed` and `requirement_ids: [ARCH-01]`
    - Each file has Requirement Coverage table with at least 4 criterion rows
    - Each file has Test Results section
    - tests/test_key_fallback_policy.py exits 0
    - tests/test_encryption_at_rest.py exits 0
  </acceptance_criteria>
  <done>3 VERIFICATION.md files created confirming SEC-02, SEC-03, ARCH-01 are satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Export RotationResult from cacheness.__init__.py</name>
  <files>src/cacheness/__init__.py</files>
  <read_first>
    src/cacheness/__init__.py
    src/cacheness/interfaces.py
  </read_first>
  <action>
    Add `RotationResult` to the import from `.interfaces` in `__init__.py` (line ~42-50):
    ```python
    from .interfaces import (
        BlobReadContext,
        CacheHandler,
        EntryData,
        EntrySummary,
        HandlerResult,
        IntegrityReport,
        RotationResult,    # ADD THIS LINE
        SignableFields,
        WriteBlobResult,
    )
    ```

    Add `"RotationResult"` to the `__all__` list, in the interfaces section (around line 253), after `"IntegrityReport"`:
    ```python
        "IntegrityReport",  # Typed return contract for verify_integrity()
        "RotationResult",   # Typed return contract for rotate_key()
        "SignableFields",   # Typed contract for signable entry fields
    ```

    Verify with:
    ```bash
    uv run python -c "from cacheness import RotationResult; print(RotationResult)"
    ```
  </action>
  <verify>
    <automated>uv run python -c "from cacheness import RotationResult; print(RotationResult)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/cacheness/__init__.py` contains `RotationResult` in the interfaces import block
    - `src/cacheness/__init__.py` `__all__` list contains `"RotationResult"`
    - `uv run python -c "from cacheness import RotationResult"` exits 0
  </acceptance_criteria>
  <done>RotationResult is importable from cacheness top-level package</done>
</task>

<task type="auto">
  <name>Task 3: Update planning docs to reflect actual completion</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/v0.10.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    **REQUIREMENTS.md changes:**
    1. Check SEC-03 checkbox: `- [ ] **SEC-03**` → `- [x] **SEC-03**`
    2. Verify SEC-02 and SEC-04 are already `[x]` (they should be from gap closure plan creation)
    3. Check ARCH-01 checkbox: `- [ ] **ARCH-01**` → `- [x] **ARCH-01**`
    4. Update traceability table:
       - SEC-02: Status → `Complete`
       - SEC-03: Status → `Complete`
       - ARCH-01: Status → `Complete`
       - SEC-04: Status → `Complete` (already verified by Phase 17)

    **ROADMAP.md changes:**
    1. Phase 18 checkbox: `- [ ] **Phase 18:` → `- [x] **Phase 18:` (already done in gap closure)
    2. Phase 19 checkbox: Already `- [x]` from gap closure
    3. Phase 18 plan checkboxes: `- [ ] 18-01-PLAN.md` → `- [x] 18-01-PLAN.md`, same for 18-02
    4. Progress table: Phase 18 status → `Complete`, date → today. Phase 19 status → `Complete`, date → today.

    **STATE.md changes:**
    1. Update `last_activity` to today
    2. Update `progress.completed_phases` to 7 (15, 16, 17, 18, 19, 21 + Phase 20 superseded = 5 original + 2 gap closure phases counted)
    3. Update `Current Position` to reflect Phase 21 complete
    4. Update `percent` to reflect actual progress
    5. Remove "Encryption at rest for metadata and blobs" from Pending Todos (it's done)
  </action>
  <verify>
    <automated>
      uv run python -c "
import pathlib
req = pathlib.Path('.planning/REQUIREMENTS.md').read_text()
assert '[x] **SEC-02**' in req, 'SEC-02 not checked'
assert '[x] **SEC-03**' in req, 'SEC-03 not checked'
assert '[x] **SEC-04**' in req, 'SEC-04 not checked'
assert '[x] **ARCH-01**' in req, 'ARCH-01 not checked'
print('All checkboxes verified')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `[x] **SEC-02**`, `[x] **SEC-03**`, `[x] **SEC-04**`, `[x] **ARCH-01**`
    - REQUIREMENTS.md traceability table shows `Complete` for SEC-02, SEC-03, SEC-04, ARCH-01
    - ROADMAP.md Phase 18 and 19 progress rows show `Complete`
    - STATE.md `last_activity` is today's date
    - STATE.md no longer lists "Encryption at rest" in Pending Todos
  </acceptance_criteria>
  <done>All planning documents accurately reflect the completion state of Phases 15-19</done>
</task>

</tasks>

<verification>
1. All 3 VERIFICATION.md files exist with `status: passed`
2. `from cacheness import RotationResult` succeeds
3. REQUIREMENTS.md has `[x]` for SEC-02, SEC-03, SEC-04, ARCH-01
4. ROADMAP.md shows Phase 18, 19 as Complete
5. Full test suite passes: `uv run pytest tests/ -x -q --ignore=tests/test_tensorflow_handler.py --override-ini="addopts="`
</verification>

<success_criteria>
- 3 VERIFICATION.md files confirm SEC-02, SEC-03, ARCH-01 are satisfied
- RotationResult importable from cacheness top-level
- All planning docs (REQUIREMENTS.md, ROADMAP.md, STATE.md) reflect actual completion state
- Full test suite still passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/21-retroactive-verification/21-01-SUMMARY.md`
</output>
