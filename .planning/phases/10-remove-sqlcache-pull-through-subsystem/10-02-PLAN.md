---
phase: 10-remove-sqlcache-pull-through-subsystem
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - tools/verify_phase4_cutover.py
  - tests/test_phase4_cutover_verifier.py
  - tools/verify_phase6_contracts.py
  - tests/test_phase6_contract_verifier.py
  - tools/verify_phase071_contracts.py
  - tests/test_phase071_contract_verifier.py
autonomous: true
requirements: [CACH-07]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Closed verifier manifests no longer require files that Phase 10 deletes (D-09, D-15)."
    - "Phase 4 and Phase 6 continue enforcing their retained lifecycle/cache contracts without rewriting historical validation artifacts (D-10)."
    - "Phase 07.1 routes the retired-product assertion to the Phase 10 negative contract while preserving its own closed inventories."
  artifacts:
    - path: tools/verify_phase4_cutover.py
      provides: "Phase 4 verifier without live parsing of deleted diagnostic nodes"
    - path: tools/verify_phase6_contracts.py
      provides: "Phase 6 verifier owning CACH-01 through CACH-06 only"
    - path: tools/verify_phase071_contracts.py
      provides: "Phase 07.1 verifier selecting the Phase 10 negative contract"
  key_links:
    - from: tools/verify_phase6_contracts.py
      to: tests/test_phase6_contract_verifier.py
      via: "literal fixed matrix mirrored by tests"
      pattern: "CACH-0[1-6]"
    - from: tools/verify_phase071_contracts.py
      to: tests/test_phase10_sqlcache_removal.py
      via: "exact negative selector"
      pattern: "test_phase10_sqlcache_removal.py"
  prohibitions:
    - "flagged-unverified: Closed verifier repair must not reopen, reinterpret, or rewrite completed phase history."
---

<objective>
Invert fixed verifier manifests before their referenced SqlCache test nodes disappear.

Purpose: Keep collection and diagnostics meaningful throughout the cut instead of hiding deletion behind broken verifier imports.
Output: Repaired Phase 4, Phase 6, and Phase 07.1 verifier/test pairs.
</objective>

<scope_guard>
Verifier inventories only. Do not change production lifecycle code, historical validation documents, guarantees, or Phase 10 runtime deletion.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-CONTEXT.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-RESEARCH.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-PATTERNS.md
@tools/verify_phase4_cutover.py
@tools/verify_phase6_contracts.py
@tools/verify_phase071_contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-02-01: Remove deleted diagnostics from the Phase 4 live verifier</name>
  <files>tools/verify_phase4_cutover.py, tests/test_phase4_cutover_verifier.py</files>
  <read_first>tools/verify_phase4_cutover.py; tests/test_phase4_cutover_verifier.py; .planning/phases/04-metadata-composition-and-topology-contracts/04-VALIDATION.md</read_first>
  <behavior>
    - "Phase 4 retained lifecycle ownership remains closed and executable."
    - "Historical deferred SqlCache paths are no longer normalized or existence-checked as live files."
  </behavior>
  <action>Per D-09/D-10/D-15, remove the three dedicated SqlCache test paths from live parsing and validation, adapt mirrored assertions, and leave the historical Phase 4 validation artifact untouched. Preserve the fixed Phase 4 owned matrix, normalized-path validation, and failure semantics for every retained node.</action>
  <acceptance_criteria>The Phase 4 verifier and its tests pass without requiring any soon-to-be-deleted file and without changing its lifecycle claims.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase4_cutover_verifier.py -x &amp;&amp; uv run --isolated --group dev --frozen ruff check tools/verify_phase4_cutover.py tests/test_phase4_cutover_verifier.py</automated></verify>
  <done>Phase 4 verification remains closed, literal, and independent of Phase 10 deletions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-02-02: Return Phase 6 verification to CACH-01 through CACH-06</name>
  <files>tools/verify_phase6_contracts.py, tests/test_phase6_contract_verifier.py</files>
  <read_first>tools/verify_phase6_contracts.py; tests/test_phase6_contract_verifier.py; .planning/REQUIREMENTS.md CACH-01 through CACH-07</read_first>
  <behavior>
    - "Phase 6 no longer owns a live CACH-07 regression node."
    - "All retained Phase 6 contract names, fixed paths, and AST checks remain exact."
  </behavior>
  <action>Remove the dedicated test path and CACH-07 regression entry from both literal verifier and mirrored tests, per D-15. Keep Phase 6 ownership of CACH-01 through CACH-06 and all retained cache-policy/lifecycle checks unchanged; do not broaden discovery or infer nodes from Git state.</action>
  <acceptance_criteria>The Phase 6 verifier passes with a closed retained matrix and delegates CACH-07 exclusively to Phase 10.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase6_contract_verifier.py -x &amp;&amp; uv run --isolated --group dev --frozen ruff check tools/verify_phase6_contracts.py tests/test_phase6_contract_verifier.py</automated></verify>
  <done>Phase 6 verifies its actual supported cache-policy scope without a deleted product node.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-02-03: Point Phase 07.1 retirement coverage at the Phase 10 contract</name>
  <files>tools/verify_phase071_contracts.py, tests/test_phase071_contract_verifier.py</files>
  <read_first>tools/verify_phase071_contracts.py; tests/test_phase071_contract_verifier.py; tests/test_phase10_sqlcache_removal.py created by Plan 10-01</read_first>
  <behavior>
    - "The obstore phase verifies the new negative boundary rather than a positive SqlCache selector."
    - "Phase 07.1 plan, decision, threat, and non-claim inventories remain byte-for-byte semantically closed."
  </behavior>
  <action>Replace only the retired-product selector/path with the exact Phase 10 negative-contract node and update its mirrored assertion. Preserve every Phase 07.1-owned inventory and storage assertion; do not touch obstore or lifecycle production code.</action>
  <acceptance_criteria>The Phase 07.1 verifier can run after deletion and still proves all of its original storage claims.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase071_contract_verifier.py -x &amp;&amp; uv run --isolated --group dev --frozen ruff check tools/verify_phase071_contracts.py tests/test_phase071_contract_verifier.py</automated></verify>
  <done>The fixed Phase 07.1 verifier is connected to the new retirement owner without scope expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| historical phase evidence -> live verifier | Historical truth must not force deleted files to remain live. |
| literal verifier matrix -> repository paths | Every retained path stays normalized, explicit, and existence-checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-05 | Repudiation | historical verifier repair | medium | mitigate | Preserve historical artifacts and change only current executable manifests. |
| T-10-06 | Tampering | fixed verifier inventories | medium | mitigate | Keep literal normalized matrices and mirrored exact-set tests. |
</threat_model>

<verification>
- All three verifier test modules pass independently.
- Scoped Ruff passes for the six changed Python files.
</verification>

<acceptance_criteria>
- D-09 permits later physical deletion without collection failure.
- D-10 preserves historical records.
- D-15 retains useful closed verification.
</acceptance_criteria>

## Artifacts this phase produces

- Updated closed verifier manifests only.
- No new production modules or public symbols.

<source_coverage>
- GOAL: Remove the separate product without destabilizing retained verification.
- REQ: CACH-07 dedicated-test deletion and surviving contracts.
- RESEARCH: Fixed-manifest inversion must precede deletion.
- CONTEXT: D-09, D-10, D-15.
</source_coverage>

<success_criteria>
- No closed verifier names a deleted SqlCache test as a live contract node.
- Retained lifecycle/cache verification remains literal and passing.
</success_criteria>

<output>
Create `.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-02-SUMMARY.md` when done.
</output>
