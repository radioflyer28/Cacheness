---
phase: 10-remove-sqlcache-pull-through-subsystem
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-04]
files_modified:
  - src/cacheness/sql_cache.py
  - src/cacheness/__init__.py
  - src/cacheness/error_handling.py
  - tests/test_sql_cache.py
  - tests/test_sql_cache_documentation.py
  - tests/test_sql_cache_failure_contract.py
  - tests/test_public_api_contract.py
  - tests/test_phase6_public_api_contract.py
autonomous: true
requirements: [CACH-07]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The dedicated runtime module and all public names disappear physically and imports fail naturally (D-01, D-13)."
    - "All three dedicated test modules are deleted after reusable contracts no longer depend on them (D-09, D-15)."
    - "Only proven SqlCache-only error reasons disappear; the shared typed hierarchy remains intact (D-07, D-08)."
    - "Version 0.3.14 and all BlobStore/UnifiedCache/lifecycle behavior remain unchanged (D-04)."
  artifacts:
    - path: src/cacheness/__init__.py
      provides: "Post-cut BlobStore/UnifiedCache package barrel"
    - path: src/cacheness/error_handling.py
      provides: "Retained shared error hierarchy without orphan reasons"
  key_links:
    - from: src/cacheness/__init__.py
      to: tests/test_phase10_sqlcache_removal.py
      via: "literal natural-absence contract"
      pattern: "__all__|find_spec"
  prohibitions:
    - "flagged-unverified: Direct deletion must not be replaced by a tombstone, alias, package hook, tailored runtime error, or new query-cache abstraction."
    - "flagged-unverified: Runtime deletion must not touch BlobStore, UnifiedCache, handler, authority, topology, or caller database code."
---

<objective>
Execute the direct source/public/test cut while preserving the shared storage/cache foundation.

Purpose: Remove the unrelated runtime and maintenance contract before the first supported release.
Output: No sql_cache module, public names, dedicated tests, or orphan error reasons.
</objective>

<scope_guard>
This is deletion only. Do not add deprecation behavior, inspect caller databases, change version, or modify storage lifecycle/composition code governed by ADR 0001.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-CONTEXT.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-01-SUMMARY.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-02-SUMMARY.md
@.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-05-01: Delete the runtime and top-level public surface</name>
  <files>src/cacheness/sql_cache.py, src/cacheness/__init__.py</files>
  <read_first>src/cacheness/sql_cache.py public symbol inventory; src/cacheness/__init__.py; tests/test_phase10_sqlcache_removal.py; 10-CONTEXT.md D-01/D-04/D-13</read_first>
  <behavior>
    - "The module file is absent and both stale import forms raise natural Python exceptions."
    - "The package has no retired exports, attributes, compatibility hooks, or aliases."
    - "The package version remains exactly 0.3.14."
  </behavior>
  <action>Delete `src/cacheness/sql_cache.py`. Remove its exact import and two `__all__` entries from the package barrel, rewrite only the barrel's current product description, and preserve every retained export and exact version per D-01/D-04/D-13. Add no substitute module, dynamic attribute handling, alias, exception translation, or replacement feature.</action>
  <acceptance_criteria>Both stale import forms fail naturally, the physical source is gone, and retained package imports are unchanged.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase10_sqlcache_removal.py::test_public_names_and_module_are_naturally_absent tests/test_phase10_sqlcache_removal.py::test_package_version_remains_unchanged -x</automated></verify>
  <done>The runtime and public surface are physically absent with no compatibility residue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-05-02: Prune only the orphan public error reasons</name>
  <files>src/cacheness/error_handling.py, tests/test_public_api_contract.py, tests/test_phase6_public_api_contract.py</files>
  <read_first>src/cacheness/error_handling.py; tests/test_public_api_contract.py after Plan 10-01; tests/test_phase6_public_api_contract.py after Plan 10-01; repository-wide callers of CacheReason</read_first>
  <behavior>
    - "The three SQL operation reasons and missing optional dependency reason are absent."
    - "Every retained CacheReason and exception inheritance contract stays exact."
    - "BlobStore and UnifiedCache still import and own their declared roles."
  </behavior>
  <action>Remove only the four values proven exclusive to the deleted subsystem and finish the Wave 0 public tests so they assert the now-green exact reason set and retained ownership, per D-07/D-08/D-15. Do not rename or reorganize shared errors, optional dependency behavior elsewhere, or storage/cache imports.</action>
  <acceptance_criteria>The exact-set/public tests pass and no retained source references a removed reason.</acceptance_criteria>
  <verify><automated>uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_public_api_contract.py tests/test_phase6_public_api_contract.py tests/test_phase6_suite_isolation.py -x &amp;&amp; uv run --isolated --group dev --frozen ruff check src/cacheness/__init__.py src/cacheness/error_handling.py tests/test_public_api_contract.py tests/test_phase6_public_api_contract.py</automated></verify>
  <done>The shared error/public contracts are minimal, exact, and fully retained.</done>
</task>

<task type="auto">
  <name>Task 10-05-03: Delete the three dedicated SqlCache test modules</name>
  <files>tests/test_sql_cache.py, tests/test_sql_cache_documentation.py, tests/test_sql_cache_failure_contract.py</files>
  <read_first>Plan 10-02 SUMMARY proving fixed manifests were inverted; Plan 10-04 SUMMARY proving quality/environment manifests were inverted; git references to the three paths</read_first>
  <action>Delete all three dedicated modules outright per D-09. Do not archive, rename, exclude, or replace them with compatibility tests. Confirm no current executable manifest imports or existence-checks them; historical planning and dated audit text may continue to name them.</action>
  <acceptance_criteria>All three files are absent and test collection succeeds without exclusions or broken fixed manifests.</acceptance_criteria>
  <verify><automated>test ! -e tests/test_sql_cache.py &amp;&amp; test ! -e tests/test_sql_cache_documentation.py &amp;&amp; test ! -e tests/test_sql_cache_failure_contract.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q tests/test_phase10_sqlcache_removal.py::test_removed_source_and_dedicated_tests_are_absent &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q --collect-only</automated></verify>
  <done>The dedicated runtime test product is removed and the remaining suite still collects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Python package -> stale callers | Absence must be ordinary and complete, not behavior-preserving. |
| subsystem errors -> shared hierarchy | Orphan pruning must not damage retained typed failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Spoofing | removed API | high | mitigate | Physical deletion plus export/attribute/spec/hook assertions. |
| T-10-09 | Denial of service | retained package surface | high | mitigate | Exact retained export/error tests and full collection after deletion. |
</threat_model>

<verification>
- Phase 10, public API, Phase 6 public/isolation, and collection gates pass for runtime/test absence.
- Scoped Ruff passes for every changed surviving Python file.
</verification>

<acceptance_criteria>
- D-01/D-13 remove runtime and compatibility residue.
- D-04 preserves package version.
- D-09/D-15 delete dedicated tests after contract inversion.
- D-07/D-08 prune only proven orphans.
</acceptance_criteria>

## Artifacts this phase produces

- A smaller existing public barrel/error model.
- No new production modules or public symbols; the plan deletes public symbols.

<source_coverage>
- GOAL: Remove the unrelated runtime while keeping one BlobStore foundation and cache-policy layer.
- REQ: CACH-07 implementation, exports, tests, and retained surfaces.
- RESEARCH: Coordinated runtime/API/error/test cut after Wave 0 inversion.
- CONTEXT: D-01, D-04, D-07, D-08, D-09, D-13, D-15.
</source_coverage>

<success_criteria>
- No runtime/test file or public behavior remains.
- BlobStore, UnifiedCache, and shared error behavior remain intact.
</success_criteria>

<output>
Create `.planning/phases/10-remove-sqlcache-pull-through-subsystem/10-05-SUMMARY.md` when done.
</output>
