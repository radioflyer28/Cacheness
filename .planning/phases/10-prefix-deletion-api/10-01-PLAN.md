---
phase: 10-prefix-deletion-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cacheness/metadata/base.py
  - src/cacheness/metadata/sqlite_backend.py
  - src/cacheness/metadata/json_backend.py
  - src/cacheness/core.py
  - tests/test_core.py
autonomous: true
requirements:
  - MGMT-01

must_haves:
  truths:
    - "cache.delete_by_prefix('myapp/models/') deletes all entries whose cache key starts with the given prefix"
    - "SQLite backend uses SQL LIKE for prefix match, not Python-side iteration"
    - "JSON backend falls back to Python-side prefix filtering"
    - "Deleting by prefix removes corresponding blob files, not just metadata"
    - "Calling delete_by_prefix with a non-matching prefix returns 0 without error"
  artifacts:
    - path: "src/cacheness/metadata/base.py"
      provides: "keys_by_prefix() method on MetadataBackend ABC"
      contains: "keys_by_prefix"
    - path: "src/cacheness/core.py"
      provides: "delete_by_prefix() public API method"
      contains: "delete_by_prefix"
  key_links:
    - from: "src/cacheness/core.py"
      to: "src/cacheness/metadata/base.py"
      via: "self.metadata_backend.keys_by_prefix()"
---

<objective>
Add delete_by_prefix() API for bulk deletion by cache key prefix.

Purpose: Enable users to delete groups of related cache entries efficiently (e.g., all model caches, all entries for a specific dataset).

Output: New public method on UnifiedCache, backend-optimized prefix queries, tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/cacheness/metadata/base.py
@src/cacheness/metadata/sqlite_backend.py
@src/cacheness/metadata/json_backend.py
@src/cacheness/core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keys_by_prefix to metadata backends + delete_by_prefix to core</name>
  <files>src/cacheness/metadata/base.py, src/cacheness/metadata/sqlite_backend.py, src/cacheness/metadata/json_backend.py, src/cacheness/core.py</files>
  <action>
  1. In `base.py` MetadataBackend ABC:
     - Add `keys_by_prefix(self, prefix: str) -> List[str]` with default implementation using iter_entry_summaries() + Python startswith filter
     - Also add to MetadataBackendWrapper to delegate

  2. In `sqlite_backend.py` SqliteBackend:
     - Override `keys_by_prefix()` with SQL: `SELECT cache_key FROM cache_entries WHERE cache_key LIKE ? AND namespace = ?` using `prefix + '%'` parameter (safe — parameterized, no injection)

  3. In `json_backend.py` JsonMetadataBackend:
     - Override `keys_by_prefix()` with Python dict keys filter (explicit, clearer than base default)

  4. In `core.py` UnifiedCache:
     - Add `delete_by_prefix(self, prefix: str) -> int` method near delete_where/delete_matching
     - Implementation: call `self.metadata_backend.keys_by_prefix(prefix)`, then `self._blob_store.delete(key)` for each, return count
     - Use self._lock for thread safety
  </action>
  <verify>
    <automated>uv run pytest tests/test_core.py -x -q --override-ini="addopts="</automated>
  </verify>
  <done>delete_by_prefix() works with all backends. SQLite uses SQL LIKE.</done>
</task>

<task type="auto">
  <name>Task 2: Add delete_by_prefix tests</name>
  <files>tests/test_core.py</files>
  <action>
  Add TestDeleteByPrefix class to test_core.py:

  1. test_delete_by_prefix_basic — put entries with different prefixes, delete one prefix, verify only matching are removed
  2. test_delete_by_prefix_removes_blobs — verify blob files are gone, not just metadata
  3. test_delete_by_prefix_no_match — non-matching prefix returns 0
  4. test_delete_by_prefix_empty_prefix — empty string matches all (or document behavior)
  5. test_delete_by_prefix_sqlite — same as basic but with sqlite backend

  Use cache.put(..., test_key="prefix/a"), cache.put(..., test_key="prefix/b"), cache.put(..., test_key="other/c") pattern.
  </action>
  <verify>
    <automated>uv run pytest tests/test_core.py -x -q --override-ini="addopts=" -k "delete_by_prefix"</automated>
  </verify>
  <done>5 tests pass confirming delete_by_prefix works correctly.</done>
</task>

</tasks>

<verification>
Existing tests pass. New delete_by_prefix tests pass. Both JSON and SQLite backends work.
</verification>

<success_criteria>
- delete_by_prefix() deletes entries by prefix and returns count
- SQLite uses SQL LIKE for prefix query
- Blob files removed along with metadata
- Non-matching prefix returns 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-prefix-deletion-api/10-01-SUMMARY.md`
</output>
