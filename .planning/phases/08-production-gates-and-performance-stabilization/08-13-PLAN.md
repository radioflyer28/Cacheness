---
phase: 08-production-gates-and-performance-stabilization
plan: 13
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - pyproject.toml
  - uv.lock
  - src/cacheness/config.py
  - src/cacheness/handlers.py
  - src/cacheness/storage/handlers/__init__.py
  - tests/test_tensorflow_removal.py
  - tests/test_tensorflow_handler.py
  - tests/packaging/test_wheel_matrix.py
  - tests/qualification/test_phase8_platform.py
  - tools/phase8_evidence.py
  - tools/run_phase8_packaging.py
  - tools/run_phase8_platform_gates.py
  - README.md
  - docs/CONFIGURATION.md
  - docs/CROSS_PLATFORM_GUIDE.md
  - docs/README.md
  - docs/TENSORFLOW_HANDLER_STATUS.md
  - docs/TENSORFLOW_TENSOR_GUIDE.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The built package has no native TensorFlow dependency group, lock package, handler, config field, registry identity, public export, guide, or Phase 8 package/platform evidence slot (D-23)."
    - "An authenticated stored payload contract naming `tensorflow_tensor` fails through the existing typed unsupported-version path before handler I/O; no compatibility read or migration is introduced (D-23)."
    - "Generic custom-handler registration and resolution remain usable for application-owned formats, while NumPy, NPZ, Blosc2, and dataframe/Parquet support remain unchanged (D-07, D-08, D-23)."
    - "Plans 08-02/08-03 and their summaries remain historical evidence, explicitly superseded rather than rewritten; current production-surface scans exclude `.planning` history (D-23)."
    - "All changed Python files satisfy direct Ruff lint and formatting checks, and Plan 08-05 retains its complete critical-scope formatting gate (D-16, D-17)."
  artifacts:
    - path: "tests/test_tensorflow_removal.py"
      provides: "Executable runtime, package, lock, docs, matrix, unsupported-contract, and custom-handler removal contract"
    - path: "src/cacheness/handlers.py"
      provides: "Handler registry without an in-tree TensorFlow implementation or identity"
    - path: "pyproject.toml"
      provides: "Final advertised dependency groups without TensorFlow"
    - path: "tools/run_phase8_packaging.py"
      provides: "Final one-wheel-per-advertised-group qualification inventory"
    - path: "tools/run_phase8_platform_gates.py"
      provides: "Stable/advisory/platform roles without a TensorFlow feature profile"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "normal uv lock regeneration removes TensorFlow and TensorFlow-named transitive packages"
      pattern: "project.optional-dependencies|dependency-groups"
    - from: "src/cacheness/handlers.py"
      to: "src/cacheness/storage/blob_store.py"
      via: "existing versioned handler resolution raises the typed unsupported payload error before handler I/O"
      pattern: "resolve_payload_contract|CacheBlobPayloadUnsupportedVersionError"
    - from: "tools/run_phase8_packaging.py"
      to: "tools/phase8_evidence.py"
      via: "the same exact remaining optional-group inventory is produced and validated"
      pattern: "OPTIONAL_GROUPS|REQUIRED_PACKAGING_GROUPS"
    - from: "tests/test_tensorflow_removal.py"
      to: "README.md and docs/*.md"
      via: "bounded current-surface scan excludes historical `.planning` summaries"
      pattern: "planning|docs"
---

<objective>
Remove native TensorFlow support from the shipped and qualified Cacheness surface before remaining Phase 8 gates.

Purpose: Implement D-23 as a clean pre-production support cutover, eliminating a low-value dependency and dormant handler without weakening custom extensibility, NumPy/Blosc2 behavior, versioned unsupported-format handling, ADR 0001, or D-16 quality enforcement.
Output: TensorFlow-free runtime/package/docs/evidence surfaces plus an executable absence and compatibility-boundary contract.
</objective>

<scope_guard>
This plan changes support surface only. It must not alter `BlobStore`, `AuthorityLifecycleEngine`, payload publication, reconciliation, cache policy, canonical SHA-256 semantics, or any ADR 0001 guarantee. It must not add a TensorFlow compatibility reader, migration, placeholder handler, empty dependency group, matrix incompatibility row, or alternate lifecycle path.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-02-SUMMARY.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-03-SUMMARY.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@tests/test_blob_manifest.py
@tests/test_handler_registration.py
</context>

<interfaces>
The retained public extension boundary is `HandlerRegistry.register_handler(...)` / `store.handlers.register_handler(...)` using the existing `CacheHandler` contract. The retained stored-format boundary is `HandlerRegistry.resolve_payload_contract(handler_type, payload_format, payload_format_version)`, translated by `BlobStore` to `CacheBlobPayloadUnsupportedVersionError`. Removing the in-tree `tensorflow_tensor` identity must make that existing boundary reject it; it does not create a new special-case exception or migration route.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove the native runtime handler through the versioned read boundary</name>
  <files>tests/test_tensorflow_removal.py, tests/test_tensorflow_handler.py, src/cacheness/config.py, src/cacheness/handlers.py, src/cacheness/storage/handlers/__init__.py</files>
  <behavior>
    - "`HandlerConfig` exposes no TensorFlow enablement field or priority identity, and default/runtime registries contain no TensorFlow handler."
    - "Neither `cacheness.handlers` nor `cacheness.storage.handlers` exposes the removed handler class, lazy loader, or availability flag."
    - "Resolving an authenticated `tensorflow_tensor`/`b2tr` contract raises the existing typed unsupported-version error before any handler payload read."
    - "A test-defined non-TensorFlow `CacheHandler` still registers, resolves by identity/versioned format, and round-trips through the retained custom-handler seam."
    - "Existing NumPy and Blosc2 handler selectors remain present and pass."
  </behavior>
  <action>Write the focused removal contract first. Delete the dedicated TensorFlow test module, lazy import state/function, handler class, configuration flag/priority mapping, registry/builtin identity, and compatibility-barrel export per D-23. Exercise the existing versioned unsupported-handler translation rather than adding a TensorFlow-specific branch. Prove the generic custom-handler seam with a test-local handler and run existing handler-registration plus NumPy/Blosc2 tests. Do not edit lifecycle code or use object/pickle fallback as a compatibility reader for an authenticated `tensorflow_tensor` manifest.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_tensorflow_removal.py -k 'runtime or stored or custom_handler or numpy or blosc2' tests/test_handler_registration.py tests/test_blob_manifest.py -x</automated>
  </verify>
  <done>The in-tree runtime identity is absent, stored TensorFlow contracts fail through the existing typed unsupported boundary before I/O, and custom plus NumPy/Blosc2 handlers remain operational.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove TensorFlow from dependency, packaging, platform, and evidence inventories</name>
  <files>pyproject.toml, uv.lock, tests/packaging/test_wheel_matrix.py, tests/qualification/test_phase8_platform.py, tools/phase8_evidence.py, tools/run_phase8_packaging.py, tools/run_phase8_platform_gates.py</files>
  <behavior>
    - "Project optional dependencies and development groups contain no TensorFlow group or requirement, and normal lock regeneration leaves no TensorFlow-named package node."
    - "The literal packaging inventory is exactly `recommended`, `dataframes`, `s3`, `postgresql`, and `cloud`, with one independent representative public probe per group."
    - "Platform roles retain Linux 3.11-3.14, macOS 3.11/3.14, prerelease advisory, and Windows nonclaim behavior without a TensorFlow feature profile, compatible-minor subset, row, probe, or `UNAVAILABLE` slot."
    - "Phase 8 packaging evidence validation accepts only the post-removal inventory and rejects a forged reintroduced TensorFlow entry."
  </behavior>
  <action>Remove the TensorFlow project optional dependency and development dependency group, then regenerate `uv.lock` with the repository's normal `uv lock` workflow so the dependency graph contains no TensorFlow-named packages. Delete all TensorFlow group/probe/compatibility parameters and branches from the packaging runner; delete the feature profile, compatible-minor constants, aggregators, and row rules from the platform runner; and update the strict evidence allow-list to the exact remaining groups per D-23. Rewrite the existing package/platform tests around the final literal inventories and add adversarial cases that reject a restored group or row. Preserve every base/NumPy/Blosc2/Parquet/service-nonclaim and Python/platform assertion that does not depend on TensorFlow.</action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_tensorflow_removal.py tests/packaging/test_wheel_matrix.py tests/qualification/test_phase8_platform.py -x</automated>
  </verify>
  <done>The package metadata, lock graph, package runner, platform runner, evidence schema, and their tests agree on one TensorFlow-free advertised surface without weakening any retained group or platform role.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove public documentation claims and close the complete absence gate</name>
  <files>README.md, docs/CONFIGURATION.md, docs/CROSS_PLATFORM_GUIDE.md, docs/README.md, docs/TENSORFLOW_HANDLER_STATUS.md, docs/TENSORFLOW_TENSOR_GUIDE.md, tests/test_tensorflow_removal.py</files>
  <behavior>
    - "The two dedicated TensorFlow guides are deleted, and current README/configuration/cross-platform/documentation-index surfaces contain no native support or enablement claim."
    - "The bounded repository scan covers current source, tests, tools, package metadata, lockfile, workflows, README, and docs while deliberately excluding historical `.planning` summaries and Git internals."
    - "A synthetic reintroduction in each surface class makes the absence contract fail, preventing a stale claim from reaching final verification."
    - "All changed Python files pass direct Ruff lint and format checks per D-16; Plan 08-05 remains responsible for the broader critical-scope normalization and baseline."
  </behavior>
  <action>Delete the dedicated status and tensor guides. Remove TensorFlow rows, examples, configuration fields, index links, and platform skip claims from the current README and documentation while keeping generic custom-handler documentation intact. Complete `tests/test_tensorflow_removal.py` with explicit structural/runtime/package/lock/docs/matrix assertions and bounded mutation cases. The scan must treat `.planning` summaries and completed Plans 08-02/08-03 as historical evidence, not current product surface. Run direct Ruff lint and formatting checks for every changed Python file per D-16/D-17; do not weaken or bypass the paused Plan 08-05 full critical-scope formatting requirement.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_tensorflow_removal.py tests/packaging/test_wheel_matrix.py tests/qualification/test_phase8_platform.py tests/test_handler_registration.py tests/test_blob_manifest.py -x &amp;&amp; uv run ruff check src/cacheness/config.py src/cacheness/handlers.py src/cacheness/storage/handlers/__init__.py tests/test_tensorflow_removal.py tests/packaging/test_wheel_matrix.py tests/qualification/test_phase8_platform.py tools/phase8_evidence.py tools/run_phase8_packaging.py tools/run_phase8_platform_gates.py &amp;&amp; uv run ruff format --check src/cacheness/config.py src/cacheness/handlers.py src/cacheness/storage/handlers/__init__.py tests/test_tensorflow_removal.py tests/packaging/test_wheel_matrix.py tests/qualification/test_phase8_platform.py tools/phase8_evidence.py tools/run_phase8_packaging.py tools/run_phase8_platform_gates.py</automated>
  </verify>
  <done>The current shipped/documented/qualified surface is TensorFlow-free, historical planning records remain explicitly superseded, and all retained handler/platform/package behavior plus direct quality gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| project metadata/lock -> installed wheel | A stale extra or transitive package can silently preserve the removed dependency. |
| authenticated manifest -> handler registry | A removed handler identity must fail typed and closed before payload bytes reach handler I/O. |
| package/platform evidence -> release aggregation | A stale compatibility row can falsely advertise removed support. |
| custom application handler -> registry | Removal must not narrow the generic user-extension contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-13-01 | Tampering | package metadata and lock graph | high | mitigate | Parse exact project groups and lock package names; regenerate normally and reject any restored TensorFlow group/requirement/node. |
| T-08-13-02 | Denial of Service | runtime import/handler discovery | high | mitigate | Delete lazy import state and handler registration, then prove package import and retained handler tests execute without TensorFlow. |
| T-08-13-03 | Tampering | stored payload contract | high | mitigate | Assert `tensorflow_tensor` resolves only to the existing typed unsupported-version path before handler I/O, with no fallback reader or migration. |
| T-08-13-04 | Spoofing | packaging/platform evidence | high | mitigate | Use exact final group/profile inventories and adversarially reject a forged removed entry or compatibility status. |
| T-08-13-05 | Repudiation | completed Phase 8 history | medium | mitigate | Preserve completed summaries and append explicit D-23 supersession; exclude `.planning` only from current-surface absence scans. |
| T-08-13-06 | Elevation of Privilege | custom handler seam | medium | mitigate | Keep the generic registry contract and prove a test-defined handler registers/resolves without privileged in-tree identity. |
</threat_model>

<verification>
- The focused removal suite proves all seven requested absence classes: optional group, public symbol, config field, registry identity, docs claim, lock package, and Phase 8 matrix/evidence row.
- Existing versioned manifest tests prove unknown stored `tensorflow_tensor` fails typed before handler I/O, with no compatibility read or migration.
- Existing custom-handler, NumPy, Blosc2, dataframe/Parquet, base-wheel, and platform-role tests remain green.
- Direct Ruff lint/format passes on every Python file changed by this plan; Plan 08-05 subsequently runs the unchanged broader D-16 critical-scope gate.
- No lifecycle, cache-policy, authority, participant, reconciliation, or digest-semantic file is modified.
</verification>

<success_criteria>
- Cacheness neither ships nor advertises native TensorFlow support.
- Final package/platform evidence has no TensorFlow-specific exception, row, state, or claim.
- Generic custom-handler extension and retained NumPy/Blosc2/Parquet paths remain functional.
- Unsupported stored TensorFlow identities fail through the existing versioned handler contract without reading payload bytes or mutating stored state.
- The Phase 8 dependency graph runs this removal before Plans 08-05 and 08-09 through 08-12.
</success_criteria>

<source_coverage>
- GOAL: Release evidence describes only the actual final shipped support surface.
- REQ: QUAL-01, QUAL-02, QUAL-03, QUAL-04.
- RESEARCH: D-23 supersession addendum, clean-wheel literal inventories, versioned unsupported handler boundary, and public custom-handler seam.
- CONTEXT: D-01, D-02, D-03, D-04, D-07, D-08, D-16, D-17, D-23.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-13-SUMMARY.md` when done.
</output>
