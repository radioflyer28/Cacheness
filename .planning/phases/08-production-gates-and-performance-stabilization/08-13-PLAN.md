---
phase: 08-production-gates-and-performance-stabilization
plan: 13
type: tdd
wave: 6
depends_on:
  - 08-10
files_modified:
  - benchmarks/phase8_benchmarks.py
  - tests/performance/test_phase8_benchmarks.py
  - tools/verify_phase8_contracts.py
  - tests/test_phase8_contract_verifier.py
autonomous: true
gap_closure: true
requirements:
  - QUAL-06
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The controlled-runner preflight accepts only the exact requested clean detached commit on the fixed cacheness-perf-linux-x64 Linux x86-64 identity (D-10, D-20)."
    - "The preflight is read-only: it never measures workloads, writes an output or baseline, or mutates repository state (D-03, D-20)."
    - "Success emits one bounded allow-listed JSON record, while macOS, attached/dirty/wrong revisions, and uncontrolled labels fail truthfully without exposing environment or repository details (D-20, D-22)."
    - "The fixed Phase 8 verifier explicitly binds Plan 08-13, its threat nodes, and its preflight regression selector rather than discovering them dynamically."
  artifacts:
    - path: "benchmarks/phase8_benchmarks.py"
      provides: "Read-only controlled-runner preflight CLI contract"
      contains: "--preflight-runner"
    - path: "tests/performance/test_phase8_benchmarks.py"
      provides: "Success, non-mutation, sanitization, and fail-closed preflight regressions"
    - path: "tools/verify_phase8_contracts.py"
      provides: "Literal Plan 08-13 and threat-to-selector bindings"
    - path: "tests/test_phase8_contract_verifier.py"
      provides: "Closed-inventory regression for the additive gap plan"
  key_links:
    - from: ".planning/phases/08-production-gates-and-performance-stabilization/08-11-PLAN.md"
      to: "benchmarks/phase8_benchmarks.py"
      via: "the exact --preflight-runner --expect-label ... --revision ... command"
      pattern: "preflight-runner|expect-label|revision"
    - from: "tools/verify_phase8_contracts.py"
      to: "tests/performance/test_phase8_benchmarks.py"
      via: "literal Plan 08-13 threat selector"
      pattern: "T-08-13|test_preflight_runner"
---

<objective>
Close the missing controlled-runner CLI contract that blocks Plan 08-11 before any baseline capture begins.

Purpose: Make the external runner checkpoint machine-verifiable and non-mutating, so QUAL-06 can distinguish an eligible exact-commit runner from macOS or an uncontrolled/dirty/attached checkout without producing performance evidence prematurely.
Output: A tested read-only preflight command and corresponding fixed-verifier bindings.
</objective>

<scope_guard>
This gap plan changes only benchmark preflight validation, its tests, and the fixed Phase 8 inventory that must acknowledge the additive plan. It does not run workloads, write or recalibrate a baseline, change performance envelopes, change runtime deadlines, touch storage/lifecycle/concurrency code, or strengthen ADR 0001 claims. TensorFlow work is deferred to SEED-005 and is outside this plan.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-11-PLAN.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-10-SUMMARY.md
@benchmarks/phase8_benchmarks.py
@tests/performance/test_phase8_benchmarks.py
@tools/verify_phase8_contracts.py
@tests/test_phase8_contract_verifier.py
</context>

<feature>
  <name>Read-only exact-commit controlled-runner preflight</name>
  <files>benchmarks/phase8_benchmarks.py, tests/performance/test_phase8_benchmarks.py, tools/verify_phase8_contracts.py, tests/test_phase8_contract_verifier.py</files>
  <behavior>
    - RED: Add focused CLI tests proving `--preflight-runner --expect-label cacheness-perf-linux-x64 --revision &lt;lowercase-40-hex-sha&gt;` returns success only when the actual runner identity is the fixed label, the platform is Linux, the machine is x86_64/amd64, `HEAD` equals the requested revision, the checkout is detached, and the complete worktree/index/untracked status is clean (D-10, D-20).
    - RED: Assert the successful stdout value is exactly one bounded JSON object with an explicit preflight schema/status, requested revision, fixed runner identity, normalized platform/machine, and clean-detached state. Reject extra keys and prove no environment mapping, path, credential, raw command output, hostname, username, or timing sample is emitted (D-20, D-22).
    - RED: Snapshot repository-visible files and monkeypatch every measurement/baseline/output-writing seam to fail if called; assert both successful and rejected preflights create or modify no file and never enter workload measurement, output writing, capture, recalibration, or verification (D-03, D-20).
    - RED: Parametrize macOS, non-x86 Linux, missing/wrong/uncontrolled labels, malformed or mismatched revisions, attached HEAD, dirty index/worktree, and untracked files. Each case must return a nonzero CLI result with a stable sanitized reason and no success record (D-20, D-22).
    - GREEN: Add the three missing argparse options. Treat preflight as its own mutually exclusive mode, require both expected label and revision in that mode, validate the requested revision before invoking Git, compare the actual identity from the existing `--runner-identity`/`CACHENESS_PERF_RUNNER` seam to the fixed expected label, and reuse the existing controlled Linux/x86-64 platform guard.
    - GREEN: Add bounded Git checks with explicit repository root and timeouts for exact `HEAD`, detached state, and an empty porcelain status including untracked files. Translate command/validation failures into stable `BaselineVerificationError`/CLI diagnostics without relaying subprocess stderr or repository details. Return after printing the allow-listed record, before constructing a measurement document or touching any output/baseline path.
    - GREEN: Extend the literal verifier and its adversarial test to include `08-13-PLAN.md`, T-08-13-01 through T-08-13-03, and the exact preflight regression selector. Preserve closed inventories; do not replace them with plan or test discovery.
    - REFACTOR: Keep source-identity, platform, serialization, and CLI-dispatch helpers focused and directly testable. Run Ruff only on the four changed Python files; make no unrelated formatting rewrite.
  </behavior>
  <implementation>
Implement the behavior in the existing benchmark harness without adding dependencies. Preserve the current capture/recalibrate/verify and diagnostic semantics. Use the existing `CONTROLLED_RUNNER_IDENTITY`, `environment_fingerprint()`, `BaselineVerificationError`, and standalone `main()` boundary. The preflight record is eligibility evidence only, not controlled-performance evidence and not a baseline. Update the fixed verifier solely because its reviewed manifest intentionally enumerates every Phase 8 plan and threat.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| runner/process environment -> preflight CLI | The claimed runner label and host platform are untrusted until compared to the fixed controlled identity. |
| Git checkout -> release checkpoint | Repository state must prove the requested exact clean detached commit without mutation. |
| local diagnostics -> emitted evidence | Only bounded allow-listed eligibility fields may cross into the checkpoint artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-13-01 | Spoofing | runner identity and platform | high | mitigate | Require the fixed expected label, matching actual runner identity, Linux, and x86_64/amd64 before emitting success. |
| T-08-13-02 | Tampering | exact release revision and repository state | high | mitigate | Require lowercase full SHA equality, detached HEAD, and empty tracked/index/untracked porcelain status using bounded read-only Git commands. |
| T-08-13-03 | Information Disclosure | preflight evidence and failures | medium | mitigate | Emit one exact allow-list and stable reasons; never include environment dumps, subprocess stderr, paths, identities, credentials, or timing data. |
</threat_model>

<verification>
- RED must fail because argparse does not yet expose the documented preflight mode.
- GREEN: `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/performance/test_phase8_benchmarks.py -k preflight_runner -x` passes.
- Fixed inventory: `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase8_contract_verifier.py -k fixed_manifest -x` passes.
- Regression and style: `uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/performance/test_phase8_benchmarks.py tests/test_phase8_contract_verifier.py -x` and `uv run --isolated --group dev --frozen ruff check benchmarks/phase8_benchmarks.py tests/performance/test_phase8_benchmarks.py tools/verify_phase8_contracts.py tests/test_phase8_contract_verifier.py` pass.
- On the current macOS host, invoking the documented preflight command with any revision returns nonzero, writes no baseline/output, and reports the controlled Linux boundary truthfully.
</verification>

<success_criteria>
- Plan 08-11's exact runner preflight command exists and is safe to retry before baseline capture.
- Eligibility evidence proves the fixed label, Linux x86-64, exact clean detached revision, and nothing more.
- Every rejected environment/repository state fails closed without mutation or sensitive/unbounded output.
- The fixed Phase 8 verifier remains literal and passes after binding the additive gap plan and threats.
- No performance threshold, storage lifecycle, concurrency mechanism, TensorFlow surface, or ADR 0001 guarantee changes.
</success_criteria>

<source_coverage>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Reproducible release evidence across supported environments | 08-13 | COVERED | Machine-verifiable runner eligibility precedes controlled evidence capture. |
| REQ | QUAL-06 | Checked distributions and reviewed budgets on named environments | 08-13 | COVERED | The named environment must prove eligibility without writing the baseline. |
| RESEARCH | R-08 | Controlled Linux relative performance envelope | 08-13 | COVERED | Closes the missing runner preflight CLI seam only. |
| CONTEXT | D-03 | Separate performance from correctness/progress claims | 08-13 | COVERED | Preflight emits eligibility, not performance evidence. |
| CONTEXT | D-10 | Qualify the exact clean release commit | 08-13 | COVERED | Exact detached clean revision is mandatory. |
| CONTEXT | D-20 | Named controlled Linux runner and reviewed envelope | 08-13 | COVERED | Fixed label/platform/machine validation precedes capture. |
| CONTEXT | D-22 | Controlled local performance blocks; other timing is diagnostic | 08-13 | COVERED | macOS/uncontrolled runners fail as nonqualifying. |
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-13-SUMMARY.md` when done.
</output>
