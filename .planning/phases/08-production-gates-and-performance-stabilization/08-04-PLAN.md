---
phase: 08-production-gates-and-performance-stabilization
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tests/test_phase8_lifecycle_coverage.py
  - tests/test_phase8_cache_policy_coverage.py
autonomous: true
requirements:
  - QUAL-04
  - QUAL-05
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Named lifecycle tests cover integrity-before-deserialization, ambiguous publication, cleanup debt, bounded clear, and typed progress outcomes (D-03, D-04, D-15)."
    - "Named cache-policy tests cover typed lookup causes, invalidation accounting, continuation restart, and immutable maintenance results (D-02, D-15)."
    - "Coverage growth comes from meaningful fault and boundary behavior, not happy-path execution solely to raise percentages (D-14, D-15)."
  artifacts:
    - path: "tests/test_phase8_lifecycle_coverage.py"
      provides: "Named critical lifecycle and authority boundary selectors"
    - path: "tests/test_phase8_cache_policy_coverage.py"
      provides: "Named cache policy, invalidation, and observer selectors"
  key_links:
    - from: "tests/test_phase8_lifecycle_coverage.py"
      to: "src/cacheness/storage/lifecycle.py"
      via: "fault-injected public lifecycle behavior and exact authority/participant assertions"
      pattern: "AuthorityLifecycleEngine|BlobStore"
    - from: "tests/test_phase8_cache_policy_coverage.py"
      to: "src/cacheness/core.py"
      via: "public UnifiedCache policy outcomes over an injected BlobStore"
      pattern: "UnifiedCache"
---

<objective>
Close named high-risk lifecycle and cache-policy branch gaps before selecting numerical coverage floors.

Purpose: Satisfy D-14/D-15 with executable behavior contracts whose meaning remains stronger than a percentage.
Output: Focused lifecycle and cache-policy coverage suites.
</objective>

<scope_guard>
This plan adds tests only. A failing case is evidence to classify against ADR 0001, not authorization to add lifecycle coordination or change topology guarantees.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@src/cacheness/storage/lifecycle.py
@src/cacheness/storage/blob_store.py
@src/cacheness/cache_policy.py
@src/cacheness/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add named lifecycle integrity and recovery branch contracts</name>
  <files>tests/test_phase8_lifecycle_coverage.py</files>
  <read_first>
    - src/cacheness/storage/lifecycle.py: publication, verification, promotion, read, clear, and reconciliation branches
    - tests/test_blob_store_reconciliation.py: bounded debt/recovery fixtures
    - tests/contracts/test_obstore_generation_io.py: lost-response/collision/absence taxonomy
  </read_first>
  <behavior>
    - "Authority and canonical SHA-256/size verification always precede handler deserialization."
    - "Ambiguous publication settles only from the exact locator and matching digest/size; mismatch fails closed."
    - "Post-promotion cleanup failure persists exact cleanup debt and deterministic reconciliation retires only proven exact work."
    - "Clear remains bounded and preserves changed generations; contention ends only as success, exact conflict, or typed retryable timeout."
  </behavior>
  <action>Add focused fault-injection tests using existing authorities, participants, handlers, and `LifecycleLimits`. Name exact selectors for integrity-before-handler-read, ambiguous exact-locator settlement, post-commit cleanup debt, bounded clear continuation, and ADR progress outcomes per D-03/D-04/D-15. Initialize shared-worker fixtures before starting contenders. Assert safety and recovery independently from progress; do not require every contender to succeed and do not add production fixes inside this task.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase8_lifecycle_coverage.py -x</automated>
  </verify>
  <done>Each named lifecycle risk has a deterministic selector that proves its correct safety/recovery/progress class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add named cache-policy and invalidation branch contracts</name>
  <files>tests/test_phase8_cache_policy_coverage.py</files>
  <read_first>
    - src/cacheness/cache_policy.py: validation, maintenance, continuation, and immutable result models
    - src/cacheness/core.py: lookup classification, statistics, invalidation, clear, and stale-cursor restart
    - tests/test_phase6_statistics.py: storage-free observer contract
    - tests/test_phase6_removal_contract.py: exact delete and cursor behavior
  </read_first>
  <behavior>
    - "UnifiedCache preserves declared BlobStore causes and maps only documented lookup outcomes."
    - "Invalidation and clear accounting use exact BlobStore deletion results and preserve conflicts/retryability."
    - "Stale continuation restarts only through the documented bounded policy path."
    - "Statistics remain immutable and storage-free; malformed maintenance results fail at their boundary."
  </behavior>
  <action>Add public-policy tests over injected stores for typed lookup failures, invalidation result accounting, stale-cursor restart, immutable maintenance validation, and zero-storage statistics per D-02/D-15. Assert exact store calls and returned domain values, not internal line execution. Preserve `UnifiedCache` as policy-only and avoid any cache-side lifecycle retry, lock, or repair behavior.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase8_cache_policy_coverage.py -x</automated>
  </verify>
  <done>Named cache-policy branches are executable contracts over the existing BlobStore lifecycle rather than percentage-only coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authority/participant -> handler | Corrupt or unauthenticated bytes must not reach deserialization. |
| BlobStore result -> UnifiedCache policy | Typed storage causes must not be erased or relabeled. |
| test scheduler -> guarantee claim | One observed schedule must not strengthen progress guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04-01 | Tampering | handler read boundary | high | mitigate | Named tests fail if handler execution precedes authority authentication and SHA-256/size verification. |
| T-08-04-02 | Repudiation | cleanup debt | high | mitigate | Assert exact persisted debt identity and deterministic retirement evidence. |
| T-08-04-03 | Denial of Service | contention tests | medium | mitigate | Initialize first, bound operations, and accept only declared typed progress outcomes. |
| T-08-04-04 | Spoofing | cache miss classification | medium | mitigate | Assert original typed causes and exact public outcome mapping. |
</threat_model>

<verification>
- Both focused suites pass independently and with the inherited Phase 07.1 fixed gate.
- Tests assert named behavior and ordering, not coverage-line counts.
- No production file changes occur.
</verification>

<success_criteria>
- High-risk lifecycle and cache-policy gaps are closed before baselines are recorded.
- QUAL-04 finite regressions remain intact under ADR 0001 vocabulary.
- QUAL-05 can ratchet meaningful coverage in the next plan.
</success_criteria>

<source_coverage>
- GOAL: Release evidence covers failures and commit boundaries, not just happy paths.
- REQ: QUAL-04 and QUAL-05.
- RESEARCH: Named critical lifecycle/cache gaps and measure-close-ratchet order.
- CONTEXT: D-02, D-03, D-04, D-14, D-15.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-04-SUMMARY.md` when done.
</output>
