---
phase: 08-production-gates-and-performance-stabilization
plan: 05
type: execute
wave: 3
depends_on:
  - 08-04
files_modified:
  - pyproject.toml
  - tools/verify_phase8_coverage.py
  - tests/test_phase8_coverage_gate.py
  - tests/qualification/phase8_coverage_baseline.json
autonomous: true
requirements:
  - QUAL-03
  - QUAL-05
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Coverage is measured with branches enabled after named gaps are closed; repository-total and critical-scope statement/branch rates cannot decrease (D-14, D-15)."
    - "The baseline is an explicit measured artifact and ordinary verification never rewrites it (D-14, D-15)."
    - "Changed Python files plus complete lifecycle, cache-policy, qualification, and packaging scopes pass direct Ruff lint and format checks without a custom debt ledger (D-16, D-17)."
  artifacts:
    - path: "tools/verify_phase8_coverage.py"
      provides: "Strict total/critical statement-and-branch ratchet verifier"
    - path: "tests/qualification/phase8_coverage_baseline.json"
      provides: "Post-gap measured coverage floors and named-selector inventory"
    - path: "tests/test_phase8_coverage_gate.py"
      provides: "Malformed, regressed, omitted, and non-mutating baseline tests"
  key_links:
    - from: "pyproject.toml"
      to: "tools/verify_phase8_coverage.py"
      via: "branch-aware Coverage.py JSON output"
      pattern: "branch"
    - from: "tests/qualification/phase8_coverage_baseline.json"
      to: "tests/test_phase8_lifecycle_coverage.py"
      via: "literal named-selector list required alongside percentage floors"
      pattern: "test_phase8_lifecycle_coverage"
---

<objective>
Measure the post-gap branch-aware baseline and enforce direct coverage, lint, and formatting ratchets.

Purpose: Close QUAL-05 using measured non-regression floors plus named contracts, never arbitrary percentages or a bespoke lint-debt fingerprint.
Output: Coverage configuration, strict verifier/self-tests, and checked-in measured baseline.
</objective>

<scope_guard>
This plan may change quality configuration and evidence only. Baseline capture must follow measurement and named-gap closure; ordinary verification is read-only and cannot auto-accept a regression.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md
@pyproject.toml
@tests/test_phase8_lifecycle_coverage.py
@tests/test_phase8_cache_policy_coverage.py
@tools/verify_phase071_contracts.py
</context>

<interfaces>
The critical aggregate contains the explicit release modules: `cache_policy.py`, `core.py`, `storage/blob_store.py`, `storage/lifecycle.py`, `storage/lifecycle_authority.py`, memory/SQLite/PostgreSQL authority implementations, reconciliation, composition, guarded handler I/O, obstore generation I/O, and transport evidence. The baseline stores integer covered/total statement and branch counts plus derived rates, exact source revision, environment identity, and literal named selectors.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the strict branch-aware coverage ratchet</name>
  <files>pyproject.toml, tools/verify_phase8_coverage.py, tests/test_phase8_coverage_gate.py</files>
  <read_first>
    - pyproject.toml: current Coverage.py source/omit/exclusion and Ruff settings
    - tests/test_phase1_quality_gates.py: strict JSON parsing and subprocess result patterns
    - tools/verify_phase071_contracts.py: literal selector validation
  </read_first>
  <behavior>
    - "Coverage JSON missing branch fields, critical modules, exact source files, or named selectors is rejected."
    - "Baseline capture refuses to run unless the literal PostgreSQL selector families `postgresql_error_classification`, `postgresql_replay`, `postgresql_pagination`, and `postgresql_transaction_rollback` collect without skip/deselection and pass."
    - "Either repository-total or critical-scope statement/branch regression fails, even if the displayed combined percentage rises."
    - "Ordinary verify mode never writes or changes the baseline."
    - "Ruff scope is changed Python files union a fixed critical inventory; missing critical paths or unsafe argv construction fails."
  </behavior>
  <action>Enable Coverage.py branch measurement in `pyproject.toml`. Implement strict typed/range validation of Coverage.py JSON, compute repository and explicit critical aggregates from integer counts, validate literal named selectors by AST, and compare all four rates/counts to a checked baseline. Before capture, require the exact Phase 8 PostgreSQL selector families `postgresql_error_classification`, `postgresql_replay`, `postgresql_pagination`, and `postgresql_transaction_rollback` plus the remaining lifecycle/cache-policy selector inventory to collect without skip/deselection and pass; missing or failing selectors block baseline writes. Provide a separately named capture command that requires an explicit justification and atomic replacement; verify mode must be non-mutating. Implement direct Ruff lint/format argv calculation from safely parsed merge-base paths union fixed critical scopes per D-16/D-17; do not persist finding fingerprints or tolerate missing inputs.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase8_coverage_gate.py -x</automated>
  </verify>
  <done>The quality verifier rejects malformed or regressed statement/branch evidence and enforces direct Ruff scopes without a debt ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Measure and record the post-gap coverage floors</name>
  <files>tests/qualification/phase8_coverage_baseline.json</files>
  <read_first>
    - .planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md: pre-gap measured totals and critical rates
    - tests/test_phase8_lifecycle_coverage.py: named lifecycle selectors from Plan 04
    - tests/test_phase8_cache_policy_coverage.py: named cache-policy selectors from Plan 04
  </read_first>
  <action>Run the deterministic non-live suite with statement and branch JSON/XML reporting after the Plan 04 selectors pass. Capture the actual repository-total and critical-module integer counts/rates, environment, command, exact source revision, and reviewed selector inventory into the baseline using the explicit capture mode and a justification naming Phase 8 initial ratchet establishment. Re-run verify mode and direct Ruff checks. Per D-14/D-15, keep the measured floors even if they are lower than desired; raise them only through additional meaningful tests, never by excluding files or deleting selectors.</action>
  <verify>
    <automated>uv run python tools/verify_phase8_coverage.py --report build/phase8/coverage.json --baseline tests/qualification/phase8_coverage_baseline.json</automated>
  </verify>
  <done>The checked baseline equals the measured post-gap result, contains all named selectors, and passes read-only non-regression verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Coverage.py JSON -> release gate | Malformed or selectively omitted metrics must not pass. |
| baseline capture -> repository | A normal CI run must not rewrite regression thresholds. |
| git diff -> Ruff argv | Hostile filenames or an incomplete diff must not escape fixed critical scopes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-05-01 | Tampering | coverage JSON/baseline | high | mitigate | Validate exact schema, integer counts, module inventory, derived rates, revision, and canonical JSON. |
| T-08-05-02 | Repudiation | baseline changes | high | mitigate | Separate capture from verify, require justification, and keep normal verification read-only. |
| T-08-05-03 | Spoofing | named behavior coverage | high | mitigate | Require literal AST-validated selectors in addition to numerical floors. |
| T-08-05-04 | Elevation of Privilege | Ruff path arguments | medium | mitigate | Build subprocess argv as a list from bounded repository-relative paths and fixed scopes. |
</threat_model>

<verification>
- Coverage gate self-tests reject every missing/regressed/forged dimension.
- The real post-gap deterministic report verifies against the checked baseline.
- Direct Ruff lint and format checks pass for changed plus critical Python scopes.
</verification>

<success_criteria>
- QUAL-05 has statement and branch non-regression gates for total and critical scopes.
- Named behavior cannot be removed while a percentage remains green.
- Baselines and lint policy remain reviewable, direct, and non-mutating by default.
</success_criteria>

<source_coverage>
- GOAL: Reproducible release evidence includes measured code-quality gates.
- REQ: QUAL-03 and QUAL-05.
- RESEARCH: Measured pre-gap baseline, named gaps, direct Ruff scopes, and measure-close-ratchet sequence.
- CONTEXT: D-14, D-15, D-16, D-17.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-05-SUMMARY.md` when done.
</output>
