---
phase: 08-production-gates-and-performance-stabilization
plan: 16
type: execute
wave: 10
depends_on:
  - 08-17
files_modified:
  - tools/verify_phase8_contracts.py
  - tools/verify_phase8_release.py
  - tests/test_phase8_contract_verifier.py
  - tests/qualification/test_phase8_release.py
  - docs/RELEASE_QUALIFICATION.md
  - .planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json
  - .planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-07
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One fixed command proves the exact-source deterministic integrity/recovery suite, a source-free base-wheel import and public round trip, measured coverage plus Ruff, and structural memory/backend-call bounds before reporting LOCAL_READY (D-03, D-07, D-14, D-15, D-16, D-21, D-24)."
    - "BACK-05 is exactly DEFERRED/NOT_QUALIFIED to SEED-007, QUAL-06 is exactly DEFERRED/NOT_QUALIFIED to SEED-006, and publication is exactly DEFERRED/NOT_PUBLISHED to SEED-007; none is represented as PASS (D-23, D-24)."
    - "The local-readiness result identifies the actual current host but makes no Linux-matrix, Windows, live PostgreSQL/Amazon-S3, controlled-performance, or immutable-release claim (D-05, D-06, D-09-D-13, D-20, D-22, D-24)."
    - "The existing protected-live collection, strict release aggregation, draft approval, and immutable-publication commands remain intact for SEED-007; local closure adds no alternate lifecycle, remote evidence, or release mutation path (D-01, D-02, D-04, D-24)."
  artifacts:
    - path: tools/verify_phase8_contracts.py
      provides: "Separate exact local-readiness mode plus canonical/superseded plan, decision, requirement, and threat inventory"
    - path: tools/verify_phase8_release.py
      provides: "Bounded local-readiness schema/validator with explicit deferred service/performance/publication records"
    - path: .planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json
      provides: "Exact-source machine-readable local readiness and nonclaim evidence"
    - path: docs/RELEASE_QUALIFICATION.md
      provides: "Public local-use boundary and future qualification requirements"
  key_links:
    - from: tools/verify_phase8_contracts.py
      to: tools/run_phase8_local_gates.py
      via: "fixed core gate plus source-free base-wheel probe, never caller-selected tests or external workflow dispatch"
      pattern: "local-ready|core|run_base_probe"
    - from: tools/verify_phase8_contracts.py
      to: tools/verify_phase8_release.py
      via: "canonical local-readiness record construction and validation"
      pattern: "LOCAL_READY|DEFERRED|NOT_PUBLISHED"
    - from: .planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json
      to: .planning/seeds/SEED-006-qualify-controlled-linux-performance.md
      via: "exact QUAL-06 deferred requirement identity"
      pattern: "QUAL-06|SEED-006|NOT_QUALIFIED"
    - from: .planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json
      to: .planning/seeds/SEED-007-qualify-real-postgresql-s3-and-publish-release.md
      via: "exact BACK-05 and immutable-publication deferred identities"
      pattern: "BACK-05|SEED-007|NOT_QUALIFIED|NOT_PUBLISHED"
---

<objective>
Close Phase 8 on deterministic local readiness without converting unavailable external qualification into a release claim.

Purpose: Implement the user-approved D-24 cutover while preserving ADR 0001, the original BACK-05/immutable-publication standard, and all completed Phase 8 qualification machinery for SEED-006/SEED-007.
Output: A tested local-readiness mode, exact machine-readable local evidence/nonclaims, and final qualification documentation/validation.
</objective>

<scope_guard>
This plan changes qualification tooling, its tests, and documentation only. It must not edit `src/cacheness`, storage lifecycle/concurrency behavior, handler behavior, TensorFlow support, benchmark thresholds, workflow files, live runner behavior, or any GitHub release. It must not run PostgreSQL/AWS qualification, dispatch GitHub Actions, create a draft/tag, or publish a release. D-24 narrows only the current milestone claim; it does not weaken future SEED-007 evidence.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-15-SUMMARY.md
@.planning/seeds/SEED-006-qualify-controlled-linux-performance.md
@.planning/seeds/SEED-007-qualify-real-postgresql-s3-and-publish-release.md
@tools/verify_phase8_contracts.py
@tools/verify_phase8_release.py
@tools/run_phase8_local_gates.py
@tools/run_phase8_packaging.py
</context>

<interfaces>
Preserve these existing entry points and semantics:

- `tools/verify_phase8_contracts.py`: `validate_fixed_manifest()`, `local_gate_commands()`, `render_external_statuses()`, `external_status_is_blocking()`, `_run_local()`, and `main()`.
- `tools/run_phase8_local_gates.py`: `run_core(output_directory=...)` produces deterministic, coverage, and structural envelopes; no live or timing evidence.
- `tools/run_phase8_packaging.py`: `build_wheel(...)` and `run_base_probe(...)` perform the source-free base installation/public round trip independently of the optional-group aggregate.
- `tools/verify_phase8_release.py`: existing `collect`, `aggregate`, `publication-preflight`, `prepare-draft`, `publish-and-verify`, and `verify-published` commands remain the strict SEED-007 release path.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add a separate exact-source local-readiness path and bind D-24</name>
  <reversibility rating="reversible">The local-readiness mode is additive; SEED-007 can later run the unchanged stricter release path without a data or API migration.</reversibility>
  <files>tools/verify_phase8_contracts.py, tools/verify_phase8_release.py, tests/test_phase8_contract_verifier.py, tests/qualification/test_phase8_release.py, .planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md, docs/RELEASE_QUALIFICATION.md</files>
  <behavior>
    - "The fixed inventory contains canonical Plans 01-10 and 13-16, separately requires Plans 11/12 to declare status superseded with SEED-007 reasons, covers D-01 through D-24, and excludes BACK-05/QUAL-06 from current requirements while retaining exact deferred records."
    - "A local-readiness manifest is LOCAL_READY only when deterministic, coverage/Ruff, structural, and source-free base-wheel evidence all pass for one exact revision/source digest."
    - "The manifest records BACK-05 as DEFERRED/NOT_QUALIFIED to SEED-007, QUAL-06 as DEFERRED/NOT_QUALIFIED to SEED-006, publication as DEFERRED/NOT_PUBLISHED to SEED-007, and Linux-matrix/Windows/live/performance/release claims as absent."
    - "Missing, malformed, stale, dirty, skipped, wrong-class, duplicate, or contradictory local evidence fails; local/mock/preflight evidence cannot fill live or publication fields."
    - "The original release aggregate still rejects missing live QUALIFIED/CLEAN evidence, and every publication command retains its exact remote preconditions and mutation boundary."
    - "The local-ready command performs no live-service or GitHub operation and writes only the requested bounded local-readiness file atomically; ordinary locked dependency resolution is not represented as offline execution."
  </behavior>
  <action>Write the four named adversarial tests first. In `tools/verify_phase8_contracts.py`, split the literal inventory into canonical plans (01-10, 13-16) and explicitly superseded plans (11/12); validate the latter's `status`, `superseded_by`, D-24 reason, and SEED-007 identity instead of deleting them or counting their threats as current acceptance. Add D-24 and T-08-16-01 through T-08-16-05 to closed selector maps. Keep QUAL-01/02/03/04/05/07 current; represent QUAL-06 and BACK-05 in separate exact deferred records. Add `--local-ready --output PATH` as a mode distinct from legacy `--all`: after the static/self-test audit, run only the existing fixed `run_core` local path and the real source-free base-wheel builder/probe in private temporary directories, require one unchanged clean source identity, and delegate canonical record creation/validation to the release evidence module. Record actual OS/Python/machine only as observed local scope, never as a platform-matrix pass. Exit zero only for `LOCAL_READY`; keep legacy `--all` release-boundary behavior available for SEED-007.

In `tools/verify_phase8_release.py`, add a bounded `cacheness-phase8-local-readiness-v1` record validator/builder and exact D-23/D-24 nonclaim constants. Admit only deterministic, coverage, structural, and base-wheel local evidence for one revision/source digest. Require the exact SEED-006 and SEED-007 records and reject any live-service, controlled-performance, publication, Linux-equivalence, Windows, scheduled, mock, preflight, draft, tag, run-ID, or remote asset as local proof. Do not loosen or redirect `CURRENT_RELEASE_EVIDENCE_CLASSES`, `aggregate_collection`, `_closed_aggregate`, or any release/publication command; regression tests must prove the future real-service path still requires live `QUALIFIED`/`CLEAN` evidence and exact immutable publication. Do not add a second source digest, status mirror, workflow, dependency, lock, retry loop, lifecycle authority, or production code change.

Before Task 1 is committed, update `08-VALIDATION.md` and `docs/RELEASE_QUALIFICATION.md` to the final executable local-readiness schema and D-24 boundary. State what the local result proves, which optional/platform scopes were not proved on this host, and exactly what SEED-006/SEED-007 must still do. Keep both documents inside the reviewed-source identity used by the verifier, and run the focused verifier/release tests after these edits so marker and wording contracts cannot drift. Task 1's commit must contain the tools, tests, and both reviewed-source documents together; Task 2 may not edit any reviewed source.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase8_contract_verifier.py tests/qualification/test_phase8_release.py -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tools/verify_phase8_contracts.py tools/verify_phase8_release.py tests/test_phase8_contract_verifier.py tests/qualification/test_phase8_release.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff format --check tools/verify_phase8_contracts.py tools/verify_phase8_release.py tests/test_phase8_contract_verifier.py tests/qualification/test_phase8_release.py</automated>
  </verify>
  <done>The fixed verifier exposes a separately tested, bounded local-readiness mode with exact D-23/D-24 nonclaims; the final validation/public boundary documents are committed in the same reviewed-source identity, while the preserved release path still fails closed without real live and immutable-publication evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the exact local-readiness record from committed reviewed source</name>
  <precondition>Task 1 is committed; Git reports no changes under the fixed reviewed source inventory before the local-readiness command starts.</precondition>
  <files>.planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json</files>
  <action>Run the new fixed local-readiness command once from committed Task 1 HEAD. It must exercise the inherited finite integrity/recovery contracts, measured coverage plus direct Ruff ratchet, structural call/RSS bounds, and a real wheel built and imported outside the source checkout with generic-object and NumPy public round trips. Write the bounded canonical result to `08-LOCAL-READINESS.json`; require `LOCAL_READY`, one exact revision/source digest, and the exact three nonclaims: QUAL-06/SEED-006 `DEFERRED` plus `NOT_QUALIFIED`, BACK-05/SEED-007 `DEFERRED` plus `NOT_QUALIFIED`, and publication/SEED-007 `DEFERRED` plus `NOT_PUBLISHED`. Record the actual host only as the environment that ran this local evidence; explicitly deny Linux-matrix, Windows, live-service, controlled-performance, and immutable-publication qualification.

Do not edit tools, tests, `08-VALIDATION.md`, `docs/RELEASE_QUALIFICATION.md`, or any other reviewed source after evidence generation begins. Validate the generated JSON read-only and commit only that evidence artifact. Preserve the frozen live runner, workflows, same-SHA collector/aggregate, exact cleanup, release controller, TensorFlow code, migration fixtures, and ADR 0001 nonclaims. Do not create a tag, draft, workflow run, remote asset, or release.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase8_contracts.py --local-ready --output .planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json &amp;&amp; uv run --isolated --all-extras --group dev --frozen python -c "import json; from pathlib import Path; d=json.loads(Path('.planning/phases/08-production-gates-and-performance-stabilization/08-LOCAL-READINESS.json').read_text()); by_id={item['requirement']: item for item in d['deferred_requirements']}; assert d['status']=='LOCAL_READY'; assert by_id['BACK-05']['qualification']=='NOT_QUALIFIED'; assert by_id['QUAL-06']['qualification']=='NOT_QUALIFIED'; assert d['publication']['status']=='NOT_PUBLISHED'" &amp;&amp; uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase8_contract_verifier.py tests/qualification/test_phase8_release.py -x</automated>
  </verify>
  <done>The committed exact-source local-readiness record passes every local gate and visibly preserves BACK-05, QUAL-06, remote platform, and publication nonclaims without any external mutation or substitute evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local test/tool output -> readiness record | Only fixed, bounded, exact-source deterministic/base-package/coverage/structural evidence may enter the local record. |
| local readiness -> remote/platform/release claims | A local pass must not authorize or imply another evidence class. |
| preserved release controller -> future SEED-007 | Current edits must not weaken exact-SHA live cleanup, approval, asset, digest, or immutable-state requirements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-16-01 | Spoofing | local source identity | high | mitigate | Require one clean 40-character revision and reviewed-source digest before/after every fixed child. |
| T-08-16-02 | Tampering | deferred status records | high | mitigate | Validate exact D-23/D-24 requirement, seed, status, and qualification/publication tuples; reject alternate keys or PASS aliases. |
| T-08-16-03 | Elevation of Privilege | local-to-remote claim boundary | high | mitigate | Local schema has no live, tag, draft, asset, workflow, Linux-equivalence, Windows, or publication authorization field. |
| T-08-16-04 | Information Disclosure | readiness artifact | medium | mitigate | Use a bounded allow-list containing digests, versions, host class, test identities, and statuses only; reject raw logs, paths outside the repo, credentials, DSNs, payloads, and environment values. |
| T-08-16-05 | Denial of Service | local gate orchestration | medium | mitigate | Use existing fixed commands, private temporary directories, hard timeouts, no live-service or GitHub dispatch, and immediate fail-closed child propagation. |
</threat_model>

<verification>
- The complete local-readiness command exits zero and writes a schema-valid `LOCAL_READY` record from one committed source identity.
- The real source-free base wheel round trip, deterministic integrity/recovery suite, branch coverage/Ruff ratchet, and structural bounds all pass.
- Fixed tests prove BACK-05, QUAL-06, publication, Linux-matrix, Windows, and controlled-performance states remain nonclaims.
- Existing live collection/release aggregation/publication tests still require real exact-SHA evidence and immutable remote state.
- No production, workflow, handler, lifecycle, TensorFlow, benchmark-baseline, dependency, or lockfile file changes.
</verification>

<acceptance_criteria>
- Phase 8 has one machine-readable local readiness result rather than a synthetic release result.
- Plans 08-11 and 08-12 remain visibly superseded/unexecuted and are not counted as completed evidence.
- BACK-05 and publication point to SEED-007; QUAL-06 points to SEED-006; all retain their nonpassing status.
- A user may rely on the tested local boundary without being told that an untested remote topology, platform, performance budget, or GitHub release is qualified.
</acceptance_criteria>

<source_coverage>
- GOAL: Reproducible local-readiness evidence without unproved external claims.
- REQ: QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-07.
- RESEARCH: Exact-source evidence classes, source-free base wheel, coverage/Ruff ratchet, structural formulas/RSS, preserved future live/publication machinery, and explicit non-substitution.
- CONTEXT: D-01 through D-24, with D-24 implementing the approved local-readiness cutover and preserving D-23/SEED-006 plus D-09-D-13/SEED-007 future standards.
</source_coverage>

<success_criteria>
- Deterministic local use is backed by one exact-source, fail-closed command and record.
- Remote live service, controlled performance, Windows/Linux matrix, and publication remain explicit nonclaims rather than omitted or promoted states.
- The future SEED-007 path remains stricter than local readiness and still requires real PostgreSQL/Amazon-S3 plus immutable publication.
- ADR 0001's one-authority, topology-specific guarantee vocabulary remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-16-SUMMARY.md` when done.
</output>
