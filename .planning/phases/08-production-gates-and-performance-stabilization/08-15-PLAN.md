---
phase: 08-production-gates-and-performance-stabilization
plan: 15
type: execute
wave: 8
depends_on:
  - 08-14
files_modified:
  - tools/run_phase8_qualification.py
  - tests/qualification/test_phase8_evidence.py
  - tools/verify_phase8_contracts.py
  - tests/test_phase8_contract_verifier.py
autonomous: true
gap_closure: true
requirements:
  - BACK-05
  - QUAL-03
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`tools/run_phase8_qualification.py --preflight` performs a configuration-only, no-side-effect check and returns success only when the protected PostgreSQL/Amazon-S3 prerequisites are locally well formed for one clean exact source identity (D-09, D-10, D-12)."
    - "The preflight never opens PostgreSQL, resolves or contacts AWS services, lists or mutates S3, creates a qualification namespace, runs pytest, invokes cleanup, or writes an evidence file; it cannot qualify BACK-05 (D-03, D-11, D-12)."
    - "Every CONFIGURED, UNAVAILABLE, and INVALID preflight result contains the literal JSON field `\"service_state\": \"NOT_RUN\"`; configuration readiness never implies credential, identity, IAM, or service proof (D-11, D-12)."
    - "Preflight output is canonical, bounded, and sanitized: it names missing fields and policy states but never emits the PostgreSQL DSN, bucket, region, manifest key, AWS credential material, endpoint values, or parser/provider diagnostics (D-11, D-12)."
    - "The fixed verifier binds Plan 08-15 and every T-08-15 threat to literal executable tests before Plan 08-11 selects the release candidate (D-10)."
  artifacts:
    - path: "tools/run_phase8_qualification.py"
      provides: "Configuration-only live qualification preflight whose CONFIGURED, UNAVAILABLE, and INVALID output schema always includes `\"service_state\": \"NOT_RUN\"`, plus the unchanged side-effecting qualification path"
    - path: "tests/qualification/test_phase8_evidence.py"
      provides: "Adversarial preflight purity, redaction, prerequisite, source-identity, and cleanup-policy contracts"
    - path: "tools/verify_phase8_contracts.py"
      provides: "Literal Plan 08-15 and threat-to-selector bindings"
    - path: "tests/test_phase8_contract_verifier.py"
      provides: "Closed-inventory tests for the additive gap plan"
  key_links:
    - from: "tools/run_phase8_qualification.py --preflight"
      to: "tools/run_phase8_qualification.py run_qualification"
      via: "shared pure configuration and source-identity validators, with separate effect boundaries"
      pattern: "preflight|run_qualification"
    - from: "tools/run_phase8_qualification.py"
      to: "tests/qualification/conftest.py"
      via: "read-only validation of the reviewed owner-marker and cleanup-cap contract; runtime marker creation remains qualification-only"
      pattern: "owner|cleanup|MAX"
    - from: "tools/verify_phase8_contracts.py"
      to: "tests/qualification/test_phase8_evidence.py"
      via: "fixed T-08-15 selector ownership"
      pattern: "T-08-15|preflight"
---

<objective>
Add the missing configuration-only live-service preflight that revised Plan 08-11 already requires.

Purpose: Let the executor prove protected PostgreSQL/Amazon-S3 configuration and exact-source prerequisites without touching either service, creating qualification resources, or writing evidence before the release-candidate dispatch checkpoint.
Output: A tested `--preflight` CLI and fixed-verifier bindings for the additive Phase 8 gap.
</objective>

<scope_guard>
This plan changes only qualification tooling and deterministic tests. It does not dispatch a workflow, authenticate to GitHub, provision or contact PostgreSQL/AWS, run the live suite, create/delete/list any service resource, publish release evidence, alter storage/lifecycle/concurrency code, change BACK-05 QUALIFIED/CLEAN semantics, modify the D-23 performance deferral, or touch TensorFlow.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-14-SUMMARY.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-11-PLAN.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@tools/run_phase8_qualification.py
@tests/qualification/conftest.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add one pure, sanitized live-configuration preflight path</name>
  <files>tools/run_phase8_qualification.py, tests/qualification/test_phase8_evidence.py</files>
  <read_first>
    - tools/run_phase8_qualification.py: REQUIRED_CONFIGURATION, _DISALLOWED_S3_CONFIGURATION, _missing_configuration, _validate_external_configuration, _qualification_source_identity, resolve_aws_service_identity, run_qualification, and main
    - tests/qualification/test_phase8_evidence.py: _configured_environment, runner-loading seam, missing/invalid configuration tests, source-identity tests, cleanup-cap tests, and secret-fragment assertions
    - tests/qualification/conftest.py: QualificationConfig, qualification_config_from_environment, owner-marker constants, cleanup caps, namespace ownership, and cleanup_qualification_resources
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: D-03 and D-09 through D-12
  </read_first>
  <behavior>
    - `test_preflight_accepts_sanitized_configuration_without_external_effects`: valid required configuration plus an injected clean `SourceIdentity` returns exit 0 and one canonical bounded stdout JSON object whose status is `CONFIGURED`, which contains the literal field `"service_state": "NOT_RUN"`, and whose remaining fields contain only fixed names/booleans, exact revision/source digest, standard-AWS provider-chain policy selection, PostgreSQL structural validity, and reviewed cleanup-policy caps; none of the supplied environment values appear
    - `test_preflight_rejects_missing_invalid_and_disallowed_configuration_without_external_effects`: absent required names return a canonical `UNAVAILABLE` object and malformed bucket/region/key/DSN or any prohibited endpoint/owner override returns a canonical `INVALID` object; both result classes contain the literal field `"service_state": "NOT_RUN"`, remain sanitized and bounded, and invoke no effect seam
    - `test_preflight_requires_clean_source_and_reviewed_cleanup_contract`: absent/dirty relevant-source identity, missing fixed source, unavailable local DSN parser, or drifted owner-marker/cleanup-cap contract fails nonzero with an `UNAVAILABLE` or `INVALID` object containing the literal field `"service_state": "NOT_RUN"`, without a service call or file write
    - `test_preflight_cli_never_runs_or_writes_qualification_evidence`: CLI `--preflight` rejects effect-bearing output/role combinations, never calls AWS credential resolution, the frozen pytest runner, qualification cleanup, namespace generation, or evidence writing, and leaves the default output absent
  </behavior>
  <action>Write the four behaviors first, then add a dedicated `run_preflight` path selected by `main` when `--preflight` is present. Reuse the existing required-name, bucket/region/key, prohibited-S3-setting, fixed-source digest, and clean-worktree validation rather than creating a second configuration vocabulary. Parse the PostgreSQL DSN locally with psycopg's conninfo parser without opening a connection; require a structurally usable database target, catch all parser diagnostics at the boundary, and expose only a fixed validity/error code. For AWS, verify only that the standard provider-chain policy is selected and that every endpoint/owner override is absent; credentials, identity, IAM authorization, and service availability remain `NOT_RUN`, so do not instantiate a client, resolve/refresh credentials, or contact AWS. Load the existing qualification fixture locally only to verify its exact owner-marker requirement and positive fixed cleanup page/object/byte caps; state in the report that the per-run marker is required at execution rather than pretending it already exists. Produce one fixed-schema canonical JSON object on stdout capped at 4 KiB with status `CONFIGURED`, `UNAVAILABLE`, or `INVALID`; require every result class to contain the exact literal field `"service_state": "NOT_RUN"`, plus exact source identity when available, required/missing configuration names, policy booleans, and cleanup caps. Never include any supplied environment value or raw exception text. Return 0 only for the complete configuration contract, 2 for absent local prerequisites, and 1 for invalid/drifted configuration. Keep ordinary `run_qualification` behavior and evidence schema unchanged. Ensure `--preflight` cannot be combined with an output target or scheduled role, and prove that all network, live-test, cleanup, namespace, and write seams remain uncalled.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/qualification/test_phase8_evidence.py::test_preflight_accepts_sanitized_configuration_without_external_effects tests/qualification/test_phase8_evidence.py::test_preflight_rejects_missing_invalid_and_disallowed_configuration_without_external_effects tests/qualification/test_phase8_evidence.py::test_preflight_requires_clean_source_and_reviewed_cleanup_contract tests/qualification/test_phase8_evidence.py::test_preflight_cli_never_runs_or_writes_qualification_evidence -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tools/run_phase8_qualification.py tests/qualification/test_phase8_evidence.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff format --check tools/run_phase8_qualification.py tests/qualification/test_phase8_evidence.py</automated>
  </verify>
  <done>The checked-in CLI accepts `--preflight`; every CONFIGURED, UNAVAILABLE, and INVALID result contains the exact literal JSON field `"service_state": "NOT_RUN"`; the command proves only the local configuration/source/policy contract, emits no supplied values, and performs no external or filesystem mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the additive preflight plan into the fixed Phase 8 verifier</name>
  <files>tools/verify_phase8_contracts.py, tests/test_phase8_contract_verifier.py</files>
  <read_first>
    - tools/verify_phase8_contracts.py: _REVIEWED_PLAN_PATHS, _REVIEWED_THREATS, _THREAT_TEST_MODULES, _REVIEWED_THREAT_NODES, validate_mapping_inventory, and validate_fixed_manifest
    - tests/test_phase8_contract_verifier.py: EXPECTED_PLAN_PATHS, full manifest assertions, hostile inventory mutation tests, and deferred-performance fixed bindings
    - tests/qualification/test_phase8_evidence.py: the four exact preflight selector names created by Task 1
    - .planning/phases/08-production-gates-and-performance-stabilization/08-15-PLAN.md: T-08-15 threat register and source coverage
  </read_first>
  <behavior>
    - `test_fixed_manifest_binds_live_configuration_preflight_gap`: the reviewed plan inventory contains exact Plans 01 through 15, all T-08-15 threats map to existing literal preflight tests, the decision/requirement sets remain unchanged, and fixed-manifest validation passes
    - existing source-mutation and selector-validation tests still fail closed when the new plan, threat, or selector is removed, renamed, duplicated, or adaptively discovered
  </behavior>
  <action>Add `08-15-PLAN.md` to the literal reviewed plan tuple and add T-08-15-01 through T-08-15-05 to the literal threat inventory. Bind information disclosure to the sanitized-output selector, nonstandard resource configuration to the invalid-configuration selector, stale source identity to the clean-source selector, overbroad cleanup authorization to the reviewed-cleanup selector, and external effects/evidence writes to the CLI-purity selector. Extend the adversarial manifest test to require exactly Plans 01 through 15 and these five mappings. Do not add a D-24, alter the seven current requirement IDs, reclassify deferred QUAL-06, discover plans/tests at runtime, run external preflight from the fixed verifier, or change any external evidence status.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_phase8_contract_verifier.py::test_fixed_manifest_binds_live_configuration_preflight_gap tests/test_phase8_contract_verifier.py::test_fixed_manifest_rejects_source_mutation_that_drops_plan_or_threat tests/test_phase8_contract_verifier.py::test_selector_validation_rejects_removed_renamed_duplicate_and_unowned_nodes tests/qualification/test_phase8_evidence.py::test_preflight_accepts_sanitized_configuration_without_external_effects tests/qualification/test_phase8_evidence.py::test_preflight_rejects_missing_invalid_and_disallowed_configuration_without_external_effects tests/qualification/test_phase8_evidence.py::test_preflight_requires_clean_source_and_reviewed_cleanup_contract tests/qualification/test_phase8_evidence.py::test_preflight_cli_never_runs_or_writes_qualification_evidence -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tools/verify_phase8_contracts.py tests/test_phase8_contract_verifier.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff format --check tools/verify_phase8_contracts.py tests/test_phase8_contract_verifier.py</automated>
  </verify>
  <done>The fixed verifier treats Plan 08-15 and its five threat mitigations as closed literal inventory while preserving every existing decision, requirement, evidence-class, and performance-deferral boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| protected environment -> preflight report | Secret-bearing configuration is parsed in memory and only fixed status/name fields may leave the process. |
| local preflight -> PostgreSQL/Amazon S3 | Configuration inspection must stop before connection, credential refresh, request, listing, mutation, or cleanup. |
| reviewed candidate source -> live dispatch | Only one clean exact Git revision and fixed relevant-source digest may pass the local prerequisite gate. |
| future run namespace -> destructive cleanup | Preflight verifies the reviewed bounded owner-marker policy but cannot create a marker or authorize cleanup before the live run owns its namespace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-15-01 | Information Disclosure | preflight stdout/errors | high | mitigate | Emit one 4-KiB-capped allow-listed JSON object whose CONFIGURED, UNAVAILABLE, and INVALID variants all contain the exact literal field `"service_state": "NOT_RUN"`; adversarially assert every supplied value and raw exception is absent. |
| T-08-15-02 | Spoofing | PostgreSQL/Amazon-S3 configuration | high | mitigate | Require the four named values, local DSN structure, explicit bucket/region/key validity, standard AWS provider policy, and absence of endpoint/owner overrides without claiming service availability. |
| T-08-15-03 | Tampering | candidate revision/source digest | high | mitigate | Reuse the fixed relevant-source inventory and clean exact-HEAD identity; reject missing, dirty, or drifted source before success. |
| T-08-15-04 | Elevation of Privilege | cleanup authorization | high | mitigate | Bind success to the reviewed exact owner-marker policy and positive fixed caps; actual deletion remains forbidden until a run-created marker is verified. |
| T-08-15-05 | Denial of Service | configuration-only boundary | high | mitigate | Patch every network, test, namespace, cleanup, and write seam to fail if invoked and require preflight to finish from local bounded parsing alone. |
</threat_model>

<verification>
- The four preflight behavior tests pass from an isolated all-extras/dev environment without credentials or service access.
- The output-schema assertions require the exact literal field `"service_state": "NOT_RUN"` in CONFIGURED, UNAVAILABLE, and INVALID results.
- Direct Ruff lint and format checks pass for all four modified Python files.
- The fixed manifest contains Plan 08-15 and exact selectors for T-08-15-01 through T-08-15-05.
- The existing side-effecting qualification mode and its QUALIFIED/CLEAN evidence contract remain unchanged.
</verification>

<acceptance_criteria>
- `tools/run_phase8_qualification.py --preflight` is a valid, configuration-only command and returns success only for a complete locally valid protected-live contract.
- Every CONFIGURED, UNAVAILABLE, and INVALID result contains the exact literal JSON field `"service_state": "NOT_RUN"`.
- No environment value, credential, DSN, bucket, region, endpoint, signing key, or raw exception appears in stdout/stderr.
- No PostgreSQL/AWS connection or data operation, pytest execution, namespace creation, cleanup, evidence write, workflow dispatch, or release mutation occurs.
- Missing/invalid configuration, source drift, parser absence, and cleanup-policy drift fail with distinct truthful nonzero classes.
- Plan 08-11 depends on completed Plan 08-15 before selecting or dispatching the exact candidate SHA.
</acceptance_criteria>

<success_criteria>
- The protected live checkpoint now has an executable preflight rather than an unsupported CLI flag.
- Preflight readiness remains strictly weaker than BACK-05 qualification; only the later real-service QUALIFIED/CLEAN envelope can close BACK-05.
- The gap is covered by literal fixed-verifier plan and threat inventory.
</success_criteria>

<source_coverage>
- GOAL: Reproducible exact-commit release evidence has an executable, non-mutating live-service prerequisite gate.
- REQ: BACK-05 and QUAL-03.
- RESEARCH: R-10 fail-closed live runner/exact cleanup, R-11 protected release-candidate execution, and R-12 fixed selector/source/threat verification.
- CONTEXT: D-03, D-09, D-10, D-11, D-12, and D-23.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-15-SUMMARY.md` when done.
</output>
