---
phase: 08-production-gates-and-performance-stabilization
plan: 10
type: execute
wave: 6
depends_on:
  - 08-07
  - 08-08
  - 08-09
files_modified:
  - tools/verify_phase8_contracts.py
  - tools/verify_phase8_release.py
  - tests/test_phase8_contract_verifier.py
  - tests/qualification/test_phase8_release.py
autonomous: true
requirements:
  - BACK-05
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06
  - QUAL-07
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A fixed verifier binds all 13 plans, all 23 decisions, eight requirement IDs, every Phase 8 threat, source inventory, workflow command, and named selector to executable evidence, including D-23 TensorFlow absence."
    - "The release aggregator requires deterministic, packaging, platform, coverage, structural, controlled-performance, and live-service evidence for one exact commit and relevant-source digest (D-10)."
    - "Exact-SHA orchestration dispatches the quality, controlled-performance, and live workflows, records their run IDs, waits for those exact runs, downloads fixed artifacts by run ID and name, and validates every envelope before aggregation (D-10, D-11)."
    - "UNAVAILABLE, NOT_QUALIFIED, stale, dirty, malformed, skipped, unclean, wrong-runner, or missing evidence produces a truthful blocking report and never a release pass (D-11, D-20)."
    - "The verifier preserves ADR 0001 limits and canonical SHA-256 semantics while keeping remote latency diagnostic (D-03, D-04, D-19, D-22)."
  artifacts:
    - path: "tools/verify_phase8_contracts.py"
      provides: "Fixed phase decisions/requirements/threats/selectors/source verifier"
    - path: "tools/verify_phase8_release.py"
      provides: "Exact-SHA workflow dispatcher/collector, multi-class aggregator, and immutable-publication verifier"
    - path: "tests/test_phase8_contract_verifier.py"
      provides: "Fail-closed fixed-inventory and source/selector mutation tests"
    - path: "tests/qualification/test_phase8_release.py"
      provides: "Cross-class stale/missing/nonclaim/revision aggregation tests"
  key_links:
    - from: "tools/verify_phase8_contracts.py"
      to: "tools/run_phase8_local_gates.py"
      via: "literal AST-validated quick/all command inventory"
      pattern: "PHASE8_TEST_NODES|run_phase8_local_gates"
    - from: "tools/verify_phase8_release.py"
      to: "tools/phase8_evidence.py"
      via: "strict per-class envelope parser and common source/revision identity"
      pattern: "phase8_evidence"
    - from: "tools/verify_phase8_release.py"
      to: ".github/workflows/quality.yml"
      via: "exact candidate-SHA dispatch, run-ID wait, fixed artifact download, and envelope validation shared with performance/live workflows"
      pattern: "workflow run|run view|run watch|run download"
---

<objective>
Create the final fixed Phase 8 verifier and exact-commit release evidence aggregator.

Purpose: Make every phase claim mechanically reachable while guaranteeing truthful failure when controlled or live prerequisites are absent.
Output: Fixed deterministic verifier, release aggregator, and adversarial self-tests.
</objective>

<scope_guard>
This plan verifies existing tools/workflows/tests and aggregates evidence. It cannot repair a failing storage test by changing production lifecycle code or weaken any required evidence class.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@tools/verify_phase071_contracts.py
@tools/phase8_evidence.py
@docs/RELEASE_QUALIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind the complete fixed phase contract to exact selectors and source audits</name>
  <files>tools/verify_phase8_contracts.py, tests/test_phase8_contract_verifier.py</files>
  <read_first>
    - tools/verify_phase071_contracts.py: literal plan/decision/threat/selector registries, AST checks, quick/all execution
    - tests/test_phase071_contract_verifier.py: omission, rename, comment/string, threat, and source-inventory mutation tests
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: exact D-01 through D-23 set
  </read_first>
  <behavior>
    - "The verifier owns literal Plan 01-13, D-01 through D-23, eight requirement IDs, every T-08 threat, source path, workflow, and exact test selector."
    - "Removed, renamed, duplicated, malformed, skipped, comment-only, dynamically discovered, or unowned evidence fails before execution can go green."
    - "AST audits reject reintroduced legacy payload mechanics/selectors, production boto3 fallback, lifecycle locks/queues/leases/sidecars, parallel authorities, digest changes, and unavailable-to-pass conversions."
    - "Quick mode runs bounded deterministic self-tests; all mode invokes every local class and reports controlled/live prerequisites without falsifying them."
  </behavior>
  <action>Follow the established fixed-verifier pattern with literal reviewed inventories and AST selector validation. Map every plan, decision, requirement, threat, evidence class, workflow source, and named test to exact proof. Bind Plan 08-13's exact runtime/package/docs/lock/matrix absence assertions while excluding historical `.planning` summaries from production-surface scans. Add prohibited-architecture audits derived from D-01 through D-04, D-19, and D-23, plus checks that Windows, controlled performance, and live services cannot pass from local/mocked evidence. Quick mode must remain bounded; all mode runs deterministic, packaging, platform, coverage/Ruff, structural, and benchmark/live schema gates, then prints exact external statuses. Add adversarial tests for every registry/source/selector/status mutation, including restoration of a native TensorFlow surface.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/test_phase8_contract_verifier.py -x</automated>
  </verify>
  <done>The fixed verifier covers the complete Phase 8 contract and fails closed on drift, omission, architecture regression, or false qualification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dispatch exact-SHA workflows and collect fixed artifacts by run ID</name>
  <files>tools/verify_phase8_release.py, tests/qualification/test_phase8_release.py</files>
  <read_first>
    - .github/workflows/quality.yml: exact-SHA release-candidate input and fixed deterministic/package/platform/coverage/structural artifact names
    - .github/workflows/performance.yml: controlled-runner exact-SHA input and controlled-performance artifact name
    - .github/workflows/live_qualification.yml: protected exact-SHA input, scheduled nonclaim, and live artifact name
    - .planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md: exact-SHA dispatch/run-ID wait/artifact download contract and official `gh` command semantics
  </read_first>
  <behavior>
    - "The controller accepts exactly one lowercase 40-character candidate SHA and dispatches each required workflow with that SHA as explicit input."
    - "For each workflow it records one run ID, verifies workflow/event/headSha/status/conclusion, waits for that exact run with failure propagation, and downloads only fixed artifact names using that run ID."
    - "Latest-run lookup, filename-only download, duplicate/wrong workflow IDs, unsuccessful conclusions, scheduled diagnostics, old SHA, source drift, or missing artifacts fail before aggregation."
    - "Collected envelopes retain workflow/run/artifact digests and contain no credential, raw payload, or unbounded log output."
  </behavior>
  <action>Implement an exact-SHA orchestration subcommand behind an injectable subprocess/API seam. Dispatch the trusted quality, controlled-performance, and protected live workflows with the required candidate SHA; discover and record the resulting run ID without selecting a prior or latest run; use `gh run view` to require the expected workflow, event, and `headSha`; use `gh run watch RUN_ID --exit-status`; and use `gh run download RUN_ID -n FIXED_NAME` for every declared artifact. Parse each bounded envelope, recompute relevant-source identity, and require class-specific terminal rules before returning collected inputs. Persist workflow identity, run ID, artifact name, artifact SHA-256, revision, and relevant-source digest only. Add fake-CLI tests for wrong run/SHA/workflow/event, duplicate IDs, incomplete/failing/cancelled runs, missing or extra artifacts, scheduled substitutions, command drift, and any invocation lacking both run ID and artifact name.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_release.py -k 'dispatch or run_id or artifact_collection' -x</automated>
  </verify>
  <done>Every required external evidence class is collected from a successful exact-candidate workflow run by recorded run ID and fixed artifact name, with no latest-run or diagnostic substitution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Aggregate exact-commit evidence and verify immutable publication state</name>
  <files>tools/verify_phase8_release.py, tests/qualification/test_phase8_release.py</files>
  <read_first>
    - tools/run_phase8_qualification.py: live terminal-state and source-binding contract
    - benchmarks/phase8_benchmarks.py: controlled runner/baseline evidence schema
    - tools/phase8_evidence.py: common envelope parser and class names
    - tools/verify_phase5_contracts.py: fixed source/test inventories, read-only live-evidence status, and fail-closed absence reporting
    - tests/test_phase3_release_evidence.py: Git tag/archive/release-artifact inspection and adversarial repository-state fixtures
  </read_first>
  <behavior>
    - "Exactly one qualifying envelope for every required class shares the requested release SHA and recomputed relevant-source digest."
    - "Live requires QUALIFIED/CLEAN complete real-service proof; controlled performance requires matching runner/baseline/envelope; stable platform/package/coverage/structural/deterministic classes require complete PASS."
    - "Diagnostics, scheduled live runs, prerelease Python, remote latency, old commits, filenames, and timestamps never fill a required slot."
    - "The aggregate is sanitized, canonical, immutable-ready, and contains digests/references rather than secrets or raw payloads."
    - "Publication verification rejects a tag that does not resolve to the candidate SHA, a draft or mutable/non-immutable release, any missing/extra/starter/diagnostic asset, any asset not in uploaded state, and any API/local SHA-256 digest mismatch."
  </behavior>
  <action>Following `tools/verify_phase5_contracts.py`, implement a read-only aggregator with literal evidence/source inventories that loads bounded collected envelopes, recomputes common relevant-source identity, requires exact release revision, checks every class-specific blocking rule, and writes a canonical sanitized release manifest only after all classes qualify. Following `tests/test_phase3_release_evidence.py`, add injectable Git/GitHub CLI/API inspection for the intended tag, release state, exact asset allow-list, API upload states, and locally recomputed SHA-256 digests. The final publication verifier must require the tag to resolve to the candidate commit, the release to be published/non-draft and reported immutable, `gh release verify` to succeed, and `gh release verify-asset` to succeed for every local qualifying asset. It must never upgrade/repair input, select the newest filename, inherit an earlier pass, accept scheduled diagnostics, or mutate the release while verifying. Report each unavailable/missing/stale class explicitly with exit code 2 for prerequisites versus exit code 1 for invalid/failing evidence. Add cross-class substitution, duplicate, old-revision, source-drift, unclean-live, wrong-runner, Windows/prerelease, remote-latency, secret-injection, tag-target, draft/mutability, asset set/state, and digest mutation tests.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_release.py tests/test_phase8_contract_verifier.py -x</automated>
  </verify>
  <done>The release verifier produces a manifest only for complete same-revision evidence, and its final read-only mode accepts only the exact published immutable release with the exact uploaded asset set and matching SHA-256 digests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| planning/source/test inventory -> fixed verifier | Omitted or drifted claims must not escape executable review. |
| independent evidence files -> release manifest | One class/revision must not impersonate another. |
| release manifest -> release record | Only sanitized complete same-commit evidence may be attached as qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-10-01 | Tampering | fixed inventories/selectors | high | mitigate | Literal registries, AST validation, source audits, and adversarial omission/rename tests. |
| T-08-10-02 | Spoofing | evidence class/revision | high | mitigate | Exact one-per-class validation with common SHA/source digest and class-specific schema/status rules. |
| T-08-10-03 | Repudiation | release decision | high | mitigate | Canonical aggregate lists input digests, producer classes, revision, environment, and decision reason. |
| T-08-10-04 | Information Disclosure | aggregate manifest | high | mitigate | Strict safe-field allow-list and recursive secret/DSN/key/token scan before write. |
| T-08-10-05 | Elevation of Privilege | architecture audit bypass | high | mitigate | Executable AST/source rules reject second authorities, coordination machinery, legacy payload paths, and digest mutation. |
| T-08-10-06 | Denial of Service | artifact parser/child gates | medium | mitigate | Bounded files/items/strings and fixed child timeouts with explicit unavailable/failure status. |
</threat_model>

<verification>
- Fixed-verifier and aggregator self-tests pass against adversarial source/evidence mutations.
- Quick verification runs locally; all mode truthfully reports unavailable external classes if not supplied.
- No release manifest is produced from incomplete inputs.
</verification>

<success_criteria>
- Every goal, requirement, research constraint, and D-01 through D-23 decision has executable coverage.
- Release qualification is one exact-commit conjunction of separate evidence classes.
- Unavailable prerequisites block without becoming false failures of the shipped architecture or false passes.
</success_criteria>

<source_coverage>
- GOAL: Users can rely on reproducible exact-commit evidence across every declared class.
- REQ: BACK-05 and QUAL-01 through QUAL-07.
- RESEARCH: Fixed selector/source verifier, exact-commit aggregator, separate blocking rules, immutable sanitized release manifest.
- CONTEXT: D-01 through D-23.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-10-SUMMARY.md` when done.
</output>
