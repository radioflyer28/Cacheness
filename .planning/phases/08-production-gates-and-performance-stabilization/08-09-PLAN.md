---
phase: 08-production-gates-and-performance-stabilization
plan: 09
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
  - 08-05
  - 08-06
files_modified:
  - tools/run_phase8_local_gates.py
  - .github/workflows/quality.yml
  - tests/qualification/test_phase8_quality_workflow.py
  - docs/RELEASE_QUALIFICATION.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-07
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Pull requests run deterministic contracts, package probes, branch coverage, direct Ruff scopes, and structural bounds without live secrets or controlled-performance claims (D-09, D-14 through D-17, D-21)."
    - "Linux stable, macOS boundary, prerelease advisory, and feature-aware package jobs preserve the exact matrix from D-05 through D-08."
    - "Release documentation distinguishes all evidence classes, topology limits, Windows nonclaim, and retained migration fixtures (D-03, D-04, D-06)."
  artifacts:
    - path: ".github/workflows/quality.yml"
      provides: "Deterministic PR and supported-Python/platform quality workflow"
    - path: "tools/run_phase8_local_gates.py"
      provides: "Expanded fixed local/CI orchestration with class-separated outputs"
    - path: "docs/RELEASE_QUALIFICATION.md"
      provides: "Operator-facing evidence matrix, commands, nonclaims, and retention rules"
    - path: "tests/qualification/test_phase8_quality_workflow.py"
      provides: "Workflow command/matrix/permission/drift tests"
  key_links:
    - from: ".github/workflows/quality.yml"
      to: "tools/run_phase8_local_gates.py"
      via: "fixed deterministic and quality commands, with selectors kept out of YAML"
      pattern: "run_phase8_local_gates"
    - from: "tools/run_phase8_local_gates.py"
      to: "tools/verify_phase8_coverage.py"
      via: "coverage/Ruff quality evidence child command"
      pattern: "verify_phase8_coverage"
---

<objective>
Compose deterministic, packaging, platform, coverage, lint/format, and structural gates into the pull-request and supported-runtime workflow.

Purpose: Close the local/CI portion of QUAL-03 without collapsing live or controlled-performance evidence into an omnibus green job.
Output: Quality workflow, expanded local command, workflow self-tests, and release-qualification guide.
</objective>

<scope_guard>
Workflow YAML orchestrates tested Python commands only. It cannot duplicate selectors/threshold math, receive live secrets on pull requests, or treat a skipped/unavailable evidence class as green.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@tools/run_phase8_local_gates.py
@tools/run_phase8_packaging.py
@tools/run_phase8_platform_gates.py
@tools/verify_phase8_coverage.py
@tools/run_phase8_scale_gates.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire fixed quality commands into the supported CI matrix</name>
  <files>tools/run_phase8_local_gates.py, .github/workflows/quality.yml, tests/qualification/test_phase8_quality_workflow.py</files>
  <read_first>
    - tools/run_phase8_local_gates.py: tracer command/evidence behavior from Plan 01
    - tools/run_phase8_platform_gates.py: exact row roles and aggregation
    - tools/verify_phase8_coverage.py: coverage and Ruff invocation contracts
    - .github/workflows/live_qualification.yml: separate protected live boundary
  </read_first>
  <behavior>
    - "PR jobs run deterministic/fault contracts, packaging matrix, coverage, direct Ruff lint/format, and structural call/memory self-tests with no live credentials."
    - "Linux has full stable 3.11-3.14 rows; macOS has 3.11/3.14 boundary rows; prerelease is continue-on-error advisory; TensorFlow uses only compatible stable rows."
    - "Commands use frozen isolated uv environments, pinned action SHAs, least workflow permissions, fixed timeouts, and uploaded sanitized evidence."
    - "Missing commands, rows, evidence artifacts, or a skip-based pass fail the workflow contract tests."
  </behavior>
  <action>Expand the local gate CLI into explicit deterministic, packaging, platform, coverage/Ruff, and structural subcommands whose fixed selectors and validation remain in Python. Add `quality.yml` for pull requests and trusted branch pushes with fail-fast disabled across the D-05/D-06 matrix. Pin actions to reviewed full SHAs, use least permissions and frozen uv resolution, and upload separate sanitized evidence artifacts. The workflow must not reference live secrets, run the controlled performance gate, or use repository-wide legacy Ruff debt as the blocking scope. Add YAML contract tests that validate exact commands, matrices, permissions, timeouts, artifact classes, and absence of cross-class substitutions.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_quality_workflow.py -x</automated>
  </verify>
  <done>The PR workflow exercises every deterministic/local evidence class across its exact supported runtime roles while leaving live/performance gates separate.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the release qualification and nonclaim contract</name>
  <files>docs/RELEASE_QUALIFICATION.md, tests/qualification/test_phase8_quality_workflow.py</files>
  <read_first>
    - .planning/ROADMAP.md: Phase 8 six success criteria
    - docs/adr/0001-topology-specific-storage-guarantees.md: guarantee vocabulary and topology matrix
    - .planning/phases/07.1-obstore-payload-participant-unification/07.1-VERIFICATION.md: final architecture and Phase 8 nonclaims
  </read_first>
  <action>Document each evidence class, producer command/workflow, blocking rule, revision/environment identity, retention, and expected terminal states. State Linux/macOS/Python/TensorFlow scope, Windows NOT_QUALIFIED, exact frozen live requirements, controlled-Linux-only timing gate, structural call/memory gates, and retained current/immediately-previous migration fixtures. Per D-03/D-04, explain that cross-resource ACID, universal contender success, and exact reclamation of an unattributed pre-checkpoint orphan are not claimed. Document that controlled regression review never changes runtime deadlines and real-service latency is diagnostic. Add doc contract assertions to the workflow self-test.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_quality_workflow.py -k documentation -x</automated>
  </verify>
  <done>Operators can determine exactly what a release is and is not qualified for, including every external prerequisite and evidence-retention boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| untrusted pull request -> CI | PR code must not gain live secrets or release permissions. |
| workflow YAML -> fixed gate tools | Command drift must not omit a class or duplicate decision logic. |
| documentation -> release operator | Ambiguous claims could turn partial evidence into an advertised guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-09-01 | Elevation of Privilege | PR workflow | high | mitigate | Least permissions, no live environment/secrets, pinned actions, and no privileged pull-request target trigger. |
| T-08-09-02 | Tampering | workflow commands/matrix | high | mitigate | Static self-tests compare exact Python entry points, rows, artifact classes, and timeouts. |
| T-08-09-03 | Spoofing | aggregate CI green | high | mitigate | Upload and validate separate evidence envelopes; missing or unavailable required classes fail. |
| T-08-09-04 | Repudiation | qualification claims | medium | mitigate | Version-controlled guide maps every claim/nonclaim to a producer and terminal-state rule. |
| T-08-09-SC | Tampering | CI dependency resolution | high | mitigate | Use frozen `uv.lock`, the built project wheel, and reviewed full action SHAs; no dependency is added. |
</threat_model>

<verification>
- Workflow contract tests validate exact jobs, rows, commands, permissions, artifacts, and secret separation.
- The expanded local gate passes every deterministic/local evidence class.
- Documentation tests cover all claim/nonclaim and prerequisite rows.
</verification>

<success_criteria>
- QUAL-03 deterministic CI scope is complete and evidence-class separated.
- Supported installations/platforms and quality/structural gates are reproducible.
- Live services and controlled performance remain explicit later prerequisites.
</success_criteria>

<source_coverage>
- GOAL: Reproducible release evidence across installations, Python versions, failures, and scale.
- REQ: QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-07.
- RESEARCH: Deterministic quality workflow, fixed Python commands, direct Ruff scopes, separate artifacts, operator matrix.
- CONTEXT: D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-14, D-15, D-16, D-17, D-21.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-09-SUMMARY.md` when done.
</output>
