---
phase: 08-production-gates-and-performance-stabilization
plan: 08
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tools/run_phase8_qualification.py
  - tests/qualification/test_phase8_evidence.py
  - tests/qualification/conftest.py
  - .github/workflows/live_qualification.yml
  - tests/qualification/test_phase8_live_workflow.py
autonomous: true
requirements:
  - BACK-05
  - QUAL-03
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The frozen real PostgreSQL/Amazon-S3 suite remains non-substitutable, no-skip, exact-cleanup, and bound to the exact release commit (D-10, D-11, D-12)."
    - "Production S3 behavior uses ObstoreGenerationIO with explicit bucket/region, standard AWS identity, no production endpoint override, and no ExpectedBucketOwner claim (D-01, D-12)."
    - "Protected release-candidate and scheduled drift runs are distinct; schedule evidence never qualifies another revision (D-09, D-10)."
    - "QUALIFIED evidence is sanitized and release-lifetime eligible; failed/unavailable/scheduled diagnostics retain bounded 30-day evidence and never substitute for a pass (D-11, D-13)."
  artifacts:
    - path: "tools/run_phase8_qualification.py"
      provides: "Phase 8 exact-revision real-service runner and evidence schema"
    - path: ".github/workflows/live_qualification.yml"
      provides: "Protected RC and scheduled live-service orchestration"
    - path: "tests/qualification/test_phase8_evidence.py"
      provides: "Fail-closed live evidence, redaction, source-binding, and cleanup tests"
    - path: "tests/qualification/test_phase8_live_workflow.py"
      provides: "Secret-boundary and workflow trigger contract tests"
  key_links:
    - from: "tools/run_phase8_qualification.py"
      to: "tests/integration/test_remote_topology.py"
      via: "fixed literal no-skip live test modules and marker expression"
      pattern: "test_remote_topology"
    - from: ".github/workflows/live_qualification.yml"
      to: "tools/run_phase8_qualification.py"
      via: "protected exact-SHA runner invocation"
      pattern: "run_phase8_qualification"
---

<objective>
Adapt and freeze the real PostgreSQL/Amazon-S3 qualification machinery for the completed obstore architecture.

Purpose: Make BACK-05 executable and fail closed while keeping deterministic PR contracts, protected RC evidence, scheduled diagnostics, and live latency separate.
Output: Phase 8 live runner, strict evidence/cleanup tests, and protected workflow.
</objective>

<scope_guard>
This plan may adapt qualification source inventories and test fixtures only as required by the completed obstore cutover. It cannot modify production storage/lifecycle paths, accept mocks/emulators/skips, add an endpoint override, or weaken exact cleanup/source-revision rules.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/07.1-obstore-payload-participant-unification/07.1-VERIFICATION.md
@tools/run_phase5_qualification.py
@tests/qualification/conftest.py
@tests/qualification/test_live_evidence.py
@tests/integration/test_postgresql_authority.py
@tests/integration/test_s3_generation.py
@tests/integration/test_remote_topology.py
</context>

<interfaces>
The four required secret/config names remain `CACHENESS_TEST_POSTGRES_DSN`, `CACHENESS_TEST_S3_BUCKET`, `CACHENESS_TEST_MANIFEST_KEY_B64`, and `CACHENESS_TEST_AWS_REGION`. Terminal statuses remain `QUALIFIED`, `UNAVAILABLE`, and `NOT_QUALIFIED`; cleanup remains `NOT_ATTEMPTED`, `CLEAN`, `RESIDUE`, or `ERROR`. Only `QUALIFIED` plus `CLEAN`, complete fixed tests, stable exact source, and standard AWS identity can satisfy BACK-05.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebind the frozen live runner to Phase 8 exact source and obstore evidence</name>
  <files>tools/run_phase8_qualification.py, tests/qualification/test_phase8_evidence.py</files>
  <read_first>
    - tools/run_phase5_qualification.py: constants, evidence schema, dirty-source checks, fixed suite, and run/cleanup flow
    - tests/qualification/test_live_evidence.py: contradictory terminal states, incomplete output, source drift, redaction, and callback tests
    - tests/integration/test_s3_generation.py: native obstore live path and markers
  </read_first>
  <behavior>
    - "The runner executes exactly the three frozen live modules with their live markers and rejects zero tests, skips, deselection, xfail, timeout, dirty source, or incomplete output."
    - "Evidence binds exact revision, relevant-source digest, obstore version, standard AWS provider/region, PostgreSQL service, namespace, result, and cleanup."
    - "Missing configuration exits 2 with UNAVAILABLE/NOT_ATTEMPTED; failures exit nonzero with NOT_QUALIFIED; only complete clean proof exits 0 with QUALIFIED."
  </behavior>
  <action>Copy the proven Phase 5 fail-closed shape into a Phase 8-owned runner while preserving the frozen behavior suite per D-12. Expand the relevant-source inventory to the active obstore participant, current authority/lifecycle/cache code, live tests/fixtures, qualification tools, workflows, and final contract definitions. Record obstore as sanitized service metadata, reject endpoint override/emulator signals and any owner-pinning claim, and keep production payload calls inside current topology composition. Reuse strict JSON/redaction/dirty-source/no-skip/atomic-write rules; an earlier Phase 5 artifact or different commit cannot qualify.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_evidence.py -k runner -x</automated>
  </verify>
  <done>The Phase 8 runner preserves the frozen live contract, targets the active obstore architecture, and cannot qualify missing/incomplete/stale evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve exact bounded live cleanup and secret-safe evidence</name>
  <files>tests/qualification/conftest.py, tests/qualification/test_phase8_evidence.py</files>
  <read_first>
    - tests/qualification/conftest.py: namespace markers, S3 page/object/byte/upload caps, PostgreSQL owner row, and cleanup statuses
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: D-11 through D-13
  </read_first>
  <behavior>
    - "S3 cleanup acts only below the exact run-owned prefix after marker verification and remains bounded by pages, objects, bytes, deletes, and multipart observations."
    - "PostgreSQL cleanup drops only the exact marker-owned schema and always rolls back/closes on errors."
    - "Uncertainty yields RESIDUE or ERROR; marker deletion happens only after clean bounded resource cleanup."
    - "No DSN, key, credential, token, payload, or secret value appears in evidence or captured logs."
  </behavior>
  <action>Generalize only the qualification run-ID/schema names needed for Phase 8 while retaining the existing exact owner markers and caps. Ensure fixture setup uses the active PostgreSQL authority and `ObstoreGenerationIO` S3 topology. Add malformed-marker, escaping-prefix, cap-exceeded, partial-delete, PostgreSQL ownership mismatch, cleanup exception, and forbidden-value tests. Per D-11/D-13, keep failed/unavailable evidence sanitized and diagnostic; never convert residue or uncertainty into clean completion.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_evidence.py -k 'cleanup or redact or secret' -x</automated>
  </verify>
  <done>Live cleanup is exact, bounded, ownership-checked, and secret-safe across both external services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestrate protected release-candidate and scheduled drift evidence</name>
  <files>.github/workflows/live_qualification.yml, tests/qualification/test_phase8_live_workflow.py</files>
  <read_first>
    - tools/run_phase8_qualification.py: fixed arguments, exit/status meanings, and required environment
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: D-09 through D-13 and D-22
  </read_first>
  <behavior>
    - "No pull-request trigger or untrusted-ref path can receive live secrets."
    - "Protected manual RC execution checks out and qualifies the explicit candidate SHA; scheduled execution uses current default branch and is diagnostic only."
    - "Failed, unavailable, and scheduled artifacts retain 30 days; sanitized clean RC evidence is marked for release-lifetime attachment."
    - "Remote latency distributions are uploaded as diagnostics and never enter pass/fail logic."
  </behavior>
  <action>Create a protected-environment workflow with manual exact-SHA release-candidate input and an off-hour schedule per D-09. Pin third-party actions to reviewed full commit SHAs, pass secrets only to the fixed runner step, and upload sanitized output with a 30-day diagnostic retention selected under discretion. Make scheduled output incapable of `QUALIFIED` release use, and mark only exact-RC clean evidence as eligible for immutable release attachment. Add static workflow tests for triggers, permissions, SHA checkout, environment protection, required secrets, runner command, artifact retention, and absence of remote-latency thresholds.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/qualification/test_phase8_live_workflow.py tests/qualification/test_phase8_evidence.py -x</automated>
  </verify>
  <done>Live service jobs are protected, exact-revision, sanitized, cleanup-bound, and separated from PR and performance evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| protected workflow -> PostgreSQL/AWS | Real credentials and shared resources cross into a fixed qualification namespace. |
| live pytest output -> evidence file | Child output may contain secrets or incomplete/misleading status. |
| cleanup fixture -> external resources | Cleanup must not escape exact marker-owned schema/prefix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-08-01 | Elevation of Privilege | workflow secrets | high | mitigate | Protected environment, no PR trigger, least-privilege credentials, exact SHA checkout, and secret-scoped runner step. |
| T-08-08-02 | Information Disclosure | evidence/logs | high | mitigate | Exact allow-list plus forbidden-name/value scans; upload only sanitized JSON. |
| T-08-08-03 | Tampering | release revision/source | high | mitigate | Require 40-character candidate SHA, clean relevant sources, and recomputed source digest. |
| T-08-08-04 | Tampering | cleanup targets | high | mitigate | Verify run owner marker, prefix/schema containment, and page/object/byte/delete bounds before mutation. |
| T-08-08-05 | Spoofing | real-service pass | high | mitigate | Reject mocks, endpoint overrides, skips, incomplete tests, substitute services, stale artifacts, and unclean cleanup. |
| T-08-08-06 | Denial of Service | live suite/cleanup | medium | mitigate | Fixed suite timeout and bounded cleanup; residue blocks qualification without expanding retries. |
</threat_model>

<verification>
- Runner/evidence/cleanup/workflow self-tests pass without real secrets.
- A local no-credential invocation emits sanitized UNAVAILABLE evidence and exit code 2.
- No production source, live test contract, or topology guarantee is weakened.
</verification>

<success_criteria>
- BACK-05 has executable real-service qualification machinery but remains open until Plan 11 runs it successfully.
- Protected RC and scheduled diagnostic evidence cannot be confused.
- Exact cleanup and source identity remain non-negotiable qualification conditions.
</success_criteria>

<source_coverage>
- GOAL: Reproducible release evidence across real PostgreSQL and Amazon S3 behavior.
- REQ: BACK-05 and QUAL-03.
- RESEARCH: Phase 5 runner reuse, exact-revision schema, protected/scheduled split, lifetime versus bounded artifacts.
- CONTEXT: D-01, D-09, D-10, D-11, D-12, D-13, D-22.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-08-SUMMARY.md` when done.
</output>
