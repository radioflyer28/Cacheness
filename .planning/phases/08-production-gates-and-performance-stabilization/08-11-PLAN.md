---
phase: 08-production-gates-and-performance-stabilization
plan: 11
status: superseded
superseded_by: "SEED-007 (future real-service qualification); 08-16-PLAN.md (current local-readiness closure)"
superseded_reason: "D-24 closes the current milestone on local readiness while preserving BACK-05 as DEFERRED/NOT_QUALIFIED; no eligible real PostgreSQL/Amazon-S3 environment is available and no substitute evidence is permitted."
type: execute
wave: 9
depends_on:
  - 08-15
files_modified:
  - build/phase8/evidence
  - build/phase8/live_qualification.json
  - build/phase8/release_qualification.json
autonomous: true
requirements:
  - BACK-05
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-07
user_setup:
  - service: postgresql-and-amazon-s3
    why: "Run the non-substitutable BACK-05 real-service suite"
    env_vars:
      - name: CACHENESS_TEST_POSTGRES_DSN
        source: "Protected release environment secret for the dedicated qualification database"
      - name: CACHENESS_TEST_S3_BUCKET
        source: "Protected release environment variable for the dedicated Amazon S3 bucket"
      - name: CACHENESS_TEST_MANIFEST_KEY_B64
        source: "Protected release environment secret containing the test manifest signing key"
      - name: CACHENESS_TEST_AWS_REGION
        source: "Protected release environment variable containing the bucket region"
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The frozen real PostgreSQL/Amazon-S3 suite produces sanitized QUALIFIED/CLEAN evidence for the exact final candidate commit, or BACK-05 remains blocked (D-10, D-11, D-12)."
    - "Quality and live workflows are dispatched for the exact candidate SHA, waited by recorded run ID, and downloaded by run ID plus fixed artifact name before aggregation (D-10, D-11)."
    - "The release aggregate requires deterministic, packaging, platform, coverage, structural, and live-service proof for one SHA/source digest while recording controlled performance as DEFERRED/NOT_QUALIFIED under SEED-006 (D-03, D-23)."
    - "macOS timing, the retained performance workflow, unavailable prerequisites, mocks, skips, scheduled diagnostics, or stale evidence cannot satisfy a current qualifying class (D-11, D-22, D-23)."
  artifacts:
    - path: "build/phase8/evidence"
      provides: "Exact run-ID-collected required quality and live workflow artifacts plus canonical collection manifest"
    - path: "build/phase8/live_qualification.json"
      provides: "Generated sanitized exact-commit real-service evidence"
    - path: "build/phase8/release_qualification.json"
      provides: "Generated canonical current-milestone release manifest with explicit performance deferral"
  key_links:
    - from: ".github/workflows/quality.yml"
      to: "build/phase8/evidence"
      via: "exact candidate-SHA dispatch, recorded run-ID wait, and fixed-name artifact download"
      pattern: "candidate_sha|run_id|artifact"
    - from: ".github/workflows/live_qualification.yml"
      to: "build/phase8/live_qualification.json"
      via: "release-candidate QUALIFIED/CLEAN envelope copied from the exact collected run"
      pattern: "QUALIFIED|CLEAN"
    - from: "build/phase8/evidence"
      to: "build/phase8/release_qualification.json"
      via: "strict same-SHA aggregation of every non-deferred class plus the closed D-23 deferral record"
      pattern: "SEED-006|DEFERRED|NOT_QUALIFIED"
---

> **Superseded 2026-09-15 by D-24:** This exact-SHA live collection plan was not
> executed and must not be counted as a BACK-05 pass. Its service, cleanup,
> source-identity, and non-substitution contract is preserved intact for SEED-007.

<objective>
Collect the non-substitutable external evidence and aggregate every current qualifying class for the exact release commit.

Purpose: Close BACK-05 only from real PostgreSQL/Amazon-S3 proof while preserving a truthful explicit QUAL-06 deferral rather than waiting for or substituting controlled-Linux performance.
Output: Fixed exact-SHA workflow artifacts, sanitized live evidence, and final same-commit release manifest.
</objective>

<scope_guard>
This plan performs qualification after Plan 08-14 revises the evidence boundary. An unavailable live service or release prerequisite stops with a truthful blocking checkpoint; it never authorizes substitute evidence, a source edit, threshold weakening, a topology/lifecycle change, performance workflow dispatch, or macOS/Linux equivalence.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-15-SUMMARY.md
@.planning/seeds/SEED-006-qualify-controlled-linux-performance.md
@docs/RELEASE_QUALIFICATION.md
@tools/run_phase8_qualification.py
@tools/verify_phase8_release.py
</context>

<external_checkpoint_contracts>

| Gate | Owner | Exact preflight | Resume evidence | Failure/absence contract |
|---|---|---|---|---|
| Protected PostgreSQL/Amazon-S3 environment | Cloud/repository administrator | `tools/run_phase8_qualification.py --preflight` proves the four named variables exist, the standard AWS provider-chain policy is selected, endpoint/owner overrides are absent, and the bounded owner-marker cleanup policy is locally valid without emitting values. Credentials, identity, IAM authorization, and service availability remain `NOT_RUN`; preflight cannot prove them and must not resolve credentials or contact AWS. | Sanitized successful configuration-only preflight for the exact protected environment, including `"service_state": "NOT_RUN"` | Stop with BACK-05 `UNAVAILABLE`/`NOT_QUALIFIED`; no mock, emulator, earlier run, or substitute service |
| Exact release-candidate dispatch | Release operator | `tools/verify_phase8_release.py preflight-collection --candidate-sha CANDIDATE_SHA` proves `gh auth status`, workflow visibility/dispatch permission, trusted definitions, exact fixed current artifact inventory, and all prerequisite local gates | Explicit approval of the 40-character candidate SHA; subsequent automation records exact workflow run IDs | Do not dispatch another/latest revision; retain only bounded sanitized diagnostics |

An unmet task precondition becomes a `gate="blocking-human"` checkpoint. After the named owner supplies the resume evidence, the executor reruns the exact preflight; a human assertion alone cannot satisfy or replace it.
</external_checkpoint_contracts>

<tasks>

<task type="tracer">
  <name>Task 1: Dispatch and collect every exact-SHA non-deferred workflow artifact</name>
  <precondition>The cloud/repository administrators' live and GitHub collection preflights above succeed for one clean candidate commit after Plan 08-15; on absence the executor stops at a blocking-human checkpoint and leaves the affected current evidence class UNAVAILABLE.</precondition>
  <files>build/phase8/evidence, build/phase8/live_qualification.json</files>
  <read_first>
    - tools/verify_phase8_release.py: current release workflow inventory, exact-SHA preflight, dispatch, run-ID wait/download, and class-specific envelope validation
    - .github/workflows/quality.yml: fixed deterministic/package/platform/coverage/structural artifact producers
    - .github/workflows/live_qualification.yml: protected live producer, fixed artifact name, and scheduled nonclaim
    - .planning/phases/08-production-gates-and-performance-stabilization/08-14-SUMMARY.md: implemented current-required/deferred evidence boundary
  </read_first>
  <action>Establish one clean candidate SHA after Plan 08-14, run the current collection preflight, and invoke the tested controller for that exact SHA. Dispatch only `quality.yml` and the release-candidate path of `live_qualification.yml`. For each dispatch, record the new run ID; require `gh run view` to report the expected workflow/event/headSha; wait on that exact ID with failure propagation; and download every fixed required artifact using both run ID and name. Validate each bounded envelope's schema, revision, relevant-source digest, terminal status, producer role, fixed selector/matrix inventory, and the live `QUALIFIED`/`CLEAN` cleanup result. Reject latest-run selection, scheduled live diagnostics, wrong or duplicate IDs, cancelled/failing runs, missing/extra artifacts, stale source, skips, remote-latency substitution, controlled-performance/macOS timing artifacts, and unsanitized output per D-10 through D-13 and D-23.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase8_release.py collect --candidate-sha "$(git rev-parse HEAD)" --output-dir build/phase8/evidence --live-output build/phase8/live_qualification.json</automated>
  </verify>
  <done>Every non-deferred class has a validated artifact from a recorded successful exact-candidate workflow run; live evidence is QUALIFIED/CLEAN, or execution stops with the affected class explicitly unqualified.</done>
</task>

<task type="auto">
  <name>Task 2: Validate and aggregate the collected exact-commit evidence</name>
  <precondition>Task 1's fixed artifact directory contains one validated envelope for every non-deferred class, all bound to the clean candidate SHA and one relevant-source digest.</precondition>
  <files>build/phase8/release_qualification.json</files>
  <read_first>
    - tools/verify_phase8_release.py: exact same-revision aggregation, deferral record, and sanitization
    - build/phase8/evidence: exact run-ID-collected deterministic, packaging, platform, coverage, structural, and live-service envelopes
    - build/phase8/live_qualification.json: exact run-ID-collected live service envelope
    - .planning/seeds/SEED-006-qualify-controlled-linux-performance.md: deferred requirement identity and macOS non-substitution
  </read_first>
  <action>Aggregate deterministic, packaging, platform, coverage, structural, and live evidence from the exact run-ID collection for the clean candidate commit. Require every non-deferred class to pass and share one revision/source digest; revalidate workflow/run/artifact identities and SHA-256 digests; require the live class to remain QUALIFIED/CLEAN; require the canonical aggregate's controlled-performance entry to be exactly `DEFERRED` and `NOT_QUALIFIED` for QUAL-06/SEED-006; scan the aggregate for secrets; and write `release_qualification.json` atomically. Reject a performance artifact or macOS timing diagnostic as an aggregate or publication input. Retain failed, unavailable, and scheduled diagnostics for 30 days only. Do not mutate a GitHub release or tracked source in this task, so D-10 exact-commit identity remains true.</action>
  <verify>
    <automated>uv run --isolated --group dev --frozen python tools/verify_phase8_release.py aggregate --candidate-sha "$(git rev-parse HEAD)" --evidence-dir build/phase8/evidence --live-evidence build/phase8/live_qualification.json --output build/phase8/release_qualification.json</automated>
  </verify>
  <done>The canonical sanitized manifest names one exact commit, every required collected run/artifact digest, and the exact QUAL-06 deferral, with no unavailable or diagnostic state represented as a current pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| protected secrets -> live services | Credentials authorize bounded qualification resources and must never enter evidence. |
| exact workflow runs -> collected evidence | Only fixed artifacts from recorded successful run IDs for the candidate SHA may enter aggregation. |
| performance diagnostics -> deferred nonclaim | The current release records deferral but admits no macOS or controlled-performance artifact as qualifying proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-11-01 | Spoofing | workflow run/revision | high | mitigate | Require exact workflow, event, candidate SHA, newly recorded run ID, terminal success, and fixed artifact name. |
| T-08-11-02 | Elevation of Privilege | PostgreSQL/AWS credentials | high | mitigate | Use protected environment and least-privilege database/schema plus bucket/prefix authorization. |
| T-08-11-03 | Tampering | exact release revision | high | mitigate | Require clean source and forbid tracked changes after evidence collection. |
| T-08-11-04 | Information Disclosure | live/release artifacts | high | mitigate | Enforce strict allow-list, recursive secret scan, sanitized assets, and bounded diagnostics retention. |
| T-08-11-05 | Tampering | external cleanup | high | mitigate | Require exact owner markers, bounded cleanup, and CLEAN terminal status before qualification. |
| T-08-11-06 | Repudiation | performance deferral and workflow collection | high | mitigate | Record exact workflow/run/artifact provenance plus the closed D-23/QUAL-06/SEED-006 nonclaim. |
</threat_model>

<verification>
- Live runner returns exit 0 with QUALIFIED/CLEAN exact-commit evidence; otherwise the plan stops without closing BACK-05.
- Collection dispatches only current required workflows and downloads only fixed artifacts from exact recorded run IDs.
- Aggregation passes only with every non-deferred same-SHA/source class and the exact QUAL-06 deferral record.
- No controlled-Linux runner, baseline capture, performance threshold, or macOS substitution is required or claimed.
</verification>

<acceptance_criteria>
- BACK-05 closes only from real PostgreSQL/Amazon-S3 proof of the exact final commit.
- QUAL-06 remains `DEFERRED`/`NOT_QUALIFIED` under SEED-006 and is not part of the current blocking aggregate.
- Every other required class is complete, exact-SHA, sanitized, and immutable-ready for Plan 08-12.
</acceptance_criteria>

<success_criteria>
- The current release aggregate is a strict conjunction of every non-deferred class.
- The aggregate preserves an explicit performance nonclaim without publishing diagnostic evidence.
- Release-lifetime evidence is sanitized and ready for separately authorized immutable publication.
</success_criteria>

<source_coverage>
- GOAL: Users can rely on reproducible current release evidence for the exact release commit.
- REQ: BACK-05, QUAL-01, QUAL-02, QUAL-03, QUAL-04, QUAL-05, QUAL-07.
- RESEARCH: Exact-SHA workflow dispatch/run-ID collection, protected live runner, strict aggregation, and explicit performance deferral.
- CONTEXT: D-03, D-09, D-10, D-11, D-12, D-13, D-22, D-23.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-11-SUMMARY.md` when done.
</output>
