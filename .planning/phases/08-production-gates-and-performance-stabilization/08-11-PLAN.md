---
phase: 08-production-gates-and-performance-stabilization
plan: 11
type: execute
wave: 7
depends_on:
  - 08-10
files_modified:
  - benchmarks/phase8_baseline.json
  - build/phase8/evidence/deterministic.json
  - build/phase8/evidence/packaging.json
  - build/phase8/evidence/platform.json
  - build/phase8/evidence/coverage.json
  - build/phase8/evidence/structural.json
  - build/phase8/evidence/controlled_performance.json
  - build/phase8/live_qualification.json
  - build/phase8/release_qualification.json
autonomous: true
requirements:
  - BACK-05
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-06
  - QUAL-07
user_setup:
  - service: controlled-linux-runner
    why: "Capture the release-blocking performance baseline on the fixed cacheness-perf-linux-x64 identity"
    dashboard_config:
      - task: "Provide a dedicated Linux x86-64 runner labeled cacheness-perf-linux-x64 with stable CPU policy and local filesystem"
        location: "GitHub repository or organization Actions runner settings"
  - service: postgresql-and-amazon-s3
    why: "Run the non-substitutable BACK-05 real-service suite"
    env_vars:
      - name: CACHENESS_TEST_POSTGRES_DSN
        source: "Protected release environment secret for the dedicated qualification database"
      - name: CACHENESS_TEST_S3_BUCKET
        source: "Protected release environment variable for the dedicated Amazon S3 bucket"
      - name: CACHENESS_TEST_MANIFEST_KEY_B64
        source: "Protected release environment secret containing the test manifest signing key"
      - name: CACHENESS_TEST_AWS_REGION
        source: "Protected release environment variable containing the bucket region"
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The checked performance baseline is captured on the named controlled Linux runner after all relevant source/tool/test/workflow changes and records reviewed distributions/envelopes (D-20)."
    - "The frozen real PostgreSQL/Amazon-S3 suite produces sanitized QUALIFIED/CLEAN evidence for the exact baseline-containing commit, or BACK-05 remains blocked (D-10, D-11, D-12)."
    - "The final fixed verifier and release aggregator pass against one exact commit; unavailable prerequisites remain explicit nonclaims (D-11)."
    - "Quality, packaging, platform, coverage, structural, controlled-performance, and live workflows are dispatched for the exact candidate SHA, waited by recorded run ID, and downloaded by run ID plus fixed artifact name before aggregation (D-10, D-11)."
    - "Collected packaging/platform evidence matches the post-Plan-08-13 inventories and cannot reintroduce a TensorFlow group, row, profile, compatibility state, or release claim (D-23)."
  artifacts:
    - path: "benchmarks/phase8_baseline.json"
      provides: "Reviewed controlled-Linux performance distributions and relative envelopes"
    - path: "build/phase8/live_qualification.json"
      provides: "Generated sanitized exact-commit real-service evidence"
    - path: "build/phase8/release_qualification.json"
      provides: "Generated canonical multi-class release manifest"
  key_links:
    - from: "benchmarks/phase8_baseline.json"
      to: "benchmarks/phase8_benchmarks.py"
      via: "same-runner non-mutating distribution comparison"
      pattern: "controlled_performance"
    - from: "build/phase8/live_qualification.json"
      to: "tools/verify_phase8_release.py"
      via: "QUALIFIED/CLEAN exact-revision live evidence input"
      pattern: "live_services"
    - from: ".github/workflows/quality.yml"
      to: "build/phase8/evidence"
      via: "exact-SHA dispatch, recorded run-ID wait, and fixed-name artifact download shared with performance/live workflows"
      pattern: "candidate_sha|run_id|artifact"
---

<objective>
Capture the non-substitutable external evidence, freeze the controlled baseline, and aggregate qualification for the exact release commit.

Purpose: Close BACK-05 and QUAL-06 only when the real services and controlled runner actually prove the final source, and ensure every required envelope is collected from an exact recorded workflow run before aggregation.
Output: Checked controlled baseline, fixed exact-SHA workflow artifacts, sanitized live evidence, and final same-commit release manifest.
</objective>

<scope_guard>
This plan performs qualification after all implementation plans. An unavailable runner/service/release prerequisite stops with a truthful blocking checkpoint; it never authorizes substitute evidence, a source edit, threshold weakening, or a topology/lifecycle change.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@docs/RELEASE_QUALIFICATION.md
@benchmarks/phase8_benchmarks.py
@tools/run_phase8_qualification.py
@tools/verify_phase8_contracts.py
@tools/verify_phase8_release.py
</context>

<external_checkpoint_contracts>

| Gate | Owner | Exact preflight | Resume evidence | Failure/absence contract |
|---|---|---|---|---|
| Controlled Linux runner | Repository/infrastructure maintainer | `benchmarks/phase8_benchmarks.py --preflight-runner --expect-label cacheness-perf-linux-x64 --revision CANDIDATE_SHA` proves Linux x86-64, stable fingerprint/noise, clean detached source, and exact SHA without writing a baseline | Sanitized successful preflight artifact naming the fixed label, machine fingerprint, and candidate SHA | Stop with QUAL-06 `UNAVAILABLE`; local/macOS evidence remains diagnostic |
| Protected PostgreSQL/Amazon-S3 environment | Cloud/repository administrator | `tools/run_phase8_qualification.py --preflight` proves the four named variables exist, standard AWS identity is used, no endpoint override is present, and bounded owner-marker cleanup is authorized without emitting values | Sanitized successful configuration-only preflight for the exact protected environment | Stop with BACK-05 `UNAVAILABLE`/`NOT_QUALIFIED`; no mock, emulator, earlier run, or substitute service |
| Exact release-candidate dispatch | Release operator | `tools/verify_phase8_release.py preflight-collection --candidate-sha CANDIDATE_SHA` proves `gh auth status`, workflow visibility/dispatch permission, trusted definitions, exact fixed artifact inventory, and all prerequisite local gates | Explicit approval of the 40-character candidate SHA; subsequent automation records exact workflow run IDs | Do not dispatch another/latest revision; retain only bounded sanitized diagnostics |

An unmet task precondition becomes a `gate="blocking-human"` checkpoint. After the named owner supplies the resume evidence, the executor reruns the exact preflight; a human assertion alone cannot satisfy or replace it.
</external_checkpoint_contracts>

<tasks>

<task type="auto">
  <name>Task 1: Capture and commit the controlled-Linux performance baseline</name>
  <precondition>The repository/infrastructure maintainer-owned controlled-runner preflight above has produced its sanitized exact-SHA success artifact; on absence the executor stops at a blocking-human checkpoint and leaves QUAL-06 UNAVAILABLE.</precondition>
  <files>benchmarks/phase8_baseline.json</files>
  <read_first>
    - benchmarks/phase8_benchmarks.py: capture/recalibrate/verify modes, runner identity, workload inventory, and stability rules
    - .github/workflows/performance.yml: controlled runner and artifact contract
    - .planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md: D-18 through D-22
  </read_first>
  <action>Run the canonical full workload suite on `cacheness-perf-linux-x64`, reject unstable measurements, and review raw p50/p95/p99 distributions, RSS, format/layer separations, and SHA-256/XXH3 data. Capture the initial checked baseline with an explicit justification and statistically defensible relative median/tail envelopes, then commit it before generating any exact-release evidence. Establish the resulting clean HEAD as the sole candidate SHA and re-run non-mutating verification on the committed baseline. Per D-19/D-20, do not change canonical digest semantics or copy envelope numbers into runtime deadlines.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python benchmarks/phase8_benchmarks.py --verify-baseline benchmarks/phase8_baseline.json</automated>
  </verify>
  <done>The baseline is a reviewed, stable, current-architecture distribution captured and verified on the named controlled Linux identity.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch, wait for, and download every exact-SHA workflow artifact</name>
  <precondition>The cloud/repository administrators' live and GitHub collection preflights above succeed for the clean commit containing `benchmarks/phase8_baseline.json`; on absence the executor stops at a blocking-human checkpoint and leaves the affected evidence class UNAVAILABLE.</precondition>
  <files>build/phase8/evidence/deterministic.json, build/phase8/evidence/packaging.json, build/phase8/evidence/platform.json, build/phase8/evidence/coverage.json, build/phase8/evidence/structural.json, build/phase8/evidence/controlled_performance.json, build/phase8/live_qualification.json</files>
  <read_first>
    - tools/verify_phase8_release.py: exact-SHA preflight, dispatch, run-ID wait/download, and class-specific envelope validation
    - .github/workflows/quality.yml: fixed deterministic/package/platform/coverage/structural artifact producers
    - .github/workflows/performance.yml: controlled-performance producer and fixed runner contract
    - .github/workflows/live_qualification.yml: protected live producer, fixed artifact name, and scheduled nonclaim
  </read_first>
  <action>Invoke the tested collection controller with the exact clean candidate SHA from Task 1. Dispatch `quality.yml`, `performance.yml`, and the release-candidate path of `live_qualification.yml` with that explicit SHA. For every dispatched workflow, record its resulting run ID; require `gh run view` to report the expected workflow/event/headSha; wait on that exact ID with failure propagation; and download each fixed artifact name using both the run ID and name. Validate each bounded envelope's schema, revision, relevant-source digest, terminal status, producer role, fixed selector/matrix inventory, runner identity where applicable, and live `QUALIFIED`/`CLEAN` cleanup. Reject latest-run selection, scheduled live diagnostics, wrong/duplicate IDs, cancelled/failing runs, missing/extra artifacts, stale source, skips, remote-latency substitution, and any unsanitized output per D-10 through D-13 and D-22.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase8_release.py collect --candidate-sha "$(git rev-parse HEAD)" --output-dir build/phase8/evidence --live-output build/phase8/live_qualification.json</automated>
  </verify>
  <done>Every required class has a validated artifact from a recorded successful exact-candidate workflow run; live evidence is QUALIFIED/CLEAN, or execution stops with the affected class explicitly unqualified.</done>
</task>

<task type="auto">
  <name>Task 3: Validate and aggregate the collected exact-commit evidence</name>
  <precondition>Task 2's fixed artifact directory contains one validated envelope for every required class, all bound to the clean candidate SHA and relevant-source digest.</precondition>
  <files>build/phase8/release_qualification.json</files>
  <read_first>
    - tools/verify_phase8_contracts.py: final fixed all-mode verifier and external-status reporting
    - tools/verify_phase8_release.py: exact same-revision aggregation and sanitization
    - build/phase8/evidence: exact run-ID-collected deterministic, packaging, platform, coverage, structural, and controlled-performance envelopes
    - build/phase8/live_qualification.json: exact run-ID-collected live service envelope
  </read_first>
  <action>Run the complete fixed verifier, then aggregate deterministic, packaging, platform, coverage, structural, controlled-performance, and live evidence from the exact run-ID collection for the clean candidate commit. Require all blocking classes to pass and share one revision/source digest; revalidate workflow/run/artifact identities and SHA-256 digests; scan the canonical aggregate for secrets; and write `release_qualification.json` atomically. Retain failed/unavailable/scheduled diagnostics for 30 days only. Do not create or mutate a GitHub release in this task; Plan 08-12 owns the separately authorized publication transition. Make no tracked source change after evidence generation, so D-10 exact-commit identity remains true.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen python tools/verify_phase8_contracts.py --all && uv run --isolated --group dev --frozen python tools/verify_phase8_release.py aggregate --candidate-sha "$(git rev-parse HEAD)" --evidence-dir build/phase8/evidence --live-evidence build/phase8/live_qualification.json --output build/phase8/release_qualification.json</automated>
  </verify>
  <done>The canonical sanitized manifest names one exact commit and the exact collected run/artifact digests for every qualifying class, with no unavailable or diagnostic state represented as a pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| controlled runner -> checked baseline | Machine identity/noise and reviewed distributions establish the only blocking timing source. |
| protected secrets -> live services | Credentials authorize bounded qualification resources and must never enter evidence. |
| exact workflow runs -> collected evidence | Only fixed artifacts from recorded successful run IDs for the candidate SHA may enter aggregation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-11-01 | Spoofing | controlled runner/baseline | high | mitigate | Assert fixed label and full machine fingerprint; reject environment mismatch or unstable samples. |
| T-08-11-02 | Elevation of Privilege | PostgreSQL/AWS credentials | high | mitigate | Protected environment and least-privilege database/schema plus bucket/prefix authorization. |
| T-08-11-03 | Tampering | exact release revision | high | mitigate | Commit baseline first, require clean source, resolve tag/SHA, and forbid tracked changes after evidence. |
| T-08-11-04 | Information Disclosure | live/release artifacts | high | mitigate | Strict allow-list, recursive secret scan, sanitized assets, and bounded diagnostics retention. |
| T-08-11-05 | Tampering | external cleanup | high | mitigate | Require exact owner markers, bounded cleanup, and CLEAN terminal status before qualification. |
| T-08-11-06 | Repudiation | workflow collection | high | mitigate | Record workflow/run IDs and artifact names/digests; wait and download only by exact run ID. |
</threat_model>

<verification>
- Controlled baseline verifies on the exact named runner after commit.
- Live runner returns exit 0 with QUALIFIED/CLEAN exact-commit evidence; otherwise the plan stops without closing BACK-05.
- Final fixed verifier and aggregator pass only after exact-SHA workflow dispatch, run-ID wait, fixed-name download, and envelope validation.
</verification>

<success_criteria>
- BACK-05 closes only from real PostgreSQL/Amazon-S3 proof of the exact final commit.
- QUAL-06 closes only from reviewed controlled-Linux distributions and envelopes.
- Release-lifetime evidence is sanitized and immutable-ready for Plan 08-12; diagnostic states remain bounded nonclaims.
</success_criteria>

<source_coverage>
- GOAL: Users can rely on reproducible release evidence for the exact release commit.
- REQ: BACK-05, QUAL-03, QUAL-06.
- RESEARCH: External prerequisite contract, controlled baseline capture, exact-SHA workflow dispatch/run-ID collection, live runner, and immutable-ready aggregation.
- CONTEXT: D-09, D-10, D-11, D-12, D-13, D-18, D-19, D-20, D-22, D-23.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-11-SUMMARY.md` when done.
</output>
