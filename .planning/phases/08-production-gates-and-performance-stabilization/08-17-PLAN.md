---
phase: 08-production-gates-and-performance-stabilization
plan: 17
type: execute
wave: 9
depends_on:
  - 08-15
files_modified:
  - tests/test_blob_store_concurrency.py
autonomous: true
gap_closure: true
requirements:
  - QUAL-04
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A delete racing a clear after its exact snapshot may either complete or return CacheBlobLifecycleConflictError; neither valid progress outcome is mislabeled as corruption (D-03, D-04)."
    - "Every accepted outcome still converges to final absence, no authority entry, no metadata/payload disagreement, and no cleanup debt (D-02, D-03)."
    - "The regression proves bounded worker completion and fails on every unexpected clear/delete exception without imposing a scheduling or all-contenders-succeed guarantee (D-03)."
    - "No lifecycle, concurrency, selector, gate, dependency, workflow, benchmark, packaging, or ADR implementation changes are made."
  artifacts:
    - path: tests/test_blob_store_concurrency.py
      provides: "Correct exact-snapshot clear/delete progress contract plus bounded repeated safety regression"
  key_links:
    - from: tests/test_blob_store_concurrency.py
      to: docs/adr/0001-topology-specific-storage-guarantees.md
      via: "separate safety/recovery assertions from the topology's documented typed progress outcome"
      pattern: "CacheBlobLifecycleConflictError|pending_cleanup_debts"
    - from: tests/test_blob_store_concurrency.py
      to: tests/test_phase3_gap_acceptance.py
      via: "retain the existing CR-01 test node name while correcting its assertions"
      pattern: "test_clear_and_delete_converge_after_an_exact_snapshot"
---

<objective>
Correct the inherited clear/delete concurrency test so it enforces ADR 0001's safety and bounded-progress contract instead of requiring every contender to succeed.

Purpose: Unblock Plan 08-16's exact local coverage run without patching safe lifecycle behavior or starting another race-fix loop.
Output: One deterministic exact-snapshot contention regression and one bounded repeated safety regression in the existing concurrency test module.
</objective>

<scope_guard>
This plan changes tests only. It must not edit `src/cacheness`, lifecycle or concurrency implementation, test selectors, qualification gates, package/runtime dependencies, workflows, benchmark baselines, `pyproject.toml`, `uv.lock`, or ADR 0001. It must not require a particular winner, require the typed conflict to occur in every run, retry a rejected operation, add sleeps as an ordering oracle, or weaken any final safety/recovery assertion.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0001-topology-specific-storage-guarantees.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-CONTEXT.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-VALIDATION.md
@.planning/phases/08-production-gates-and-performance-stabilization/08-16-PLAN.md
@tests/test_blob_store_concurrency.py
@tests/test_phase3_gap_acceptance.py
</context>

<interfaces>
Preserve these existing contracts:

- `tests/test_blob_store_concurrency.py::_store(root)` creates the supported SQLite-authority/filesystem-payload local topology.
- `tests/test_blob_store_concurrency.py::_join(thread)` supplies the bounded worker-completion assertion.
- `tests/_lifecycle_test_support.py::ReleaseGate` orders the exact `clear.snapshot_committed` boundary without sleeps.
- `tests/test_phase3_gap_acceptance.py::REQUIRED_GAP_NODES["CR-01"]` requires the exact existing test node `test_clear_and_delete_converge_after_an_exact_snapshot`; do not rename it.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Correct the exact-snapshot clear/delete outcome contract</name>
  <files>tests/test_blob_store_concurrency.py</files>
  <behavior>
    - "After clear commits and pauses at its exact snapshot, clear and delete contend at the existing tombstone-promotion test seam without sleeps."
    - "The public delete outcome is either its ordinary boolean completion or exactly one CacheBlobLifecycleConflictError; clear completes with its documented zero-or-one removal count."
    - "Any other delete exception, any clear exception, an unfinished worker, or more than one terminal outcome fails the test."
    - "For both allowed progress outcomes, get returns absence, authority has no entry for the key, and pending cleanup debt is empty."
  </behavior>
  <action>Refactor `test_clear_and_delete_converge_after_an_exact_snapshot` around a small test-only helper that performs one exact ordered collision and returns or asserts the complete outcome record. Preserve the public CR-01 test name. Use `ReleaseGate` at `clear.snapshot_committed`, then the existing lifecycle fault hook at `delete.before_tombstone_promotion` (the legacy alias is `tombstone_publish`) with a finite `threading.Barrier` so both clear's exact-target delete and the concurrent public delete prepare against the same observed generation before either promotion proceeds. Run clear and public delete in separately named workers, release the snapshot gate explicitly, and join both through the bounded `_join` helper.

Record clear results/errors and delete results/errors separately. Require clear to return exactly one value in `{0, 1}` with no exception. Require public delete to produce exactly one terminal outcome: an ordinary boolean result or one `CacheBlobLifecycleConflictError`. Reject all other exception types and cardinalities explicitly; do not broadly discard worker exceptions. Regardless of which contender wins, assert public absence, absence of the authority entry, and an empty cleanup-debt tuple before closing the store. Retain cleanup releases in `finally` so an assertion cannot strand a worker or database handle. Per D-03/D-04, do not assert which contender wins, do not retry the conflict, and do not change storage code to force success.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_blob_store_concurrency.py::test_clear_and_delete_converge_after_an_exact_snapshot tests/test_phase3_gap_acceptance.py::test_phase3_gap_acceptance_inventory -x</automated>
  </verify>
  <done>The inherited CR-01 node deterministically exercises exact-snapshot tombstone contention, accepts only the documented success/conflict progress outcomes, and requires the complete absence/debt-free safety state after bounded worker completion.</done>
</task>

<task type="auto">
  <name>Task 2: Add a bounded repeated safety regression across valid outcomes</name>
  <files>tests/test_blob_store_concurrency.py</files>
  <behavior>
    - "Multiple fresh-store exact-snapshot collisions reuse the same safety/outcome oracle."
    - "Every iteration is event/barrier ordered and bounded; aggregate wall-clock performance is not asserted."
    - "The regression does not require observing both scheduling outcomes, but any unsafe state or unexpected exception fails immediately."
  </behavior>
  <action>Add a clearly named stress/regression test that invokes the Task 1 helper for 16 isolated roots. Reuse the exact snapshot gate, tombstone-promotion barrier, bounded joins, terminal-outcome validation, and postcondition assertions rather than copying a second race harness. Keep the iteration count literal and small enough for the focused command to complete comfortably within the normal test budget. Do not use sleep, statistical pass rates, elapsed-time thresholds, randomized retries, or an assertion that at least one conflict must occur: those would turn scheduler behavior into a correctness requirement. The repeated form exists to amplify detection of corruption, lingering authority state, leaked cleanup debt, deadlock, and swallowed unexpected exceptions while accepting either ADR-valid progress result on each iteration.</action>
  <verify>
    <automated>uv run --isolated --all-extras --group dev --frozen pytest -q -o log_cli=false tests/test_blob_store_concurrency.py -k 'clear_and_delete' -x &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff check tests/test_blob_store_concurrency.py &amp;&amp; uv run --isolated --all-extras --group dev --frozen ruff format --check tests/test_blob_store_concurrency.py</automated>
  </verify>
  <done>Sixteen bounded exact-snapshot collisions all finish in an allowed progress outcome and prove final absence, empty authority state, and no cleanup debt; unexpected exceptions and unsafe states remain hard failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| worker outcome -> test verdict | Concurrent clear/delete results must be classified without swallowing an unexpected exception. |
| progress outcome -> safety verdict | A documented typed contention rejection is valid progress but never substitutes for final integrity/recovery assertions. |
| test timing seam -> product contract | Events and barriers may order the test but must not become production coordination or a scheduling guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-17-01 | Tampering | delete outcome classification | high | mitigate | Admit only a boolean result or `CacheBlobLifecycleConflictError`; fail every other exception and terminal-outcome shape. |
| T-08-17-02 | Repudiation | worker error capture | medium | mitigate | Keep clear/delete result and error channels separate, require exact cardinalities, and surface captured unexpected exceptions in assertions. |
| T-08-17-03 | Elevation of Privilege | progress-to-safety boundary | high | mitigate | Run absence, authority-entry, and cleanup-debt assertions after every allowed outcome; a conflict never bypasses safety checks. |
| T-08-17-04 | Denial of Service | contention regression | medium | mitigate | Use finite event/barrier waits, bounded joins, a literal 16-case loop, no sleeps, and no unbounded retry. |
| T-08-17-05 | Tampering | lifecycle architecture | high | mitigate | Limit the diff to the existing test module and reject any production coordination, selector, gate, package/runtime dependency, workflow, or baseline change. |
</threat_model>

<verification>
- The original CR-01 node remains collected under its exact name and passes for either documented public-delete progress outcome.
- The bounded repeated regression exercises 16 fresh exact-snapshot collisions without timing assertions or retries.
- Every run asserts final public absence, no authority entry, empty cleanup debt, and bounded thread completion.
- Only `tests/test_blob_store_concurrency.py` changes during execution; Phase 08-16 then resumes against the corrected inherited contract.
</verification>

<acceptance_criteria>
- The formerly flaky test no longer treats `CacheBlobLifecycleConflictError` as corruption or requires every concurrent contender to succeed.
- Ordinary deletion success and typed contention rejection remain distinguishable, and unexpected exceptions are never accepted.
- Safety and recovery checks are identical for both valid progress outcomes.
- No lifecycle/concurrency implementation or qualification machinery is changed.
</acceptance_criteria>

<source_coverage>
- GOAL: Restore reproducible local-readiness execution by correcting one inherited assertion contract, not by changing safe lifecycle behavior.
- REQ: QUAL-04 finite integrity/recovery/commit-boundary regressions with typed contention outcomes distinguished from corruption.
- RESEARCH: The confirmed 30-run reproduction produced 22 passes and 8 typed conflicts; the minimized 50-run harness produced 42 ordinary successes, 8 typed conflicts, and zero unsafe states.
- CONTEXT: D-02 single lifecycle authority, D-03 separate integrity/recovery/progress/performance claims, and D-04 no silent strengthening of accepted topology limits.
</source_coverage>

<success_criteria>
- The exact-snapshot clear/delete test expresses ADR 0001's declared topology contract.
- Repeated event-ordered contention detects unsafe state while remaining agnostic about scheduler winner and conflict frequency.
- Plan 08-16 can rerun its unchanged local-readiness gates without this false all-contenders-succeed requirement.
</success_criteria>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-17-SUMMARY.md` when done.
</output>
