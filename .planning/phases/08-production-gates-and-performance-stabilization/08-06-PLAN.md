---
phase: 08-production-gates-and-performance-stabilization
plan: 06
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - tools/run_phase8_scale_gates.py
  - tests/performance/test_complexity_contracts.py
  - tests/performance/test_memory_bounds.py
autonomous: true
requirements:
  - QUAL-03
  - QUAL-07
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Inventory, reconciliation, statistics, clear, invalidation, and cache maintenance have explicit call-count/work formulas independent of wall-clock speed (D-21)."
    - "Fixed scale tiers prove page/work bounds and detect accidental N+1 authority or participant calls (D-21)."
    - "Peak-memory evidence is collected in isolated subprocesses and compared structurally by workload size; timing remains a different evidence class (D-21, D-22)."
  artifacts:
    - path: "tools/run_phase8_scale_gates.py"
      provides: "Canonical structural call/memory evidence runner"
    - path: "tests/performance/test_complexity_contracts.py"
      provides: "Per-operation authority and participant call formulas"
    - path: "tests/performance/test_memory_bounds.py"
      provides: "Isolated fixed-tier peak-memory contracts"
  key_links:
    - from: "tests/performance/test_complexity_contracts.py"
      to: "src/cacheness/storage/lifecycle.py"
      via: "instrumented authority and participant protocols with exact per-class counters"
      pattern: "operation_page_size|max_reconcile_actions"
    - from: "tools/run_phase8_scale_gates.py"
      to: "tools/phase8_evidence.py"
      via: "structural evidence containing formulas, tiers, counts, and memory observations"
      pattern: "phase8_evidence"
---

<objective>
Turn bounded inventory and aggregate behavior into exact structural call and memory gates.

Purpose: Close QUAL-07 without using machine timing to stand in for complexity or memory correctness.
Output: Fixed formula tests, isolated memory tests, and a structural evidence runner.
</objective>

<scope_guard>
Instrumentation belongs in tests and wrappers around existing public/protocol seams. This plan cannot optimize production code, add caching/coordination, or strengthen ADR progress guarantees in response to a scale result.
</scope_guard>

<execution_context>
@/Users/akriz/.codex/gsd-core/workflows/execute-plan.md
@/Users/akriz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-gates-and-performance-stabilization/08-RESEARCH.md
@src/cacheness/storage/blob_store.py
@src/cacheness/storage/lifecycle.py
@src/cacheness/storage/reconciliation.py
@src/cacheness/core.py
@src/cacheness/cache_policy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode per-operation authority and participant call formulas</name>
  <files>tests/performance/test_complexity_contracts.py, tools/run_phase8_scale_gates.py</files>
  <read_first>
    - tests/test_catalog_query_contract.py: access-spy and zero-call invalid-input patterns
    - tests/test_blob_store_reconciliation.py: bounded rows/actions/bytes/continuation fixtures
    - tests/test_phase6_statistics.py: O(1) zero-storage observer assertions
    - tests/test_phase6_removal_contract.py: exact delete and cursor restart assertions
  </read_first>
  <behavior>
    - "Catalog query performs one authority page per public page, examines at most work_cap, returns at most limit, and performs no payload calls."
    - "Reconciliation performs one bounded authority work page plus at most one bounded participant inventory page, with independent read/write/head/open/delete/list counters."
    - "Statistics performs zero storage calls; invalidation/clear/maintenance use one page plus at most one exact delete per selected candidate/step."
    - "The same formulas hold at 10, 100, 1,000, and 10,000 seeded entries with page sizes below and above defaults."
  </behavior>
  <action>Build protocol-level counting wrappers that preserve participant/authority behavior while recording authority reads, authority writes, participant head/open/delete, and inventory list separately. Encode the D-21 formulas for catalog, reconciliation, statistics, UnifiedCache invalidation/clear, BlobStore clear, and cache-size maintenance. Exercise fixed scale tiers and signed continuation behavior; seed outside the measured invocation and fail on any extra call class rather than accepting an aggregate total that hides N+1 access.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/performance/test_complexity_contracts.py -x</automated>
  </verify>
  <done>Every named inventory/aggregate operation has an exact scale-invariant call formula and independent backend-call counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove bounded peak-memory behavior separately from timing</name>
  <files>tests/performance/test_memory_bounds.py, tools/run_phase8_scale_gates.py</files>
  <read_first>
    - .codex/skills/spike-findings-cacheness/references/backend-mechanics.md: MemoryStore/S3 materialization observations and direct-upload bound
    - src/cacheness/config.py: page, action, record-byte, maintenance, and upload bounds
    - src/cacheness/storage/obstore_generation_io.py: bounded staging/snapshot and inventory behavior
  </read_first>
  <behavior>
    - "Each operation runs in a fresh child process and records normalized Linux peak RSS plus workload cardinality/bytes."
    - "Inventory and aggregate peak memory tracks configured page/work limits rather than total store cardinality."
    - "The documented direct-upload cap remains a resource boundary and no test implies bounded-memory S3 streaming beyond the shipped contract."
  </behavior>
  <action>Add isolated subprocess probes for inventory, reconciliation, clear, statistics, and cache maintenance at fixed cardinalities. On Linux, read `resource.getrusage` and `/proc` facts with explicit units; ordinary self-tests use small fixtures and fake samples to validate formulas. Store raw observations and ratios, reject missing child results, and keep timing out of the structural envelope. Per D-21/D-22 and the verified obstore spike, preserve the existing upload cap and MemoryStore/S3 materialization limitations rather than adding multipart or a second payload path.</action>
  <verify>
    <automated>uv run pytest -q -o log_cli=false tests/performance/test_memory_bounds.py tests/performance/test_complexity_contracts.py -x</automated>
  </verify>
  <done>Structural evidence detects unbounded memory and N+1 behavior independently of machine speed and without changing runtime semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| counting wrapper -> production protocol | Instrumentation must preserve the exact calls/results it measures. |
| child process -> memory evidence | Missing, crashed, or unit-mismatched RSS output must not pass. |
| scale observation -> lifecycle design | A regression must not authorize a new coordination mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-06-01 | Tampering | backend call counters | high | mitigate | Count each authority/participant method independently and assert wrapper transparency. |
| T-08-06-02 | Spoofing | bounded-work claim | high | mitigate | Exercise fixed cardinalities/page sizes and compare observed counts to explicit formulas. |
| T-08-06-03 | Denial of Service | scale/memory probes | medium | mitigate | Use configured caps, isolated children, timeouts, and bounded fixtures. |
| T-08-06-04 | Repudiation | memory units/environment | medium | mitigate | Record OS, units, workload bytes/cardinality, raw RSS, and child exit status. |
</threat_model>

<verification>
- Formula tests pass at every fixed scale tier and page-size regime.
- Memory self-tests reject missing, crashed, unit-mismatched, and cardinality-proportional results.
- Existing direct-upload and topology limitations remain unchanged and explicit.
</verification>

<success_criteria>
- QUAL-07 has deterministic structural proof for all named operations.
- Backend-call classes and peak memory are independently observable.
- Structural gates do not depend on remote latency or machine-speed thresholds.
</success_criteria>

<source_coverage>
- GOAL: Release evidence covers operational scale and bounded resource use.
- REQ: QUAL-03 and QUAL-07.
- RESEARCH: Exact per-operation formulas, scale tiers, isolated RSS, and separate call classes.
- CONTEXT: D-03, D-04, D-21, D-22.
</source_coverage>

<output>
Create `.planning/phases/08-production-gates-and-performance-stabilization/08-06-SUMMARY.md` when done.
</output>
